---
phase: 22-expo-sdk-54-57-bump
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/__tests__/screens/ProfileHandoff.test.tsx
  - apps/mobile/__tests__/screens/GuestRequestsList.test.tsx
  - apps/mobile/android/
  - apps/mobile/app.json
autonomous: true

must_haves:
  truths:
    - "npx jest in apps/mobile finishes with a defined, green baseline (0 failing) OR an explicitly-quarantined known-red set recorded in the SUMMARY"
    - "The committed apps/mobile/android/ directory no longer exists in git (New-Arch divergence source removed)"
    - "app.json remains the single source of truth for newArchEnabled (true) with no conflicting committed gradle.properties"
  artifacts:
    - path: "apps/mobile/app.json"
      provides: "New Architecture config source of truth (newArchEnabled: true)"
      contains: "newArchEnabled"
  key_links:
    - from: "apps/mobile jest run"
      to: "green/known baseline"
      via: "fix or test.skip of the 3 pre-existing failures"
      pattern: "test\\.skip|expect"
---

<objective>
Establish the preconditions that make the three SDK hops measurable and clean:
(1) a defined green jest baseline so the per-hop `npx jest` gate can distinguish upgrade breakage from pre-existing failures, and
(2) MOBILE-02 New-Architecture reconciliation by deleting the stale, EAS-ignored committed `android/` directory so `app.json` (newArchEnabled: true) is the sole source of truth before hop 1.

This is NOT a hop — no EAS build runs in this plan. It only sets the baseline.

Purpose: The jest baseline is currently RED (3 tests failing on SDK 54, unrelated to the upgrade). Without fixing/quarantining these, "each hop passes jest" is undefined. The committed `android/gradle.properties` (newArchEnabled=false) diverges from `app.json` (true); deleting `android/` (a CNG project regenerates it) permanently removes the divergence.
Output: Green (or explicitly-quarantined) jest baseline + `android/` removed from git.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-expo-sdk-54-57-bump/22-RESEARCH.md
@apps/mobile/__tests__/screens/ProfileHandoff.test.tsx
@apps/mobile/__tests__/screens/GuestRequestsList.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish a green (or explicitly-quarantined) jest baseline</name>
  <files>apps/mobile/__tests__/screens/ProfileHandoff.test.tsx, apps/mobile/__tests__/screens/GuestRequestsList.test.tsx</files>
  <action>
From `apps/mobile/`, run `npx jest` to confirm the current failing set (research: 3 tests across 2 suites — `ProfileHandoff.test.tsx` ×1, `GuestRequestsList.test.tsx` ×2; 406/409 passing; `npx tsc --noEmit` is already clean).

For EACH of the 3 failures, root-cause and FIX it (the research notes a likely stale selector such as `getByRole("button", { name: "Room 214" })` that no longer matches the rendered component). Read the failing test AND the component it exercises; update the test's selector/assertion to match current, correct behavior. Do NOT change app/source behavior to satisfy a stale test — fix the test to assert what the component correctly does today.

FALLBACK (only if, during investigation, a failure's root cause is genuinely unrelated to this phase and disproportionate to fix here): quarantine that single test with `test.skip(...)` plus an inline comment naming the pre-existing issue (e.g. `// TODO(phase-22): pre-existing failure, selector drift, tracked separately`). Do not blind-skip without first attempting a root-cause.

Record in the SUMMARY exactly which tests were fixed vs quarantined, and the resulting baseline (e.g. "409/409 green" or "407 green, 2 quarantined: <names>").
  </action>
  <verify>From apps/mobile/: `npx jest` exits 0 with 0 unexpected failures (quarantined tests show as skipped, not failed). `npx tsc --noEmit` still exits 0.</verify>
  <done>jest has a defined baseline: either fully green, or green-with-N-explicitly-skipped where N and the reasons are documented in the SUMMARY. This baseline is the reference the three hop plans compare against.</done>
</task>

<task type="auto">
  <name>Task 2: MOBILE-02 — delete stale committed android/ directory, confirm app.json is New-Arch source of truth</name>
  <files>apps/mobile/android/, apps/mobile/app.json</files>
  <action>
This is a Continuous-Native-Generation (CNG) project: `.easignore` excludes `android/`, so EAS cloud builds already prebuild fresh from `app.json` — the committed `android/gradle.properties` (`newArchEnabled=false`) only affects local `expo run:android` and is the source of the New-Arch config divergence (50 git-tracked files, stale June build artifacts).

Per locked decision: DELETE it. From `apps/mobile/`, run `git rm -r android` to remove the tracked directory. It is fully revertible from git history if ever needed for a local Gradle workflow.

Then confirm `apps/mobile/app.json` still sets `newArchEnabled: true` (this remains valid until SDK 55 removes the field in a later plan). Do NOT edit app.json in this task beyond confirming the value — it is the source of truth. Do NOT run a prebuild or an EAS build here.

Leave the `babel-plugin-dynamic-import-node` plugin in `babel.config.js` untouched (it guards a Hermes/Supabase dynamic-import crash and must survive all hops).
  </action>
  <verify>`git ls-files apps/mobile/android/ | wc -l` returns 0. `grep newArchEnabled apps/mobile/app.json` shows `true`. `apps/mobile/babel.config.js` still lists `dynamic-import-node`.</verify>
  <done>android/ is gone from git tracking; app.json newArchEnabled:true is the sole committed New-Arch config; babel plugin intact.</done>
</task>

</tasks>

<verification>
- `npx jest` (from apps/mobile/) exits 0 with a documented baseline (green or green+quarantined).
- `npx tsc --noEmit` exits 0.
- `git ls-files apps/mobile/android/` returns nothing.
- app.json newArchEnabled:true confirmed; babel dynamic-import-node plugin present.
</verification>

<success_criteria>
A defined green/quarantined jest baseline exists AND the New-Arch config divergence is eliminated (android/ deleted, app.json authoritative) — both prerequisites satisfied before hop 1 begins.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-01-SUMMARY.md` recording: the exact jest baseline (which tests fixed vs quarantined and why), confirmation android/ was removed, and that app.json/babel remain the New-Arch and Hermes-workaround sources of truth.
</output>
