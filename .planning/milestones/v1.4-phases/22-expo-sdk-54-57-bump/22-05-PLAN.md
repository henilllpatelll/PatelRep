---
phase: 22-expo-sdk-54-57-bump
plan: 05
type: execute
wave: 5
depends_on: ["22-04"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
  - .planning/phases/22-expo-sdk-54-57-bump/22-AUDIT-ACCEPTED-RISK.md
autonomous: true

must_haves:
  truths:
    - "The ~20 (roadmap's '19', drifted) npm audit advisories in apps/mobile are each either RESOLVED (cleared by the SDK 57 toolchain and/or refreshed overrides) or EXPLICITLY documented as accepted risk with rationale"
    - "The stale `tar: ^6.2.1` override (and any still-failing brace-expansion/uuid) is updated to a patched version, WITHOUT running `npm audit fix --force`"
    - "No dependency-graph regression: expo-doctor still passes and expo remains 57.0.9 after override changes"
  artifacts:
    - path: ".planning/phases/22-expo-sdk-54-57-bump/22-AUDIT-ACCEPTED-RISK.md"
      provides: "documented resolution/acceptance of each residual advisory"
      min_lines: 10
    - path: "apps/mobile/package.json"
      provides: "refreshed overrides block (patched tar/brace-expansion/uuid as needed)"
      contains: "overrides"
  key_links:
    - from: "package.json overrides"
      to: "patched transitive versions"
      via: "npm overrides + npm install"
      pattern: "overrides"
---

<objective>
MOBILE-04: resolve or explicitly accept the npm audit advisories in `apps/mobile`. The roadmap tracked 19; research found the count drifted to ~20 (16 moderate, 3 high, 1 critical), all resolving to THREE build/dev-time toolchain root packages — `tar`, `brace-expansion`, `uuid` — none shipped in the runtime APK.

Runs AFTER the SDK 57 hop is green (Plan 04), because the SDK 57 `--fix` pulls newer `@expo/*` that pin patched versions and should clear most advisories on its own. Do NOT run `npm audit fix --force` (it pulls deps outside the SDK's supported matrix and breaks the build).

Purpose: Close the last requirement of the phase with a defensible resolved-or-accepted posture for each advisory.
Output: refreshed overrides + an accepted-risk document; clean or explicitly-documented `npm audit`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-expo-sdk-54-57-bump/22-RESEARCH.md
@.planning/phases/22-expo-sdk-54-57-bump/22-04-SUMMARY.md
@apps/mobile/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-audit after SDK 57, refresh stale overrides (no --force)</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
All commands from `apps/mobile/`.

1. `npm audit` — capture the post-SDK-57 advisory set (count, severities, root packages). Many/all should already be cleared by the SDK 57 toolchain's newer `@expo/*`.
2. The `overrides` block in package.json currently pins `"tar": "^6.2.1"` — this is STALE (new `tar` GHSAs affect the `<7` range). For each still-failing root package, update the override to a patched version:
   - `tar` → the patched major that clears the DoS / path-traversal / infinite-loop / PAX GHSAs (per `npm audit` recommendation).
   - `brace-expansion` → patched version if still flagged.
   - `uuid` → patched version if still flagged.
   Run `npm install` after editing overrides.
3. DO NOT run `npm audit fix --force`. Use `overrides` + the SDK bump only.
4. Regression guard: after override changes, run `npx expo-doctor@latest` (must still pass) and confirm `npx expo --version` is still `57.0.9`. Optionally re-run `npx tsc --noEmit` / `npx jest` if overrides touched anything runtime-adjacent (they should not — these are build/dev-time deps).
  </action>
  <verify>`npm audit` shows the advisory count reduced to zero OR only residual advisories that cannot be cleared without breaking the SDK matrix. `npx expo-doctor@latest` passes. `npx expo --version` = 57.0.9. No `npm audit fix --force` was run.</verify>
  <done>Overrides refreshed to patched versions; audit count minimized; no dependency-graph regression (expo-doctor green, expo still 57.0.9).</done>
</task>

<task type="auto">
  <name>Task 2: Document resolved-or-accepted posture for each advisory</name>
  <files>.planning/phases/22-expo-sdk-54-57-bump/22-AUDIT-ACCEPTED-RISK.md</files>
  <action>
Create `22-AUDIT-ACCEPTED-RISK.md` recording MOBILE-04 closure:

1. The BEFORE count (research: ~20; roadmap: 19) and the AFTER `npm audit` count post-fix.
2. A table of each RESOLVED advisory (root package, how cleared — SDK 57 toolchain vs override bump).
3. For each RESIDUAL advisory that cannot be cleared without breaking the SDK's supported matrix, an ACCEPTED-RISK entry with: root package, severity, advisory id, and the rationale — **build/dev-time-only dependency, not present in the shipped APK runtime, no untrusted archives processed in CI** (tar/brace-expansion/uuid all live in `@expo/*` prebuild/config toolchain, not the app runtime).
4. State explicitly that `npm audit fix --force` was deliberately NOT used and why (pulls deps outside SDK matrix).

MOBILE-04 permits "resolved OR explicitly re-documented as accepted risk" — every advisory must fall into exactly one bucket here.
  </action>
  <verify>`22-AUDIT-ACCEPTED-RISK.md` exists and accounts for every advisory in the post-fix `npm audit` output as either resolved or accepted-with-rationale. Before/after counts recorded.</verify>
  <done>Every npm audit advisory is documented as resolved or explicitly accepted risk; MOBILE-04 satisfied.</done>
</task>

</tasks>

<verification>
- Post-fix `npm audit` count recorded; every advisory resolved or documented as accepted risk.
- Overrides refreshed (patched tar/brace-expansion/uuid) without `npm audit fix --force`.
- expo-doctor passes; expo still 57.0.9 (no regression from override changes).
- 22-AUDIT-ACCEPTED-RISK.md exists with before/after counts and per-advisory disposition.
</verification>

<success_criteria>
The ~20 npm audit advisories are each resolved or explicitly accepted with rationale (MOBILE-04); the dependency graph remains healthy on SDK 57.0.9.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-05-SUMMARY.md` recording: before/after audit counts, which advisories were resolved vs accepted, the override versions applied, and confirmation expo-doctor still passes on 57.0.9. Reference 22-AUDIT-ACCEPTED-RISK.md.
</output>
