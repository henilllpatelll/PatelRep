---
phase: 22-expo-sdk-54-57-bump
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
  - apps/mobile/app.json
autonomous: true

must_haves:
  truths:
    - "apps/mobile resolves expo to a 55.x version (target 55.0.28) with all expo-* peers aligned by --fix"
    - "New Architecture is enabled with no config divergence (SDK 55 removes newArchEnabled from app.json; New Arch is mandatory)"
    - "The 54→55 hop passes expo-doctor, tsc --noEmit, and jest (no new failures vs the Plan 01 baseline), then produces a GREEN EAS Android cloud build, committed as its own boundary"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "expo pinned to SDK 55 line"
      contains: "55.0"
  key_links:
    - from: "npx expo install expo@^55.0.0 --fix"
      to: "aligned expo-* peer graph"
      via: "expo install --fix peer resolution"
      pattern: "expo.*55"
---

<objective>
Execute hop 1 of 3: upgrade `apps/mobile` from Expo SDK 54 to SDK 55 (target `55.0.28`, RN 0.83), then pass all four gates in order and land a green EAS Android cloud build as an isolated commit before hop 2 begins.

SDK 55 makes the New Architecture MANDATORY and REMOVES `newArchEnabled` from `app.json` — MOBILE-02 self-resolves here (Plan 01 already deleted the divergent `android/`).

Purpose: One SDK major at a time; each hop is its own revertible commit boundary so a later failure rolls back to the last-good SDK.
Output: apps/mobile on expo 55.x, green EAS Android build, committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-expo-sdk-54-57-bump/22-RESEARCH.md
@.planning/phases/22-expo-sdk-54-57-bump/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade to SDK 55, reconcile New-Arch field removal, pass local gates</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json, apps/mobile/app.json</files>
  <action>
All commands from `apps/mobile/`.

1. `npx expo install expo@^55.0.0 --fix` — this bumps `expo` to the latest SDK 55 (target `55.0.28`) and aligns every `expo-*` / peer dependency (react-native → 0.83, etc.) to the SDK-55 matrix. Do NOT hand-edit versions; let `--fix` own the peer graph.
2. New-Arch reconciliation: SDK 55 removes the `newArchEnabled` field from `app.json` and makes New Arch mandatory. If `expo-doctor` or the upgrade flags `newArchEnabled` as no-longer-valid, remove it from `app.json`. New Arch is now on by default — no gradle config needed (android/ was deleted in Plan 01).
3. Deprecation check: grep for `<StatusBar` `backgroundColor`/`translucent` props (Plan 01 research confirmed NONE exist — re-confirm after --fix in case anything shifted) and confirm `expo-av` is not used (it is not). Reanimated is not a dependency, so the SDK 56 Reanimated migration does not apply.
4. Keep the `babel-plugin-dynamic-import-node` plugin in `babel.config.js` (Hermes/Supabase dynamic-import guard). If `--fix` bumped `@supabase/supabase-js`, re-confirm the plugin is still applied.
5. Local gates IN THIS ORDER:
   - `npx expo-doctor@latest` — resolve anything it flags with `npx expo install <pkg> --fix`.
   - `npx tsc --noEmit` — expect exit 0.
   - `npx jest` — expect NO new failures vs the Plan 01 baseline (quarantined set stays quarantined; nothing new red).
  </action>
  <verify>`npx expo --version` (or package.json) shows 55.x. `npx expo-doctor@latest` passes. `npx tsc --noEmit` exits 0. `npx jest` shows no new failures vs Plan 01 baseline.</verify>
  <done>expo resolves to 55.x, peer graph aligned, all three local gates green relative to baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Green EAS Android cloud build gate + commit the hop</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json, apps/mobile/app.json</files>
  <action>
Only after Task 1's three local gates are green.

1. From `apps/mobile/`, run the FINAL gate — the real signal:
   `eas build --profile preview --platform android --non-interactive`
   eas-cli is already authenticated as `henilllpatelll` (project `0fc41a33-8e3c-40cf-9114-26dc0b95d044`). Do NOT pass `--no-wait` — the command must WAIT and return a FINISHED/green build, not merely "submitted". If it errors on an auth/quota issue, surface it rather than forcing.
2. Confirm the build status is `finished` (green) on the EAS dashboard output. A build that is still "in queue"/"in progress" or that "errored"/"failed" does NOT satisfy the gate.
3. On green: commit this hop as its own boundary, e.g. `feat(22-02): bump apps/mobile to Expo SDK 55 (hop 1/3, green EAS Android build)`. This commit is the rollback point before hop 2. Do NOT proceed to Plan 03 unless this build is green and committed.

This is 1 of 3 required EAS builds for the phase — do not trigger extra/redundant builds.
  </action>
  <verify>EAS build for the preview/android profile returns status `finished` (green). `git log --oneline -1` shows the hop-1 commit. package.json shows expo 55.x.</verify>
  <done>Green EAS Android build for SDK 55 completed and the hop committed as an isolated, revertible boundary.</done>
</task>

</tasks>

<verification>
- expo resolves to 55.x; expo-doctor, tsc, jest all green vs baseline.
- EAS Android preview build status = finished (green).
- Hop committed as its own commit; babel dynamic-import-node plugin still present.
</verification>

<success_criteria>
apps/mobile runs on Expo SDK 55 with New Arch enabled and no config divergence; all four gates (expo-doctor → tsc → jest → EAS build) passed in order; the hop is an isolated green commit.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-02-SUMMARY.md` recording: resolved expo/RN versions, whether newArchEnabled was removed from app.json, the EAS build id/URL and its green status, and the commit hash of the hop.
</output>
