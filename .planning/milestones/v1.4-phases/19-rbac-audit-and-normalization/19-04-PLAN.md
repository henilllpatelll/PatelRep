---
phase: 19-rbac-audit-and-normalization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/core/roles.py
  - apps/api/routers/programs.py
  - apps/api/routers/safety.py
  - apps/api/routers/hotels.py
autonomous: true

must_haves:
  truths:
    - "A single source-of-truth module apps/api/core/roles.py defines ALL_ROLES, ALL_STAFF_ROLES, MANAGER_ROLES, PROGRAM_MANAGER_ROLES"
    - "programs.py imports PROGRAM_MANAGER_ROLES (its old 4-value MANAGER_ROLES incl. engineer) — all 9 call sites resolve to the same membership as before"
    - "safety.py imports MANAGER_ROLES from core.roles with an identical value to its old local definition — zero behavior change"
    - "hotels.py imports ALL_STAFF_ROLES from core.roles; the duplicate 'engineer' is gone; membership behavior is unchanged"
    - "No local MANAGER_ROLES / ALL_STAFF_ROLES tuple definitions remain in programs.py, safety.py, or hotels.py"
  artifacts:
    - path: "apps/api/core/roles.py"
      provides: "Canonical role-group constants + chief_engineer/migration-064 comment"
      contains: "PROGRAM_MANAGER_ROLES"
      min_lines: 8
    - path: "apps/api/routers/programs.py"
      provides: "9 require_role sites using imported PROGRAM_MANAGER_ROLES"
      contains: "PROGRAM_MANAGER_ROLES"
    - path: "apps/api/routers/safety.py"
      provides: "MANAGER_ROLES imported from core.roles"
      contains: "from core.roles import"
    - path: "apps/api/routers/hotels.py"
      provides: "ALL_STAFF_ROLES imported from core.roles"
      contains: "from core.roles import"
  key_links:
    - from: "apps/api/routers/programs.py"
      to: "apps/api/core/roles.py PROGRAM_MANAGER_ROLES"
      via: "from core.roles import PROGRAM_MANAGER_ROLES"
      pattern: "from core.roles import"
    - from: "apps/api/routers/safety.py"
      to: "apps/api/core/roles.py MANAGER_ROLES"
      via: "from core.roles import MANAGER_ROLES"
      pattern: "from core.roles import"
    - from: "apps/api/routers/hotels.py"
      to: "apps/api/core/roles.py ALL_STAFF_ROLES"
      via: "from core.roles import ALL_STAFF_ROLES"
      pattern: "from core.roles import"
---

<objective>
Close RBAC-04: consolidate the drifted role-group constants into a single source of truth at `apps/api/core/roles.py`, then repoint the three affected routers. This is a rename-and-relocate exercise that changes ZERO effective access — every call site resolves to the same role membership it does today. Per RESEARCH Decisions 1-3 (locked, do not reopen): the two same-named `MANAGER_ROLES` are two different tiers, disambiguated as `MANAGER_ROLES` (safety) and `PROGRAM_MANAGER_ROLES` (programs); `ALL_STAFF_ROLES` is dedup-only (drop the duplicate "engineer", do NOT re-add retired chief_engineer).

Purpose: RBAC-04 requirement + ROADMAP success criterion 4.
Output: new `core/roles.py` + updated imports in programs.py, safety.py, hotels.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rbac-audit-and-normalization/19-RESEARCH.md
@apps/api/routers/programs.py
@apps/api/routers/safety.py
@apps/api/routers/hotels.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the source-of-truth module apps/api/core/roles.py</name>
  <files>apps/api/core/roles.py</files>
  <action>
Create `apps/api/core/roles.py` with the canonical role-group constants (matches RESEARCH's "Where the source-of-truth module should live" section). Exact contents:

```python
"""Canonical role-group constants. Single source of truth — routers must import
from here rather than defining local *_ROLES tuples, to prevent silent drift."""

ALL_ROLES = ("gm", "housekeeping_supervisor", "engineer", "front_desk", "housekeeper")
# chief_engineer was retired by migration 064_merge_chief_engineer.sql — it can no
# longer occur in a live JWT. Some routers still reference it in role-group
# constants as a harmless no-op; do not re-add it to ALL_ROLES / ALL_STAFF_ROLES.

ALL_STAFF_ROLES = ALL_ROLES  # hotels.py's prior definition had a duplicate "engineer"

# Two DIFFERENT authority tiers that previously shared the name MANAGER_ROLES
# (see Phase 19 RESEARCH Decision 1) — kept distinct on purpose, not merged:
MANAGER_ROLES = ("gm", "housekeeping_supervisor", "chief_engineer")  # leadership/compliance tier (safety.py)
PROGRAM_MANAGER_ROLES = ("gm", "housekeeping_supervisor", "engineer", "chief_engineer")  # operational-program tier incl. line engineers (programs.py)
```

This is a leaf module (no imports from other app modules), so it introduces no circular-import risk. Verify it imports cleanly: `cd apps/api && python -c "from core.roles import ALL_ROLES, ALL_STAFF_ROLES, MANAGER_ROLES, PROGRAM_MANAGER_ROLES; print(ALL_STAFF_ROLES, MANAGER_ROLES, PROGRAM_MANAGER_ROLES)"`.
  </action>
  <verify>`cd apps/api && python -c "from core.roles import ALL_ROLES, ALL_STAFF_ROLES, MANAGER_ROLES, PROGRAM_MANAGER_ROLES; assert ALL_STAFF_ROLES == ('gm','housekeeping_supervisor','engineer','front_desk','housekeeper'); assert MANAGER_ROLES == ('gm','housekeeping_supervisor','chief_engineer'); assert PROGRAM_MANAGER_ROLES == ('gm','housekeeping_supervisor','engineer','chief_engineer'); print('ok')"` prints `ok`.</verify>
  <done>core/roles.py exists and exports the four constants with exactly the specified values.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint programs.py, safety.py, and hotels.py to import from core.roles</name>
  <files>apps/api/routers/programs.py, apps/api/routers/safety.py, apps/api/routers/hotels.py</files>
  <action>
Repoint each router to the new module WITHOUT changing any effective role membership. Each router keeps using the same identifier at its call sites — only the definition source changes (and programs.py's identifier is renamed to disambiguate).

**programs.py** (local def at line 43: `MANAGER_ROLES = ("gm", "housekeeping_supervisor", "engineer", "chief_engineer")`):
- Delete the local `MANAGER_ROLES = (...)` line 43.
- Add `from core.roles import PROGRAM_MANAGER_ROLES` alongside the existing `from core.database import supabase` / `from middleware.auth import ...` imports at the top.
- Replace all 9 `require_role(*MANAGER_ROLES)` call sites (lines 121, 188, 220, 283, 289, 321, 433, 447, 459) with `require_role(*PROGRAM_MANAGER_ROLES)`. Use a scoped find/replace of `*MANAGER_ROLES` → `*PROGRAM_MANAGER_ROLES` within this file. NOTE: the value is identical to the old local one (4 roles incl. engineer), so access is unchanged — this is a pure rename to end the same-name collision with safety.py.
- After: `grep -n "MANAGER_ROLES" apps/api/routers/programs.py` must show only `PROGRAM_MANAGER_ROLES` occurrences (the import + 9 call sites) and NO local `MANAGER_ROLES = (` definition.

**safety.py** (local def at line 34: `MANAGER_ROLES = ("gm", "housekeeping_supervisor", "chief_engineer")`):
- Delete the local `MANAGER_ROLES = (...)` line 34.
- Add `from core.roles import MANAGER_ROLES` alongside the existing top-of-file imports.
- Leave every existing `MANAGER_ROLES` usage (lines 84, 121, 139, 164, 186, 249, 264) unchanged — same name, same value, now imported. Zero behavior change.

**hotels.py** (local def at line 11: `ALL_STAFF_ROLES = ("gm", "housekeeping_supervisor", "engineer", "front_desk", "housekeeper", "engineer")` — note the duplicate "engineer"):
- Delete the local `ALL_STAFF_ROLES = (...)` line 11.
- Add `from core.roles import ALL_STAFF_ROLES` alongside the existing `from core.database import supabase` import.
- Leave the `require_role(*ALL_STAFF_ROLES)` usages (lines 118, 194) unchanged. The imported value drops the duplicate "engineer" but membership is identical (a set-membership check is unaffected by a duplicate), so behavior is unchanged.

Do NOT touch any other constants, tenant-scoping, or handler logic in these files. Do NOT modify evidence.py / services/safety/contracts.py / ai_copilot.py (their independently-converged constants are out of scope for this rename). Then confirm the whole app still imports and the API test suite is green.
  </action>
  <verify>Run:
`cd apps/api && python -c "import main; print('app import ok')"` (confirms no import errors / circular imports after the repointing);
`grep -nE "^MANAGER_ROLES *=|^ALL_STAFF_ROLES *=" apps/api/routers/programs.py apps/api/routers/safety.py apps/api/routers/hotels.py` returns NOTHING (no local definitions remain);
`grep -c "PROGRAM_MANAGER_ROLES" apps/api/routers/programs.py` returns 10 (1 import + 9 call sites);
`cd apps/api && python -m pytest tests/ -q` passes (no regressions — the existing test_ai_copilot_rbac.py / test_evidence_foundation.py role assertions still hold since those files were not changed).</verify>
  <done>All three routers import their role groups from core.roles; no local MANAGER_ROLES/ALL_STAFF_ROLES definitions remain; programs.py uses PROGRAM_MANAGER_ROLES at all 9 sites; app imports cleanly and the API test suite passes.</done>
</task>

</tasks>

<verification>
- core/roles.py is the single definition site for ALL_ROLES/ALL_STAFF_ROLES/MANAGER_ROLES/PROGRAM_MANAGER_ROLES.
- programs.py → PROGRAM_MANAGER_ROLES (imported), safety.py → MANAGER_ROLES (imported), hotels.py → ALL_STAFF_ROLES (imported).
- `grep -nE "^(MANAGER_ROLES|ALL_STAFF_ROLES) *=" apps/api/routers/{programs,safety,hotels}.py` is empty.
- `cd apps/api && python -m pytest tests/ -q` green; `python -c "import main"` clean.
- No effective access change anywhere (values verified equal in Task 1).
</verification>

<success_criteria>
ROADMAP success criterion 4: a single source-of-truth module defines the consolidated role-group constants; the previously-drifted MANAGER_ROLES resolves to one explicitly-confirmed membership per tier (safety vs programs), and ALL_STAFF_ROLES resolves to one deduped membership — each collision resolved as a documented product decision, not auto-merged.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-audit-and-normalization/19-04-SUMMARY.md`
</output>
