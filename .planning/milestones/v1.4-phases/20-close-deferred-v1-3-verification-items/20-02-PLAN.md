---
phase: 20-close-deferred-v1-3-verification-items
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - e2e/20-verify-gm.spec.ts
  - apps/web/lib/utils/avatar.ts
  - apps/web/components/guest-requests/GuestRequestDrawer.tsx
  - apps/web/components/guest-requests/GuestRequestsPage.tsx
autonomous: true

must_haves:
  truths:
    - "a staff row with NULL/empty full_name renders 'Unnamed Staff' on the Staff, Scheduling, and Housekeeping pages"
    - "the NULL-name fallback renders with zero browser console errors (no null reaching .split/getInitials)"
    - "the Guest Request drawer advance buttons transition a request through the legal status chain and the kanban board reflects each new status after each click"
  artifacts:
    - path: "e2e/20-verify-gm.spec.ts"
      provides: "Durable Playwright assertions for VERIFY-02 (Unnamed Staff fallback) and VERIFY-03 (guest-request advance chain + kanban reflect)"
      contains: "Unnamed Staff"
  key_links:
    - from: "e2e/20-verify-gm.spec.ts"
      to: "apps/web/lib/utils/avatar.ts getDisplayName"
      via: "renders fallback text 'Unnamed Staff' on staff/scheduling/housekeeping pages"
      pattern: "Unnamed Staff"
    - from: "e2e/20-verify-gm.spec.ts"
      to: "guest_requests transition endpoint"
      via: "drawer advance button -> guestRequestsApi.transitionRequest -> kanban query invalidation"
      pattern: "transition|advance"
---

<objective>
Close the two GM-single-role deferred verification items from v1.3 with live-browser evidence against current (post-Phase-19) code:

- VERIFY-02 — A NULL/empty staff `full_name` renders the "Unnamed Staff" fallback across the Staff, Scheduling, and Housekeeping pages, with zero console errors.
- VERIFY-03 — The Guest Request drawer status-advance buttons click through the legal status chain end-to-end, and the kanban board reflects the new status after each click.

Both items' code is present (Phase 17) and untouched by Phase 19 (VERIFY-03's `POST /{id}/transition` is a different handler than the `DELETE` gate Phase 19 added). This plan produces durable Playwright coverage plus a Claude-driven live click-through, and fixes any REAL bug surfaced (never weaken an assertion to make it pass).

Depends on 20-01 (wave 2, not a code dependency): serialized to avoid two concurrent Playwright runs seeding/hammering the SAME cloud dev hotel + rate-limited API at once (20-RESEARCH.md Pitfall 4).

Purpose: Confirm live behavior, completing the phase's "zero open items" outcome.
Output: `e2e/20-verify-gm.spec.ts` (new spec, both checks passing) + live-browser confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-close-deferred-v1-3-verification-items/20-RESEARCH.md
@e2e/helpers/rbac-users.ts
@e2e/golden-paths/staff.spec.ts
@e2e/golden-paths/scheduling.spec.ts
@e2e/10-guest-requests.spec.ts
@apps/web/lib/utils/avatar.ts
@apps/web/components/guest-requests/GuestRequestDrawer.tsx
@apps/web/components/guest-requests/GuestRequestsPage.tsx
@apps/web/lib/api/guest_requests.ts

# Environment / target — SAME as 20-01: web app local (localhost:3000, `npm run dev:web`),
# API + Supabase = cloud dev project. Set PLAYWRIGHT_BASE_URL=http://localhost:3000.
# TEST_PASSWORD (GM durable account) MUST be set or specs test.skip(). Both checks run as GM.
# Service-role Supabase key (SUPABASE_SERVICE_ROLE_KEY in apps/api/.env) is available for direct
# DB seeding of the NULL-name row. AI/SMS creds are absent locally but NEITHER check needs them
# (guest-request transition is DB-only; do NOT touch the SMS "contact guest" action).
</context>

<tasks>

<task type="auto">
  <name>Task 1: VERIFY-02 — NULL full_name renders "Unnamed Staff" on 3 pages, zero console errors</name>
  <files>e2e/20-verify-gm.spec.ts</files>
  <action>
Create `e2e/20-verify-gm.spec.ts`. Reuse `getGmToken` / `GM_TEST_USER` / `TEST_PASSWORD` and the `loginViaUI` + `EMPTY_STORAGE` context pattern from `e2e/helpers/rbac-users.ts` / `e2e/16-rbac.spec.ts` (all runs are as GM). `test.skip(!TEST_PASSWORD, ...)`.

Data-state precondition (see 20-RESEARCH.md Pitfall 3 — the fallback ONLY renders when full_name is actually null/empty; do NOT claim it works from code reading): in `beforeAll`, ensure exactly one staff row in the GM hotel has NULL/empty `full_name`. Preferred: seed a staff user via `POST /staff/add-direct` (as in the rbac harness), then set its `full_name` to NULL directly via the service-role Supabase key (SUPABASE_SERVICE_ROLE_KEY from apps/api/.env) against the profiles/staff table. Record the row id; `afterAll` deletes it (reversible fixture — do not leave residue). Confirm the exact table/column the pages read from `apps/api/routers/staff.py:136` (`profile.get("full_name") or ""`) and the render sites listed in 20-RESEARCH.md.

Register a `page.on('console')` (and `page.on('pageerror')`) listener that collects errors BEFORE navigating. Then, logged in as GM, verify the fallback text `"Unnamed Staff"` (from `getDisplayName` default in `avatar.ts:11`) renders for that row on all three surfaces:
  - Staff page `/staff` (render sites staff/page.tsx:157/533/535/587/746/921/923)
  - Scheduling page `/scheduling` (scheduling/page.tsx:167/192/337/936/1044/1048)
  - Housekeeping page `/housekeeping` (housekeeping/page.tsx:99)
On each page assert `page.getByText('Unnamed Staff').first()` is visible, and after all three assert the collected console/page errors array is empty (the original failure mode was a null hitting `.split(' ')`/`getInitials`; `avatar.ts` is now null-safe — a regression would repopulate this array).

If "Unnamed Staff" does NOT render, or a console error appears, diagnose the root cause (a non-null-safe call site, or a page bypassing `getDisplayName`) and FIX it at the source — do not weaken the assertion. Log any real bug to `.wolf/buglog.json`.
  </action>
  <verify>PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-gm.spec.ts -g "VERIFY-02" passes. Live headed pass: the seeded row shows "Unnamed Staff" on /staff, /scheduling, /housekeeping with a clean console. `afterAll` deletes the seeded row.</verify>
  <done>The NULL-name row renders "Unnamed Staff" on all three pages with zero console/page errors; spec green; seeded row torn down; any real bug fixed and logged.</done>
</task>

<task type="auto">
  <name>Task 2: VERIFY-03 — Guest Request drawer advance chain + kanban reflects each status</name>
  <files>e2e/20-verify-gm.spec.ts</files>
  <action>
Add VERIFY-03 assertions to `e2e/20-verify-gm.spec.ts`, run as GM. The transition endpoint is a STATE MACHINE — illegal jumps raise 422 (20-RESEARCH.md Pitfall 2). Do NOT seed one request per status and jump; instead seed ONE request and walk it legally through the chain, which is exactly what the drawer's per-status advance button does.

Data-state precondition: in `beforeAll` (or the test's start), create one guest request in the GM hotel at the chain start via the guest-requests create path (see `apps/api/routers/guest_requests.py` / `guestRequestsApi`). Record its id; `afterAll` deletes it (reversible).

Drive the flow on `/guest-requests`: open the request's drawer (`GuestRequestDrawer.tsx`), and for each legal step in the chain (acknowledged → dispatched → arrived → guest_contacted → resolved → verified — confirm exact chain against `GuestRequestDrawer.tsx:178-203`) click the advance button. `handleAdvance` (`GuestRequestsPage.tsx:187-190`) calls `guestRequestsApi.transitionRequest(id,{status})` (`guest_requests.ts:117`) which invalidates `['guest-requests-kanban']`. After EACH click, assert (a) no 422/error toast, and (b) the kanban board reflects the new status — the request's card now sits in / is labeled with the new status column (the mutation invalidates the kanban query; give it a short settle or wait for the column change). Skip the SMS "contact guest" action at `guest_requests.py:214` — it is a separate action, not part of the advance chain, and needs absent Twilio creds.

If a legal transition returns 422, the board fails to update, or a button is missing at a status, diagnose the root cause (drawer button wiring, `handleAdvance`, `transitionRequest`, or the backend state machine) and FIX it — do not weaken the assertion or skip a step. Log any real bug to `.wolf/buglog.json`.
  </action>
  <verify>PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-gm.spec.ts -g "VERIFY-03" passes. Live headed pass: open the drawer, click through each advance button in order, watch the kanban column update after each. `afterAll` deletes the seeded request.</verify>
  <done>The drawer advances the request through the full legal chain with no 422s, the kanban board reflects each new status after every click; spec green; seeded request torn down; any real bug fixed and logged.</done>
</task>

</tasks>

<verification>
- `PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-gm.spec.ts` — all VERIFY-02 and VERIFY-03 assertions pass (not skipped).
- Claude-driven live browser pass (headed / playwright-cli) confirms both golden paths visually, per the Self-Verification Policy — do NOT ask the user to test.
- All seeded fixtures (NULL-name staff row, guest request) removed in `afterAll`; a second run shows no accumulated test data.
- `npm run lint` clean on the new spec file.
</verification>

<success_criteria>
- Success Criterion 2 (roadmap): NULL staff full_name renders "Unnamed Staff" across Staff, Scheduling, and Housekeeping with zero console errors — confirmed live.
- Success Criterion 3 (roadmap): Guest Request drawer status-advance buttons click through end-to-end at each status and the kanban reflects the new status after each click — confirmed live.
- Any real bug surfaced is fixed at the source (assertion never weakened) and logged to `.wolf/buglog.json`.
- No test data left behind in the cloud dev hotel.
</success_criteria>

<output>
After completion, create `.planning/phases/20-close-deferred-v1-3-verification-items/20-02-SUMMARY.md`. Record: env used, whether any real bug was found/fixed, and confirmation that all seeded fixtures were torn down (feeds Phase 21 hygiene).
</output>
