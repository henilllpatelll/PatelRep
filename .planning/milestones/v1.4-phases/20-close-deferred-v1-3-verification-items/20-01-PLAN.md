---
phase: 20-close-deferred-v1-3-verification-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/20-verify-rbac.spec.ts
  - apps/web/app/(dashboard)/engineering/work-orders/page.tsx
  - apps/web/app/(dashboard)/housekeeping/inspections/page.tsx
autonomous: true

must_haves:
  truths:
    - "housekeeper and front_desk are redirected away from /engineering and never render the Work Orders page"
    - "chief_engineer reaches /engineering/work-orders but the 'Archive...' button is absent (canManage === false)"
    - "a housekeeping_supervisor is a selectable option in the Inspections re-assign picker and a failed inspection re-assigns to them end-to-end (success toast + assignment updates)"
  artifacts:
    - path: "e2e/20-verify-rbac.spec.ts"
      provides: "Durable Playwright assertions for VERIFY-01 (archive-button role gate) and VERIFY-04 (inspection re-assign to supervisor)"
      contains: "seedRbacUsers"
  key_links:
    - from: "e2e/20-verify-rbac.spec.ts"
      to: "e2e/helpers/rbac-users.ts"
      via: "getGmToken -> seedRbacUsers -> per-role loginViaUI -> teardownRbacUsers"
      pattern: "seedRbacUsers|loginViaUI|teardownRbacUsers"
    - from: "e2e/20-verify-rbac.spec.ts"
      to: "http://localhost:3000 (web) + cloud dev API"
      via: "PLAYWRIGHT_BASE_URL points at local web; harness seeds against cloud dev API"
      pattern: "engineering/work-orders|inspections"
---

<objective>
Close the two RBAC-gated deferred verification items from v1.3 with live-browser evidence against current (post-Phase-19) code:

- VERIFY-01 — Non-manager roles do not see the "Archive..." button on the Engineering Work Orders page.
- VERIFY-04 — The Inspections re-assign picker re-assigns a failed inspection to a `housekeeping_supervisor` end-to-end.

Both items already have their code in place (confirmed on disk in Phase 15/17 and unchanged by Phase 19, which was backend-only). This plan produces durable Playwright coverage plus a Claude-driven live click-through, and fixes any REAL bug surfaced (do not weaken an assertion to make it pass).

Purpose: Confirm live behavior, giving the phase its "zero open items" outcome with repeatable regression coverage.
Output: `e2e/20-verify-rbac.spec.ts` (new spec, both checks passing) + live-browser confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-close-deferred-v1-3-verification-items/20-RESEARCH.md
@e2e/helpers/rbac-users.ts
@e2e/16-rbac.spec.ts
@e2e/golden-paths/hk-inspection.spec.ts
@apps/web/app/(dashboard)/engineering/work-orders/page.tsx
@apps/web/app/(dashboard)/housekeeping/inspections/page.tsx

# Environment / target (per 20-RESEARCH.md Open Question 1):
# - Run the WEB app locally: `npm run dev:web` (localhost:3000). Set PLAYWRIGHT_BASE_URL=http://localhost:3000.
# - API + Supabase remain the cloud dev project (JWT/auth is cloud-based; the rbac harness ignores localhost and
#   seeds via the prod/cloud API `POST /staff/add-direct`). Confirm apps/web local env points at the SAME Supabase
#   project the GM test account (hp.patelrep@gmail.com) lives in.
# - TEST_PASSWORD (or RBAC_TEST_PASSWORD) MUST be set for the GM + seeded users, or the specs test.skip(). The GM
#   password is the durable test account (see project memory reference_test_account.md). If it is genuinely
#   unavailable to you at runtime, that is the one thing to surface — everything else is automatable.
</context>

<tasks>

<task type="auto">
  <name>Task 1: VERIFY-01 — Archive button role gate (route-redirect + button-gate)</name>
  <files>e2e/20-verify-rbac.spec.ts</files>
  <action>
Create `e2e/20-verify-rbac.spec.ts`. Mirror the harness pattern in `e2e/16-rbac.spec.ts`: import `getGmToken`, `seedRbacUsers`, `teardownRbacUsers`, `GM_TEST_USER`, `TEST_PASSWORD` from `./helpers/rbac-users`; `test.skip(!TEST_PASSWORD, ...)`; `beforeAll` seeds users and captures the GM token; `afterAll` tears them down; copy the `loginViaUI(page, email, password)` helper and the `EMPTY_STORAGE` per-role `browser.newContext({ storageState })` pattern verbatim.

Add VERIFY-01 assertions covering BOTH enforcement layers (see 20-RESEARCH.md Pitfall 1 — housekeeper/front_desk never reach /engineering, so a route-only check "confirms nothing" about the button gate):
  (a) Route-redirect layer: log in as the seeded `housekeeper`, then as `front_desk`; for each, `page.goto('/engineering')` and assert redirect to `/dashboard(\?unauthorized|$)` (they never render the Work Orders page). This satisfies VERIFY-01 as literally written.
  (b) Button-gate layer (the meaningful assertion): log in as the seeded `chief_engineer` (route-ALLOWED for /engineering per ROLE_ALLOWED, but `canManage = role === 'engineer' || role === 'gm'` at page.tsx:253 excludes them). `page.goto('/engineering/work-orders')`, assert the page actually loads (NOT redirected), then assert the "Archive..." button is absent — e.g. `await expect(page.getByRole('button', { name: /archive/i })).toHaveCount(0)`. Confirm the exact button label/selector against `apps/web/app/(dashboard)/engineering/work-orders/page.tsx:379-384` before asserting.
  (c) Positive control (proves the selector is real, not a typo that trivially passes): log in as GM, `goto('/engineering/work-orders')`, assert the "Archive..." button IS present (`toHaveCount(1)` or visible).

If (b) or (c) fails because the button gate is actually broken (button shows for chief_engineer, or is missing for GM), diagnose the root cause in `page.tsx` and FIX the underlying gate — do NOT relax the assertion. Log any real bug to `.wolf/buglog.json` per project rules.

OUT-OF-SCOPE OBSERVATION (do NOT fix — flag only): `canManage` excludes `chief_engineer`. VERIFY-01 only names housekeeper/front_desk, so a correctly-hidden Archive button for chief_engineer is the expected test outcome here. Whether chief_engineer *should* be able to archive is a separate product question — record it in the SUMMARY as an observation, do not change the gate.
  </action>
  <verify>PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-rbac.spec.ts -g "VERIFY-01" — all VERIFY-01 assertions pass. Also drive the page live (headed / playwright-cli skill) as GM and confirm the Archive button renders, per the Self-Verification Policy.</verify>
  <done>housekeeper and front_desk redirect away from /engineering; chief_engineer loads /engineering/work-orders with NO Archive button; GM sees the Archive button. All assertions green; any real gate bug fixed (not masked) and logged.</done>
</task>

<task type="auto">
  <name>Task 2: VERIFY-04 — Inspections re-assign to housekeeping_supervisor end-to-end</name>
  <files>e2e/20-verify-rbac.spec.ts</files>
  <action>
Add VERIFY-04 assertions to the same spec, run as GM (GM can reach `/housekeeping/inspections`). The seeded `test-hk-supervisor@patelrep-test.com` user from `beforeAll` provides the supervisor that must be selectable (the picker filter at `inspections/page.tsx:170` was widened in 17-05 to `role === 'housekeeper' || role === 'housekeeping_supervisor'`).

Data-state precondition (see 20-RESEARCH.md Pitfall 3 + Open Question 3 — reversible fixture, teardown so Phase 21 hygiene isn't burdened): a room must be in a "ready for inspection" / failed-inspection state so the re-assign modal (`inspections/page.tsx:479-550`) is reachable. Establish it, preferring the least-invasive route:
  1. First check whether the GM test hotel already has a room in that state (open `/housekeeping/inspections` and read the list). If one exists, use it — no seeding needed.
  2. If none exists, seed one via the service-role Supabase key from `apps/api/.env` (SUPABASE_SERVICE_ROLE_KEY): create/assign a room and mark its clean session / inspection failed using the schema in `supabase/migrations` (room_assignments: `assigned_to`, `assignment_date`; clean sessions / inspection tables per CLAUDE.md Database Schema Gotchas). Record every row you insert so `afterAll` can delete it. Do NOT leave test data behind.

End-to-end assertion: open the re-assign modal for that room, assert the `housekeeping_supervisor` (Test Hk Supervisor) appears as a selectable `<option>` (this is the core of the 17-05 fix), select it, submit (`housekeepingApi.assignRooms([{room_id, housekeeper_id}])` at `inspections/page.tsx:206-225`), and confirm success — the success toast (`housekeeping.inspectionsPage.toast.reassignSuccess`) shows AND the room's assignment now reflects the supervisor.

If the supervisor is NOT selectable, or submit fails/500s, diagnose the root cause (picker filter, `_ensure_housekeeper()` backend acceptance, or assign call) and FIX it — do not weaken the assertion. Log any real bug to `.wolf/buglog.json`. Stay out of the SMS "contact guest" path — irrelevant here and needs absent Twilio creds (20-RESEARCH.md Pitfall 5).
  </action>
  <verify>PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-rbac.spec.ts -g "VERIFY-04" passes. Live headed pass: open the re-assign modal, see the supervisor option, submit, see the success toast and updated assignment. `afterAll` teardown removes any seeded room/inspection + rbac users (re-run the spec: no residual test data accumulates).</verify>
  <done>The seeded housekeeping_supervisor is selectable in the re-assign picker and a failed inspection re-assigns to them with a success toast and updated assignment; spec green; seeded data torn down; any real bug fixed and logged.</done>
</task>

</tasks>

<verification>
- `PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test e2e/20-verify-rbac.spec.ts` — all VERIFY-01 and VERIFY-04 assertions pass (not skipped; TEST_PASSWORD present).
- Claude-driven live browser pass (headed / playwright-cli) confirms both golden paths visually, per the Self-Verification Policy — do NOT ask the user to test.
- `teardownRbacUsers` + any seeded-room cleanup run in `afterAll`; a second spec run shows no accumulated `@patelrep-test.com` users or orphaned inspection rows.
- `npm run lint` clean on the new spec file.
</verification>

<success_criteria>
- Success Criterion 1 (roadmap): housekeeper and front_desk confirmed live to not see the Archive button (route-redirected), AND chief_engineer confirmed to reach the page with the button gated off.
- Success Criterion 4 (roadmap): Inspections re-assign picker re-assigns a failed inspection to a housekeeping_supervisor end-to-end, confirmed live.
- Any real bug surfaced during verification is fixed at the source (assertion never weakened) and logged to `.wolf/buglog.json`.
- No test data left behind in the cloud dev hotel.
</success_criteria>

<output>
After completion, create `.planning/phases/20-close-deferred-v1-3-verification-items/20-01-SUMMARY.md`. Record: env used (local web + cloud API), the chief_engineer `canManage` out-of-scope observation, and whether any real bug was found/fixed.
</output>
