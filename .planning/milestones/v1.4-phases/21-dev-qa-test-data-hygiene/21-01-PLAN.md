---
phase: 21-dev-qa-test-data-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/094_tenant_is_test_flag.sql
autonomous: true
must_haves:
  truths:
    - "The tenants table has an is_test BOOLEAN NOT NULL DEFAULT false column (confirmed via schema inspection)"
    - "Every non-fixture tenant on the dev/QA project has is_test = true"
    - "The standing QA fixture tenant (23264962-aa09-4e4f-a49d-fc345cc91414) has is_test = false"
  artifacts:
    - path: "supabase/migrations/094_tenant_is_test_flag.sql"
      provides: "On-disk source-of-truth for the is_test column add + rollback comment"
      contains: "is_test"
  key_links:
    - from: "supabase/migrations/094_tenant_is_test_flag.sql"
      to: "live project oacnwalhcpqdabivweki"
      via: "Supabase MCP apply_migration (NOT supabase db push)"
      pattern: "ADD COLUMN IF NOT EXISTS is_test"
---

<objective>
Add the schema-level test-tenant marker (QA-01) and flag the delete-candidate tenants.

Purpose: Success Criterion #1 — `tenants.is_test BOOLEAN NOT NULL DEFAULT false` must exist and be confirmable via schema inspection. This column is the cleanup script's source-of-truth selector (Plan 03 intersects `is_test = true` with a hardcoded allowlist constant as belt-and-suspenders). Setting the flag is the ONE allowed live-data write in this phase — it is reversible metadata, NOT a destructive delete.
Output: Migration file `094_tenant_is_test_flag.sql` on disk, the column live on the dev/QA project, and every non-fixture tenant flagged `is_test = true`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-dev-qa-test-data-hygiene/21-RESEARCH.md
@supabase/migrations/085_opera_pilot_flag.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply migration 094 (is_test column)</name>
  <files>supabase/migrations/094_tenant_is_test_flag.sql</files>
  <action>
Create `supabase/migrations/094_tenant_is_test_flag.sql` mirroring the `085_opera_pilot_flag.sql` idiom verbatim (verified as the project's boolean-flag idiom):

```sql
ALTER TABLE public.tenants ADD COLUMN IF NOT EXISTS is_test BOOLEAN NOT NULL DEFAULT FALSE;
COMMENT ON COLUMN public.tenants.is_test IS
  'Marks a tenant as dev/QA test data eligible for the cleanup script. Standing QA fixture(s) stay FALSE. (Phase 21 QA-01).';

-- ROLLBACK:
-- ALTER TABLE public.tenants DROP COLUMN is_test;
```

Then apply it to the live dev/QA project using the Supabase MCP tool `mcp__plugin_supabase_supabase__apply_migration` with project_id `oacnwalhcpqdabivweki`, migration name `tenant_is_test_flag`, and the ALTER+COMMENT SQL as the query. DO NOT use `supabase db push` — remote migration-history drift makes it unsafe (migration 093 in Phase 20 was applied the same MCP way for this exact reason).

If the Supabase MCP tools are not available in your execution context, do NOT guess or skip: SendMessage the lead ("main") requesting the migration be applied via MCP apply_migration and the confirmed column reported back, then treat this task as blocked pending that confirmation.
  </action>
  <verify>
Run `mcp__plugin_supabase_supabase__execute_sql` (project_id `oacnwalhcpqdabivweki`) with:
`SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='tenants' AND column_name='is_test';`
Expect exactly one row: data_type `boolean`, is_nullable `NO`, column_default `false`.
  </verify>
  <done>The migration file exists on disk AND the live `tenants` table reports an `is_test` boolean, NOT NULL, default false.</done>
</task>

<task type="auto">
  <name>Task 2: Flag every non-fixture tenant is_test = true</name>
  <files>supabase/migrations/094_tenant_is_test_flag.sql</files>
  <action>
Set `is_test = true` on all delete-candidate tenants via the Supabase MCP tool `mcp__plugin_supabase_supabase__execute_sql` (project_id `oacnwalhcpqdabivweki`). Scope by EXCLUDING the PRESERVE fixture so the write is self-correcting regardless of the exact candidate count:

`UPDATE public.tenants SET is_test = true WHERE id <> '23264962-aa09-4e4f-a49d-fc345cc91414';`

Rationale for excluding-the-fixture rather than an explicit IN-list: the research's prose says "10 delete candidates" but its inventory table enumerates only 9 non-fixture UUIDs — an internal count discrepancy. Flagging "all tenants except the locked PRESERVE fixture" honors the locked decision (flag the candidates, never the fixture) and is correct whether the true count is 9, 10, or drifts before execution. This is reversible metadata (a flag), NOT a delete.

Do NOT delete anything. Do NOT set is_test on the fixture. Do NOT pass any --execute flag anywhere — that is a different, out-of-scope tool from Plan 03.
  </action>
  <verify>
Run `mcp__plugin_supabase_supabase__execute_sql` (project_id `oacnwalhcpqdabivweki`):
`SELECT is_test, count(*) FROM public.tenants GROUP BY is_test ORDER BY is_test;`
Expect: exactly one row with is_test=false and count 1 (the fixture), and one row with is_test=true and count = (total tenants − 1). Also run
`SELECT is_test FROM public.tenants WHERE id='23264962-aa09-4e4f-a49d-fc345cc91414';`
and confirm it returns `false`.
  </verify>
  <done>Exactly one tenant (the PRESERVE fixture) has is_test=false; all others have is_test=true. Record the actual true-count in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `supabase/migrations/094_tenant_is_test_flag.sql` exists and contains `is_test`.
- Live schema inspection confirms the `is_test` boolean column (Success Criterion #1).
- Live data confirms only the fixture tenant is is_test=false; all others is_test=true.
- No row in any table was deleted by this plan.
</verification>

<success_criteria>
Phase Success Criterion #1 satisfied: `tenants.is_test BOOLEAN NOT NULL DEFAULT false` confirmed via schema inspection. Delete-candidate tenants flagged, fixture preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dev-qa-test-data-hygiene/21-01-SUMMARY.md` recording the confirmed column definition and the actual count of tenants flagged is_test=true.
</output>
