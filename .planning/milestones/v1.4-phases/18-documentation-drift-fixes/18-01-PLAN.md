---
phase: 18-documentation-drift-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md]
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md's Cron Jobs section describes the in-process AsyncIOScheduler (apps/api/core/scheduler.py), not GitHub Actions"
    - "CLAUDE.md's Current Scope credential note says only OPENAI_API_KEY/ANTHROPIC_API_KEY plus Twilio/OHIP are absent, with Stripe (test-mode) and Supabase service-role present"
    - "CLAUDE.md's Domain Map lists all 30 routers, including the 9 previously missing"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected Cron Jobs, Current Scope, and Domain Map sections"
      contains: "in-process APScheduler"
  key_links:
    - from: "CLAUDE.md Cron Jobs section"
      to: "apps/api/core/scheduler.py"
      via: "documented mechanism reference"
      pattern: "core/scheduler\\.py"
---

<objective>
Correct three drifted sections of `CLAUDE.md` (project root) so the doc matches the actual codebase: the Cron Jobs mechanism, the Current Scope local-credential note, and the Domain Map router table.

Purpose: The doc currently misdirects any session — it claims GitHub Actions triggers cron (retired; production uses in-process APScheduler), claims no local credentials exist (a populated `apps/api/.env` exists with Stripe test-mode + Supabase keys), and omits 9 of 30 routers. All replacement text is verified and ready in `18-RESEARCH.md`.
Output: Edited `CLAUDE.md` with all three sections corrected. No code changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-documentation-drift-fixes/18-RESEARCH.md
@CLAUDE.md

This is a pure Markdown find-and-replace phase. `18-RESEARCH.md` contains the exact
old-text-to-replace AND the verified new text for all three sections. Read it first — do not
re-derive the replacement text. Every claim in it was verified against source
(`apps/api/core/scheduler.py`, `apps/api/main.py`, `apps/api/core/config.py`, `apps/api/.env`
key names only, `apps/api/routers/*.py`). Do NOT read `apps/api/.env` values — the credential
facts are already established in research (key names only, no secrets).

Note: the current on-disk "Current Scope" bullet is a single unwrapped line; research shows it
line-wrapped. Read CLAUDE.md and match the actual on-disk text when replacing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Cron Jobs section with in-process APScheduler description</name>
  <files>CLAUDE.md</files>
  <action>
    In CLAUDE.md, replace the entire "## Cron Jobs (GitHub Actions → FastAPI `/v1/internal/*`)"
    section — the heading, the intro paragraph, and the 12-row endpoint table — with the "New text"
    block from Code Example 1 in `18-RESEARCH.md` (research lines ~75-104). The new heading is
    "## Cron Jobs (in-process APScheduler → FastAPI internal coroutines)".

    The new table uses Job IDs (e.g. `predictions.run`, `opera.sync-reservations`) instead of HTTP
    endpoints and has 13 rows (adds `lost-found.retention-check` per the research note on line 106).
    Copy the new text exactly as written in research — do not paraphrase, reorder rows, or drop the
    `lost-found.retention-check` row. Preserve surrounding sections (the "## Infrastructure" section
    that follows must remain intact).
  </action>
  <verify>grep -n "in-process APScheduler" CLAUDE.md returns the new heading; grep -c "lost-found.retention-check" CLAUDE.md returns 1; grep "GitHub Actions → FastAPI" CLAUDE.md returns nothing (old heading gone)</verify>
  <done>The Cron Jobs section describes the AsyncIOScheduler in apps/api/core/scheduler.py gated by should_run_scheduler(), states the GitHub Actions workflow is retired, and the table lists 13 job IDs with UTC schedules. The old GitHub-Actions framing and endpoint table are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the Current Scope local-credential note</name>
  <files>CLAUDE.md</files>
  <action>
    In CLAUDE.md's "## Current Scope" section, replace the bullet that begins "**No live API
    credentials in the local environment.**" (currently a single line at ~line 208) with the "New
    text" block from Code Example 2 in `18-RESEARCH.md` (research lines ~122-129). The new bullet
    begins "**Partial local credentials.**" and states: `apps/api/.env` exists with Supabase, Stripe
    test-mode, and cron/app-config keys present (billing + Supabase paths CAN be exercised locally);
    only `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` plus Twilio and Opera/OHIP OAuth credentials are absent.

    Match the actual on-disk text of the old bullet (it is one unwrapped line). Replace only that
    bullet — leave the "Active surface: web app only" bullet and the section heading untouched.
  </action>
  <verify>grep -n "Partial local credentials" CLAUDE.md returns the new bullet; grep "No live API credentials" CLAUDE.md returns nothing; grep -c "STRIPE_SECRET_KEY" CLAUDE.md includes the new mention</verify>
  <done>The Current Scope note states Stripe (test-mode) and Supabase service-role credentials are present locally, and only the AI provider keys plus Twilio/OHIP are absent. The false "no .env / no Stripe keys" claim is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Domain Map table with all 30 routers</name>
  <files>CLAUDE.md</files>
  <action>
    In CLAUDE.md's "## Domain Map" section, replace the existing router table with the full verified
    30-file table from Code Example 3 in `18-RESEARCH.md` (research lines ~136-166). This adds the 9
    previously-missing routers (`clean_sessions`, `cleaning_checklists`, `evidence`, `feedback`,
    `late_checkout`, `management_roi`, `programs`, `safety`, `shifts`) and includes the `internal.py`
    row (cross-referenced to the Cron Jobs section).

    Copy the table rows exactly as in research, preserving the existing grouping convention
    (`hotels.py, onboarding.py` share one row; `work_orders.py, assets.py` share one row; every other
    router gets its own row). Do not touch the "## Conventions" section that follows.
  </action>
  <verify>For each of clean_sessions, cleaning_checklists, evidence, feedback, late_checkout, management_roi, programs, safety, shifts — grep -c "<name>.py" CLAUDE.md returns >=1; the table lists all 30 router files</verify>
  <done>The Domain Map lists all 30 routers in apps/api/routers/, including the 9 previously missing. Every router file has a table row with its web route (or a dash for internal/no-route APIs).</done>
</task>

</tasks>

<verification>
- All three CLAUDE.md sections replaced with the verified research text; no old (stale) framing remains.
- `git diff CLAUDE.md` shows changes confined to the three target sections — no unintended edits elsewhere.
- No code files changed (`git status` shows only CLAUDE.md modified among tracked source).
- Markdown tables render (pipe-delimited, header separator rows intact).
</verification>

<success_criteria>
1. CLAUDE.md's Cron Jobs section describes the in-process AsyncIOScheduler (`apps/api/core/scheduler.py`), not GitHub Actions.
2. CLAUDE.md's Current Scope note states only `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` plus Twilio/OHIP are absent locally, with Stripe (test-mode) and Supabase service-role present.
3. CLAUDE.md's Domain Map lists all 30 routers, including the 9 previously missing.
</success_criteria>

<output>
After completion, create `.planning/phases/18-documentation-drift-fixes/18-01-SUMMARY.md`
</output>
