---
phase: 09-remaining-screens-rollout
plan: 07
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - apps/mobile/components/supervisor/RoomDetailSheet.tsx
autonomous: true
requirements: [SCREENS-03]
must_haves:
  truths:
    - "RoomDetailSheet renders via useTheme() + primitives (zero C. refs); the Modal/overlay wrapper is structurally untouched"
    - "The 2 confirms at :50 (confirmRemove) and :191 (DND-override wellness check) stay Alert.alert"
    - "The 2 outcome alerts at :206 (dndOverrideCreated) and :208 (dndOverrideError) become toast.success/error"
    - "Remove-assignment + DND-override api behavior + tenant scoping are unchanged"
  artifacts:
    - path: "apps/mobile/components/supervisor/RoomDetailSheet.tsx"
      provides: "RoomDetailSheet on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/components/supervisor/RoomDetailSheet.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "dndOverride outcome alerts → toast"
      pattern: "useToast"
---

<objective>
Migrate `RoomDetailSheet.tsx` (363 lines, 50 `C.` refs, 14 `TouchableOpacity`, 4 `Alert.alert`) onto primitives — its own dedicated plan per D-08 (largest of the 7 supervisor files, reachable from Assignments + Phase-8's Room Board/Inspect). Two confirms STAY (`:50` confirmRemove, `:191` DND wellness-check); two outcome alerts CONVERT (`:206` success, `:208` error). Analog: `work-orders/[woId].tsx` (mixed confirm+Toast handlers).

Purpose: SCREENS-03 (RoomDetailSheet) — supervisor room detail/actions on primitives; behavior unchanged.
Output: sheet on primitives, zero `C.` refs, 2 confirms intact, 2 outcomes → Toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
##          `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## D-10 classification (verified): :50 confirmRemove STAYS · :191 DND wellness-check STAYS · :206 dndOverrideCreated → toast.success · :208 dndOverrideError → toast.error.
## Pitfall 3: handlers mix real remove-assignment / DND-override api.* logic with the alert — change ONLY the feedback call; never touch the preceding awaited api calls, guards, or tenant args.
## No-analog note: migrate internals via WorkOrderCard (Button/StatusBadge/color) + inspect (Toast); leave the Modal/overlay wrapper structurally untouched. Card NOT force-fit onto the sheet container.
</migration_reference>

<interfaces>
## atoms.tsx (migrated in plan 09-08, same wave, no file overlap) exposes shared supervisor atoms whose PROPS do not change — RoomDetailSheet keeps importing/using them unchanged. No dependency on 09-08's completion (props stable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RoomDetailSheet.tsx (50 C. refs, 2 confirms stay, 2 outcomes → Toast)</name>
  <files>apps/mobile/components/supervisor/RoomDetailSheet.tsx</files>
  <read_first>
    - apps/mobile/components/supervisor/RoomDetailSheet.tsx (full — 363 lines, 50 C. refs, 14 TouchableOpacity; Alert.alert at :50 confirmRemove, :191 DND wellness-check, :206 dndOverrideCreated, :208 dndOverrideError; the remove + dndOverride handler bodies)
    - apps/mobile/app/(app)/work-orders/[woId].tsx (Phase-8 exemplar — mixed confirm-stays + Toast handlers)
    - apps/mobile/components/ui/Button.tsx, StatusBadge.tsx
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` and `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace all 50 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep the existing `StyleSheet.create()` and the `Modal`/overlay wrapper structure exactly; append color objects as the LAST style-array element. Do NOT force-fit `Card` onto the sheet container.
    3. Convert action buttons/rows (submit/cancel/reassign/remove/DND controls) that are `TouchableOpacity` to `<Button variant=... size=... />`. StatusKey-matching chips → `<StatusBadge label={t(...)} />`.
    4. Alerts (D-10): LEAVE `Alert.alert` at :50 (confirmRemove, Cancel/Confirm) and :191 (DND-override wellness-check, Cancel/Confirm) unchanged — both are blocking confirms before a state change. Convert :206 dndOverrideCreated → `toast.success(msg)` and :208 dndOverrideError → `toast.error(msg)` using the existing translated strings. Change ONLY those two feedback calls — do NOT touch the preceding `await api.*` remove/DND-override calls, early-return guards, or `hotel_id`/tenant args.
    5. Do NOT change the atoms.tsx import usage or any prop contract this sheet exposes to its callers (Assignments / Room Board / Inspect).
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Confirms intact: `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 2) { exit 1 }` exits 0 (only :50 + :191)
    - `@('useToast', 'useTheme') | ForEach-Object { if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -SimpleMatch $_ -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: remove/DND-override api calls + guards + tenant args unchanged; Modal/overlay wrapper structurally unchanged
  </acceptance_criteria>
  <done>RoomDetailSheet on primitives, zero C. refs, :50/:191 confirms stay blocking, :206/:208 outcomes are Toasts, sheet wrapper + remove/DND logic untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (remove-assignment, DND-override, hotel_id-scoped) | Supervisor-role-gated mutations; NOT modified — only :206/:208 feedback calls change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Elevation of Privilege / Information Disclosure | Converting :206/:208 drops the preceding remove/DND `await api.*` call, its guard, or `hotel_id` scoping in the shared handler | mitigate | Pitfall-3 rule: change ONLY the two feedback calls; diff review asserts api/guard/tenant unchanged |
| T-09-07-02 | Tampering | The :50 remove-confirm or :191 DND wellness-check is converted to Toast, letting a destructive action fire without confirmation | mitigate | D-10: both stay Alert.alert; acceptance criteria assert exactly 2 Alert.alert remain |
| T-09-07-03 | Tampering | Force-fitting Card onto the Modal/overlay wrapper changes the sheet's dismiss/backdrop behavior | mitigate | Wrapper left structurally untouched; no Card on the container |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/RoomDetailSheet.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 2) { exit 1 }`
</verification>

<success_criteria>
SCREENS-03 (RoomDetailSheet, D-08): supervisor room detail/actions on primitives; 2 confirms stay blocking; 2 outcomes are Toasts; remove/DND behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-07-SUMMARY.md`.
</output>
