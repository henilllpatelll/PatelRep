---
phase: 09-remaining-screens-rollout
plan: 14
type: execute
wave: 6
depends_on: ["12", "13"]
files_modified:
  - apps/mobile/app/(app)/copilot/index.tsx
autonomous: true
requirements: [SCREENS-08]
must_haves:
  truths:
    - "copilot keeps its intentional dark aesthetic — the static darkTheme.* import + message-bubble structure stay as-is"
    - "The 1 real C. ref (C.alert :285) becomes theme.status.dirty; the 6 Alert.alert calls become Toast"
    - "send/mic/quick-action/confirm-card controls adopt Button/IconButton; confirmCard MAY become a Card; bubbles are NOT force-fit onto Card"
    - "AI task/WO/guest-request confirm behavior + AsyncStorage history are unchanged"
  artifacts:
    - path: "apps/mobile/app/(app)/copilot/index.tsx"
      provides: "Copilot minimal migration (dark preserved)"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/copilot/index.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "6 outcome alerts → toast"
      pattern: "useToast"
---

<objective>
Migrate `copilot/index.tsx` (425 lines, chat UI) per D-11 **minimal migration — keep dark**. It is built on static `darkTheme.*` tokens (intentional dark AI aesthetic), NOT `C.*` like other screens; three keys (`surfaceElevated`/`glass`/`glassBorder`) have no `lightTheme` equivalent. Do NOT route through `useTheme()` (would resolve those to `undefined` in light mode — Pitfall 4). Scope is exactly: migrate the 1 real `C.` ref, convert 6 `Alert.alert` → Toast, adopt `Button`/`IconButton` on the controls, optionally wrap the `confirmCard` in `Card`. Message bubbles stay as-is (D-09).

Purpose: SCREENS-08 (copilot) — primitive-button + Toast adoption; dark aesthetic + chat behavior preserved.
Output: copilot on Toast + Button/IconButton, 1 C. ref migrated, dark surface intact, no tokens.ts change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## D-11 (LOCKED): keep dark, minimal migration. Do NOT add useTheme(). Do NOT add lightTheme keys. Do NOT edit tokens.ts. The static `darkTheme` import at :19 STAYS. ~40 darkTheme.* refs STAY.
## The ONLY C. ref: `C.alert` at :285 → `theme.status.dirty`. But since this screen does NOT use useTheme(), route it to the static equivalent instead: use `darkTheme.status.dirty` (the same static-import idiom the screen already uses for all its dark tokens) — do NOT introduce useTheme() just for one ref. [If darkTheme has no `status.dirty`, use the darkTheme error/danger key that maps to the same red role — confirm against tokens.ts during read_first.]
## D-10 classification (verified, all 6 → Toast): copilot:158/168/178 success (taskCreated/workOrderCreated/guestRequestCreated, all use t()); copilot:160/170/180 error (each drops a hardcoded "Error" title → `toast.error(err.message)`). The 3 confirm actions are in-UI confirmCard buttons (:201-242), NOT alerts — zero blocking confirms here.
## Imports to add: `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## Button/IconButton: `<Button label=... onPress=... />` for the quick-action/confirm-card Create/Dismiss buttons; `<IconButton>` for icon-only send/mic controls. confirmCard (:201-242) MAY become `<Card>` + two `<Button>`. Message BUBBLES stay as-is — do NOT wrap in Card (D-09).
## Pitfall 3: the confirmTask/confirmWorkOrder/confirmGuestRequest handlers mix real api.* logic with the alert — change ONLY the feedback call; never touch the preceding awaited AI/create calls, guards, or tenant args.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copilot minimal migration (1 C. ref, 6 alerts → Toast, Button/IconButton controls)</name>
  <files>apps/mobile/app/(app)/copilot/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/copilot/index.tsx (full — 425 lines; static darkTheme import :19, C.alert :285, Alert.alert :158/160/168/170/178/180, confirmCard :201-242, send/mic controls, message bubbles; AsyncStorage key @patelrep/copilot_history)
    - apps/mobile/components/shared/tokens.ts (confirm darkTheme has status.dirty or the equivalent red key for the :285 ref; confirm surfaceElevated/glass/glassBorder are darkTheme-only — do NOT add light equivalents)
    - apps/mobile/components/ui/Button.tsx, IconButton (Button.tsx exports), Card.tsx
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`. Do NOT add `useTheme()`. Leave the static `darkTheme` import at :19 and all ~40 `darkTheme.*` refs untouched.
    2. Migrate the SINGLE `C.alert` ref at :285 to the static dark equivalent (`darkTheme.status.dirty`, matching the C.alert→dirty role; confirm the exact darkTheme key during read_first). This is the only `C.` change — the file must reach zero `C.` refs.
    3. Convert the 6 alerts to Toast (all outcome-reporting): :158 → `toast.success(t(...))`, :168 → `toast.success(t(...))`, :178 → `toast.success(t(...))`; :160/:170/:180 error → `toast.error(err.message)` (drop the hardcoded "Error" title). Change ONLY the feedback call in each of the confirmTask/confirmWorkOrder/confirmGuestRequest handlers — do NOT touch the preceding AI/create `await api.*` calls, guards, or tenant args.
    4. Adopt primitives on controls only: icon-only send + mic → `<IconButton>`; quick-action buttons and the confirmCard Create/Dismiss buttons → `<Button>`. The `confirmCard` (:201-242) MAY become `<Card>` + two `<Button>`. Do NOT wrap message bubbles in `Card` (D-09) and do NOT restructure the chat layout.
    5. Do NOT change the AsyncStorage `@patelrep/copilot_history` read/write, the streaming/chat send logic, or the AI api calls.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Zero Alert.alert: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Dark preserved: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'darkTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }; if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 }; if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -Pattern 'IconButton|Button' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - tokens.ts NOT modified (not in files_modified); MobileVisualTokens.test.ts untouched
    - `npm run type-check` + `npm run lint` green
    - Diff review: AsyncStorage history + AI api calls + guards + tenant args unchanged; message bubbles not wrapped in Card
  </acceptance_criteria>
  <done>Copilot dark aesthetic + chat preserved, useTheme NOT added, 1 C. ref → static dark equivalent, 6 alerts → Toast, controls on Button/IconButton, tokens.ts untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → AI/API (create task / WO / guest-request from copilot confirm, hotel_id-scoped) | Role-gated AI-driven mutations; NOT modified — only the 6 feedback calls change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14-01 | Elevation of Privilege / Information Disclosure | Converting a confirm alert drops the AI-driven create `await api.*`, its guard, or tenant scoping | mitigate | Pitfall-3 rule: change ONLY the feedback call; diff review asserts AI/create calls + tenant unchanged |
| T-09-14-02 | Denial of Service | Routing copilot through useTheme() resolves darkTheme-only keys (surfaceElevated/glass/glassBorder) to undefined in light mode → broken screen (Pitfall 4) | mitigate | D-11: no useTheme(); grep asserts useTheme absent + darkTheme present; tokens.ts untouched |
| T-09-14-03 | Tampering | Force-fitting Card onto chat bubbles breaks the chat layout / message rendering | mitigate | D-09: bubbles stay as-is; only confirmCard may become Card; diff review |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/copilot/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -ne 0) { exit 1 }` (dark preserved)
</verification>

<success_criteria>
SCREENS-08 (copilot): dark AI aesthetic + chat behavior preserved; 1 C. ref migrated to the static dark equivalent; 6 alerts → Toast; send/mic/quick-action/confirm controls on Button/IconButton; no tokens.ts change; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-14-SUMMARY.md`.
</output>
