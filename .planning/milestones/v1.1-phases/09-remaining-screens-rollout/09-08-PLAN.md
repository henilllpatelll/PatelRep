---
phase: 09-remaining-screens-rollout
plan: 08
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - apps/mobile/components/supervisor/atoms.tsx
  - apps/mobile/components/supervisor/BroadcastModal.tsx
  - apps/mobile/components/supervisor/DirectMessageModal.tsx
  - apps/mobile/components/supervisor/EndShiftModal.tsx
  - apps/mobile/components/supervisor/ShiftNoteModal.tsx
  - apps/mobile/components/supervisor/HousekeeperPicker.tsx
autonomous: true
requirements: [SCREENS-03]
must_haves:
  truths:
    - "The shared atoms.tsx migrates first; the 5 supervisor modals render via useTheme() + primitives (zero C. refs each)"
    - "All 6 modal Alert.alert calls convert to Toast; the two success→onClose cases fire toast + onClose together"
    - "Each modal's Modal/overlay wrapper + submit/api behavior are unchanged"
  artifacts:
    - path: "apps/mobile/components/supervisor/atoms.tsx"
      provides: "Shared supervisor atoms on primitives"
      contains: "useTheme"
    - path: "apps/mobile/components/supervisor/BroadcastModal.tsx"
      provides: "Broadcast modal on Button + Toast"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/components/supervisor/BroadcastModal.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "submit outcome → toast"
      pattern: "useToast"
---

<objective>
Migrate the 6 remaining `components/supervisor/` files onto primitives (D-07): the shared `atoms.tsx` (235 lines, 21 `C.`, 0 alert — migrate FIRST) plus `BroadcastModal.tsx` (137, 16 `C.`, 2 alerts), `DirectMessageModal.tsx` (118, 11 `C.`, 1 alert), `EndShiftModal.tsx` (139, 16 `C.`, 1 alert), `ShiftNoteModal.tsx` (104, 10 `C.`, 2 alerts), `HousekeeperPicker.tsx` (146, 12 `C.`, 0 alert). All 6 alert calls are outcome-reporting → Toast. Analog: `inspect/index.tsx` (Toast) + `WorkOrderCard.tsx` (Button/color).

Purpose: SCREENS-03 (supervisor modals) — closes the cross-phase supervisor-modal gap; behavior unchanged.
Output: 6 files on primitives, zero `C.` refs each, all alerts → Toast, type-check + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
##          `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## D-10 classification (verified, all → Toast): BroadcastModal:38 success / :40 error (:38 fires onClose together — hardcoded EN "Sent"/"Message queued…"/"Error", pass through as-is); DirectMessageModal:37 error (t("directMessage.sendError")); EndShiftModal:43 error (t("endShift.submitError")); ShiftNoteModal:28 success / :30 error (:28 fires onClose together — hardcoded EN "Saved"/"Shift note logged."/"Error").
## Locked "success → side effect" pattern: `toast.success(msg); onClose();` fired together — do NOT gate onClose behind toast dismissal.
## Pitfall 3: modals mix real submit/api.* logic with the alert — change ONLY the feedback call; never touch the preceding awaited calls, guards, or tenant args. Leave each Modal/overlay wrapper structurally untouched.
## atoms.tsx is SHARED — migrate it FIRST (Task 1); its prop signatures must not change (other supervisor files + RoomDetailSheet import it).
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate shared atoms.tsx first (0 alerts)</name>
  <files>apps/mobile/components/supervisor/atoms.tsx</files>
  <read_first>
    - apps/mobile/components/supervisor/atoms.tsx (full — 235 lines, 21 C. refs, 7 TouchableOpacity, 0 Alert.alert; shared atoms used by the other supervisor files + RoomDetailSheet)
    - apps/mobile/components/engineering/WorkOrderCard.tsx (Button/StatusBadge/color exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` inside each atom component that renders `C.*` (hooks go in component bodies, not module scope).
    2. Replace all 21 `C.*` refs with `theme.*` per map; route any `shellTokens`/`C.shell*` to `theme.shell.*`. If a `C.*` sits in a module-level plain color-map const, source it from the static `lightTheme` export. Keep StyleSheet; color objects LAST.
    3. Convert tappable atoms that are `TouchableOpacity` to `<Button>` where they are semantic buttons; StatusKey-matching chips → `<StatusBadge label={t(...)} />`.
    4. Do NOT change any atom's exported prop signature — every other supervisor file (and RoomDetailSheet in plan 09-07) depends on these unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/atoms.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/atoms.tsx' -Pattern 'useTheme|lightTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: exported atom prop signatures unchanged
  </acceptance_criteria>
  <done>Shared atoms.tsx on primitives, zero C. refs, prop signatures unchanged, type-check + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 4 alert-bearing modals (Broadcast/DirectMessage/EndShift/ShiftNote)</name>
  <files>apps/mobile/components/supervisor/BroadcastModal.tsx, apps/mobile/components/supervisor/DirectMessageModal.tsx, apps/mobile/components/supervisor/EndShiftModal.tsx, apps/mobile/components/supervisor/ShiftNoteModal.tsx</files>
  <read_first>
    - apps/mobile/components/supervisor/BroadcastModal.tsx (137 lines, 16 C., Alert.alert :38 success→onClose / :40 error)
    - apps/mobile/components/supervisor/DirectMessageModal.tsx (118 lines, 11 C., Alert.alert :37 error)
    - apps/mobile/components/supervisor/EndShiftModal.tsx (139 lines, 16 C., Alert.alert :43 error)
    - apps/mobile/components/supervisor/ShiftNoteModal.tsx (104 lines, 10 C., Alert.alert :28 success→onClose / :30 error)
    - apps/mobile/app/(app)/inspect/index.tsx (Phase-8 Toast handler exemplar)
    - apps/mobile/components/ui/Button.tsx, apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    For EACH of the 4 files:
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` and `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace all `C.*` refs with `theme.*` per map. Convert submit/cancel `TouchableOpacity` buttons to `<Button>`. Keep each modal's `Modal`/overlay wrapper structurally untouched.
    3. Convert alerts (all → Toast):
       - BroadcastModal :38 success → `toast.success(msg); onClose();` fired together (hardcoded EN "Sent"/"Message queued…" pass through as-is); :40 error → `toast.error(msg)`.
       - DirectMessageModal :37 error → `toast.error(t("directMessage.sendError"))`.
       - EndShiftModal :43 error → `toast.error(t("endShift.submitError"))`.
       - ShiftNoteModal :28 success → `toast.success(msg); onClose();` together (hardcoded EN "Saved"/"Shift note logged." pass through); :30 error → `toast.error(msg)`.
    4. Change ONLY the feedback call in each handler — do NOT touch the preceding `await api.*`/`enqueueAction` submit logic, guards, or tenant args. Do NOT add new `t()` to the hardcoded-EN Broadcast/ShiftNote strings (not in i18n gate scope — flagged separately).
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs in each: `if (@(Select-String -LiteralPath @('apps/mobile/components/supervisor/BroadcastModal.tsx', 'apps/mobile/components/supervisor/DirectMessageModal.tsx', 'apps/mobile/components/supervisor/EndShiftModal.tsx', 'apps/mobile/components/supervisor/ShiftNoteModal.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Zero Alert.alert in each: `if (@(Select-String -LiteralPath @('apps/mobile/components/supervisor/BroadcastModal.tsx', 'apps/mobile/components/supervisor/DirectMessageModal.tsx', 'apps/mobile/components/supervisor/EndShiftModal.tsx', 'apps/mobile/components/supervisor/ShiftNoteModal.tsx') -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `@('apps/mobile/components/supervisor/BroadcastModal.tsx', 'apps/mobile/components/supervisor/DirectMessageModal.tsx', 'apps/mobile/components/supervisor/EndShiftModal.tsx', 'apps/mobile/components/supervisor/ShiftNoteModal.tsx') | ForEach-Object { if (@(Select-String -LiteralPath $_ -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: submit/api logic + guards + tenant args unchanged; Modal wrappers untouched; onClose fires together with the two success toasts
  </acceptance_criteria>
  <done>4 modals on primitives + Button, zero C. refs, all 6 alerts → Toast (2 success fire onClose together), submit logic untouched, type-check + lint green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate HousekeeperPicker.tsx (pure color swap, 0 alerts)</name>
  <files>apps/mobile/components/supervisor/HousekeeperPicker.tsx</files>
  <read_first>
    - apps/mobile/components/supervisor/HousekeeperPicker.tsx (full — 146 lines, 12 C. refs, 6 TouchableOpacity, 0 Alert.alert)
    - apps/mobile/components/engineering/WorkOrderCard.tsx (Button/StatusBadge/color exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 12 `C.*` refs with `theme.*` per map; route any `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert selectable rows/buttons that are `TouchableOpacity` to `<Button>` or Pressable-wraps-Card as fits; StatusKey-matching chips → `<StatusBadge>`.
    4. Keep the Modal/overlay wrapper untouched. Do NOT change the select/callback prop contract.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/HousekeeperPicker.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/HousekeeperPicker.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/HousekeeperPicker.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0 (had none)
    - `npm run type-check` + `npm run lint` green
    - Diff review: select-callback prop contract unchanged; Modal wrapper untouched
  </acceptance_criteria>
  <done>HousekeeperPicker on primitives, zero C. refs, prop contract unchanged, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (broadcast / direct-message / end-shift / shift-note submits, hotel_id-scoped) | Supervisor-role-gated mutations; NOT modified — only feedback calls change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Elevation of Privilege / Information Disclosure | Converting a modal alert drops the preceding submit `await api.*`/`enqueueAction`, its guard, or tenant scoping | mitigate | Pitfall-3 rule: change ONLY the feedback call; diff review asserts submit logic + tenant args unchanged |
| T-09-08-02 | Tampering | BroadcastModal:38 / ShiftNoteModal:28 lose their `onClose()` side effect when the OK-gated success alert becomes Toast (modal stays open / stale state) | mitigate | Locked pattern: `toast.success(msg); onClose();` fired together; acceptance criteria assert 0 Alert.alert |
| T-09-08-03 | Tampering | atoms.tsx prop-signature change breaks every dependent supervisor file (+ RoomDetailSheet) | mitigate | Task 1 migrates atoms first, prop signatures asserted unchanged; type-check catches breakage |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/components/supervisor/atoms.tsx', 'apps/mobile/components/supervisor/BroadcastModal.tsx', 'apps/mobile/components/supervisor/DirectMessageModal.tsx', 'apps/mobile/components/supervisor/EndShiftModal.tsx', 'apps/mobile/components/supervisor/ShiftNoteModal.tsx', 'apps/mobile/components/supervisor/HousekeeperPicker.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath @('apps/mobile/components/supervisor/BroadcastModal.tsx', 'apps/mobile/components/supervisor/DirectMessageModal.tsx', 'apps/mobile/components/supervisor/EndShiftModal.tsx', 'apps/mobile/components/supervisor/ShiftNoteModal.tsx', 'apps/mobile/components/supervisor/HousekeeperPicker.tsx') -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-03 (supervisor modals, D-07): all 6 supervisor files on primitives; 6 alerts → Toast (2 success fire onClose together); submit/api behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-08-SUMMARY.md`.
</output>
