---
phase: 09-remaining-screens-rollout
plan: 06
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - apps/mobile/app/(app)/scheduling/index.tsx
  - apps/mobile/app/(app)/staff/index.tsx
autonomous: true
requirements: [SCREENS-03]
must_haves:
  truths:
    - "scheduling/index.tsx renders via useTheme() + primitives (zero C. refs)"
    - "staff/index.tsx renders via useTheme() + primitives (zero C. refs)"
    - "Both screens' read/data behavior unchanged; neither has an Alert.alert to convert"
  artifacts:
    - path: "apps/mobile/app/(app)/scheduling/index.tsx"
      provides: "Scheduling screen on primitives"
      contains: "useTheme"
    - path: "apps/mobile/app/(app)/staff/index.tsx"
      provides: "Staff screen on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/scheduling/index.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "theme hook drives colors"
      pattern: "useTheme"
---

<objective>
Migrate two small supervisor-facing read-mostly screens onto primitives: `scheduling/index.tsx` (156 lines, 21 `C.` refs, 0 `Alert.alert`, 0 `t()` — raw literals not gated) and `staff/index.tsx` (158 lines, 17 `C.` refs, 0 `Alert.alert`, partial `t()`). Bundled — both are light. Analog: `work-orders/index.tsx`.

Purpose: SCREENS-03 (scheduling + staff) — render on primitives; data behavior unchanged.
Output: both screens on primitives, zero `C.` refs each, type-check + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}. Card has NO onPress → Pressable wraps Card.
## NOTE: scheduling/staff are NOT in the i18n gate `files` list — do NOT add new t() wiring here (raw literals stay as-is; this is presentation-only migration, not i18n scope).
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate scheduling/index.tsx onto primitives</name>
  <files>apps/mobile/app/(app)/scheduling/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/scheduling/index.tsx (full — 156 lines, 21 C. refs, 0 Alert.alert, read-mostly list)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero/StateBlock exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 21 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error to `<StateBlock>`; tappable rows (if any) → Pressable-wraps-Card; StatusKey-matching chips → `<StatusBadge>`.
    4. Do NOT add new `t()` calls (not in i18n gate scope). Do NOT change data-fetch/read logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/scheduling/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/scheduling/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/scheduling/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: no data-fetch logic changed
  </acceptance_criteria>
  <done>scheduling on primitives, zero C. refs, type-check + lint green, data behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate staff/index.tsx onto primitives</name>
  <files>apps/mobile/app/(app)/staff/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/staff/index.tsx (full — 158 lines, 17 C. refs, 0 Alert.alert, partial t(), read-mostly list)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero/StateBlock exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 17 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error to `<StateBlock>`; tappable rows → Pressable-wraps-Card; StatusKey-matching chips → `<StatusBadge>`.
    4. Do NOT add new `t()` calls beyond what already exists. Do NOT change data-fetch/read logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/staff/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/staff/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/staff/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: no data-fetch logic changed
  </acceptance_criteria>
  <done>staff on primitives, zero C. refs, type-check + lint green, data behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (schedule/staff read) | Supervisor-role-gated reads; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Elevation of Privilege | A styling edit drops a role/tenant-scoped fetch or changes a default-export shape (breaks a supervisor tab) | mitigate | Diff review: only style/color lines change; no file move/rename; type-check green |
| T-09-06-02 | Information Disclosure | Neither screen has an Alert.alert; no handler logic is touched | accept | Low risk — read-only screens, no mutation handlers in scope |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/scheduling/index.tsx', 'apps/mobile/app/(app)/staff/index.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-03 (scheduling + staff): both render on primitives; data behavior unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-06-SUMMARY.md`.
</output>
