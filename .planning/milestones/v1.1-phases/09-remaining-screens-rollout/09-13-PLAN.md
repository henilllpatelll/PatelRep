---
phase: 09-remaining-screens-rollout
plan: 13
type: execute
wave: 5
depends_on: ["10", "11"]
files_modified:
  - apps/mobile/app/(app)/sop/index.tsx
  - apps/mobile/app/(app)/sop/[sopId].tsx
autonomous: true
requirements: [SCREENS-07]
must_haves:
  truths:
    - "sop/index.tsx list + sop/[sopId].tsx detail render via useTheme() + primitives (zero C. refs each)"
    - "SOP fetch/read behavior unchanged; neither file has an Alert.alert to convert"
  artifacts:
    - path: "apps/mobile/app/(app)/sop/index.tsx"
      provides: "SOP list on primitives"
      contains: "useTheme"
    - path: "apps/mobile/app/(app)/sop/[sopId].tsx"
      provides: "SOP detail on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/sop/index.tsx"
      to: "apps/mobile/app/(app)/sop/[sopId].tsx"
      via: "list row navigates to detail (route unchanged)"
      pattern: "sopId"
---

<objective>
Migrate the SOP list+detail pair together (D-02): `sop/index.tsx` (133 lines, 19 `C.` refs, 3 `TouchableOpacity`, 0 `Alert.alert`, 0 `t()`) and `sop/[sopId].tsx` (122 lines, 23 `C.` refs, 0 `Alert.alert`, 0 `t()`). Analogs: `work-orders/index.tsx` (list) + `work-orders/[woId].tsx` (detail).

Purpose: SCREENS-07 (SOP) — list+detail render on primitives; read behavior unchanged.
Output: both files on primitives, zero `C.` refs each, type-check + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}.
## Both files at 0 Alert.alert and 0 t() — no Toast, no new t() (not i18n gate scope). Presentation-only.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sop/index.tsx (list)</name>
  <files>apps/mobile/app/(app)/sop/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/sop/index.tsx (full — 133 lines, 19 C. refs, 3 TouchableOpacity, 0 Alert.alert; list rows navigate to [sopId])
    - apps/mobile/app/(app)/work-orders/index.tsx (list exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 19 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild tappable rows as Pressable-wraps-Card; loading/empty/error → `<StateBlock>`; `RefreshControl tintColor={theme.primaryAction}`.
    4. Do NOT add new `t()`. Do NOT change fetch logic or the navigation route to `[sopId]`.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch/navigation unchanged
  </acceptance_criteria>
  <done>SOP list on primitives, zero C. refs, type-check + lint green, read behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sop/[sopId].tsx (detail)</name>
  <files>apps/mobile/app/(app)/sop/[sopId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/sop/[sopId].tsx (full — 122 lines, 23 C. refs, 0 Alert.alert, 0 t(); fetch-one detail render)
    - apps/mobile/app/(app)/work-orders/[woId].tsx (detail exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 23 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error → `<StateBlock>`; any action controls → `<Button>`.
    4. Do NOT add new `t()`. Do NOT change the fetch-one logic or tenant args — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/[sopId].tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/[sopId].tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/[sopId].tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch-one logic + tenant args unchanged
  </acceptance_criteria>
  <done>SOP detail on primitives, zero C. refs, read logic untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (SOP list + detail read, hotel_id-scoped) | Role-gated reads; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13-01 | Information Disclosure | A styling edit drops the tenant-scoped SOP fetch, showing another hotel's SOPs | mitigate | Presentation-only rule; diff review asserts fetch/tenant args unchanged |
| T-09-13-02 | Tampering | List row Card rebuild drops `onPress`, breaking navigation to detail | mitigate | Pressable-wraps-Card mandated; type-check + manual pass |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/sop/index.tsx', 'apps/mobile/app/(app)/sop/[sopId].tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-07 (SOP): list+detail render on primitives; read behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-13-SUMMARY.md`.
</output>
