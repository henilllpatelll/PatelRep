---
phase: 09-remaining-screens-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/profile/index.tsx
autonomous: true
requirements: [SCREENS-01]
must_haves:
  truths:
    - "Profile screen renders chrome/rows/state via useTheme() + primitives (zero C. refs)"
    - "The sign-out confirmation stays a blocking Alert.alert (genuine confirm before a state change)"
    - "Profile data-fetch, sign-out, and role/language behavior are unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/index.tsx"
      provides: "Profile on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/profile/index.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "theme hook drives all colors"
      pattern: "useTheme"
---

<objective>
Migrate `profile/index.tsx` (539 lines, 24 `C.` refs, 9 `TouchableOpacity`, 1 `Alert.alert` at :163 that STAYS) onto `useTheme()` + the Phase 7 primitives, following the shipped Phase-8 exemplar `work-orders/index.tsx`. Highest daily traffic screen — everyone opens Profile.

Purpose: SCREENS-01 — Profile renders on primitives; sign-out and data behavior unchanged.
Output: `profile/index.tsx` on primitives, zero `C.` refs, sign-out Alert intact, `ProfileHandoff.test.tsx` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — do NOT re-derive; name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (name-change gotchas: caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Pattern (work-orders/index.tsx:240-256): keep StyleSheet.create() for layout; append `{ color: theme.* }` as the LAST element of each style array. Dark hero chrome → theme.shell.bg/.ink/.ink2/.ink3/.surface.
## StateBlock: status="loading"|"empty"|"error"; if onRetry passed, retryLabel is REQUIRED. RefreshControl tintColor={theme.primaryAction}.
## Card has NO onPress → wrap any tappable row in <Pressable>, re-apply asymmetric padding via style (merged last).
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate profile/index.tsx onto primitives (sign-out Alert stays)</name>
  <files>apps/mobile/app/(app)/profile/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/profile/index.tsx (full — 539 lines, 24 C. refs, hero chrome, 9 TouchableOpacity rows, sign-out Alert.alert at :163, language toggle)
    - apps/mobile/components/ui/StateBlock.tsx, Button.tsx, Card.tsx
    - apps/mobile/app/(app)/work-orders/index.tsx (Phase-8 migrated exemplar — hero on theme.shell.*, StateBlock, inline color merge)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` at the top of the component body.
    2. Replace all 24 `C.*` refs with `theme.*` per the map above. Route hero/dark-chrome refs and any `shellTokens`/`C.shell*` to `theme.shell.*`. Keep the existing `StyleSheet.create()`; add `{ color: theme.* }` / `{ backgroundColor: theme.* }` objects as the LAST element of each `style` array.
    3. Convert the 9 action/setting rows that are `TouchableOpacity` to either keep them as-is (if a plain pressable row is idiomatic) OR wrap a `Card`-styled row in `<Pressable>` re-applying its asymmetric padding via `style`. Convert any primary/secondary buttons (e.g. sign-out button, edit) to `<Button variant=... size=... />`.
    4. Convert loading/empty/error states (if any) to `<StateBlock>` reusing existing `t(...)` keys.
    5. LEAVE the sign-out confirm at :163 as `Alert.alert` — it is a genuine blocking confirm (title + Cancel/Confirm) before a state change (D-10 classification). Do NOT convert it to Toast.
    6. Do NOT change the profile fetch, sign-out handler body, language-toggle logic, or role display — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/ProfileHandoff.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - Sign-out Alert intact: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 1) { exit 1 }` exits 0
    - `npx jest __tests__/screens/ProfileHandoff.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: no fetch/sign-out-handler/language-toggle/role-display logic changed
  </acceptance_criteria>
  <done>Profile renders on primitives, zero C. refs, sign-out Alert stays blocking, ProfileHandoff test + type-check + lint green, behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (profile fetch, sign-out) | Auth/session state change on sign-out; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Restyling accidentally converts the sign-out confirm to Toast, letting sign-out fire without confirmation | mitigate | Explicit rule: :163 Alert stays; acceptance criteria assert exactly 1 Alert.alert remains |
| T-09-01-02 | Elevation of Privilege | A "styling-only" edit drops a role/language display guard or changes the default-export shape (breaks Profile tab via roleTabs.ts) | mitigate | Diff review: only style/color lines change; ProfileHandoff test asserts render; no file move/rename |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/ProfileHandoff.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/profile/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 1) { exit 1 }`
</verification>

<success_criteria>
SCREENS-01: Profile renders on the new primitives; sign-out confirm stays blocking; data/role/language behavior unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-01-SUMMARY.md`.
</output>
