---
phase: 09-remaining-screens-rollout
plan: 15
type: execute
wave: 7
depends_on: ["14"]
files_modified:
  - apps/mobile/app/(app)/alerts/index.tsx
  - apps/mobile/app/(app)/notifications/index.tsx
autonomous: true
requirements: [SCREENS-09]
must_haves:
  truths:
    - "alerts/index.tsx renders via useTheme() + primitives (zero C. refs)"
    - "notifications/index.tsx renders via useTheme() + primitives (zero C. refs)"
    - "Both screens' read/mark-read behavior unchanged; neither has an Alert.alert to convert"
  artifacts:
    - path: "apps/mobile/app/(app)/alerts/index.tsx"
      provides: "Alerts screen on primitives"
      contains: "useTheme"
    - path: "apps/mobile/app/(app)/notifications/index.tsx"
      provides: "Notifications screen on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/alerts/index.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "theme hook drives colors"
      pattern: "useTheme"
---

<objective>
Migrate the two notification-family screens onto primitives: `alerts/index.tsx` (177 lines, 20 `C.` refs, 0 `TouchableOpacity`, 0 `Alert.alert`) and `notifications/index.tsx` (152 lines, 17 `C.` refs, 3 `TouchableOpacity`, 0 `Alert.alert`, 0 `t()`). Analog: `work-orders/index.tsx`.

Purpose: SCREENS-09 (alerts + notifications) — render on primitives; data behavior unchanged.
Output: both screens on primitives, zero `C.` refs each, type-check + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}.
## Both files at 0 Alert.alert — no Toast conversion. notifications has 0 t() — do NOT add new t() (not i18n gate scope). Presentation-only.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate alerts/index.tsx onto primitives</name>
  <files>apps/mobile/app/(app)/alerts/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/alerts/index.tsx (full — 177 lines, 20 C. refs, 0 TouchableOpacity, 0 Alert.alert)
    - apps/mobile/app/(app)/work-orders/index.tsx (list/StateBlock exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 20 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error → `<StateBlock>`; severity/status chips that match a StatusKey → `<StatusBadge label={t(...)} />`; non-matching chips color-migrated only. `RefreshControl tintColor={theme.primaryAction}`.
    4. Do NOT change fetch/mark-read logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/alerts/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/alerts/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/alerts/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch/mark-read logic unchanged
  </acceptance_criteria>
  <done>alerts on primitives, zero C. refs, type-check + lint green, data behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate notifications/index.tsx onto primitives</name>
  <files>apps/mobile/app/(app)/notifications/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/notifications/index.tsx (full — 152 lines, 17 C. refs, 3 TouchableOpacity, 0 Alert.alert, 0 t())
    - apps/mobile/app/(app)/work-orders/index.tsx (list/StateBlock exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 17 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild tappable notification rows as Pressable-wraps-Card; loading/empty/error → `<StateBlock>`; StatusKey chips → `<StatusBadge>`. `RefreshControl tintColor={theme.primaryAction}`.
    4. Do NOT add new `t()`. Do NOT change fetch/mark-read logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/notifications/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/notifications/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/notifications/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch/mark-read logic unchanged
  </acceptance_criteria>
  <done>notifications on primitives, zero C. refs, type-check + lint green, data behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (alerts/notifications read + mark-read, hotel_id-scoped) | Role-gated reads/mutations; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15-01 | Information Disclosure | A styling edit drops the tenant-scoped fetch, surfacing another hotel's alerts/notifications | mitigate | Presentation-only rule; diff review asserts fetch/tenant args unchanged |
| T-09-15-02 | Tampering | Notification row Card rebuild drops `onPress`, breaking mark-read/navigation | mitigate | Pressable-wraps-Card mandated; type-check + manual pass |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/alerts/index.tsx', 'apps/mobile/app/(app)/notifications/index.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-09 (alerts + notifications): both render on primitives; read/mark-read behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-15-SUMMARY.md`.
</output>
