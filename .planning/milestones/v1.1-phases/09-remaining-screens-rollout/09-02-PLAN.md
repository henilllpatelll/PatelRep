---
phase: 09-remaining-screens-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/home/index.tsx
  - apps/mobile/components/home/CompanionHome.tsx
autonomous: true
requirements: [SCREENS-02]
must_haves:
  truths:
    - "home/index.tsx renders the Housekeeper default + inline FrontDesk/GM dashboards via useTheme() + primitives (zero C. refs)"
    - "CompanionHome.tsx (FocusCard/ShiftMosaic/SignalChips) renders via primitives (zero C. refs)"
    - "Role-conditional rendering + default-export shape are unchanged (roleTabs.ts untouched, no file move)"
  artifacts:
    - path: "apps/mobile/app/(app)/home/index.tsx"
      provides: "Home/dashboard router on primitives"
      contains: "useTheme"
    - path: "apps/mobile/components/home/CompanionHome.tsx"
      provides: "Housekeeper companion widgets on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/home/index.tsx"
      to: "apps/mobile/components/home/CompanionHome.tsx"
      via: "housekeeper path renders CompanionHome (props unchanged)"
      pattern: "CompanionHome"
---

<objective>
Migrate the housekeeper/front-desk/GM home surface onto primitives: `home/index.tsx` (570 lines, 31 `C.` refs — `HousekeeperHomeScreen` default export + inline `FrontDeskHomeScreen`:248 + `GMHomeScreen`:300; 0 `Alert.alert`) and its housekeeper-path companion `CompanionHome.tsx` (337 lines, 12 `C.` refs — exports `FocusCard`/`ShiftMosaic`/`SignalChips`, in scope per D-05). Part of the SCREENS-02 dashboard family (all 5 migrate in wave 1).

Purpose: SCREENS-02 (home + companion) — daily-traffic dashboards on primitives; role-gating unchanged.
Output: both files on primitives, zero `C.` refs, `HousekeeperHome.test.tsx` green, D-06 kept (no inline extraction).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; append `{ color: theme.* }` LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; layout overrides via style (last). StatusBadge label is caller-translated.
## Static-object escape hatch (WorkOrderCard.tsx:27-40): if a color map is consumed as a plain object (no hook), source from static `lightTheme` export (values identical this milestone) — use for any CATEGORY_META-style const in CompanionHome.

## D-05: CompanionHome's OWN FocusCard/ShiftMosaic/SignalChips ARE in scope. D-13/anti-pattern: do NOT migrate evening.tsx composites (StatusPill/StatusRail/ProgressBar/Chip/AIBriefingCard/SectionHeader) — screens keep calling them unchanged.
## D-06: keep FrontDeskHomeScreen/GMHomeScreen INLINE in home/index.tsx — no extraction. Pitfall 8: no file move, no default-export shape change, no role-guard drop; roleTabs.ts frozen.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CompanionHome.tsx (FocusCard/ShiftMosaic/SignalChips)</name>
  <files>apps/mobile/components/home/CompanionHome.tsx</files>
  <read_first>
    - apps/mobile/components/home/CompanionHome.tsx (full — 337 lines, 12 C. refs, exports FocusCard/ShiftMosaic/SignalChips; note which C. refs are inside plain color-map consts vs render)
    - apps/mobile/components/engineering/WorkOrderCard.tsx (Phase-8 exemplar — Pressable-wraps-Card, StatusBadge, static-lightTheme escape hatch at :27-40)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` inside each exported component that renders `C.*` (FocusCard/ShiftMosaic/SignalChips).
    2. Replace all 12 `C.*` refs with `theme.*` per the map. If any `C.*` lives in a module-level plain color-map const (no hook available), source it from the static `lightTheme` export instead (WorkOrderCard.tsx:27-40 pattern).
    3. If FocusCard/ShiftMosaic tiles are tappable and currently a single `TouchableOpacity`, wrap the `Card` in `<Pressable>` and re-apply asymmetric padding via `style`. Chips that match a StatusKey → `<StatusBadge>` with caller-translated label; non-matching chips stay hand-rolled, color-migrated only.
    4. Do NOT migrate any imported `evening.tsx` composite — only CompanionHome's own exports. Do NOT change component prop signatures (home/index.tsx calls them unchanged).
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/components/home/CompanionHome.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/home/CompanionHome.tsx' -Pattern 'useTheme|lightTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/home/CompanionHome.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: FocusCard/ShiftMosaic/SignalChips export signatures unchanged
  </acceptance_criteria>
  <done>CompanionHome's own widgets on primitives, zero C. refs, exports unchanged, type-check + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate home/index.tsx (Housekeeper default + inline FrontDesk/GM)</name>
  <files>apps/mobile/app/(app)/home/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/home/index.tsx (full — 570 lines, 31 C. refs, HousekeeperHomeScreen default export + FrontDeskHomeScreen:248 + GMHomeScreen:300 inline; imports CompanionHome/SupervisorHome/EngineerHome widgets)
    - apps/mobile/app/(app)/work-orders/index.tsx (Phase-8 hero/StateBlock exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` inside EACH of the three screen functions that render `C.*` (HousekeeperHomeScreen, FrontDeskHomeScreen, GMHomeScreen) — a hook must be called inside a component body, so add it per-function, not module-scope.
    2. Replace all 31 `C.*` refs with `theme.*` per the map; route dark hero chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; inline color objects last.
    3. Convert loading/empty/error blocks to `<StateBlock>` and primary/secondary buttons to `<Button>`. Tappable Card rows → Pressable-wraps-Card.
    4. D-06: keep FrontDeskHomeScreen/GMHomeScreen INLINE — do NOT extract to new files. Do NOT change the default export (`HousekeeperHomeScreen`), the role-branch that selects which screen renders, or the imported widget call sites (CompanionHome props unchanged from Task 1).
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/HousekeeperHome.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/home/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/home/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Inline screens kept: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/home/index.tsx' -Pattern 'FrontDeskHomeScreen|GMHomeScreen' -ErrorAction Stop).Count -lt 2) { exit 1 }` exits 0 (still defined inline)
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/home/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0 (family has none)
    - `npx jest __tests__/screens/HousekeeperHome.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: default-export shape + role-branch + widget props unchanged; no file move/rename
  </acceptance_criteria>
  <done>home/index.tsx three screens on primitives, zero C. refs, FrontDesk/GM stay inline, HousekeeperHome test + type-check + lint green, role-gating unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role → rendered dashboard (roleTabs.ts string-matched routing) | Which dashboard a role sees; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation of Privilege | A "styling-only" edit drops/reorders a role-conditional branch, showing the wrong role's dashboard (Pitfall 8 peaks here) | mitigate | Diff review: only style/color lines change; role-branch + default export asserted unchanged; roleTabs.ts untouched |
| T-09-02-02 | Tampering | Extracting or moving FrontDesk/GM inline screens changes the module/route shape and breaks a role tab | mitigate | D-06 mandates no extraction; grep asserts both still defined inline; no file move |
| T-09-02-03 | Denial of Service | CompanionHome hook added at module scope (invalid — hooks only in component bodies) causes a runtime crash | mitigate | Action mandates per-function hook placement; type-check + HousekeeperHome render test catch it |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/HousekeeperHome.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/home/index.tsx', 'apps/mobile/components/home/CompanionHome.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-02 (home + companion half): housekeeper/front-desk/GM dashboards + companion widgets render on primitives; role-gating and default-export shape unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-02-SUMMARY.md`.
</output>
