---
phase: 09-remaining-screens-rollout
plan: 16
type: execute
wave: 7
depends_on: ["14"]
files_modified:
  - apps/mobile/app/(app)/room-status/index.tsx
autonomous: true
requirements: [SCREENS-10]
must_haves:
  truths:
    - "room-status/index.tsx renders hero/list/room-cards via useTheme() + primitives (zero C. refs)"
    - "The 2 action-sheets at :311 (OOO-reason) and :330 (room actions) stay Alert.alert; the 3 error alerts (:286/:298/:433) become toast.error"
    - "The rendered CreateWorkOrderModal (Phase-8-migrated) still integrates correctly (D-04 verification, not re-migration)"
    - "OOO/create-WO api behavior + tenant scoping are unchanged (only feedback calls change)"
  artifacts:
    - path: "apps/mobile/app/(app)/room-status/index.tsx"
      provides: "Room Status screen on primitives + Toast"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/room-status/index.tsx"
      to: "apps/mobile/components/engineering/CreateWorkOrderModal.tsx"
      via: "renders the Phase-8-migrated modal (integration intact — D-04)"
      pattern: "CreateWorkOrderModal"
---

<objective>
Migrate `room-status/index.tsx` (576 lines, 44 `C.` refs, 12 `TouchableOpacity`, 5 `Alert.alert`) onto primitives, and verify (D-04) the `CreateWorkOrderModal` it renders (already Phase-8-migrated) still integrates. Two action-sheets STAY (`:311` OOO-reason 3-way, `:330` room-actions menu); three error alerts CONVERT (`:286`/`:298`/`:433`). Split into 2 tasks (chrome/cards, then alerts + D-04 verify). Analog: `work-orders/index.tsx` + `inspect/index.tsx`.

Purpose: SCREENS-10 (room status) — renders on primitives; OOO/create-WO behavior + modal integration unchanged.
Output: screen on primitives, zero `C.` refs, 2 sheets intact, 3 errors → Toast, `RoomStatusList.test.tsx` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
##          `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## D-10 classification (verified): :311 OOO-reason sub-menu (Cancel/No reason/Add reason) STAYS · :330 room action sheet (Create WO / OOO options / Cancel) STAYS · :286 oooError / :298 removeOooError / :433 oooError → toast.error.
## Pitfall 3: the OOO / create-WO handlers mix real api.* logic with the alert — change ONLY the feedback call; never touch preceding awaited calls, guards, or tenant args.
## D-04: CreateWorkOrderModal is already migrated (Phase 8) — do NOT re-migrate it. Only verify the integration still works after theming the parent. Room cards (:274) are single TouchableOpacity → Pressable-wraps-Card.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate room-status hero + room cards + StateBlock (44 C. burn-down)</name>
  <files>apps/mobile/app/(app)/room-status/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/room-status/index.tsx (full — 576 lines, 44 C. refs; hero chrome, filter/segment controls, room cards at :274 (single TouchableOpacity), loading/empty/error blocks; renders CreateWorkOrderModal)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero/StateBlock exemplar), apps/mobile/components/engineering/WorkOrderCard.tsx (Pressable-wraps-Card)
    - apps/mobile/components/ui/StateBlock.tsx
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace `C.*` refs (hero, filters, room cards, chips) with `theme.*` per map — including the status name-changes (`C.caution`→`theme.status.pickup`, `C.ooo`→`theme.status.outOfOrder`, `C.alert`→`theme.status.dirty`, `C.info`→`theme.status.clean`). Route dark hero + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild the room cards at :274 (single `TouchableOpacity`) as `<Pressable onPress={...}><Card style={[styles.cardLayoutOverrides]}>…</Card></Pressable>` re-applying asymmetric padding via style. Room-status chips → `<StatusBadge statusKey=... label={t(...)} />` (ready/clean/dirty/occupied/pickup/outOfOrder per the room's status).
    4. Convert loading/empty/error → `<StateBlock>`; `RefreshControl tintColor={theme.primaryAction}`.
    5. Leave the 5 `Alert.alert` handlers and the CreateWorkOrderModal integration for Task 2. Do NOT change fetch/filter/RBAC logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomStatusList.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `@('useTheme', 'StateBlock') | ForEach-Object { if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch $_ -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `@('<Card', 'Pressable') | ForEach-Object { if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch $_ -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npx jest __tests__/screens/RoomStatusList.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: fetch/filter/RBAC unchanged; alert handlers untouched (Task 2)
  </acceptance_criteria>
  <done>Room Status hero/cards/state on primitives, room cards on Pressable-wraps-Card + StatusBadge, RoomStatusList test + type-check + lint green; alerts deferred to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Finish C. burn-down + alert reclassification + D-04 modal verification</name>
  <files>apps/mobile/app/(app)/room-status/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/room-status/index.tsx (Alert.alert at :286 oooError, :298 removeOooError, :311 OOO-reason sub-menu, :330 room action sheet, :433 oooError; the OOO/create-WO handler bodies; the CreateWorkOrderModal render site + its open/onClose props)
    - apps/mobile/components/engineering/CreateWorkOrderModal.tsx (already Phase-8-migrated — READ ONLY for D-04 verification; do NOT edit)
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace ALL remaining `C.*` refs with `theme.*` so the file reaches zero.
    3. Alerts (D-10): LEAVE `Alert.alert` at :311 (OOO-reason 3-way: Cancel / No reason / Add reason) and :330 (room actions menu: Create WO / OOO options / Cancel) unchanged — both are multi-way choice sheets. Convert :286 oooError → `toast.error(msg)`, :298 removeOooError → `toast.error(msg)`, :433 oooError → `toast.error(msg)` using the existing translated strings. Change ONLY those three feedback calls — do NOT touch the preceding OOO set/remove `await api.*` calls, guards, or tenant args.
    4. D-04 verification (verify, do NOT modify): confirm the `CreateWorkOrderModal` render site still passes the same `open`/`onClose`/room props it did before, and that opening it from the :330 sheet's "Create WO" path still renders/functions. The modal file itself stays untouched.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomStatusList.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Sheets intact: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 2) { exit 1 }` exits 0 (only :311 + :330)
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch 'CreateWorkOrderModal' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0 (render site preserved)
    - CreateWorkOrderModal.tsx NOT in this plan's diff (D-04 verify only)
    - `npx jest __tests__/screens/RoomStatusList.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: OOO set/remove api calls + guards + tenant args unchanged; modal props unchanged
  </acceptance_criteria>
  <done>Room Status fully on primitives, zero C. refs, :311/:330 sheets stay blocking, 3 oooError → toast.error, CreateWorkOrderModal integration verified intact (unmodified), RoomStatusList test + type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (set/remove OOO, create-WO from room sheet, hotel_id-scoped) | Role-gated mutations; NOT modified — only :286/:298/:433 feedback calls change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16-01 | Elevation of Privilege / Information Disclosure | Converting an oooError alert drops the OOO set/remove `await api.*`, its guard, or tenant scoping in the shared handler | mitigate | Pitfall-3 rule: change ONLY the three feedback calls; diff review asserts api/guard/tenant unchanged |
| T-09-16-02 | Tampering | The :311 OOO-reason or :330 room-actions choice sheet is converted to Toast, losing the multi-way gate before a state change | mitigate | D-10: both stay Alert.alert; acceptance criteria assert exactly 2 Alert.alert remain |
| T-09-16-03 | Tampering | Theming the parent breaks the CreateWorkOrderModal integration (changed props / render site) | mitigate | D-04 verify-only: modal untouched, render-site props asserted unchanged, RoomStatusList test + manual create-WO pass |
| T-09-16-04 | Tampering | Room card rebuild drops `onPress`, making room cards non-interactive | mitigate | Pressable-wraps-Card mandated; grep asserts Pressable + Card; RoomStatusList test exercises cards |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/RoomStatusList.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/room-status/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 2) { exit 1 }`
- Manual: open Create Work Order from the migrated room-status sheet — modal renders/functions (D-04)
</verification>

<success_criteria>
SCREENS-10 (room status, D-04): renders on primitives; :311/:330 action sheets stay blocking; 3 oooError → toast.error; CreateWorkOrderModal integration intact (unmodified); OOO/create-WO behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-16-SUMMARY.md`.
</output>
