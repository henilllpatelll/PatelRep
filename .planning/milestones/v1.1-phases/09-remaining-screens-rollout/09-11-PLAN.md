---
phase: 09-remaining-screens-rollout
plan: 11
type: execute
wave: 4
depends_on: ["09"]
files_modified:
  - apps/mobile/app/(app)/lost-found/index.tsx
autonomous: true
requirements: [SCREENS-05]
must_haves:
  truths:
    - "lost-found/index.tsx renders via useTheme() + primitives (zero C. refs)"
    - "The 1 log-item error alert at :99 becomes toast.error"
    - "Log-item api behavior + tenant scoping are unchanged (only the feedback call changes)"
  artifacts:
    - path: "apps/mobile/app/(app)/lost-found/index.tsx"
      provides: "Lost & Found screen on primitives + Toast"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/lost-found/index.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "log-item error → toast.error"
      pattern: "useToast"
---

<objective>
Migrate `lost-found/index.tsx` (290 lines, 42 `C.` refs, 11 `TouchableOpacity`, 1 `Alert.alert` → Toast, 0 `t()`) onto primitives. The :99 hardcoded-EN error ("Error"/"Could not log item. Try again.") converts to `toast.error`. Analog: `work-orders/index.tsx` + `inspect/index.tsx` (Toast).

Purpose: SCREENS-05 (lost & found) — renders on primitives; log-item behavior unchanged.
Output: screen on primitives, zero `C.` refs, :99 → toast.error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
##          `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## D-10 classification (verified): lost-found:99 error → toast.error. Hardcoded EN string passes through as-is (not in i18n gate scope — do NOT add new t()).
## Pitfall 3: the log-item handler mixes real api.* logic with the alert — change ONLY the feedback call; never touch the preceding awaited call, guards, or tenant args.
## Card has NO onPress → Pressable wraps Card; StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}.
## Note: lost-found renders FoundItemModal (already Phase-8-migrated) — do NOT re-touch it; only migrate lost-found/index.tsx's own C. refs.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate lost-found/index.tsx (1 alert → Toast)</name>
  <files>apps/mobile/app/(app)/lost-found/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/lost-found/index.tsx (full — 290 lines, 42 C. refs, 11 TouchableOpacity, Alert.alert :99; the log-item handler body; renders FoundItemModal)
    - apps/mobile/app/(app)/work-orders/index.tsx (list exemplar), apps/mobile/app/(app)/inspect/index.tsx (Toast exemplar)
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` and `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace all 42 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild tappable rows as Pressable-wraps-Card; StatusKey chips → `<StatusBadge>`; buttons → `<Button>`; loading/empty/error → `<StateBlock>`. `RefreshControl tintColor={theme.primaryAction}`.
    4. Alert: :99 error → `toast.error(msg)`, passing the existing hardcoded EN string through as-is (do NOT add t()). Change ONLY the feedback call — do NOT touch the log-item `await api.*` call, guards, or tenant args.
    5. Do NOT re-touch `FoundItemModal` (already migrated in Phase 8) — only migrate this screen's own refs.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Zero Alert.alert: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 }; if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: log-item api call + guards + tenant args unchanged; FoundItemModal untouched
  </acceptance_criteria>
  <done>lost-found on primitives, zero C. refs, :99 error → toast.error, log-item logic + FoundItemModal untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (log-found-item, hotel_id-scoped) | Role-gated mutation; NOT modified — only the :99 feedback call changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11-01 | Elevation of Privilege / Information Disclosure | Converting the :99 alert drops the log-item `await api.*` call, its guard, or tenant scoping | mitigate | Pitfall-3 rule: change ONLY the feedback call; diff review asserts api/guard/tenant unchanged |
| T-09-11-02 | Tampering | Re-touching the already-migrated FoundItemModal introduces a regression to Phase-8 work | mitigate | Explicit rule: FoundItemModal untouched; diff review confirms no change to that file |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/lost-found/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-05 (lost & found): renders on primitives; :99 error → toast.error; log-item behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-11-SUMMARY.md`.
</output>
