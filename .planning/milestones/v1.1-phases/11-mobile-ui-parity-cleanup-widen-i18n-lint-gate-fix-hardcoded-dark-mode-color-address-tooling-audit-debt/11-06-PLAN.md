---
phase: 11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt
plan: 06
type: execute
wave: 5
depends_on: ["11-01", "11-03", "11-04", "11-05"]
files_modified:
  - apps/mobile/eslint.config.mjs
autonomous: true
must_haves:
  truths:
    - "eslint.config.mjs's i18next/no-literal-string files list now includes all 16 previously-uncovered directories (profile, home, assignments, scheduling, staff, assets, pm-schedules, guest-requests, lost-found, logbook, sop, copilot, alerts/notifications, components/supervisor, components/home) alongside the original 10"
    - "npm run lint passes with the gate active across the full widened file set, with zero i18next/no-literal-string violations"
    - "markupOnly:true and the jsx-attributes include list are unchanged — only the files array grew"
  artifacts:
    - path: "apps/mobile/eslint.config.mjs"
      provides: "Widened files glob array covering all Phase-9-migrated directories"
      contains: "app/(app)/profile/**"
  key_links:
    - from: "apps/mobile/eslint.config.mjs"
      to: "the 16 newly-gated directories"
      via: "files glob array now includes them; npm run lint enforces i18next/no-literal-string on all of them"
      pattern: "no-literal-string"
---

<objective>
Widen `apps/mobile/eslint.config.mjs`'s `i18next/no-literal-string` gate to cover all remaining Phase-9-migrated directories, now that `11-03`, `11-04`, and `11-05` have wired every one of the 52 violations that a widened gate would surface. This is the final step in Phase 11's i18n-gate-widening work — it runs last (and depends on all 3 literal-fixing plans plus `11-01`, which shares the same locale files) so the gate is never enabled while known violations still exist.

Purpose: ROADMAP success criterion 1 (completes it — full app-wide i18n-gate coverage).
Output: `eslint.config.mjs`'s `files` array widened by 16 new globs; `npm run lint` green across the whole app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-CONTEXT.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-RESEARCH.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-03-SUMMARY.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-04-SUMMARY.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-05-SUMMARY.md
@apps/mobile/eslint.config.mjs

<current_config>
## Current gated `files` array (apps/mobile/eslint.config.mjs:26-37) — verified at plan-drafting time:
```js
files: [
  'components/ui/**/*.{ts,tsx,js,jsx}',
  'app/(app)/my-rooms/**/*.{ts,tsx,js,jsx}',
  'app/(app)/room-board/**/*.{ts,tsx,js,jsx}',
  'app/(app)/room-status/**/*.{ts,tsx,js,jsx}',
  'app/(app)/work-orders/**/*.{ts,tsx,js,jsx}',
  'app/(app)/tasks/**/*.{ts,tsx,js,jsx}',
  'app/(app)/inspect/**/*.{ts,tsx,js,jsx}',
  'components/housekeeping/**/*.{ts,tsx,js,jsx}',
  'components/engineering/**/*.{ts,tsx,js,jsx}',
  'components/tasks/**/*.{ts,tsx,js,jsx}',
],
```
This is the block to extend — do not create a second `files`-scoped rule block, do not touch `markupOnly: true` or the `jsx-attributes.include` list a few lines below it (`['aria-label', 'placeholder', 'title']`) — CONTEXT and research are explicit that widening WHAT the rule checks is out of scope; only widening WHICH directories it checks is in scope.

## The 16 new globs to add (verified directory names against the actual `app/(app)/` and `components/` tree):
```
'app/(app)/profile/**/*.{ts,tsx,js,jsx}',
'app/(app)/home/**/*.{ts,tsx,js,jsx}',
'app/(app)/assignments/**/*.{ts,tsx,js,jsx}',
'app/(app)/scheduling/**/*.{ts,tsx,js,jsx}',
'app/(app)/staff/**/*.{ts,tsx,js,jsx}',
'app/(app)/assets/**/*.{ts,tsx,js,jsx}',
'app/(app)/pm-schedules/**/*.{ts,tsx,js,jsx}',
'app/(app)/guest-requests/**/*.{ts,tsx,js,jsx}',
'app/(app)/lost-found/**/*.{ts,tsx,js,jsx}',
'app/(app)/logbook/**/*.{ts,tsx,js,jsx}',
'app/(app)/sop/**/*.{ts,tsx,js,jsx}',
'app/(app)/copilot/**/*.{ts,tsx,js,jsx}',
'app/(app)/alerts/**/*.{ts,tsx,js,jsx}',
'app/(app)/notifications/**/*.{ts,tsx,js,jsx}',
'components/supervisor/**/*.{ts,tsx,js,jsx}',
'components/home/**/*.{ts,tsx,js,jsx}',
```
Note: `app/(app)/alerts/` and `app/(app)/notifications/` are BOTH real, separate route directories (confirmed at plan-drafting time) — they are different screens (`alerts/index.tsx` is the AI risk-alerts list, already fully i18n-wired with zero gate violations; `notifications/index.tsx` is the push-notification list whose on-screen title happens to also read "Alerts" — its 2 violations were wired in `11-04`). Add globs for BOTH directories.

8 of these 16 (`home`, `staff`, `assets`, `pm-schedules`, `logbook`, `copilot`, `alerts`, `components/home`) produced ZERO violations in the research's widened-config dry run — adding their globs is a pure config change requiring no accompanying literal-fixing work. The other 8 (`profile`, `assignments`, `scheduling`, `guest-requests`, `lost-found`, `sop`, `notifications`, `components/supervisor`) had violations, all now wired by `11-03`/`11-04`/`11-05`.
</current_config>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the files array + confirm npm run lint is fully green</name>
  <files>apps/mobile/eslint.config.mjs</files>
  <read_first>
    - apps/mobile/eslint.config.mjs:26-58 (the exact block to extend; re-read fresh in case anything shifted since plan-drafting)
  </read_first>
  <action>
    Add all confirmed-real globs from the 16 listed above to the existing `files` array in the SAME rule block (`eslint.config.mjs:26-37`) — do not create a new object in the config array, do not touch `markupOnly: true`, do not touch the `jsx-attributes` include list, do not touch the top-level `ignores` block.

    After editing, run `cd apps/mobile && npm run lint`. This MUST pass with zero violations, since `11-01`, `11-03`, `11-04`, and `11-05` have already wired every literal the widened gate would flag. If it does NOT pass:
    - If the failure is in one of the 12 files handled by `11-03`/`11-04`/`11-05`, this means either those plans didn't fully complete, or the live code has drifted since those plans' SUMMARYs were written (re-run the fix for whatever's flagged — do not just re-add an `ignores` entry to route around it, that would defeat this plan's purpose).
    - If the failure is a genuinely NEW violation not enumerated in research (e.g. code changed between research and execution), wire it the same way (add an EN/ES key, use `t()`), following the interpolation/plural idioms established in the sibling plans.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with the gate active on all previously-exempt directories
    - `if (@(Select-String -LiteralPath 'apps/mobile/eslint.config.mjs' -SimpleMatch 'markupOnly: true' -ErrorAction Stop).Count -ne 1) { exit 1 }` exits 0 (PowerShell) — unchanged
    - `if (@(Select-String -LiteralPath 'apps/mobile/eslint.config.mjs' -SimpleMatch "app/(app)/profile/**" -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0 (PowerShell) — confirms at least one new glob landed
    - `npm run type-check` still passes (config-only change shouldn't affect this, but confirm no regression)
  </acceptance_criteria>
  <done>All 16 confirmed-real directory globs added to the existing gated files array; markupOnly and jsx-attributes untouched; npm run lint green across the fully-widened gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Lint gate → CI | There is no mobile CI job today (`.github/workflows/ci.yml` has zero mobile-related jobs, confirmed via research) — this gate is enforced only when someone runs `npm run lint` locally. Do not claim this widened gate is "CI-enforced" in any verification note; it isn't. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-11-06-01 | Tampering | Widening the gate before all violations are fixed would make `npm run lint` red, potentially tempting a future editor to silently re-add `ignores` entries to route around it | mitigate | This plan's `depends_on` ensures all 3 literal-fixing plans complete first; the task's own acceptance criteria forbid adding new `ignores` entries as a workaround |
| T-11-06-02 | Repudiation | A future contributor assumes this gate is CI-enforced and skips local lint before pushing | accept | Documented in this plan's threat model and SUMMARY as a known, pre-existing limitation (not introduced by this phase, no CI-wiring item in Phase 11's locked scope) |
</threat_model>

<verification>
- `cd apps/mobile && npm run lint` exits 0
- `cd apps/mobile && npm run type-check` exits 0
- `eslint.config.mjs` diff shows only `files` array additions — no rule-config changes
</verification>

<success_criteria>
ROADMAP success criterion 1 fully satisfied: the `i18next/no-literal-string` gate covers all Phase-9-migrated directories (profile, home, assignments, scheduling, staff, assets, pm-schedules, guest-requests, lost-found, logbook, sop, copilot, alerts/notifications, components/supervisor, components/home), not just the original 10. `npm run lint` is green across the fully-widened gate.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-06-SUMMARY.md`.
</output>
