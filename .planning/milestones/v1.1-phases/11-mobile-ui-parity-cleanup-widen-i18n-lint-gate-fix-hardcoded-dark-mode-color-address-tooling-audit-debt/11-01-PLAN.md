---
phase: 11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(app)/home/index.tsx
  - apps/mobile/components/housekeeping/FoundItemModal.tsx
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/__tests__/components/FoundItemModal.test.tsx
autonomous: true
must_haves:
  truths:
    - "The AI-sparkles icon on the home screen renders with theme.ai.primary (adapts correctly in light and dark mode), not a hardcoded hex value"
    - "Submitting a found item while the API call fails shows the user a Toast error instead of silently leaving the modal in a stuck state"
    - "Spanish users see a translated placeholder in the work-orders search field instead of the English defaultValue fallback"
    - "A new automated test proves FoundItemModal's failure path calls toast.error, closing the coverage gap noted in Phase 11 research"
  artifacts:
    - path: "apps/mobile/app/(app)/home/index.tsx"
      provides: "sparkles icon color sourced from theme.ai.primary"
      contains: "theme.ai.primary"
    - path: "apps/mobile/components/housekeeping/FoundItemModal.tsx"
      provides: "catch block calls toast.error(t(\"foundItem.submitError\"))"
      contains: "foundItem.submitError"
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "foundItem.submitError + workOrders.searchPlaceholder keys"
      contains: "searchPlaceholder"
    - path: "apps/mobile/i18n/locales/es.json"
      provides: "matching ES keys (parity)"
      contains: "searchPlaceholder"
    - path: "apps/mobile/__tests__/components/FoundItemModal.test.tsx"
      provides: "test asserting toast.error fires on submission failure"
      contains: "toast"
  key_links:
    - from: "apps/mobile/app/(app)/work-orders/index.tsx:275"
      to: "apps/mobile/i18n/locales/es.json"
      via: "t(\"workOrders.searchPlaceholder\") resolves to a real ES value instead of falling back to defaultValue"
      pattern: "searchPlaceholder"
    - from: "apps/mobile/components/housekeeping/FoundItemModal.tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "toast.error(t(\"foundItem.submitError\")) resolves a real key"
      pattern: "foundItem\\.submitError"
---

<objective>
Close 3 of Phase 11's 5 tech-debt items in one pass — the ones that are small, independently verified, exact diffs with zero interdependency on the i18n-gate-widening work: (1) replace the hardcoded `#CBB8F0` AI-sparkles color in `home/index.tsx:212` with the semantic `theme.ai.primary` token used by every other "sparkles" icon in the app, (2) fix `FoundItemModal.tsx`'s empty catch block to surface a Toast error (mirroring the sibling `SupplyRequestModal.tsx` pattern) plus add the missing test coverage, (3) add the missing `workOrders.searchPlaceholder` locale key so Spanish users stop seeing an English fallback.

Purpose: ROADMAP success criteria 2, 3, 4 for Phase 11.
Output: home/index.tsx themed correctly in light/dark, FoundItemModal fails loudly instead of silently, workOrders search placeholder translated, new test file closing a real coverage gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-CONTEXT.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-RESEARCH.md

<verified_current_code>
## home/index.tsx:212 (current, buggy) — `theme` already in scope via useTheme() used throughout this component:
```tsx
<Ionicons name="sparkles" size={12} color="#CBB8F0" />
```
Fix: `<Ionicons name="sparkles" size={12} color={theme.ai.primary} />` — same-line swap, no new import. `theme.ai.primary` = `#7C3AED` (light) / `#A78BFA` (dark), defined in `components/shared/tokens.ts:54-70`.

## FoundItemModal.tsx:99-128 (current, buggy) — `t`, `toast` already imported/in-scope (lines 12, 35, 38):
```tsx
async function handleSubmit() {
  if (!description.trim()) return;
  if (!isOnline) {
    toast.error(t("foundItem.offlineError"));
    return;
  }
  setSubmitting(true);
  try {
    let photoUrl: string | undefined;
    if (photoUri) {
      const url = await uploadPhoto(photoUri);
      if (url) photoUrl = url;
    }
    await createLostFoundItem({ description: description.trim(), room_id: roomId, location_found: `Room ${roomNumber}`, photo_url: photoUrl });
    resetForm();
    onClose();
  } catch {
    // keep modal open on error
  } finally {
    setSubmitting(false);
  }
}
```
Fix the catch block only: `} catch { toast.error(t("foundItem.submitError")); } finally { setSubmitting(false); }` — mirrors `SupplyRequestModal.tsx`'s `toast.error(...)` idiom, using this file's own established translated-key convention (`foundItem.offlineError` already exists at this call site).

## work-orders/index.tsx:275 (current, already correct, zero code change needed):
```tsx
placeholder={t("workOrders.searchPlaceholder", { defaultValue: "Search work orders…" })}
```
The `defaultValue` means the app doesn't crash — it silently falls back to English for Spanish users. Fix is additive-only: add the key to both locale files.

## Locale insertion points (verified):
- `en.json:470` opens `"workOrders": {`; `en.json:481` has `"locationPlaceholder": "Room number or area (e.g. 204, Lobby HVAC)",` — add `"searchPlaceholder": "Search work orders…"` in this object.
- `en.json:760` opens `"foundItem": {`; `en.json:769` has `"offlineError": "Connect to the internet to report a found item.",` — add `"submitError": "Could not submit found item. Try again."` in this object.
- Mirror both additions in `es.json`'s matching `workOrders`/`foundItem` objects.
</verified_current_code>

<test_template>
## Test template — `__tests__/components/ReportIssueModal.test.tsx` (render pattern) + `__tests__/screens/LostFoundScreen.test.tsx` (toast-on-failure assertion pattern)

`ReportIssueModal.test.tsx` shows the render/mock shape for a housekeeping modal: mocks `@/stores/appStore` (`useAppStore` selector pattern for `isOnline`), renders wrapped in `<ThemeProvider>`, uses `getByTestId`/`fireEvent`.

`LostFoundScreen.test.tsx` shows the toast-on-failure assertion shape (real i18n, not mocked — asserts the actual resolved English string):
```tsx
jest.mock("@/lib/theme/useToast", () => ({ useToast: () => ({ error: mockToastError }) }));
...
mockCreateLostFoundItem.mockRejectedValue(new Error("Network unavailable"));
...
await waitFor(() => expect(mockToastError).toHaveBeenCalledWith("Could not log item. Try again."));
```
No test file exists today for `FoundItemModal.tsx` — it is only referenced as a full mock-out in `__tests__/screens/RoomDetail.test.tsx:57`. Follow this exact combined shape: `ThemeProvider` wrapper (per ReportIssueModal.test.tsx) + toast-mock + rejected-promise assertion (per LostFoundScreen.test.tsx), asserting on the real resolved string `"Could not submit found item. Try again."` (matching the EN key added in Task 1) — do NOT mock `react-i18next` to identity, since this test should prove the real key resolves through the real i18n resources.
</test_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI color token fix + locale key additions (workOrders.searchPlaceholder + foundItem.submitError)</name>
  <files>apps/mobile/app/(app)/home/index.tsx, apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <read_first>
    - apps/mobile/app/(app)/home/index.tsx:200-216 (confirm `theme` from `useTheme()` is in scope in this component — it is, used at :206 for `theme.shell.line`/`theme.shell.surface`)
    - apps/mobile/i18n/locales/en.json:470-482 (workOrders namespace), :760-772 (foundItem namespace)
    - apps/mobile/i18n/locales/es.json (same two namespaces — confirm equivalent line ranges before editing)
  </read_first>
  <action>
    1. In `home/index.tsx:212`, replace `color="#CBB8F0"` with `color={theme.ai.primary}`. Do not touch anything else on that line or in the surrounding TouchableOpacity — this is a same-line value swap only.
    2. In `en.json`'s `workOrders` object, add `"searchPlaceholder": "Search work orders…"` (reuse the existing ellipsis character style already used by `locationPlaceholder` if present, otherwise plain `...`).
    3. In `es.json`'s `workOrders` object, add `"searchPlaceholder": "Buscar órdenes de trabajo…"`.
    4. In `en.json`'s `foundItem` object, add `"submitError": "Could not submit found item. Try again."` (matches the app-wide `<namespace>.submitError` idiom already used by `inspect.submitError` and the logbook family).
    5. In `es.json`'s `foundItem` object, add `"submitError": "No se pudo reportar el objeto. Intenta de nuevo."`.
    Do not reorder or reformat any other keys in either locale file — additive edits only.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const en=require('./i18n/locales/en.json'),es=require('./i18n/locales/es.json');const miss=['workOrders.searchPlaceholder','foundItem.submitError'].filter(k=>{const p=k.split('.');return !p.reduce((a,x)=>a&&a[x],en)||!p.reduce((a,x)=>a&&a[x],es)});if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node parity script above prints "parity ok" and exits 0
    - `home/index.tsx` contains `theme.ai.primary` and no longer contains `#CBB8F0`
    - Both locale files remain valid JSON (the node require in the verify script would throw otherwise)
  </acceptance_criteria>
  <done>home/index.tsx's sparkles icon uses theme.ai.primary; both new locale keys exist in EN+ES at parity.</done>
</task>

<task type="auto">
  <name>Task 2: FoundItemModal catch-block fix + new test</name>
  <files>apps/mobile/components/housekeeping/FoundItemModal.tsx, apps/mobile/__tests__/components/FoundItemModal.test.tsx</files>
  <read_first>
    - apps/mobile/components/housekeeping/FoundItemModal.tsx:1-50,95-128 (confirm `t`/`toast` imports and scope; the offlineError call site at :102 shows the exact `t()`/`toast.error()` idiom to mirror)
    - apps/mobile/components/housekeeping/SupplyRequestModal.tsx:56-73 (the sibling toast-on-catch pattern this fix mirrors)
    - apps/mobile/__tests__/components/ReportIssueModal.test.tsx (render/mock shape template)
    - apps/mobile/__tests__/screens/LostFoundScreen.test.tsx (toast-on-failure assertion shape template)
  </read_first>
  <action>
    In `FoundItemModal.tsx`'s `handleSubmit()`, change ONLY the empty catch block:
    ```
    } catch {
      // keep modal open on error
    } finally {
    ```
    to:
    ```
    } catch {
      toast.error(t("foundItem.submitError"));
    } finally {
    ```
    Do not touch the try block's upload/create logic, `resetForm()`/`onClose()` calls on success, or the offline-check branch above it.

    Then create `apps/mobile/__tests__/components/FoundItemModal.test.tsx`: render `<FoundItemModal visible roomId="room-1" roomNumber="204" onClose={jest.fn()} />` wrapped in `<ThemeProvider>` (per ReportIssueModal.test.tsx's shape), mock `@/lib/api/lostFound`'s `createLostFoundItem` to reject with `new Error("Network unavailable")`, mock `@/lib/theme/useToast` to expose a `mockToastError` jest.fn() (per LostFoundScreen.test.tsx's shape), fill the description field, press submit, and assert `mockToastError` was called with `"Could not submit found item. Try again."` (the real EN string from Task 1's new key — do not mock react-i18next to identity, this test should exercise the real resolved translation). Also assert the modal does NOT call `onClose` on failure (stays open), proving the "keep modal open on error" comment's original intent still holds even though the empty catch is now filled in.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npx jest __tests__/components/FoundItemModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/components/FoundItemModal.test.tsx` passes
    - `npm run type-check` passes
    - Diff review: only the catch block changed in FoundItemModal.tsx — no other line touched
  </acceptance_criteria>
  <done>FoundItemModal's catch block surfaces a Toast error via the new i18n key; a new passing test proves the failure path fires toast.error and does not close the modal.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npm run type-check` green
- `cd apps/mobile && npx jest __tests__/components/FoundItemModal.test.tsx` passes
- EN/ES parity node-script exits 0 for both new keys
- Manual/visual: home screen sparkles icon still renders (color changes subtly — theme.ai.primary is close in hue to the old hardcoded value), in both light and dark mode via the existing theme toggle
</verification>

<success_criteria>
ROADMAP criteria 2, 3, 4 satisfied: the AI-sparkles color is a semantic theme.ai.* token (light+dark), FoundItemModal surfaces a Toast on submission failure instead of silently swallowing it (with new test coverage), and workOrders.searchPlaceholder exists in both locale files so Spanish users no longer see the English fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-01-SUMMARY.md`.
</output>
</output>
