---
phase: 11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/components/supervisor/BroadcastModal.tsx
  - apps/mobile/components/supervisor/ShiftNoteModal.tsx
  - apps/mobile/components/supervisor/atoms.tsx
  - apps/mobile/app/(app)/profile/index.tsx
  - apps/mobile/app/(app)/assignments/index.tsx
autonomous: true
must_haves:
  truths:
    - "All 9 raw JSX-text literals across the 3 supervisor components + profile/index.tsx + assignments/index.tsx are wired through t()"
    - "Every new EN key has a matching ES key (parity)"
    - "No behavior change: broadcast-send, shift-note-submit, profile sign-out, and assignments load-balancing logic is untouched"
  artifacts:
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "New supervisorTools namespace + profile.appVersion + assignments.vipBadge keys"
      contains: "supervisorTools"
  key_links:
    - from: "apps/mobile/components/supervisor/BroadcastModal.tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "t() call resolves supervisorTools.* key"
      pattern: "t\\(\"supervisorTools\\."
    - from: "apps/mobile/app/(app)/assignments/index.tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "t() call resolves assignments.vipBadge key"
      pattern: "assignments\\.vipBadge"
---

<objective>
Wire real `t()` calls for the final 9 raw-literal violations that surface once the ESLint gate widens to `components/supervisor/**`, `app/(app)/profile/**`, and `app/(app)/assignments/**` — the last of the 12 files with actual violations found by Phase 11 research's widened-config ESLint run. This is the last i18n-content plan in the wave-chain before `11-06` widens the gate config itself.

Purpose: ROADMAP success criterion 1 (final slice — completes literal-fixing for all 12 violating files across the 16 newly-gated directories; the remaining 8 directories are zero-violation, pure config additions handled entirely in 11-06).
Output: 5 files wired to `t()`, `supervisorTools` namespace added + 1 key each added to the existing `profile`/`assignments` namespaces, all at EN/ES parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-CONTEXT.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-RESEARCH.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-04-SUMMARY.md

<i18n_reference>
## Namespace notes
- `profile` (en.json:701) and `assignments` (en.json:961) already exist as top-level objects (used elsewhere in these same files, e.g. `t("profile.account")`, `t("profile.signOut")`) — just ADD one new key to each, do not create new namespaces for these two.
- No top-level `supervisorTools` namespace exists today. There IS an existing `home.supervisor: {...}` object (dashboard-card labels like `shiftNote: "Shift Note"`, `messageTeam: "Message Team"` — these are the button labels that OPEN these modals from the home screen, a different concern from the modal's own internal copy). Do not nest new keys inside `home.supervisor` — create a new top-level `supervisorTools` namespace for the modal-internal copy instead, to keep dashboard-label strings and modal-content strings clearly separated.

## Out-of-scope module-level strings — do not touch (mirrors the exact ReportIssueModal/SupplyRequestModal precedent from research's Pitfall 1)
`BroadcastModal.tsx`'s `QUICK_MESSAGES` const array (4 quick-message strings), and both modals' `toast.success(...)`/`toast.error((err as Error).message ?? "...")` string arguments, are NOT JSX text nodes or gated attributes — the widened gate does not flag them (confirmed pattern: `ReportIssueModal.tsx` has identical un-flagged `label=`/toast-string cases today in an already-gated directory). Wiring these would be scope creep beyond what CONTEXT and the widened gate actually require — leave them as raw strings, exactly as the sibling `ReportIssueModal.tsx`/`SupplyRequestModal.tsx` do today.
</i18n_reference>

<literals>
## components/supervisor/BroadcastModal.tsx — 2 violations:
- :57 `<Text ...>Message Team</Text>` — title
- :59 `<Text ...>Sends to all housekeeping staff on shift</Text>` — subtitle
No `useTranslation` import exists — add it (import already has `useToast`, mirror that import line's placement).

## components/supervisor/ShiftNoteModal.tsx — 2 violations:
- :47 `<Text ...>Shift Note</Text>` — title
- :49 `<Text ...>Logged to the shift logbook — visible to all supervisors</Text>` — subtitle
No `useTranslation` import exists — add it.

## components/supervisor/atoms.tsx — 3 violations (verify exact line numbers during read_first — approximate ±1 line from research's citation, content is what matters):
- ~:155 `<Text ...>⚠ Over</Text>` — over-assigned badge (keep the ⚠ glyph inside the translated string, same treatment as `assignments.vipBadge` below)
- ~:159 `<Text ...>{load.done}/{load.total}</Text>` — NOT a violation (pure expression, no literal text) — re-confirm during read_first which of the two nearby Text nodes is actually flagged before editing
- ~:171-172 `<Text ...>In {activeRoom.roomNumber}</Text>` — "In " prefix (interpolation)
Check whether `useTranslation` is already imported in this file (it exports shared atoms/sub-components used by supervisor screens) — add it if missing.

## app/(app)/profile/index.tsx — 1 violation:
- :408 `<Text ...>PatelRep v{appVersion}</Text>` — "PatelRep v" prefix (interpolation). This file ALREADY has `useTranslation`/`t` in scope (used at :395/:399 for `profile.account`/`profile.signOut`) — no new import needed.

## app/(app)/assignments/index.tsx — 1 violation:
- :379 `<Text ...>★ VIP</Text>` — combined glyph+word text node. This file ALREADY has `useTranslation`/`t` in scope (imported :14, used :58/:561) — no new import needed. Split into a non-translatable decorative glyph + a translated word: `{"★ "}{t("assignments.vipBadge")}` — the bare "★ " text child (symbol only, no letters) is not gate-flagged (confirmed pattern: the 09-00 precedent left a lone `*` character untouched because the rule doesn't flag punctuation/symbol-only text nodes), while `{t(...)}` is an expression, not a literal, so it's never flagged either.
</literals>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add supervisorTools namespace + profile.appVersion + assignments.vipBadge keys</name>
  <files>apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <read_first>
    - apps/mobile/i18n/locales/en.json:701-720 (profile namespace — find insertion point), :961-980 (assignments namespace — find insertion point)
    - apps/mobile/i18n/locales/en.json — grep `"supervisorTools":` and `"home"` → `"supervisor":` to reconfirm the namespace-separation reasoning above still holds
    - apps/mobile/i18n/locales/es.json — same three checks
  </read_first>
  <action>
    1. Add a new top-level namespace to BOTH en.json and es.json:
    ```jsonc
    "supervisorTools": {
      "messageTeamTitle": "Message Team",
      "messageTeamSub": "Sends to all housekeeping staff on shift",
      "shiftNoteTitle": "Shift Note",
      "shiftNoteSub": "Logged to the shift logbook — visible to all supervisors",
      "overBadge": "⚠ Over",
      "inRoomPrefix": "In {{room}}"
    }
    ```
    ES: `messageTeamTitle: "Mensaje al equipo"`, `messageTeamSub: "Se envía a todo el personal de limpieza en turno"`, `shiftNoteTitle: "Nota de turno"`, `shiftNoteSub: "Registrado en el libro de turno — visible para todos los supervisores"`, `overBadge: "⚠ Exceso"`, `inRoomPrefix: "En {{room}}"`.

    2. Add ONE key to the EXISTING `profile` object in both files: `"appVersion": "PatelRep v{{version}}"` (ES: same — version strings/app name are typically not translated, but keep the key structure consistent).

    3. Add ONE key to the EXISTING `assignments` object in both files: `"vipBadge": "VIP"` (ES: `"VIP"` — VIP is an accepted loanword in Spanish hospitality contexts, same value both languages is correct here, not a missed translation).
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const en=require('./i18n/locales/en.json'),es=require('./i18n/locales/es.json');const keys=['supervisorTools.messageTeamTitle','supervisorTools.overBadge','supervisorTools.inRoomPrefix','profile.appVersion','assignments.vipBadge'];const get=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o);const miss=keys.filter(k=>!get(en,k)||!get(es,k));if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node parity script prints "parity ok" and exits 0
    - Both files remain valid JSON
    - `profile` and `assignments` existing objects gained exactly one new key each (no existing key removed or renamed)
  </acceptance_criteria>
  <done>supervisorTools namespace + profile.appVersion + assignments.vipBadge exist in both locale files at EN/ES parity.</done>
</task>

<task type="auto">
  <name>Task 2: Wire t() across the 5 files (9 literals)</name>
  <files>apps/mobile/components/supervisor/BroadcastModal.tsx, apps/mobile/components/supervisor/ShiftNoteModal.tsx, apps/mobile/components/supervisor/atoms.tsx, apps/mobile/app/(app)/profile/index.tsx, apps/mobile/app/(app)/assignments/index.tsx</files>
  <read_first>
    - apps/mobile/components/supervisor/BroadcastModal.tsx:1-60 (confirm no useTranslation import; confirm QUICK_MESSAGES const + toast strings are NOT touched)
    - apps/mobile/components/supervisor/ShiftNoteModal.tsx:1-50 (confirm no useTranslation import)
    - apps/mobile/components/supervisor/atoms.tsx:140-180 (re-confirm exact line numbers + which Text nodes are the actual 3 violations vs. pure-expression Text nodes)
    - apps/mobile/app/(app)/profile/index.tsx:390-410 (confirm `t` already in scope)
    - apps/mobile/app/(app)/assignments/index.tsx:370-382 (confirm `t` already in scope)
    - apps/mobile/i18n/locales/en.json (confirm Task 1's keys exist)
  </read_first>
  <action>
    BroadcastModal.tsx: add `import { useTranslation } from "react-i18next";` + `const { t } = useTranslation();`. Wire:
    - :57 → `{t("supervisorTools.messageTeamTitle")}`
    - :59 → `{t("supervisorTools.messageTeamSub")}`
    Leave `QUICK_MESSAGES`, `toast.success(...)`, and `toast.error(...)` strings untouched — not gate-flagged, out of scope.

    ShiftNoteModal.tsx: add `import { useTranslation } from "react-i18next";` + `const { t } = useTranslation();`. Wire:
    - :47 → `{t("supervisorTools.shiftNoteTitle")}`
    - :49 → `{t("supervisorTools.shiftNoteSub")}`
    Leave `toast.success(...)`/`toast.error(...)` strings and the `placeholder="e.g. VIP arrival in 201..."` untouched (placeholder attrs are not gate-flagged per the established codebase pattern — confirmed in research).

    atoms.tsx: add `useTranslation` import/hook if missing. Wire the confirmed "⚠ Over" text to `{t("supervisorTools.overBadge")}` and the confirmed "In {room}" text to `{t("supervisorTools.inRoomPrefix", { room: activeRoom.roomNumber })}`. Do NOT touch `{load.done}/{load.total}` (pure expression, not a literal).

    profile/index.tsx :408 → `{t("profile.appVersion", { version: appVersion })}`

    assignments/index.tsx :379 → `{"★ "}{t("assignments.vipBadge")}` (two separate JSX children replacing the single `★ VIP` text node — the glyph stays as a bare symbol-only text child, which is not gate-flagged)

    Change ONLY the literal→t() swaps and needed import/hook additions. Do NOT alter `send()`/`submit()` handlers, `signOut`, AI-balance-suggestion logic, or any theme.*/styling in these 5 files.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` passes
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/supervisor/BroadcastModal.tsx' -SimpleMatch 't("supervisorTools' -ErrorAction Stop).Count -lt 2) { exit 1 }` exits 0 (PowerShell)
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/assignments/index.tsx' -SimpleMatch 't("assignments.vipBadge")' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0 (PowerShell)
    - Diff review: no `send`/`submit`/`signOut`/AI-suggestion logic line changed in any of the 5 files
  </acceptance_criteria>
  <done>All 9 literals resolve through t() with Task-1 keys; type-check green; eslint.config.mjs NOT yet modified (deferred to 11-06). This closes out literal-fixing for all 52 violations across all 12 files found by research.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npm run type-check` green
- EN/ES parity node-script exits 0
- Manual diff review confirms zero logic-line changes across the 5 files
- All 52 violations across all 12 files (this plan + 11-03 + 11-04) are now wired — ready for 11-06 to widen the gate config
</verification>

<success_criteria>
Final 9 of 52 total gate-widening violations resolved (supervisor components + profile + assignments); supervisorTools namespace + 2 reused-namespace keys at EN/ES parity; zero behavior change. All literal-fixing work for Phase 11's i18n-gate widening is now complete.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-05-SUMMARY.md`.
</output>
