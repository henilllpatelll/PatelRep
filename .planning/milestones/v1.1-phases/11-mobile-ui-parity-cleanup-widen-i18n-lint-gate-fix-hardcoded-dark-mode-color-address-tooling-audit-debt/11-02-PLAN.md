---
phase: 11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
autonomous: true
must_haves:
  truths:
    - "npm audit --audit-level=high advisory count in apps/mobile is reduced via a safe (non --force) fix"
    - "Every advisory deliberately left open is documented with reasoning, not silently dropped"
    - "The dependency change (it touches expo/babel-preset-expo/expo-updates at patch level) passes a green EAS build before being considered mergeable, per CLAUDE.md's fragile-pipeline rule"
    - "No React Native / Expo / Jest / ESLint major version bump was introduced"
  artifacts:
    - path: "apps/mobile/package-lock.json"
      provides: "Safe npm audit fix applied (patch/minor bumps only, no --force)"
  key_links:
    - from: "apps/mobile/package.json"
      to: "EAS build"
      via: "eas build --platform android --profile preview proves the bumped expo/babel-preset-expo/expo-updates trio still builds"
      pattern: "expo"
---

<objective>
Review the live `npm audit --audit-level=high` advisories in `apps/mobile` and remediate what's safe, per CLAUDE.md's existing fragile-pipeline discipline ("zero new npm dependencies planned by design; any exception requires a green EAS build before merging"). Apply the safe `npm audit fix` (no `--force`), verify the app still type-checks and builds, and explicitly document why the remaining advisories that require a major `expo@57.0.9` bump are deliberately left open this phase.

Purpose: ROADMAP success criterion 5.
Output: `package-lock.json` updated with the safe fix, a green EAS build (or documented equivalent per the open-question resolution below) proving the touched `expo`/`babel-preset-expo`/`expo-updates` patch bumps are safe, and a documented decision for the advisories left open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-CONTEXT.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-RESEARCH.md

<audit_state>
## IMPORTANT: re-run `npm audit --audit-level=high --json` yourself before acting — do not trust these numbers, the advisory database changes independent of any code change (confirmed twice already: milestone-audit said 58/42/1, this plan's own drafting-time re-check confirmed 27 total: 1 critical, 10 high, 15 moderate, 1 low). Use whatever your own fresh run reports as the source of truth in your SUMMARY.

As of plan-drafting time, confirmed via `npm audit fix --dry-run` (no `--force`):
- Safe fix resolves: 6 HIGH + 1 LOW + 1 MODERATE (the `expo-dev-launcher` chain)
- Safe fix touches (patch/minor only): `expo 54.0.35→54.0.36`, `babel-preset-expo 54.0.11→54.0.12`, `expo-updates 29.0.18→29.0.19`, plus `brace-expansion`, `js-yaml`, `undici`, `shell-quote`, `form-data`, `fast-uri`, `@babel/core`, `postcss` (the @expo/metro-config-bundled copy), `semver`, `nanoid`, `hasown`, `regjsparser`, `lightningcss`, and several `@expo/*` packages (`schema-utils`, `package-manager`, `osascript`, `metro-config`, `env`, `config-plugins`, `config`, `cli`), `@0no-co/graphql.web` — all within the same major line (added 13, removed 2, changed 45 packages).
- Remaining after the safe fix (NOT resolvable without `expo@57.0.9`, `isSemVerMajor: true`): 1 CRITICAL (`tar`, via `@expo/cli`/`@expo/metro-config`) + 4 HIGH (`@expo/cli`, `@expo/metro-config`, `expo` itself, `postcss` — the top-level-override node, different from the safe-fix's bundled copy) + 14 MODERATE (the entire `@expo/*`/`expo-*` family: `@expo/config`, `@expo/config-plugins`, `@expo/prebuild-config`, `expo-asset`, `expo-constants`, `expo-dev-client`, `expo-linking`, `expo-manifests`, `expo-notifications`, `expo-splash-screen`, `expo-updates`, `jest-expo`, `uuid`, `xcode`).
- `tar`'s vulnerability (arbitrary file write via hardlink/symlink during archive extraction) is only reachable via `@expo/cli`/`@expo/metro-config` at `expo start`/`expo prebuild`/build-time template extraction — never inside the shipped React Native JS bundle. This is the reasoning to document for leaving it open (do not silently drop it — CONTEXT explicitly requires documenting deliberately-open advisories).

## EAS build gate — resolved (research's Open Question 1)
`eas whoami` was confirmed authenticated at plan-drafting time (logged in as `henilllpatelll` / `henillpatel2004@gmail.com`). Per research's own recommendation ("escalate to a real EAS cloud build only if eas whoami shows authentication available"), this plan requires attempting a REAL EAS cloud build, not just a local dry-run substitute. If `eas whoami` fails when you run it (session expired), fall back to local `npm run type-check` + `npx expo export --platform android` as the minimum gate and document why the cloud build was skipped.
</audit_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply safe npm audit fix + re-verify counts</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <read_first>
    - Run `cd apps/mobile && npm audit --audit-level=high --json` fresh — record the live before-counts (do not assume the numbers in this plan's context are still current)
    - Run `cd apps/mobile && npm audit fix --dry-run` — confirm which advisories the safe fix resolves vs. which remain `isSemVerMajor: true`
  </read_first>
  <action>
    Run `cd apps/mobile && npm audit fix` (no `--force` flag — this is a hard constraint per CONTEXT and CLAUDE.md's fragile-pipeline rule; do NOT run `npm audit fix --force` or manually bump `expo` to `57.0.9` or any other major version).

    After the fix, run `npm audit --audit-level=high --json` again and record the after-counts. Confirm no package outside the safe-fix's own dependency resolution was manually touched (i.e., you ran exactly one command, no manual `package.json` edits).

    Do NOT attempt to resolve the remaining critical/high/moderate advisories that require `expo@57.0.9` — this is explicitly out of scope per CONTEXT (skip any major version bump touching Expo/RN/Jest/ESLint core packages).
  </action>
  <verify>
    <automated>cd apps/mobile && npm audit --audit-level=high --json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json`'s dependency version ranges for `expo`/`react`/`react-native`/`jest`/`eslint` are unchanged (only `package-lock.json` resolved versions moved, within existing semver ranges)
    - The after-fix `npm audit` JSON shows a reduced total advisory count vs. the before-fix run (exact delta recorded in the plan's SUMMARY, not hardcoded here since live numbers drift)
    - No `git diff apps/mobile/package.json` shows a changed version range (only `package-lock.json` should show substantive changes)
  </acceptance_criteria>
  <done>Safe npm audit fix applied; before/after advisory counts recorded from live output (not assumed); no major-version/`--force` bump introduced.</done>
</task>

<task type="auto">
  <name>Task 2: EAS build gate + document remaining open advisories</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
    1. Run `cd apps/mobile && npm run type-check` — must pass (proves the dependency bump didn't break TypeScript compilation).
    2. Run `cd apps/mobile && npx eas whoami` to confirm EAS CLI authentication is still available in this environment.
       - If authenticated: run `npx eas build --platform android --profile preview --non-interactive --no-wait` to queue a real cloud build against the bumped `expo`/`babel-preset-expo`/`expo-updates`. Capture the build ID from the output, then poll `npx eas build:view <build-id> --json` (or `eas build:list --limit 1 --json`) periodically until the build's status is `finished` or `errored`. A `finished` status satisfies the CLAUDE.md/CONTEXT "green EAS build before merging" gate for this dependency change. If it errors, investigate and fix before considering this task done — do not merge a red build.
       - If NOT authenticated (session expired): fall back to `npx expo export --platform android` (a local bundling dry-run) as the minimum verification, and explicitly document in the SUMMARY that the full cloud-build gate was skipped due to missing EAS auth in this environment, per the research's own documented fallback.
    3. In this plan's own SUMMARY.md (created at the end per the `<output>` instruction), write a short "npm audit remediation" section documenting: the exact before/after advisory counts (from Task 1), the exact list of advisories left open (1 critical `tar` + however many high/moderate remain after your fresh run), and the reasoning for leaving them open — specifically that `tar`'s exploitation path runs only through `@expo/cli`/`@expo/metro-config` at build/dev time (`expo start`/`expo prebuild`), never inside the shipped React Native JS bundle, so it is not a runtime-exposed risk to end users of the published app, and that resolving it requires the explicitly-out-of-scope `expo@57.0.9` major bump. This satisfies CONTEXT's "document any advisory deliberately left open with reasoning" instruction — no separate doc file needed, the SUMMARY is the record.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` passes after the dependency bump
    - Either a `finished` EAS build result is captured, or a documented, reasoned fallback (missing auth) is recorded in the SUMMARY
    - SUMMARY.md contains a clear "advisories left open + reasoning" section — not a silent drop
  </acceptance_criteria>
  <done>Dependency bump verified safe via type-check + (EAS build or documented fallback); remaining advisories documented with reasoning in SUMMARY.md.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npm audit --audit-level=high --json` re-run, after-counts recorded and compared to the before-counts
- `cd apps/mobile && npm run type-check` green
- EAS build result (`finished`) captured, or documented fallback reasoning if auth was unavailable at execution time
- No `--force` used; no major version bump to `expo`/`react-native`/`jest`/`eslint` introduced
</verification>

<success_criteria>
ROADMAP criterion 5 satisfied: the live npm audit advisory count is reduced via the safe, non-force fix; the dependency change (which does touch `expo`/`babel-preset-expo`/`expo-updates` at patch level) is verified via a green EAS build (or documented fallback); advisories deliberately left open (the `expo@57.0.9`-only-resolvable set) are documented with reasoning rather than silently dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-02-SUMMARY.md`.
</output>
