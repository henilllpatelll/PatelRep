---
phase: 07-theme-foundation-primitives
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/mobile/components/ui/Button.tsx
  - apps/mobile/components/shared/mobileHandoff.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Button renders primary/secondary/ghost/destructive variants and sm/md/lg sizes"
    - "Every Button size has minHeight ≥ 44 (touch-target floor)"
    - "Button loading state shows a spinner in place of the label with no layout shift"
    - "IconButton resolves its tone colors via useTheme() instead of the static C constant, structure unchanged"
    - "All 13 IconButton tones resolve to byte-identical light-mode values (no visible regression on the 8 existing call sites)"
    - "IconButton accepts an optional accessibilityLabel prop (no existing call site broken)"
  artifacts:
    - path: "apps/mobile/components/ui/Button.tsx"
      provides: "General-purpose Button primitive (4 variants, 3 sizes, loading)"
      exports: ["Button"]
      contains: "ActivityIndicator"
    - path: "apps/mobile/components/shared/mobileHandoff.tsx"
      provides: "IconButton theme-wired to useTheme() + optional accessibilityLabel"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/components/ui/Button.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "useTheme() for variant colors"
      pattern: "useTheme"
    - from: "apps/mobile/components/shared/mobileHandoff.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "useTheme() replaces static C in IconButton tone resolution"
      pattern: "useTheme"
---

<objective>
Create the general-purpose `Button` primitive (`components/ui/Button.tsx`) with the four variants and three sizes locked in CONTEXT.md, plus a no-layout-shift loading state — and theme-wire the existing `IconButton` (`mobileHandoff.tsx`) so its tone colors come from `useTheme()` instead of the frozen `C` snapshot, without changing its structure or size surface.

Purpose: Satisfies UI-01. `HeroButton` is explicitly left untouched (D-05) — `Button` is a separate, general-purpose primitive. No call site adopts either yet.
Output: new `apps/mobile/components/ui/Button.tsx`; in-place theme-wire of `IconButton` inside `apps/mobile/components/shared/mobileHandoff.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-theme-foundation-primitives/07-CONTEXT.md
@.planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md
@.planning/phases/07-theme-foundation-primitives/07-PATTERNS.md

<interfaces>
`useTheme()` from `@/lib/theme/useTheme` (plan 01) returns the light theme object (byte-identical to the frozen `C` snapshot in light mode). Keys needed here (verified against `apps/mobile/components/shared/tokens.ts`):
- Surfaces/text/border: `surface` (#FFFDFC), `surfaceMuted` (#F0EBE1), `border` (#E4D6C4), `textPrimary` (#2C2621), `textSecondary` (#766D63).
- Accent (green): `primary` (#2F5D50), `primaryAction` (#4F7A5A), `primarySoft` (#E0EBE1), `primaryLine` (#B9CFBC).
- Status contract `theme.status.*` (= `statusTokens`): `ready` (#0E7468), `readySoft` (#D7EDE7), `readyLine` (#A7D2C9), `clean` (#2F6F95), `cleanSoft` (#DDEAF1), `cleanLine` (#ACC9DB), `dirty` (#A9363F), `dirtySoft` (#F6DDE0), `dirtyLine` (#E7A9B0), `occupied` (#A9363F), `pickup` (#B7791F), `pickupSoft` (#F7E8C8), `pickupLine` (#E4C174), `outOfOrder` (#746D63), `outOfOrderSoft` (#EDE7DD), `outOfOrderLine` (#D5CAB8).
- AI `theme.ai.*` (= `aiTokens`): `primary` (#7C3AED), `soft` (rgba(124, 58, 237, 0.14)), `line` (rgba(124, 58, 237, 0.32)).

Name-shift gotcha between `C.*` and `theme.status.*`: the IconButton tones named `caution`/`alert`/`info`/`ooo` map onto the status keys `pickup`/`dirty`/`clean`/`outOfOrder` respectively. That is exactly how the current `C.*` aliases already resolve (`tokens.ts` C block lines 143-158: `C.caution = statusTokens.pickup`, `C.alert = statusTokens.dirty`, `C.info = statusTokens.clean`, `C.ooo = statusTokens.outOfOrder`). `accent` maps to the green accent keys (`primarySoft/primaryAction/primaryLine`), and `ai` maps to `theme.ai.*` — neither lives under `theme.status.*`.

Existing `HeroButton` structure to mirror (DO NOT edit HeroButton — D-05), from `mobileHandoff.tsx`:
```tsx
// styles.heroButton: minHeight 48, borderRadius 13, paddingHorizontal 16,
//   flexDirection row, alignItems/justifyContent center, gap 6
// heroButtonText: fontSize 15, fontWeight "600"
```
Existing `IconButton` to modify (`mobileHandoff.tsx` ~lines 105-132), currently reads static `C` via a module-level `toneColors` map (`mobileHandoff.tsx` lines 30-44):
```tsx
export function IconButton({ icon, tone = "neutral", size = 36 }) {
  const colors = toneColors[tone];   // toneColors built from static C.*
  return (
    <View style={[styles.iconButton, { width: size, height: size, borderRadius: R.md,
        backgroundColor: colors.bg, borderColor: colors.line }]}>
      <Ionicons name={icon} size={size > 40 ? 18 : 16} color={colors.fg} />
    </View>
  );
}
```
The current module-level `toneColors` has 13 tones (verified `mobileHandoff.tsx` lines 30-44). Every one of them must be reproduced faithfully — see the full mapping table in Task 2.

Existing IconButton call sites (8) that must NOT break — none passes `accessibilityLabel` today:
`components/home/SupervisorHome.tsx:179`, `app/(app)/notifications/index.tsx:112`, `app/(app)/lost-found/index.tsx:143`, `app/(app)/home/index.tsx:155,273,330`, `app/(app)/sop/index.tsx:79,95`.

Icon prop type: `React.ComponentProps<typeof Ionicons>["name"]`. Import alias `@/` = `apps/mobile/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Button primitive</name>
  <files>apps/mobile/components/ui/Button.tsx</files>
  <read_first>
    - apps/mobile/components/shared/mobileHandoff.tsx (HeroButton structure to mirror; do NOT edit it here)
    - apps/mobile/components/engineering/WorkOrderCard.tsx (claim-button loading pattern reference, ~lines 181-199)
    - apps/mobile/lib/theme/useTheme.ts (color source, plan 01)
    - .planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md (§"Button / IconButton (UI-01)" — variants, sizes, loading tables)
    - .planning/phases/07-theme-foundation-primitives/07-PATTERNS.md (§"components/ui/Button.tsx")
  </read_first>
  <action>
Create `apps/mobile/components/ui/Button.tsx` as a `Pressable`-based primitive.

Props:
```ts
type ButtonVariant = "primary" | "secondary" | "ghost" | "destructive";
type ButtonSize = "sm" | "md" | "lg";
interface ButtonProps {
  label: string;                 // caller-provided, already translated — NO default
  onPress: () => void;
  variant?: ButtonVariant;       // default "primary"
  size?: ButtonSize;             // default "md"
  loading?: boolean;
  disabled?: boolean;
  icon?: React.ComponentProps<typeof Ionicons>["name"];
  style?: StyleProp<ViewStyle>;
}
```
There is NO default label — the caller always supplies an already-translated string (Pitfall 4 / i18n floor contract). Never call `t()` inside this file.

Colors via `const theme = useTheme();` (resolve at render, inline — keep static layout in `StyleSheet.create`). Variant color map (EXACT values):
| variant | bg (default) | text/icon color | pressed |
|---|---|---|---|
| primary | `theme.primaryAction` | `#FFFFFF` | bg → `theme.primary` |
| secondary | `theme.surface`, border 1px `theme.border` | `theme.textPrimary` | bg → `theme.surfaceMuted` |
| ghost | `transparent`, no border | `theme.primaryAction` | bg → `theme.primarySoft` |
| destructive | `theme.status.dirty` | `#FFFFFF` | dim fill to `opacity: 0.85` |

For `destructive` pressed feedback, apply `opacity: 0.85` on press rather than a hardcoded darker hex — this honors the codebase's `activeOpacity` 0.84–0.85 convention (HeroButton/WorkOrderCard) and avoids inlining a non-token status hex (Pitfall 9). All other variants swap `backgroundColor` to the mapped pressed token via `Pressable`'s `style={({ pressed }) => ...}`.

Size map (EXACT — all meet the ≥44 touch-target floor via minHeight, no hitSlop needed):
| size | minHeight | paddingHorizontal | fontSize | iconSize | borderRadius |
|---|---|---|---|---|---|
| sm | 44 | 12 | 14 (weight "600") | 14 | 12 (`R.md`) |
| md | 48 | 16 | 15 (weight "600") | 16 | 12 (`R.md`) |
| lg | 56 | 24 | 16 (weight "600") | 18 | 12 (`R.md`) |

Disabled: `opacity: 0.4`, no press feedback. `loading` implies `disabled`.

Loading state (D-07, NO layout shift): render the label `<Text>` with `opacity: 0` (so it still reserves its intrinsic width/height) and absolutely position an `<ActivityIndicator size="small" />` centered over it. Spinner color = the variant's foreground color (`#FFFFFF` for primary/destructive, `theme.primaryAction` for secondary/ghost). Do not change the button's size/position between idle and loading.

Layout: `flexDirection: "row"`, `alignItems: "center"`, `justifyContent: "center"`, `gap: 8` (icon-to-label). If `icon` is set and not loading, render `<Ionicons name={icon} size={iconSize} color={fg} />` before the label.

Style merge order (LOCKED, Pitfall 6): `style={[styles.base, variantStyle, sizeStyle, props.style]}` — base first, variant, size, caller `style` LAST.

Do NOT import `C`, `lightTheme`, or `darkTheme` directly — colors come ONLY through `useTheme()` (the enforceable boundary).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTheme" apps/mobile/components/ui/Button.tsx` matches; `grep -En "\\bC\\.|lightTheme|darkTheme" apps/mobile/components/ui/Button.tsx` returns nothing (never imports the frozen constant/raw themes).
    - `grep -o "minHeight: 44\|minHeight: 48\|minHeight: 56" apps/mobile/components/ui/Button.tsx | sort -u` shows all three sizes present (touch-target floor).
    - `grep -n "ActivityIndicator" apps/mobile/components/ui/Button.tsx` matches and `grep -n "opacity: 0" apps/mobile/components/ui/Button.tsx` matches (label hidden but reserving space during loading).
    - `grep -n "theme.status.dirty" apps/mobile/components/ui/Button.tsx` matches (destructive fill from theme, not inline hex).
    - `grep -n "styles.base" apps/mobile/components/ui/Button.tsx` shows the merge array begins with the base style and ends with the caller `style` prop (Pitfall 6).
    - `grep -n "t(" apps/mobile/components/ui/Button.tsx` returns nothing (no internal translation / no English default).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Button exists with 4 variants, 3 sizes (all minHeight ≥44), no-layout-shift loading, theme-only colors, locked merge order, no default label; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Theme-wire IconButton in mobileHandoff.tsx</name>
  <files>apps/mobile/components/shared/mobileHandoff.tsx</files>
  <read_first>
    - apps/mobile/components/shared/mobileHandoff.tsx (current IconButton + the module-level toneColors map built from C.*, lines 30-44)
    - apps/mobile/components/shared/tokens.ts (theme object; C block lines 117-168 shows how each C.* alias resolves — use it to confirm every mapping below)
    - apps/mobile/lib/theme/useTheme.ts (the color source to switch to)
    - .planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md (§"Button / IconButton (UI-01)" — the IconButton D-08 paragraph and accessibilityLabel requirement)
    - .planning/phases/07-theme-foundation-primitives/07-PATTERNS.md (§"components/shared/mobileHandoff.tsx IconButton")
  </read_first>
  <action>
Modify ONLY the `IconButton` component (and its tone-color resolution) in `mobileHandoff.tsx`. Do NOT touch `HeroButton`, `Pill`, `SectionLabel`, `Segmented`, `Avatar`, `Mono`, `AILabel`, or any other export in this file (D-05, and non-regression).

1. Move tone-color resolution from the static module-level `toneColors` (built from `C.*`) to a render-time map derived from `useTheme()`. Inside `IconButton`, add `const theme = useTheme();` and build a `Record<Tone, { bg; fg; line }>` from `theme`.

   Reproduce ALL 13 existing tones EXACTLY. The table below was derived by resolving each current `C.*` entry (`mobileHandoff.tsx` lines 30-44) through the `C` alias block (`tokens.ts` lines 117-168) to its underlying light-theme value, then picking the `theme.*` key that yields the SAME value. Because the light theme is byte-identical to the `C` snapshot (Success Criterion 5), every tone has an exact themed equivalent — there are NO approximations or deviations in this map. Write the map literally as:

   ```ts
   const toneColors: Record<Tone, { bg: string; fg: string; line: string }> = {
     neutral:  { bg: theme.surfaceMuted,        fg: theme.textSecondary,   line: theme.border },
     dirty:    { bg: theme.status.dirtySoft,    fg: theme.status.dirty,    line: theme.status.dirtyLine },
     occupied: { bg: theme.status.dirtySoft,    fg: theme.status.occupied, line: theme.status.dirtyLine },
     progress: { bg: theme.status.pickupSoft,   fg: theme.status.pickup,   line: theme.status.pickupLine },
     clean:    { bg: theme.status.cleanSoft,    fg: theme.status.clean,    line: theme.status.cleanLine },
     ready:    { bg: theme.status.readySoft,    fg: theme.status.ready,    line: theme.status.readyLine },
     pickup:   { bg: theme.status.pickupSoft,   fg: theme.status.pickup,   line: theme.status.pickupLine },
     accent:   { bg: theme.primarySoft,         fg: theme.primaryAction,   line: theme.primaryLine },
     ai:       { bg: theme.ai.soft,             fg: theme.ai.primary,      line: theme.ai.line },
     alert:    { bg: theme.status.dirtySoft,    fg: theme.status.dirty,    line: theme.status.dirtyLine },
     caution:  { bg: theme.status.pickupSoft,   fg: theme.status.pickup,   line: theme.status.pickupLine },
     info:     { bg: theme.status.cleanSoft,    fg: theme.status.clean,    line: theme.status.cleanLine },
     ooo:      { bg: theme.status.outOfOrderSoft, fg: theme.status.outOfOrder, line: theme.status.outOfOrderLine },
   };
   ```

   Value-equivalence proof each mapping must satisfy (light mode) — do NOT deviate from these:
   | tone | old (`C.*` → value) | new (`theme.*` → value) |
   |---|---|---|
   | neutral.bg | `C.surface3` → `#F0EBE1` | `theme.surfaceMuted` → `#F0EBE1` |
   | neutral.fg | `C.ink2` → `#766D63` | `theme.textSecondary` → `#766D63` |
   | neutral.line | `C.line` → `#E4D6C4` | `theme.border` → `#E4D6C4` |
   | dirty/alert.bg | `C.alertSoft` → `#F6DDE0` | `theme.status.dirtySoft` → `#F6DDE0` |
   | dirty/alert.fg | `C.alert` → `#A9363F` | `theme.status.dirty` → `#A9363F` |
   | dirty/alert.line | `C.alertLine` → `#E7A9B0` | `theme.status.dirtyLine` → `#E7A9B0` |
   | occupied.fg | `C.occupied` → `#A9363F` | `theme.status.occupied` → `#A9363F` |
   | progress/pickup/caution.bg | `C.cautionSoft` → `#F7E8C8` | `theme.status.pickupSoft` → `#F7E8C8` |
   | progress/pickup/caution.fg | `C.caution` → `#B7791F` | `theme.status.pickup` → `#B7791F` |
   | progress/pickup/caution.line | `C.cautionLine` → `#E4C174` | `theme.status.pickupLine` → `#E4C174` |
   | clean/info.bg | `C.infoSoft` → `#DDEAF1` | `theme.status.cleanSoft` → `#DDEAF1` |
   | clean/info.fg | `C.info` → `#2F6F95` | `theme.status.clean` → `#2F6F95` |
   | clean/info.line | `C.infoLine` → `#ACC9DB` | `theme.status.cleanLine` → `#ACC9DB` |
   | ready.bg | `C.readySoft` → `#D7EDE7` | `theme.status.readySoft` → `#D7EDE7` |
   | ready.fg | `C.ready` → `#0E7468` | `theme.status.ready` → `#0E7468` |
   | ready.line | `C.readyLine` → `#A7D2C9` | `theme.status.readyLine` → `#A7D2C9` |
   | accent.bg | `C.accentSoft` → `#E0EBE1` | `theme.primarySoft` → `#E0EBE1` |
   | accent.fg | `C.accent` → `#4F7A5A` | `theme.primaryAction` → `#4F7A5A` |
   | accent.line | `C.accentLine` → `#B9CFBC` | `theme.primaryLine` → `#B9CFBC` |
   | ai.bg | `C.aiSoft` → `rgba(124, 58, 237, 0.14)` | `theme.ai.soft` → `rgba(124, 58, 237, 0.14)` |
   | ai.fg | `C.ai` → `#7C3AED` | `theme.ai.primary` → `#7C3AED` |
   | ai.line | `C.aiLine` → `rgba(124, 58, 237, 0.32)` | `theme.ai.line` → `rgba(124, 58, 237, 0.32)` |
   | ooo.bg | `C.oooSoft` → `#EDE7DD` | `theme.status.outOfOrderSoft` → `#EDE7DD` |
   | ooo.fg | `C.ooo` → `#746D63` | `theme.status.outOfOrder` → `#746D63` |
   | ooo.line | `C.oooLine` → `#D5CAB8` | `theme.status.outOfOrderLine` → `#D5CAB8` |

   CRITICAL: `neutral.bg` is `theme.surfaceMuted` (#F0EBE1), NOT `theme.surface` (#FFFDFC). `neutral` is IconButton's DEFAULT tone (`tone = "neutral"`) used by all 8 existing call sites — a `surface`/`surfaceMuted` swap is a silent visible regression that `tsc` cannot catch (both are valid keys). `accent` and `ai` do NOT live under `theme.status.*` — use `theme.primary*` and `theme.ai.*` as shown. Keep `Tone` type and its 13 members unchanged; do not add or rename tones.
2. Keep the structure identical: still a `<View>` (NOT pressable), same `size = 36` default, same `borderRadius: R.md`, same `Ionicons` size logic (`size > 40 ? 18 : 16`). Do NOT add a new variant or size surface (D-08 forbids it). Do NOT "fix" the sub-44 default size this phase.
3. Add an OPTIONAL `accessibilityLabel?: string` prop to `IconButton`'s props and pass it to the root view: `<View accessibilityLabel={accessibilityLabel} accessibilityRole="button" ...>`. It MUST be optional (not required) this phase: the 8 existing call sites listed in `<interfaces>` do not pass one, and making it required would break their type-check and force out-of-scope screen edits, violating the zero-visual-change constraint (SC5). Do NOT edit those 8 call sites. In the SUMMARY, flag for the Phase 8 planner: any screen adopting IconButton in a touch-target context must pass both `accessibilityLabel` and an explicit `size={44}`-or-greater, and Phase 8 may promote `accessibilityLabel` to required once all call sites are migrated.
4. The module-level `toneColors` const built from `C.*` is now replaced by the in-component map. Before deleting the old module-level const, `grep -n "toneColors" apps/mobile/components/shared/mobileHandoff.tsx` — `getToneColors()` (line ~46) and `getRoomTone()` also reference it. If `getToneColors`/other exports still use the module-level map, KEEP that module-level map (it is theirs) and simply have IconButton build its own theme-derived map; do NOT delete a still-referenced const (that would break `getToneColors` — non-regression). Only remove the module-level const if grep confirms zero remaining references after your change.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTheme" apps/mobile/components/shared/mobileHandoff.tsx` matches (IconButton now theme-driven).
    - `grep -n "accessibilityLabel" apps/mobile/components/shared/mobileHandoff.tsx` matches and it is declared OPTIONAL (`accessibilityLabel?:`), not required.
    - `grep -n "size = 36" apps/mobile/components/shared/mobileHandoff.tsx` still matches (default size unchanged — D-08).
    - `grep -n "export function HeroButton\|export function Pill\|export function Segmented" apps/mobile/components/shared/mobileHandoff.tsx` still returns matches (other exports untouched).
    - Per-tone byte-identical assertion (each MUST match in the new IconButton `toneColors` map — this is what verifies the zero-regression guarantee, since `tsc` cannot):
      - `grep -Eq "neutral:\s*\{\s*bg:\s*theme\.surfaceMuted,\s*fg:\s*theme\.textSecondary,\s*line:\s*theme\.border" apps/mobile/components/shared/mobileHandoff.tsx` (neutral bg = surfaceMuted, NOT surface — the critical default-tone check).
      - `grep -Eq "accent:\s*\{\s*bg:\s*theme\.primarySoft,\s*fg:\s*theme\.primaryAction,\s*line:\s*theme\.primaryLine" apps/mobile/components/shared/mobileHandoff.tsx` (accent via primary keys, not status).
      - `grep -Eq "ai:\s*\{\s*bg:\s*theme\.ai\.soft,\s*fg:\s*theme\.ai\.primary,\s*line:\s*theme\.ai\.line" apps/mobile/components/shared/mobileHandoff.tsx` (ai via theme.ai.*, not status).
      - `grep -Eq "(dirty|alert):\s*\{\s*bg:\s*theme\.status\.dirtySoft,\s*fg:\s*theme\.status\.dirty,\s*line:\s*theme\.status\.dirtyLine" apps/mobile/components/shared/mobileHandoff.tsx` (dirty + alert both → status.dirty*).
      - `grep -Eq "occupied:\s*\{\s*bg:\s*theme\.status\.dirtySoft,\s*fg:\s*theme\.status\.occupied,\s*line:\s*theme\.status\.dirtyLine" apps/mobile/components/shared/mobileHandoff.tsx`.
      - `grep -Eq "(progress|pickup|caution):\s*\{\s*bg:\s*theme\.status\.pickupSoft,\s*fg:\s*theme\.status\.pickup,\s*line:\s*theme\.status\.pickupLine" apps/mobile/components/shared/mobileHandoff.tsx` (progress/pickup/caution all → status.pickup*).
      - `grep -Eq "(clean|info):\s*\{\s*bg:\s*theme\.status\.cleanSoft,\s*fg:\s*theme\.status\.clean,\s*line:\s*theme\.status\.cleanLine" apps/mobile/components/shared/mobileHandoff.tsx` (clean + info → status.clean*).
      - `grep -Eq "ready:\s*\{\s*bg:\s*theme\.status\.readySoft,\s*fg:\s*theme\.status\.ready,\s*line:\s*theme\.status\.readyLine" apps/mobile/components/shared/mobileHandoff.tsx`.
      - `grep -Eq "ooo:\s*\{\s*bg:\s*theme\.status\.outOfOrderSoft,\s*fg:\s*theme\.status\.outOfOrder,\s*line:\s*theme\.status\.outOfOrderLine" apps/mobile/components/shared/mobileHandoff.tsx`.
      - Count check: the new IconButton `toneColors` map must contain all 13 tone keys — `grep -cE "^\s*(neutral|dirty|occupied|progress|clean|ready|pickup|accent|ai|alert|caution|info|ooo):" apps/mobile/components/shared/mobileHandoff.tsx` returns ≥13.
    - `grep -rn "<IconButton" apps/mobile/components/home/SupervisorHome.tsx apps/mobile/app/\(app\)/notifications/index.tsx apps/mobile/app/\(app\)/lost-found/index.tsx apps/mobile/app/\(app\)/home/index.tsx apps/mobile/app/\(app\)/sop/index.tsx` shows the 8 call sites are UNCHANGED (no accessibilityLabel forced onto them).
    - `npx tsc --noEmit` reports no new errors (confirms optional prop did not break the 8 call sites).
  </acceptance_criteria>
  <done>IconButton resolves all 13 tones via useTheme() with byte-identical light-mode colors (verified per-tone by grep, including neutral=surfaceMuted, accent=primary*, ai=ai.*), has an optional accessibilityLabel, keeps its structure/size default; the 8 existing call sites and other exports untouched; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Presentation-only. `Button` triggers a caller-supplied `onPress` — it introduces no destructive action or confirmation itself (destructive variant is styling-only per UI-SPEC copywriting contract). Boundary in scope: shared file `mobileHandoff.tsx` consumed by many existing screens.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | `mobileHandoff.tsx` (shared; regression risk to unmigrated screens) | mitigate | Only IconButton's color resolution changes; all 13 tones re-mapped to `theme.*` keys that resolve to values byte-identical to the prior `C` snapshot (SC5), verified by per-tone grep acceptance criteria (not just `tsc`, which cannot catch a surface↔surfaceMuted swap); `accessibilityLabel` kept optional so the 8 existing call sites are untouched. Acceptance criteria also assert other exports, default size, and the 8 call sites are unchanged. |
| T-07-06 | Repudiation | destructive-variant Button | accept | Styling-only; any real destructive action wires its own confirmation in Phase 8/9 (UI-SPEC copywriting contract). |
</threat_model>

<verification>
- `npx tsc --noEmit` from `apps/mobile` is clean.
- No new dependency added.
- All 13 IconButton tones resolve to their exact prior light-mode values (per-tone grep assertions in Task 2 pass).
- No existing screen imports `Button`. The 8 IconButton call sites are byte-identical (optional prop means no forced edits).
</verification>

<success_criteria>
- `Button`/`IconButton` primitive available with size variants, loading state, and ≥44pt/48dp touch target (ROADMAP SC3, UI-01).
- IconButton is dark-ready (reads `useTheme()`) with zero visual change in light mode across all 13 tones (ROADMAP SC5).
</success_criteria>

<output>
After completion, create `.planning/phases/07-theme-foundation-primitives/07-03-SUMMARY.md`.
</output>
