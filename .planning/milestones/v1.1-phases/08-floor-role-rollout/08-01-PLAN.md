---
phase: 08-floor-role-rollout
plan: 01
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/my-rooms/index.tsx
  - apps/mobile/components/shared/evening.tsx
autonomous: true
requirements: [FLOOR-01]
must_haves:
  truths:
    - "My Rooms list renders loading/empty states via StateBlock and room cards via the rebuilt RoomQueueCard"
    - "Each room card is still tappable (navigates to room detail) after the Card+Pressable rebuild"
    - "Room fetch, assignment filtering, and offline behavior in my-rooms/index.tsx are byte-for-byte unchanged (only presentation swapped)"
  artifacts:
    - path: "apps/mobile/app/(app)/my-rooms/index.tsx"
      provides: "My Rooms list on primitives (useTheme + StateBlock)"
      contains: "useTheme"
    - path: "apps/mobile/components/shared/evening.tsx"
      provides: "RoomQueueCard rebuilt on Card + Pressable"
      contains: "Card"
  key_links:
    - from: "apps/mobile/components/shared/evening.tsx (RoomQueueCard)"
      to: "apps/mobile/components/ui/Card.tsx"
      via: "Pressable-wraps-Card shell rebuild"
      pattern: "<Card"
    - from: "apps/mobile/app/(app)/my-rooms/index.tsx"
      to: "apps/mobile/components/ui/StateBlock.tsx"
      via: "loading/empty state rendering"
      pattern: "StateBlock"
---

<objective>
Migrate the My Rooms list screen (`my-rooms/index.tsx`, 391 lines, 11 `C.` refs) and rebuild `RoomQueueCard` (in `components/shared/evening.tsx`, D-09) onto Phase 7 primitives. Full migration depth per D-03: zero `C.*` references remain in `my-rooms/index.tsx`, and `RoomQueueCard`'s card-shell is rebuilt on `Card` + `Pressable`.

Purpose: My Rooms is the housekeeper's highest-traffic screen (D-01 first). This is a call-site swap — no change to room fetching, assignment filtering, or offline-sync.
Output: My Rooms list + RoomQueueCard rendered via primitives, list test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-RESEARCH.md, exhaustive — cite per line, do NOT re-derive)
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
C.paper→theme.background · C.surface→theme.surface · C.surface2→theme.surfaceSubtle · C.surface3→theme.surfaceMuted
C.line→theme.border · C.line2→theme.borderSubtle
C.ink→theme.textPrimary · C.ink2→theme.textSecondary · C.ink3→theme.textMuted · **C.ink4→theme.textDisabled** (Wave 0 key)
C.primary→theme.primary · C.accent→theme.primaryAction · C.accentSoft→theme.primarySoft · C.accentLine→theme.primaryLine
C.brass→theme.accentBrass · **C.brassSoft→theme.accentBrassSoft** (Wave 0) · **C.brassLine→theme.accentBrassLine** (Wave 0)
C.clay→theme.accentClay
C.shell*→theme.shell.* (bg/surface/raised/line/ink/ink2/ink3); also switch any direct `import { shellTokens }` to `theme.shell`
C.ready/readySoft/readyLine→theme.status.ready/readySoft/readyLine
C.caution/cautionSoft/cautionLine→theme.status.pickup/pickupSoft/pickupLine  (NAME CHANGE caution→pickup)
C.alert/alertSoft/alertLine→theme.status.dirty/dirtySoft/dirtyLine  (NAME CHANGE alert→dirty)
C.info/infoSoft/infoLine→theme.status.clean/cleanSoft/cleanLine  (NAME CHANGE info→clean)
C.occupied→theme.status.occupied
C.ooo/oooSoft/oooLine→theme.status.outOfOrder/outOfOrderSoft/outOfOrderLine
</migration_reference>

<interfaces>
## StateBlock (components/ui/StateBlock.tsx) — exact discriminated union
type StateBlockProps =
  | { status: "loading" }
  | { status: "empty"; emptyIcon?: IconName; emptyTitle: string; emptyBody?: string }
  | { status: "error"; errorIcon?: IconName; errorMessage: string; onRetry?: () => void; retryLabel?: string }
  | { status: "ready"; children: React.ReactNode };
// retryLabel is REQUIRED whenever onRetry is passed (throws at runtime otherwise).

## Card (components/ui/Card.tsx) — NO onPress prop
interface CardProps { children: React.ReactNode; dimmed?: boolean; style?: StyleProp<ViewStyle>; }
// Card.styles.base = uniform padding:16 box, borderRadius:16. Caller `style` applies LAST (overrides base).
// Card has NO onPress → wrap in <Pressable> to preserve tap target.

## RoomQueueCard current shell (evening.tsx:196) — entire card is one TouchableOpacity:
<TouchableOpacity activeOpacity={0.85} onPress={onPress} style={[styles.card, dimmed && styles.cardDimmed]} testID={`room-card-${room.room_number}`}>
// Card was literally extracted from this file's styles.card in Phase 7 (matches base/dimmed behavior).

## RoomQueueCard StatusKey mapping (only if a bare badge-shaped chip exists; StatusPill/StatusRail stay untouched):
DIRTY→dirty · OCCUPIED→occupied · PICKUP→pickup · IN_PROGRESS→inProgress · CLEAN→clean · INSPECTED→ready · OOO/OUT_OF_ORDER/OUT_OF_SERVICE→outOfOrder
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate my-rooms/index.tsx to useTheme + StateBlock</name>
  <files>apps/mobile/app/(app)/my-rooms/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/my-rooms/index.tsx (current state — 391 lines, 11 C. refs, 4 TouchableOpacity, 3 ad-hoc empty-state blocks at ~lines 232-237/292-295/310-314, loading spinner)
    - apps/mobile/components/ui/StateBlock.tsx (target primitive for loading/empty)
    - apps/mobile/components/ui/EmptyState.tsx (what StateBlock renders for empty)
    - apps/mobile/components/shared/tokens.ts (confirm the Wave 0 keys textDisabled/accentBrassSoft/accentBrassLine now exist on lightTheme)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme";` and call `const theme = useTheme();` at the top of the component body.
    2. Replace ALL 11 `C.*` color references with their `theme.*` equivalent per the C.*→theme.* map above (e.g. `C.ink4` → `theme.textDisabled`, `C.surface` → `theme.surface`). Keep the existing `StyleSheet.create()` for layout/spacing; move only the color values to inline `theme.*` at render (the Phase 7 "inline color props, not makeStyles rewrite" pattern). Any direct `shellTokens.*` import used for colors switches to `theme.shell.*`.
    3. Convert the loading spinner and the 3 ad-hoc empty-state `<View style={styles.emptyCard}>...` blocks to `<StateBlock>`:
       - loading path → `<StateBlock status="loading" />`
       - "no rooms" / "all rooms done" / "nothing done yet" → `<StateBlock status="empty" emptyIcon="checkmark-done-outline" emptyTitle={t(...)} emptyBody={t(...)} />` reusing the exact existing `t(...)` keys (e.g. `t("rooms.allRoomsDone")`, `t("rooms.allRoomsDoneHint")`). Do NOT introduce new string literals.
    4. Do NOT change any data-fetch `useEffect`, the room/assignment filtering logic, the `RoomQueueCard` call sites' props (`room`, `onPress`, `dimmed`), navigation, or role-conditional branches. This is presentation-only.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/MyRoomsScreen.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero `C.` references remain: `grep -c "C\." apps/mobile/app/(app)/my-rooms/index.tsx` returns 0
    - No direct shellTokens color import remains: `grep -c "shellTokens" apps/mobile/app/(app)/my-rooms/index.tsx` returns 0
    - `grep -c "useTheme" apps/mobile/app/(app)/my-rooms/index.tsx` >= 1
    - `grep -c "StateBlock" apps/mobile/app/(app)/my-rooms/index.tsx` >= 1
    - No new raw string literal: `npm run lint` passes (i18n gate clean)
    - `npx jest __tests__/screens/MyRoomsScreen.test.tsx` passes; `npm run type-check` passes
    - Diff touches only color/StyleSheet/JSX-presentation lines — no change to any `api.*`, `enqueueAction`, `useEffect` fetch, or role branch (reviewer confirms)
  </acceptance_criteria>
  <done>my-rooms/index.tsx renders via useTheme + StateBlock, zero C. refs, list test + type-check + lint green, data/fetch logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild RoomQueueCard on Card + Pressable (D-09)</name>
  <files>apps/mobile/components/shared/evening.tsx</files>
  <read_first>
    - apps/mobile/components/shared/evening.tsx (full file — RoomQueueCard ~lines 180-210, its styles.card/cardDimmed ~337-353, the badge/badgeCritical/badgeBrass VIP/DND styles ~371-376, and STATUS_META ~22-32)
    - apps/mobile/components/ui/Card.tsx (target — note dimmed prop and no onPress)
    - apps/mobile/components/ui/StatusBadge.tsx (only if a bare badge-shaped chip needs converting)
  </read_first>
  <action>
    Scope is STRICTLY `RoomQueueCard` and its card-shell only. Per D-09, DO NOT touch `StatusPill`, `StatusRail`, `ProgressBar`, `Chip`, `AIBriefingCard`, or `SectionHeader` — leave those exports byte-for-byte unchanged.

    1. Add `import { useTheme } from "@/lib/theme/useTheme";` (if not present) and `const theme = useTheme();` inside `RoomQueueCard`.
    2. Replace `RoomQueueCard`'s outer `<TouchableOpacity ... onPress={onPress} style={[styles.card, dimmed && styles.cardDimmed]} ...>` shell with the Pressable-wraps-Card pattern (Card has no onPress):
       ```tsx
       <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={`Room ${room.room_number}`} testID={`room-card-${room.room_number}`}>
         <Card dimmed={dimmed} style={styles.cardLayoutOverrides}>
           {/* existing internal content, unchanged structurally */}
         </Card>
       </Pressable>
       ```
       Pass `dimmed` straight to `Card`'s `dimmed` prop. Add a `styles.cardLayoutOverrides` entry re-applying only the non-uniform layout from the old `styles.card` (its flexDirection:"row" / asymmetric padding / gap / position:"relative" / overflow:"hidden") since Card's base is a uniform padding:16 box; caller `style` applies last. Do NOT re-declare bg/border/radius/shadow — Card owns those.
    3. `RoomQueueCard` continues calling `<StatusPill status={room.status} />` inside the new Card — leave StatusPill usage as-is (D-09 excludes replacing it with StatusBadge; StatusPill takes raw DB enum + optional label, a different contract).
    4. The VIP/DND `badge`/`badgeBrass` chips (styles ~371-376) use `C.brassSoft`/`C.brassLine` — these do NOT map to any of StatusBadge's 13 StatusKeys, so keep them hand-rolled but route their colors through `theme.accentBrassSoft`/`theme.accentBrassLine` (Wave 0 keys). Replace every remaining `C.*` inside RoomQueueCard + its styles with the theme.* equivalent per the map above.
    5. Delete the now-unused old `styles.card`/`styles.cardDimmed` entries ONLY IF nothing else in evening.tsx references them (grep first — other exports may share them; if shared, leave them).
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/MyRoomsScreen.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `RoomQueueCard` renders `<Card` wrapped in `<Pressable`: `grep -c "<Card" apps/mobile/components/shared/evening.tsx` >= 1
    - Zero `C.` references remain inside the RoomQueueCard function and its style block: after migration `grep -c "C\." apps/mobile/components/shared/evening.tsx` returns 0 (the whole file is expected to reach 0 since RoomQueueCard is the only C.-using card here; if StatusPill/StatusRail contained C. refs they must ALSO be theme-mapped, but their JSX/component structure must NOT change — confirm via diff)
    - `StatusPill`, `StatusRail`, `ProgressBar`, `Chip` export signatures unchanged: `grep -nE "export (function|const) (StatusPill|StatusRail|ProgressBar|Chip)" apps/mobile/components/shared/evening.tsx` count unchanged from before
    - `npx jest __tests__/screens/MyRoomsScreen.test.tsx` passes (room-card testID `room-card-*` still present and tappable); `npm run type-check` + `npm run lint` pass
  </acceptance_criteria>
  <done>RoomQueueCard rebuilt on Card+Pressable, dimmed passthrough works, tap target preserved, StatusPill/StatusRail/etc. untouched, zero C. refs, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (`api.*` room fetch / status actions) | Untrusted-ish; but these calls are NOT modified this plan — only presentation changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Elevation of Privilege / Information Disclosure | A "styling-only" diff in `my-rooms/index.tsx` accidentally drops the `hotel_id`-scoped/role-conditional room fetch or assignment filter | mitigate | Diff scope constrained to color/StyleSheet/JSX-presentation + StateBlock swap; acceptance criteria require reviewer to confirm no `api.*`/`enqueueAction`/`useEffect`-fetch/role-branch line changed; existing MyRoomsScreen test asserts data behavior |
| T-08-01-02 | Tampering | RoomQueueCard rebuild silently drops the `onPress` tap target (Card has no onPress) leaving rooms unnavigable | mitigate | Pressable-wraps-Card pattern mandated; acceptance criteria require `room-card-*` testID present and the existing test exercises tap navigation |
| T-08-01-03 | Spoofing | D-09 scope creep alters StatusPill/StatusRail (shared by out-of-phase screens) | accept→mitigate | Explicit "do not touch" list + grep-assert export signatures unchanged |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/MyRoomsScreen.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/my-rooms/index.tsx` == 0
- Diff review: only presentation lines changed; no data/fetch/role logic altered
</verification>

<success_criteria>
FLOOR-01 (list half): a housekeeper sees My Rooms list rendered with StateBlock + rebuilt RoomQueueCard (Card+Pressable), tap-to-detail works, offline/data behavior unchanged, zero legacy C tokens left in the list screen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-01-SUMMARY.md`.
</output>
