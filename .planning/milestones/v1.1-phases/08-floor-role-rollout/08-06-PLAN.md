---
phase: 08-floor-role-rollout
plan: 06
type: execute
wave: 3
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/work-orders/[woId].tsx
autonomous: true
requirements: [FLOOR-03]
must_haves:
  truths:
    - "WO detail renders all actions/status/comments/photos via primitives (zero C. refs)"
    - "10 of 11 Alert.alert calls become Toasts; the escalate-confirm at line 250 STAYS a blocking Alert.alert"
    - "claim/complete/hold/escalate/arrive/comment/photo handlers keep exact api/queue logic — only feedback lines change"
  artifacts:
    - path: "apps/mobile/app/(app)/work-orders/[woId].tsx"
      provides: "WO detail on primitives + Toast feedback (1 confirm dialog retained)"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/work-orders/[woId].tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "outcome Alert.alert → toast"
      pattern: "toast\\.(error|success|info)"
---

<objective>
Migrate the Work Orders detail screen (`work-orders/[woId].tsx`, 970 lines, 79 `C.` refs, 21 TouchableOpacity, 11 Alert.alert) onto Phase 7 primitives. Second-largest file in the phase — split into two tasks by section. 10 of 11 alerts are outcome-reporting → Toast; exactly ONE (escalate confirm, line 250) is a genuine Yes/Cancel choice → STAYS `Alert.alert` (D-04).

Purpose: FLOOR-03 (detail half) — engineer acts on a work order via primitives + Toast; RBAC/data/state-transition logic unchanged.
Output: WO detail on primitives, WorkOrderDetail test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map + Button/StatusBadge/StateBlock/useToast signatures: see 08-02-PLAN.md (identical).
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` + `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
WO StatusKey mapping: priority emergency→emergency/urgent→urgent/low→low · status on_hold→onHold · overdue→overdue · completed→completed.
Button destructive → theme.status.dirty (never inline hex).
</migration_reference>

<alert_classification>
## [woId].tsx — 11 Alert.alert, 10 → Toast, 1 STAYS (from 08-RESEARCH.md, verified per line)
| Line | What | Action |
|---|---|---|
| 172 | Claim failed | toast.error |
| 188 | "Completed" (single OK → router.back()) | toast.success(msg); router.back(); together (no dismiss gate) |
| 194 | "Offline, queued" | toast.info |
| 197 | Complete failed | toast.error |
| 210 | Set-status failed | toast.error |
| 228 | Add-comment failed | toast.error |
| 242 | Arrive failed | toast.error |
| **250** | **Escalate confirm — Cancel/Confirm buttons gating a state-changing escalation** | **STAYS Alert.alert — do NOT convert** |
| 265 | Escalate-failed (nested in the confirm's onPress) | toast.error |
| 279 | Camera permission denied | toast.error |
| 297 | Add-photo failed | toast.error |

RULE: change ONLY the feedback line; do not alter preceding `await api.*`/`enqueueAction` calls, state-transition logic, or early-return offline guards in the same handler.
</alert_classification>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate [woId].tsx part A — status actions (claim/complete/hold/escalate/arrive)</name>
  <files>apps/mobile/app/(app)/work-orders/[woId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/work-orders/[woId].tsx (full — Part A: header, status display, and the action handlers handleClaim/handleComplete/handleSetStatus/handleEscalate/handleArrive around lines 172-265)
    - apps/mobile/components/ui/Button.tsx, StatusBadge.tsx, StateBlock.tsx
    - apps/mobile/lib/theme/useToast.ts
    - apps/mobile/components/shared/tokens.ts (Wave 0 keys)
  </read_first>
  <action>
    1. Add useTheme + useToast imports/calls.
    2. PART A scope = header/status area + the state-action handlers and their buttons (claim, complete, set-status/hold, escalate, arrive) — roughly the action half of the file. Within scope:
       - Replace `C.*` refs with `theme.*` (map). Keep StyleSheet layout; inline colors.
       - Convert action `TouchableOpacity`+`ActivityIndicator` buttons to `<Button>` (destructive actions use `variant="destructive"`).
       - Convert WO status/priority chips to `<StatusBadge>` per WO StatusKey mapping.
       - Convert the Part-A alerts per alert_classification: 172→toast.error; 188→`toast.success(msg); router.back();` together; 194→toast.info; 197→toast.error; 210→toast.error; 242→toast.error; 265→toast.error. **Line 250 escalate-confirm STAYS `Alert.alert`** — leave it exactly as-is (Cancel/Confirm dialog).
    3. Change ONLY feedback lines inside handlers; do not alter `api.*`/`enqueueAction`/state-transition/offline-guard logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/WorkOrderDetail.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useToast" apps/mobile/app/(app)/work-orders/[woId].tsx` >= 1; `grep -c "useTheme" ...` >= 1
    - `grep -c "StatusBadge" apps/mobile/app/(app)/work-orders/[woId].tsx` >= 1
    - The escalate-confirm Alert.alert is still present (Part A must NOT remove it): `grep -c "Alert.alert" apps/mobile/app/(app)/work-orders/[woId].tsx` >= 1 after this task
    - `npx jest __tests__/screens/WorkOrderDetail.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no `api.*`/`enqueueAction`/state-transition/guard line changed
  </acceptance_criteria>
  <done>WO detail Part A on primitives; status actions render via Button/StatusBadge; Part-A outcome alerts are Toasts; escalate-confirm retained; WorkOrderDetail test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate [woId].tsx part B (comments/photos/activity log), drive C. refs to zero</name>
  <files>apps/mobile/app/(app)/work-orders/[woId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/work-orders/[woId].tsx (Part B region: comments, photos/camera, activity log, and any remaining C. refs anywhere in the file; comment/photo handlers around lines 228/279/297)
    - apps/mobile/components/ui/Button.tsx, StateBlock.tsx
  </read_first>
  <action>
    1. Finish the migration for the comments/photos/activity-log sections and any leftover C. refs anywhere in the file (C.*→theme.*, TouchableOpacity buttons→Button, state blocks→StateBlock).
    2. Convert the Part-B alerts per alert_classification: 228→toast.error; 279→toast.error; 297→toast.error.
    3. AFTER this task the file must have ZERO `C.` refs and EXACTLY ONE `Alert.alert` remaining (the escalate-confirm at line 250).
    4. Change only feedback + presentation lines; do not alter comment-submit/photo-upload/api logic or offline guards.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/WorkOrderDetail.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/app/(app)/work-orders/[woId].tsx` returns 0
    - `grep -c "shellTokens" apps/mobile/app/(app)/work-orders/[woId].tsx` returns 0
    - Exactly one Alert.alert remains (escalate confirm): `grep -c "Alert.alert" apps/mobile/app/(app)/work-orders/[woId].tsx` returns 1
    - `npx jest __tests__/screens/WorkOrderDetail.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no comment/photo/api/guard logic changed
  </acceptance_criteria>
  <done>WO detail fully on primitives, zero C. refs, exactly one blocking Alert.alert (escalate confirm) retained, WorkOrderDetail test + type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API / offline queue (claim, complete, set-status, escalate, arrive, comment, photo) | These handlers mix state-transition + RBAC logic with the Alert.alert being converted — high regression risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Elevation of Privilege | Converting a handler's alert accidentally alters the preceding state-transition/`api.*`/RBAC call | mitigate | alert_classification "change only feedback line" rule; acceptance criteria + diff review; WorkOrderDetail test asserts handler behavior |
| T-08-06-02 | Tampering | Escalate-confirm (250) wrongly converted to Toast, removing the Yes/Cancel gate before a state-changing escalation | mitigate | Explicitly locked to STAY Alert.alert; acceptance criteria assert exactly 1 Alert.alert remains after both tasks |
| T-08-06-03 | Tampering | "Completed" success (188) drops its `router.back()` side effect | mitigate | Locked fire-together pattern (`toast.success(msg); router.back();`); diff review |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/WorkOrderDetail.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/work-orders/[woId].tsx` == 0
- `grep -c "Alert.alert" apps/mobile/app/(app)/work-orders/[woId].tsx` == 1
</verification>

<success_criteria>
FLOOR-03 (detail half): engineer acts on a work order via primitives + non-blocking Toasts; the escalate confirmation still blocks; all state-transition/RBAC/offline logic unchanged; zero legacy C tokens in the detail screen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-06-SUMMARY.md`.
</output>
