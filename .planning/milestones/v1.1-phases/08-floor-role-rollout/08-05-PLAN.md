---
phase: 08-floor-role-rollout
plan: 05
type: execute
wave: 3
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/work-orders/index.tsx
  - apps/mobile/components/engineering/WorkOrderCard.tsx
  - apps/mobile/components/engineering/CreateWorkOrderModal.tsx
autonomous: true
requirements: [FLOOR-03]
must_haves:
  truths:
    - "Work Orders list renders search/filter/list via primitives (zero C. refs)"
    - "WorkOrderCard is rebuilt on Card+Pressable with priority/status chips as StatusBadge and claim as Button"
    - "CreateWorkOrderModal renders on primitives; its 2 outcome alerts become Toasts"
    - "Work Orders RBAC/data-fetch behavior is unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/components/engineering/WorkOrderCard.tsx"
      provides: "WorkOrderCard rebuilt on Card+StatusBadge+Button"
      contains: "StatusBadge"
    - path: "apps/mobile/app/(app)/work-orders/index.tsx"
      provides: "WO list on primitives"
      contains: "useTheme"
    - path: "apps/mobile/components/engineering/CreateWorkOrderModal.tsx"
      provides: "Create WO modal on Button + Toast"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/components/engineering/WorkOrderCard.tsx"
      to: "apps/mobile/components/ui/Card.tsx"
      via: "Pressable-wraps-Card shell rebuild"
      pattern: "<Card"
    - from: "apps/mobile/app/(app)/work-orders/index.tsx"
      to: "apps/mobile/components/engineering/WorkOrderCard.tsx"
      via: "list renders rebuilt card (props unchanged)"
      pattern: "WorkOrderCard"
---

<objective>
Migrate the Work Orders list (`work-orders/index.tsx`, 474 lines, 29 `C.` refs, search bar + filter chips), rebuild `WorkOrderCard` (316 lines, 53 `C.` refs — D-08, the StatusBadge reference implementation), and migrate `CreateWorkOrderModal` (318 lines, 31 `C.` refs, 2 alerts, i18n-gate-exempt). Migrating the modal here also benefits Phase 9's Room Status for free (D-10) — but do NOT touch `room-status/index.tsx`.

Purpose: FLOOR-03 (list half) — engineer views/creates work orders on primitives; RBAC/data unchanged.
Output: WO list + WorkOrderCard + CreateWorkOrderModal on primitives, WorkOrdersList test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map + Button/Card/StateBlock/StatusBadge/useToast signatures: see 08-01/08-02 PLAN interfaces (identical).
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
Card has NO onPress → wrap in <Pressable>. Card base = uniform padding:16; caller style applies last.

## WorkOrderCard StatusKey mapping (verified in 08-RESEARCH.md against WorkOrderCard.tsx conditionals):
priority "emergency"→emergency · priority "urgent"→urgent · priority "low"→low
status "on_hold"→onHold · due?.kind==="overdue"→overdue · status "completed"→completed
Task/WO chip labels are caller-provided translated strings (StatusBadge does no internal t()).

## D-11: no icon-only escape hatch — the current bare checkmark "done" badge (WorkOrderCard.tsx:172-175)
becomes a full <StatusBadge statusKey="completed" label={t(...)} />.
</migration_reference>

<interfaces>
## WorkOrderCard current shell (WorkOrderCard.tsx:60-68) — entire card is one TouchableOpacity onPress={onPress}.
## Current styles.card (204-221): asymmetric paddingLeft:16/paddingRight:12/paddingVertical:13/gap:10/position:relative/overflow:hidden + shadow. cardAlert: borderColor C.alertLine.
## Claim button (181-198): TouchableOpacity + ActivityIndicator(C.accent) → <Button label={t("workOrders.claim")} onPress={onClaim} loading={claiming} variant="secondary" size="sm" icon="hand-right-outline" />.
## Priority/status chips (95-134): hand-rolled urgentChip/lowChip/holdChip → StatusBadge per mapping.
## CreateWorkOrderModal alerts (08-RESEARCH.md): line 89 success "Work Order Created" (OK→onClose) → toast.success(msg); onClose(); together. line 91 error → toast.error(msg). Hardcoded EN strings pass through (D-06 exempt — no new t()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild WorkOrderCard on Card+Pressable+StatusBadge+Button (D-08)</name>
  <files>apps/mobile/components/engineering/WorkOrderCard.tsx</files>
  <read_first>
    - apps/mobile/components/engineering/WorkOrderCard.tsx (full — shell 60-68, styles.card 204-221, chips 95-134, done badge 172-175, claim btn 181-198, CATEGORY_META 23-32)
    - apps/mobile/components/ui/Card.tsx, StatusBadge.tsx, Button.tsx
    - apps/mobile/components/shared/tokens.ts (Wave 0 keys)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace the outer `<TouchableOpacity ... onPress={onPress} style={[styles.card, ...alert]}>` with Pressable-wraps-Card:
       ```tsx
       <Pressable onPress={onPress} accessibilityRole="button" accessibilityLabel={wo.title} testID={`wo-${wo.id}`}>
         <Card style={[styles.cardLayoutOverrides, (isEmergency || isUrgent || due?.kind === "overdue") && styles.cardAlertOverride]}>
           {/* existing rail/tile/body/chips content */}
         </Card>
       </Pressable>
       ```
       `styles.cardLayoutOverrides` re-applies ONLY the non-uniform layout (paddingLeft:16/paddingRight:12/paddingVertical:13/gap:10/position:"relative"/overflow:"hidden"); Card owns bg/border/radius/shadow. `styles.cardAlertOverride` = `{ borderColor: theme.status.dirtyLine }` (was C.alertLine).
    3. Convert priority/status chips (95-134) to StatusBadge: emergency→`statusKey="emergency"`, urgent→`"urgent"`, low→`"low"`, on_hold→`"onHold"`, overdue→`"overdue"`. Use the existing `t(...)` labels (e.g. `t("workOrders.chipEmergency")`).
    4. Convert the bare checkmark "done" badge (172-175) to `<StatusBadge statusKey="completed" label={t(...)} />` (D-11 — no icon-only).
    5. Convert the claim button (181-198) to `<Button label={t("workOrders.claim")} onPress={onClaim} loading={claiming} variant="secondary" size="sm" icon="hand-right-outline" />`.
    6. Replace all remaining `C.*` refs (including CATEGORY_META category tile + location/guest chips, which have no StatusKey — keep hand-rolled, theme colors only) with `theme.*` per map. `C.brassSoft/brassLine` (furniture category) → `theme.accentBrassSoft/accentBrassLine`.
    7. Do NOT change `onClaim`/`onPress` prop contracts or the card's external props — the list calls it unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/WorkOrdersList.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/components/engineering/WorkOrderCard.tsx` returns 0
    - `grep -c "<Card" apps/mobile/components/engineering/WorkOrderCard.tsx` >= 1 and `grep -c "Pressable" ...` >= 1
    - `grep -c "StatusBadge" apps/mobile/components/engineering/WorkOrderCard.tsx` >= 1 (chips + completed)
    - `grep -c "shellTokens" apps/mobile/components/engineering/WorkOrderCard.tsx` returns 0
    - `wo-` testID preserved: `grep -c "wo-" apps/mobile/components/engineering/WorkOrderCard.tsx` >= 1
    - `npx jest __tests__/screens/WorkOrdersList.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: onClaim/onPress/props unchanged
  </acceptance_criteria>
  <done>WorkOrderCard rebuilt on Card+Pressable, chips + done state are StatusBadge, claim is Button, zero C. refs, tap+claim preserved, list test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate work-orders/index.tsx (search/filter/list)</name>
  <files>apps/mobile/app/(app)/work-orders/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/work-orders/index.tsx (full — 474 lines, 29 C. refs; search bar placeholder + filter chips use C.ink4; loading/empty/error blocks; renders WorkOrderCard)
    - apps/mobile/components/ui/StateBlock.tsx
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 29 `C.*` refs with `theme.*` (map above; `C.ink4`→`theme.textDisabled` for the search placeholder/icon and filter chips). Keep StyleSheet layout; inline colors only. Switch direct `shellTokens` imports to `theme.shell.*`.
    3. Convert loading/empty/error blocks to `<StateBlock>` (reuse existing `t(...)` keys).
    4. Leave the `WorkOrderCard` call sites' props unchanged (Task 1 kept its external contract). Do NOT change the WO fetch, search/filter state logic, RBAC branches, or offline behavior — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/WorkOrdersList.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/app/(app)/work-orders/index.tsx` returns 0
    - `grep -c "shellTokens" apps/mobile/app/(app)/work-orders/index.tsx` returns 0
    - `grep -c "useTheme" apps/mobile/app/(app)/work-orders/index.tsx` >= 1; `grep -c "StateBlock" ...` >= 1
    - `npx jest __tests__/screens/WorkOrdersList.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no fetch/search/filter/RBAC line changed
  </acceptance_criteria>
  <done>WO list on primitives, zero C. refs, WorkOrdersList test + type-check + lint green, data/RBAC behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate CreateWorkOrderModal (2 alerts → Toast)</name>
  <files>apps/mobile/components/engineering/CreateWorkOrderModal.tsx</files>
  <read_first>
    - apps/mobile/components/engineering/CreateWorkOrderModal.tsx (full — 318 lines, 31 C. refs, 2 Alert.alert at 89/91, i18n-gate-exempt, hardcoded EN strings)
    - apps/mobile/components/ui/Button.tsx
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add useTheme + useToast imports/calls.
    2. Replace all 31 `C.*` refs with `theme.*`; convert submit/cancel `TouchableOpacity` buttons to `<Button>`. Keep the modal's own Modal/overlay wrapper untouched.
    3. Convert the 2 alerts: line 89 success ("Work Order Created", OK→onClose) → `toast.success(msg); onClose();` fired together (no dismiss gate); line 91 error → `toast.error(msg)`. Pass the existing hardcoded English strings through as-is (D-06 exempt — do NOT add t()).
    4. Do NOT change the create-WO submit/api call or form state. Do NOT touch `room-status/index.tsx` (the modal's other call site, out of scope D-10).
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/components/engineering/CreateWorkOrderModal.tsx` returns 0
    - Zero Alert.alert: `grep -c "Alert.alert" apps/mobile/components/engineering/CreateWorkOrderModal.tsx` returns 0
    - `grep -c "useToast" apps/mobile/components/engineering/CreateWorkOrderModal.tsx` >= 1
    - `npm run type-check` + `npm run lint` pass (lint confirms no new i18n-gate violation introduced)
    - No `room-status/index.tsx` change in diff; submit/api logic unchanged
  </acceptance_criteria>
  <done>CreateWorkOrderModal on primitives, both alerts are Toasts (success fires onClose together), zero C. refs, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (WO list fetch, claim action, create-WO submit) | Engineer-role-gated WO operations; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Elevation of Privilege | Restyling the WO list drops a role/tenant-scoped fetch or an RBAC branch (engineer vs other roles) | mitigate | Presentation-only edits; WorkOrdersList test asserts data behavior; diff review confirms fetch/RBAC unchanged |
| T-08-05-02 | Tampering | WorkOrderCard rebuild drops `onPress`/`onClaim` (Card has no onPress) leaving cards non-interactive | mitigate | Pressable-wraps-Card mandated; `wo-` testID + claim Button asserted; list test exercises card |
| T-08-05-03 | Tampering | CreateWorkOrderModal success side effect `onClose()` dropped converting its OK-gated alert | mitigate | Locked fire-together pattern; acceptance criteria assert 0 Alert.alert + diff review |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/WorkOrdersList.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." <work-orders/index.tsx | WorkOrderCard.tsx | CreateWorkOrderModal.tsx>` == 0 each
- `grep -c "Alert.alert" CreateWorkOrderModal.tsx` == 0
</verification>

<success_criteria>
FLOOR-03 (list half): engineer views WO list + creates a WO on primitives; WorkOrderCard is Card+StatusBadge+Button; RBAC/data unchanged; zero legacy C tokens in the three files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-05-SUMMARY.md`.
</output>
