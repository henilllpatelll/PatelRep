---
phase: 08-floor-role-rollout
plan: 04
type: execute
wave: 2
depends_on: ["08-00"]
files_modified:
  - apps/mobile/__tests__/screens/RoomBoard.test.tsx
  - apps/mobile/app/(app)/room-board/index.tsx
autonomous: true
requirements: [FLOOR-02]
must_haves:
  truths:
    - "A render/smoke test exists for the Room Board screen (previously none existed)"
    - "Room Board renders loading/empty/error via StateBlock, room status via StatusBadge, and colors via useTheme"
    - "Room Board's fetch and offline-sync behavior are unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/__tests__/screens/RoomBoard.test.tsx"
      provides: "Automated render test for room-board/index (Nyquist gate for FLOOR-02)"
      contains: "room-board"
    - path: "apps/mobile/app/(app)/room-board/index.tsx"
      provides: "Room Board on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/room-board/index.tsx"
      to: "apps/mobile/components/ui/StatusBadge.tsx"
      via: "room status chip → StatusBadge"
      pattern: "StatusBadge"
---

<objective>
Migrate the Room Board screen (`room-board/index.tsx`, 413 lines, 25 `C.` refs, 0 Alert.alert) onto Phase 7 primitives. This is a single screen with no list+detail pair and no in-scope reachable modals (D-10 excludes `room-status/index.tsx`, which is a DIFFERENT Phase 9 screen).

**Correction baked in:** Room Board has NO existing test. `__tests__/screens/RoomStatusList.test.tsx` imports `@/app/(app)/room-status/index` (the out-of-scope Phase 9 screen), NOT room-board. So this plan's Task 1 creates the missing render test (Nyquist compliance for FLOOR-02) before the migration.

Purpose: FLOOR-02 — housekeeper/supervisor view the board on primitives; offline-sync/data unchanged.
Output: RoomBoard.test.tsx created + green; room-board/index.tsx on primitives, zero C. refs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map + StateBlock/StatusBadge signatures: see 08-01-PLAN.md (identical).
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
Room StatusKey mapping: DIRTY→dirty · OCCUPIED→occupied · PICKUP→pickup · IN_PROGRESS→inProgress · CLEAN→clean · INSPECTED→ready · OOO/OUT_OF_ORDER/OUT_OF_SERVICE→outOfOrder
</migration_reference>

<interfaces>
## Existing screen-test scaffold pattern to mirror (from __tests__/screens/RoomStatusList.test.tsx):
- `import { render, waitFor } from "@testing-library/react-native";`
- Mocks `@/lib/api/client` (`jest.mock("@/lib/api/client", ...)`) to return canned room data
- `import RoomStatusScreen from "@/app/(app)/room-status/index";` ← for RoomBoard.test.tsx, import from `@/app/(app)/room-board/index` instead
- Renders the screen and asserts a known label / testID appears
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoomBoard.test.tsx render/smoke test (Nyquist gate)</name>
  <files>apps/mobile/__tests__/screens/RoomBoard.test.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/room-board/index.tsx (the screen under test — note its default export name, the api client it calls, and a stable label/testID to assert on)
    - apps/mobile/__tests__/screens/RoomStatusList.test.tsx (closest existing scaffold to mirror — mocking style, render/waitFor usage; note it targets room-status, NOT room-board)
    - apps/mobile/__tests__/screens/MyRoomsScreen.test.tsx (second reference for the mocking approach on a room-list screen)
  </read_first>
  <action>
    Create `apps/mobile/__tests__/screens/RoomBoard.test.tsx` mirroring the structure of `RoomStatusList.test.tsx` but importing the Room Board screen from `@/app/(app)/room-board/index`. The test must:
    1. Mock `@/lib/api/client` (and any other data source room-board imports — check its imports) to return a small canned set of rooms across a couple of statuses.
    2. Render the screen and `await waitFor(...)` for a stable assertion: e.g. a room number / status label / testID that room-board renders for a fetched room. Use an assertion that will still hold AFTER the primitive migration (assert on data-driven text/testID, NOT on a specific color or the legacy chip markup).
    3. Include at least a render-without-crash assertion for the loading and the populated state.
    This test must pass against the CURRENT (pre-migration) room-board so it is a genuine regression guard, then continue passing after Task 2.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomBoard.test.tsx && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/mobile/__tests__/screens/RoomBoard.test.tsx`
    - It imports the room-board screen: `grep -c "app/(app)/room-board" apps/mobile/__tests__/screens/RoomBoard.test.tsx` >= 1
    - `npx jest __tests__/screens/RoomBoard.test.tsx` passes against current (un-migrated) code
    - `npm run type-check` passes
  </acceptance_criteria>
  <done>RoomBoard.test.tsx exists and passes against current code, giving FLOOR-02 an automated verify before migration.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate room-board/index.tsx to primitives</name>
  <files>apps/mobile/app/(app)/room-board/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/room-board/index.tsx (full — 413 lines, 25 C. refs; note it uses C.ink4 per RESEARCH; find its loading/empty/error blocks and status chips)
    - apps/mobile/components/ui/StatusBadge.tsx, StateBlock.tsx
    - apps/mobile/components/shared/tokens.ts (confirm Wave 0 keys exist)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 25 `C.*` refs with `theme.*` equivalents (map above; `C.ink4`→`theme.textDisabled`). Keep StyleSheet layout; inline only colors. Switch any direct `shellTokens` color import to `theme.shell.*`.
    3. Convert loading/empty/error blocks to `<StateBlock>` (loading/empty/error props per its union; pass existing `t(...)` keys).
    4. Convert room-status chips to `<StatusBadge statusKey=... label={...} />` using the room StatusKey mapping above.
    5. Do NOT change the room fetch, filtering/grouping, or offline logic — presentation only. Do NOT touch `room-status/index.tsx` (out of scope, D-10).
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomBoard.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/app/(app)/room-board/index.tsx` returns 0
    - `grep -c "shellTokens" apps/mobile/app/(app)/room-board/index.tsx` returns 0
    - `grep -c "useTheme" apps/mobile/app/(app)/room-board/index.tsx` >= 1
    - `grep -c "StatusBadge" apps/mobile/app/(app)/room-board/index.tsx` >= 1
    - `npx jest __tests__/screens/RoomBoard.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - No `room-status/index.tsx` change in the diff
  </acceptance_criteria>
  <done>room-board/index.tsx on primitives, zero C. refs, RoomBoard test + type-check + lint green, data behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (room-board fetch) | Read-only board fetch; not modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | Styling diff drops the `hotel_id`-scoped board fetch or role filter | mitigate | Presentation-only edits; new RoomBoard.test.tsx asserts data-driven render; diff review confirms fetch/filter unchanged |
| T-08-04-02 | Spoofing | Migration accidentally edits `room-status/index.tsx` (adjacent Phase 9 screen sharing naming) instead of `room-board` | mitigate | Explicit file scope; acceptance criteria assert no room-status change; RoomStatusList.test.tsx (room-status's own test) stays green in the wave-merge run |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/RoomBoard.test.tsx` green (new)
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/room-board/index.tsx` == 0
</verification>

<success_criteria>
FLOOR-02: a housekeeper/supervisor sees Room Board rendered with StateBlock + StatusBadge + themed colors; offline-sync/data unchanged; a genuine automated render test now guards the screen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-04-SUMMARY.md`.
</output>
