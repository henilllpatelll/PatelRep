---
phase: 10-dark-mode-accessibility-qa
plan: 09
type: execute
wave: 3
depends_on: [10-03, 10-04, 10-05, 10-06, 10-07, 10-08]
files_modified:
  - apps/mobile/__tests__/components/ThemeSourceAudit.test.ts
  - apps/mobile/__tests__/components/AccessibilityContracts.test.tsx
autonomous: true
requirements: [DARK-02, DARK-03, DARK-05]
must_haves:
  truths:
    - "Automated source checks reject normal runtime C-token islands, auto status-bar mode, and accidental removal of the explicit Copilot dark lock"
    - "Shared controls expose stable roles, names, selected/disabled/busy state, and minimum targets under both effective modes"
    - "The full mobile Jest, type-check, lint/i18n, and Android export gates pass together after all dark/a11y changes"
    - "Automated results are treated as release evidence but not as a replacement for rendered Android inspection"
  artifacts:
    - path: "apps/mobile/__tests__/components/ThemeSourceAudit.test.ts"
      provides: "App-wide static theme/D-11/chrome/i18n source guard"
    - path: "apps/mobile/__tests__/components/AccessibilityContracts.test.tsx"
      provides: "Cross-primitive light/dark semantics and target contract"
  key_links:
    - from: "apps/mobile/__tests__/components/ThemeSourceAudit.test.ts"
      to: "apps/mobile/app, components, and lib runtime trees"
      via: "explicit exclusions for tokens compatibility data and D-11 darkTheme"
      pattern: "\\bC\\."
    - from: "full mobile gates"
      to: "Android Metro export"
      via: "npx expo export --platform android after Jest/type/lint"
      pattern: "expo export --platform android"
---

<objective>
Run the complete automated dark-mode/accessibility regression gate and produce a clean Android bundle before cloud/device closeout.

Purpose: This catches cross-plan integration defects, stale static token islands, i18n drift, and Android bundling failures while clearly preserving the mandatory human boundary.
Output: App-wide source/accessibility guards plus green full-suite, type, lint, and Android export evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-VERIFICATION.md
@.planning/phases/09-remaining-screens-rollout/09-UAT.md
@apps/mobile/__tests__/components/MobileVisualTokens.test.ts
@apps/mobile/lib/navigation/roleTabs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app-wide theme-source and accessibility integration guards</name>
  <files>apps/mobile/__tests__/components/ThemeSourceAudit.test.ts, apps/mobile/__tests__/components/AccessibilityContracts.test.tsx</files>
  <action>
Create an explicit runtime inventory across `apps/mobile/app`, `components`, and `lib` (exclude Android generated code, tests, and the compatibility declaration inside `tokens.ts`). Assert normal runtime files contain no `C.*`, root contains no `StatusBar style="auto"`, theme switching has no Animated/LayoutAnimation transition, and light/dark theme key shapes stay compatible. Assert Copilot still imports/uses `darkTheme` and does not adopt `useTheme` for its content, while surrounding layout chrome is theme-driven. Assert EN/ES locale leaf-key parity and the existing roleTabs tests remain authoritative.

Create a cross-primitive render contract using the real providers/mocked storage/scheme. Exercise light, dark, and System rerender for Button, appearance selector seam, StatusBadge, OfflineBanner, Toast, IconButton/Segmented, and representative navigation chrome. Assert accessible role/name/order/state and >=44 targets where interactive; ensure badges retain icon+label and reduced-motion Toast reaches its final state without animation.

Keep these tests focused on shared contracts; do not claim they render every route or replace TalkBack/200% Android testing.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/ThemeSourceAudit.test.ts __tests__/components/AccessibilityContracts.test.tsx --runInBand</verify>
  <done>App-wide static invariants and cross-primitive accessibility contracts pass under light, dark, and System modes.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full mobile and local Android release gates</name>
  <files>apps/mobile/__tests__/components/ThemeSourceAudit.test.ts, apps/mobile/__tests__/components/AccessibilityContracts.test.tsx</files>
  <action>
Run gates serially from `apps/mobile`: full Jest `--runInBand` (the known parallel timeout flake is not acceptable evidence), type-check, lint/i18n, then Android Expo export. Use a temporary export directory if supported and remove only that intentional output after success; retain no generated bundle in git.

If a check fails, stop, fix the failure in the owning Phase 10 implementation/test file, rerun the narrow check, then rerun the complete sequence. Do not bypass lint/i18n, weaken contrast thresholds, delete tests, or mark an Expo export as the EAS build/device gate. Capture exact suite/test counts, commands, Android module/file counts, and any warnings in `10-09-SUMMARY.md` for final verification.

Run a final diff/static review confirming no workflow/data/routing/RBAC/API/offline changes and no new runtime dependency.
  </action>
  <verify>cd apps/mobile; npm test -- --runInBand; npm run type-check; npm run lint; npx expo export --platform android</verify>
  <done>All automated mobile gates and Android export pass in one final state, outputs are cleaned, and evidence is recorded without claiming human/EAS completion.</done>
</task>

</tasks>

<verification>
- Full Jest, type-check, lint/i18n, and Android export all pass.
- Runtime `C.*` only exists as compatibility data in tokens, not as a rendered source.
- Copilot D-11 static-dark content and theme-reactive surrounding chrome are both guarded.
- No generated export directory, debug file, or new dependency remains.
</verification>

<success_criteria>
- Automated integration is green after all Phase 10 code.
- Static/token/semantic regressions become repeatable failures.
- The plan explicitly leaves DARK-04 and rendered Android/TalkBack/200% evidence open for 10-10.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-09-SUMMARY.md`.
</output>
