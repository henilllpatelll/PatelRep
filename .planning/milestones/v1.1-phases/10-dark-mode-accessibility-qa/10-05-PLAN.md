---
phase: 10-dark-mode-accessibility-qa
plan: 05
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/components/shared/evening.tsx
  - apps/mobile/components/shared/mobileHandoff.tsx
  - apps/mobile/components/home/SupervisorHome.tsx
  - apps/mobile/components/home/CompanionHome.tsx
  - apps/mobile/components/supervisor/atoms.tsx
  - apps/mobile/__tests__/components/SharedThemeComposites.test.tsx
autonomous: true
requirements: [DARK-02, DARK-05]
must_haves:
  truths:
    - "Every normal shared Evening Lobby composite resolves colors from the active theme instead of the light-only C snapshot"
    - "Status rails, pills, mosaics, progress, chips, hero buttons, segmented controls, and AI-adjacent cards preserve behavior and status meaning in both themes"
    - "Interactive shared composites have meaningful names/state and at least 44pt targets"
    - "Copilot dark presentation paths continue to use explicit darkTheme tokens while non-Copilot branches follow the app theme"
  artifacts:
    - path: "apps/mobile/components/shared/evening.tsx"
      provides: "Theme-reactive room/status shared composites"
    - path: "apps/mobile/components/shared/mobileHandoff.tsx"
      provides: "Theme-reactive shared handoff controls with explicit D-11 branch"
    - path: "apps/mobile/__tests__/components/SharedThemeComposites.test.tsx"
      provides: "Light/dark render, target, semantics, and D-11 regression coverage"
  key_links:
    - from: "apps/mobile/components/shared/evening.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "render-time status/surface token resolution"
      pattern: "useTheme"
    - from: "apps/mobile/components/supervisor/atoms.tsx"
      to: "apps/mobile/components/shared/evening.tsx"
      via: "getStatusMeta receives current theme"
      pattern: "getStatusMeta"
    - from: "apps/mobile/components/shared/mobileHandoff.tsx"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "darkTheme only for explicit Copilot dark branch"
      pattern: "darkTheme"
---

<objective>
Remove the remaining light-only shared-composite islands while preserving the deliberate Copilot dark exception.

Purpose: Phase 8/9 screens call these helpers extensively. A themed route still fails DARK-02 if a shared rail, status resolver, segmented control, or AI handoff card snapshots `C`.
Output: Theme-aware shared composites, updated callers for pure status helpers, accessibility/target fixes, and focused dual-mode tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/09-remaining-screens-rollout/09-14-SUMMARY.md
@apps/mobile/components/shared/evening.tsx
@apps/mobile/components/shared/mobileHandoff.tsx

<interfaces>
`getStatusMeta` and `getToneColors` are also used as pure helpers. Keep them deterministic by accepting a `ThemeTokens` argument (or a backward-compatible light default for non-render metadata), while every color-rendering call site passes the active theme. React hooks must never be called at module scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make room/status Evening composites theme-reactive</name>
  <files>apps/mobile/components/shared/evening.tsx, apps/mobile/components/home/SupervisorHome.tsx, apps/mobile/components/home/CompanionHome.tsx, apps/mobile/components/supervisor/atoms.tsx, apps/mobile/__tests__/components/SharedThemeComposites.test.tsx</files>
  <action>
Write failing light/dark tests around StatusPill/StatusRail/ProgressBar/Chip/RoomQueueCard and pure status metadata before changing production code. Assert the operational mapping, translated label/icon/pattern cues, and occupied stripe remain stable while foreground/fill/border values differ appropriately by theme.

Replace module-scope color-bearing `STATUS_META`/`C.*` snapshots with semantic token-key metadata plus a resolver that receives `ThemeTokens`. Components call `useTheme()` at render; pure external callers pass their current theme. Update SupervisorHome, CompanionHome, and supervisor atoms accordingly without changing navigation, data, props, room status enums, labels, ordering, or handlers. Use the new explicit purple in-progress token rather than the pickup/yellow family.

Move every remaining static StyleSheet color in `evening.tsx` to render-time theme objects. Preserve the striped occupied rail, Card/Pressable shell, all test IDs, and unknown-status fallback. Do not alter offline/data workflow.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/SharedThemeComposites.test.tsx __tests__/screens/RoomBoard.test.tsx __tests__/screens/MyRoomsScreen.test.tsx --runInBand; npm run type-check</verify>
  <done>Evening composites and all status-meta callers render correctly in both modes with no runtime `C.*` and no status/workflow change.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate general handoff composites and harden their accessibility</name>
  <files>apps/mobile/components/shared/mobileHandoff.tsx, apps/mobile/__tests__/components/SharedThemeComposites.test.tsx</files>
  <action>
Add failing tests for light/dark tone resolution, HeroButton, FloatingAIButton, Segmented, IconButton, and CopilotHero. Cover >=44pt for interactive shapes, required/forwarded accessible names, radio/selected semantics for segmented options, disabled state where present, text wrapping, and explicit D-11 behavior.

Remove all runtime `C.*` use from normal branches. Components call `useTheme()`; pure helpers accept an active theme and keep a light default only where existing test/public helper compatibility requires it. Move color-bearing StyleSheet values inline so mode changes update without remount.

For `CopilotHero tone="dark"` and the actual D-11 surface, continue using `darkTheme` explicitly for content. Its alternate/non-dark branch must use the current app theme. Do not route the Copilot screen through light surfaces, modify bubbles, endpoints, send/mic gestures, history, or AI behavior.

Raise interactive defaults to >=44 without inflating non-interactive decorative avatars; ensure wrappers do not create duplicate TalkBack announcements. Add/forward `accessibilityLabel`, role, selected/disabled state as appropriate. Theme changes use no animation.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/SharedThemeComposites.test.tsx __tests__/screens/EngineerHome.test.tsx __tests__/screens/CopilotScreen.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>Shared handoff controls are theme-reactive and accessible, with the Copilot dark-only content contract explicitly preserved.</done>
</task>

</tasks>

<verification>
- Focused composite plus dependent screen suites pass.
- `npm run type-check` and `npm run lint` pass.
- `rg -n "\\bC\\." apps/mobile/components/shared/evening.tsx apps/mobile/components/shared/mobileHandoff.tsx` returns no runtime match.
- `darkTheme` remains explicitly tested for Copilot dark content only.
- Room/status mappings, occupied stripe, navigation callbacks, and API/data behavior are unchanged.
</verification>

<success_criteria>
- Shared helper output never freezes a light palette in dark mode.
- High-frequency status and handoff composites remain legible, operable, and non-color-dependent.
- D-11 remains a deliberate dark content surface surrounded by app-themed chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-05-SUMMARY.md`.
</output>
