---
phase: 10-dark-mode-accessibility-qa
plan: 07
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/components/housekeeping/SupplyRequestModal.tsx
  - apps/mobile/components/housekeeping/ReportIssueModal.tsx
  - apps/mobile/components/housekeeping/KnockModal.tsx
  - apps/mobile/components/housekeeping/ChecklistSection.tsx
  - apps/mobile/app/(app)/inspect/index.tsx
  - apps/mobile/__tests__/components/HousekeepingSemanticOnColor.test.ts
autonomous: true
requirements: [DARK-02, DARK-05]
must_haves:
  truths:
    - "Housekeeping and inspection controls never assume white text/icon over a semantic color that changes between themes"
    - "Owned primary, destructive, and selected-state foregrounds resolve through contrast-tested tokens"
    - "Owned icon-only and status controls expose meaningful labels/state and interactive targets are at least 44pt"
    - "At 200% text scale, owned modal/action rows use minimum rather than fixed heights and do not hide required actions"
    - "Photo actions, mutations, confirmations, callbacks, payloads, and RBAC behavior remain unchanged"
  artifacts:
    - path: "apps/mobile/__tests__/components/HousekeepingSemanticOnColor.test.ts"
      provides: "Source-level guard against theme-unsafe raw on-colors in audited housekeeping and inspection files"
    - path: "apps/mobile/components/housekeeping/ReportIssueModal.tsx"
      provides: "Theme-safe destructive and primary modal actions"
    - path: "apps/mobile/app/(app)/inspect/index.tsx"
      provides: "Theme-safe inspection status and action controls"
  key_links:
    - from: "owned floor controls"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "theme.onPrimary/onDestructive/onAi and semantic soft fills"
      pattern: "onPrimary|onDestructive|onAi"
    - from: "apps/mobile/__tests__/components/HousekeepingSemanticOnColor.test.ts"
      to: "the five owned housekeeping and inspection runtime files"
      via: "explicit audited source inventory"
      pattern: "#fff|#FFFFFF"
---

<objective>
Burn down the known hardcoded on-colors and large-text/target risks in housekeeping modals and inspection controls.

Purpose: Token AA tests only protect callers that use semantic roles. These five audited files currently contain raw white on dynamic primary, destructive, or selected backgrounds.
Output: A bounded source guard and presentation-only fixes for housekeeping and inspection controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-VERIFICATION.md
@.planning/phases/09-remaining-screens-rollout/09-VERIFICATION.md
@apps/mobile/components/shared/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the housekeeping/inspection source and target contract</name>
  <files>apps/mobile/__tests__/components/HousekeepingSemanticOnColor.test.ts</files>
  <action>
Create a test with an explicit list of the five owned housekeeping and inspection runtime files. Make it fail on raw `#fff/#FFFFFF` foregrounds identified by research/current audit. Add targeted source assertions that each file resolves semantic foregrounds through the current theme.

Alongside the automated guard, record a per-file checklist in the test comments for interactive `Pressable`/`TouchableOpacity` controls: >=44x44 or minHeight 44, accessible label/role, selected/disabled/busy state where applicable, and no fixed-height text container that hides a required action at 200%. Do not create a broad regex that misclassifies decorative dots/avatars as touch targets.

Run the test and retain the RED output before production edits.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/HousekeepingSemanticOnColor.test.ts --runInBand</verify>
  <done>The explicit source contract fails on the currently known unsafe foreground/fill sites and documents the human-reviewed target inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Fix housekeeping and inspection semantic foregrounds/targets</name>
  <files>apps/mobile/components/housekeeping/SupplyRequestModal.tsx, apps/mobile/components/housekeeping/ReportIssueModal.tsx, apps/mobile/components/housekeeping/KnockModal.tsx, apps/mobile/components/housekeeping/ChecklistSection.tsx, apps/mobile/app/(app)/inspect/index.tsx</files>
  <action>
Replace raw white foregrounds on primary/destructive/status selections with `theme.onPrimary`/`onDestructive` (or the exact tested semantic on-role for that fill). Use theme soft/border roles for selection and failure boundaries. Preserve all status label/icon cues.

Audit each interactive control in these files against the Task 1 checklist. Raise only actual touch targets below 44, add/forward meaningful accessibility labels/roles and selected/disabled/busy state, and replace fixed action heights with minimum/flexible layouts where Spanish or 200% scaling can clip. Keep modal/backdrop structure, photo source/action sheets, camera/photo behavior, submit payloads, guards, Toast/Alert classifications, and callbacks unchanged.

Run the existing focused modal/inspect tests after each file group; if a behavior assertion fails, stop and restore the behavior before continuing.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/HousekeepingSemanticOnColor.test.ts __tests__/components/ReportIssueModal.test.tsx __tests__/screens/InspectorQueue.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>Housekeeping/inspection controls use semantic on-colors, meet target/semantics requirements, and retain their existing workflows.</done>
</task>

</tasks>

<verification>
- Housekeeping/inspection semantic source guard and all named focused suites pass.
- `npm run type-check` and `npm run lint` pass.
- No raw audited white foreground remains in the five files.
- Diff review confirms only styles, accessibility props, and layout minima changed; handlers, endpoints, payloads, routing, RBAC, offline queueing, and confirmations did not.
</verification>

<success_criteria>
- High-frequency floor actions remain readable in both modes and at 200% text scale.
- Interactive targets/semantics meet the locked accessibility bar.
- Existing bilingual, offline, and role behavior stays intact.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-07-SUMMARY.md`.
</output>
