---
phase: 10-dark-mode-accessibility-qa
plan: 06
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/components/ui/Button.tsx
  - apps/mobile/components/ui/Card.tsx
  - apps/mobile/components/ui/StatusBadge.tsx
  - apps/mobile/components/shared/OfflineBanner.tsx
  - apps/mobile/lib/accessibility/useReducedMotion.ts
  - apps/mobile/lib/theme/ToastProvider.tsx
  - apps/mobile/__tests__/components/SharedPrimitiveAccessibility.test.tsx
  - apps/mobile/__tests__/components/OfflineBanner.test.tsx
  - apps/mobile/__tests__/lib/theme/ToastProvider.test.tsx
autonomous: true
requirements: [DARK-02, DARK-05]
must_haves:
  truths:
    - "Shared Buttons, Cards, and StatusBadges use contrast-tested semantic foreground/surface/border roles in both themes"
    - "Button exposes meaningful disabled and busy state and every size remains at least 44pt"
    - "OfflineBanner and Toast are themed, bilingual/semantic, readable at large text, and correctly announced by TalkBack"
    - "Toast remains below OfflineBanner and suppresses entry/exit/spring motion when Android reduce-motion is enabled"
    - "Cards and overlays remain distinguishable in dark mode by tonal layer plus visible border, not shadow alone"
  artifacts:
    - path: "apps/mobile/lib/accessibility/useReducedMotion.ts"
      provides: "Live OS reduce-motion preference hook"
      exports: ["useReducedMotion"]
    - path: "apps/mobile/lib/theme/ToastProvider.tsx"
      provides: "Theme-safe, accessible, reduced-motion-aware Toast viewport"
    - path: "apps/mobile/components/shared/OfflineBanner.tsx"
      provides: "Translated theme-safe connection banner"
    - path: "apps/mobile/__tests__/components/SharedPrimitiveAccessibility.test.tsx"
      provides: "Target, semantic-state, status cue, and dark-border regression coverage"
  key_links:
    - from: "apps/mobile/components/ui/Button.tsx"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "onPrimary/onDestructive semantic foregrounds"
      pattern: "onPrimary|onDestructive"
    - from: "apps/mobile/lib/theme/ToastProvider.tsx"
      to: "apps/mobile/lib/accessibility/useReducedMotion.ts"
      via: "skip Animated timing/spring when reduce motion is on"
      pattern: "useReducedMotion"
    - from: "apps/mobile/components/shared/OfflineBanner.tsx"
      to: "apps/mobile/i18n/locales"
      via: "existing common.offline key"
      pattern: "common\\.offline"
---

<objective>
Harden the shared primitive and overlay layer for contrast, semantics, large text, and reduced motion.

Purpose: Most migrated routes inherit these components. Fixing them centrally provides broad DARK-02/DARK-05 coverage while preserving floor workflows.
Output: Semantic primitives, live reduce-motion hook, themed/translated OfflineBanner, accessible Toast, and focused tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/07-theme-foundation-primitives/07-VERIFICATION.md
@.planning/phases/08-floor-role-rollout/08-VERIFICATION.md
@apps/mobile/components/ui/Button.tsx
@apps/mobile/components/ui/Card.tsx
@apps/mobile/components/ui/StatusBadge.tsx
@apps/mobile/components/shared/OfflineBanner.tsx
@apps/mobile/lib/theme/ToastProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply semantic contrast and accessibility contracts to primitives</name>
  <files>apps/mobile/components/ui/Button.tsx, apps/mobile/components/ui/Card.tsx, apps/mobile/components/ui/StatusBadge.tsx, apps/mobile/__tests__/components/SharedPrimitiveAccessibility.test.tsx</files>
  <action>
Write render tests first for all Button sizes/variants/states, Card normal/dimmed dark separation, and every StatusBadge key. Assert minHeight >=44 for Buttons; role/name plus disabled/busy states; caller style still merges last; badges always render label + icon + color and expose one meaningful accessible label; occupied remains non-color-dependent; and in-progress uses the dedicated purple semantic rather than pickup yellow.

Replace hardcoded white with 10-02 semantic on-colors. Keep loading label width/no-layout-shift behavior and existing props/test IDs. Ensure pressed/disabled pairings still pass the token contract. Card must retain a visible contrast-safe border/tonal layer when dark even if shadow is ineffective. StatusBadge uses explicit `status.inProgress`/soft/line tokens, permits text growth, and does not become an interactive 44pt control.

Do not add a status meaning, change a label, introduce icon-only badges, or alter caller actions.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/SharedPrimitiveAccessibility.test.tsx __tests__/components/MobileVisualTokens.test.ts --runInBand; npm run type-check</verify>
  <done>Shared primitives use only tested semantic pairs and expose the required target/state/non-color contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Theme, translate, and announce OfflineBanner</name>
  <files>apps/mobile/components/shared/OfflineBanner.tsx, apps/mobile/__tests__/components/OfflineBanner.test.tsx</files>
  <action>
Update the existing tests first to render under ThemeProvider/i18n and fail on the current hardcoded English/red/white implementation. Cover online null render, translated `common.offline`, dark/light semantic colors, visible boundary, and accessibility live-region/alert semantics.

Implement with `useTheme()` and `useTranslation()`, using the contrast-tested banner/destructive foreground/fill/border roles. Keep its inline measured layout and `width:"100%"` so `ToastViewport topOffset={insets.top + bannerHeight}` remains correct. Permit multiline/200% text growth; no fixed height or `numberOfLines`. Do not modify store/network/sync behavior.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/OfflineBanner.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>OfflineBanner remains store-driven and layout-compatible while becoming themed, translated, contrast-safe, and TalkBack-announced.</done>
</task>

<task type="auto">
  <name>Task 3: Respect reduced motion and accessibility in Toast</name>
  <files>apps/mobile/lib/accessibility/useReducedMotion.ts, apps/mobile/lib/theme/ToastProvider.tsx, apps/mobile/__tests__/lib/theme/ToastProvider.test.tsx</files>
  <action>
Write failing tests for a live reduced-motion hook using `AccessibilityInfo.isReduceMotionEnabled()` plus `reduceMotionChanged`, cleanup on unmount, and both motion branches. Cover Toast role/live announcement, full message at large text (no two-line clipping), theme/on-color/border selection for success/error/info, replacement race safety, auto-dismiss durations, and swipe behavior.

Implement `useReducedMotion()` from React Native only. In Toast, theme switching remains unanimated. When reduce motion is true, set final entry values directly, dismiss directly on exit, and reset drag without `Animated.timing`/`spring`; when false retain the existing 150-200ms transitions and swipe threshold. Keep one-toast replacement, 3s/5s durations, topOffset positioning, and the finished=false race guard. Use semantic on/fill/border roles and make the Toast an assertive accessible alert without duplicate icon announcement.

Do not add a motion library or change caller `useToast()` API.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/ToastProvider.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>Toast behavior is unchanged functionally, accessible in both themes, and has a tested no-motion branch tied to the OS setting.</done>
</task>

</tasks>

<verification>
- Three focused suites plus MobileVisualTokens pass.
- `npm run type-check` and `npm run lint` pass.
- No hardcoded `#fff/#FFFFFF/#EF4444` remains in owned runtime files.
- OfflineBanner measurement/topOffset contract and Toast durations/replacement/swipe behavior remain intact.
- No new dependency.
</verification>

<success_criteria>
- Shared controls and overlays pass automated contrast/semantic/target contracts.
- Offline/reconnect feedback remains visible and correctly layered.
- Android reduced-motion preference disables Toast motion without affecting dismissal.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-06-SUMMARY.md`.
</output>
