---
phase: 10-dark-mode-accessibility-qa
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/theme/appearance.ts
  - apps/mobile/lib/theme/ThemeProvider.tsx
  - apps/mobile/__tests__/lib/theme/appearance.test.ts
  - apps/mobile/__tests__/lib/theme/ThemeProvider.test.tsx
autonomous: true
requirements: [DARK-01, DARK-03]
must_haves:
  truths:
    - "With no valid saved preference, PatelRep selects System and follows the current OS light/dark scheme"
    - "An explicit Light or Dark choice applies immediately, survives provider remount/app restart, and ignores later OS scheme changes"
    - "While System is selected, a live OS appearance change updates the effective mode without an app restart"
    - "Theme preference hydration always settles, including missing, corrupt, and failed AsyncStorage reads"
  artifacts:
    - path: "apps/mobile/lib/theme/appearance.ts"
      provides: "AppearancePreference validation, storage key, and effective-mode derivation"
      exports: ["AppearancePreference", "APPEARANCE_STORAGE_KEY", "normalizeAppearancePreference", "resolveThemeMode"]
    - path: "apps/mobile/lib/theme/ThemeProvider.tsx"
      provides: "Hydrated device-local appearance context"
      exports: ["ThemeProvider", "useThemeMode", "useAppearancePreference"]
    - path: "apps/mobile/__tests__/lib/theme/ThemeProvider.test.tsx"
      provides: "Persistence and live-System regression coverage"
  key_links:
    - from: "apps/mobile/lib/theme/ThemeProvider.tsx"
      to: "@react-native-async-storage/async-storage"
      via: "hydrate and persist the single appearance preference"
      pattern: "AsyncStorage\\.(getItem|setItem)"
    - from: "apps/mobile/lib/theme/ThemeProvider.tsx"
      to: "react-native useColorScheme"
      via: "derive effective mode only when preference is system"
      pattern: "resolveThemeMode"
---

<objective>
Turn the Phase 7 light-pinned provider into one hydrated, device-local System/Light/Dark appearance source of truth.

Purpose: DARK-01 and the no-flash portion of DARK-03 require the saved preference to be distinct from the effective mode. This plan establishes that contract without touching navigation, Profile, account data, or appStore.
Output: Test-driven appearance helpers and a provider exposing `{ preference, mode, setPreference, isHydrated }`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/07-theme-foundation-primitives/07-PATTERNS.md
@.planning/phases/07-theme-foundation-primitives/07-VERIFICATION.md
@apps/mobile/lib/theme/ThemeProvider.tsx
@apps/mobile/lib/theme/useTheme.ts
@apps/mobile/components/shared/tokens.ts
@apps/mobile/package.json

<interfaces>
Locked preference contract:
```typescript
export type AppearancePreference = "system" | "light" | "dark";
type AppearanceContextValue = {
  preference: AppearancePreference;
  mode: "light" | "dark";
  setPreference: (next: AppearancePreference) => Promise<void>;
  isHydrated: boolean;
};
```

Use installed AsyncStorage 2.2.0. Do not add Zustand/server/account synchronization and do not add a dependency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify and implement preference normalization and mode derivation</name>
  <files>apps/mobile/lib/theme/appearance.ts, apps/mobile/__tests__/lib/theme/appearance.test.ts</files>
  <action>
RED: create `appearance.test.ts` first and prove it fails because the helper does not exist. Cover `null`, unknown/corrupt values, and the three valid strings; all invalid/absent values normalize to `"system"`. Cover effective-mode cases: System + dark OS => dark, System + light/null OS => light, explicit Light/Dark override every OS value.

GREEN: create `appearance.ts` with the exported `AppearancePreference`, a stable namespaced storage key, a narrow validator/normalizer, and a pure `resolveThemeMode(preference, systemScheme)` returning only `"light" | "dark"`. Keep this module free of React, AsyncStorage, user/account state, and animation.

REFACTOR: keep the helpers exhaustive and small; no duplicated string unions in the provider.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/appearance.test.ts --runInBand</verify>
  <done>All normalization and effective-mode cases pass, with System as the only absence/corruption fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Hydrate, persist, and expose the appearance preference</name>
  <files>apps/mobile/lib/theme/ThemeProvider.tsx, apps/mobile/__tests__/lib/theme/ThemeProvider.test.tsx</files>
  <action>
RED: create a small probe component under the real provider and mock AsyncStorage plus `useColorScheme`. Add failing tests for: no saved value => System; valid saved Light/Dark restoration; invalid saved value => System; `isHydrated` false before the storage promise settles and true afterward; immediate visible mode change before `setItem` resolves; exact key/value persistence; System reacting to a light->dark OS rerender; explicit modes ignoring that rerender; read/write rejection not crashing or trapping hydration.

GREEN: evolve the existing context rather than adding another store/provider. Initialize preference as System, hydrate once in an effect, validate through `normalizeAppearancePreference`, and set `isHydrated` in `finally`. Derive `mode` from the live `useColorScheme()` value through `resolveThemeMode`. `setPreference` must update React state first (no animation) and then write the small value asynchronously; a write failure must not block floor work. Memoize the complete context value.

Keep `useThemeMode()` backward-compatible for all Phase 8/9 callers and export `useAppearancePreference()` for Profile/root. Do not write preference to Supabase, `appStore`, auth profile, or any account-scoped API. Do not render an error screen for storage failures.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/appearance.test.ts __tests__/lib/theme/ThemeProvider.test.tsx --runInBand; npm run type-check</verify>
  <done>The provider passes all default, persistence, override, live-System, and failure-path tests and existing `useTheme()` callers retain their API.</done>
</task>

</tasks>

<verification>
- Targeted preference/provider Jest suites pass serially.
- `npm run type-check` passes.
- `rg -n "appStore|supabase|api\\." apps/mobile/lib/theme/appearance.ts apps/mobile/lib/theme/ThemeProvider.tsx` finds no account/server coupling.
- No animation API is introduced in either file.
</verification>

<success_criteria>
- System is the default for new and existing installations with no saved key.
- Explicit Light/Dark persists locally and wins over OS changes.
- System follows live OS appearance changes.
- Consumers can distinguish hydrated preference from effective mode for first-frame gating.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-01-SUMMARY.md`.
</output>
