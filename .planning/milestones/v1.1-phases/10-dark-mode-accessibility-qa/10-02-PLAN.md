---
phase: 10-dark-mode-accessibility-qa
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/mobile/components/shared/tokens.ts
  - apps/mobile/__tests__/helpers/contrast.ts
  - apps/mobile/__tests__/components/MobileVisualTokens.test.ts
autonomous: true
requirements: [DARK-02]
must_haves:
  truths:
    - "Normal text, controls, selected states, navigator chrome, banners, Toasts, and every semantic status pairing meet the locked WCAG AA thresholds in light and dark mode"
    - "RGBA soft fills are tested after compositing over the actual declared surface, not as standalone colors"
    - "Dark mode retains warm-charcoal Evening Lobby layers and forest-green primary actions"
    - "Room/work-order status meanings remain green ready, blue clean, purple in progress, red dirty/occupied, yellow pickup, and stone OOO/OOS with non-color cues unchanged"
  artifacts:
    - path: "apps/mobile/components/shared/tokens.ts"
      provides: "Contrast-safe light/dark semantic tokens, per-mode shell tokens, and on-color roles"
      exports: ["lightTheme", "darkTheme", "getThemeTokens", "ThemeTokens"]
    - path: "apps/mobile/__tests__/helpers/contrast.ts"
      provides: "Deterministic hex/RGBA compositing and WCAG ratio helpers"
    - path: "apps/mobile/__tests__/components/MobileVisualTokens.test.ts"
      provides: "Named AA contracts for production token pairings"
  key_links:
    - from: "apps/mobile/__tests__/components/MobileVisualTokens.test.ts"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "named foreground/background/composited pair assertions"
      pattern: "expect.*Contrast"
    - from: "apps/mobile/components/shared/tokens.ts"
      to: "lightTheme.shell and darkTheme.shell"
      via: "distinct theme-resolved navigator shell objects"
      pattern: "shell:"
---

<objective>
Make WCAG AA a deterministic semantic-token contract before dark mode is exposed.

Purpose: Screen-by-screen inspection is mandatory later, but stable token pairs must fail fast in Jest, including translucent dark status fills.
Output: A small contrast helper, exhaustive named pair tests, and the smallest contrast-safe Evening Lobby token adjustments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/07-theme-foundation-primitives/07-VERIFICATION.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md
@apps/mobile/components/shared/tokens.ts
@apps/mobile/__tests__/components/MobileVisualTokens.test.ts

<interfaces>
Thresholds are locked:
- normal text: >= 4.5:1
- large text and essential icon/control/selection/non-text boundary: >= 3:1

Test visible composites:
```typescript
visibleSoftFill = composite(rgbaSoftToken, declaredBaseSurface);
ratio(statusForeground, visibleSoftFill);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a failing composited WCAG token contract</name>
  <files>apps/mobile/__tests__/helpers/contrast.ts, apps/mobile/__tests__/components/MobileVisualTokens.test.ts</files>
  <action>
RED: implement a test-only helper that parses 3/6/8-digit hex and `rgb/rgba`, alpha-composites in sRGB over an explicit opaque base, computes WCAG relative luminance, and returns contrast ratio. Unit-check the helper against known black/white and alpha-composite examples before using it.

Extend `MobileVisualTokens.test.ts` with named assertions whose failure identifies the staff-visible pair. Cover both themes:
- primary/secondary/muted/disabled text on background, surface, subtle, muted, elevated, and shell tiers where each role is actually used;
- shell active/inactive tab text/icons and meaningful shell boundaries;
- primary, pressed, disabled, destructive, and AI action foreground/background pairs;
- card/sheet/modal/selected borders at the 3:1 non-text threshold where they are the meaningful boundary;
- OfflineBanner and success/error/info Toast text/icon/fill/border pairs;
- every ready/clean/in-progress/dirty/occupied/pickup/OOO status foreground, soft fill composited over its surface, and meaningful border;
- Copilot D-11 dark-only text, bubble/control, AI, and confirmation pairings.

Prove the new contract is RED against current production tokens. Do not weaken thresholds, omit failing pairs, round ratios before comparison, or declare screenshots a substitute.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/MobileVisualTokens.test.ts --runInBand</verify>
  <done>The helper is independently checked and the named production pairing suite exposes current AA failures before token changes.</done>
</task>

<task type="auto">
  <name>Task 2: Tune semantic tokens without changing PatelRep status meaning</name>
  <files>apps/mobile/components/shared/tokens.ts, apps/mobile/__tests__/components/MobileVisualTokens.test.ts</files>
  <action>
GREEN: make the smallest token-level adjustments needed to pass every named pair. Preserve the warm `#0F0D0B`/brown-charcoal family and layered dark surfaces; do not replace it with neutral black or blue-gray. Preserve forest green as primary. Add explicit semantic foreground roles such as `onPrimary`, `onDestructive`, `onAi`, and shell/toast/banner on-colors instead of relying on universal hardcoded white.

Split the current single `shellTokens` alias into light and dark shell semantics so both `lightTheme.shell` and `darkTheme.shell` have the same keys but the dark shell is a visibly deeper warm frame around dark content. Export a shared `ThemeTokens` type derived from the resolver for later helpers.

For statuses, retain the universal hue/meaning mapping and the icon/label/occupied-stripe contract. Add explicit `inProgress`, `inProgressSoft`, and `inProgressLine` purple semantics to both light and dark status objects; Phase 8's current pickup/yellow fallback is not the locked mapping and must not survive. Adjust dark foreground/soft/line brightness or saturation only as required by the composited tests. Keep the light compatibility `C` object only as a temporary source-compatibility export; do not add new runtime `C` consumers.

REFACTOR: keep light/dark theme key shapes aligned and named by semantic role. Record each changed failing pair and its final ratio in the plan summary for final verification.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/MobileVisualTokens.test.ts --runInBand; npm run type-check</verify>
  <done>All named token/composite pairs pass AA, theme shapes type-check, and the locked visual/status identities are intact.</done>
</task>

</tasks>

<verification>
- Targeted visual-token Jest suite passes.
- `npm run type-check` passes.
- Tests contain explicit 4.5 and 3.0 thresholds and RGBA-over-base composition.
- Light and dark `shell` objects are distinct values with the same semantic keys.
- Final ratios and token changes are captured in `10-02-SUMMARY.md` for `10-VERIFICATION.md`.
</verification>

<success_criteria>
- Automated AA coverage guards every shared semantic pairing used by migrated screens.
- Alpha status fills are measured as rendered.
- Token changes preserve Evening Lobby, forest green, Copilot D-11, and operational status meaning.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-02-SUMMARY.md`.
</output>
