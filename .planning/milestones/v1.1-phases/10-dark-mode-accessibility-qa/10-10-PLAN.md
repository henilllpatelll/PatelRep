---
phase: 10-dark-mode-accessibility-qa
plan: 10
type: execute
wave: 4
depends_on: [10-09]
files_modified:
  - .planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md
autonomous: false
requirements: [DARK-01, DARK-02, DARK-03, DARK-04, DARK-05]
must_haves:
  truths:
    - "The exact execution commit has a terminally successful EAS Android production build identified by profile, build ID, direct link, timestamp, outcome, and artifact"
    - "The same commit has a separately identified installable EAS preview/internal-distribution APK that is installed on the walkthrough device or emulator"
    - "The installed preview APK exercises every migrated route and meaningful state in both light and dark modes"
    - "Housekeeper, engineer, supervisor, front-desk, and GM shells retain their existing routing and RBAC behavior"
    - "EN and ES, narrow layouts, 200% text scale, TalkBack order/state, reduced motion, and offline/reconnect behavior pass the locked matrix"
    - "Neither production build success nor an installable preview build alone satisfies the phase; the production evidence and approved human matrix are both mandatory"
  artifacts:
    - path: ".planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md"
      provides: "Distinct same-commit EAS production AAB and installable preview APK identities plus approved ten-row Android walkthrough evidence"
      contains: "EAS"
  key_links:
    - from: ".planning/phases/10-dark-mode-accessibility-qa/10-09-SUMMARY.md"
      to: "EAS Android production build"
      via: "green final-state automated gate before cloud build"
      pattern: "Jest|type-check|lint|export"
    - from: "EAS Android production build"
      to: ".planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md"
      via: "production profile, build ID, direct link, timestamp, outcome/artifact, app version/versionCode, and exact commit SHA"
      pattern: "build ID|production|Android"
    - from: "same-commit EAS preview internal-distribution APK"
      to: ".planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md"
      via: "preview profile, APK type, build ID, direct link, exact commit SHA, and device installation evidence"
      pattern: "preview|APK|installed"
    - from: "human Android walkthrough"
      to: ".planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md"
      via: "explicit approval and ten completed matrix rows against the installed preview APK"
      pattern: "TalkBack|200%|offline|reconnect"
---

<objective>
Produce the mandatory EAS Android production build, obtain a same-commit installable preview APK, and close the rendered Android accessibility gate through an explicit human checkpoint.

Purpose: Automated checks and a local Expo export cannot prove rendered contrast, clipping, screen-reader order, device persistence, offline layering, or role-shell behavior.
Output: Distinct same-commit production and installable build records plus a human-approved ten-row Android matrix captured in `10-10-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/10-dark-mode-accessibility-qa/10-09-SUMMARY.md
@.planning/phases/09-remaining-screens-rollout/09-UAT.md
@apps/mobile/eas.json
@apps/mobile/app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the production artifact and same-commit installable walkthrough APK</name>
  <files>.planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md</files>
  <action>
First verify the `10-09` automated gate is green on the exact execution commit. From `apps/mobile`, run the non-interactive EAS Android build with the `production` profile and wait for terminal completion. This terminally successful production build is mandatory DARK-04 evidence; do not substitute `preview`, a development client, a local Expo export, or an earlier build for it.

Record the production build separately: profile `production`, Android platform, build ID, direct dashboard URL, exact commit SHA, start/completion timestamp, terminal outcome, artifact type/link, and app version/versionCode. The repository's production profile normally yields an AAB; do not assume that artifact is directly installable or use it as the walkthrough artifact.

Without changing commits, run a second non-interactive EAS Android build using the repository's existing `preview` profile. `eas.json` explicitly configures this path as `distribution: internal` with `android.buildType: apk`; wait for terminal success and verify its reported commit SHA exactly matches the production build/execution commit. Record this installable build separately: profile `preview`, artifact type APK, distribution, build ID, direct dashboard/download URL, exact commit SHA, timestamps, terminal outcome, app version/versionCode, and any checksum EAS exposes.

Install the preview APK—not the production AAB—on a real Android device or emulator. Record installation success plus device/emulator model, Android version/API, display size, installed package/app version, preview build ID, locale, and test account/role fixtures without secrets.

If either build reports a credential, account, network, quota, or configuration blocker, stop at that blocker rather than weakening its profile or claiming completion. If either build fails for source/configuration reasons, fix only the owning Phase 10 implementation/configuration issue, rerun the narrow check, rerun the complete `10-09` gates, and launch fresh production and preview builds from the resulting same commit. Never pair artifacts from different commits.

Prepare a ten-row walkthrough sheet with pass/fail/notes and screenshot references:
1. Cold launch, login, onboarding, hydration, and no wrong-theme flash.
2. Profile Light/Dark/System selector: persistence across restart, live System change, selected semantics, EN/ES fit.
3. Shared shell: headers, back actions, role tabs, status/navigation bars, FAB, loading/empty/error/banner/badge/Toast/modal/sheet states.
4. Housekeeper shell: My Rooms and housekeeping actions, destructive/primary selections, offline queueing and reconnect.
5. Supervisor shell: inspection queue/detail and housekeeping oversight states.
6. Engineer shell: engineering/work-order list/detail and status/confirmation flows.
7. Front-desk shell: guest requests, logbook, lost & found, SOP, and applicable shared task flows.
8. GM shell: dashboard, tasks, scheduling, staff, reports, billing, settings/integrations, and Profile.
9. AI Copilot: intentional D-11 dark content in both preferences, theme-reactive surrounding chrome, input/actions, service-recovery states.
10. Cross-cutting accessibility: both themes, EN/ES/narrow width, 200% text scale, TalkBack name/role/order/state, 44pt targets, reduced motion, contrast/boundary/status checks.

Every migrated route and meaningful loading, empty, error, modal/sheet, banner, badge, and Toast state must be accounted for in one row; mark unavailable fixture states explicitly for setup before the checkpoint, not as automatic passes.
  </action>
  <verify>cd apps/mobile; npx eas-cli build --platform android --profile production --non-interactive --wait; npx eas-cli build --platform android --profile preview --non-interactive --wait; verify both EAS records report the exact same execution commit SHA, then install the preview APK and confirm its installed build identity</verify>
  <done>The exact execution commit has a terminally successful production build and a distinct terminally successful preview/internal-distribution APK; both identities are recorded, the preview APK is installed with matching device evidence, and the ten-row walkthrough sheet is ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the rendered Android matrix</name>
  <files>.planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md</files>
  <action>
Present the production build identity, the distinct same-commit preview APK identity and installation evidence, the device identity, and all ten prepared matrix rows. This checkpoint is a release gate: do not approve on the basis of either build alone or screenshots alone where interaction, persistence, TalkBack, offline/reconnect, or OS appearance changes are required.
  </action>
  <what-built>A production-profile EAS Android build for DARK-04 and a separate preview/internal-distribution APK from the exact same green Phase 10 commit, with the preview APK installed and the ten-row light/dark, role, localization, accessibility, and offline walkthrough prepared.</what-built>
  <how-to-verify>
On the recorded Android device/emulator:

1. Confirm the production build and preview APK records show the same exact commit SHA. Complete rows 1-10 using the installed EAS `preview` internal-distribution APK, not the production AAB. For each row, record pass/fail, roles/routes/states exercised, theme, locale, text scale, TalkBack/reduced-motion setting where applicable, and screenshot or observation references.
2. Confirm both Light and Dark plus live System switching, restart persistence, no launch flash, and the D-11 Copilot exception with theme-following surrounding chrome.
3. Confirm all five role shells show only their existing authorized destinations and all floor mutations, confirmations, offline queueing, reconnect synchronization, Toast/banner layering, and navigation remain unchanged.
4. At 200% text and narrow width, confirm required actions remain visible/operable; with TalkBack, confirm logical order plus meaningful name, role, selected/disabled/busy state; confirm touch targets remain operable without precision taps.
5. Reject the checkpoint for any unreadable contrast, status conveyed only by color, clipped/hidden required action, missing Spanish staff-facing copy, broken role/offline behavior, or missing matrix evidence. Report the exact row, route, role, mode, locale, and reproduction steps for correction.
  </how-to-verify>
  <verify>Human returns explicit approval only after confirming a successful production build, a separately installed same-commit preview APK, and all ten rows passing against that preview APK; otherwise the plan remains blocked for fixes and a rerun of both same-commit builds, the affected rows, and the `10-09` automated gates as applicable.</verify>
  <done>Human confirms the successful production evidence and explicitly approves all ten Android rows against the separately recorded same-commit installed preview APK, with no unresolved release-blocking contrast, accessibility, localization, offline, or RBAC defect.</done>
  <resume-signal>Reply "approved" with the completed row results, or report failing row numbers and reproduction details.</resume-signal>
</task>

</tasks>

<verification>
- `10-09` automated gates are green on the exact built commit.
- The Android `production` build is terminally successful and identified by profile, build ID, direct link, commit, timestamp, outcome, and artifact.
- A distinct Android `preview` internal-distribution APK from the exact same commit is identified by profile/type/build ID/link/commit and device installation evidence.
- All ten human rows are captured against the installed preview APK.
- Explicit human approval exists; neither production success nor preview/installable success alone is accepted.
</verification>

<success_criteria>
- DARK-04 has concrete production EAS evidence.
- Rendered Android, role, localization, 200%, TalkBack, reduced-motion, and offline behavior have human evidence from the same-commit installed preview APK.
- Production build evidence and approved human matrix evidence are both present and remain distinctly identified.
- Failures block advancement to 10-11 until fixed and reverified.
</success_criteria>

<output>
After approval, create `.planning/phases/10-dark-mode-accessibility-qa/10-10-SUMMARY.md` with separate production-build, installable-preview, installation, and human evidence, then advance to 10-11. If either build is missing, the commits differ, installation is unproven, or approval is withheld, do not create a passing summary or advance.
</output>
