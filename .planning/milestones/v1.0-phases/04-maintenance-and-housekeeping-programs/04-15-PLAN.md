---
phase: 04-maintenance-and-housekeeping-programs
plan: 15
type: execute
wave: 7
depends_on: ["04-14"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/app/(dashboard)/engineering/assets/page.tsx
  - apps/web/app/(dashboard)/engineering/predictions/page.tsx
  - apps/web/app/(dashboard)/engineering/work-orders/page.tsx
  - apps/web/app/(dashboard)/engineering/page.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The engineering assets, predictions, work-orders, and landing route pages render every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/assets/page.tsx"
      provides: "Bilingual assets route"
      contains: "useTranslation"
    - path: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      provides: "Bilingual work-orders route"
      contains: "useTranslation"
  key_links:
    - from: "app/(dashboard)/engineering/work-orders/page.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under engineering.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the engineering route pages (`assets/page.tsx`, `predictions/page.tsx`, `work-orders/page.tsx`, and the `engineering/page.tsx` landing) into EN + ES. These app-route pages are inside D-04's locked gate glob `app/(dashboard)/{...,engineering,...}/**` and D-03's engineering-directory scope; they must be literal-free before the gate widens (04-16). `engineering/page.tsx` (5 lines, 0 literals) needs only a check.

Purpose: Complete D-03 engineering-directory coverage.
Output: Engineering route pages fully bilingual with matching en.ts/es.ts keys.

Depends on 04-14 (shares en.ts/es.ts — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
Namespace assignment (under top-level `engineering`):
  - engineering.assetsPage.*      → engineering/assets/page.tsx
  - engineering.predictionsPage.* → engineering/predictions/page.tsx
  - engineering.workOrdersPage.*  → engineering/work-orders/page.tsx
Reuse engineering.workOrderCard.*/workOrderList.*/failurePrediction.* (from 04-13/04-14), nav.*, common.* where copy is identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate engineering/assets/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/engineering/assets/page.tsx (1107 lines — ~53 literals; the asset register)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog for t() wiring)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.assetsPage.*` keys to en.ts AND es.ts for all user-facing copy (heading, asset field/column labels, criticality/status labels, filter and action buttons, form modals, empty/loading/error states, aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Leave dynamic asset data raw or wrap non-copy nodes with `data-i18n-skip="true"`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/engineering/assets/page.tsx"` returns >= 1
    - `grep -c "assetsPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>assets/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate engineering/predictions/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/engineering/predictions/page.tsx (636 lines — ~25 literals)
    - apps/web/components/engineering/FailurePredictionSidebar.tsx (translated in 04-14 — reuse failurePrediction keys where copy matches)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.predictionsPage.*` keys to en.ts AND es.ts covering heading, prediction/risk labels, action buttons, empty/error copy, aria-label/placeholder/title. Rewire to `useTranslation()` + `t()`. Reuse engineering.failurePrediction.* labels where identical. Render risk/enum values via translated label maps, not raw API values. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/engineering/predictions/page.tsx"` returns >= 1
    - `grep -c "predictionsPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>predictions/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 3: Translate engineering/work-orders/page.tsx + confirm engineering/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/engineering/work-orders/page.tsx (458 lines — ~7 literals)
    - apps/web/app/(dashboard)/engineering/page.tsx (5 lines — expected 0 literals; verify it is a redirect/wrapper with no user-facing copy)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.workOrdersPage.*` keys to en.ts AND es.ts covering heading, filter/tab labels, action buttons, empty/error copy, aria-label/placeholder/title. Rewire work-orders/page.tsx to `useTranslation()` + `t()`, reusing engineering.workOrderCard.*/workOrderList.* labels where identical. For engineering/page.tsx: if it contains any user-facing literal, wrap it via t(); if it is a pure redirect/wrapper (no copy), leave it untouched and note that in the summary (it is already literal-free per the i18next markupOnly scan). Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"` returns >= 1
    - `grep -c "workOrdersPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>work-orders/page.tsx renders via t(); engineering/page.tsx confirmed literal-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | Inside the D-04 gate glob; must be literal-free before 04-16 widens it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-41 | Tampering (process) | untranslated engineering route copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-42 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- assets/predictions/work-orders pages return `useTranslation` grep count >= 1; engineering/page.tsx literal-free
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- Engineering directory fully covered in EN + ES (D-03/BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-15-SUMMARY.md`
</output>
