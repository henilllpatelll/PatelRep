---
phase: 04-maintenance-and-housekeeping-programs
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - apps/web/lib/api/programs.ts
  - apps/web/lib/api/engineering.ts
  - apps/web/components/engineering/PMCompletionModal.tsx
  - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx
autonomous: false
requirements: [PM-02, PM-03, G3, G12]

must_haves:
  truths:
    - "Completing a PM captures versioned checklist results (pass/fail/n-a per item), technician, and verifier"
    - "The technician can attach photos and vendor certificates, uploaded to the evidence platform (signed-URL backed)"
    - "The technician can record measurements/meter readings, labor, structured parts, and defects"
    - "An evidence-required item cannot be submitted without proof (422 surfaced in the UI)"
    - "A failed item explains that a corrective work order will be created"
  artifacts:
    - path: "apps/web/components/engineering/PMCompletionModal.tsx"
      provides: "Full defensible PM completion capture form"
      min_lines: 120
    - path: "apps/web/lib/api/programs.ts"
      provides: "PM completion + deferral + evidence-upload typed clients"
      contains: "completePM"
  key_links:
    - from: "PMCompletionModal"
      to: "POST /assets/pm-schedules/{id}/complete"
      via: "typed client submit with items + evidence IDs"
      pattern: "pm-schedules"
    - from: "PMCompletionModal photo upload"
      to: "POST /evidence/records + /evidence/records/{id}/file"
      via: "evidence upload returning IDs"
      pattern: "evidence"
---

<objective>
Slice 4A — replace the stub PM completion UI (G3). Today the web "Complete" button posts a single canned `technician_attestation` passed item (engineering.ts:263) — it collects none of the checklist results, measurements, verifier, photos, parts, labor, or defects the defensible model supports. Build the real completion capture form so a completion proves actual work (Phase 4 exit criteria), wired to the evidence platform (04-02) and the hardened backend (04-03).

Purpose: PM completion proves work was done, not merely that a due date moved.
Output: A `PMCompletionModal` capturing the full defensible record, evidence upload via the Phase 2 platform, and the completed typed client.

NOTE: This surface is intentionally left in English here; 4C (04-08) applies the bilingual contract. Do not hardcode final copy in ways that block later i18n — use plain strings that 04-08 will replace with `t()` keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-02-SUMMARY.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-04-SUMMARY.md

<interfaces>
<!-- READ apps/web/node_modules/next/dist/docs/ before non-trivial web work (AGENTS.md). -->

Backend contract (from 04-02/04-03/04-04):
  POST /assets/pm-schedules/{id}/complete  body = CompletePMProgramRequest:
    { checklist_template_id?, checklist_version?, verifier_id?, measurements:{}, meter_readings:{},
      photos:[evidence_id...], labor_minutes, parts_used:[{}], defects:[{}], vendor_name?,
      certificate_attachments:[evidence_id...], notes?, items:[{key?,label,result,requires_evidence,evidence:[evidence_id],note?}] }
    -> 422 when a requires_evidence item has empty evidence
  POST /programs/pm-schedules/{id}/deferrals  body = { reason, deferred_until, approved_by }  -> 422 on self-approval
  GET  /programs/templates ... (via overview) for checklist template selection

Evidence upload (reuse — apps/web/lib/api, mirror components/safety evidence upload if present):
  POST /evidence/records  { evidence_type, related_entity_type:"asset", related_entity_id:<asset_id>, label }
  POST /evidence/records/{id}/file  (multipart file)  -> stored in private bucket
  -> submit the returned evidence_record ids in photos / certificate_attachments / item.evidence

apps/web/lib/api/engineering.ts:263 completePMSchedule(scheduleId) -> currently posts canned attestation (REPLACE)
apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx -> CompletePMModal (stub, lines 239-368) (REPLACE body)
  useRole(): { isGM, role }; canEdit = isGM || role==='engineer' (extend for chief_engineer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PM completion + deferral + evidence-upload typed clients</name>
  <read_first>
    - apps/web/lib/api/programs.ts (current programsApi)
    - apps/web/lib/api/engineering.ts (completePMSchedule:263, createWorkOrder pattern)
    - apps/web/lib/api/client.ts (apiClient — get/post shape; how multipart is sent)
  </read_first>
  <action>
    Extend `apps/web/lib/api/programs.ts` (and/or engineering.ts) with typed clients:
    (a) `completePM(scheduleId, payload: PMCompletionPayload)` posting the full CompletePMProgramRequest body to `/assets/pm-schedules/{id}/complete`. Define the `PMCompletionPayload`, `PMChecklistItemInput`, `PMPart`, `PMDefect` types matching the backend contract above.
    (b) `deferPM(scheduleId, { reason, deferred_until, approved_by })` -> `/programs/pm-schedules/{id}/deferrals`.
    (c) `uploadEvidence(file, { related_entity_type, related_entity_id, evidence_type, label })` -> creates an evidence_record then uploads the file, returning the evidence_record id. Reuse the existing evidence-upload client if one exists (check lib/api/safety.ts or lib/api/evidence.ts); do not duplicate.
    Replace the stub `completePMSchedule(scheduleId)` (canned attestation) — remove it or repoint it to `completePM`.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "completePM\|deferPM\|uploadEvidence" apps/web/lib/api/programs.ts apps/web/lib/api/engineering.ts` shows the three clients
    - `grep -c "technician_attestation" apps/web/lib/api/engineering.ts` returns 0 (stub removed)
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>Typed clients for full completion, deferral, and evidence upload exist; canned stub gone.</done>
</task>

<task type="auto">
  <name>Task 2: Build the PMCompletionModal defensible capture form</name>
  <read_first>
    - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx (CompletePMModal to replace, lines 230-368)
    - apps/web/components/ui/ (Button, Input, Card primitives)
    - apps/web/components/safety/ (evidence-upload UI pattern, if present, to mirror)
  </read_first>
  <action>
    Create `apps/web/components/engineering/PMCompletionModal.tsx` replacing the stub CompletePMModal. The form captures the defensible record:
    - Checklist items: render the selected template's items (or a default set); each item has a pass/fail/not_applicable control, an optional note, and — when requires_evidence — a photo upload (via uploadEvidence, storing returned IDs on the item).
    - Verifier: a verifier selector (user id) distinct from the technician.
    - Measurements + meter readings: key/value inputs (JSON object).
    - Labor minutes, structured parts (list of {name, qty, cost?}), defects (list of {description, severity?}).
    - Vendor name + certificate attachments (evidence upload -> IDs).
    - On a failed item, show inline text that a corrective work order will be created.
    - Client-side: block submit if a requires_evidence item lacks evidence; surface the backend 422 detail if the server rejects.
    Wire it into pm-schedules/page.tsx in place of the old modal (import + state). Keep `min-h-[44px]` touch targets and the existing card/modal styling. Update `canEdit` to include `chief_engineer`.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/engineering/PMCompletionModal.tsx` exists (>=120 lines) with item results, verifier, measurements, parts, defects, evidence upload
    - pm-schedules/page.tsx imports PMCompletionModal and no longer posts a canned single-item attestation
    - `npm run type-check && npm run lint` clean
  </acceptance_criteria>
  <done>The completion form captures the full defensible record with evidence upload.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full PM completion capture UI wired to the evidence platform and hardened backend (checklist results, verifier, measurements, parts, defects, photos/certs via signed-URL evidence, failed-check corrective-WO notice, deferral with distinct approver).</what-built>
  <how-to-verify>
    1. Start dev servers: `npm run dev:api` (:8000) and `npm run dev:web` (:3000). Log in as the GM test account.
    2. Go to /engineering/pm-schedules. Click Complete on a schedule.
    3. Fill an evidence-required item without a photo -> submit -> confirm the UI blocks/surfaces the 422.
    4. Attach a photo, set one item to Failed with a note, set a verifier, add a measurement + a part, submit -> expect success; confirm a corrective work order was created for the failed item (check /engineering work orders).
    5. Reopen and confirm the completion is immutable (no edit of a saved record).
    6. Confirm no console errors and photos load via signed URL (not a public path).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> completion API | Client cannot be trusted to enforce evidence rules |
| uploaded file -> evidence bucket | Attachment must go to the private bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Tampering | evidence-required bypass | mitigate | Server-side 422 is authoritative (04-02/03); UI mirrors it but does not replace it |
| T-04-17 | Information Disclosure | attachment display | mitigate | Photos rendered via signed URL from evidence platform; no public URL in client |
| T-04-18 | Spoofing | verifier == technician | mitigate | Verifier selector distinct from technician; deferral approver distinct (backend-enforced) |
</threat_model>

<verification>
- `cd apps/web && npm run type-check && npm run lint` clean
- Human verification of the golden path (checkpoint)
- Full completion record persists; failed item creates a corrective WO; evidence via signed URL
</verification>

<success_criteria>
- PM completion captures the full defensible record with evidence (PM-02/03)
- Evidence-required items cannot be submitted without proof
- Stub canned attestation removed
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-05-SUMMARY.md`
</output>
