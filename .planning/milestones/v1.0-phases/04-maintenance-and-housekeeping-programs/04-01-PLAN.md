---
phase: 04-maintenance-and-housekeeping-programs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/tests/test_programs_routes.py
  - apps/api/routers/programs.py
  - apps/api/routers/assets.py
autonomous: true
requirements: [PM-01, PM-04, D-01, D-08, G6, G7, G9]
nyquist_compliant: true

must_haves:
  truths:
    - "A housekeeper cannot read the program overview (DND policy, pars, sampling rules) — 403"
    - "A chief_engineer can complete a PM and manage programs"
    - "A cross-tenant PM schedule returns 404 and writes zero rows"
    - "pm_completion_records / pm_deferrals / deep_clean_occurrences reject UPDATE and DELETE at the DB level"
    - "Route handlers survive maybe_single()->None with no AttributeError (bug-449 guard)"
  artifacts:
    - path: "apps/api/tests/test_programs_routes.py"
      provides: "TestClient route tests: tenant isolation, RBAC, None-guard"
      contains: "def test_"
    - path: "apps/api/routers/programs.py"
      provides: "Overview read gate + chief_engineer role set + None-safe reads"
      contains: "require_role"
  key_links:
    - from: "programs.get_program_overview"
      to: "require_role(*MANAGER_ROLES)"
      via: "Depends"
      pattern: "get_program_overview.*require_role"
    - from: "programs.MANAGER_ROLES"
      to: "chief_engineer"
      via: "role tuple membership"
      pattern: "chief_engineer"
---

<objective>
Slice 0 foundation. Stand up the missing TestClient route-test harness that proves the LIVE `programs.py` scaffold obeys the locked cross-phase contracts, then land the small correctness/RBAC fixes the audit surfaced (G6 overview read gate, G7 chief_engineer role set, bug-449 None-safety, duplicate PM-complete endpoint), and prove DB-level immutability against the real database.

This plan blocks all later Phase 4 work: every subsequent slice extends `test_programs_routes.py`, and the RBAC/None fixes here are prerequisites for the evidence and completion work.

Purpose: The scaffold is live in production against real tenant data (D-01). Contract violations found here are production-security fixes, not just Phase 4 tasks.
Output: `test_programs_routes.py` (route harness), hardened `programs.py`/`assets.py`, and a rolled-back DB immutability proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-VALIDATION.md

<interfaces>
<!-- Extracted from the scaffold — the executor should not re-explore these. -->

apps/api/routers/programs.py (current, to be hardened):
  MANAGER_ROLES = ("gm", "housekeeping_supervisor", "engineer")   # G7: chief_engineer MISSING
  GET  /programs/overview            -> get_program_overview(Depends(get_current_user))   # G6: NO require_role
  POST /programs/pm-schedules/{id}/complete -> require_role("engineer","gm")   # duplicate of assets.py; G7 missing chief_engineer
  POST /programs/pm-schedules/{id}/deferrals -> require_role("gm")
  _get_pm_schedule(): `if not result.data:` on a maybe_single() result (bug-449 shape risk)

apps/api/routers/assets.py:
  POST /assets/pm-schedules/{id}/complete -> require_role("engineer","gm")   # web-wired endpoint (engineering.ts:263). G7 missing chief_engineer

Role vocabulary (CLAUDE.md): housekeeper, engineer, housekeeping_supervisor, chief_engineer, front_desk, gm

bug-449 pattern (STATE.md, internal.py:44): supabase-py `.maybe_single().execute()` returns `None` on no row.
  Correct guard: `if result and result.data:` — NOT `if result.data:`.

Test analogs to mirror:
  apps/api/tests/test_evidence_foundation.py  (route-test + fake-supabase style used in this repo)
  Phase 1 tenant-isolation test (STATE.md: "cross-hotel work order returns 404, zero audit events written")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create programs route-test harness (tenant isolation, RBAC, None-guard)</name>
  <read_first>
    - apps/api/tests/test_operational_programs.py (existing pure-logic tests — do not duplicate)
    - apps/api/tests/test_evidence_foundation.py (route-test + TestClient / fake-supabase harness pattern)
    - apps/api/routers/programs.py (routes under test)
    - apps/api/middleware/auth.py (require_role / CurrentUser / get_current_user override pattern for tests)
  </read_first>
  <behavior>
    - test_overview_requires_manager_role: housekeeper token -> GET /programs/overview -> 403
    - test_overview_allows_manager: gm and chief_engineer -> 200
    - test_pm_complete_rbac: housekeeper -> POST complete -> 403; engineer, chief_engineer, gm -> allowed to reach handler
    - test_cross_tenant_pm_schedule_404: schedule owned by tenant B, request as tenant A -> 404 and zero rows inserted into pm_completion_records
    - test_maybe_single_none_returns_404_not_500: _get_pm_schedule with no matching row -> 404 (not AttributeError/500) — bug-449 regression guard
  </behavior>
  <action>
    Create `apps/api/tests/test_programs_routes.py`. Use the repo's existing TestClient + dependency-override harness (copy the auth/fake-supabase override style from test_evidence_foundation.py — do NOT invent a new harness). Build fixtures for a housekeeper, engineer, chief_engineer, and gm CurrentUser, and a two-tenant fixture (tenant A + tenant B) for isolation. Assert the five behaviors above. For the None-guard test, drive the fake supabase so `.maybe_single().execute()` returns an object whose `.data` is None (the real supabase-py shape returns `None` object entirely — assert the handler returns 404, mirroring internal.py:44). These tests encode the FIXED behavior; some will fail until Task 2 lands — that is the RED state.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" apps/api/tests/test_programs_routes.py` returns >= 5
    - File imports the app via TestClient and overrides get_current_user (grep: `dependency_overrides`)
    - Running the suite executes all 5 tests (pass or fail — file must import + collect cleanly)
  </acceptance_criteria>
  <done>Route-test file exists and pytest collects/runs its 5 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Fix overview read gate (G6), chief_engineer role set (G7), None-safety, and remove duplicate complete endpoint</name>
  <read_first>
    - apps/api/routers/programs.py
    - apps/api/routers/assets.py (lines 226-254, the web-wired complete endpoint)
    - apps/web/lib/api/engineering.ts (line 263 — confirms web calls /assets/pm-schedules/{id}/complete)
  </read_first>
  <action>
    In `apps/api/routers/programs.py`:
    (a) G6: change `get_program_overview` from `Depends(get_current_user)` to `Depends(require_role(*MANAGER_ROLES))`.
    (b) G7: add `"chief_engineer"` to `MANAGER_ROLES` -> `("gm", "housekeeping_supervisor", "engineer", "chief_engineer")`. Engineering-owned routes (`complete`, `deferrals`) must include `chief_engineer`; keep `housekeeper` excluded from PM completion.
    (c) None-safety: in `_get_pm_schedule` and every `.maybe_single().execute()` read in this router, use `result = (...).maybe_single().execute()` then `if not result or not result.data:` (bug-449 pattern).
    (d) Duplicate endpoint: `programs.py` defines `POST /programs/pm-schedules/{id}/complete` which duplicates the web-wired `POST /assets/pm-schedules/{id}/complete` (assets.py:226). Remove the `programs.py` duplicate (`complete_pm_schedule_with_evidence`) so completion has ONE canonical home. Leave deferrals in programs.py.
    In `apps/api/routers/assets.py`: change the `/pm-schedules/{id}/complete` and `/pm-schedules` role gates from `require_role("engineer","gm")` to include `"chief_engineer"`. Apply the same `if not result or not result.data:` guard to the maybe_single read at assets.py:238.
    Do NOT change persist_pm_completion behavior here — evidence/audit rework lands in 04-02/04-03.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py tests/test_operational_programs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "chief_engineer" apps/api/routers/programs.py` shows it in MANAGER_ROLES
    - `grep -c "get_program_overview" apps/api/routers/programs.py` and the handler line shows `require_role`
    - `grep -c "complete_pm_schedule_with_evidence" apps/api/routers/programs.py` returns 0 (duplicate removed)
    - No `if result.data:` without a preceding `result and` guard on maybe_single reads (grep review)
    - Task 1 tests all pass green
  </acceptance_criteria>
  <done>Overview is manager-gated, chief_engineer can complete/manage, maybe_single reads are None-safe, duplicate complete endpoint removed, all Task 1 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Prove DB-level immutability of append-only program tables (Supabase MCP, rolled back)</name>
  <read_first>
    - supabase/migrations/071_operational_programs.sql (lines 173-179 — reject_operational_program_mutation triggers)
    - .planning/STATE.md (lines 33 — the Phase 3 rolled-back incident-immutability proof to mirror)
  </read_first>
  <action>
    Using the Supabase MCP against the production project, mirror the Phase 3 immutability proof. First run `SELECT count(*) FROM pm_completion_records; SELECT count(*) FROM pm_deferrals; SELECT count(*) FROM deep_clean_occurrences;` and record counts in the SUMMARY (Runtime State Inventory / research A3 — confirms whether any prod rows carry raw-URL attachments before 04-02 changes the write path). Then, inside a single transaction that is ROLLED BACK at the end (zero residue): INSERT one seed row into `pm_completion_records`, attempt `UPDATE ... SET notes='x'` (must raise the append-only exception), attempt `DELETE` (must raise), then ROLLBACK. Repeat the UPDATE/DELETE-blocked assertion for `pm_deferrals` and `deep_clean_occurrences`. Record in the SUMMARY: append_ok, update_blocked, delete_blocked for each table, and confirmed zero residual rows after rollback. This is verification-only; write no migration.
  </action>
  <verify>
    <automated>MISSING — manual Supabase MCP rolled-back transaction (see 04-VALIDATION.md Manual-Only Verifications); record append_ok/update_blocked/delete_blocked + zero-residue in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records prod row counts for the three tables
    - SUMMARY records update_blocked=true AND delete_blocked=true for pm_completion_records, pm_deferrals, deep_clean_occurrences
    - SUMMARY confirms zero residual rows after ROLLBACK
  </acceptance_criteria>
  <done>Immutability triggers proven enforced against the real DB with zero residue; prod row counts recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated floor user -> /programs/overview | Untrusted role reads management config |
| tenant A user -> tenant B PM schedule | Cross-tenant access attempt |
| API -> append-only program tables | Attempt to rewrite/erase completion history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | GET /programs/overview | mitigate | Task 2: `require_role(*MANAGER_ROLES)` on overview (G6); route test asserts housekeeper 403 |
| T-04-02 | Elevation of Privilege | PM complete/deferral RBAC | mitigate | Task 2: correct role set incl. chief_engineer, housekeeper excluded (G7); route test asserts |
| T-04-03 | Info Disclosure / Tampering | cross-tenant PM schedule | mitigate | In-handler `.eq("tenant_id")` (present); Task 1 test asserts 404 + zero writes |
| T-04-04 | Repudiation / Tampering | append-only program tables | mitigate | Task 3 proves UPDATE/DELETE rejected at DB level (trigger-enforced) |
| T-04-05 | Denial of Service | maybe_single None -> 500 | mitigate | Task 2 None-safe guard (bug-449); Task 1 regression test |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/ -q` green (full suite, no regressions in the 312 baseline)
- Route harness proves tenant isolation + RBAC + None-safety
- DB immutability proven via rolled-back MCP transaction with zero residue
</verification>

<success_criteria>
- Housekeeper 403 on overview; chief_engineer can complete/manage; cross-tenant 404 with zero writes
- No duplicate PM-complete endpoint
- Append-only tables reject UPDATE/DELETE at the DB level
- Full API suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-01-SUMMARY.md`
</output>
