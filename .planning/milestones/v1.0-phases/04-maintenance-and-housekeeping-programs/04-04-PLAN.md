---
phase: 04-maintenance-and-housekeeping-programs
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - supabase/migrations/083_program_template_facilities.sql
  - apps/api/services/programs/contracts.py
  - apps/api/routers/programs.py
  - apps/api/models/requests.py
  - apps/api/tests/test_programs_routes.py
  - apps/api/tests/test_operational_programs.py
autonomous: true
requirements: [PM-08, D-05, G5]

must_haves:
  truths:
    - "Initializing property templates seeds pool/backflow/domestic-water templates ONLY where the property's facilities include them"
    - "A manager can edit a template's frequency and checklist items"
    - "A manager can create a generic custom template for other configured obligations"
    - "The 7 named engineering templates + 2 housekeeping templates seed with real multi-item checklists, not a single canned item"
  artifacts:
    - path: "apps/api/services/programs/contracts.py"
      provides: "Applicability-gated template definitions with real checklist items"
      contains: "DEFAULT_PROGRAM_TEMPLATES"
    - path: "apps/api/routers/programs.py"
      provides: "Gated initialize + template editor + generic builder routes"
      contains: "templates"
  key_links:
    - from: "initialize_property_templates"
      to: "property_applicability.facilities"
      via: "facility gating"
      pattern: "property_applicability"
    - from: "template editor route"
      to: "pm_checklist_templates"
      via: "update frequency + items"
      pattern: "pm_checklist_templates"
---

<objective>
Slice 4A — the property-configurable PM template library (D-05, G5, PM-08). Today `POST /programs/templates/initialize` seeds all 9 templates regardless of the property's facilities, each with a single canned checklist item, and there is no editor or generic builder. Deliver: (1) applicability gating so pool/backflow/domestic-water surface only where the facility exists; (2) real multi-item checklists per named template; (3) a template editor (frequency + checklist items); (4) a generic builder for "other configured obligations."

CRITICAL correction to research assumption A2: `property_applicability` is a SINGLE-ROW-per-tenant table with a `facilities` JSONB array (migrations 069/074), NOT `facility_key`/`applies` columns. The canonical `facilities` values (migration 074 CHECK) are currently `pool, spa, elevator, boiler, cooling_tower` — they do NOT include `backflow` or `domestic_water`. Gating those two therefore requires extending the canonical constraint first.

Purpose: PM templates match the actual property; managers configure obligations without code changes.
Output: Migration 083 (applied), gated + richer seeding, editor + generic builder routes, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-03-SUMMARY.md

<interfaces>
DB facts (verified this planning session):
  property_applicability: ONE row per tenant. Columns facilities JSONB, services JSONB, brand_requirements JSONB (069:5-7)
  migration 074 CHECK: facilities constrained to ARRAY['pool','spa','elevator','boiler','cooling_tower']
  Read pattern (evidence.py:145): supabase.table("property_applicability").select("*").eq("tenant_id", tid).maybe_single()
    -> row.get("facilities", []) is a JSONB list of facility keys

apps/api/services/programs/contracts.py:
  DEFAULT_PROGRAM_TEMPLATES: 9 dicts (fire_extinguisher, emergency_lighting, fire_alarm_sprinkler,
    elevator_certificate, pool_check, domestic_water, backflow, privacy_guest_present_entry,
    sharps_body_fluid_spill). Each currently only has code/program_area/name/name_es/requires_evidence.

apps/api/routers/programs.py:
  POST /programs/templates/initialize -> require_role("gm")   # G5: seeds ALL regardless of facilities; 1 canned item each
  pm_checklist_templates columns (071:13-26): code, program_area, name, name_es, version, items JSONB,
    is_active, UNIQUE(tenant_id, code, version). Config table = MUTABLE (editable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migration 083 — extend canonical facilities for gating; apply via Supabase MCP</name>
  <read_first>
    - supabase/migrations/074_evidence_applicability_contract.sql (facilities canonical CHECK, lines 22-30)
    - supabase/migrations/069_evidence_foundation.sql (property_applicability table)
  </read_first>
  <action>
    Create `supabase/migrations/083_program_template_facilities.sql` that drops + recreates the `property_applicability_facilities_canonical` CHECK constraint to add `'backflow'` and `'domestic_water'` (and keep `pool, spa, elevator, boiler, cooling_tower`). This makes the two gated facilities configurable per property. Do NOT loosen the constraint to arbitrary strings — keep it an explicit canonical allowlist. APPLY to production via the Supabase MCP `apply_migration` tool and verify the new constraint via SELECT (project convention; a schema constraint change is invisible to the TS build). Confirm no advisor regressions.
  </action>
  <verify>
    <automated>MISSING — Supabase MCP apply_migration + verify constraint; SUMMARY records applied=true. Local: `grep -c "backflow\|domestic_water" supabase/migrations/083_program_template_facilities.sql` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - Migration file adds backflow + domestic_water to the canonical facilities allowlist
    - Applied via Supabase MCP; SELECT confirms constraint updated; SUMMARY records applied=true
  </acceptance_criteria>
  <done>property_applicability.facilities accepts backflow + domestic_water; migration applied to prod.</done>
</task>

<task type="auto">
  <name>Task 2: Applicability-gated seeding + real checklist items</name>
  <read_first>
    - apps/api/services/programs/contracts.py (DEFAULT_PROGRAM_TEMPLATES)
    - apps/api/routers/programs.py (initialize_property_templates, lines 65-80)
    - apps/api/routers/evidence.py (_get_property_applicability, lines 145-152 — reuse the read helper)
  </read_first>
  <action>
    (a) Enrich each entry in `DEFAULT_PROGRAM_TEMPLATES` with a real `items` list (multiple checklist items with key/label/requires_evidence and a suggested default frequency in days) reflecting the obligation — e.g. fire_extinguisher: gauge in range, pin/seal intact, accessible, tag signed (photo required); elevator_certificate: current cert on file (cert attachment required), inspection date, load rating. Keep name_es. Do NOT reduce scope with "v1/basic" language.
    (b) Add a `GATED` map: `{"pool_check": "pool", "domestic_water": "domestic_water", "backflow": "backflow"}`. In `initialize_property_templates`, read the tenant's `property_applicability` row (reuse `_get_property_applicability` style), extract `facilities` (a list). Seed a gated template only if its facility is present in `facilities`; always seed non-gated templates. Preserve the existing "skip codes already present" behavior (idempotent).
    (c) Seed each template's `items` from the enriched definitions (not a single canned `verification` item).
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_operational_programs.py tests/test_programs_routes.py -k "template" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GATED" apps/api/routers/programs.py` present with pool/domestic_water/backflow mapping
    - Seeding reads property_applicability.facilities and filters gated templates
    - Each DEFAULT_PROGRAM_TEMPLATES entry has an `items` list with >1 item (grep review)
  </acceptance_criteria>
  <done>Templates seed gated by facilities with real multi-item checklists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Template editor + generic builder routes + tests</name>
  <read_first>
    - apps/api/routers/programs.py (template + MANAGER_ROLES context)
    - apps/api/models/requests.py (Phase 4 request models region, ~line 634)
    - apps/api/tests/test_programs_routes.py (harness)
  </read_first>
  <behavior>
    - test_initialize_gated: property without pool -> pool_check NOT seeded; with pool -> seeded
    - test_edit_template: PUT updates a template's items + frequency; version handling per unique(tenant,code,version)
    - test_generic_builder: POST creates a custom template with program_area + code + items
    - test_edit_rbac: housekeeper -> 403 on edit/create
  </behavior>
  <action>
    Add two routes to `programs.py` (both `require_role(*MANAGER_ROLES)`):
    (a) `PUT /programs/templates/{template_id}` — update `name`, `name_es`, `items` (checklist), and default frequency for a tenant-scoped template. Since pm_checklist_templates has UNIQUE(tenant_id, code, version), either bump `version` on a material checklist change or update in place per Claude's discretion — document the choice; keep it tenant-scoped (`.eq("tenant_id", ...)`).
    (b) `POST /programs/templates` — generic builder: create a custom template (`program_area` in engineering|housekeeping, unique `code`, `items` list, optional `name_es`). Reject duplicate codes with 409.
    Add `UpdateProgramTemplateRequest` and `CreateProgramTemplateRequest` to requests.py (SanitizedBaseModel; validate items shape). Add the four tests above.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py -k "template or builder or initialize" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PUT.*templates\|@router.put" apps/api/routers/programs.py` shows the editor route
    - `grep -n "@router.post(\"/templates\"" apps/api/routers/programs.py` shows the generic builder
    - RBAC test asserts housekeeper 403; gating test asserts pool absence
  </acceptance_criteria>
  <done>Managers can edit templates and build custom ones; gating + RBAC proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager -> template config | Only managers may edit/create templates |
| property config -> gated templates | Non-applicable obligations must not surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Elevation of Privilege | template editor/builder | mitigate | require_role(*MANAGER_ROLES); RBAC test asserts housekeeper 403; ASVS V4 |
| T-04-14 | Tampering | cross-tenant template edit | mitigate | All template reads/writes `.eq("tenant_id")`; tenant-scoped update |
| T-04-15 | Input Validation | generic builder items | mitigate | SanitizedBaseModel validates items shape; canonical facilities allowlist (Task 1) |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/ -q` green
- Migration 083 applied to prod; facilities gating verified
- Editor + generic builder RBAC-gated and tenant-scoped
</verification>

<success_criteria>
- Gated templates surface only where the facility exists (D-05)
- Managers edit frequency/checklist + build custom templates
- Named templates seed with real multi-item checklists
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-04-SUMMARY.md`
</output>
