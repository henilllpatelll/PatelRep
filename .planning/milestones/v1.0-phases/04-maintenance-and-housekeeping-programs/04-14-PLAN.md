---
phase: 04-maintenance-and-housekeeping-programs
plan: 14
type: execute
wave: 6
depends_on: ["04-13"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/components/engineering/CreateWorkOrderModal.tsx
  - apps/web/components/engineering/WorkOrderList.tsx
  - apps/web/components/engineering/EngineeringRoomBoard.tsx
  - apps/web/components/engineering/FailurePredictionSidebar.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "Create-work-order modal, work-order list, engineering room board, and failure-prediction sidebar render every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/components/engineering/CreateWorkOrderModal.tsx"
      provides: "Bilingual create-work-order modal"
      contains: "useTranslation"
    - path: "apps/web/components/engineering/FailurePredictionSidebar.tsx"
      provides: "Bilingual failure-prediction sidebar"
      contains: "useTranslation"
  key_links:
    - from: "components/engineering/CreateWorkOrderModal.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under engineering.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the remaining engineering/work-order floor components — CreateWorkOrderModal, WorkOrderList, EngineeringRoomBoard, FailurePredictionSidebar — into EN + ES, completing the D-03 "engineering / work-orders" component scope (WorkOrderCard and PMCompletionModal already done in 04-08; WorkOrderDetailDrawer in 04-13).

Purpose: Finish the engineering-component bilingual coverage named by D-03.
Output: Four engineering components fully bilingual with matching en.ts/es.ts keys.

Depends on 04-13 (shares en.ts/es.ts — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
  Pattern (analog: components/engineering/WorkOrderCard.tsx). Extend the top-level `engineering` namespace.
Namespace assignment (under top-level `engineering`):
  - engineering.createWorkOrder.*   → CreateWorkOrderModal.tsx
  - engineering.workOrderList.*      → WorkOrderList.tsx
  - engineering.roomBoard.*          → EngineeringRoomBoard.tsx
  - engineering.failurePrediction.*  → FailurePredictionSidebar.tsx
Reuse engineering.workOrderCard.*/workOrderDetail.* (status/priority/category labels), common.* where copy is identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate CreateWorkOrderModal.tsx</name>
  <read_first>
    - apps/web/components/engineering/CreateWorkOrderModal.tsx (429 lines — ~18 literals)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.createWorkOrder.*` keys to en.ts AND es.ts for all user-facing copy (modal title, form field labels, category/priority option labels, action buttons, validation/error copy, aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Reuse engineering.workOrderCard.* category/priority labels where identical. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/engineering/CreateWorkOrderModal.tsx` returns >= 1
    - `grep -c "createWorkOrder" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>CreateWorkOrderModal renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate WorkOrderList.tsx + EngineeringRoomBoard.tsx</name>
  <read_first>
    - apps/web/components/engineering/WorkOrderList.tsx (90 lines — ~1 literal)
    - apps/web/components/engineering/EngineeringRoomBoard.tsx (208 lines — ~8 literals)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.workOrderList.*` (WorkOrderList.tsx) and `engineering.roomBoard.*` (EngineeringRoomBoard.tsx) keys to en.ts AND es.ts covering headings, column/status labels, empty/error copy, aria-label/placeholder/title. Rewire both to `useTranslation()` + `t()`. Reuse engineering.workOrderCard.* status labels where identical. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/engineering/WorkOrderList.tsx` returns >= 1
    - `grep -c "useTranslation" apps/web/components/engineering/EngineeringRoomBoard.tsx` returns >= 1
    - `grep -c "workOrderList\|roomBoard" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>WorkOrderList + EngineeringRoomBoard render via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 3: Translate FailurePredictionSidebar.tsx</name>
  <read_first>
    - apps/web/components/engineering/FailurePredictionSidebar.tsx (287 lines — ~9 literals)
    - apps/web/i18n/locales/en.ts (engineering.* keys)
  </read_first>
  <action>
    Add `engineering.failurePrediction.*` keys to en.ts AND es.ts covering heading, prediction/risk labels, action buttons, empty/error copy, aria-label/placeholder/title. Rewire to `useTranslation()` + `t()`. Full EN/ES parity. Note the deferred-items.md item #3 pattern: render enum values (e.g. risk bands) via a translated label map, not the raw API value.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/engineering/FailurePredictionSidebar.tsx` returns >= 1
    - `grep -c "failurePrediction" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>FailurePredictionSidebar renders via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | These files must be literal-free before the gate widens (04-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-39 | Tampering (process) | untranslated engineering-component copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-40 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- All four files return `useTranslation` grep count >= 1
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- Engineering/work-order components usable in EN + ES; D-03 engineering-component scope complete (BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-14-SUMMARY.md`
</output>
