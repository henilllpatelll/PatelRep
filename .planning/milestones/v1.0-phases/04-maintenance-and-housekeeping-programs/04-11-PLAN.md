---
phase: 04-maintenance-and-housekeeping-programs
plan: 11
type: execute
wave: 3
depends_on: ["04-10"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/app/(dashboard)/housekeeping/page.tsx
  - apps/web/app/(dashboard)/housekeeping/assignments/page.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The housekeeping landing page and the assignments page render every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/app/(dashboard)/housekeeping/page.tsx"
      provides: "Bilingual housekeeping landing route"
      contains: "useTranslation"
    - path: "apps/web/app/(dashboard)/housekeeping/assignments/page.tsx"
      provides: "Bilingual assignments route"
      contains: "useTranslation"
  key_links:
    - from: "app/(dashboard)/housekeeping/page.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under housekeeping.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the housekeeping route pages `app/(dashboard)/housekeeping/page.tsx` and `.../assignments/page.tsx` into EN + ES. These app-route pages sit inside D-04's locked gate glob `app/(dashboard)/{housekeeping,engineering,tasks,programs}/**` — they MUST be literal-free before the gate widens (04-16), and D-03's locked scope names the housekeeping directory, not just its components.

Purpose: Full D-03 directory coverage so the widened lint gate is safe and floor pages are bilingual.
Output: Both housekeeping route pages fully bilingual with matching en.ts/es.ts keys.

Depends on 04-10 (shares en.ts/es.ts — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
  Page pattern: these are client pages ('use client'); `const { t } = useTranslation()` then `t('housekeeping.page.key')`.
Namespace assignment (under top-level `housekeeping`):
  - housekeeping.page.*            → housekeeping/page.tsx
  - housekeeping.assignmentsPage.* → housekeeping/assignments/page.tsx
Reuse shared keys (nav.*, common.*, and housekeeping.* sub-keys added in 04-09/04-10) where copy is identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate housekeeping/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/housekeeping/page.tsx (797 lines — ~22 literals)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog for t() wiring)
    - apps/web/i18n/locales/en.ts (existing housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.page.*` keys to en.ts AND es.ts for all user-facing copy (page heading/eyebrow, tab/section labels, action buttons, empty/loading/error states, aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Reuse existing housekeeping.*/common.*/nav.* keys where identical. Leave dynamic data raw or wrap non-copy nodes with `data-i18n-skip="true"`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/housekeeping/page.tsx"` returns >= 1
    - `grep -c "housekeeping" apps/web/i18n/locales/en.ts` equals `... es.ts` (parity maintained)
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>housekeeping/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate housekeeping/assignments/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/housekeeping/assignments/page.tsx (265 lines — ~16 literals)
    - apps/web/i18n/locales/en.ts (existing housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.assignmentsPage.*` keys to en.ts AND es.ts covering heading, assignment labels, action buttons, empty/error copy, aria-label/placeholder/title. Rewire to `useTranslation()` + `t()`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/housekeeping/assignments/page.tsx"` returns >= 1
    - `grep -c "assignmentsPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>assignments/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | These app pages are inside the D-04 gate glob; must be literal-free before 04-16 widens it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Tampering (process) | untranslated HK route copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-34 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- Both pages return `useTranslation` grep count >= 1
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- housekeeping landing + assignments pages usable in EN + ES (D-03/BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-11-SUMMARY.md`
</output>
