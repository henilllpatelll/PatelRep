---
phase: 04-maintenance-and-housekeeping-programs
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/components/housekeeping/RoomStatusBoard.tsx
  - apps/web/components/housekeeping/RoomCard.tsx
  - apps/web/components/housekeeping/AssignmentSidebar.tsx
  - apps/web/components/housekeeping/PredictionPanel.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The Housekeeping Room Status Board (a Realtime floor surface) renders every user-facing string via t() in EN and ES"
    - "Room cards, the assignment sidebar, and the prediction panel render via t() with matching EN + ES keys"
  artifacts:
    - path: "apps/web/components/housekeeping/RoomStatusBoard.tsx"
      provides: "Bilingual room status board"
      contains: "useTranslation"
    - path: "apps/web/components/housekeeping/RoomCard.tsx"
      provides: "Bilingual room card"
      contains: "useTranslation"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Spanish keys for the housekeeping board surface"
      contains: "housekeeping"
  key_links:
    - from: "components/housekeeping/RoomStatusBoard.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under housekeeping.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the first batch of housekeeping floor components (EN + ES), starting with the Room Status Board — one of only three Supabase Realtime surfaces in this codebase (CLAUDE.md "Realtime scope A2") and the highest-traffic daily housekeeping screen. Closes part of the D-03 locked bilingual floor contract that 04-08 narrowed (see 04-VERIFICATION.md truth #34, 04-08 deferred-items.md).

Purpose: Housekeepers use these screens every shift; D-03 requires them in EN and ES.
Output: RoomStatusBoard, RoomCard, AssignmentSidebar, PredictionPanel fully bilingual with matching en.ts/es.ts keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-08-SUMMARY.md

<interfaces>
<!-- READ apps/web/node_modules/next/dist/docs/ before non-trivial web work (AGENTS.md). -->
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448).
  Every EN key MUST have an ES counterpart in the same nested position.
  Component pattern (copy from the already-translated analog components/engineering/WorkOrderCard.tsx):
    `import { useTranslation } from 'react-i18next'` then `const { t } = useTranslation()` then `t('housekeeping.roomStatus.someKey')`.
  For a data-driven click wrapper that must NOT be linted, the existing code marks it `data-i18n-skip="true"` — reuse where a literal is genuinely a value not copy.

Namespace assignment for this plan (add these sub-objects under the top-level `housekeeping` namespace — create it if absent, mirroring the existing `programs` namespace at ~line 110 of en.ts):
  - housekeeping.roomStatus.*   → RoomStatusBoard.tsx
  - housekeeping.roomCard.*      → RoomCard.tsx
  - housekeeping.assignmentSidebar.* → AssignmentSidebar.tsx
  - housekeeping.predictionPanel.*   → PredictionPanel.tsx

Spanish wording reference: apps/mobile/i18n/ (en/es) has existing floor-string translations for rooms/housekeeping — draw equivalent Spanish phrasing where a matching string exists (mobile stays parked; reference only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate RoomStatusBoard.tsx + AssignmentSidebar.tsx</name>
  <read_first>
    - apps/web/components/housekeeping/RoomStatusBoard.tsx (702 lines — the Realtime board; extract every JSX text child, aria-label, placeholder, title)
    - apps/web/components/housekeeping/AssignmentSidebar.tsx (106 lines)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog — copy its useTranslation/t() wiring and its data-i18n-skip pattern)
    - apps/web/i18n/locales/en.ts (structure; `programs` namespace ~line 110 shows the nesting convention)
    - apps/web/i18n/locales/es.ts (mirror structure)
  </read_first>
  <action>
    Add `housekeeping.roomStatus.*` keys to en.ts AND es.ts covering all user-facing copy in RoomStatusBoard.tsx: status labels/legend, column/section headings, filter and action labels, empty/loading/error copy, and any aria-label/placeholder/title. Add `housekeeping.assignmentSidebar.*` for AssignmentSidebar.tsx. Rewire both components to call `useTranslation()` + `t('housekeeping.roomStatus...')`. Leave dynamic data values (room numbers, guest names, API enum values rendered as data) as-is; where a raw value would otherwise trip the lint gate, wrap the surrounding non-copy node with `data-i18n-skip="true"` as WorkOrderCard.tsx does — do NOT invent translated labels for raw data. Every EN key gets an ES counterpart; draw Spanish from apps/mobile/i18n where equivalent strings exist.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/housekeeping/RoomStatusBoard.tsx` returns >= 1
    - `grep -c "useTranslation" apps/web/components/housekeeping/AssignmentSidebar.tsx` returns >= 1
    - `grep -c "roomStatus" apps/web/i18n/locales/en.ts` and `... es.ts` return equal, non-zero counts
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>RoomStatusBoard + AssignmentSidebar render via t() with matching EN/ES keys; no type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Translate RoomCard.tsx + PredictionPanel.tsx</name>
  <read_first>
    - apps/web/components/housekeeping/RoomCard.tsx (335 lines)
    - apps/web/components/housekeeping/PredictionPanel.tsx (184 lines)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog)
    - apps/web/i18n/locales/en.ts (the housekeeping.* keys added in Task 1)
  </read_first>
  <action>
    Add `housekeeping.roomCard.*` (RoomCard.tsx) and `housekeeping.predictionPanel.*` (PredictionPanel.tsx) keys to en.ts AND es.ts covering all user-facing copy: status/badge labels, action buttons, time/label copy, empty/error states, aria-label/placeholder/title. Rewire both to `useTranslation()` + `t()`. Same rules as Task 1 (dynamic data left raw or wrapped with data-i18n-skip; full EN/ES parity).
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/housekeeping/RoomCard.tsx` returns >= 1
    - `grep -c "useTranslation" apps/web/components/housekeeping/PredictionPanel.tsx` returns >= 1
    - `grep -c "roomCard\|predictionPanel" apps/web/i18n/locales/en.ts` equals the same in es.ts, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>RoomCard + PredictionPanel render via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | Untranslated literals on these files must be blockable once the gate widens (plan 04-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-29 | Tampering (process) | untranslated HK board copy | mitigate | Full t() coverage now; the widened no-literal-string gate (04-16) will hard-fail future regressions |
| T-04-30 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart (grep-verified equal counts) |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- All four files return `useTranslation` grep count >= 1
- en.ts / es.ts new-key counts are equal (parity)
</verification>

<success_criteria>
- RoomStatusBoard, RoomCard, AssignmentSidebar, PredictionPanel usable in EN + ES (D-03/BL-01)
- No hardcoded user-facing copy remains in these four files
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-09-SUMMARY.md`
</output>
