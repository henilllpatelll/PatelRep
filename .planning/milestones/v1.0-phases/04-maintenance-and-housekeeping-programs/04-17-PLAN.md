---
phase: 04-maintenance-and-housekeeping-programs
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/e2e/phase4-programs.spec.ts
autonomous: true
requirements: [BL-03]

must_haves:
  truths:
    - "Phase 4 Playwright test 3 waits for the role-gated housekeeping depth panels to mount before deciding to skip, so a GM account genuinely exercises the EN<->ES panel translation instead of racing to a false skip"
  artifacts:
    - path: "apps/web/e2e/phase4-programs.spec.ts"
      provides: "Race-free bilingual panel test with an explicit wait for the manager-gated panels"
      contains: "Deep-clean schedules"
  key_links:
    - from: "e2e/phase4-programs.spec.ts test 3"
      to: "manager-gated /programs depth panels"
      via: "explicit wait-for-visible before the skip/count decision"
      pattern: "waitFor|toBeVisible"
---

<objective>
Fix the client-side role-resolution race in `apps/web/e2e/phase4-programs.spec.ts` test 3 ("Housekeeping deep-clean and DND welfare policy panels translate EN <-> ES at 390px"). The test currently checks `enDeepClean.count()` immediately after a best-effort `networkidle` wait and self-skips ("This account/role does not render the manager-gated housekeeping depth panels") before the role-gated panels have mounted — even though 04-07's SUMMARY and the same GM account render these panels correctly. The panel copy and ES strings already exist and are verified present (04-VERIFICATION.md); this is a test-authoring race, not a product defect.

Purpose: Turn a race-induced false skip into a genuine EN/ES pass (BL-03, 04-VERIFICATION.md human_verification item 2).
Output: test 3 waits for the manager-gated panel to be visible before deciding, and passes for the GM account when credentials are present.

Independent of the translation plans (touches only the spec file; the /programs panels it tests were already translated in 04-07/04-08) — runs in wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-VERIFICATION.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-07-SUMMARY.md

<interfaces>
Playwright (@playwright/test), config apps/web/playwright.phase4.config.ts (390px viewport project).
  Test file: apps/web/e2e/phase4-programs.spec.ts (test 3 at ~line 114).
  setLanguage(page, 'en'|'es') sets localStorage 'patelrep-language' then reloads.
  Confirmed-present ES headings the test asserts (already in i18n/locales/es.ts):
    - deep-clean: 'Programas de limpieza profunda'
    - DND policy:  'Tiempo de bienestar DND y política de escalamiento'
    EN counterparts: 'Deep-clean schedules' / 'DND welfare timing & escalation policy'.
  Credentials: TEST_EMAIL / TEST_PASSWORD env vars; the suite skips gracefully when TEST_PASSWORD is
  absent (this project has no live creds locally per CLAUDE.md "Current Scope"). The race fix must
  preserve that graceful no-cred skip while removing the FALSE role-gate skip when creds ARE present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the immediate count()-based skip with an explicit wait for the gated panel</name>
  <read_first>
    - apps/web/e2e/phase4-programs.spec.ts (whole file; focus test 3 at ~line 114-139 and the setLanguage/loginAsGM helpers)
    - apps/web/components/programs/HousekeepingDepthPanels.tsx (the DeepClean/DND panel headings + any role gate / testid that signals the manager panels have mounted)
  </read_first>
  <action>
    In test 3, after navigating to /programs and setting language to EN, replace the immediate `const hasManagerPanels = await enDeepClean.count()` skip with an explicit bounded wait for the manager-gated panel to actually mount: e.g. `await page.getByRole('heading', { name: 'Deep-clean schedules' }).waitFor({ state: 'visible', timeout: 15000 }).catch(() => {})`, then only skip if it is STILL not visible after the wait (`if (!(await enDeepClean.isVisible())) test.skip(...)`). This removes the role-resolution race so a GM account (which per 04-07 does render these panels) proceeds to the EN<->ES assertions instead of racing to a false skip. If HousekeepingDepthPanels.tsx exposes a stable data-testid for the manager panel container, prefer waiting on that testid over the heading text (more robust across EN/ES). Keep the graceful no-credentials skip in loginAsGM() intact. Do NOT change any product code or translation keys.
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test --config=playwright.phase4.config.ts -g "deep-clean and DND"</automated>
  </verify>
  <acceptance_criteria>
    - test 3 no longer contains an immediate `.count()`-gated `test.skip` before any wait for the panel
    - test 3 waits for the gated panel (heading or testid) to be visible before the skip/assert decision
    - With TEST_PASSWORD set against a running dev server, test 3 PASSES (both EN and ES headings become visible) rather than self-skipping; if creds are unavailable locally, the run skips gracefully at loginAsGM and the summary documents that the race fix is verified structurally + must be re-run with creds
    - The two other tests in the file are unchanged and still pass/skip as before
  </acceptance_criteria>
  <done>test 3 waits for the manager-gated panels and yields a genuine EN/ES pass for the GM account instead of a race-induced skip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test timing → skip decision | A premature skip must not mask a real bilingual regression on the /programs panels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-47 | (Test integrity) | race-induced false skip hides EN/ES regressions | mitigate | Explicit wait-for-visible before the skip/assert decision; genuine pass for GM account |
| T-04-48 | (Env dependency) | no local credentials | accept | Graceful no-cred skip retained; re-run with creds documented as the confirming step |
</threat_model>

<verification>
- `npx playwright test --config=playwright.phase4.config.ts -g "deep-clean and DND"` passes with creds (or documents graceful skip without creds)
- test 3 has no pre-wait `.count()` skip; waits for the gated panel first
</verification>

<success_criteria>
- Test 3 is race-free and gives a genuine EN/ES 390px pass for the GM account (BL-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-17-SUMMARY.md`
</output>
