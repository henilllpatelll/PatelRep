---
phase: 31-shell-navigation-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/routers/work_orders.py
  - apps/api/routers/guest_requests.py
  - apps/web/lib/api/engineering.ts
  - apps/web/lib/api/guest_requests.ts
  - apps/web/app/(dashboard)/engineering/work-orders/page.tsx
  - apps/web/app/(dashboard)/guest-requests/page.tsx
  - apps/web/app/(dashboard)/sop/page.tsx
autonomous: true

must_haves:
  truths:
    - "GET /work-orders and GET /guest-requests accept an optional q param that filters by title via case-insensitive ilike, staying tenant-scoped, and behave exactly as before when q is absent"
    - "The work-orders, guest-requests, and SOP list pages each open/scroll-to-and-highlight a specific record when loaded with ?focus=<id>, mirroring the Phase-26 ?asset= pattern, and render normally (no error) when the id is missing/stale"
    - "The engineering and guest-requests web API clients forward an optional q param to their list endpoints"
  artifacts:
    - path: "apps/api/routers/work_orders.py"
      provides: "Optional q: str = Query(None) → .ilike('title', f'%{q}%') on BOTH the engineer two-query branch and the default branch of list_work_orders"
      contains: "ilike"
    - path: "apps/api/routers/guest_requests.py"
      provides: "Optional q param → .ilike('title', …) on list_guest_requests"
      contains: "ilike"
    - path: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      provides: "?focus=<id> deep-link handling (scroll-into-view + transient highlight)"
      contains: "useSearchParams"
  key_links:
    - from: "command palette (31-04) work-order/guest result"
      to: "list page auto-open"
      via: "?focus=<id> query param read by useSearchParams on the list page"
      pattern: "focus"
    - from: "list endpoints"
      to: "tenant isolation"
      via: "existing .eq('tenant_id', current_user.hotel_id) retained; q only adds an ilike"
      pattern: "tenant_id"
---

<objective>
Land the minimal, additive backend + deep-link scaffolding the command-palette record search (31-04) consumes: one optional `q` → `title ilike` param on the paginated Work Orders and Guest Requests list endpoints, matching web-client `q` forwarding, and a `?focus=<id>` scroll-into-view-and-highlight convention on the three non-frozen list pages (work-orders, guest-requests, sop), mirroring the proven Phase-26 `?asset=` precedent. No shell files touched — runs fully parallel with 31-01.

Purpose: Rooms and SOPs are fully returned by their existing list calls (client-side filter, zero backend change); Work Orders and Guest Requests are paginated with no text filter, so a client-side filter over one page would silently miss records — the honest fix is one `ilike` param each (v1.6 Phase 26 precedent for a roadmap "pure-frontend" framing that undersold the need). Rooms deep-link via the existing frozen `?room=` mechanism (no code here); the other three entities need `?focus=`.
Output: two additive API params, two client updates, three list pages that honor `?focus=<id>`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-shell-navigation-redesign/31-CONTEXT.md
@.planning/phases/31-shell-navigation-redesign/31-RESEARCH.md

# Endpoints to extend (additive q param — keep tenant scoping)
@apps/api/routers/work_orders.py
@apps/api/routers/guest_requests.py
# Web clients to forward q
@apps/web/lib/api/engineering.ts
@apps/web/lib/api/guest_requests.ts
# List pages to add ?focus= to (NONE are frozen)
@apps/web/app/(dashboard)/engineering/work-orders/page.tsx
@apps/web/app/(dashboard)/guest-requests/page.tsx
@apps/web/app/(dashboard)/sop/page.tsx
# The proven precedent to mirror (?asset= scroll-into-view + transient ring highlight)
@apps/web/app/(dashboard)/engineering/predictions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional q → title ilike to the Work Orders and Guest Requests list endpoints</name>
  <files>apps/api/routers/work_orders.py, apps/api/routers/guest_requests.py</files>
  <action>
`work_orders.py` `list_work_orders` (starts ~line 168): add `q: Optional[str] = Query(None)` to the signature. This handler has TWO query paths — apply `q` to BOTH:
  - the `engineer` branch's inner `_base()` builder (add `if q: query = query.ilike("title", f"%{q}%")` inside `_base()` so both `r_mine`/`r_open` sub-queries filter),
  - the default branch's `query` (add the same `if q: query = query.ilike("title", f"%{q}%")` alongside the other optional filters, ~line 255).
Keep the existing `.eq("tenant_id", current_user.hotel_id)` scoping — `q` only adds a title filter, never widens tenant scope. When `q` is None, behavior is byte-identical to today.

`guest_requests.py` `list_guest_requests` (starts ~line 508): add `q: Optional[str] = Query(None)` and `if q: query = query.ilike("title", f"%{q}%")` after the existing status/room_id filters. `guest_requests` has a `title` column (referenced elsewhere in the file). Tenant scope unchanged.

Do NOT add FTS, a new index, a new endpoint, or search any column other than `title` — scope is exactly what the palette needs (31-RESEARCH "Don't Hand-Roll"). Confirm `Optional`/`Query` are already imported in both files (they are — used by existing params).
  </action>
  <verify>cd apps/api && python -c "import ast,sys; [ast.parse(open(f).read()) for f in ['routers/work_orders.py','routers/guest_requests.py']]" (parses); if the local Supabase-backed env is available, `pytest tests/` passes; grep confirms `ilike("title"` appears in BOTH the engineer branch and the default branch of work_orders.py and once in guest_requests.py</verify>
  <done>Both list endpoints accept optional q and filter title via ilike on every query path, tenant scoping intact, no-op when q absent, no new index/endpoint/column</done>
</task>

<task type="auto">
  <name>Task 2: Forward q in the engineering + guest-requests web clients</name>
  <files>apps/web/lib/api/engineering.ts, apps/web/lib/api/guest_requests.ts</files>
  <action>
`engineering.ts` `listWorkOrders` (~line 146): add optional `q?: string` to its params type; it already spreads `params` into `apiClient.get('/work-orders', { params })`, so no call-site change is needed beyond widening the type. Confirm `q` flows through `params`.

`guest_requests.ts` `listRequests` (~line 77): add optional `q?: string` to its params type; same pass-through via `{ params }`.

Rooms and SOPs need NO client change — the palette will filter their existing full-set list responses client-side (`roomsApi.list` returns all tenant room_status rows; `sopApi.listDocuments` returns all SOP docs). Do not add a q param to those clients.
  </action>
  <verify>cd apps/web && npm run type-check green; grep shows `q?: string` in both listWorkOrders and listRequests param types</verify>
  <done>Both clients accept and forward an optional q to their list endpoints; rooms/sop clients untouched; type-check green</done>
</task>

<task type="auto">
  <name>Task 3: Add ?focus=&lt;id&gt; deep-link handling to the work-orders, guest-requests, and SOP list pages</name>
  <files>apps/web/app/(dashboard)/engineering/work-orders/page.tsx, apps/web/app/(dashboard)/guest-requests/page.tsx, apps/web/app/(dashboard)/sop/page.tsx</files>
  <action>
Mirror the Phase-26 precedent in `predictions/page.tsx` (the `?asset=` handler, lines ~495-520) for each list page, adapted to its entity:
- Read `const searchParams = useSearchParams()` (wrap the page content in a `<Suspense>` boundary if not already — `useSearchParams` requires it in this Next version; predictions/page.tsx shows the `PredictionsPageContent` + default-export-`<Suspense>` split to copy).
- On mount / when data loads: `const focusId = searchParams.get('focus')`. If absent or the record isn't in the loaded set, render normally (graceful no-op — deleted/stale/cross-tenant id must NOT error; the list queries are already tenant-scoped server-side).
- If found: set a transient `highlightedId`, scroll its row/card into view (`ref.scrollIntoView({ behavior:'smooth', block:'center' })`), apply a transient highlight ring (reuse the precedent's `ring-2 ring-[var(--caution)]`-style transient class, cleared after ~3s). If the page has filters that would hide the focused record, relax them (as predictions does: reset filters so the focused item is visible) — keep this minimal.
- If the record's detail lives in an in-component drawer/modal (as with guest requests / work orders), opening that drawer for `focusId` is the ideal target; if that's more than a light change, the highlight-and-scroll baseline is acceptable and still satisfies "navigates to that record's view" (31-RESEARCH Open Q #1). Prefer opening the drawer where the existing local state makes it a small change.

Do NOT add any i18n copy strings here (the highlight is visual only, matching the precedent) — this keeps 31-03 off en.ts/es.ts so it runs parallel with 31-01. Do NOT touch rooms — the room detail drawer is inside the FROZEN RoomStatusBoard, which already reads `?room=<id>` (RoomStatusBoard.tsx:212,336); the palette links rooms straight to `/housekeeping?room=<id>` with zero code here.
  </action>
  <verify>cd apps/web && npm run build && npm run type-check green; loading `/engineering/work-orders?focus=<real-id>`, `/guest-requests?focus=<real-id>`, `/sop?focus=<real-id>` scrolls-to-and-highlights (or opens the drawer for) that record; a bogus `?focus=zzz` renders the list normally with no console error</verify>
  <done>All three list pages honor ?focus=<id> (scroll+highlight or drawer-open), degrade gracefully on missing id, add no new copy strings, and leave rooms to the existing frozen ?room= path</done>
</task>

</tasks>

<verification>
- `work_orders.py` + `guest_requests.py` parse; q applies ilike on every query path; tenant scope intact; no-op when q absent.
- Web clients forward q (type-check green); rooms/sop clients unchanged.
- Three list pages honor `?focus=<id>` with graceful fallback; no new i18n keys added (parity gate unaffected by this plan).
- `npm run build` + `npm run type-check` green from `apps/web`.
</verification>

<success_criteria>
NAV-04 backend half met: Work Orders + Guest Requests are title-searchable via a minimal additive param; all four entities have a working navigation target (rooms via frozen `?room=`, the other three via new `?focus=`). 31-04 can now wire the palette to real, role-gated record search.
</success_criteria>

<output>
After completion, create `.planning/phases/31-shell-navigation-redesign/31-03-SUMMARY.md` recording: the exact q/ilike additions (and that the engineer branch has two query paths both needing it), the client type widening, and per-entity focus target chosen (drawer-open vs scroll-highlight) for work-orders/guest-requests/sop.
</output>
