---
phase: 31-shell-navigation-redesign
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/web/components/shared/Header.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "From the shell header a user can open the notification panel and toggle between Unread and All, where All shows already-read history (what happened while they were away) — hotel_id + user scoped by the existing backend"
    - "The unread badge count keeps reflecting only unread notifications regardless of which tab is shown"
    - "Every previously-hardcoded English string in the notification panel (title, mark-all-read, empty state) and the two new tab labels are i18n keys present in BOTH en.ts and es.ts; the timestamp respects the active locale"
    - "When the shell redesign flag is on, the Header renders in the v2 token system; when off, it renders as before"
  artifacts:
    - path: "apps/web/components/shared/Header.tsx"
      provides: "Unread/All notification toggle (client-only, re-queries is_read), i18n'd strings, locale-aware timestamp, v2 restyle gated by redesigned prop"
      contains: "notificationsAll"
  key_links:
    - from: "Header notification All tab"
      to: "GET /notifications?is_read=true (returns read+unread history)"
      via: "notificationsApi.list({ is_read: tab === 'all', limit: 20 })"
      pattern: "is_read"
    - from: "Header unread badge"
      to: "unread-only query"
      via: "separate query keyed ['notifications','unread'] with is_read:false"
      pattern: "notifications.*unread"
---

<objective>
Close the NAV-03 notification-inbox gaps on the ~80%-built header bell: add an Unread/All history toggle (so a user can review what happened while on the floor), i18n the hardcoded panel strings + fix the locale-hardcoded timestamp, and apply the Phase-30 v2 restyle to the Header — all gated by the `redesigned` prop threaded in 31-01. The interaction pattern (bell + dropdown + mark-read) is kept; this is gap-close + restyle, not a rebuild. No backend change (the server already returns history when `is_read` is truthy).

Purpose: NAV-03 (review-while-away) + NAV-01 Header restyle.
Output: extended `Header.tsx`, new/reused notification i18n keys in both locales.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-shell-navigation-redesign/31-CONTEXT.md
@.planning/phases/31-shell-navigation-redesign/31-RESEARCH.md
@.planning/phases/31-shell-navigation-redesign/31-01-SUMMARY.md

@apps/web/components/shared/Header.tsx
@apps/web/lib/api/notifications.ts
# Server contract to rely on (is_read truthy ⇒ returns read+unread history; DO NOT edit)
@apps/api/routers/notifications.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification Unread/All history toggle (client-only) + i18n string cleanup + locale timestamp fix</name>
  <files>apps/web/components/shared/Header.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
HISTORY TOGGLE (NAV-03) — the server already supports it, so this is client-only (31-RESEARCH Pattern 2). Note the counter-intuitive semantics and document it inline: `GET /notifications` param is named `is_read` but its effect is "include-read" — `false`/absent ⇒ unread-only, `true` ⇒ all (read+unread) history, all `hotel_id`+`user_id` scoped.
- Add panel state `const [tab, setTab] = useState<'unread'|'all'>('unread')` and render two small tabs (Unread / All) atop the dropdown panel (above the list, near the existing title/Mark-all-read row).
- Change the panel's list query to be tab-parameterized: `queryKey: ['notifications', tab]`, `queryFn: () => notificationsApi.list({ is_read: tab === 'all', limit: 20 })`.
- KEEP a SEPARATE unread-only query for the BADGE so the badge always reflects true unread count regardless of the visible tab: keep/derive `unreadCount` from a query keyed `['notifications','unread']` with `is_read:false` (this preserves today's badge behavior). `handleMarkAllRead` / `handleMarkRead` must invalidate BOTH the badge unread query and the active list query so the UI updates.
- In the All tab, read items may render without the unread dot — style read vs unread rows distinctly (e.g. the leading dot only on unread) so history is legible.

I18N CLEANUP (the strings are raw English literals today; note per 31-RESEARCH Correction #2 this is hygiene, not un-blocking a red gate — `components/shared/**` is out of the `no-literal-string` scope — but the CONTEXT decision to fix them stands):
- Panel title `Notifications` (Header.tsx:221) → reuse existing `header.notifications`.
- `Mark all read` (:229) → new `header.markAllRead`.
- `No new notifications` (:234) → new `header.noNotifications` ("No notifications" / es equivalent).
- New tab labels → `header.notificationsUnread` ("Unread") and `header.notificationsAll` ("All").
- Add ALL new keys to BOTH `en.ts` and `es.ts` under the `header` block (parity gate `check:i18n-parity` fails CI if either locale is missing a key — Pitfall #3). Provide correct Spanish values, not English placeholders.

TIMESTAMP FIX: the notification timestamp at Header.tsx:247 is hardcoded `toLocaleTimeString('en-US', …)`. Change to `i18n.language === 'es' ? 'es-US' : 'en-US'` to match the date/shift formatting already at lines 133-138 (a real i18n defect the redesign is touching anyway).
  </action>
  <verify>cd apps/web && npm run build && npm run check:i18n-parity green; opening the panel and switching to All shows previously-read items; the badge count stays unread-only while the All tab is shown; marking one/all read updates both badge and list; timestamps localize under ES</verify>
  <done>Unread/All toggle works client-only against the existing endpoint, badge stays unread-only, all panel strings + tab labels are keyed in both locales with real ES values, timestamp is locale-aware, parity gate green</done>
</task>

<task type="auto">
  <name>Task 2: Header v2 token restyle, gated by the redesigned prop</name>
  <files>apps/web/components/shared/Header.tsx</files>
  <action>
Accept the `redesigned?: boolean` prop threaded from DashboardShell in 31-01. Apply the Phase-30 v2 tokens via className ONLY when `redesigned` is true; when false, keep the current classes verbatim (legacy branch unchanged). Migrations (31-RESEARCH Pattern 5, Header):
- `sticky top-0 z-50` → `z-[var(--z-header)]` (1200).
- Interactive focus states (search box, bell, user-menu buttons) → `focus-visible:ring-[var(--focus-ring)]`.
- Short transitions `duration-150` → `duration-[var(--motion-fast)]`.
- Apply `--brand*` where a primary brand accent is wanted (e.g. the search focus border/ring can move from `--accent-soft` toward `--brand`), at discretion — keep it tasteful and consistent with the sidebar's active-state treatment from 31-01.
- LEAVE the copilot `--ai-*` chrome as-is (it is semantic AI styling, not brand) and do NOT retint any `--alert/--info/--ready/--progress/--caution/--blocked` or `colors.status.*` value (Pitfall #4 — the sign-out `text-alert`/`--alert-soft` must keep its current token, not be re-hued).

Keep the notification/user-menu interaction behavior and all existing a11y (aria-expanded, focus trap in the user menu) intact.
  </action>
  <verify>cd apps/web && npm run build && npm run check:frozen-files && npm run check:contrast green; with the shell flag on the header shows v2 focus rings + z-token layering and still passes contrast; with the flag off it looks unchanged</verify>
  <done>Header renders v2 tokens only under the redesigned flag (legacy path unchanged), header z-index uses --z-header, focus states use --focus-ring, AI + status tokens untouched, a11y preserved, contrast + frozen gates green</done>
</task>

</tasks>

<verification>
- Unread/All toggle re-queries history via the existing endpoint (no backend edit); badge stays unread-only.
- All notification panel strings + tab labels keyed in both en.ts and es.ts (`check:i18n-parity` green); timestamp locale-aware.
- Header v2 restyle applies only under the flag; `--z-header` + `--focus-ring` used; status/AI tokens untouched.
- `npm run build`, `check:i18n-parity`, `check:frozen-files`, `check:contrast` green from `apps/web`.
</verification>

<success_criteria>
NAV-03 fully met (inbox reachable from header, review-while-away via All tab, hotel+user scoped, strings i18n'd). NAV-01 Header restyle met. No backend change; badge semantics preserved; frozen boards + contrast unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/31-shell-navigation-redesign/31-02-SUMMARY.md` recording: the tab/query-key structure (how badge stays unread-only), the exact new header i18n keys + ES values, the timestamp fix, and the Header restyle decisions.
</output>
