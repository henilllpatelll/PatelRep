---
phase: 31-shell-navigation-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/stores/uiPreferencesStore.ts
  - apps/web/components/shared/DashboardShell.tsx
  - apps/web/components/shared/Sidebar.tsx
  - apps/web/components/shared/Breadcrumbs.tsx
  - apps/web/components/shared/MobileFloorNav.tsx
  - apps/web/package.json
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "A user can toggle the desktop sidebar to an icon-only rail (icons + hover/focus tooltips) and back, and the collapsed/expanded choice survives a page reload and re-login (persisted per-user in uiPreferencesStore)"
    - "When section 'shell' is redesigned for the tenant, the Sidebar/Breadcrumbs/MobileFloorNav render in the Phase-30 v2 token system; when it is not, they render exactly as before (legacy branch untouched)"
    - "The collapsed rail shows every icon the expanded sidebar shows for the current role — no role loses a nav entry when collapsed — because the rail iterates the same opsItems/intelItems/peopleItems/bottomItems arrays derived from getAllowedNavItems"
    - "Mobile sidebar behavior (slide-in drawer) and the floor bottom-tab are unchanged — collapse is desktop-only"
  artifacts:
    - path: "apps/web/stores/uiPreferencesStore.ts"
      provides: "sidebarCollapsed persisted field + setter/toggle, mirroring density/theme/accent"
      contains: "sidebarCollapsed"
    - path: "apps/web/components/shared/Sidebar.tsx"
      provides: "Collapse-to-rail rendering + v2 token restyle, gated by redesigned flag and md: breakpoints"
      contains: "sidebarCollapsed"
    - path: "apps/web/components/shared/DashboardShell.tsx"
      provides: "Computes shellV2 once via isSectionRedesigned('shell', hotel), threads it to shell components, mounts one Tooltip.Provider"
      contains: "isSectionRedesigned"
  key_links:
    - from: "apps/web/components/shared/Sidebar.tsx collapse toggle"
      to: "uiPreferencesStore.sidebarCollapsed"
      via: "toggleSidebarCollapsed() + zustand persist (patelrep-ui-prefs)"
      pattern: "sidebarCollapsed|toggleSidebarCollapsed"
    - from: "apps/web/components/shared/DashboardShell.tsx"
      to: "Sidebar/Header/CommandPalette redesigned prop"
      via: "isSectionRedesigned('shell', hotel) computed once, passed as prop"
      pattern: "isSectionRedesigned\\('shell'"
---

<objective>
Lay the shell-redesign foundation: add the persisted `sidebarCollapsed` preference, thread the `RedesignGate` section `"shell"` decision into the shell once, install the Radix tooltip primitive, and deliver the two lowest-risk shell surfaces — the collapsible icon-rail Sidebar (NAV-02) and the light-touch Breadcrumbs/MobileFloorNav restyle (NAV-01, partial). Header and CommandPalette restyle land in 31-02 / 31-04 (they own those files).

Purpose: NAV-02 in full, plus the flag-threading + Tooltip.Provider scaffolding every other Phase-31 plan reads. This is the first end-to-end test of the per-section flag actually gating a real surface.
Output: extended `uiPreferencesStore`, `DashboardShell` flag threading + Tooltip.Provider, redesigned+collapsible `Sidebar`, restyled `Breadcrumbs`/`MobileFloorNav`, new collapse i18n keys in both locales, `@radix-ui/react-tooltip` installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-shell-navigation-redesign/31-CONTEXT.md
@.planning/phases/31-shell-navigation-redesign/31-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# The store to extend (mirror the density/theme/accent fields exactly)
@apps/web/stores/uiPreferencesStore.ts
# The single shell mount point (thread the flag here, add ONE Tooltip.Provider)
@apps/web/components/shared/DashboardShell.tsx
@apps/web/lib/utils/redesignFlag.ts
@apps/web/components/shared/RedesignGate.tsx
# The components to restyle / add collapse to
@apps/web/components/shared/Sidebar.tsx
@apps/web/components/shared/Breadcrumbs.tsx
@apps/web/components/shared/MobileFloorNav.tsx
# RBAC source of truth — READ-ONLY, never hardcode a route list in a shell component
@apps/web/lib/utils/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebarCollapsed to uiPreferencesStore + thread shellV2 flag + Tooltip.Provider in DashboardShell</name>
  <files>apps/web/stores/uiPreferencesStore.ts, apps/web/components/shared/DashboardShell.tsx, apps/web/package.json</files>
  <action>
1. Install the tooltip primitive (from `apps/web/`): `npm install @radix-ui/react-tooltip@1.2.16` (version cleared in 31-RESEARCH.md STACK section). This edits package.json + lockfile.

2. In `uiPreferencesStore.ts`, add a new persisted field mirroring density/theme/accent EXACTLY (same shape, same store, same `patelrep-ui-prefs` persist name — zustand/persist picks it up with no migration):
   - interface: `sidebarCollapsed: boolean` + `setSidebarCollapsed: (v: boolean) => void` + `toggleSidebarCollapsed: () => void`
   - create(): `sidebarCollapsed: false`, `setSidebarCollapsed: (sidebarCollapsed) => set({ sidebarCollapsed })`, `toggleSidebarCollapsed: () => set({ sidebarCollapsed: !get().sidebarCollapsed })`

3. In `DashboardShell.tsx`:
   - Import `isSectionRedesigned` from `@/lib/utils/redesignFlag` and `useHotelStore`. Compute ONCE: `const hotel = useHotelStore((s) => s.hotel); const shellV2 = isSectionRedesigned('shell', hotel)`. Do NOT wrap the whole shell body in `<RedesignGate>` (that would double-mount the tree — see 31-RESEARCH "The shell mount point"). Thread `shellV2` as a `redesigned` prop into `<Sidebar redesigned={shellV2} …>`, `<Header redesigned={shellV2} …>`, and `<CommandPalette redesigned={shellV2} />`. Header/CommandPalette will accept but ignore the prop until 31-02/31-04 — declaring the optional prop now keeps DashboardShell owned solely by this plan (no later plan re-touches it).
   - Wrap the shell subtree that contains the sidebar rail in ONE `<Tooltip.Provider delayDuration={200}>` from `@radix-ui/react-tooltip` (the collapsed-rail tooltips in Task 2 need a provider ancestor). Placing it high in DashboardShell is fine; verify no z-index conflict with the command-palette overlay (tooltip content uses `--z-tooltip` 1700, highest by design).
   - Also read `sidebarCollapsed` from the store here if you need it for layout width, OR let Sidebar read it itself (Sidebar already reads other stores — reading it inside Sidebar keeps DashboardShell thinner; prefer that).
  </action>
  <verify>cd apps/web && npm run build succeeds; `@radix-ui/react-tooltip` present in package.json dependencies; grep confirms `sidebarCollapsed` + `toggleSidebarCollapsed` in the store and `isSectionRedesigned('shell'` in DashboardShell</verify>
  <done>Store persists sidebarCollapsed under patelrep-ui-prefs; DashboardShell computes shellV2 once, threads a redesigned prop to Sidebar/Header/CommandPalette, and mounts one Tooltip.Provider; tooltip lib installed; build green</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar collapse-to-rail (NAV-02) + v2 token restyle, gated by the redesigned prop</name>
  <files>apps/web/components/shared/Sidebar.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
Accept a new optional prop `redesigned?: boolean` on `Sidebar`. Read `sidebarCollapsed` + `toggleSidebarCollapsed` from `useUIPreferencesStore`.

COLLAPSE (NAV-02) — desktop-only, gate ALL collapse styling behind the existing `md:` breakpoints so the mobile drawer (`-translate-x-full` / `mobileOpen` transform) is untouched (Pitfall #5):
- When `sidebarCollapsed` is true on desktop, render the rail at `md:w-16` (≈64px) instead of `md:w-[232px]`. Animate the width using the Phase-30 motion tokens (`duration-[var(--motion-base)]` + `ease-[var(--ease-standard)]`); do NOT fight the existing mobile `transition-transform duration-300` path.
- Collapsed state HIDES: hotel-switcher text (keep the hotel avatar square as the trigger, or hide the switcher entirely at rail width — keep the logo mark), nav-item `<span>` labels, the three group-heading `<p>` eyebrows, the `tag` badges, and the user-identity text block (keep the avatar circle).
- Collapsed state KEEPS: the logo mark, every item `<Icon>`, the active-state left-bar indicator, the gm-only Settings icon, the avatar circle. Group dividers become a thin rule at rail width.
- CRITICAL (Pitfall #2): the rail MUST iterate the SAME `opsItems` / `intelItems` / `peopleItems` / `bottomItems` arrays already derived from `getAllowedNavItems(...)`. Do NOT introduce a new list or any `role === …` / route-literal branch (the only allowed role transforms are the four existing ones: housekeeper "My Rooms" relabel, housekeeping subnav, gm-only settings, ROLE_LABELS). This guarantees the rail can never show fewer/more items than the expanded sidebar for any role.
- Wrap each collapsed icon-link in `Tooltip.Root` > `Tooltip.Trigger asChild` (the `<Link>`) > `Tooltip.Portal` > `Tooltip.Content side="right" sideOffset={8}` showing `navLabel(label)`, with `className="z-[var(--z-tooltip)] …"` (see the code example in 31-RESEARCH "Radix Tooltip on a collapsed rail item"). Give each collapsed `<Link>` an `aria-label={navLabel(label)}`. In expanded mode, render tooltips-off / labels as today.
- Add a collapse TOGGLE button where the stale "Logo + collapse button" comment sits (line ~162). Use lucide `PanelLeftClose` (expanded → collapse) / `PanelLeft` (collapsed → expand), calling `toggleSidebarCollapsed`. Give it `aria-label` (new i18n key `nav.collapseSidebar` / `nav.expandSidebar`) and `aria-expanded={!sidebarCollapsed}`. Desktop-only (`hidden md:inline-flex`).

RESTYLE (NAV-01, additive tokens via className) — apply ONLY when `redesigned` is true; else keep the current classes verbatim (legacy branch must stay byte-identical in behavior). Migrate: active-item accent bar `bg-accent` → may adopt `bg-[var(--brand)]`; transitions → `--motion-*`/`--ease-*`; interactive focus states → `focus-visible:ring-[var(--focus-ring)]`. Do NOT retint any `--alert/--info/--ready/--progress/--caution/--blocked` or `colors.status.*` value (Pitfall #4). Keep reading through `getAllowedNavItems` (NAV-01).

i18n: add `nav.collapseSidebar` ("Collapse sidebar" / "Contraer barra lateral") and `nav.expandSidebar` ("Expand sidebar" / "Expandir barra lateral") to BOTH `en.ts` and `es.ts` under the existing `nav` block (parity gate `check:i18n-parity` fails CI if either is missing — Pitfall #3).
  </action>
  <verify>cd apps/web && npm run build && npm run check:i18n-parity && npm run check:frozen-files all green; manually (in Task-6 live pass this is re-checked, but confirm now): with shell redesign flag on, toggling collapse narrows to a rail with tooltips and the choice persists across reload; the rail icon count equals the expanded row count for a test role</verify>
  <done>Sidebar collapses to a desktop icon rail with Radix tooltips, persists via uiPreferencesStore, iterates the same RBAC-derived group arrays (no dropped items), v2 restyle applies only under the redesigned flag, mobile drawer + status tokens untouched, both locales have the new keys, parity + frozen gates green</done>
</task>

<task type="auto">
  <name>Task 3: Breadcrumbs + MobileFloorNav v2 token restyle (NAV-01, light touch)</name>
  <files>apps/web/components/shared/Breadcrumbs.tsx, apps/web/components/shared/MobileFloorNav.tsx</files>
  <action>
Both are light-touch restyles — align active color + focus ring to the v2 tokens only; no structural or behavioral change.

Breadcrumbs renders in the PageHeader `eyebrow` slot (NOT DashboardShell), so it does not receive the `redesigned` prop. Read the flag directly the same way `RedesignGate` does: `const hotel = useHotelStore((s) => s.hotel); const redesigned = isSectionRedesigned('shell', hotel)`. When `redesigned`, apply `--brand`/`--focus-ring` to the active/hover link states and `--motion-*` to transitions; else keep current classes. Keep reading labels through `NAV_LABEL_KEYS`/`navigation.ts` (NAV-01) — do not hardcode.

MobileFloorNav: same direct-flag-read pattern. Apply v2 active-color + `focus-visible:ring-[var(--focus-ring)]` alignment ONLY. Do NOT add collapse behavior (NAV-02 is desktop-only, explicit anti-feature to touch the floor bottom-tab) and do NOT touch its curated `FLOOR_NAV` list (that hardcoded floor set is an existing, intentional pattern out of scope for this phase). Do not change status tokens (Pitfall #4).
  </action>
  <verify>cd apps/web && npm run build && npm run check:frozen-files green; Breadcrumbs still renders correct parent→child labels on a sub-route (e.g. /engineering/work-orders); MobileFloorNav bottom-tab still shows the same items and highlights the active route</verify>
  <done>Breadcrumbs + MobileFloorNav read the shell flag directly and apply v2 active/focus tokens when on, legacy classes when off; no structural/behavioral change, no collapse on mobile, RBAC labels still sourced from navigation.ts, status tokens untouched</done>
</task>

</tasks>

<verification>
- `npm run build`, `npm run check:i18n-parity`, `npm run check:frozen-files` all green from `apps/web`.
- `sidebarCollapsed` persists across reload (localStorage `patelrep-ui-prefs`); collapse is desktop-only (mobile drawer unaffected).
- Rail icon count == expanded row count for every role (guaranteed structurally by iterating the same RBAC-derived arrays; asserted machine-side by 31-05's group-coverage test).
- Redesign flag OFF ⇒ Sidebar/Breadcrumbs/MobileFloorNav render as before; flag ON ⇒ v2 tokens applied.
- No `--alert/--info/--ready/--progress/--caution/--blocked` or `colors.status.*` value changed (frozen-files gate).
</verification>

<success_criteria>
NAV-02 fully met (collapsible icon rail, persisted per-user). NAV-01 partially met (Sidebar/Breadcrumbs/MobileFloorNav on v2 tokens, still RBAC-driven). Flag-threading + Tooltip.Provider scaffolding in place for 31-02/31-04. Zero regression to mobile nav or frozen boards.
</success_criteria>

<output>
After completion, create `.planning/phases/31-shell-navigation-redesign/31-01-SUMMARY.md` recording: the collapse UX decisions (rail width, what hides/keeps), the flag-threading approach (redesigned prop vs direct read for Breadcrumbs/MobileFloorNav), the exact new i18n keys, and confirmation that the rail iterates the same RBAC-derived arrays (Pitfall #2 held).
</output>
