---
phase: 31-shell-navigation-redesign
plan: 06
type: execute
wave: 4
depends_on: ["31-01", "31-02", "31-03", "31-04", "31-05"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The Room-Board regression gate (npm run test:e2e:regression) re-passes with zero drift after all shell changes land — the shell wraps RoomStatusBoard/RoomDetailDrawer/EngineeringRoomBoard"
    - "All standing web gates pass on the redesigned shell: check:frozen-files, check:i18n-parity, check:contrast, type-check, lint, and the 31-05 matrix test:unit"
    - "The redesigned shell is self-verified live in a browser: sidebar collapse persists across reload, notification Unread/All toggle works, and command-palette record search returns role-appropriate grouped results that navigate correctly"
    - "The 6-role nav visibility is confirmed unchanged — the automated matrix (31-05) is the machine proof; the live pass confirms each reachable role's shell renders and navigates without hunting (NAV-05/06 human pass)"
  artifacts: []
  key_links:
    - from: "the redesigned shell (Sidebar/Header/CommandPalette restyle + tokens)"
      to: "the frozen Room Boards it wraps"
      via: "additive tokens only — pixel-diff regression proves zero drift"
      pattern: "test:e2e:regression"
    - from: "the redesign flag"
      to: "the shell surfaces"
      via: "isSectionRedesigned('shell', hotel) — verified both on (v2) and off (legacy) render paths"
      pattern: "isSectionRedesigned"
---

<objective>
Prove Phase 31 landed cleanly: re-run the Phase-30 Room-Board regression harness (success criterion 6) as the explicit last step, run the full standing gate suite against the redesigned shell, and self-verify the golden paths live in a browser (Self-Verification Policy) — including the NAV-05/06 per-role nav pass on top of 31-05's automated matrix. No source changes expected; this plan verifies and, only if a gate fails, files gap tasks rather than silently patching.

Purpose: the shell wraps the excluded boards, so the additive-only guarantee must be re-proven exactly as Phase 30 re-proved zero drift after its own token work (30-04). This is the close-out gate for the phase.
Output: green regression + gate suite, a live-verified shell, and a SUMMARY recording evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-shell-navigation-redesign/31-CONTEXT.md
@.planning/phases/31-shell-navigation-redesign/31-01-SUMMARY.md
@.planning/phases/31-shell-navigation-redesign/31-02-SUMMARY.md
@.planning/phases/31-shell-navigation-redesign/31-03-SUMMARY.md
@.planning/phases/31-shell-navigation-redesign/31-04-SUMMARY.md
@.planning/phases/31-shell-navigation-redesign/31-05-SUMMARY.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full standing gate suite + Room-Board regression re-pass</name>
  <files></files>
  <action>
From `apps/web/`, run every standing gate and confirm green (fix-forward only if a failure is a genuine Phase-31 regression; if a fix is non-trivial, STOP and create a gap task rather than patching blindly):
- `npm run type-check`
- `npm run lint`
- `npm run build`
- `npm run check:frozen-files`  (must show NO frozen-file hash change and NO room-status value change — the shell restyle was additive-only; if this fails, a plan touched a frozen file or retinted a status token — that is a hard regression to fix, not allowlist)
- `npm run check:i18n-parity`  (all new keys from 31-01/02/04 present in BOTH en.ts and es.ts)
- `npm run check:contrast`
- `npm run verify:i18n-gate` and `npm run check:floor-copy` (should be unaffected; confirm)
- `npm run test:unit`  (the 31-05 matrix: 6-role allow-set == baseline + group-coverage invariant)

Then the EXPLICIT success-criterion-6 step — re-prove the excluded boards did not drift, mirroring 30-04's re-proof:
- `npm run test:e2e:regression` (needs `.env.regression` sourced — the file exists locally, gitignored; if the seeded tenant is stale, `npm run seed:regression-fixture` first). Must pass with ZERO pixel drift on RoomStatusBoard / RoomDetailDrawer / EngineeringRoomBoard.

Run BOTH flag states where it matters: the regression harness represents the legacy path (flag off for the regression tenant unless it opts into 'shell'); additionally confirm that turning the 'shell' section on for a tenant does not drift the boards (the boards read the same CSS vars, which were not retinted — so on/off must both be zero-drift). If the regression tenant can toggle `web_redesign_sections` to include 'shell', re-run once with it on to prove the redesigned shell chrome still leaves the wrapped boards byte-identical.
  </action>
  <verify>Every listed gate exits 0; `test:e2e:regression` reports zero drift; `check:frozen-files` confirms no frozen hash / room-status value changed</verify>
  <done>type-check, lint, build, frozen-files, i18n-parity, contrast, i18n-gate, floor-copy, test:unit, and test:e2e:regression all green; zero board drift with the shell flag both off and on</done>
</task>

<task type="auto">
  <name>Task 2: Live browser self-verification of the redesigned shell + per-role nav pass</name>
  <files></files>
  <action>
Per the MANDATORY Self-Verification Policy, verify live (Claude drives the browser via Playwright / the playwright-cli skill — do NOT ask the user to test). Start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Use the seeded/test credentials available for this project (the GM test account is documented in memory; use any additional seeded role users the regression fixture provides).

Enable the 'shell' redesign section for the test tenant (set `web_redesign_sections` to include 'shell' via the tenants row / settings) so the v2 path is exercised, then walk the golden paths:
1. NAV-02 collapse: toggle the sidebar to the icon rail — icons + hover/focus tooltips show, labels/hotel-switcher hide; reload the page and confirm the collapsed choice persisted; expand again. Confirm on a narrow (mobile) viewport the sidebar still slides in full-width (no 64px rail) and the floor bottom-tab is unchanged.
2. NAV-03 notifications: open the header bell, switch Unread ↔ All (All shows read history), confirm the badge stays unread-only, mark one/all read updates both.
3. NAV-04 palette: ⌘K, search a room number (opens the room drawer via ?room=), a work-order title, a guest title, an SOP title (each scrolls-to/opens its record); confirm grouped output.
4. NAV-01: confirm no console errors, header/sidebar/breadcrumbs/palette render in the v2 look, and adjacent surfaces (a couple of dashboard routes) are not visually broken (Non-Regression Policy).

NAV-05/06 per-role pass: the automated matrix (31-05) is the authoritative 6-role RBAC proof. For the live human-style pass, log in as each role for which a credential exists (at minimum GM; plus any seeded housekeeper/engineer/front_desk/supervisor/chief_engineer users) and confirm each role's sidebar + collapsed rail show exactly its allowed sections and the user can reach every role-relevant section without hunting. For any role lacking a seeded credential, record that the matrix test covers it and note which live logins were exercised. Do NOT weaken RBAC or create privileged users just to log in.

If any golden path is broken, fix-forward if small; otherwise STOP and create a gap task describing the failure. Do not mark the phase complete with a known-broken golden path.
  </action>
  <verify>Dev servers reachable; each golden path (collapse+persist, notification toggle, palette record search, no console errors) passes live with the shell flag on; per-role sidebars match their allowed sections for every role logged in; adjacent routes unregressed</verify>
  <done>The redesigned shell is confirmed working live: collapse persists, notification history toggle works, palette record search navigates correctly, no console errors, and every reachable role's nav matches its allow-set (with the matrix covering all 6)</done>
</task>

</tasks>

<verification>
- Success criterion 6: `npm run test:e2e:regression` re-passes with zero drift (boards unchanged, flag off AND on).
- All standing gates green including the 31-05 matrix `test:unit`.
- Live self-verification of NAV-01/02/03/04 golden paths passes with no console errors.
- NAV-05/06: automated matrix identical to baseline + live per-role nav confirmed for available role logins.
</verification>

<success_criteria>
Phase 31 close-out: the redesigned shell (NAV-01–04) is live-verified, the 6-role RBAC nav is provably unchanged (NAV-05/06), and the Room-Board regression gate re-passes (criterion 6). The additive-only contract against the Phase-30 frozen set held.
</success_criteria>

<output>
After completion, create `.planning/phases/31-shell-navigation-redesign/31-06-SUMMARY.md` recording: each gate's result, the regression zero-drift confirmation (flag off and on), which role logins were exercised live vs covered only by the matrix, and any gap tasks filed. This is the phase's verification record.
</output>
