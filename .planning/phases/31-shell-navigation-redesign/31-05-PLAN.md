---
phase: 31-shell-navigation-redesign
plan: 05
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/web/lib/utils/navigation.test.ts
  - apps/web/lib/utils/navigation.matrix.json
  - apps/web/package.json
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "An automated test emits the 6-role × nav-item allow-set from navigation.ts and asserts it byte-identically against a committed baseline snapshot — so any drift in RBAC nav visibility fails the test"
    - "The test enforces the group-coverage invariant (Pitfall #2): every allowed href for every role falls into Operations/Intelligence/People/Settings groups, so a redesigned sidebar/rail can never silently drop an ungrouped-but-allowed item"
    - "The test runs via an npm script and is wired into CI as its own job"
  artifacts:
    - path: "apps/web/lib/utils/navigation.test.ts"
      provides: "node:test matrix over the 6 base roles + group-coverage guard, importing getAllowedNavItems/OPERATIONS_HREFS/INTELLIGENCE_HREFS/PEOPLE_HREFS from ./navigation"
      contains: "getAllowedNavItems"
    - path: "apps/web/lib/utils/navigation.matrix.json"
      provides: "Committed baseline: role -> ordered allowed hrefs, the frozen RBAC allow-set"
      contains: "housekeeper"
  key_links:
    - from: "navigation.test.ts"
      to: "the RBAC source of truth navigation.ts"
      via: "direct import of the pure getAllowedNavItems + group HREF lists (no browser, no @/ alias — relative ./navigation)"
      pattern: "from './navigation'"
    - from: ".github/workflows/ci.yml"
      to: "the matrix test"
      via: "a new job running npm run test:unit"
      pattern: "test:unit"
---

<objective>
Deliver the NAV-05/06 automated RBAC-nav guarantee: a machine test that emits the 6-role × nav-item allow-set directly from the pure `navigation.ts` functions and asserts it against a committed baseline, plus the Pitfall-#2 group-coverage invariant, wired into CI. This is the tripwire that proves the redesigned nav keeps RBAC visibility byte-identical — the machine half of NAV-05 (the live per-role login pass is done in 31-06).

Purpose: `navigation.ts` is pure/deterministic and is NOT modified by any Phase-31 plan, so its output is the same before and after the redesign — the committed snapshot IS the pre-redesign baseline, and re-running the test post-redesign proves zero drift. It also structurally catches the real regression surface (an allowed-but-ungrouped item silently dropped by the sidebar's group filter).
Output: `navigation.test.ts`, committed `navigation.matrix.json` baseline, a `test:unit` script, a CI job.

NOTE ON TEST RUNNER (deviation from 31-RESEARCH, documented): research suggested vitest, but vitest is NOT installed. The repo's actual unit-test convention is Node's built-in `node:test` + `node:assert/strict` (see `apps/web/lib/utils/housekeepingNavigation.test.ts`, `roomType.test.ts`, etc.), using relative imports — which sidesteps the `@/` alias problem entirely. This plan follows that existing convention rather than adding a new framework. `tsx` is added as the minimal dev-only TypeScript runner so these `node:test` files (currently unwired) actually execute in CI; this is not a competing test framework.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-shell-navigation-redesign/31-CONTEXT.md
@.planning/phases/31-shell-navigation-redesign/31-RESEARCH.md

# The pure RBAC source of truth to snapshot (READ-ONLY — never modify)
@apps/web/lib/utils/navigation.ts
# The existing node:test convention to mirror EXACTLY (relative import, node:assert/strict)
@apps/web/lib/utils/housekeepingNavigation.test.ts
# CI to extend with a new unit-test job
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 6-role × nav matrix test + commit the baseline snapshot</name>
  <files>apps/web/lib/utils/navigation.test.ts, apps/web/lib/utils/navigation.matrix.json</files>
  <action>
Create `lib/utils/navigation.test.ts` mirroring the existing `node:test` convention (`import test from 'node:test'`, `import assert from 'node:assert/strict'`, RELATIVE import `from './navigation'` — the `@/stores/authStore` import inside navigation.ts is type-only and erased, and `lucide-react` resolves as a normal dependency under tsx).

Import `getAllowedNavItems`, `OPERATIONS_HREFS`, `INTELLIGENCE_HREFS`, `PEOPLE_HREFS` from `./navigation`.

Define `const ROLES = ['gm','housekeeping_supervisor','housekeeper','engineer','chief_engineer','front_desk'] as const`.

Build the matrix with the STATIC defaults (document these caveats from 31-RESEARCH Pattern 4 as comments):
```
row[role] = getAllowedNavItems({ role, customRoleModules: null, frontDeskModules: null }).map(i => i.href)
```
This covers the 6 BASE roles with the DEFAULT front-desk module set (`DEFAULT_FRONT_DESK_MODULES`). Custom roles (per-hotel data, migrations 028/029) and hotel-customized front_desk_modules are NOT statically enumerable and are asserted at runtime elsewhere — note this in a comment so a future reader doesn't think the matrix is total.

Test 1 (regression baseline): assert the built matrix deep-equals the committed `navigation.matrix.json`. Generate that JSON from the CURRENT navigation.ts output and commit it as the frozen allow-set (this is the pre-redesign baseline; since navigation.ts is untouched by the redesign, it must remain identical post-redesign).

Test 2 (group-coverage invariant, Pitfall #2): for every role, `const grouped = new Set([...OPERATIONS_HREFS, ...INTELLIGENCE_HREFS, ...PEOPLE_HREFS, '/settings'])`; assert `row[role].filter(h => !grouped.has(h))` is empty — i.e. no allowed href falls outside the three sidebar groups + settings, so the sidebar/rail group filter can never silently drop an allowed item.

(Optional, if low-effort) Test 3: snapshot the two Sidebar-only transforms not captured by getAllowedNavItems — housekeeper "My Rooms" relabel and `getHousekeepingSubNavItems(role)` — for full fidelity; skip if it adds meaningful complexity, they are covered by the existing housekeepingNavigation.test.ts.
  </action>
  <verify>cd apps/web && after Task 2 wiring, `npm run test:unit` runs navigation.test.ts and both the matrix-equals-baseline and group-coverage assertions PASS; deliberately hand-editing a role's baseline row makes the test FAIL (confirms it actually guards), then revert</verify>
  <done>navigation.test.ts asserts the 6-role allow-set against a committed baseline and enforces group coverage; navigation.matrix.json is the frozen pre-redesign snapshot; both assertions pass against current navigation.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add tsx + test:unit script and wire a CI job</name>
  <files>apps/web/package.json, .github/workflows/ci.yml</files>
  <action>
1. From `apps/web/`: `npm install -D tsx` (minimal dev-only TypeScript runner; the repo already writes `node:test` files but has no runner wired — this enables them). NOTE: package.json is also edited by 31-01 (tooltip install); this plan depends on 31-01 so the edits serialize — do not run these in parallel.

2. Add an npm script to `apps/web/package.json`:
   `"test:unit": "tsx --test lib/utils/*.test.ts"`
   (This runs the new matrix test AND the pre-existing, previously-unwired `lib/utils/*.test.ts` files. Confirm the existing ones still pass; if a pre-existing test is already broken/stale, note it in the SUMMARY but do NOT fix unrelated logic here — scope is the matrix + wiring. If a pre-existing test fails only due to the `@/` alias, restrict the glob to the files that use relative imports and note the limitation.)

3. In `.github/workflows/ci.yml`, add a new job mirroring the existing web jobs' shape (checkout, node setup, `cd apps/web && npm ci`, then `cd apps/web && npm run test:unit`). Add the new job to the aggregation `needs:` list at the bottom (the summary job ~line 206) so the gate is enforced on the branch, consistent with how frozen-guard/contrast/i18n-parity are aggregated.
  </action>
  <verify>cd apps/web && npm run test:unit passes locally (matrix + any existing node:test files that use relative imports); `.github/workflows/ci.yml` contains a job running `npm run test:unit` and it is listed in the final aggregation `needs:`</verify>
  <done>tsx installed dev-only; test:unit script runs the node:test suite including the matrix; a CI job runs it and is aggregated into the branch gate</done>
</task>

</tasks>

<verification>
- `npm run test:unit` green: 6-role matrix equals committed baseline + group-coverage invariant holds.
- Tampering with the baseline (or with a group HREF list) fails the test — proven and reverted.
- `navigation.matrix.json` committed as the frozen allow-set; `navigation.ts` unmodified.
- CI runs `test:unit` as its own job, aggregated into the branch gate.
</verification>

<success_criteria>
NAV-05 machine half met: the RBAC nav allow-set for all 6 base roles is snapshotted and CI-enforced, structurally catching the group-drop regression (NAV-06 discoverability guard). The live per-role login verification (NAV-05 human pass) is executed in 31-06.
</success_criteria>

<output>
After completion, create `.planning/phases/31-shell-navigation-redesign/31-05-SUMMARY.md` recording: the runner decision (node:test + tsx over vitest, with the reason), the matrix baseline contents, the group-coverage invariant, any pre-existing node:test file that couldn't be wired (and why), and the CI job added.
</output>
