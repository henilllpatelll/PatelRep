---
phase: 11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/app/(app)/notifications/index.tsx
  - apps/mobile/app/(app)/scheduling/index.tsx
  - apps/mobile/app/(app)/sop/index.tsx
  - apps/mobile/app/(app)/sop/[sopId].tsx
autonomous: true
must_haves:
  truths:
    - "All 21 raw JSX-text literals across notifications/index.tsx, scheduling/index.tsx, sop/index.tsx, and sop/[sopId].tsx are wired through t()"
    - "Every new EN key has a matching ES key (parity)"
    - "sop/[sopId].tsx's 'Ask about this SOP' button (label + inert onPress={() => undefined}) is left byte-for-byte untouched — it is explicitly out of scope for this phase per CONTEXT"
    - "No behavior change: notification-marking, shift-loading, and SOP-fetching logic is untouched"
  artifacts:
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "New alertsScreen, scheduling, and sop top-level namespace objects"
      contains: "alertsScreen"
  key_links:
    - from: "apps/mobile/app/(app)/scheduling/index.tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "t() call resolves scheduling.* key"
      pattern: "t\\(\"scheduling\\."
    - from: "apps/mobile/app/(app)/sop/[sopId].tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "t() call resolves sop.* key, button at :66 unchanged"
      pattern: "t\\(\"sop\\."
---

<objective>
Wire real `t()` calls for the 21 raw-literal violations in `notifications/index.tsx`, `scheduling/index.tsx`, `sop/index.tsx`, and `sop/[sopId].tsx` (exact counts/lines confirmed by an actual widened-config ESLint run during Phase 11 research). Add new `alertsScreen`, `scheduling`, and `sop` locale namespaces at EN/ES parity.

**Critical scope boundary:** `sop/[sopId].tsx` has 7 of these 21 violations, but ALSO contains the "Ask about this SOP" button (`:66`, `<Button label="Ask about this SOP" onPress={() => undefined} .../>`) that is explicitly OUT of scope per `11-CONTEXT.md` — its inert `onPress` handler must be preserved exactly as-is. The button's `label` prop is NOT one of the 7 flagged violations (the gate's narrow attribute list doesn't include arbitrary `label` props — confirmed by direct code read), so simply not touching line 66 at all satisfies both constraints at once. Do not "helpfully" wire the button's label through `t()` either — leave that whole line untouched.

Purpose: ROADMAP success criterion 1 (partial — this covers 3 of the 16 newly-gated directories).
Output: 4 screen files wired to `t()`, `alertsScreen`/`scheduling`/`sop` namespaces added to both locale files at parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-CONTEXT.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-RESEARCH.md
@.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-03-SUMMARY.md

<i18n_reference>
## Namespace collision check
No top-level `alertsScreen`, `scheduling`, or `sop` object exists today. There IS an existing top-level `notifications: {...}` object (push-message-template copy: taskAssigned/woAssigned/inspectionFailed/slaWarning) — do NOT nest the Alerts screen's own UI strings inside it; that would conflate push-copy templates with screen chrome. Use a distinctly-named `alertsScreen` namespace instead (matches the screen's own visible title "Alerts"). There's also a plain string `home...scheduling: "Scheduling"` / `schedulingSub` and `home.gm.guestRequests`-style nav labels under unrelated parents — these do not collide with a new top-level `scheduling` object.

## Plural idiom (i18next v23, CLDR `_one`/`_other`) — use for sop/index.tsx's "N procedure(s)" count:
```
"procedureCount_one": "{{count}} procedure",
"procedureCount_other": "{{count}} procedures"
```

## Ternary-embedded literals are NOT gate-flagged — do not touch
Confirmed: `{todayAssignment.is_on_shift ? "On shift" : "Scheduled"}` (scheduling/index.tsx) and `{doc.indexing_status === "indexed" ? "AI-indexed" : doc.indexing_status}` (sop/[sopId].tsx) are ternary expressions inside `{}` — NOT flagged by the widened gate (confirmed empirically during research; only direct JSX text-node children are). Do not wire these — out of scope, would be scope creep.
</i18n_reference>

<literals>
## notifications/index.tsx — 2 violations:
- :91 `{notifications.length} unread` — text node " unread" (interpolation with count)
- :93 `<Text ...>Alerts</Text>` — title
No `useTranslation` import exists in this file today — add it.

## scheduling/index.tsx — 6 violations:
- :74 `<Text ...>My shifts</Text>` — title
- :88-90 `Today — {DAY_LABELS[today.getDay()]} {SHORT_MONTHS[today.getMonth()]} {today.getDate()}` — "Today — " prefix text node before 3 embedded expressions
- :104 `<Text ...>Day off</Text>` — text (the "no shift today" branch)
- :105 `<Text ...>No shift scheduled</Text>` — text
- :111 `<SectionLabel>This week</SectionLabel>` — text
(Note: :99's `Clocked in` inside `<Pill tone="ready">Clocked in</Pill>` — verify this exact line during read_first; if flagged, wire it as `scheduling.clockedIn`; the ternary at :98 `{todayAssignment.is_on_shift ? "On shift" : "Scheduled"}` is NOT flagged, per the note above — do not touch it.)
No `useTranslation` import exists in this file today — add it.

## sop/index.tsx — 6 violations:
- :62 `{docs.length} procedures` — text node " procedures" — replace whole expression with plural idiom: `{t("sop.procedureCount", { count: docs.length })}`
- :63 `<Text ...>How-to</Text>` — title
- :73 `<Text ...>Ask "how do I..."</Text>` — searchText (note the embedded quotes — keep them in the translated string)
- :83 `<SectionLabel>Categories</SectionLabel>` — text
- :91 `{count} procedure{count !== 1 ? "s" : ""}` — same plural pattern as :62, per-category count — replace whole expression with `{t("sop.procedureCount", { count })}`
- :99 `<SectionLabel>Recently added</SectionLabel>` — text
No `useTranslation` import exists in this file today — add it.

## sop/[sopId].tsx — 7 violations (DO NOT touch :66's "Ask about this SOP" button — not one of these 7):
- :55 `<Text ...>Updated {formatDate(doc.created_at)}</Text>` — "Updated " prefix (interpolation)
- :69 `I can answer questions about ` — text segment before the nested `<Text>{doc.title}</Text>`
- :70 ` or help you apply it step by step.` — text segment after the nested Text (both :69/:70 are part of ONE parent `<Text>` wrapping a nested `<Text>{doc.title}</Text>` — wire as `{t("sop.assistantIntro", { title: "" })}`-style is awkward since title is itself a nested styled Text; instead keep the structure and just wrap each surrounding text segment: `{t("sop.assistantIntroPrefix")}` before the nested Text, `{t("sop.assistantIntroSuffix")}` after it — read the exact JSX nesting during read_first before editing)
- :76 `<SectionLabel>Overview</SectionLabel>` — text
- :86 `<Text ...>Category</Text>` — infoLabel
- :92 `<Text ...>Pages</Text>` — infoLabel
- :98 `<Text ...>Added</Text>` — infoLabel
This file already imports `useEffect`/`useState` etc. but NOT `useTranslation` — add it. Do not add or modify anything on line 66 (`<Button label="Ask about this SOP" onPress={() => undefined} icon="sparkles" size="sm" />`) — leave that line character-for-character identical to today.
</literals>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alertsScreen + scheduling + sop EN/ES namespaces</name>
  <files>apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <read_first>
    - apps/mobile/i18n/locales/en.json — grep for `"alertsScreen":`, `"scheduling":`, `"sop":` at the top level to reconfirm none exist yet as objects
    - apps/mobile/i18n/locales/es.json — same check
  </read_first>
  <action>
    Add three new top-level namespace objects to BOTH en.json and es.json:

    ```jsonc
    "alertsScreen": {
      "unreadSuffix_one": "{{count}} unread",
      "unreadSuffix_other": "{{count}} unread",
      "title": "Alerts"
    },
    "scheduling": {
      "title": "My shifts",
      "todayPrefix": "Today — {{day}} {{month}} {{date}}",
      "clockedIn": "Clocked in",
      "dayOff": "Day off",
      "noShiftScheduled": "No shift scheduled",
      "thisWeek": "This week"
    },
    "sop": {
      "title": "How-to",
      "searchPrompt": "Ask \"how do I...\"",
      "categories": "Categories",
      "procedureCount_one": "{{count}} procedure",
      "procedureCount_other": "{{count}} procedures",
      "recentlyAdded": "Recently added",
      "updatedPrefix": "Updated {{date}}",
      "assistantIntroPrefix": "I can answer questions about",
      "assistantIntroSuffix": "or help you apply it step by step.",
      "overview": "Overview",
      "category": "Category",
      "pages": "Pages",
      "added": "Added"
    }
    ```
    Note on `unreadSuffix`: i18next requires both `_one`/`_other` forms even if the English text doesn't change between them (as shown above) — this is still needed for the ES translation to pluralize correctly. Note on `todayPrefix`: use named interpolation params (`day`, `month`, `date`) matching the 3 embedded expressions at scheduling/index.tsx:88-90.

    Provide human-quality Spanish for every ES value. Example ES values: `unreadSuffix_one/_other: "{{count}} sin leer"`, `title (alertsScreen): "Alertas"`, `scheduling.title: "Mis turnos"`, `todayPrefix: "Hoy — {{day}} {{month}} {{date}}"`, `clockedIn: "Marcado"`, `dayOff: "Día libre"`, `noShiftScheduled: "Sin turno programado"`, `thisWeek: "Esta semana"`, `sop.title: "Cómo hacerlo"`, `searchPrompt: "Preguntar \"cómo hago...\""`, `categories: "Categorías"`, `procedureCount_one: "{{count}} procedimiento"`, `procedureCount_other: "{{count}} procedimientos"`, `recentlyAdded: "Agregado recientemente"`, `updatedPrefix: "Actualizado {{date}}"`, `assistantIntroPrefix: "Puedo responder preguntas sobre"`, `assistantIntroSuffix: "o ayudarte a aplicarlo paso a paso."`, `overview: "Resumen"`, `category: "Categoría"`, `pages: "Páginas"`, `added: "Agregado"`.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const en=require('./i18n/locales/en.json'),es=require('./i18n/locales/es.json');const keys=['alertsScreen.title','scheduling.title','scheduling.todayPrefix','sop.title','sop.procedureCount_one','sop.procedureCount_other'];const get=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o);const miss=keys.filter(k=>!get(en,k)||!get(es,k));if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node parity script prints "parity ok" and exits 0
    - Both files remain valid JSON
    - No existing top-level `alertsScreen`/`scheduling`/`sop` object was overwritten (each key appears exactly once at the top level)
  </acceptance_criteria>
  <done>alertsScreen, scheduling, and sop namespaces exist in both locale files with all keys listed above, at EN/ES parity.</done>
</task>

<task type="auto">
  <name>Task 2: Wire t() across the 4 files (21 literals), preserving sop/[sopId].tsx's inert button untouched</name>
  <files>apps/mobile/app/(app)/notifications/index.tsx, apps/mobile/app/(app)/scheduling/index.tsx, apps/mobile/app/(app)/sop/index.tsx, apps/mobile/app/(app)/sop/[sopId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/notifications/index.tsx:1-30,85-99 (confirm no useTranslation import)
    - apps/mobile/app/(app)/scheduling/index.tsx:1-115 (confirm no useTranslation import; re-verify line :99's "Clocked in" text is actually gate-flagged before wiring it — if the widened-config eslint run in Task 3 of 11-06 doesn't flag it, leave it; if unsure, wire it anyway since it's harmless either way)
    - apps/mobile/app/(app)/sop/index.tsx:1-105 (confirm no useTranslation import)
    - apps/mobile/app/(app)/sop/[sopId].tsx:1-105 — READ CAREFULLY, especially :62-72 (the CopilotHero block containing both the out-of-scope button at :66 and the in-scope text at :69/:70) and confirm the exact current JSX nesting before editing
    - apps/mobile/i18n/locales/en.json (confirm Task 1's keys exist)
  </read_first>
  <action>
    For each file, replace the raw literal(s) at the exact violation lines with the matching Task-1 key, adding `import { useTranslation } from "react-i18next";` and `const { t } = useTranslation();` where missing (mirror an already-i18n-wired sibling screen, e.g. `app/(app)/tasks/index.tsx`, for hook placement).

    notifications/index.tsx:
    - :91 → `{t("alertsScreen.unreadSuffix", { count: notifications.length })}`
    - :93 → `{t("alertsScreen.title")}`

    scheduling/index.tsx:
    - :74 → `{t("scheduling.title")}`
    - :88-90 → `{t("scheduling.todayPrefix", { day: DAY_LABELS[today.getDay()], month: SHORT_MONTHS[today.getMonth()], date: today.getDate() })}`
    - :99 (if confirmed flagged) → `{t("scheduling.clockedIn")}`
    - :104 → `{t("scheduling.dayOff")}`
    - :105 → `{t("scheduling.noShiftScheduled")}`
    - :111 → `{t("scheduling.thisWeek")}`
    Leave the :98 ternary (`{todayAssignment.is_on_shift ? "On shift" : "Scheduled"}`) untouched — not gate-flagged.

    sop/index.tsx:
    - :62 → replace the whole `{docs.length} procedures` expression with `{t("sop.procedureCount", { count: docs.length })}`
    - :63 → `{t("sop.title")}`
    - :73 → `{t("sop.searchPrompt")}`
    - :83 → `{t("sop.categories")}`
    - :91 → replace the whole `{count} procedure{count !== 1 ? "s" : ""}` expression with `{t("sop.procedureCount", { count })}`
    - :99 → `{t("sop.recentlyAdded")}`

    sop/[sopId].tsx:
    - :55 → `{t("sop.updatedPrefix", { date: formatDate(doc.created_at) })}`
    - :69/:70 → wrap the two literal text segments surrounding the nested `<Text>{doc.title}</Text>`: `{t("sop.assistantIntroPrefix")} <Text style={[...]}>{doc.title}</Text> {t("sop.assistantIntroSuffix")}` (preserve the nested Text's own styling exactly as today — only the two surrounding literal segments change)
    - :76 → `{t("sop.overview")}`
    - :86 → `{t("sop.category")}`
    - :92 → `{t("sop.pages")}`
    - :98 → `{t("sop.added")}`
    **Do NOT touch line 66** (`<Button label="Ask about this SOP" onPress={() => undefined} icon="sparkles" size="sm" />`) — leave it character-for-character identical. Do NOT touch the :57 ternary (`doc.indexing_status === "indexed" ? "AI-indexed" : doc.indexing_status`) — not gate-flagged, out of scope.

    Change ONLY the literal→t() swaps and needed import/hook additions. Do NOT alter `handleMarkAll`, `mySchedule`/`load`/`onRefresh`, `getDocument`, or any theme.*/styling logic.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` passes
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/[sopId].tsx' -SimpleMatch 'Ask about this SOP' -ErrorAction Stop).Count -ne 1) { exit 1 }` exits 0 (PowerShell) — the button's label is untouched, still present verbatim
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/sop/[sopId].tsx' -SimpleMatch 'onPress={() => undefined}' -ErrorAction Stop).Count -ne 1) { exit 1 }` exits 0 (PowerShell) — the inert handler is untouched
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/scheduling/index.tsx' -SimpleMatch 't("scheduling' -ErrorAction Stop).Count -lt 5) { exit 1 }` exits 0 (PowerShell)
    - Diff review: no data-fetching/handler/business-logic line changed in any of the 4 files
  </acceptance_criteria>
  <done>All 21 literals resolve through t() with Task-1 keys; sop/[sopId].tsx's out-of-scope button preserved exactly; type-check green; eslint.config.mjs NOT yet modified (deferred to 11-06).</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npm run type-check` green
- EN/ES parity node-script exits 0
- `sop/[sopId].tsx` line 66 unchanged (both PowerShell assertions above pass)
- Manual diff review confirms zero logic-line changes across the 4 files
</verification>

<success_criteria>
21 of the 52 total gate-widening violations resolved (notifications + scheduling + sop directories); alertsScreen/scheduling/sop locale namespaces at EN/ES parity; the explicitly out-of-scope "Ask about this SOP" button untouched; zero behavior change.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-ui-parity-cleanup-widen-i18n-lint-gate-fix-hardcoded-dark-mode-color-address-tooling-audit-debt/11-04-SUMMARY.md`.
</output>
