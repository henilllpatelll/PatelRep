---
phase: 06-pms-and-ai-expansion
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified: []
autonomous: false
requirements: [D-06]
tags: [verification, e2e, self-verification, opera, ai]

must_haves:
  truths:
    - "The full API test suite passes with all Phase 6 test files included"
    - "apps/web type-check passes"
    - "A live authenticated GM browser walkthrough exercises the AI copilot fast-path + Opera settings surface with zero console errors"
    - "The Opera settings web surface behaves correctly for a pilot-enabled vs non-pilot hotel"
  artifacts:
    - path: ".planning/phases/06-pms-and-ai-expansion/06-05-SUMMARY.md"
      provides: "phase verification record (test counts, walkthrough results, residual gaps)"
  key_links:
    - from: "apps/web/app/(dashboard)/settings/integrations/page.tsx"
      to: "/integrations/opera/* API"
      via: "web settings surface driving the pilot-gated endpoints"
      pattern: "opera"
    - from: "apps/web/components/shared/DashboardShell.tsx"
      to: "AICopilotBubble"
      via: "copilot mounted on the authenticated shell"
      pattern: "AICopilotBubble"
---

<objective>
Phase gate: prove the whole Phase 6 audit surface to the CLAUDE.md Self-Verification Policy (D-06) — full suite green, web type-check green, and a live authenticated browser walkthrough on localhost that a fully-automated E2E cannot cover (no live OpenAI/Anthropic/OHIP credentials exist locally).

The Opera web surface exists (`apps/web/app/(dashboard)/settings/integrations/page.tsx`, 61 Opera references) and the AI copilot mounts via `apps/web/components/shared/DashboardShell.tsx::AICopilotBubble` — both are reachable for the walkthrough.

Environment caveats (from 06-RESEARCH): no local LLM keys → only the zero-credit rule-engine `try_fast_path()` copilot path and RBAC/credit-cap-rejection paths can be walked live; actual GPT/Claude output cannot. No OHIP sandbox → the Opera settings UI can be exercised for pilot-gated vs non-pilot behavior and safe-error handling, but a real connect→sync→webhook round trip cannot. Verify what CAN be verified; document what cannot as an accepted deferral, not a gap.

Purpose: Final D-06 sign-off before `/gsd-verify-work`.
Output: 06-05-SUMMARY.md recording test counts, walkthrough observations, and residual accepted deferrals.

Depends on all four implementation plans (06-01..06-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pms-and-ai-expansion/06-CONTEXT.md
@.planning/phases/06-pms-and-ai-expansion/06-RESEARCH.md
@.planning/phases/06-pms-and-ai-expansion/06-01-SUMMARY.md
@.planning/phases/06-pms-and-ai-expansion/06-02-SUMMARY.md
@.planning/phases/06-pms-and-ai-expansion/06-03-SUMMARY.md
@.planning/phases/06-pms-and-ai-expansion/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate — API suite + web type-check</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/06-pms-and-ai-expansion/06-VALIDATION.md (sampling rate + full-suite command + Wave 0 file list to confirm all five new test files are present and passing)
    - apps/api/tests/ (confirm the 5 new files exist: test_ai_copilot_rbac.py, test_ai_copilot_credits.py, tests/smoke/test_opera_routes.py, tests/smoke/test_opera_webhooks.py, test_opera_pilot_gate.py)
  </read_first>
  <action>
    Run the full API test suite and the web type-check. Concretely:
    1. `cd apps/api && python -m pytest tests/ -q` — must be zero failures. Confirm all five Phase 6 test files are collected and green (grep the run summary for their names or run them explicitly: `python -m pytest tests/test_ai_copilot_rbac.py tests/test_ai_copilot_credits.py tests/smoke/test_opera_routes.py tests/smoke/test_opera_webhooks.py tests/test_opera_pilot_gate.py -q`).
    2. `cd apps/web && npm run type-check` — must pass (the confirm_tasks/TaskPreview change is API-side, but AICopilotBubble.tsx consumes the confirm contract — a wire mismatch would surface at runtime, not type-check, so also note this for Task 2's live check).
    3. Record the exact test count (e.g. "N passed") and compare against the STATE.md ~427-baseline to confirm net-new tests added and none regressed.
    If anything is red, STOP and fix (or route back to the owning plan) before the checkpoint — do not present a walkthrough on a red suite.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/ -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` reports zero failures/errors
    - All five Phase 6 test files are present and pass (explicit run green)
    - `cd apps/web && npm run type-check` exits 0
    - Test count recorded and shown to have grown vs the ~427 baseline
  </acceptance_criteria>
  <done>Full API suite + web type-check green with all five Phase 6 test files included; counts recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live authenticated browser walkthrough (AI copilot + Opera settings)</name>
  <read_first>
    - apps/web/app/(dashboard)/settings/integrations/page.tsx (Opera settings surface to exercise)
    - apps/web/components/ai/AICopilotBubble.tsx (copilot UI + confirm flow)
    - apps/web/components/shared/DashboardShell.tsx (where the copilot bubble mounts)
    - CLAUDE.md (§Self-Verification Policy — GM test account; §Current Scope — no local LLM/OHIP keys)
    - Project memory reference_test_account.md (GM login for localhost testing)
  </read_first>
  <what-built>
    Phase 6 hardening: token-derived AI credits + single SOP audit-log (06-01), Opera pilot flag + RBAC/tenant coverage (06-02), AI copilot RBAC + typed confirm_tasks (06-03), corrected Opera webhook signature + pilot no-op (06-04). This checkpoint verifies the user-visible surfaces on localhost per the mandatory Self-Verification Policy.

    Before pausing, the executor MUST have already: started `npm run dev:api` (:8000) and `npm run dev:web` (:3000), logged in as the GM test account, and driven the walkthrough itself (Playwright or the playwright-cli skill) — capturing that each step works with zero console errors. This checkpoint is the human confirming that automated walkthrough, not doing the work.
  </what-built>
  <how-to-verify>
    On localhost:3000, logged in as the GM test account:
    1. Open the AI copilot bubble (bottom of the dashboard shell). Type a message that hits the zero-credit rule-engine fast path (e.g. a simple task phrasing per clientFastPath.ts / try_fast_path) — confirm a preview card renders and the confirm flow creates a task WITHOUT requiring an LLM key. Confirm no console errors and the created task is scoped to the GM's hotel.
    2. Confirm the confirm_tasks change did not break the wire contract: the task-confirm request succeeds (200), not 422 — i.e. TaskPreview's fields match what AICopilotBubble sends.
    3. Navigate to Settings → Integrations (`/settings/integrations`). Observe the Opera surface. For a hotel with `opera_pilot_enabled=FALSE` (default), confirm Opera actions (connect/test/sync) are rejected/gated (expect 403 from the API — the UI should surface this gracefully, not crash). If the test hotel is set to `opera_pilot_enabled=TRUE`, confirm the Opera actions are reachable (connect attempt may still fail safely without OHIP creds — the SAFE error, no leaked internals, is the pass condition).
    4. Confirm no uncaught console errors and no broken UI across both surfaces.

    Note explicitly (accepted deferrals, NOT gaps): actual GPT/Claude copilot responses (no local LLM keys) and a real Opera connect→sync→webhook round trip (no OHIP sandbox) cannot be exercised live.
  </how-to-verify>
  <resume-signal>Type "approved" if both surfaces work with no console errors, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GM browser → web app → API | Live authenticated exercise of the hardened AI + Opera surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Tampering | Wire-contract mismatch (TaskPreview vs AICopilotBubble payload) 422s valid requests | mitigate | Live walkthrough confirms the task-confirm flow returns 200, not 422 (Task 2 step 2). Type-check alone cannot catch this (cross-runtime), so it is verified in-browser. |
| T-06-20 | Elevation of Privilege | Pilot gate not enforced end-to-end at the UI | mitigate | Live check of non-pilot vs pilot hotel Opera actions (Task 2 step 3) confirms the API 403 gate surfaces correctly through the web UI. |
| T-06-21 | Info Disclosure | OHIP/LLM credential-dependent paths unverifiable locally | accept | No local LLM keys / OHIP sandbox (CLAUDE.md Current Scope). Documented as accepted deferrals in the SUMMARY, consistent with Phase 5's Twilio deferral precedent. |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/ -q` — zero failures
- `cd apps/web && npm run type-check` — exit 0
- Human-approved live walkthrough of AI copilot fast-path + Opera settings surface
</verification>

<success_criteria>
- Full API suite + web type-check green with all Phase 6 tests included
- Live GM walkthrough of AI copilot fast-path + Opera settings passes with zero console errors
- Pilot-gated vs non-pilot Opera behavior confirmed through the UI
- Residual credential-dependent paths documented as accepted deferrals, not gaps
</success_criteria>

<output>
After completion, create `.planning/phases/06-pms-and-ai-expansion/06-05-SUMMARY.md` recording test counts, walkthrough results, and accepted deferrals.
</output>
