---
phase: 06-pms-and-ai-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/085_opera_pilot_flag.sql
  - apps/api/routers/integrations.py
  - apps/api/tests/smoke/test_opera_routes.py
  - apps/api/tests/test_opera_pilot_gate.py
autonomous: true
requirements: [D-01, D-03, D-06]
tags: [opera, pms, pilot-flag, rbac, tenant-isolation, migration]

must_haves:
  truths:
    - "A new tenants.opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE column exists in the live Supabase DB"
    - "All 7 /integrations/opera/* endpoints return 403 for a hotel whose opera_pilot_enabled is FALSE"
    - "A pilot-enabled hotel can reach the Opera endpoints (subject to normal RBAC)"
    - "Cross-tenant Opera credential/conflict reads return 404/empty and write zero rows for the wrong tenant"
    - "Per-role RBAC holds on every Opera endpoint (connect/sync/test/disconnect = gm; conflicts = gm/chief_engineer; status = any authenticated)"
  artifacts:
    - path: "supabase/migrations/085_opera_pilot_flag.sql"
      provides: "opera_pilot_enabled column on tenants"
      contains: "opera_pilot_enabled"
    - path: "apps/api/tests/smoke/test_opera_routes.py"
      provides: "RBAC + tenant-isolation coverage for all 7 Opera endpoints"
      contains: "def test_"
    - path: "apps/api/tests/test_opera_pilot_gate.py"
      provides: "D-03 pilot-flag enforcement tests"
      contains: "opera_pilot"
    - path: "apps/api/routers/integrations.py"
      provides: "pilot-flag guard on every Opera endpoint"
      contains: "opera_pilot_enabled"
  key_links:
    - from: "apps/api/routers/integrations.py Opera handlers"
      to: "tenants.opera_pilot_enabled"
      via: "guard check raising 403 when disabled"
      pattern: "opera_pilot_enabled"
    - from: "apps/api/routers/integrations.py"
      to: "opera_credentials / integration_sync_conflicts"
      via: "tenant-scoped .eq(tenant_id, current_user.hotel_id)"
      pattern: "\\.eq\\(\"tenant_id\""
---

<objective>
Build the D-03 Opera pilot-flag mechanism (which does not exist anywhere in the schema today — Opera is currently live for every hotel) and prove the whole Opera routes surface to Phase 1–5 rigor (RBAC + tenant isolation), tests-first.

Two deliverables:
1. **Pilot flag.** Add `tenants.opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE` (single boolean, matching the existing `tenants.is_active` / `opera_credentials.is_connected` idiom — a `pilot_features` table is premature per YAGNI, only Opera needs gating; AI copilot stays ungated per D-04). Enforce it on all 7 `/integrations/opera/*` endpoints: raise 403 when the calling hotel's flag is FALSE. This makes CLAUDE.md's stale "Opera feature-flagged for pilot" claim actually true.
2. **Routes audit.** New `test_opera_routes.py` covering per-role RBAC (403 matrix) and tenant isolation (cross-hotel → 404/empty + zero writes) for all 7 endpoints, plus `test_opera_pilot_gate.py` for the flag.

Purpose: Gate Opera to explicitly enrolled pilot hotels (D-03) and close the zero-RBAC/zero-tenant-isolation test gap (D-01/D-06). Fix-in-place per D-05.
Output: migration 085 (applied to live DB), pilot-gate guards in integrations.py, two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pms-and-ai-expansion/06-CONTEXT.md
@.planning/phases/06-pms-and-ai-expansion/06-RESEARCH.md
@.planning/phases/06-pms-and-ai-expansion/06-PATTERNS.md

<interfaces>
<!-- Verified in 06-PATTERNS.md — RBAC map for integrations.py, exact line refs. -->

RBAC map (apps/api/routers/integrations.py):
- POST   /opera/connect               → require_role("gm")                       (line 20)
- GET    /opera/status                → get_current_user only (any role)         (line 68)
- POST   /opera/sync                  → require_role("gm")                       (line 94)
- GET    /opera/conflicts             → require_role("gm", "chief_engineer")    (line 110)
- POST   /opera/conflicts/{id}/resolve→ require_role("gm", "chief_engineer")    (line 128)
- POST   /opera/test                  → require_role("gm")                       (line 171)
- DELETE /opera/disconnect            → require_role("gm")                       (line 187)

Tenant scoping already present on every read/write: .eq("tenant_id", current_user.hotel_id) (lines 73, 115, 133, 151, 158).

Correct bug-449-class None-guard already used here (copy this idiom for the new pilot lookup):
```python
# apps/api/routers/integrations.py::opera_status lines 77-78
if not result or not result.data or not result.data.get("is_connected"):
    return {"data": {"connected": False}}
```

Migration style to mirror (supabase/migrations/084_guest_phone_adr_and_retention.sql):
```sql
ALTER TABLE public.tenants ADD COLUMN IF NOT EXISTS opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE;
COMMENT ON COLUMN public.tenants.opera_pilot_enabled IS
  'Gates Opera Cloud/OHIP connect, sync, and webhook processing to explicitly enrolled pilot hotels (Phase 6 D-03).';
-- ROLLBACK:
-- ALTER TABLE public.tenants DROP COLUMN opera_pilot_enabled;
```

Test harness (apps/api/tests/smoke/test_integrations_security.py + tests/smoke/fake_supabase.py FakeDB):
```python
from middleware.auth import CurrentUser
from routers import integrations as integrations_router
from tests.smoke.fake_supabase import FakeDB  # rich FakeDB (select/update/delete/maybe_single/in_/eq)
USER = CurrentUser(user_id="user-a-1", hotel_id="hotel-a", role="gm", email="gm@example.com")
# RBAC: pull role_check off the route object
route = next(r for r in integrations_router.router.routes if r.path == "/opera/sync" and "POST" in r.methods)
role_check = route.dependant.dependencies[0].call
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Add pilot-flag migration and apply it to the live DB</name>
  <files>supabase/migrations/085_opera_pilot_flag.sql</files>
  <read_first>
    - supabase/migrations/084_guest_phone_adr_and_retention.sql (ADD COLUMN IF NOT EXISTS + COMMENT + rollback-comment style to copy)
    - supabase/migrations/002_tenants.sql (tenants.is_active boolean idiom this mirrors; confirm table is public.tenants)
    - .planning/STATE.md (Phase 3 section: "Migration 080 applied to production via Supabase MCP" — the established apply convention)
  </read_first>
  <action>
    Create `supabase/migrations/085_opera_pilot_flag.sql` with exactly:
    ```sql
    ALTER TABLE public.tenants ADD COLUMN IF NOT EXISTS opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE;
    COMMENT ON COLUMN public.tenants.opera_pilot_enabled IS
      'Gates Opera Cloud/OHIP connect, sync, and webhook processing to explicitly enrolled pilot hotels (Phase 6 D-03).';

    -- ROLLBACK:
    -- ALTER TABLE public.tenants DROP COLUMN opera_pilot_enabled;
    ```
    Then [BLOCKING] APPLY it to the live Supabase project using the established project convention — the Supabase MCP tool `mcp__plugin_supabase_supabase__apply_migration` (name: `opera_pilot_flag`, the SQL above). This is mandatory: TypeScript/Pydantic type checks and pytest FakeDB tests will pass WITHOUT the column existing (false-positive), but the D-06 live browser walkthrough (06-05) and the runtime `.eq`/select on `opera_pilot_enabled` require the real column. Do not proceed to any task that reads the column live until this apply succeeds.

    After applying, verify via the Supabase MCP (list_tables or a `SELECT column_name FROM information_schema.columns WHERE table_name='tenants' AND column_name='opera_pilot_enabled'`) that the column exists with default FALSE.
    Note: the existing pilot hotel(s) will need `opera_pilot_enabled=TRUE` set manually before their Opera integration works again — flag this in the SUMMARY as a required enrollment step (do NOT auto-enable all hotels; DEFAULT FALSE is intentional per D-03).
  </action>
  <verify>
    <automated>test -f supabase/migrations/085_opera_pilot_flag.sql && grep -q "opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE" supabase/migrations/085_opera_pilot_flag.sql && echo "migration file OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/085_opera_pilot_flag.sql` exists
    - `grep -q "opera_pilot_enabled BOOLEAN NOT NULL DEFAULT FALSE" supabase/migrations/085_opera_pilot_flag.sql` succeeds
    - `grep -q "ROLLBACK" supabase/migrations/085_opera_pilot_flag.sql` succeeds
    - Migration applied to live DB via Supabase MCP; a information_schema query confirms `tenants.opera_pilot_enabled` exists (column_default = false)
    - SUMMARY notes the manual per-hotel enrollment step (set TRUE for pilot hotels)
  </acceptance_criteria>
  <done>Migration 085 written in the 084 style and applied to the live Supabase DB; column verified present with DEFAULT FALSE; enrollment note recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED Opera routes RBAC + tenant-isolation + pilot-gate tests</name>
  <files>apps/api/tests/smoke/test_opera_routes.py, apps/api/tests/test_opera_pilot_gate.py</files>
  <read_first>
    - apps/api/tests/smoke/test_integrations_security.py (existing Opera test fixtures — extend, do not duplicate; opera_connect encryption + safe-error tests already here)
    - apps/api/tests/smoke/fake_supabase.py (rich FakeDB for the 6 non-connect endpoints needing select/update/delete/maybe_single/in_)
    - apps/api/routers/integrations.py (all 7 handlers — exact function names, RBAC decorators per the interfaces map, tenant-scoping .eq calls)
    - apps/api/models/requests.py (OperaConnectRequest, ResolveOperaSyncConflictRequest shapes)
    - 06-PATTERNS.md §test_opera_routes.py and §test_opera_pilot_gate.py sections
  </read_first>
  <behavior>
    - RBAC matrix per endpoint: wrong-role CurrentUser → role_check raises HTTPException("not authorized"); allowed roles pass. Cover all 7 endpoints per the interfaces RBAC map (connect/sync/test/disconnect=gm-only; conflicts list+resolve=gm/chief_engineer; status=any authenticated role passes).
    - Tenant isolation: rows created for hotel-a; handler invoked with CurrentUser(hotel_id="hotel-b"); assert empty/404 result and zero writes to hotel-a's rows (db.updates/db.upserts untouched for the other tenant).
    - Pilot gate (test_opera_pilot_gate.py): a hotel with opera_pilot_enabled=FALSE → each of the 7 handlers raises 403; a hotel with opera_pilot_enabled=TRUE → handler proceeds (no pilot-gate 403; normal RBAC still applies).
  </behavior>
  <action>
    Create two files using the direct-async-handler-invocation pattern (NOT TestClient), importing `from tests.smoke.fake_supabase import FakeDB` and `from routers import integrations as integrations_router`.

    `apps/api/tests/smoke/test_opera_routes.py`:
    - For each of the 7 endpoints, add an RBAC test: pull `role_check = route.dependant.dependencies[0].call` off the matching route and assert `pytest.raises(HTTPException, match="not authorized")` for a disallowed role, and no raise for an allowed role. Use the exact RBAC map from the interfaces block.
    - Add tenant-isolation tests for `opera_status`, `list_opera_sync_conflicts`, and `resolve_opera_sync_conflict`: seed FakeDB rows under `tenant_id="hotel-a"`, invoke the handler with `CurrentUser(hotel_id="hotel-b", role="gm", ...)`, assert the result is empty/not-connected/404-shaped and that hotel-a's rows are unchanged (zero cross-tenant writes).
    - Reuse the bug-449 None-guard idiom in assertions; use FakeDB's real bare-None maybe_single (no `SimpleNamespace(data=None)`).

    `apps/api/tests/test_opera_pilot_gate.py`:
    - Seed a `tenants` row with `opera_pilot_enabled=False` for hotel-a; invoke each of the 7 handlers with a gm/chief_engineer CurrentUser (role appropriate per endpoint) and assert an HTTP 403 is raised with a pilot-related detail (e.g. match "pilot").
    - Seed `opera_pilot_enabled=True` and assert the handler does NOT raise the pilot 403 (it may still fail later for missing creds — that's fine; assert specifically that the pilot-gate 403 is absent).

    Run both files; confirm the pilot-gate tests FAIL (no gate exists yet — RED) and the RBAC tests reflect current reality (they should largely pass since RBAC decorators already exist; any tenant-isolation gaps that fail are real findings to fix). Commit as RED.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/smoke/test_opera_routes.py tests/test_opera_pilot_gate.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; `grep -c "def test_" apps/api/tests/smoke/test_opera_routes.py` >= 7 and `grep -c "def test_" apps/api/tests/test_opera_pilot_gate.py` >= 2
    - `grep -q "opera_pilot" apps/api/tests/test_opera_pilot_gate.py` succeeds
    - `grep -c "SimpleNamespace(data=None)" apps/api/tests/smoke/test_opera_routes.py` returns 0 (uses real FakeDB bare-None)
    - Pytest run shows the pilot-gate tests FAILING (RED — no gate implemented yet), not import/collection errors
  </acceptance_criteria>
  <done>test_opera_routes.py (RBAC + tenant isolation for all 7 endpoints) and test_opera_pilot_gate.py exist; pilot-gate assertions are RED against current ungated code.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the pilot flag on all 7 Opera endpoints (GREEN)</name>
  <files>apps/api/routers/integrations.py</files>
  <read_first>
    - apps/api/routers/integrations.py (full file — all 7 handler signatures, existing require_role decorators, the current_user param, the opera_status None-guard idiom to copy for the pilot lookup)
    - apps/api/middleware/auth.py (CurrentUser shape, require_role helper — to see how dependencies compose)
    - 06-RESEARCH.md Pattern 3 (single-boolean guard recommendation) and the anti-pattern "Do NOT add pilot-gating to ai_copilot.py"
  </read_first>
  <action>
    Add a single reusable pilot-flag guard and call it at the top of every Opera handler (after the existing RBAC dependency runs). Concretely:

    1. Add a helper in `integrations.py` (or a small shared dependency), e.g.:
       ```python
       def _require_opera_pilot(current_user: CurrentUser) -> None:
           result = supabase.table("tenants").select("opera_pilot_enabled") \
               .eq("id", current_user.hotel_id).maybe_single().execute()
           if not result or not result.data or not result.data.get("opera_pilot_enabled"):
               raise HTTPException(status_code=403, detail="Opera pilot not enabled for this hotel")
       ```
       Use the bug-449-safe `if not result or not result.data` guard (never `if result.data:` alone). Confirm the correct tenants PK column name by reading 002_tenants.sql (`id` vs `tenant_id`) — match whatever `current_user.hotel_id` keys against elsewhere in this file.
    2. Call `_require_opera_pilot(current_user)` at the start of ALL 7 handlers: `opera_connect`, `opera_status`, `opera_sync`, `list_opera_sync_conflicts`, `resolve_opera_sync_conflict`, `opera_test`, `opera_disconnect`. (Yes, including `opera_status` — a non-pilot hotel should not probe Opera connection state either.)
    3. Do NOT touch `ai_copilot.py` — D-04 explicitly keeps AI copilot ungated.
    4. Re-run Task 2's tests: pilot-gate tests GREEN (403 when disabled, pass-through when enabled); RBAC/tenant tests still green.

    Non-regression: run the full suite. The Opera cron sync (`/v1/internal/opera/sync-reservations`) also drives `sync_reservations` — verify the cron path either goes through the same gate or is intentionally out of scope (the cron already only syncs hotels with connected credentials; note in SUMMARY whether cron respects the pilot flag — if it iterates connected hotels, add the pilot check there too, or document why not).
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_opera_pilot_gate.py tests/smoke/test_opera_routes.py -x 2>&1 | tail -20 && python -m pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_require_opera_pilot\|opera_pilot_enabled" apps/api/routers/integrations.py` shows the guard defined once and called in all 7 handlers (>= 8 occurrences)
    - `grep -q "opera_pilot" apps/api/routers/ai_copilot.py` returns NOTHING (AI copilot stays ungated per D-04)
    - Pilot lookup uses the bug-449 guard: `grep -q "if not result or not result.data" apps/api/routers/integrations.py` succeeds
    - `test_opera_pilot_gate.py` and `test_opera_routes.py` PASS
    - Full suite `python -m pytest tests/ -q` shows zero failures
  </acceptance_criteria>
  <done>Every Opera endpoint checks tenants.opera_pilot_enabled and 403s when disabled; ai_copilot.py untouched; pilot + routes tests GREEN; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (GM/staff) → /integrations/opera/* | Authenticated request crosses into an external-system integration control surface |
| API → external Oracle OHIP | Only pilot-enrolled hotels should reach this boundary |
| API → opera_credentials / integration_sync_conflicts (DB) | Per-tenant secrets + conflict data; must be tenant-isolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | Ungated Opera access (no pilot flag exists today) | mitigate | Add tenants.opera_pilot_enabled + 403 guard on all 7 endpoints (Tasks 1,3). Non-enrolled hotels can no longer connect/sync/probe Opera. |
| T-06-06 | Information Disclosure | Cross-tenant Opera credential/conflict read | mitigate | Tenant-isolation tests assert cross-hotel reads return 404/empty + zero writes (Task 2); existing `.eq("tenant_id", hotel_id)` filters verified by test, not assumed. |
| T-06-07 | Elevation of Privilege | Missing per-role gate on Opera mutations | mitigate | RBAC matrix test proves connect/sync/test/disconnect are gm-only, conflicts gm/chief_engineer, status any-auth (Task 2). |
| T-06-08 | Spoofing | Forged pilot state via tenants PK confusion | mitigate | Guard keys on the same `current_user.hotel_id` used for all other tenant scoping; confirmed against 002_tenants.sql PK column (Task 3). |
| T-06-09 | Info Disclosure | Opera error leaking OHIP creds | accept | Already correct — `test_opera_connect_returns_safe_error_for_unreachable_ohip` proves connection errors are not leaked (06-RESEARCH Security Domain). No change. |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/smoke/test_opera_routes.py tests/test_opera_pilot_gate.py -x` — green
- `cd apps/api && python -m pytest tests/ -q` — full suite green
- Supabase MCP: `tenants.opera_pilot_enabled` column present in live DB
- `grep opera_pilot apps/api/routers/ai_copilot.py` — no matches (D-04 respected)
</verification>

<success_criteria>
- tenants.opera_pilot_enabled exists in the live DB (DEFAULT FALSE)
- All 7 Opera endpoints 403 for non-pilot hotels; pass-through for pilot hotels
- Per-role RBAC + cross-tenant isolation proven for all 7 endpoints
- AI copilot remains ungated (D-04)
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-pms-and-ai-expansion/06-02-SUMMARY.md`
</output>
