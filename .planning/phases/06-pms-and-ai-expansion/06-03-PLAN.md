---
phase: 06-pms-and-ai-expansion
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/api/tests/test_ai_copilot_rbac.py
  - apps/api/routers/ai_copilot.py
  - apps/api/models/requests.py
autonomous: true
requirements: [D-01, D-05, D-06]
tags: [ai, rbac, tenant-isolation, input-validation, fastapi]

must_haves:
  truths:
    - "Role-gated AI endpoints (/ai/insights|/ai/recommendations|/ai/recommendations/metrics) 403 for housekeeper/engineer/front_desk and pass for gm/chief_engineer/housekeeping_supervisor"
    - "Intentionally-open endpoints (chat, task/work-order/guest-request confirm) accept any authenticated role (matching sibling non-AI endpoints)"
    - "/ai/assignments/confirm remains gm/housekeeping_supervisor/engineer only (housekeeper excluded)"
    - "confirm_tasks accepts a typed Pydantic model, returning 422 (not 500) on malformed input"
    - "Cross-tenant confirm writes are scoped to current_user.hotel_id (server-resolved, not client-trusted)"
  artifacts:
    - path: "apps/api/tests/test_ai_copilot_rbac.py"
      provides: "per-role 403/200 matrix + tenant-isolation for ai_copilot endpoints"
      contains: "def test_"
    - path: "apps/api/models/requests.py"
      provides: "typed TaskPreview model for confirm_tasks"
      contains: "class TaskPreview"
  key_links:
    - from: "apps/api/routers/ai_copilot.py::confirm_tasks"
      to: "apps/api/models/requests.py::TaskPreview"
      via: "typed list[TaskPreview] param replacing raw list[dict]"
      pattern: "list\\[TaskPreview\\]"
    - from: "apps/api/routers/ai_copilot.py confirm handlers"
      to: "tasks/work_orders/guest_requests tables"
      via: "insert scoped to current_user.hotel_id"
      pattern: "current_user.hotel_id"
---

<objective>
Close the AI copilot RBAC / tenant-isolation test gap (zero coverage today) and fix the one robustness defect research found, tests-first.

1. **RBAC + tenant isolation coverage (D-01/D-06).** New `test_ai_copilot_rbac.py`: a per-role 403/200 matrix for every `ai_copilot.py` endpoint, plus cross-tenant confirm-write isolation. This encodes the CORRECT contract, distinguishing genuinely role-gated endpoints from intentionally-open ones (research Pitfall 6 confirmed `confirm_work_orders`/`confirm_guest_requests`/`confirm_tasks` are open BY DESIGN — their sibling non-AI create endpoints are also open; do NOT "fix" these into role restrictions, that would be a regression).
2. **confirm_tasks robustness (Pitfall 5, D-05).** `confirm_tasks` currently takes a raw `list[dict]` while its three siblings take typed `SanitizedBaseModel` previews; `task["title"]` (no `.get()`) raises an uncaught `KeyError` → 500 on malformed input. Give it a typed `TaskPreview` model so malformed input returns 422 and length/sanitization limits apply — matching `WorkOrderPreview`/`GuestRequestPreview`/`AssignmentPreview`.

Purpose: Bring the AI copilot RBAC/tenant/input-validation surface to Phase 1–5 rigor (D-01/D-06) and fix the confirm_tasks consistency gap in-place (D-05).
Output: new `test_ai_copilot_rbac.py`, a `TaskPreview` model, typed `confirm_tasks`.

Depends on 06-01 (shares apps/api/routers/ai_copilot.py — must land after the credit/double-log edits to avoid conflicts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pms-and-ai-expansion/06-CONTEXT.md
@.planning/phases/06-pms-and-ai-expansion/06-RESEARCH.md
@.planning/phases/06-pms-and-ai-expansion/06-PATTERNS.md
@.planning/phases/06-pms-and-ai-expansion/06-01-SUMMARY.md

<interfaces>
<!-- Verified in 06-PATTERNS.md — RBAC classification for ai_copilot.py endpoints. -->

ROLE-GATED (require_role("gm","chief_engineer","housekeeping_supervisor"), ai_copilot.py ~lines 688-711):
- /ai/insights (POST insight variants), /ai/recommendations, /ai/recommendations/metrics
  → housekeeper/engineer/front_desk MUST 403; gm/chief_engineer/housekeeping_supervisor pass.

INTENTIONALLY OPEN (get_current_user only — any authenticated role — NOT a bug per Pitfall 6):
- /ai/copilot/chat, /ai/tasks/confirm, /ai/work-orders/confirm, /ai/guest-requests/confirm, /ai/risk-alerts
- /ai/insights (GET) uses only get_current_user — research flagged as "verify intentional"; treat current behavior as the contract unless the endpoint clearly mutates/leaks cross-tenant data (it reads current_user.hotel_id-scoped data → keep open, assert it stays open).

ROLE-GATED assignment:
- /ai/assignments/confirm → require_role("housekeeping_supervisor", "engineer", "gm")  (housekeeper excluded)

Pitfall 5 target — confirm_tasks takes raw list[dict]; siblings take typed models (models/requests.py):
```python
class WorkOrderPreview(SanitizedBaseModel): ...
class GuestRequestPreview(SanitizedBaseModel): ...
class AssignmentPreview(SanitizedBaseModel): ...
# confirm_tasks currently: async def confirm_tasks(tasks: list[dict], current_user=Depends(get_current_user))
```

RBAC extraction pattern (test_evidence_foundation.py):
```python
route = next(r for r in ai_copilot.router.routes if r.path == "/ai/recommendations" and "GET" in r.methods)
role_check = route.dependant.dependencies[0].call
with pytest.raises(HTTPException, match="not authorized"):
    await role_check(CurrentUser(user_id="s1", hotel_id="h1", role="housekeeper"))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write AI copilot RBAC matrix + tenant-isolation + confirm_tasks-422 tests (RED for 422)</name>
  <files>apps/api/tests/test_ai_copilot_rbac.py</files>
  <read_first>
    - apps/api/routers/ai_copilot.py (every route decorator — which use require_role vs get_current_user; exact route paths + methods; confirm_tasks signature)
    - apps/api/tests/test_evidence_foundation.py (RBAC role_check extraction pattern)
    - apps/api/tests/smoke/test_ai_assignment_confirm.py (FakeDB + CurrentUser fixtures + confirm_assignments SUPERVISOR role)
    - apps/api/tests/smoke/fake_supabase.py (rich FakeDB)
    - apps/api/models/requests.py (SanitizedBaseModel + the three existing Preview models)
    - 06-PATTERNS.md §test_ai_copilot_rbac.py (endpoint classification, exact lines)
  </read_first>
  <behavior>
    - Role-gated matrix: for /ai/insights (role-gated variant), /ai/recommendations, /ai/recommendations/metrics — housekeeper, engineer, front_desk raise HTTPException("not authorized"); gm, chief_engineer, housekeeping_supervisor pass the role_check.
    - Open endpoints: /ai/copilot/chat, /ai/tasks/confirm, /ai/work-orders/confirm, /ai/guest-requests/confirm, /ai/risk-alerts accept any authenticated role (no require_role dependency on the route — assert the route has no role gate, i.e. these do NOT raise for a housekeeper).
    - Assignment gate: /ai/assignments/confirm raises for housekeeper; passes for housekeeping_supervisor/engineer/gm.
    - Tenant isolation: a confirm handler invoked with CurrentUser(hotel_id="hotel-b") inserts rows scoped to hotel-b only; a seeded hotel-a resource (e.g. room) referenced by a hotel-b user does not leak / is not written under hotel-a.
    - confirm_tasks input validation (RED until Task 2): a malformed tasks payload (missing "title") to confirm_tasks currently raises KeyError/500; after Task 2 it must raise a 422-style validation error. Write this test to assert the post-fix 422 behavior (RED now).
  </behavior>
  <action>
    Create `apps/api/tests/test_ai_copilot_rbac.py` using the direct-invocation + route-object role_check extraction pattern (NOT TestClient). Import `from routers import ai_copilot`, `from middleware.auth import CurrentUser`, `from tests.smoke.fake_supabase import FakeDB`, and the relevant Preview models.

    Write:
    - `test_rbac_matrix_role_gated_endpoints`: parametrize over (/ai/insights role-gated, /ai/recommendations, /ai/recommendations/metrics) × (denied roles, allowed roles); pull role_check off each route object and assert raise/no-raise.
    - `test_open_endpoints_accept_any_role`: assert /ai/copilot/chat, /ai/tasks/confirm, /ai/work-orders/confirm, /ai/guest-requests/confirm, /ai/risk-alerts routes have NO require_role dependency (inspect `route.dependant.dependencies` — assert none is the require_role gate) — encoding Pitfall 6's "these are open by design".
    - `test_assignments_confirm_excludes_housekeeper`: role_check for /ai/assignments/confirm raises for housekeeper, passes for housekeeping_supervisor.
    - `test_confirm_tasks_tenant_scoped`: FakeDB, monkeypatch ai_copilot.supabase, invoke confirm_tasks with a hotel-b user and assert inserted task rows carry hotel_id="hotel-b" and nothing is written under hotel-a.
    - `test_confirm_tasks_malformed_returns_422`: build a payload missing required fields; assert a pydantic ValidationError / 422 is raised (RED now — currently 500/KeyError).

    Run; confirm the 422 test is RED and (likely) the rest reflect current reality. Commit RED.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_ai_copilot_rbac.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "def test_" apps/api/tests/test_ai_copilot_rbac.py` >= 5
    - `grep -q "not authorized" apps/api/tests/test_ai_copilot_rbac.py` succeeds (RBAC assertions present)
    - `grep -q "422\|ValidationError" apps/api/tests/test_ai_copilot_rbac.py` succeeds
    - `grep -c "SimpleNamespace(data=None)" apps/api/tests/test_ai_copilot_rbac.py` returns 0
    - Pytest shows `test_confirm_tasks_malformed_returns_422` FAILING (RED); no import/collection errors
  </acceptance_criteria>
  <done>test_ai_copilot_rbac.py encodes the full role matrix + tenant isolation + a RED confirm_tasks-422 test.</done>
</task>

<task type="auto">
  <name>Task 2: Give confirm_tasks a typed TaskPreview model (GREEN)</name>
  <files>apps/api/models/requests.py, apps/api/routers/ai_copilot.py</files>
  <read_first>
    - apps/api/models/requests.py (SanitizedBaseModel base + WorkOrderPreview/GuestRequestPreview/AssignmentPreview — copy their field/validator style exactly)
    - apps/api/routers/ai_copilot.py (confirm_tasks handler — how it reads task["title"] and inserts into tasks; the AICopilotBubble preview payload shape it must accept)
    - apps/web/components/ai/AICopilotBubble.tsx (what fields the frontend sends for a task confirm — title, and any description/priority/due — so TaskPreview matches the wire contract)
    - apps/web/lib/api/ai.ts (the confirm-tasks client call payload)
  </read_first>
  <action>
    1. In `apps/api/models/requests.py`, add `class TaskPreview(SanitizedBaseModel)` mirroring the field style of `WorkOrderPreview`/`GuestRequestPreview`. Include exactly the fields `confirm_tasks` currently reads off each dict (at minimum `title: str`; add `description`, `priority`, `due_date`/`assigned_to` as optional if the current handler reads them — read ai_copilot.py + AICopilotBubble.tsx to enumerate the exact keys). Apply the same length/sanitization limits SanitizedBaseModel gives the siblings so overly long titles are rejected.
    2. In `apps/api/routers/ai_copilot.py`, change `confirm_tasks` signature from `tasks: list[dict]` to `tasks: list[TaskPreview]` (import TaskPreview), and update the body to read attributes (`t.title`) instead of dict keys (`task["title"]`). Preserve the existing tenant-scoping (insert with `hotel_id=current_user.hotel_id`) and the existing response shape `{"data": {...}}`.
    3. Do NOT add a role gate to confirm_tasks — it stays open per Pitfall 6 (its sibling non-AI create endpoints are open).
    4. Re-run Task 1's tests — `test_confirm_tasks_malformed_returns_422` GREEN; all RBAC/tenant tests green.

    Non-regression: the web AICopilotBubble task-confirm flow must still work with the new typed model — confirm the field names match the frontend payload exactly (a mismatch would 422 valid requests). Run the full suite.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_ai_copilot_rbac.py -x 2>&1 | tail -15 && python -m pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class TaskPreview" apps/api/models/requests.py` succeeds and it subclasses SanitizedBaseModel
    - `grep -q "list\[TaskPreview\]" apps/api/routers/ai_copilot.py` succeeds
    - `grep -q "task\[\"title\"\]\|task\['title'\]" apps/api/routers/ai_copilot.py` returns NOTHING (no raw dict access remains in confirm_tasks)
    - confirm_tasks still open (no require_role added): the /ai/tasks/confirm route has no role dependency
    - `test_confirm_tasks_malformed_returns_422` and all RBAC/tenant tests PASS
    - Full suite `python -m pytest tests/ -q` zero failures
  </acceptance_criteria>
  <done>confirm_tasks uses a typed TaskPreview list, returns 422 on malformed input, stays tenant-scoped and open-by-design; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /ai/* role-gated endpoints | GM-insight/recommendation surfaces must exclude floor roles |
| client → /ai/*/confirm | Any authenticated staff may create tasks/WOs/requests (by design); input must be validated |
| API → tasks/work_orders/guest_requests (DB) | Confirm writes must be scoped to the caller's own hotel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | Role gate on /ai/insights, /ai/recommendations, /ai/recommendations/metrics | mitigate | RBAC matrix test proves floor roles 403 and management roles pass (Task 1). Encodes the gate as a tested contract, closing the zero-coverage gap. |
| T-06-11 | Information Disclosure | Cross-tenant confirm write | mitigate | Tenant-isolation test asserts confirm inserts are scoped to current_user.hotel_id, server-resolved not client-trusted (Task 1). |
| T-06-12 | Denial of Service / Tampering | confirm_tasks untyped list[dict] → 500 on KeyError, no length limits | mitigate | Typed TaskPreview (SanitizedBaseModel) → 422 on malformed input + sanitization limits (Task 2). |
| T-06-13 | Elevation of Privilege | Over-restricting open confirm endpoints (regression risk) | accept | Pitfall 6: confirm_work_orders/guest_requests/tasks are open by design (sibling create endpoints are open). Test asserts they STAY open — deliberately not gated. |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/test_ai_copilot_rbac.py -x` — green
- `cd apps/api && python -m pytest tests/ -q` — full suite green
- `grep "task\[\"title\"\]" apps/api/routers/ai_copilot.py` — no raw dict access remains
</verification>

<success_criteria>
- Full per-role RBAC matrix + tenant isolation proven for ai_copilot endpoints
- Role-gated endpoints 403 floor roles; open endpoints stay open (Pitfall 6 respected)
- confirm_tasks typed, returns 422 on malformed input
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-pms-and-ai-expansion/06-03-SUMMARY.md`
</output>
