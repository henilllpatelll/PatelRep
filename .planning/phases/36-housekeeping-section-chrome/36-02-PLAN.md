---
phase: 36-housekeeping-section-chrome
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/web/app/(dashboard)/housekeeping/page.tsx
autonomous: true

# NOTE: this plan owns the ENTIRE 791-line housekeeping/page.tsx file (SyncBadge, HousekeeperBar,
# HousekeeperRoomItem, HousekeeperMyRoomsView, SupervisorHousekeepingPage, and the default-exported
# HousekeepingPage router) — do NOT split this file across two plans, per 36-RESEARCH.md's explicit
# recommendation (a same-file split risks a merge-conflict-prone parallel edit; this single file is
# small enough at ~800 LOC to be one plan). 36-03 (parallel, same wave) owns AssignmentSidebar.tsx
# and PredictionPanel.tsx instead — zero file overlap with this plan.
#
# FROZEN — DO NOT TOUCH: apps/web/components/housekeeping/RoomCard.tsx, RoomStatusBoard.tsx,
# RoomDetailDrawer.tsx (content-hash-frozen in apps/web/frozen-files.json). This plan imports and
# renders <RoomStatusBoard /> and <RoomDetailDrawer /> exactly as today — same props, same
# Suspense wrapper with no fallback, zero wrapping card/Skeleton added around either. Their frozen
# hashes MUST NOT change (they won't, since this plan never opens those 3 files).
#
# Realtime subscription logic (the hk_my_rooms_realtime channel + its postgres_changes listeners in
# HousekeeperMyRoomsView) is NOT touched — only the SyncBadge chrome and the myRooms board query's
# loading/error rendering change. RBAC (canAssignRooms gating, the no-access role check) is NOT
# touched — condition logic stays byte-identical, only the rendered markup changes.

must_haves:
  truths:
    - "With the 'housekeeping' flag ON, the role-loading guard, SyncBadge-bearing PageHeader, date/shift controls, HousekeeperBar chip bar, HousekeeperRoomItem list rows, and the my-rooms/no-access views all render in the v2 visual system (UI-SPEC's declared 4-size/2-weight typography, token-based color, StateBlock/Skeleton/EmptyState primitives replacing every bespoke animate-pulse/centered-text block); with the flag OFF, every one of those renders exactly as it did before this phase"
    - "HousekeeperMyRoomsView's board query and HousekeeperBar's staff-list query each show a StateBlock error state (wired to the query's own refetch) on fetch failure instead of silently rendering the wrong empty-state copy — but a BACKGROUND refetch failure with already-loaded data does NOT flash an error over the existing list (gated on isError && no-data-ever-loaded, not isError alone)"
    - "RoomStatusBoard and RoomDetailDrawer are rendered with identical props/wrapper markup to before this plan — zero diff in how this file invokes them"
    - "canAssignRooms-gated visibility (assign-mode toggle, HousekeeperBar, AssignmentSidebar) and the housekeeper/gm/housekeeping_supervisor/front_desk role-routing logic are byte-identical in condition, only the rendered markup inside each branch changes"
  artifacts:
    - path: "apps/web/app/(dashboard)/housekeeping/page.tsx"
      provides: "v2-gated chrome: flag read via isSectionRedesigned('housekeeping', hotel), StateBlock/Skeleton/EmptyState wired into HousekeeperMyRoomsView + HousekeeperBar + the role-loading guard + the no-access branch, PageHeader dataI18nSkip={v2} on both PageHeader call sites"
      contains: "isSectionRedesigned('housekeeping', hotel)"
  key_links:
    - from: "apps/web/app/(dashboard)/housekeeping/page.tsx"
      to: "@/stores/hotelStore + @/lib/utils/redesignFlag"
      via: "const v2 = isSectionRedesigned('housekeeping', hotel)"
      pattern: "isSectionRedesigned\\('housekeeping'"
    - from: "HousekeeperMyRoomsView's board useQuery"
      to: "StateBlock status prop"
      via: "isError && !boardData gating (Pitfall 3 — background refetchInterval failures must not flash an error over stale data)"
      pattern: "isError.*boardData|refetch"
    - from: "HousekeeperBar's staff-list useQuery"
      to: "StateBlock status prop"
      via: "isError destructured + wired to refetch, replacing the empty-staff-copy-on-failure gap"
      pattern: "isError.*refetch|staff-list"
    - from: "router's v2 const (SupervisorHousekeepingPage / HousekeeperMyRoomsView)"
      to: "<HousekeeperBar v2={v2} /> (line ~742) and <HousekeeperRoomItem ... v2={v2} /> (line ~606) call sites"
      via: "explicit prop passed at each JSX invocation — not left to default, otherwise the v2 styling added to both components never activates even with the flag on"
      pattern: "<HousekeeperBar v2=\\{v2\\}|<HousekeeperRoomItem[\\s\\S]*?v2=\\{v2\\}"
---

<objective>
Restyle `apps/web/app/(dashboard)/housekeeping/page.tsx` (all six components it contains: `SyncBadge`, `HousekeeperBar`, `HousekeeperRoomItem`, `HousekeeperMyRoomsView`, `SupervisorHousekeepingPage`, and the default-exported `HousekeepingPage` role router) onto the v2 visual system behind a net-new `housekeeping` flag, while `RoomStatusBoard`/`RoomDetailDrawer` render bare and completely untouched inside the new chrome. Close two genuine, confirmed UX gaps: `HousekeeperMyRoomsView`'s board query and `HousekeeperBar`'s staff-list query both currently have zero error UI (a fetch failure silently renders the wrong empty-state copy) — wire both to `StateBlock` error states using the two i18n keys 36-01 added. Convert every bespoke `animate-pulse`/centered-text block (the role-loading guard's 3 skeleton divs, `HousekeeperBar`'s chip-loading skeleton, `HousekeeperMyRoomsView`'s 3-row skeleton list and its empty/no-access states) to the shared `Skeleton`/`StateBlock`/`EmptyState` primitives established in Phase 30.

Purpose: deliver 3 of ROADMAP HSK-01's 4 named chrome elements (PageHeader, sync badge, date/shift controls, housekeeper "my rooms" list all live in this one file) plus the two loading/error gap closures, without touching a single line inside the frozen board trio.
Output: `housekeeping/page.tsx` renders the v2 redesign behind `isSectionRedesigned('housekeeping', hotel)`, flag-off renders today's UI unchanged, both confirmed query gaps closed with retry-able error states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-section-chrome/36-CONTEXT.md
@.planning/phases/36-housekeeping-section-chrome/36-UI-SPEC.md
@.planning/phases/36-housekeeping-section-chrome/36-RESEARCH.md
@.planning/phases/36-housekeeping-section-chrome/36-01-SUMMARY.md

# The file this plan restyles in full (all 6 named components + the default export)
@apps/web/app/(dashboard)/housekeeping/page.tsx

# Shared primitives to wire in (already exist, no changes needed to any of these three)
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/Skeleton.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
@apps/web/lib/utils/redesignFlag.ts

# Reference: the exact flag-read + dataI18nSkip pattern used by every prior v2 page (confirmed
# working precedent, copy this shape exactly — do not invent a different flag-read location)
@apps/web/app/(dashboard)/engineering/predictions/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the flag, restyle the role router + HousekeeperBar + HousekeeperRoomItem to v2 tokens, close the staff-list error gap</name>
  <files>apps/web/app/(dashboard)/housekeeping/page.tsx</files>
  <action>
1. Add net-new imports: `import { useHotelStore } from '@/stores/hotelStore'` and `import { isSectionRedesigned } from '@/lib/utils/redesignFlag'`, plus `StateBlock` from `@/components/ui/StateBlock`, `Skeleton` from `@/components/ui/Skeleton`, and `EmptyState` from `@/components/ui/EmptyState`.

2. In the default-exported `HousekeepingPage` (line ~763): read `const hotel = useHotelStore((s) => s.hotel)` and `const v2 = isSectionRedesigned('housekeeping', hotel)`. Convert the `isAuthLoading || !role` guard's three bespoke `animate-pulse` divs (lines 771-773) to `Skeleton` when `v2` is true (`<Skeleton className="h-8 w-56" />`, `<Skeleton variant="card" className="h-24" />`, `<Skeleton variant="card" className="h-72" />` — `variant="card"` already applies `rounded-[var(--r-lg)]`, override height via `className`), keep the exact bespoke markup when `v2` is false. Thread `v2` as a prop into `HousekeeperMyRoomsView` and `SupervisorHousekeepingPage` (e.g. `<HousekeeperMyRoomsView v2={v2} />`) since they are structurally independent views — do not drill `v2` through more than one prop hop. Convert the no-access branch (lines 782-788, `role !== 'gm' && ...`) to use `EmptyState` when `v2` (e.g. `<EmptyState title={t('housekeeping.page.noAccess')} />` inside a centered wrapper), keep the bespoke centered `<p>` when `v2` is false.

3. In `HousekeeperBar` (line ~75): accept a `v2: boolean` prop. Destructure `isError` and `refetch` from the staff-list `useQuery` (line 92) alongside the existing `data`/`isLoading`. Render: when `v2` and `isLoading`, use `Skeleton` (`className="w-24 h-9"` × 4, matching today's bespoke chip-skeleton row) instead of the bespoke `animate-pulse` divs (lines 181-184); when `v2` and `isError`, render a `StateBlock status="error"` block using `error={{ message: t('housekeeping.page.assignBar.loadError'), onRetry: refetch }}` in place of the `housekeepers.length === 0` branch (lines 186-190) — this closes the confirmed gap where a fetch failure today silently shows "No housekeeper staff found"; when `v2` is false, keep every branch exactly as today (including the `isError` still being read is fine, it's just not rendered in the legacy branch). The genuinely-empty (`isError` false, `housekeepers.length === 0`) case keeps rendering today's "No staff / Add staff" copy in both flag states — only fetch FAILURE gets the new error branch.

   **Call-site wiring (do this explicitly — the prop change in step 3 has no effect until the invocation passes it):** in `SupervisorHousekeepingPage`, update the `<HousekeeperBar />` call site at line ~742 (`{assignmentMode && canAssignRooms && <HousekeeperBar />}`) to `{assignmentMode && canAssignRooms && <HousekeeperBar v2={v2} />}`. `SupervisorHousekeepingPage` receives `v2` as its own prop from step 2's threading (`<SupervisorHousekeepingPage v2={v2} />`) — pass that same `v2` value through, do not re-derive it.

4. `HousekeeperRoomItem`: apply the UI-SPEC's typography table to this component's text sizing when the parent passes `v2` down as a prop (thread it through from `HousekeeperMyRoomsView`) — status pill / clean-type / note text at 12px regular (`text-xs font-normal`, dropping any `font-medium`), room number stays `font-mono font-semibold text-base` (unaffected, not in the phase's declared 4-size table since it's the card's own primary identifier, not label/body/heading/stat-numeral text). Keep every `--alert-soft`/`--caution-soft`/etc. room-status token usage in `statusConfig` byte-identical (frozen value tokens, shared with `RoomCard`) — only adjust surrounding spacing/text classes, never the status-color tokens themselves. Preserve the exact two-tap Done/Undo confirm mechanic unchanged (only touch surface tokens/spacing per UI-SPEC's Copywriting Contract, never the button flow).

   **Call-site wiring (do this explicitly):** in `HousekeeperMyRoomsView`'s room list (Task 2 owns this view's body, but this component-level prop addition happens here in Task 1 — accept the prop signature now so Task 2's call-site update type-checks), update the `<HousekeeperRoomItem ... />` call site at line ~606 to also pass `v2={v2}` (the `v2` `HousekeeperMyRoomsView` itself receives as a prop from step 2's threading), i.e. `<HousekeeperRoomItem key={room.room_id} room={room} onAction={handleAction} onUndo={handleUndo} onOpenDetail={setSelectedRoom} v2={v2} />`. This call site lives inside `HousekeeperMyRoomsView`, which Task 2 restyles — coordinate: Task 1 adds the `v2` prop to `HousekeeperRoomItem`'s signature, Task 2 (same file, sequential within this plan) wires `v2={v2}` at the line-606 call site when it touches that view's body.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `isSectionRedesigned('housekeeping', hotel)` present; grep confirms `isError` and `refetch` both destructured from the staff-list useQuery in HousekeeperBar; grep confirms `housekeeping.page.assignBar.loadError` is referenced; grep confirms `<HousekeeperBar v2={v2} />` at the line-742 call site (not bare `<HousekeeperBar />`); visually diff (via dev server) that flag-off renders identically to the pre-plan file for the role router, HousekeeperBar, and HousekeeperRoomItem</verify>
  <done>Flag read wired in the router; role-loading guard, no-access branch, HousekeeperBar loading/error, and HousekeeperRoomItem typography all v2-gated; the `<HousekeeperBar v2={v2} />` call site (line ~742) is updated to pass the prop — not left defaulting to false; HousekeeperBar's staff-list fetch-failure gap closed with a StateBlock error + retry; flag-off path byte-behavior-identical to pre-plan</done>
</task>

<task type="auto">
  <name>Task 2: Restyle HousekeeperMyRoomsView (close the board-query error gap, convert loading/empty to shared primitives) and SupervisorHousekeepingPage's PageHeader/date-shift chrome</name>
  <files>apps/web/app/(dashboard)/housekeeping/page.tsx</files>
  <action>
1. `HousekeeperMyRoomsView` (line ~465): accept the `v2: boolean` prop from Task 1's router. Destructure `isError` and `refetch` from the board `useQuery` (line 475-479) alongside `data: boardData`/`isLoading`. Because this query has `refetchInterval: 10_000`, gate the error render on `isError && !boardData` (Pitfall 3 from 36-RESEARCH.md) — a background refetch failure with already-loaded data must keep showing the stale room list, not flash an error over it. Wrap the existing loading/empty/data three-way branch (lines 592-615) in `StateBlock`:
   ```
   <StateBlock
     status={v2 ? (isLoading ? 'loading' : (isError && !boardData) ? 'error' : myRooms.length === 0 ? 'empty' : null) : undefined}
     error={{ message: t('housekeeping.page.myRooms.loadError'), onRetry: refetch }}
     empty={{ title: t('housekeeping.page.myRooms.emptyTitle'), body: t('housekeeping.page.myRooms.emptySubtitle') }}
   >
     {/* existing myRooms.map(...) list, unchanged */}
   </StateBlock>
   ```
   When `v2` is false, keep today's exact bespoke 3-branch markup (lines 592-615) untouched — do not run it through StateBlock at all in the legacy path, to guarantee pixel-identical flag-off behavior. Replace the bespoke `animate-pulse` skeleton rows (lines 594-596) with `Skeleton` (`variant="card" className="h-20"` × 3) only inside the `v2`-true path.
   **Call-site wiring (explicit — completes Task 1's `HousekeeperRoomItem` prop addition):** inside this same list (line ~606), update `<HousekeeperRoomItem key={room.room_id} room={room} onAction={handleAction} onUndo={handleUndo} onOpenDetail={setSelectedRoom} />` to add `v2={v2}` (the `v2` this view itself received as a prop from the router in Task 1 step 2). Without this, `HousekeeperRoomItem`'s new `v2` prop (added by Task 1) silently defaults to `false`/`undefined` and its v2 typography never activates even with the flag on — verify this explicitly, don't just rely on type-check to catch it (an optional/defaulted prop won't error).
   Pass `dataI18nSkip={v2}` to this view's `PageHeader` (line 582) — its `title`/`subtitle` are react-i18next content, and per 36-RESEARCH.md's bug-965/bug-1021 findings, every v2-gated PageHeader in this codebase uses this escape hatch regardless of whether the copy itself is new.
   Apply the UI-SPEC's stat-numeral role (20px `font-display` semibold) to the todo/in-progress/done counts (line 586-588, currently `font-display text-[var(--alert)]` etc. with no explicit size/weight — pin to `text-xl font-display font-semibold` or the project's existing 20px utility class, confirm against `tailwind.config.ts`) only in the `v2` path; legacy path keeps its current classes untouched.

2. `SupervisorHousekeepingPage` (line ~627): accept the `v2: boolean` prop. Pass `dataI18nSkip={v2}` to its `PageHeader` (line 684, same rationale as above — `eyebrow`/`title` are i18n content). Restyle the date-nav `Button`s and the shift `<select>` (lines 691-718) to v2 tokens (the select's `focus:ring-amber-400` at line 713 is a pre-v2 hardcoded color — replace with `focus-visible:ring-[var(--focus-ring)]` per the UI-SPEC's inherited token set, only in the `v2` path) and thread `v2` into `SyncBadge` if any of its token usage needs updating (its `--ready` semantic token is already correct per UI-SPEC's Color table — confirm no bespoke non-token color exists in `SyncBadge`, leave as-is if already token-based). Do NOT touch the `<Suspense><RoomStatusBoard /></Suspense>` block or the `AssignmentSidebar`/`PredictionPanel` render calls themselves (36-03 owns those two components' internals; this file only passes existing props to them, unchanged).
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `StateBlock` wraps the myRooms list with `isError && !boardData` (or equivalent) gating; grep confirms `dataI18nSkip={v2}` on both PageHeader call sites; grep confirms `<HousekeeperRoomItem` call site (line ~606) includes `v2={v2}` (not left as a bare invocation defaulting the prop away); grep confirms `<RoomStatusBoard />`/`<RoomDetailDrawer` call sites are unchanged (same props as pre-plan); npm run check:frozen-files still green (RoomStatusBoard.tsx/RoomDetailDrawer.tsx/RoomCard.tsx hashes unaffected since this plan never opens those files)</verify>
  <done>HousekeeperMyRoomsView's board-query fetch-failure gap closed with a stale-data-safe StateBlock error; both PageHeader call sites have dataI18nSkip={v2}; the `<HousekeeperRoomItem v2={v2} />` call site (line ~606) is updated to pass the prop; SupervisorHousekeepingPage's date/shift controls restyled to v2 tokens; RoomStatusBoard/RoomDetailDrawer render calls byte-identical to pre-plan; flag-off renders both views exactly as before</done>
</task>

</tasks>

<verification>
- `cd apps/web && npm run type-check` green.
- `npm run check:frozen-files` green — zero drift on `RoomCard.tsx`/`RoomStatusBoard.tsx`/`RoomDetailDrawer.tsx` (this plan's `files_modified` never lists them).
- With `housekeeping` flag OFF, `housekeeping/page.tsx` renders identically to its pre-plan state for both the housekeeper and supervisor/gm/front_desk views (spot-check via dev server).
- With flag ON, both confirmed gaps (staff-list fetch failure, my-rooms board fetch failure) show a `StateBlock` error with a working retry, and a background `refetchInterval` failure on the my-rooms board does NOT replace an already-rendered list with an error screen.
</verification>

<success_criteria>
`housekeeping/page.tsx`'s role-loading guard, `SyncBadge`-bearing `PageHeader`, date/shift controls, `HousekeeperBar`, `HousekeeperRoomItem`, and the my-rooms/no-access views all render in the v2 visual system behind the `housekeeping` flag, with both genuine loading/error gaps closed via shared primitives, while `RoomStatusBoard`/`RoomDetailDrawer` remain completely untouched and flag-off preserves the legacy UI exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-section-chrome/36-02-SUMMARY.md` recording: the exact flag-read location and how `v2` is threaded to the two views, the exact StateBlock wiring + stale-data gating logic used for both closed gaps (quote the actual condition), confirmation that RoomStatusBoard/RoomDetailDrawer render calls are byte-identical to pre-plan (diff or grep evidence), confirmation `check:frozen-files` passed, any typography/color values that needed a judgment call beyond the UI-SPEC's literal text, and any deviation from this plan's action text with rationale.
</output>
