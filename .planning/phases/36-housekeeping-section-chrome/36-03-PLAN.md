---
phase: 36-housekeeping-section-chrome
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/web/components/housekeeping/AssignmentSidebar.tsx
  - apps/web/components/housekeeping/PredictionPanel.tsx
autonomous: true

# Zero file overlap with 36-02 (which owns housekeeping/page.tsx only) — runs in parallel, same
# wave. Neither AssignmentSidebar.tsx nor PredictionPanel.tsx is in frozen-files.json (confirmed by
# direct read of the manifest); both are chrome, explicitly IN scope for v2 restyle per
# 36-CONTEXT.md. No new i18n keys needed for either component (36-01-SUMMARY.md confirms) — this
# plan is visual-only: typography/color token consolidation, converting SkeletonRow to the shared
# Skeleton primitive where reasonable, NOT adding any new error UI where none exists today.
#
# PredictionPanel's isLoading prop is fed by SupervisorHousekeepingPage's fetchPredictions, which
# deliberately swallows fetch errors ("predictions are optional" — housekeeping/page.tsx line 656-
# 657). This is an intentional design per 36-CONTEXT.md and MUST stay exactly as-is — do NOT add a
# new isError prop, new error branch, or change the silent-catch behavior. Only the visual loading
# treatment changes.
#
# The AI auto-assign / reassign / escalate / acknowledge / batch-reassign / batch-acknowledge
# action flows (in both files) are functionally frozen this phase — visuals only, per 36-CONTEXT.md
# ("preserved exactly; this phase changes visuals only"). Do not alter mutation logic, confirm-tap
# mechanics, or toast wiring.
#
# FLAG-GATED, NOT UNCONDITIONAL (locked Phase 30 mechanism — gates per-section, not all-or-nothing):
# both AssignmentSidebar.tsx and PredictionPanel.tsx self-read
# `isSectionRedesigned('housekeeping', hotel)` themselves (via `useHotelStore`), matching the exact
# precedent set by 35-03-PLAN.md's FailurePredictionSidebar.tsx/WorkOrderDetailDrawer.tsx/
# CreateWorkOrderModal.tsx/BulkArchiveModal.tsx — self-read, not a prop threaded in from 36-02's
# housekeeping/page.tsx (these two files have zero file overlap with 36-02, so a self-read keeps the
# two plans decoupled and parallel-safe). Every restyled className in both tasks below is a ternary
# on `v2` (`v2 ? newClasses : oldClasses`) — the OLD classes are the exact pre-plan values (verified
# by direct read of both files) and MUST remain reachable when the flag is off. `handleAiAutoAssign`
# in AssignmentSidebar.tsx and `runAction`/`confirmBatchAction`/both `useMutation` calls in
# PredictionPanel.tsx are untouched regardless of `v2` — flag-gating only ever wraps
# className/JSX-text, never logic.

must_haves:
  truths:
    - "With the 'housekeeping' flag ON, AssignmentSidebar's stat-tile labels and PredictionPanel's body/label text all render within the UI-SPEC's declared 4-size (13/12/14/20px) 2-weight (regular 400 / semibold 600) typography scale — no text-[11px]/text-[11.5px]/text-[10.5px]/text-[10px]/font-medium instance remains in either file's v2-branch classNames"
    - "With the 'housekeeping' flag OFF, both AssignmentSidebar.tsx and PredictionPanel.tsx render byte-identically to their pre-plan state — every restyled className falls back to its exact original value, confirmed by diff review showing the !v2 branch matches the pre-plan file line-for-line"
    - "Both components' loading states use the shared Skeleton primitive (or are visually equivalent to it) instead of fully bespoke inline animate-pulse markup under v2, and v2's color usage matches the UI-SPEC's Color table (accent reserved for the Save CTA/VIP pill/links, --alert reserved for genuinely destructive/alert states, --ai for the AI-attribution band, room-status tokens byte-unchanged); the !v2 branch keeps today's bespoke markup untouched"
    - "Every existing action flow (AI auto-assign, reassign/escalate/acknowledge single + batch, their inline confirm-before-act UI) fires the identical API calls and renders the identical result copy as before this plan — zero behavior change, confirmed by re-reading the mutation/confirm logic unchanged, in both the v2 and !v2 branches"
    - "PredictionPanel's isLoading path still has NO error UI (the intentional silent-failure design from housekeeping/page.tsx's fetchPredictions is untouched) — only its v2-gated visual loading treatment changed"
  artifacts:
    - path: "apps/web/components/housekeeping/AssignmentSidebar.tsx"
      provides: "Self-reads isSectionRedesigned('housekeeping', hotel) via useHotelStore; v2 typography (stat-tile labels at 12px regular, dropping the current text-[11px] font-medium) and token-consistent color reachable only when v2 is true; !v2 renders pre-plan classNames exactly; zero change to handleAiAutoAssign's logic or toast copy in either branch"
      contains: "isSectionRedesigned('housekeeping'"
    - path: "apps/web/components/housekeeping/PredictionPanel.tsx"
      provides: "Self-reads isSectionRedesigned('housekeeping', hotel) via useHotelStore; v2 typography (all text-[11.5px]/text-[11px]/text-[10.5px] instances consolidated to the declared 12px/13px/14px scale, font-medium dropped in favor of regular/semibold only) reachable only when v2 is true; !v2 renders pre-plan classNames exactly; zero change to any mutation/confirm-tap logic in either branch"
      contains: "isSectionRedesigned('housekeeping'"
  key_links:
    - from: "AssignmentSidebar.tsx / PredictionPanel.tsx"
      to: "@/stores/hotelStore + @/lib/utils/redesignFlag"
      via: "const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('housekeeping', hotel) — self-read in each file, no prop from 36-02's housekeeping/page.tsx"
      pattern: "isSectionRedesigned\\('housekeeping'"
    - from: "AssignmentSidebar.tsx stat tiles"
      to: "UI-SPEC.md Typography table (Label/meta role: 12px, regular 400)"
      via: "v2 ? 'text-xs text-ink3' : 'text-[11px] font-medium text-ink3' — ternary, both branches present"
      pattern: "text-\\[11px\\]"
    - from: "PredictionPanel.tsx confirm banners / batch summary / risk-factor pills"
      to: "UI-SPEC.md Typography table"
      via: "v2-gated text-xs/text-sm ternaries replacing every text-[11.5px]/text-[10.5px]/text-[11px] fractional-pixel instance; !v2 branch keeps the original fractional value"
      pattern: "text-\\[1[01]\\.?5?px\\]"
---

<objective>
Restyle `AssignmentSidebar.tsx` (97 LOC) and `PredictionPanel.tsx` (563 LOC) onto the v2 visual system's declared typography/color scale, closing 36-RESEARCH.md's Pitfall 4 (AssignmentSidebar's `text-[11px] font-medium` stat-tile labels don't map to the UI-SPEC's declared 4-size/2-weight cap) and 36-RESEARCH.md's Open Question 2 (PredictionPanel's several `text-[11.5px]`/`text-[10.5px]` instances at lines ~150, 159, 165, 207, 220, 233, 245, 385, 416, 449, 476, 506, 523 need consolidating into the declared 12px "Label/meta" role, dropping `font-medium`/500 wherever it appears since the UI-SPEC caps weights at regular(400)+semibold(600)). Both components self-read `isSectionRedesigned('housekeeping', hotel)` (via `useHotelStore`) exactly like 35-03-PLAN.md's precedent — every restyled className is a `v2 ? new : old` ternary, so flag-OFF renders byte-identically to today. Zero functional change: every AI-assign/reassign/escalate/acknowledge/batch action, its confirm-tap mechanic, its mutation call, and its toast/result copy stays byte-identical in both flag states — this is a pure visual pass.

Purpose: deliver the remaining chrome UI-SPEC governs (AssignmentSidebar/PredictionPanel body and label text, explicitly named in-scope) without touching either component's business logic, running in parallel with 36-02 since neither file overlaps, and without shipping the new visuals to hotels that don't have `housekeeping` in `web_redesign_sections` — per Phase 30's locked per-section rollout mechanism.
Output: both components pass a 4-size/2-weight typography audit with zero fractional-pixel or font-medium instances remaining under v2, zero change to any action flow, and flag-OFF pixel-for-pixel identical to pre-plan.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-section-chrome/36-CONTEXT.md
@.planning/phases/36-housekeeping-section-chrome/36-UI-SPEC.md
@.planning/phases/36-housekeeping-section-chrome/36-RESEARCH.md

# The two files this plan restyles in full
@apps/web/components/housekeeping/AssignmentSidebar.tsx
@apps/web/components/housekeeping/PredictionPanel.tsx

# Flag-read contract — self-read pattern, exact precedent from 35-03-PLAN.md
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts

# Shared primitive available if a genuine loading-block conversion makes sense (optional — see
# Task 2's discretion note; not required to force every SkeletonRow into this exact component)
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag-gate + restyle AssignmentSidebar.tsx to the declared typography/color scale</name>
  <files>apps/web/components/housekeeping/AssignmentSidebar.tsx</files>
  <action>
This component has no query of its own (reads `rooms`/`buildingFilter` from the already-loaded `useHousekeepingStore`) and its one async action (`handleAiAutoAssign`) already has complete toast-based success/error handling using existing i18n keys — do not add any loading/error UI, do not touch `handleAiAutoAssign`'s logic or its toast calls.

**Flag-gate first (mandatory, matches 35-03-PLAN.md's exact precedent — self-read, not a prop):**
1. Add imports: `import { useHotelStore } from '@/stores/hotelStore'` and `import { isSectionRedesigned } from '@/lib/utils/redesignFlag'`.
2. At the top of the `AssignmentSidebar` component body, add: `const hotel = useHotelStore((s) => s.hotel)` and `const v2 = isSectionRedesigned('housekeeping', hotel)`.
3. Every restyled className below MUST be a ternary on `v2` — `v2 ? '<new classes>' : '<exact pre-plan classes>'`. Do not replace a className outright; the old value must remain reachable when `v2` is false.

Fix the two confirmed typography violations (36-RESEARCH.md Pitfall 4), gated on `v2`:
- Lines 68 and 72: `text-[11px] font-medium text-ink3` (the "Unassigned"/"Needs work" stat-tile labels) → when `v2`: `text-xs text-ink3` (12px, regular/400 — dropping `font-medium` per the UI-SPEC's 2-weight cap; `text-ink3` color unchanged); when `!v2`: keep `text-[11px] font-medium text-ink3` exactly.
- Confirm the stat-tile numerals (line 69, 73: `font-mono text-lg font-semibold text-ink`) are within scope — `text-lg` is 18px, not one of the UI-SPEC's declared 4 sizes (13/12/14/20). Since these are count numerals analogous to the my-rooms stat numerals (which the UI-SPEC pins to 20px `font-display` semibold), align these to the same "Stat numeral" role when `v2` is true: `text-xl font-mono font-semibold text-ink` (Tailwind's `text-xl` = 20px, `font-mono` kept for numeral legibility rather than switching to `font-display` — note this discretion call in the summary); when `!v2`: keep `font-mono text-lg font-semibold text-ink` exactly.
- Review the header icon circle, title/subtitle (lines 54-64), the primary CTA (lines 77-94) and the stat-tile container borders/backgrounds (lines 67-75) against the UI-SPEC's Color table — confirm `bg-[var(--ai-soft)]`/`text-[var(--ai)]` (line 55, AI-attribution icon) stays as-is (correct per Color table's `--ai` reservation) in both flag states, and confirm no non-token hardcoded color exists anywhere in the file (none found in the current read — if none exists, no color change or `v2` branching is needed here beyond what's already token-based).
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `isSectionRedesigned('housekeeping'` present; grep confirms the pre-plan `text-[11px] font-medium text-ink3` and `font-mono text-lg font-semibold text-ink` strings still appear verbatim in the file (the `!v2` branch), alongside the new `v2`-branch classes; grep confirms `handleAiAutoAssign`'s body (API call, toast calls, error handling) is unchanged from the pre-plan version (diff shows only className/JSX text-sizing changes, zero logic lines touched); with the `housekeeping` flag OFF for the test hotel, dev-server render is visually identical to pre-plan</verify>
  <done>AssignmentSidebar.tsx self-reads the housekeeping flag; under v2, stat-tile labels are 12px regular and stat numerals use a 20px semibold treatment consistent with the my-rooms stat-numeral role; under !v2, every className matches pre-plan exactly; zero remaining fractional-pixel or font-medium text in the v2 branch; zero change to handleAiAutoAssign or any API/toast call in either branch</done>
</task>

<task type="auto">
  <name>Task 2: Flag-gate + restyle PredictionPanel.tsx — consolidate every fractional-pixel size, drop font-medium, preserve every action flow exactly</name>
  <files>apps/web/components/housekeeping/PredictionPanel.tsx</files>
  <action>
**Flag-gate first (mandatory, matches 35-03-PLAN.md's exact precedent — self-read, not a prop):**
1. Add imports: `import { useHotelStore } from '@/stores/hotelStore'` and `import { isSectionRedesigned } from '@/lib/utils/redesignFlag'`.
2. At the top of the exported `PredictionPanel` component body, add: `const hotel = useHotelStore((s) => s.hotel)` and `const v2 = isSectionRedesigned('housekeeping', hotel)`.
3. `PredictionRow` is a module-private sub-component of this same file (not exported, not called from any other file) — thread `v2: boolean` into its props interface and pass `v2={v2}` at its one call site (`<PredictionRow ... />`, inside the `atRiskRooms.map(...)` in the Body section). This is an in-file prop hop, not a cross-file/cross-plan contract, so it does not violate the self-read requirement (only the file's own top-level exported component needs to self-read the flag).
4. Every restyled className below MUST be a ternary on `v2` — `v2 ? '<new classes>' : '<exact pre-plan classes>'`. Do not replace a className outright; the old fractional-pixel/font-medium value must remain reachable when `v2` is false.

Systematically walk every `text-[N.5px]`/`text-[Npx]` instance below N=13 in this file and map it to the UI-SPEC's declared scale (13px body / 12px label-meta / 14px heading-semibold / 20px stat-semibold — this file has no stat-numeral use, so only 13/12/14 apply here). For each, keep the existing font weight ONLY if it is already regular(400) or semibold(600); if it is `font-medium`(500), drop it to regular(400) per the UI-SPEC's 2-weight cap (this mirrors 36-RESEARCH.md Open Question 2's explicit recommendation: "these odd fractional sizes are meant to be eliminated, not preserved"). All of the following are `v2 ? new : old` ternaries — the `old` side is the exact current value:

- ETA text (`text-[11px] font-mono text-ink-3`, ~line 150, inside `PredictionRow` — uses the `v2` prop from step 3) → v2: `text-xs font-mono text-ink-3` (12px label/meta, font-mono kept for numeral legibility, no weight class = regular by default) / !v2: `text-[11px] font-mono text-ink-3`.
- Risk-factor pills (`text-[10.5px] bg-surface-3 text-ink-2 border border-line`, ~line 159, inside `PredictionRow`) → v2: `text-xs bg-surface-3 text-ink-2 border border-line` / !v2: `text-[10.5px] bg-surface-3 text-ink-2 border border-line`.
- Confidence-score text (`text-[11px] font-mono text-[var(--ai)] ... font-semibold`, ~line 165, inside `PredictionRow`) → v2: `text-xs font-mono text-[var(--ai)] ... font-semibold` (12px, semibold kept — it's the row's visual emphasis point, within the 2-weight cap) / !v2: unchanged.
- The three inline confirm-banner spans (`text-[11.5px] text-ink2 font-medium flex-1`, ~lines 207, 220, 233, inside `PredictionRow`) → v2: `text-xs text-ink2 flex-1` (12px, drop font-medium → regular) / !v2: `text-[11.5px] text-ink2 font-medium flex-1`.
- `resultNote` text (`text-[11.5px] text-ink3`, ~line 245, inside `PredictionRow`) → v2: `text-xs text-ink3` / !v2: `text-[11.5px] text-ink3`.
- The "all clear" indicator span (`text-[11px] font-mono text-[var(--ai)] opacity-70`, ~line 385, in the collapsed-header row of the top-level `PredictionPanel` component — uses the top-level `v2`, not the prop) → v2: `text-xs font-mono text-[var(--ai)] opacity-70` / !v2: `text-[11px] font-mono text-[var(--ai)] opacity-70`.
- `selectAll`/`deselectAll` link text (`text-[11.5px] text-ink3 hover:text-ink2 underline underline-offset-2`, ~line 416) → v2: `text-xs text-ink3 hover:text-ink2 underline underline-offset-2` / !v2: unchanged.
- The two batch confirm-banner spans (`text-[11.5px] text-ink2 font-medium flex-1`, ~lines 449, 476) → v2: `text-xs text-ink2 flex-1` / !v2: `text-[11.5px] text-ink2 font-medium flex-1`.
- Batch result summary (`text-[11.5px] font-semibold text-ink2`, ~line 506) → v2: `text-xs font-semibold text-ink2` (semibold kept, it's a section label) / !v2: unchanged.
- Batch result list items (`text-[11px] text-ink3 font-mono`, ~line 523) → v2: `text-xs text-ink3 font-mono` / !v2: unchanged.
- The "selected count" span (`text-[12px] font-semibold text-ink`, ~line 410) is already at 12px — leave as-is (no `v2` branch needed), just confirm it stays semibold (it's a count label, matching the batch-summary treatment above).
- The `noRisks` empty message (`font-display italic text-[14px] text-ink-3 leading-relaxed`, ~line 543) is already at the declared 14px — leave the size as-is (no `v2` branch needed); this is a stylistic italic empty-state message, not a "heading" per the UI-SPEC's role table, so its weight (regular, implied by no explicit weight class) is compliant. Do not force it into `EmptyState` — 36-CONTEXT.md's "close a genuine gap, don't rewrite" discipline applies; this is not a genuine loading/error gap, it's PredictionPanel's own intentional "all clear" render when `atRiskRooms.length === 0`, distinct from the `isLoading`-driven `SkeletonRow` state above it.

Loading state (`SkeletonRow`, lines 49-66): this is a 3-row bespoke skeleton already visually similar to `Skeleton`'s `variant="card"` shape but with internal multi-line structure the shared primitive doesn't replicate exactly. Leave `SkeletonRow` as its own component, unconditionally (do not gate it on `v2` and do not force-fit it into the shared `Skeleton` primitive, which would lose the row's internal label/pill/timestamp placeholder structure) — this is a reasonable, documented exception per 36-CONTEXT.md's "restyle... don't rewrite" framing, since `SkeletonRow` is already token-based (`bg-surface-3`/`animate-pulse`) and structurally bespoke by necessity, not an oversight. Note this discretion call explicitly in the summary.

Do NOT touch: `runAction`, `handleActionComplete`, `toggleSelected`, `handleSelectAllToggle`, `confirmBatchAction`, `batchReassignMutation`/`batchAcknowledgeMutation` (their mutationFn, onSuccess), or any `mode`/`batchMode` state-transition logic — in either flag state. Do NOT add a new `isError`/error-UI branch for the `isLoading` prop — confirmed intentional silent-failure design (36-CONTEXT.md), stays exactly as-is regardless of `v2`.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `isSectionRedesigned('housekeeping'` present; grep confirms every pre-plan fractional-pixel string (`text-[11px]`, `text-[10.5px]`, `text-[11.5px]` at all 12 listed line references including line 385) still appears verbatim in the file as the `!v2` branch value, alongside the new `text-xs`-based `v2` branch classes; grep -E "text-\[1[01]\.?5px\]" still finds matches (expected — they're the `!v2` fallback, not eliminated) but every match is inside a ternary whose `v2`-true side uses `text-xs`; diff review confirms `runAction`, the two `useMutation` calls, and all `mode`/`batchMode` transition functions are byte-unchanged from pre-plan; with the `housekeeping` flag OFF for the test hotel, dev-server render is visually identical to pre-plan</verify>
  <done>PredictionPanel.tsx self-reads the housekeeping flag and threads it as a prop into the in-file PredictionRow sub-component; every fractional-pixel text size (including the line-385 "all clear" indicator) is now a v2-gated ternary consolidating to the UI-SPEC's 12/13/14px scale with font-medium dropped only on the v2 side; the !v2 side matches pre-plan exactly; zero change to any mutation/confirm/toggle logic in either branch; SkeletonRow intentionally kept as its own bespoke-but-token-based component, unconditional on v2, with rationale recorded</done>
</task>

</tasks>

<verification>
- `cd apps/web && npm run type-check` green for both files.
- Both files self-read `isSectionRedesigned('housekeeping', hotel)` via `useHotelStore` — grep confirms the exact string in each file.
- `grep -E "text-\[1[01]\.?5?px\]"` still finds matches in both files (the `!v2` fallback values are intentionally preserved, not eliminated) — but every restyle site is a ternary with a `text-xs`-based `v2`-true side.
- `grep "font-medium"` still finds matches in both files (the `!v2` fallback values) but zero `font-medium` instance exists on the `v2`-true side of any ternary.
- With the `housekeeping` flag OFF for the test hotel, dev-server render of both components is pixel-identical to their pre-plan state (spot-check via dev server).
- Diff review of both files confirms every changed line is a `className`/JSX-text/flag-read/prop-threading change only — zero line touching a function body, mutation call, state transition, or i18n key reference is different from pre-plan.
</verification>

<success_criteria>
`AssignmentSidebar.tsx` and `PredictionPanel.tsx` both self-read the `housekeeping` flag and, under `v2`, pass a 4-size (13/12/14/20px)/2-weight (regular/semibold) typography audit with zero fractional-pixel or `font-medium` instances remaining; under `!v2`, both render byte-identically to their pre-plan state; every AI-assign/reassign/escalate/acknowledge/batch action flow, its confirm-tap mechanic, and its result copy remains byte-identical to the pre-plan version in both flag states.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-section-chrome/36-03-SUMMARY.md` recording: confirmation both components self-read `isSectionRedesigned('housekeeping', hotel)` (no prop from 36-02's housekeeping/page.tsx, confirming the cross-plan decoupling held), every text-size/weight change made as a `v2`/`!v2` pair (old value → new value, with line reference), how `v2` was threaded into the in-file `PredictionRow` sub-component, the two explicit discretion calls (AssignmentSidebar's stat-numeral treatment choice, PredictionPanel's SkeletonRow-stays-bespoke-and-unconditional decision) with rationale, confirmation that zero business logic changed in either flag state (diff evidence), confirmation flag-OFF renders byte-identically to pre-plan, and any deviation from this plan's line-number estimates (the file may have shifted slightly since 36-RESEARCH.md's line-pinned read).
</output>
