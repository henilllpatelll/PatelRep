---
phase: 36-housekeeping-section-chrome
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/web/components/housekeeping/AssignmentSidebar.tsx
  - apps/web/components/housekeeping/PredictionPanel.tsx
autonomous: true

# Zero file overlap with 36-02 (which owns housekeeping/page.tsx only) — runs in parallel, same
# wave. Neither AssignmentSidebar.tsx nor PredictionPanel.tsx is in frozen-files.json (confirmed by
# direct read of the manifest); both are chrome, explicitly IN scope for v2 restyle per
# 36-CONTEXT.md. No new i18n keys needed for either component (36-01-SUMMARY.md confirms) — this
# plan is visual-only: typography/color token consolidation, converting SkeletonRow to the shared
# Skeleton primitive where reasonable, NOT adding any new error UI where none exists today.
#
# PredictionPanel's isLoading prop is fed by SupervisorHousekeepingPage's fetchPredictions, which
# deliberately swallows fetch errors ("predictions are optional" — housekeeping/page.tsx line 656-
# 657). This is an intentional design per 36-CONTEXT.md and MUST stay exactly as-is — do NOT add a
# new isError prop, new error branch, or change the silent-catch behavior. Only the visual loading
# treatment changes.
#
# The AI auto-assign / reassign / escalate / acknowledge / batch-reassign / batch-acknowledge
# action flows (in both files) are functionally frozen this phase — visuals only, per 36-CONTEXT.md
# ("preserved exactly; this phase changes visuals only"). Do not alter mutation logic, confirm-tap
# mechanics, or toast wiring.

must_haves:
  truths:
    - "AssignmentSidebar's stat-tile labels and PredictionPanel's body/label text all render within the UI-SPEC's declared 4-size (13/12/14/20px) 2-weight (regular 400 / semibold 600) typography scale — no text-[11px]/text-[11.5px]/text-[10.5px]/text-[10px]/font-medium instance remains in either file"
    - "Both components' loading states use the shared Skeleton primitive (or are visually equivalent to it) instead of fully bespoke inline animate-pulse markup, and their color usage matches the UI-SPEC's Color table (accent reserved for the Save CTA/VIP pill/links, --alert reserved for genuinely destructive/alert states, --ai for the AI-attribution band, room-status tokens byte-unchanged)"
    - "Every existing action flow (AI auto-assign, reassign/escalate/acknowledge single + batch, their inline confirm-before-act UI) fires the identical API calls and renders the identical result copy as before this plan — zero behavior change, confirmed by re-reading the mutation/confirm logic unchanged"
    - "PredictionPanel's isLoading path still has NO error UI (the intentional silent-failure design from housekeeping/page.tsx's fetchPredictions is untouched) — only its visual loading treatment changed"
  artifacts:
    - path: "apps/web/components/housekeeping/AssignmentSidebar.tsx"
      provides: "v2 typography (stat-tile labels at 12px regular, dropping the current text-[11px] font-medium) and token-consistent color, zero change to handleAiAutoAssign's logic or toast copy"
      contains: "text-xs"
    - path: "apps/web/components/housekeeping/PredictionPanel.tsx"
      provides: "v2 typography (all text-[11.5px]/text-[11px]/text-[10.5px] instances consolidated to the declared 12px/13px/14px scale, font-medium dropped in favor of regular/semibold only) and token-consistent color, zero change to any mutation/confirm-tap logic"
      contains: "text-xs"
  key_links:
    - from: "AssignmentSidebar.tsx stat tiles"
      to: "UI-SPEC.md Typography table (Label/meta role: 12px, regular 400)"
      via: "text-xs (12px) replacing text-[11px], font-medium dropped"
      pattern: "text-\\[11px\\]"
    - from: "PredictionPanel.tsx confirm banners / batch summary / risk-factor pills"
      to: "UI-SPEC.md Typography table"
      via: "text-xs/text-sm replacing every text-[11.5px]/text-[10.5px]/text-[11px] fractional-pixel instance"
      pattern: "text-\\[1[01]\\.?5?px\\]"
---

<objective>
Restyle `AssignmentSidebar.tsx` (97 LOC) and `PredictionPanel.tsx` (563 LOC) onto the v2 visual system's declared typography/color scale, closing 36-RESEARCH.md's Pitfall 4 (AssignmentSidebar's `text-[11px] font-medium` stat-tile labels don't map to the UI-SPEC's declared 4-size/2-weight cap) and 36-RESEARCH.md's Open Question 2 (PredictionPanel's several `text-[11.5px]`/`text-[10.5px]` instances at lines ~150, 159, 165, 207, 220, 233, 245, 416, 449, 476, 506, 523 need consolidating into the declared 12px "Label/meta" role, dropping `font-medium`/500 wherever it appears since the UI-SPEC caps weights at regular(400)+semibold(600)). Zero functional change: every AI-assign/reassign/escalate/acknowledge/batch action, its confirm-tap mechanic, its mutation call, and its toast/result copy stays byte-identical — this is a pure visual pass.

Purpose: deliver the remaining chrome UI-SPEC governs (AssignmentSidebar/PredictionPanel body and label text, explicitly named in-scope) without touching either component's business logic, running in parallel with 36-02 since neither file overlaps.
Output: both components pass a 4-size/2-weight typography audit with zero fractional-pixel or font-medium instances remaining, zero change to any action flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-section-chrome/36-CONTEXT.md
@.planning/phases/36-housekeeping-section-chrome/36-UI-SPEC.md
@.planning/phases/36-housekeeping-section-chrome/36-RESEARCH.md

# The two files this plan restyles in full
@apps/web/components/housekeeping/AssignmentSidebar.tsx
@apps/web/components/housekeeping/PredictionPanel.tsx

# Shared primitive available if a genuine loading-block conversion makes sense (optional — see
# Task 2's discretion note; not required to force every SkeletonRow into this exact component)
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AssignmentSidebar.tsx to the declared typography/color scale</name>
  <files>apps/web/components/housekeeping/AssignmentSidebar.tsx</files>
  <action>
This component has no query of its own (reads `rooms`/`buildingFilter` from the already-loaded `useHousekeepingStore`) and its one async action (`handleAiAutoAssign`) already has complete toast-based success/error handling using existing i18n keys — do not add any loading/error UI, do not touch `handleAiAutoAssign`'s logic or its toast calls.

Fix the two confirmed typography violations (36-RESEARCH.md Pitfall 4):
- Lines 68 and 72: `text-[11px] font-medium text-ink3` (the "Unassigned"/"Needs work" stat-tile labels) → `text-xs text-ink3` (12px, regular/400 — dropping `font-medium` per the UI-SPEC's 2-weight cap; `text-ink3` color unchanged).
- Confirm the stat-tile numerals (line 69, 73: `font-mono text-lg font-semibold text-ink`) are within scope — `text-lg` is 18px, not one of the UI-SPEC's declared 4 sizes (13/12/14/20). Since these are count numerals analogous to the my-rooms stat numerals (which the UI-SPEC pins to 20px `font-display` semibold), align these to the same "Stat numeral" role: change to a 20px equivalent semibold treatment, keeping `font-mono` (numeral legibility) rather than switching to `font-display` — use `text-xl font-mono font-semibold text-ink` (Tailwind's `text-xl` = 20px) so the numeral role stays visually consistent with the my-rooms view's stat numerals while preserving this component's existing monospace-numeral convention (not the my-rooms view's serif-display convention — these are two different "stat numeral" contexts, note this discretion call in the summary).
- Review the header icon circle, title/subtitle (lines 54-64), the primary CTA (lines 77-94) and the stat-tile container borders/backgrounds (lines 67-75) against the UI-SPEC's Color table — confirm `bg-[var(--ai-soft)]`/`text-[var(--ai)]` (line 55, AI-attribution icon) stays as-is (correct per Color table's `--ai` reservation), and confirm no non-token hardcoded color exists anywhere in the file (none found in the current read — if none exists, no color change is needed here beyond what's already token-based).
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms zero remaining `text-[11px]` or `font-medium` instances in AssignmentSidebar.tsx; grep confirms `handleAiAutoAssign`'s body (API call, toast calls, error handling) is unchanged from the pre-plan version (diff shows only className/JSX text-sizing changes, zero logic lines touched)</verify>
  <done>AssignmentSidebar.tsx's stat-tile labels at 12px regular, stat numerals at a 20px semibold treatment consistent with the my-rooms stat-numeral role, zero remaining fractional-pixel or font-medium text, zero change to handleAiAutoAssign or any API/toast call</done>
</task>

<task type="auto">
  <name>Task 2: Restyle PredictionPanel.tsx — consolidate every fractional-pixel size, drop font-medium, preserve every action flow exactly</name>
  <files>apps/web/components/housekeeping/PredictionPanel.tsx</files>
  <action>
Systematically walk every `text-[N.5px]`/`text-[Npx]` instance below N=13 in this file and map it to the UI-SPEC's declared scale (13px body / 12px label-meta / 14px heading-semibold / 20px stat-semibold — this file has no stat-numeral use, so only 13/12/14 apply here). For each, keep the existing font weight ONLY if it is already regular(400) or semibold(600); if it is `font-medium`(500), drop it to regular(400) per the UI-SPEC's 2-weight cap (this mirrors 36-RESEARCH.md Open Question 2's explicit recommendation: "these odd fractional sizes are meant to be eliminated, not preserved"):

- ETA text (`text-[11px] font-mono text-ink-3`, ~line 150) → `text-xs font-mono text-ink-3` (12px label/meta, font-mono kept for numeral legibility, no weight class = regular by default).
- Risk-factor pills (`text-[10.5px] bg-surface-3 text-ink-2 border border-line`, ~line 159) → `text-xs bg-surface-3 text-ink-2 border border-line`.
- Confidence-score text (`text-[11px] font-mono text-[var(--ai)] ... font-semibold`, ~line 165) → `text-xs font-mono text-[var(--ai)] ... font-semibold` (12px, semibold kept — it's the row's visual emphasis point, within the 2-weight cap).
- The three inline confirm-banner spans (`text-[11.5px] text-ink2 font-medium flex-1`, ~lines 207, 220, 233) → `text-xs text-ink2 flex-1` (12px, drop font-medium → regular).
- `resultNote` text (`text-[11.5px] text-ink3`, ~line 245) → `text-xs text-ink3`.
- `selectAll`/`deselectAll` link text (`text-[11.5px] text-ink3 hover:text-ink2 underline underline-offset-2`, ~line 416) → `text-xs text-ink3 hover:text-ink2 underline underline-offset-2`.
- The two batch confirm-banner spans (`text-[11.5px] text-ink2 font-medium flex-1`, ~lines 449, 476) → `text-xs text-ink2 flex-1`.
- Batch result summary (`text-[11.5px] font-semibold text-ink2`, ~line 506) → `text-xs font-semibold text-ink2` (semibold kept, it's a section label).
- Batch result list items (`text-[11px] text-ink3 font-mono`, ~line 523) → `text-xs text-ink3 font-mono`.
- The "selected count" span (`text-[12px] font-semibold text-ink`, ~line 410) is already at 12px — leave as-is, just confirm it stays semibold (it's a count label, matching the batch-summary treatment above).
- The `noRisks` empty message (`font-display italic text-[14px] text-ink-3 leading-relaxed`, ~line 543) is already at the declared 14px — leave the size as-is; this is a stylistic italic empty-state message, not a "heading" per the UI-SPEC's role table, so its weight (regular, implied by no explicit weight class) is compliant. Do not force it into `EmptyState` — 36-CONTEXT.md's "close a genuine gap, don't rewrite" discipline applies; this is not a genuine loading/error gap, it's PredictionPanel's own intentional "all clear" render when `atRiskRooms.length === 0`, distinct from the `isLoading`-driven `SkeletonRow` state above it.

Loading state (`SkeletonRow`, lines 49-66): this is a 3-row bespoke skeleton already visually similar to `Skeleton`'s `variant="card"` shape but with internal multi-line structure the shared primitive doesn't replicate exactly. Leave `SkeletonRow` as its own component (do not force-fit it into the shared `Skeleton` primitive, which would lose the row's internal label/pill/timestamp placeholder structure) — this is a reasonable, documented exception per 36-CONTEXT.md's "restyle... don't rewrite" framing, since `SkeletonRow` is already token-based (`bg-surface-3`/`animate-pulse`) and structurally bespoke by necessity, not an oversight. Note this discretion call explicitly in the summary.

Do NOT touch: `runAction`, `handleActionComplete`, `toggleSelected`, `handleSelectAllToggle`, `confirmBatchAction`, `batchReassignMutation`/`batchAcknowledgeMutation` (their mutationFn, onSuccess), or any `mode`/`batchMode` state-transition logic. Do NOT add a new `isError`/error-UI branch for the `isLoading` prop — confirmed intentional silent-failure design (36-CONTEXT.md), stays exactly as-is.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep -E "text-\[1[01]\.?5?px\]" apps/web/components/housekeeping/PredictionPanel.tsx returns zero matches; grep confirms `font-medium` no longer appears anywhere in the file (search for exact string); diff review confirms `runAction`, the two `useMutation` calls, and all `mode`/`batchMode` transition functions are byte-unchanged from pre-plan</verify>
  <done>Every fractional-pixel text size in PredictionPanel.tsx consolidated into the UI-SPEC's 12/13/14px scale, zero font-medium remaining, zero change to any mutation/confirm/toggle logic, SkeletonRow intentionally kept as its own bespoke-but-token-based component with rationale recorded</done>
</task>

</tasks>

<verification>
- `cd apps/web && npm run type-check` green for both files.
- `grep -E "text-\[1[01]\.?5?px\]"` returns zero matches across both files (no fractional sub-12px sizes remain).
- `grep "font-medium"` returns zero matches across both files.
- Diff review of both files confirms every changed line is a `className`/JSX-text change only — zero line touching a function body, mutation call, state transition, or i18n key reference is different from pre-plan.
</verification>

<success_criteria>
`AssignmentSidebar.tsx` and `PredictionPanel.tsx` both pass a 4-size (13/12/14/20px)/2-weight (regular/semibold) typography audit with zero fractional-pixel or `font-medium` instances remaining, while every AI-assign/reassign/escalate/acknowledge/batch action flow, its confirm-tap mechanic, and its result copy remains byte-identical to the pre-plan version.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-section-chrome/36-03-SUMMARY.md` recording: every text-size/weight change made (old value → new value, with line reference), the two explicit discretion calls (AssignmentSidebar's stat-numeral treatment choice, PredictionPanel's SkeletonRow-stays-bespoke decision) with rationale, confirmation that zero business logic changed (diff evidence), and any deviation from this plan's line-number estimates (the file may have shifted slightly since 36-RESEARCH.md's line-pinned read).
</output>
