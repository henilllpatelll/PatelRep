---
phase: 36-housekeeping-section-chrome
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified: []
autonomous: true

# UNLIKE Phase 35's close-out (35-07), this plan does NOT need a Task-1-style regression-harness
# precondition fix. Confirmed by direct read of apps/web/e2e/room-board-baseline.spec.ts: its
# chromeMasks() ALREADY includes page.locator('[data-testid="page-header"]'), added by Phase 35
# with an explicit comment stating "This mask is reusable as-is for Phase 36's Housekeeping chrome
# close-out too, since /housekeeping renders the same PageHeader above RoomStatusBoard/
# RoomDetailDrawer." Phase 35's own close-out (35-07-SUMMARY.md) already regenerated all 12
# baseline snapshots for this shared mask, including the 8 housekeeping-specific ones (RoomStatus-
# Board light/dark x gm/supervisor, RoomDetailDrawer light/dark x gm/supervisor). This plan's job
# is to RUN the existing regression suite, not modify it — per 36-RESEARCH.md's Pitfall 1, doing a
# Phase-35-style Task 1 here would be wasted, redundant work. If (and only if) this phase's own
# restyle introduces a genuinely new, currently-unmasked visible chrome element that varies between
# flag-on/flag-off in the SAME screenshot region as the frozen board, extend chromeMasks() then —
# but this is not expected (AssignmentSidebar/HousekeeperBar only render when assignmentMode is
# true, which is false in the regression fixture's default state, so they're outside the captured
# viewport region already).

must_haves:
  truths:
    - "All standing web gates pass on the combined Phase-36 changes: type-check, check:frozen-files (zero drift on RoomCard.tsx/RoomStatusBoard.tsx/RoomDetailDrawer.tsx specifically, plus the other 4 frozen files untouched), check:contrast (dark-mode WCAG AA), check:i18n-parity + verify:i18n-gate, and build"
    - "The Phase-30 Room-Board regression harness (already covering /housekeeping's RoomStatusBoard + RoomDetailDrawer, both light/dark, both gm/supervisor roles, chromeMasks() already including the page-header mask from Phase 35 — confirmed, no harness edit needed) re-passes at zero pixel drift for both flag-off and flag-on"
    - "With the 'housekeeping' flag ON for the test hotel, /housekeeping renders the v2 redesign for both the supervisor/gm board view (PageHeader, sync badge, date/shift controls, AssignmentSidebar, PredictionPanel, HousekeeperBar in assign mode) and the housekeeper my-rooms view (PageHeader, stat numerals, room list, empty/loading/error states) including the two newly-closed error gaps (staff-list fetch failure, my-rooms board fetch failure, with a live-forced background-refetch-failure check confirming NO error flash over already-loaded data); RoomStatusBoard/RoomDetailDrawer render completely unchanged in both flag states; with the flag OFF, the whole section renders today's UI unchanged"
    - "Each state renders correctly in light+dark and EN+ES with no console errors, no failed/forbidden API calls, no domTranslations.ts EN/ES mangling on any touched PageHeader or the 2 new StateBlock error strings — tested via BOTH the live-toggle method (bug-965-class) AND the cold-reload-with-es-persisted method (bug-1021-class, discovered in Phase 35, not yet re-tested on Housekeeping's content) — and the same-inputs→same-outputs network diff confirms zero behavior/data change; Realtime sync stays live throughout (SyncBadge confirms an active subscription, HousekeeperMyRoomsView's hk_my_rooms_realtime channel unaffected)"
  artifacts: []
  key_links:
    - from: "apps/web/e2e/room-board-baseline.spec.ts's existing housekeeping tests"
      to: "the combined 36-01/36-02/36-03 changes"
      via: "npx playwright test --config=playwright.regression.config.ts, both flag states"
      pattern: "housekeeping RoomStatusBoard|RoomDetailDrawer"
---

<objective>
Close Phase 36: verify the two wave-2 content plans (36-02's `housekeeping/page.tsx` restyle, 36-03's `AssignmentSidebar`/`PredictionPanel` restyle) combine cleanly with 36-01's locale additions, and that `RoomCard`/`RoomStatusBoard`/`RoomDetailDrawer` are provably untouched. Confirm (do not modify, per 36-RESEARCH.md's Pitfall 1) that the Phase-30 Room-Board regression harness's existing `chromeMasks()` — already extended by Phase 35 to mask the shared `PageHeader` — needs no Phase-36-specific edit before it can be trusted. Run the full standing web gate suite and re-pass the regression harness (flag-off AND flag-on). Live-verify the whole `/housekeeping` section in the browser: both role views, both genuinely-closed error gaps (including the stale-data-safe background-refetch-failure check), light+dark, EN+ES with BOTH i18n-mangling test methods established by Phase 35 (live-toggle for bug-965-class, cold-reload-with-persisted-es for bug-1021-class), network-inert, and Realtime sync staying live.

This satisfies the whole-phase view of all 4 ROADMAP HSK-01 Success Criteria: (1) PageHeader/sync badge/date-shift controls/my-rooms list in the new visual system, (2) RoomStatusBoard/RoomDetailDrawer visually/functionally identical inside the new chrome — regression pixel-diff passes, (3) Realtime sync stays live throughout and after the redesign, (4) dark-mode contrast, EN/ES key-parity, and same-inputs→same-outputs network diff all green.

Purpose: phase-close verification gate — no source edit is expected to be required (unlike Phase 35's close-out), only verification; if a real defect is found, fix it as a blocking Rule-1 item in the owning file.
Output: verification evidence in the summary; green gates + regression re-pass + live per-view confirmation; STATE.md updated to reflect Phase 36 closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-section-chrome/36-CONTEXT.md
@.planning/phases/36-housekeeping-section-chrome/36-UI-SPEC.md
@.planning/phases/36-housekeeping-section-chrome/36-RESEARCH.md

# Prior-plan summaries — what was built, exact keys/props/gating logic used
@.planning/phases/36-housekeeping-section-chrome/36-01-SUMMARY.md
@.planning/phases/36-housekeeping-section-chrome/36-02-SUMMARY.md
@.planning/phases/36-housekeeping-section-chrome/36-03-SUMMARY.md

# Phase 35's close-out precedent (chromeMasks() extension already done and confirmed reusable,
# dual i18n-mangling test method established, local-standalone-build regression workaround,
# Supabase service-role flag-flip approach, bug-965/bug-1021 discovery)
@.planning/phases/35-engineering-section-chrome/35-07-SUMMARY.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# The gate/harness surface this plan runs (reads only, no edits expected)
@apps/web/e2e/room-board-baseline.spec.ts
@apps/web/playwright.regression.config.ts
@apps/web/frozen-files.json
@apps/web/frozen-files-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full standing gate suite + Room-Board regression re-pass on combined changes (no harness edit expected)</name>
  <files></files>
  <action>
First, confirm (grep, do not assume) that `apps/web/e2e/room-board-baseline.spec.ts`'s `chromeMasks()` already contains `page.locator('[data-testid="page-header"]')` — per 36-RESEARCH.md's Pitfall 1 this should already be true from Phase 35's close-out. If it is present, proceed directly to the gate suite (no Task-1-style harness edit needed this phase). If it is somehow absent (state drift since research was written), add it following Phase 35's exact precedent (35-07-PLAN.md Task 1) before proceeding, and note this deviation prominently in the summary.

From `apps/web`, run the full standing web gate suite over the combined Phase-36 tree and confirm all green:
- `npm run type-check`
- `npm run check:frozen-files` — MUST show zero frozen-hash drift AND zero room-status value drift. Confirm `RoomCard.tsx`, `RoomStatusBoard.tsx`, `RoomDetailDrawer.tsx` hashes specifically are unchanged (none of 36-02/36-03's `files_modified` list them). Confirm the allowlist (`frozen-files-allowlist.json`) is still `entries: []` unless a wave-2 plan legitimately needed one (unlikely).
- `npm run check:contrast` — dark-mode WCAG AA over every new/extended token pairing.
- `npm run check:i18n-parity` and `npm run verify:i18n-gate` — en/es parity intact after 36-01's 2 key additions.
- `npm run build`.
Then re-pass the Phase-30 Room-Board regression harness: `npx playwright test --config=playwright.regression.config.ts`. Per 33-07/34-08/35-07's precedent, FIRST check whether the local tree is still ahead of `origin/main` (the harness's hardcoded default production URL may be dead) — if so, repeat the local-standalone-build workaround (`npm run build` + `.next/standalone`, a temporary CSP-localhost patch to `next.config.mjs` fully reverted afterward, `PLAYWRIGHT_BASE_URL` pointed at the local build) rather than assuming the deployed URL works. Flip the regression fixture tenant's `web_redesign_sections` to include `housekeeping` via direct Supabase service-role access (`apps/api/.env`'s `SUPABASE_SERVICE_ROLE_KEY`, matching prior close-outs' precedent), run the harness for BOTH flag-off and flag-on, then restore the fixture tenant's flag array back to its permanent baseline afterward.
Expect zero pixel drift on `RoomStatusBoard`/`RoomDetailDrawer` (both `/housekeeping` tests) for both flag states, since chromeMasks() already excludes the intentionally-restyled PageHeader region and this phase's restyle never touches the frozen board trio. `RoomDetailDrawer`'s known, pre-existing, deterministic sub-pixel font-AA noise (documented by 32-06/33-07/34-08/35-07) is expected environment noise if it recurs identically — not a regression. If a residual mismatch appears anywhere OTHER than the masked header region (i.e., inside the room-card grid itself), visually inspect the diff image before concluding it's spurious — that would indicate a REAL regression.
If any gate fails on a real defect, fix it in the owning file (this un-empties `files_modified` for that fix only) and re-run until green; log the fix to `.wolf/buglog.json`.
  </action>
  <verify>All of type-check / check:frozen-files / check:contrast / check:i18n-parity / verify:i18n-gate / build exit 0; check:frozen-files explicitly confirms RoomCard.tsx/RoomStatusBoard.tsx/RoomDetailDrawer.tsx (and all 4 other frozen files) unchanged + allowlist empty (or justified); Room-Board regression suite's housekeeping tests report zero-drift for both flag-off and flag-on (RoomDetailDrawer's documented sub-pixel noise excepted); any residual diff visually confirmed to be within the already-masked PageHeader region before being dismissed; any defect found is fixed and re-verified green; regression fixture tenant restored to baseline afterward</verify>
  <done>Every standing web gate green on the combined Phase-36 changes; Room-Board regression re-passes at zero drift on the housekeeping RoomStatusBoard/RoomDetailDrawer tests (both flag states); zero frozen-file / room-status drift; fixture tenant restored; no unnecessary harness edit made</done>
</task>

<task type="auto">
  <name>Task 2: Live flag-on/flag-off browser self-verification (both role views, both closed gaps, light+dark, EN+ES dual-method i18n check, network-inert, Realtime-live)</name>
  <files></files>
  <action>
Per the mandatory Self-Verification Policy (CLAUDE.md): start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Enable the `housekeeping` flag for the test hotel by adding it to its `web_redesign_sections` array (direct Supabase service-role access or `hotelsApi.update` PATCH, matching prior phases' approach; no settings-UI toggle exists).

Verify, FLAG ON:
- **Supervisor/GM board view** (`/housekeeping` as gm/housekeeping_supervisor/front_desk): PageHeader (eyebrow/title/SyncBadge/date-nav/shift-select/assign-mode toggle) renders v2; `RoomStatusBoard` renders EXACTLY as it did before this phase (same layout, same data, zero visual difference — it's frozen). Toggle assign mode on: confirm `HousekeeperBar` renders v2, force its staff-list query to fail (devtools network block) and confirm the NEW `StateBlock` error ("Couldn't load staff. Try again.") renders with a working retry that re-fires only that query; confirm `AssignmentSidebar` renders v2 (stat tiles, AI auto-assign button) with its existing AI-assign flow still fully functional (trigger it, confirm the existing toast success/error copy is unchanged). Confirm `PredictionPanel` (when at-risk predictions exist) renders v2 typography and its existing reassign/escalate/acknowledge single + batch action flows still work exactly as before (trigger at least one single-action confirm-tap and confirm the two-tap mechanic + result copy are unchanged).
- **Housekeeper my-rooms view** (`/housekeeping` as housekeeper role, or via direct component test if the role is web-unreachable per `MOBILE_ONLY_ROLES` — confirm reachability first, and if unreachable, note this in the summary and verify via a temporary role override or direct navigation, matching how Phase 35 handled the analogous `EngineerDashboard` situation): PageHeader/stat numerals/room list render v2; force the board query to fail and confirm the NEW `StateBlock` error ("Couldn't load your rooms. Try again.") renders with a working retry; CRITICALLY, after a successful initial load, force a BACKGROUND refetch failure (the query has `refetchInterval: 10_000`) and confirm the already-rendered room list does NOT get replaced by an error screen (Pitfall 3's stale-data-safe gating) — this is a required, explicit check, not optional.
- FLAG OFF: spot-check both views render today's UI unchanged (legacy branch) — including confirming neither new StateBlock error string appears anywhere and the pre-phase empty-staff/empty-rooms copy still renders on a forced fetch failure (since the legacy branch doesn't destructure/render `isError`).
- LIGHT + DARK: toggle theme; confirm no contrast/legibility break across both views and their sub-components (HousekeeperBar, HousekeeperRoomItem, AssignmentSidebar, PredictionPanel).
- EN + ES, DUAL METHOD (per 35-07-SUMMARY.md's established pattern — test BOTH, they can produce different results): (a) LIVE TOGGLE — load in EN, click the real `LanguageToggle`, force both new StateBlock errors, confirm both render as fully-correct Spanish with zero `domTranslations.ts` hybrid mangling (bug-965-class check — this is genuinely new content to the legacy translator's glossary, same category as Phase 35's 3 new strings which tested clean). (b) COLD RELOAD WITH ES PERSISTED — pre-seed `es` in localStorage before `page.goto()`/navigation, confirm both PageHeader instances (my-rooms view, board view) render correctly without the bug-1021-class hydration-race mangling Phase 35 discovered on Predictions' subtitle. If either method finds mangling on THIS phase's content, treat it as a REAL, newly-discovered defect for Phase 36 (not the already-deferred bug-965/bug-1021 themselves) — apply the same `dataI18nSkip`/`data-i18n-skip` mitigation pattern if a fix is tractable within this plan's scope, log to `.wolf/buglog.json`, and re-verify; if the underlying mechanism is architectural (matching bug-1021's root cause), defer and flag precisely rather than scope-creeping into an app-wide fix.
- REALTIME SYNC STAYS LIVE (ROADMAP Success Criterion #3, phase-specific — not covered by prior phases' close-outs): confirm `SyncBadge` shows "Live" with an active pulsing indicator both flag-on and flag-off; in the housekeeper my-rooms view, trigger a room-status change from another session/tab (or via a direct Supabase update to `room_status`) and confirm the `hk_my_rooms_realtime` channel's `postgres_changes` listener still fires and updates the list within the debounce window — zero change to this subscription logic, so this should pass trivially, but it's this phase's own named success criterion and must be explicitly confirmed, not assumed.
- NETWORK DIFF: with devtools Network open, spot-check that the SAME requests fire flag-on vs flag-off on both views (no new/removed endpoints beyond the pre-existing queries now having a rendered `isError` branch). Confirm no mutation is accidentally auto-triggered by any new v2 rendering path. Confirm no failed/forbidden (401/403) calls.
- No console errors on any view.
Record exactly what was live-verified vs code-traced, and any follow-up flagged for the orchestrator.
  </action>
  <verify>Both role views confirmed flag-on (v2) and flag-off (legacy) via screenshots/notes; both newly-closed error gaps confirmed working with retry, INCLUDING the stale-data-safe background-refetch-failure check on the my-rooms view; AssignmentSidebar/PredictionPanel action flows confirmed unchanged; light/dark confirmed with no contrast break; EN/ES confirmed clean via BOTH the live-toggle AND cold-reload methods on this phase's new StateBlock strings and PageHeader instances (explicit pass/fail recorded for each method); Realtime sync explicitly confirmed live (SyncBadge + a real postgres_changes event observed) both flag states; network diff spot-checked inert; zero console errors/forbidden calls; any un-verifiable item (e.g. housekeeper role web-reachability) explicitly flagged with the workaround used</verify>
  <done>Both /housekeeping role views live-confirmed flag-on (v2) and flag-off (legacy) with both closed error gaps working (including the stale-data-safe check), all existing action flows unchanged, light+dark and dual-method EN/ES confirmed clean (or any newly-found mangling fixed/flagged precisely), Realtime sync explicitly confirmed live, network-inert, zero console errors</done>
</task>

</tasks>

<verification>
- Full standing web gate suite green on combined changes (type-check, check:frozen-files, check:contrast, check:i18n-parity, verify:i18n-gate, build).
- check:frozen-files confirms zero drift on RoomCard.tsx/RoomStatusBoard.tsx/RoomDetailDrawer.tsx; Room-Board regression re-passes at zero drift on the housekeeping tests (both flag states, RoomDetailDrawer's known noise excepted), no harness edit was needed (or, if state had drifted, one was made and documented).
- Both role views live-verified flag-on (v2) and flag-off (legacy); both genuinely-closed error gaps (staff-list, my-rooms board) confirmed working including the stale-data-safe background-refetch check; light+dark and dual-method EN/ES confirmed with no domTranslations.ts mangling on new content (or any found mangling fixed/flagged); Realtime sync explicitly confirmed live; network diff inert; no console errors / forbidden calls.
</verification>

<success_criteria>
Phase 36 verified end-to-end: all 4 ROADMAP HSK-01 Success Criteria confirmed against the running app and the gate suite — Housekeeping's PageHeader/sync badge/date-shift controls/my-rooms list render in the new visual system, RoomStatusBoard/RoomDetailDrawer are provably pixel-identical and functionally unchanged (regression harness passes, no unnecessary harness edit), Supabase Realtime sync stays live throughout and after the redesign, and the section passes dark-mode contrast, EN/ES parity (dual-method), and same-inputs→same-outputs network diff. Any residual coverage gap or newly-discovered defect is precisely flagged for the orchestrator's close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-section-chrome/36-04-SUMMARY.md` recording: the exact gate results, confirmation no harness edit was needed (or documentation of one if state had drifted), the regression outcome (flag-off/flag-on, URL-reachability path used), a per-view/per-sub-component table of live-verified vs code-traced, the stale-data-safe background-refetch-failure check's explicit result, the Realtime-sync-stays-live confirmation (explicit, not assumed), the dual-method EN/ES i18n-mangling check's explicit pass/fail for both methods on this phase's new content, confirmation of zero frozen-file drift, any defect found+fixed (with buglog id) or deferred (with rationale), and a consolidated list of deferred items from 36-01/36-02/36-03's summaries plus any follow-up left for the orchestrator (mirroring 33-07/34-08/35-07's close-out format). Also update `.planning/STATE.md`'s Current Position to reflect Phase 36 closed and Phase 37 next.
</output>
