---
phase: 36-housekeeping-section-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

# NOTE on file ownership: en.ts / es.ts are edited ONLY here. This plan is the SOLE owner of the
# locale files for Phase 36 so the two parallel wave-2 content plans (36-02, 36-03) never collide
# on them. This phase needs exactly TWO new leaf keys (confirmed by direct code read of
# housekeeping/page.tsx and 36-UI-SPEC.md's Copywriting Contract) — both are genuine gap closures
# for queries that today have zero error UI. AssignmentSidebar.tsx and PredictionPanel.tsx need NO
# new copy: AssignmentSidebar has no query of its own (reads already-loaded Zustand state) and its
# AI-assign action already has full toast-based error copy (assignmentSidebar.failure, etc.);
# PredictionPanel's isLoading is an intentional silent-failure design per 36-CONTEXT.md and gets
# zero new error copy — only a visual restyle. Do NOT add any key beyond the two below.

must_haves:
  truths:
    - "The two genuinely-missing error strings wave-2 needs (HousekeeperMyRoomsView's board-query load error, HousekeeperBar's staff-list load error) exist in both en.ts and es.ts with real Spanish, added additively inside their existing housekeeping.page.myRooms / housekeeping.page.assignBar sub-namespaces"
    - "No new top-level or new sub-namespace is created; every pre-existing key in housekeeping.* (and every other namespace) is byte-unchanged"
    - "npm run check:i18n-parity and npm run verify:i18n-gate pass — the 2 new en keys each have an es counterpart and vice versa"
  artifacts:
    - path: "apps/web/i18n/locales/en.ts"
      provides: "housekeeping.page.myRooms.loadError, housekeeping.page.assignBar.loadError — added additively inside their existing namespaces (myRooms starts line 780, assignBar starts line 746)"
      contains: "loadError: \"Couldn't load your rooms. Try again.\""
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Exact-parity Spanish counterparts for the 2 new en.ts keys, same namespaces, same line-aligned position"
      contains: "loadError"
  key_links:
    - from: "new keys in en.ts (page.myRooms.loadError, page.assignBar.loadError)"
      to: "matching keys in es.ts"
      via: "1:1 key parity enforced by scripts/check-i18n-parity.mjs"
      pattern: "loadError"
---

<objective>
Add the ONLY two i18n keys Phase 36's wave-2 content plans genuinely need — `housekeeping.page.myRooms.loadError` (HousekeeperMyRoomsView's board query currently has zero error copy: `const { data: boardData, isLoading } = useQuery(...)`, no `isError` destructured) and `housekeeping.page.assignBar.loadError` (HousekeeperBar's staff-list query, same gap: `const { data, isLoading } = useQuery(...)`) — additively inside the EXISTING `housekeeping.page.myRooms` and `housekeeping.page.assignBar` sub-namespaces. Everything else this phase touches (SyncBadge, HousekeeperRoomItem, SupervisorHousekeepingPage, the role-loading guard, AssignmentSidebar, PredictionPanel) reuses copy that already exists today or needs no new copy — confirmed by direct read of `housekeeping/page.tsx`, `AssignmentSidebar.tsx`, `PredictionPanel.tsx`, and 36-UI-SPEC.md's Copywriting Contract table.

Purpose: unblock the two wave-2 content plans (36-02, 36-03) to run in parallel without ever touching en.ts/es.ts, and satisfy the EN/ES half of ROADMAP's HSK-01 Success Criterion #4 up front.
Output: 2 new leaf keys added additively to both locale files; check:i18n-parity + verify:i18n-gate green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-section-chrome/36-CONTEXT.md
@.planning/phases/36-housekeeping-section-chrome/36-UI-SPEC.md
@.planning/phases/36-housekeeping-section-chrome/36-RESEARCH.md

# The two locale files. housekeeping.page starts at en.ts:732 / es.ts:732 (confirmed line-aligned
# by direct read). Sub-namespaces this plan adds keys into (both pre-existing, confirmed present):
#   housekeeping.page.assignBar (en.ts line 746, es.ts line 746)
#   housekeeping.page.myRooms (en.ts line 780, es.ts line 780)
# Existing keys wave-2 plans will REUSE as style/register reference only (do not touch):
#   housekeeping.roomStatus.error.failedToLoad / .retry (en.ts line 434-435) — "Failed to load
#   rooms." / "No se pudieron cargar los cuartos." is the established failedToLoad phrasing pattern.
#   common.error / common.retry / common.loading — StateBlock's own built-in defaults, used when no
#   explicit message/label is passed (not needed here since both new keys ARE explicit messages).
@apps/web/i18n/locales/en.ts
@apps/web/i18n/locales/es.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 2 new English keys to en.ts</name>
  <files>apps/web/i18n/locales/en.ts</files>
  <action>
Add exactly two new leaf keys, additively, into two EXISTING sub-namespaces inside the `housekeeping.page` block. Leave every pre-existing key in the file byte-unchanged. Match the file's existing style (single quotes except where the string itself contains an apostrophe, trailing commas, 6-space nesting at this depth).

1. Inside `housekeeping.page.assignBar` (starts `assignBar: {` at line 746), add a `loadError` key. Place it near `noStaff`/`addStaff` (this is the exact copy from 36-UI-SPEC.md's Copywriting Contract table — HousekeeperBar's staff-list query has zero error copy today, a fetch failure currently silently renders the "No housekeeper staff found" empty-staff copy, which is factually wrong for a fetch failure):
   `loadError: "Couldn't load staff. Try again.",`

2. Inside `housekeeping.page.myRooms` (starts `myRooms: {` at line 780), add a `loadError` key. Place it near `emptyTitle`/`emptySubtitle` (same gap class — HousekeeperMyRoomsView's board query has zero error copy today):
   `loadError: "Couldn't load your rooms. Try again.",`

Use double quotes for these two specific values only (they contain no apostrophe requiring it, but match the exact copy string from the UI-SPEC verbatim, including capitalization and the period). Do NOT add any other key. Do NOT create a new namespace. Do NOT touch `housekeeping.roomStatus.error.*`, `housekeeping.assignmentSidebar.*`, `housekeeping.predictionPanel.*`, or any other existing key — these are reused as-is (or need no new copy at all) by wave-2 plans.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `housekeeping.page.assignBar` block now contains `loadError`, `housekeeping.page.myRooms` block now contains `loadError`; grep confirms no other housekeeping.* key changed (diff review shows exactly 2 added lines, zero removed/modified lines)</verify>
  <done>en.ts gains exactly 2 new leaf keys (page.assignBar.loadError, page.myRooms.loadError), all pre-existing keys byte-unchanged, type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the 2 keys into es.ts with real Spanish + prove parity</name>
  <files>apps/web/i18n/locales/es.ts</files>
  <action>
Add the exact same 2 keys, in the exact same 2 namespaces, at the same relative position, with real natural Spanish in the hotel-operations register already established in this file (formal/usted register — e.g. `housekeeping.roomStatus.error.failedToLoad`: "No se pudieron cargar los cuartos." and `housekeeping.page.myRooms.emptySubtitle`: "Consulte con su supervisor." both use formal `usted`-register phrasing, not informal `tú`):

1. `housekeeping.page.assignBar.loadError`: `'No se pudo cargar el personal. Intente de nuevo.'`
2. `housekeeping.page.myRooms.loadError`: `'No se pudieron cargar sus cuartos. Intente de nuevo.'`

Then run the parity + type gates and confirm green.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity && npm run verify:i18n-gate && npm run type-check all green; grep confirms both keys present in es.ts at the matching namespaces; diff review confirms exactly 2 added lines, zero removed/modified lines elsewhere in es.ts</verify>
  <done>es.ts mirrors both new keys from Task 1 with real Spanish; check:i18n-parity + verify:i18n-gate + type-check all green</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity`, `npm run verify:i18n-gate`, `npm run type-check` all green from `apps/web`.
- Exactly 2 new leaf keys exist in both locales, inside pre-existing namespaces (`housekeeping.page.assignBar`, `housekeeping.page.myRooms`); every other key across both files is byte-unchanged.
- No new namespace created.
</verification>

<success_criteria>
The complete Phase-36 net-new copy set (2 keys) exists in both locales with real Spanish and passes parity — satisfying the EN/ES half of HSK-01's Success Criterion #4 up front and unblocking 36-02/36-03 to run in parallel without ever touching the locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-section-chrome/36-01-SUMMARY.md` recording: the exact 2 keys added (namespace + key + English + Spanish value), explicit confirmation that AssignmentSidebar.tsx/PredictionPanel.tsx need NO new copy (so 36-03's executor doesn't waste time hunting for a locale gap that doesn't exist), confirmation of parity-gate pass, and an explicit statement that en.ts/es.ts are now frozen for the rest of Phase 36 (wave-2 plans must not edit them).
</output>
