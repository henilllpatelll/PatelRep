---
phase: 22-expo-sdk-54-57-bump
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
  - apps/mobile/app/_layout.tsx
  - apps/mobile/lib/theme/navigationTheme.ts
autonomous: true

must_haves:
  truths:
    - "apps/mobile resolves expo to a 56.x version (target 56.0.18, RN 0.85) with peers aligned by --fix"
    - "The 2 react-navigation import sites (app/_layout.tsx, lib/theme/navigationTheme.ts) import from expo-router/react-navigation (codemod applied) — no unsupported @react-navigation/* app import remains"
    - "@react-navigation/native is an explicit direct dependency in apps/mobile/package.json (MOBILE-03), pinned to the exact version transitively resolved at SDK 56, with only ONE version resolving in the tree"
    - "Navigation theming still works (navigationTheme.test.ts green + build succeeds), and the 55→56 hop passes expo-doctor, tsc, jest, then a GREEN EAS Android build, committed as its own boundary"
  artifacts:
    - path: "apps/mobile/app/_layout.tsx"
      provides: "navigation ThemeProvider import from expo-router/react-navigation"
      contains: "expo-router/react-navigation"
    - path: "apps/mobile/lib/theme/navigationTheme.ts"
      provides: "navigation theme built from expo-router/react-navigation"
      contains: "expo-router/react-navigation"
    - path: "apps/mobile/package.json"
      provides: "explicit @react-navigation/native direct dependency"
      contains: "@react-navigation/native"
  key_links:
    - from: "app/_layout.tsx + navigationTheme.ts"
      to: "expo-router/react-navigation"
      via: "codemod import rewrite"
      pattern: "expo-router/react-navigation"
    - from: "package.json @react-navigation/native pin"
      to: "single deduped version in tree"
      via: "npm ls @react-navigation/native"
      pattern: "@react-navigation/native"
---

<objective>
Execute hop 2 of 3: upgrade `apps/mobile` from SDK 55 to SDK 56 (target `56.0.18`, RN 0.85). This is the highest-risk hop: SDK 56's Expo Router FORKS from React Navigation, so app code may no longer import from `@react-navigation/*` — the 2 import sites must be rewritten to `expo-router/react-navigation`.

MOBILE-03 requires BOTH (locked decision): (a) run the official codemod to rewrite the 2 imports (the safe, supported runtime fix), AND (b) add `@react-navigation/native` as an explicit direct dependency pinned to the exact version already resolved transitively at SDK 56 (satisfies SC3's literal wording without introducing a dual-instance break, since pinning to the resolved version dedupes to one copy).

Purpose: The react-navigation fork silently breaks theming/type-check at SDK 56 if the imports are not rewritten; the codemod is the officially-supported fix and the explicit dep satisfies the requirement literally.
Output: apps/mobile on expo 56.x, imports rewritten, @react-navigation/native as direct dep (single version), green EAS Android build, committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-expo-sdk-54-57-bump/22-RESEARCH.md
@.planning/phases/22-expo-sdk-54-57-bump/22-02-SUMMARY.md
@apps/mobile/app/_layout.tsx
@apps/mobile/lib/theme/navigationTheme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade to SDK 56 and run the react-navigation codemod</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json, apps/mobile/app/_layout.tsx, apps/mobile/lib/theme/navigationTheme.ts</files>
  <action>
All commands from `apps/mobile/`.

1. `npx expo install expo@^56.0.0 --fix` — bumps `expo` to latest SDK 56 (target `56.0.18`, RN 0.85) and aligns peers. Let `--fix` own versions.
2. Run the official codemod FIRST, before any build:
   `npx expo-codemod sdk-56-expo-router-react-navigation-replace .`
   This rewrites `@react-navigation/native` imports to `expo-router/react-navigation`. Confirm it touched the 2 known sites:
   - `app/_layout.tsx` (`import { ThemeProvider as NavigationThemeProvider } from "@react-navigation/native"` → `.../expo-router/react-navigation`)
   - `lib/theme/navigationTheme.ts` (imports from `@react-navigation/native` → `expo-router/react-navigation`)
   If the codemod misses either (e.g. tool unavailable), rewrite the 2 import specifiers by hand — the runtime API is unchanged, only the module path moves.
3. Grep to prove NO app-code `@react-navigation/*` import remains: `grep -rn "@react-navigation" app/ lib/ components/` should return nothing (or only the expo-router/react-navigation-backed paths). Keep the `dynamic-import-node` babel plugin intact.
  </action>
  <verify>`grep -rn "from \"@react-navigation" apps/mobile/app apps/mobile/lib apps/mobile/components` returns no matches. Both files now reference `expo-router/react-navigation`. package.json shows expo 56.x.</verify>
  <done>expo on 56.x; both import sites rewritten to expo-router/react-navigation; no unsupported react-navigation app import remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add @react-navigation/native as explicit direct dep (MOBILE-03), verify single-version resolution + local gates</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
1. Determine the version currently resolved transitively AT SDK 56: `npm ls @react-navigation/native` (do NOT assume today's SDK-54 value of 7.2.4 — the SDK 56 --fix will have moved expo-router and its react-navigation pin). Note the exact resolved version, e.g. `7.x.y`.
2. Add `@react-navigation/native` to `apps/mobile/package.json` `dependencies`, pinned to that EXACT resolved version (not a `^` range — pin exact so npm dedupes to the one copy already in the tree). Run `npm install`.
3. CRITICAL verification — prove only ONE version resolves (no dual-instance): `npm ls @react-navigation/native` must show a single version, all other occurrences `deduped`. If TWO versions appear, adjust the pin to match expo-router's internal version until the tree dedupes to one.
4. Confirm theming still works: `npx jest` must keep `__tests__/lib/theme/navigationTheme.test.ts` GREEN (this test exercises the ThemeProvider path). Then local gates IN ORDER:
   - `npx expo-doctor@latest` — passes.
   - `npx tsc --noEmit` — exit 0.
   - `npx jest` — no new failures vs the Plan 01 baseline; navigationTheme.test.ts green.
5. If — and only if — you find real evidence of a dual-instance theming break that version-pinning cannot resolve, fall back to codemod-only (remove the explicit dep) and document the SC3 literal-compliance gap with rationale in the SUMMARY. This is an acceptable documented deviation, not a blocker.
  </action>
  <verify>`npm ls @react-navigation/native` shows exactly ONE version (rest deduped). package.json dependencies contains `@react-navigation/native` pinned exact. `npx jest` navigationTheme.test.ts green; expo-doctor + tsc pass; no new jest failures vs baseline.</verify>
  <done>@react-navigation/native is an explicit direct dep with single-version resolution; theming verified green; all local gates pass relative to baseline. (Or: documented codemod-only fallback with rationale.)</done>
</task>

<task type="auto">
  <name>Task 3: Green EAS Android cloud build gate + commit the hop</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
Only after Tasks 1-2 local gates are green.

1. From `apps/mobile/`: `eas build --profile preview --platform android --non-interactive` (authenticated; do NOT pass `--no-wait` — must return a FINISHED/green build). The build also serves as the runtime confirmation that the react-navigation fork + explicit dep did not break the app.
2. Confirm status `finished` (green). A queued/in-progress/errored/failed build does NOT satisfy the gate.
3. On green: commit as its own boundary, e.g. `feat(22-03): bump apps/mobile to Expo SDK 56 + react-navigation fork migration (hop 2/3, green EAS Android build)`. This is the rollback point before hop 3. Do NOT proceed to Plan 04 unless green and committed.

This is 2 of 3 required EAS builds for the phase — no extra builds.
  </action>
  <verify>EAS preview/android build status = finished (green). `git log --oneline -1` shows the hop-2 commit. package.json shows expo 56.x with @react-navigation/native direct dep.</verify>
  <done>Green EAS Android build for SDK 56 (with fork migration + explicit dep) completed and committed as an isolated boundary.</done>
</task>

</tasks>

<verification>
- expo resolves to 56.x; no app-code @react-navigation/* import remains (only expo-router/react-navigation).
- @react-navigation/native is an explicit direct dep; `npm ls` shows a single resolved version.
- expo-doctor, tsc, jest all green vs baseline; navigationTheme.test.ts green.
- EAS Android preview build status = finished (green); hop committed.
</verification>

<success_criteria>
apps/mobile runs on Expo SDK 56 with the react-navigation fork handled (imports rewritten) and `@react-navigation/native` as an explicit single-version direct dependency (MOBILE-03); all four gates passed in order; the hop is an isolated green commit.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-03-SUMMARY.md` recording: resolved expo/RN/@react-navigation/native versions, confirmation the 2 imports were rewritten (codemod vs manual), the `npm ls` single-version proof, theming verification result, the EAS build id/URL + green status, the commit hash, and — if the codemod-only fallback was taken — the SC3 literal-compliance gap rationale.
</output>
