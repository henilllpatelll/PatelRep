---
phase: 22-expo-sdk-54-57-bump
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
autonomous: true

must_haves:
  truths:
    - "apps/mobile is confirmed running expo@57.0.9 exactly (the roadmap's literal pin, NOT 57.0.10) with peers aligned by --fix"
    - "The 56→57 hop passes expo-doctor, tsc --noEmit, and jest (no new failures vs the Plan 01 baseline), then produces a GREEN EAS Android cloud build, committed as its own boundary"
    - "@react-navigation/native remains an explicit direct dependency with single-version resolution after the bump"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "expo pinned exactly to 57.0.9"
      contains: "57.0.9"
  key_links:
    - from: "npx expo install expo@57.0.9 --fix"
      to: "expo exactly 57.0.9 with aligned peers"
      via: "exact-version install"
      pattern: "57\\.0\\.9"
---

<objective>
Execute hop 3 of 3: upgrade `apps/mobile` from SDK 56 to SDK 57, pinned EXACTLY to `57.0.9` (RN 0.86). SDK 57 is explicitly a no-breaking-change release (0.85→0.86), so this hop is straightforward — but the same four gates apply and it must land a green EAS Android build as its own commit.

Per locked decision: target exactly `57.0.9`, NOT the newer `57.0.10` (which is `latest`/`next`). This satisfies the SC4 "confirmed running expo@57.0.9" requirement.

Purpose: Complete the SDK bump to the roadmap-pinned version with the same gate discipline.
Output: apps/mobile on expo 57.0.9, green EAS Android build, committed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-expo-sdk-54-57-bump/22-RESEARCH.md
@.planning/phases/22-expo-sdk-54-57-bump/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade to expo@57.0.9 exactly, pass local gates</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
All commands from `apps/mobile/`.

1. `npx expo install expo@57.0.9 --fix` — pin the EXACT patch (not `^57` / not 57.0.10) and align peers (RN 0.86 etc.). SDK 57 has no breaking changes, so no code rewrites are expected.
2. Confirm `@react-navigation/native` is still an explicit direct dep and still resolves to a SINGLE version after the bump (`npm ls @react-navigation/native`); if the SDK-57 --fix moved expo-router's internal pin, re-align the explicit pin to keep the tree deduped to one copy.
3. Keep the `dynamic-import-node` babel plugin intact; if `--fix` bumped `@supabase/supabase-js`, re-confirm no Hermes dynamic-import regression (the plugin must still be applied).
4. Local gates IN ORDER:
   - `npx expo-doctor@latest` — resolve flags with `npx expo install <pkg> --fix`.
   - `npx tsc --noEmit` — exit 0.
   - `npx jest` — no new failures vs the Plan 01 baseline.
  </action>
  <verify>`npx expo --version` reports `57.0.9` (and package.json shows `57.0.9`, not a range). expo-doctor passes; tsc exits 0; jest shows no new failures vs baseline. `npm ls @react-navigation/native` shows a single version.</verify>
  <done>expo is exactly 57.0.9 with aligned peers; @react-navigation/native single-version direct dep intact; all three local gates green vs baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Green EAS Android cloud build gate + commit the final hop</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
Only after Task 1's local gates are green.

1. From `apps/mobile/`: `eas build --profile preview --platform android --non-interactive` (authenticated; do NOT pass `--no-wait` — must return a FINISHED/green build).
2. Confirm status `finished` (green). Queued/in-progress/errored/failed does NOT satisfy the gate.
3. On green: commit as its own boundary, e.g. `feat(22-04): bump apps/mobile to Expo SDK 57.0.9 (hop 3/3, green EAS Android build)`.

This is 3 of 3 required EAS builds for the phase — no extra builds. npm audit resolution (MOBILE-04) is handled in Plan 05, which runs after this hop is green.
  </action>
  <verify>EAS preview/android build status = finished (green). `git log --oneline -1` shows the hop-3 commit. package.json shows expo 57.0.9.</verify>
  <done>Green EAS Android build for SDK 57.0.9 completed and committed as an isolated boundary; apps/mobile confirmed on expo@57.0.9.</done>
</task>

</tasks>

<verification>
- `npx expo --version` = 57.0.9; expo-doctor, tsc, jest green vs baseline.
- @react-navigation/native single-version direct dep intact; babel dynamic-import-node plugin present.
- EAS Android preview build status = finished (green); final hop committed.
</verification>

<success_criteria>
apps/mobile runs on Expo SDK 57.0.9 exactly (SC4 first half); all four gates (expo-doctor → tsc → jest → EAS build) passed in order; the hop is an isolated green commit.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-04-SUMMARY.md` recording: confirmed expo version (57.0.9), resolved RN version, @react-navigation/native single-version status, the EAS build id/URL + green status, and the commit hash.
</output>
