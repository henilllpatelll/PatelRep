---
phase: 22-expo-sdk-54-57-bump
plan: 06
type: execute
wave: 6
depends_on: ["22-05"]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/package-lock.json
  - .planning/phases/22-expo-sdk-54-57-bump/22-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [MOBILE-03]

must_haves:
  truths:
    - "The final Expo SDK 57 graph declares exact @react-navigation/native 7.3.14 as a direct production dependency in both the manifest and lockfile root"
    - "The manifest and lockfile change is committed as an explicit two-file rollback boundary before full local or EAS validation, and the EAS build metadata identifies that same commit hash"
    - "The direct dependency coexists with Expo Router's supported expo-router/react-navigation imports without changing the exact expo 57.0.9 pin or the scoped xcode-to-uuid 11.1.1 override"
    - "The changed graph passes focused navigation checks, the full mobile dependency/Doctor/type/Jest/audit gates, and one fresh finished EAS Android preview build"
    - "Phase re-verification records MOBILE-03 as closed at the first compatible SDK 57 boundary while preserving the historical SDK 56 Doctor incompatibility"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "Exact direct @react-navigation/native dependency on the preserved Expo 57.0.9 graph"
      contains: '"@react-navigation/native": "7.3.14"'
    - path: "apps/mobile/package-lock.json"
      provides: "Reproducible root dependency and installed resolution for @react-navigation/native 7.3.14"
      contains: '"@react-navigation/native": "7.3.14"'
    - path: ".planning/phases/22-expo-sdk-54-57-bump/22-VERIFICATION.md"
      provides: "Four-of-four Phase 22 re-verification evidence including the fresh Android artifact"
      contains: "status: passed"
  key_links:
    - from: "apps/mobile/package.json"
      to: "apps/mobile/package-lock.json"
      via: "exact npm install and root lockfile dependency"
      pattern: '"@react-navigation/native": "7\\.3\\.14"'
    - from: "apps/mobile/app/_layout.tsx"
      to: "expo-router/react-navigation"
      via: "SDK 56+ navigation provider import remains on the Router fork"
      pattern: 'from "expo-router/react-navigation"'
    - from: "apps/mobile/package-lock.json"
      to: "fresh EAS Android preview artifact"
      via: "one post-change EAS build whose gitCommitHash equals the recorded two-file dependency rollback commit"
      pattern: "@react-navigation/native"
---

<objective>
Close the sole Phase 22 verification gap by adding exact `@react-navigation/native@7.3.14` to the final Expo SDK 57.0.9 dependency graph, then re-establishing the complete local and EAS Android validation boundary.

Purpose: Satisfy MOBILE-03 at the first Expo boundary where Doctor accepts the direct dependency, without disturbing the SDK 57 pin, the Router-fork application imports, or the zero-audit security posture.
Output: Updated manifest/lockfile, one fresh green Android preview artifact for the changed graph, and a passed Phase 22 re-verification report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-expo-sdk-54-57-bump/22-VERIFICATION.md
@.planning/phases/22-expo-sdk-54-57-bump/22-04-SUMMARY.md
@.planning/phases/22-expo-sdk-54-57-bump/22-05-SUMMARY.md
@apps/mobile/package.json
@apps/mobile/package-lock.json
@apps/mobile/app/_layout.tsx
@apps/mobile/lib/theme/navigationTheme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the exact direct navigation dependency and prove focused compatibility</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
Work from `apps/mobile/` and make only the dependency/lockfile change required by MOBILE-03.

1. Run `npm install --save-exact @react-navigation/native@7.3.14`. The package must land under `dependencies` (not `devDependencies`) and the root `packages[""]` entry in `package-lock.json` must also record exact `7.3.14`.
2. Do not change any application/test import to `@react-navigation/native`. Keep `app/_layout.tsx`, `lib/theme/navigationTheme.ts`, and `__tests__/lib/theme/navigationTheme.test.ts` on `expo-router/react-navigation`; the direct package closes the manifest contract and must not create a second application provider path.
3. Preserve all existing Phase 22 boundaries exactly: `expo` remains `57.0.9`, `expo.install.exclude` remains scoped to `expo`, and `overrides.xcode.uuid` remains `11.1.1`. Do not run `expo install --fix`, `npm audit fix`, or `npm audit fix --force`.
4. Assert the manifest and lock root with:
   - `node -e "const p=require('./package.json'); if(p.dependencies['@react-navigation/native']!=='7.3.14'||p.dependencies.expo!=='57.0.9'||p.overrides?.xcode?.uuid!=='11.1.1') process.exit(1)"`
   - `node -e "const p=require('./package-lock.json'); if(p.packages[''].dependencies['@react-navigation/native']!=='7.3.14') process.exit(1)"`
   - `npm ls @react-navigation/native --depth=0`
5. Run the focused compatibility gates: `npx jest __tests__/lib/theme/navigationTheme.test.ts --runInBand` and `npx expo-doctor@latest`. Also use `rg '@react-navigation/' app components lib __tests__` to confirm no external application import was introduced; the existing `expo-router/react-navigation` matches are expected.
6. After every Task 1 assertion is green, return to the repository root and create the rollback boundary before Task 2. Commit only `apps/mobile/package.json` and `apps/mobile/package-lock.json` with message `fix(22-06): pin exact React Navigation dependency`. Use an explicit two-file staging/commit list (or the GSD commit helper with exactly those `--files`); do not stage or commit any unrelated dirty file. Record `git rev-parse HEAD` as the dependency commit hash, verify `git show --format= --name-only HEAD` lists exactly those two files, and require `git diff --exit-code -- apps/mobile/package.json apps/mobile/package-lock.json` to be clean.

If install or a focused gate fails, stop new work, diagnose within these two dependency files, fix, and rerun every Task 1 assertion before continuing. Do not weaken Doctor or tests and do not change Expo, xcode/UUID, or Router-fork imports.
  </action>
  <verify>`npm ls @react-navigation/native --depth=0` resolves exactly `7.3.14`; both Node assertions exit 0; the focused navigation-theme suite and Expo Doctor pass; source/test imports remain on `expo-router/react-navigation`; HEAD is a clean commit containing exactly the two dependency files and its hash is recorded.</verify>
  <done>MOBILE-03's direct-dependency artifact exists reproducibly on the unchanged Expo 57.0.9 graph, passes focused SDK 57 checks, and is preserved in an explicit two-file rollback commit ready for artifact validation.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full mobile gate stack and produce one matching EAS artifact</name>
  <files>apps/mobile/package.json, apps/mobile/package-lock.json</files>
  <action>
Use the committed Task 1 graph as an immutable build boundary. Do not edit package or source files during this task; a failing check must be diagnosed and repaired before the ordered gate stack is restarted from the beginning.

1. At the repository root, capture `git rev-parse HEAD` as `DEPENDENCY_COMMIT`. Require `git show --format= --name-only $DEPENDENCY_COMMIT` to list exactly `apps/mobile/package.json` and `apps/mobile/package-lock.json`, and require both files to be clean. From `apps/mobile/`, run `npm ci` to prove that committed lockfile recreates the graph. Then run, in order:
   - `node -e "if(require('expo/package.json').version!=='57.0.9'||require('@react-navigation/native/package.json').version!=='7.3.14') process.exit(1)"`
   - `npm ls @react-navigation/native --depth=0`
   - `npm ls expo --depth=0`
   - `npm ls --all`
   - `npx expo-doctor@latest`
   - `npm run type-check`
   - `npx jest --silent`
   - `npm audit --json` (the vulnerability totals must remain zero in every severity)
2. Confirm `git diff --exit-code -- package.json package-lock.json` and confirm `git rev-parse HEAD` still equals `DEPENDENCY_COMMIT` before cloud submission, so the EAS upload uses the exact rollback boundary recorded by Task 1.
3. Submit exactly one fresh Android preview build. On this trusted Windows checkout, scope Git's local-clone workaround only to the EAS process: set process-level `GIT_CLONE_PROTECTION_ACTIVE=false` immediately before `npx eas-cli@latest build --platform android --profile preview --non-interactive --wait`, then remove/restore that environment variable immediately afterward.
4. Capture the new build ID. If the local waiter detaches or times out after submission, poll that same ID with `npx eas-cli@latest build:view <BUILD_ID> --json`; do not submit a duplicate. Persist until the build reaches a terminal state.
5. Require the build record to show `FINISHED`, platform `ANDROID`, profile `preview`, an APK artifact URL, and `gitCommitHash` equal to `DEPENDENCY_COMMIT`. After it finishes, rerun the two installed-version assertions, `npm ls --all`, and `npm audit --json` to confirm the build command did not drift the graph or audit state.

The known Jest open-handle/worker teardown warning is informational only if all suites/assertions pass and Jest exits 0; do not suppress it or weaken the gate.
  </action>
  <verify>`npm ci`, exact-version assertions, both depth-zero trees, `npm ls --all`, Doctor, type-check, full Jest, and zero-vulnerability audit all exit green; exactly one new EAS Android preview build reaches FINISHED, exposes an APK, and reports the same git commit hash as Task 1's two-file rollback boundary.</verify>
  <done>The recorded dependency commit has a complete green local gate record and a fresh Android cloud artifact cryptographically tied to the same repository boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Close the verification gap with current evidence</name>
  <files>.planning/phases/22-expo-sdk-54-57-bump/22-VERIFICATION.md</files>
  <action>
Update the existing verification report in place only after Tasks 1 and 2 are fully green.

1. Change frontmatter to the current verification timestamp, `status: passed`, `score: 4/4 must-haves verified`, and an empty gaps list.
2. Mark Observable Truth 3 and MOBILE-03 as satisfied using exact manifest, lockfile-root, installed-tree, focused-test, Doctor, and fresh EAS build evidence. State honestly that SDK 56 Doctor rejected the package and that the user-authorized gap closure added it at the first compatible SDK 57 boundary; do not rewrite or erase the original execution history.
3. Refresh the Current Final Gates table with the actual post-change results and add the new EAS build ID/status/artifact, the recorded Task 1 dependency commit hash, and explicit evidence that the EAS record's `gitCommitHash` equals it.
4. Preserve evidence that `expo@57.0.9`, `expo-router/react-navigation` imports, the scoped xcode/UUID override, `npm ls --all`, and the zero-vulnerability audit all remain intact. Update the gap summary to say there are no remaining Phase 22 gaps.

Do not mark verification passed if any local gate, exact-version assertion, dependency-tree check, audit check, or EAS build is non-green.
  </action>
  <verify>`22-VERIFICATION.md` reports `status: passed`, score 4/4, no gaps, MOBILE-03 satisfied, and includes the exact two-file dependency commit hash plus a fresh finished EAS Android build whose `gitCommitHash` matches it.</verify>
  <done>The sole Phase 22 verifier gap is closed with auditable current-state and cloud-build evidence; the report preserves the SDK 56 incompatibility history without treating it as a remaining blocker.</done>
</task>

</tasks>

<verification>
- `apps/mobile/package.json` and lockfile root both declare exact `@react-navigation/native: 7.3.14` under production dependencies.
- Application and focused-test imports remain on `expo-router/react-navigation`; no unsupported external provider path is introduced.
- Installed Expo remains exactly 57.0.9 and `overrides.xcode.uuid` remains exactly 11.1.1.
- Focused navigation test, `npm ci`, `npm ls --all`, Expo Doctor, type-check, full Jest, and zero-vulnerability npm audit all pass after the change.
- One new post-change EAS Android preview build is FINISHED with an APK, and no duplicate build was submitted.
- The EAS build's `gitCommitHash` equals the clean Task 1 rollback commit containing only `apps/mobile/package.json` and `apps/mobile/package-lock.json`.
- `22-VERIFICATION.md` is a truthful 4/4 passed re-verification with no remaining gaps.
</verification>

<success_criteria>
MOBILE-03 is closed on the final supported SDK 57.0.9 graph: the exact direct dependency is reproducible, the Router fork and security override are undisturbed, all local gates remain green, and a fresh matching Android artifact plus the verification report prove the phase goal end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/22-expo-sdk-54-57-bump/22-06-SUMMARY.md` recording the exact dependency/lockfile change, the two-file rollback commit hash, focused and full gate results, the new EAS build ID/APK and matching `gitCommitHash` evidence, confirmation that Expo 57.0.9/xcode UUID/audit-zero remained intact, and the final `22-VERIFICATION.md` 4/4 closure.
</output>
