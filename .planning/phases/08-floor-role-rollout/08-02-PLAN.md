---
phase: 08-floor-role-rollout
plan: 02
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/my-rooms/[roomId].tsx
  - apps/mobile/components/housekeeping/ChecklistSection.tsx
autonomous: true
requirements: [FLOOR-01]
must_haves:
  truths:
    - "My Rooms room detail renders all buttons/cards/badges/states via primitives (zero C. tokens)"
    - "All 11 Alert.alert calls in [roomId].tsx become non-blocking Toasts (none are confirm dialogs)"
    - "Room status updates, DND toggle, notes, blockers, decline-service, and damage-photo handlers keep their exact api/queue logic — only the feedback call changes"
    - "ChecklistSection (embedded in room detail) renders on primitives, zero C. tokens"
  artifacts:
    - path: "apps/mobile/app/(app)/my-rooms/[roomId].tsx"
      provides: "Room detail on primitives + Toast feedback"
      contains: "useToast"
    - path: "apps/mobile/components/housekeeping/ChecklistSection.tsx"
      provides: "Checklist section on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/my-rooms/[roomId].tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "Alert.alert → toast.error/info/success in each handler"
      pattern: "toast\\.(error|success|info)"
    - from: "apps/mobile/app/(app)/my-rooms/[roomId].tsx"
      to: "apps/mobile/components/housekeeping/ChecklistSection.tsx"
      via: "existing import (unchanged)"
      pattern: "ChecklistSection"
---

<objective>
Migrate the My Rooms room-detail screen (`my-rooms/[roomId].tsx`, 1137 lines, 112 `C.` refs, 43 TouchableOpacity, 11 Alert.alert) and its embedded `ChecklistSection.tsx` (326 lines, 23 `C.` refs) onto Phase 7 primitives. This is the largest file in the phase — split into two tasks by section for reviewability (the migration has no architectural seam, only a review seam).

Purpose: Room detail is where the housekeeper acts on a single room (status, DND, notes, blockers, decline-service, damage photo). All 11 Alert.alert calls are outcome-reporting (per D-04 + RESEARCH classification) → all convert to Toast; ZERO stay blocking. No change to any api/queue/offline logic.
Output: Room detail + ChecklistSection on primitives, RoomDetail test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-RESEARCH.md — cite per line, do NOT re-derive)
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
C.paper→theme.background · C.surface→theme.surface · C.surface2→theme.surfaceSubtle · C.surface3→theme.surfaceMuted
C.line→theme.border · C.line2→theme.borderSubtle
C.ink→theme.textPrimary · C.ink2→theme.textSecondary · C.ink3→theme.textMuted · **C.ink4→theme.textDisabled** (Wave 0)
C.primary→theme.primary · C.accent→theme.primaryAction · C.accentSoft→theme.primarySoft · C.accentLine→theme.primaryLine
C.brass→theme.accentBrass · **C.brassSoft→theme.accentBrassSoft** (Wave 0) · **C.brassLine→theme.accentBrassLine** (Wave 0)
C.clay→theme.accentClay · C.shell*→theme.shell.* (switch direct shellTokens import to theme.shell)
C.ready→theme.status.ready · C.caution→theme.status.pickup (NAME CHANGE) · C.alert→theme.status.dirty (NAME CHANGE)
C.info→theme.status.clean (NAME CHANGE) · C.occupied→theme.status.occupied · C.ooo→theme.status.outOfOrder
(soft/line variants follow the same key, e.g. C.alertLine→theme.status.dirtyLine)

## Button (components/ui/Button.tsx) — exact signature
interface ButtonProps { label: string; onPress: () => void; variant?: "primary"|"secondary"|"ghost"|"destructive"; size?: "sm"|"md"|"lg"; loading?: boolean; disabled?: boolean; icon?: IoniconsName; style?: StyleProp<ViewStyle>; }
// destructive resolves to theme.status.dirty internally — never pass inline hex. loading spinner replaces label in place.

## useToast (lib/theme/useToast.ts) — exact contract
const toast = useToast();  // import { useToast } from "@/lib/theme/useToast";
toast.success(msg: string): void;  // 3s   toast.error(msg: string): void;  // 5s   toast.info(msg: string): void;  // 3s
// All take an already-translated string — caller still owns t().

## StateBlock / StatusBadge signatures: see 08-01-PLAN.md interfaces block (identical).
</migration_reference>

<alert_classification>
## [roomId].tsx — 11 Alert.alert calls, ALL 11 → Toast, ZERO stay blocking (from 08-RESEARCH.md, verified per line)
| Line | What | Convert to |
|---|---|---|
| 351 | Status update failed | toast.error |
| 400 | Undo failed | toast.error |
| 407 | "Notes need connection" (offline block, not a choice) | toast.info |
| 425 | Save note failed | toast.error |
| 434 | "Blockers need connection" | toast.info |
| 452 | DND escalation notice (title-only, auto-continues to sendSupervisorEscalation) | toast.info — fire toast, then call sendSupervisorEscalation() directly (do NOT gate it behind a dismiss) |
| 462 | Report blocker failed | toast.error |
| 470 | "DND needs connection" | toast.info |
| 480 | Update DND failed | toast.error |
| 488 | "Service needs connection" | toast.info |
| 498 | Update service failed | toast.error |

RULE (RESEARCH "New pitfall"): change ONLY the feedback call (the `Alert.alert(...)` line, usually last in a try/catch). Do NOT alter the preceding `await api.*`/`enqueueAction` calls or early-return guards in the same handler. Pass the existing translated message string straight to the toast.
</alert_classification>

<interfaces>
## ChecklistSection is imported ONLY by my-rooms/[roomId].tsx (verified — its React call site; hence Wave 1, not Wave 5).
`import ChecklistSection from "@/components/housekeeping/ChecklistSection";` (line 27) — this import line does NOT change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate [roomId].tsx part A — header/status/primary actions/DND/decline-service</name>
  <files>apps/mobile/app/(app)/my-rooms/[roomId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/my-rooms/[roomId].tsx (full file — focus part A: top of file through the status/DND/decline-service handlers and their JSX; note the handlers updateRoomStatus, handleToggleDnd, decline-service around lines 351/452/480/488/498)
    - apps/mobile/components/ui/Button.tsx, apps/mobile/components/ui/StatusBadge.tsx, apps/mobile/components/ui/StateBlock.tsx
    - apps/mobile/lib/theme/useToast.ts
    - apps/mobile/components/shared/tokens.ts (confirm Wave 0 keys exist)
  </read_first>
  <action>
    1. Add imports: `import { useTheme } from "@/lib/theme/useTheme";` and `import { useToast } from "@/lib/theme/useToast";`. Add `const theme = useTheme(); const toast = useToast();` in the component body.
    2. PART A scope = the screen header, room-status display/actions, primary action buttons, DND toggle, and decline-service UI (roughly the first half of the file / first ~570 lines up to but not including notes/blockers/checklist/damage-photo which are Part B). Within this scope:
       - Replace every `C.*` color ref with its `theme.*` equivalent (map above). Keep `StyleSheet.create()` for layout; inline only colors.
       - Convert action `TouchableOpacity`+`ActivityIndicator` button patterns to `<Button label={...} onPress={...} loading={...} variant=... size=... icon=... />`. Use `variant="destructive"` for destructive actions (never inline hex), `variant="secondary"` for secondary.
       - Convert room-status chips to `<StatusBadge statusKey=... label={...} />` using the room StatusKey mapping (DIRTY→dirty, OCCUPIED→occupied, PICKUP→pickup, IN_PROGRESS→inProgress, CLEAN→clean, INSPECTED→ready, OOO/OUT_OF_ORDER/OUT_OF_SERVICE→outOfOrder).
       - Convert loading/empty/error blocks in this scope to `<StateBlock>`.
       - Convert the Part-A Alert.alert calls (lines 351, 452, 470, 480, 488, 498) per the alert_classification table above — ONLY the feedback line changes; the DND escalation at 452 fires `toast.info(...)` then calls `sendSupervisorEscalation()` directly.
    3. Do NOT touch `api.*`/`enqueueAction` calls, early-return offline guards, data-fetch `useEffect`s, or role branches beyond swapping the alert feedback line.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomDetail.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Part-A Alert.alert calls converted: after this task `grep -c "Alert.alert" apps/mobile/app/(app)/my-rooms/[roomId].tsx` is reduced from 11 toward the Part-B remainder (Part B handles 400/407/425/434/462) — no Part-A alert line remains
    - `grep -c "useToast" apps/mobile/app/(app)/my-rooms/[roomId].tsx` >= 1; `grep -c "useTheme" ...` >= 1
    - `grep -c "StatusBadge" apps/mobile/app/(app)/my-rooms/[roomId].tsx` >= 1
    - `npx jest __tests__/screens/RoomDetail.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no `api.*`/`enqueueAction`/fetch-`useEffect`/role-branch line changed (only feedback + presentation)
  </acceptance_criteria>
  <done>Part A of room detail on primitives; its 6 alerts are Toasts; status/actions/DND/decline-service render via Button/StatusBadge/StateBlock; RoomDetail test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate [roomId].tsx part B (notes/blockers/damage-photo) + ChecklistSection, drive C. refs to zero</name>
  <files>apps/mobile/app/(app)/my-rooms/[roomId].tsx, apps/mobile/components/housekeeping/ChecklistSection.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/my-rooms/[roomId].tsx (part B region: notes, blockers, decline-service remainder, damage-photo capture UI, and any remaining C. refs anywhere in the file)
    - apps/mobile/components/housekeeping/ChecklistSection.tsx (full — 326 lines, 23 C. refs; determine its pass/fail status vocabulary before assigning any StatusBadge key)
    - apps/mobile/components/ui/StatusBadge.tsx, Button.tsx, StateBlock.tsx
  </read_first>
  <action>
    1. In `[roomId].tsx` PART B (notes/blockers/damage-photo sections and any leftover C. refs anywhere in the file): finish the same conversions as Task 1 (C.*→theme.*, TouchableOpacity buttons→Button, chips→StatusBadge, state blocks→StateBlock). Convert the Part-B Alert.alert calls (lines 400, 407, 425, 434, 462) per alert_classification. AFTER this task, the file must have ZERO `C.` refs and ZERO `Alert.alert` calls (all 11 were feedback → Toast).
    2. In `ChecklistSection.tsx`: add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`. Replace all 23 `C.*` refs with `theme.*` equivalents (map above). Convert any button `TouchableOpacity` patterns to `<Button>`. For checklist pass/fail states: only use `<StatusBadge>` where the state maps cleanly to a StatusKey (e.g. a "pass"/ready state → `statusKey="ready"`); if the pass/fail vocabulary does NOT map to one of the 13 StatusKeys, keep the existing chip hand-rolled but route its colors through `theme.*` — do NOT force-fit an ill-matching StatusKey. ChecklistSection has ZERO Alert.alert (nothing to convert).
    3. Do NOT change ChecklistSection's props/exports or its submit/checklist logic — presentation only. Its import line in [roomId].tsx (line 27) is unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/RoomDetail.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs in room detail: `grep -c "C\." apps/mobile/app/(app)/my-rooms/[roomId].tsx` returns 0
    - Zero Alert.alert in room detail: `grep -c "Alert.alert" apps/mobile/app/(app)/my-rooms/[roomId].tsx` returns 0
    - Zero C. refs in ChecklistSection: `grep -c "C\." apps/mobile/components/housekeeping/ChecklistSection.tsx` returns 0
    - `grep -c "useTheme" apps/mobile/components/housekeeping/ChecklistSection.tsx` >= 1
    - No direct shellTokens color import in either file: `grep -c "shellTokens" <each file>` returns 0
    - `npx jest __tests__/screens/RoomDetail.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: ChecklistSection props/exports unchanged; no api/queue/submit logic altered in either file
  </acceptance_criteria>
  <done>Room detail + ChecklistSection fully on primitives, both at zero C. refs, [roomId].tsx at zero Alert.alert, RoomDetail test + type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API / offline queue (updateRoomStatus, handleToggleDnd, blockers, decline-service, damage-photo) | These handlers mix real state-transition logic with the Alert.alert being converted — highest regression risk in the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Elevation of Privilege / Information Disclosure | Converting an Alert.alert inside a handler accidentally alters the preceding `await api.*`/`enqueueAction`/offline-guard, dropping a role/tenant-scoped call or state transition | mitigate | alert_classification rule: change ONLY the feedback line; acceptance criteria + diff review confirm no `api.*`/`enqueueAction`/guard line changed; RoomDetail test asserts handler behavior |
| T-08-02-02 | Tampering | DND escalation (line 452) side effect `sendSupervisorEscalation()` dropped when its gating title-only alert becomes a Toast | mitigate | Locked pattern: fire `toast.info(...)` AND call `sendSupervisorEscalation()` together; acceptance/diff review |
| T-08-02-03 | Denial of Service | ChecklistSection status vocab force-fit to a wrong StatusKey breaks inspection display | accept | Explicit "do not force-fit" instruction; keep hand-rolled chip themed if no StatusKey matches |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/RoomDetail.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/my-rooms/[roomId].tsx` == 0 and `grep -c "C\." apps/mobile/components/housekeeping/ChecklistSection.tsx` == 0
- `grep -c "Alert.alert" apps/mobile/app/(app)/my-rooms/[roomId].tsx` == 0
</verification>

<success_criteria>
FLOOR-01 (detail half): housekeeper acts on a room via primitives + non-blocking Toast feedback; all state-changing handlers keep exact api/queue behavior; ChecklistSection embedded in detail is fully migrated; zero legacy C tokens/Alert.alert remain in the detail screen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-02-SUMMARY.md`.
</output>
