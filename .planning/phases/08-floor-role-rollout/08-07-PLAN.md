---
phase: 08-floor-role-rollout
plan: 07
type: execute
wave: 4
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/tasks/index.tsx
  - apps/mobile/components/tasks/TaskCard.tsx
autonomous: true
requirements: [FLOOR-04]
must_haves:
  truths:
    - "Tasks list renders loading/empty/error via StateBlock and colors via useTheme (zero C. refs)"
    - "TaskCard is rebuilt on Card with priority/status chips as StatusBadge and confirm/cancel as Button"
    - "Tasks data behavior (fetch, confirm/cancel/done actions) is unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/components/tasks/TaskCard.tsx"
      provides: "TaskCard rebuilt on Card + StatusBadge + Button"
      contains: "StatusBadge"
    - path: "apps/mobile/app/(app)/tasks/index.tsx"
      provides: "Tasks list on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/components/tasks/TaskCard.tsx"
      to: "apps/mobile/components/ui/Card.tsx"
      via: "outer View → Card (no Pressable needed — card shell isn't the tap target)"
      pattern: "<Card"
    - from: "apps/mobile/app/(app)/tasks/index.tsx"
      to: "apps/mobile/components/tasks/TaskCard.tsx"
      via: "list renders rebuilt card (props unchanged)"
      pattern: "TaskCard"
---

<objective>
Migrate the Tasks list (`tasks/index.tsx`, 382 lines, 15 `C.` refs, i18n-gate-exempt per D-06) and rebuild `TaskCard` (287 lines, 43 `C.` refs — D-08) onto Phase 7 primitives.

Purpose: FLOOR-04 — any floor role views/completes tasks on primitives; data behavior unchanged.
Output: Tasks list + TaskCard on primitives, TasksVariationA test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map + Card/StatusBadge/Button/StateBlock signatures: see 08-01/08-02 PLAN interfaces (identical).
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`

## TaskCard StatusKey mapping (verified in 08-RESEARCH.md against TaskCard.tsx conditionals):
priority "urgent"||"high"→urgent · status "in_progress"→inProgress · overdueMinutes != null→overdue

## TaskCard shell (TaskCard.tsx:68) is a plain <View>, NOT a TouchableOpacity — the card itself is NOT the tap target.
So the outer <View> is replaced by <Card style={[...]}> DIRECTLY (no Pressable-wraps-Card needed — unlike WorkOrderCard/RoomQueueCard).
Only the inner doneBtn (134-146) and confirmBtn/cancelBtn (152-158) are pressable.

## doneBtn is icon-only circular (38x38, borderRadius 19) — LOCKED DECISION: no primitive covers an icon-only
PRESSABLE button (Button requires a `label`; mobileHandoff's IconButton is display-only, has no onPress). Keep doneBtn
as a themed TouchableOpacity (icon-only, colors routed through theme.*). This preserves the existing circular look
(no visual redesign per phase boundary) and still removes all C. tokens. confirmBtn/cancelBtn DO convert to Button.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild TaskCard on Card + StatusBadge + Button (D-08)</name>
  <files>apps/mobile/components/tasks/TaskCard.tsx</files>
  <read_first>
    - apps/mobile/components/tasks/TaskCard.tsx (full — 287 lines, 43 C. refs; outer View 68, chips 100-110, overdue 60-65/123-124, doneBtn 134-146, confirmBtn/cancelBtn 152-158; uses C.brassSoft/brassLine for lost_found category)
    - apps/mobile/components/ui/Card.tsx, StatusBadge.tsx, Button.tsx
    - apps/mobile/components/shared/tokens.ts (Wave 0 keys)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace the outer `<View style={[styles.card, ...]}>` (line 68) with `<Card style={styles.cardLayoutOverrides}>` DIRECTLY (no Pressable — the card shell has no onPress). `styles.cardLayoutOverrides` re-applies only the non-uniform layout from the old `styles.card`; Card owns bg/border/radius/shadow.
    3. Convert priority/status chips (100-110) to StatusBadge: urgent→`statusKey="urgent"` label `priority.toUpperCase()`; in_progress→`statusKey="inProgress"` label `t("tasks.statusInProgress")`. Overdue (60-65/123-124) → `statusKey="overdue"`.
    4. Convert `confirmBtn`/`cancelBtn` (152-158) to Button: `<Button label={t("tasks.confirmYes")} onPress={onConfirm} variant="primary" size="sm" icon="checkmark" />` and `<Button label={t("common.cancel")} onPress={onCancel} variant="secondary" size="sm" />`.
    5. Keep `doneBtn` (134-146) as a themed TouchableOpacity (icon-only, no primitive fits) — route its colors through `theme.*`, remove all C. refs. Do NOT force it into Button (would require a label and lose the circular look).
    6. Replace all remaining `C.*` refs with `theme.*` (map above; `C.brassSoft/brassLine` for lost_found category tile → `theme.accentBrassSoft/accentBrassLine`; `C.ink4`→`theme.textDisabled`). Category tile + location chips have no StatusKey — keep hand-rolled, theme colors only.
    7. Do NOT change `onConfirm`/`onCancel`/`onDone`/props contracts — the list calls TaskCard unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/TasksVariationA.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/components/tasks/TaskCard.tsx` returns 0
    - `grep -c "<Card" apps/mobile/components/tasks/TaskCard.tsx` >= 1
    - `grep -c "StatusBadge" apps/mobile/components/tasks/TaskCard.tsx` >= 1
    - `grep -c "shellTokens" apps/mobile/components/tasks/TaskCard.tsx` returns 0
    - `npx jest __tests__/screens/TasksVariationA.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: onConfirm/onCancel/onDone/props unchanged
  </acceptance_criteria>
  <done>TaskCard rebuilt on Card, chips are StatusBadge, confirm/cancel are Button, doneBtn themed in place, zero C. refs, Tasks test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tasks/index.tsx to useTheme + StateBlock</name>
  <files>apps/mobile/app/(app)/tasks/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/tasks/index.tsx (full — 382 lines, 15 C. refs, i18n-gate-exempt; loading/empty/error blocks; renders TaskCard)
    - apps/mobile/components/ui/StateBlock.tsx
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 15 `C.*` refs with `theme.*` (map above; `C.ink4`→`theme.textDisabled`). Keep StyleSheet layout; inline colors. Switch direct `shellTokens` imports to `theme.shell.*`.
    3. Convert loading/empty/error blocks to `<StateBlock>` (reuse existing strings). D-06: tasks/index.tsx is i18n-gate-exempt — pass its existing (possibly hardcoded) strings through; do NOT add new t() wiring.
    4. Leave TaskCard call sites' props unchanged. Do NOT change task fetch, action wiring, or role/data logic — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/TasksVariationA.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/app/(app)/tasks/index.tsx` returns 0
    - `grep -c "shellTokens" apps/mobile/app/(app)/tasks/index.tsx` returns 0
    - `grep -c "useTheme" apps/mobile/app/(app)/tasks/index.tsx` >= 1; `grep -c "StateBlock" ...` >= 1
    - `npx jest __tests__/screens/TasksVariationA.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no fetch/action/role logic changed
  </acceptance_criteria>
  <done>tasks/index.tsx on primitives, zero C. refs, Tasks test + type-check + lint green, data behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (task fetch, confirm/cancel/done actions) | Task operations; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Information Disclosure | Restyling the Tasks list drops a role/tenant-scoped fetch or filter | mitigate | Presentation-only edits; TasksVariationA test asserts data behavior; diff review confirms fetch/role unchanged |
| T-08-07-02 | Tampering | TaskCard rebuild drops `onConfirm`/`onCancel`/`onDone` wiring | mitigate | Props kept unchanged; confirm/cancel Button + doneBtn onPress preserved; Tasks test exercises actions |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/TasksVariationA.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/tasks/index.tsx` == 0 and `grep -c "C\." apps/mobile/components/tasks/TaskCard.tsx` == 0
</verification>

<success_criteria>
FLOOR-04: any floor role views/completes tasks on primitives; TaskCard is Card+StatusBadge+Button (doneBtn themed in place); data behavior unchanged; zero legacy C tokens in both files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-07-SUMMARY.md`.
</output>
