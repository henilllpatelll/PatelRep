---
phase: 08-floor-role-rollout
plan: 08
type: execute
wave: 5
depends_on: ["08-00"]
files_modified:
  - apps/mobile/app/(app)/inspect/index.tsx
autonomous: true
requirements: [FLOOR-05]
must_haves:
  truths:
    - "Inspect screen renders queue/list/states/buttons via primitives (zero C. refs)"
    - "Both Alert.alert calls (submit-failed, reclean-failed) become Toasts"
    - "The existing text/checkbox fail-checklist is migrated as-is — NO new photo-capture behavior is added"
    - "Inspection-submission behavior is unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/app/(app)/inspect/index.tsx"
      provides: "Inspect screen on primitives + Toast feedback"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/inspect/index.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "outcome Alert.alert → toast.error"
      pattern: "toast\\.error"
---

<objective>
Migrate the Inspect screen (`inspect/index.tsx`, 668 lines, 64 `C.` refs, 2 Alert.alert) onto Phase 7 primitives. Split into two tasks by section for reviewability. Both alerts are outcome-reporting → Toast.

**Scope guard (user-resolved + RESEARCH A1):** FLOOR-05's success criterion mentions a "photo-on-fail prompt," but `inspect/index.tsx`'s fail flow is TEXT/CHECKBOX only today — there is ZERO ImagePicker/photo-capture code in this screen. Migrate the EXISTING fail-checklist UI onto primitives as-is. Do NOT build new photo-capture functionality (that would violate the phase's "no change to inspection-submission behavior" boundary). `ChecklistSection.tsx` is NOT in this plan — its only import site is `my-rooms/[roomId].tsx`, so it was migrated in Wave 1 (plan 08-02).

Purpose: FLOOR-05 — any floor role completes an inspection on primitives; submission behavior unchanged.
Output: inspect/index.tsx on primitives, InspectorQueue test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-CONTEXT.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map + Button/StatusBadge/StateBlock/useToast signatures: see 08-02-PLAN.md (identical).
Import: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` + `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
Room StatusKey mapping (for any room-status chips shown in the queue): DIRTY→dirty · OCCUPIED→occupied · PICKUP→pickup · IN_PROGRESS→inProgress · CLEAN→clean · INSPECTED→ready · OOO/OUT_OF_ORDER/OUT_OF_SERVICE→outOfOrder.
</migration_reference>

<alert_classification>
## inspect/index.tsx — 2 Alert.alert, BOTH → Toast (from 08-RESEARCH.md, verified)
| Line | What | Action |
|---|---|---|
| 176 | Submit inspection failed | toast.error |
| 346 | Reclean failed | toast.error |
Change ONLY the feedback line; do not alter the preceding submit/reclean api/queue calls or guards.
</alert_classification>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate inspect/index.tsx part A — queue/list/summary/states + submit action</name>
  <files>apps/mobile/app/(app)/inspect/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/inspect/index.tsx (full — Part A: header/kicker/summary, the to-inspect/done queue list, loading/empty/error states, and the submit handler around line 176)
    - apps/mobile/components/ui/Button.tsx, StatusBadge.tsx, StateBlock.tsx
    - apps/mobile/lib/theme/useToast.ts
    - apps/mobile/components/shared/tokens.ts (Wave 0 keys)
  </read_first>
  <action>
    1. Add useTheme + useToast imports/calls.
    2. PART A scope = header/summary, the inspection queue list (to-inspect / done tabs), loading/empty/error blocks, and the submit-inspection action. Within scope:
       - Replace `C.*` refs with `theme.*` (map; `C.ink4`→`theme.textDisabled`). Keep StyleSheet layout; inline colors. Switch direct `shellTokens` imports to `theme.shell.*`.
       - Convert queue loading/empty/error blocks to `<StateBlock>` (reuse existing `t("inspect.*")` keys, e.g. `t("inspect.queueEmpty")`).
       - Convert action `TouchableOpacity`+`ActivityIndicator` buttons to `<Button>`.
       - Convert any room-status chips in the queue to `<StatusBadge>` per the room StatusKey mapping.
       - Convert the submit-failed alert (line 176) → `toast.error(...)`.
    3. Do NOT alter the submit/api/queue logic or offline guards beyond the feedback line. Do NOT add photo-capture code.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/InspectorQueue.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useToast" apps/mobile/app/(app)/inspect/index.tsx` >= 1; `grep -c "useTheme" ...` >= 1
    - `grep -c "StateBlock" apps/mobile/app/(app)/inspect/index.tsx` >= 1
    - After this task at least one Alert.alert converted: `grep -c "Alert.alert" apps/mobile/app/(app)/inspect/index.tsx` <= 1
    - `npx jest __tests__/screens/InspectorQueue.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: no submit/api/guard line changed; no ImagePicker/photo code added
  </acceptance_criteria>
  <done>Inspect Part A on primitives; queue/states/submit render via StateBlock/Button/StatusBadge; submit-failed alert is a Toast; InspectorQueue test green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate inspect/index.tsx part B (fail-checklist + reclean), drive C. refs to zero</name>
  <files>apps/mobile/app/(app)/inspect/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/inspect/index.tsx (Part B region: the fail-checklist UI — failChecklistLabel, checkbox rows, TextInput notes — and the reclean handler around line 346; plus any remaining C. refs anywhere in the file)
    - apps/mobile/components/ui/Button.tsx, StateBlock.tsx
  </read_first>
  <action>
    1. Migrate the fail-checklist section (text/checkbox + notes TextInput) and the reclean UI: `C.*`→`theme.*`, `TouchableOpacity` buttons→`<Button>`, any state blocks→`<StateBlock>`. Migrate the EXISTING checklist UI as-is — do NOT add photo capture, ImagePicker, or a "photo-on-fail" upload flow (out of scope per phase boundary + user decision).
    2. Convert the reclean-failed alert (line 346) → `toast.error(...)`.
    3. Finish any leftover `C.*` refs anywhere in the file. AFTER this task: ZERO `C.` refs and ZERO `Alert.alert` (both were feedback → Toast).
    4. Do NOT alter reclean/submit api/queue logic or guards beyond the feedback line.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/InspectorQueue.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `grep -c "C\." apps/mobile/app/(app)/inspect/index.tsx` returns 0
    - Zero Alert.alert: `grep -c "Alert.alert" apps/mobile/app/(app)/inspect/index.tsx` returns 0
    - `grep -c "shellTokens" apps/mobile/app/(app)/inspect/index.tsx` returns 0
    - No new photo-capture code: `grep -cE "ImagePicker|expo-image-picker|requires_photo" apps/mobile/app/(app)/inspect/index.tsx` returns 0
    - `npx jest __tests__/screens/InspectorQueue.test.tsx` passes; `npm run type-check` + `npm run lint` pass
    - Diff review: fail-checklist is the same text/checkbox UI (migrated look only); no submission-logic change
  </acceptance_criteria>
  <done>Inspect fully on primitives, zero C. refs / zero Alert.alert, fail-checklist migrated as-is (no photo capture added), InspectorQueue test + type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API / offline queue (submit inspection, reclean) | Inspection-submission logic; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering / Repudiation | Converting the submit/reclean alert accidentally alters the inspection-submission api/queue call, corrupting the evidence record | mitigate | "change only feedback line" rule; acceptance criteria + diff review; InspectorQueue test asserts submission behavior |
| T-08-08-02 | Denial of Service (scope) | Silently adding photo-capture (misreading FLOOR-05's "photo-on-fail") introduces new, untested behavior into a migration-only phase | mitigate | Explicit "do NOT add ImagePicker/photo code" instruction + grep acceptance criterion asserting no ImagePicker/requires_photo added |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/InspectorQueue.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `grep -c "C\." apps/mobile/app/(app)/inspect/index.tsx` == 0
- `grep -c "Alert.alert" apps/mobile/app/(app)/inspect/index.tsx` == 0
- `grep -cE "ImagePicker|expo-image-picker" apps/mobile/app/(app)/inspect/index.tsx` == 0
</verification>

<success_criteria>
FLOOR-05: any floor role completes an inspection on primitives; the existing text/checkbox fail-checklist is migrated as-is with no new photo-capture behavior; inspection-submission behavior unchanged; zero legacy C tokens / Alert.alert in the Inspect screen.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-08-SUMMARY.md`.
</output>
