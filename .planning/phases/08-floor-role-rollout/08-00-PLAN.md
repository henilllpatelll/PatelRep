---
phase: 08-floor-role-rollout
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/components/shared/tokens.ts
autonomous: true
requirements: [FLOOR-01, FLOOR-02, FLOOR-03, FLOOR-04, FLOOR-05]
must_haves:
  truths:
    - "useTheme() returns a value for the muted-disabled text tier that C.ink4 provided (#B7AA99 in light)"
    - "useTheme() returns values for the brass soft/line accent family that C.brassSoft/C.brassLine provided"
    - "No existing C.*, lightTheme.*, or darkTheme.* key is renamed or removed"
  artifacts:
    - path: "apps/mobile/components/shared/tokens.ts"
      provides: "textDisabled + accentBrassSoft + accentBrassLine keys on lightTheme and darkTheme"
      contains: "textDisabled"
  key_links:
    - from: "apps/mobile/lib/theme/useTheme.ts"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "getThemeTokens(mode) returns lightTheme/darkTheme including the new keys"
      pattern: "textDisabled|accentBrassSoft|accentBrassLine"
---

<objective>
Add the three color keys that exist today ONLY as hardcoded literals inside the flat `C` compatibility object (`C.ink4`, `C.brassSoft`, `C.brassLine`) to the reactive `lightTheme`/`darkTheme` objects, so `useTheme()` has a path to them. D-03 requires ZERO `C.*` usage remaining in the 5 floor screens after Phase 8 closes — and these three colors are used pervasively across every in-scope file — but `useTheme()` currently cannot produce them, so full migration is impossible without this additive change.

Purpose: Unblock every Wave 1-5 screen/component migration. This is a data-only addition (no new primitive, no new provider), consistent with Phase 7's "tokens.ts is data, keep in place" precedent.
Output: `lightTheme` and `darkTheme` each gain `textDisabled`, `accentBrassSoft`, `accentBrassLine`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-floor-role-rollout/08-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<interfaces>
<!-- Current state of the three literal-only keys in the flat C object (tokens.ts:127,133-134): -->
  ink4: "#B7AA99",
  brassSoft: "#F4E7C6",
  brassLine: "#E2C679",

<!-- Current lightTheme accent block (tokens.ts:81-82) — the new keys sit alongside these: -->
  accentBrass: "#C29A4A",
  accentClay: "#E8B89A",

<!-- Current darkTheme accent block (tokens.ts:102-103): -->
  accentBrass: "#D0A85A",   // rgb(208,168,90)
  accentClay: "#C98262",

<!-- darkTheme has no brass soft/line today; follow the alpha-fill pattern already used by darkStatusTokens (rgba softened variants). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add textDisabled + brass soft/line keys to lightTheme and darkTheme</name>
  <files>apps/mobile/components/shared/tokens.ts</files>
  <read_first>
    - apps/mobile/components/shared/tokens.ts (see current lightTheme lines 67-86, darkTheme lines 88-109, and the flat C object lines 117-168 — the source of the three literal hexes being promoted)
    - apps/mobile/__tests__/components/MobileVisualTokens.test.ts (confirm it does NOT assert an exact/frozen theme-object key set that adding 3 keys would break; if it snapshots the object, this task must update that snapshot in the same commit)
  </read_first>
  <action>
    Edit `apps/mobile/components/shared/tokens.ts`. Add exactly these three keys to BOTH `lightTheme` and `darkTheme`, placed immediately after the existing `accentClay` line in each object (do NOT touch or reorder any existing key):

    In `lightTheme` (after `accentClay: "#E8B89A",` at line ~82):
    ```
      textDisabled: "#B7AA99",
      accentBrassSoft: "#F4E7C6",
      accentBrassLine: "#E2C679",
    ```
    (These mirror the exact hexes currently living only in the flat `C` object: `C.ink4="#B7AA99"`, `C.brassSoft="#F4E7C6"`, `C.brassLine="#E2C679"`.)

    In `darkTheme` (after `accentClay: "#C98262",` at line ~103):
    ```
      textDisabled: "#6E685E",
      accentBrassSoft: "rgba(208, 168, 90, 0.20)",
      accentBrassLine: "rgba(208, 168, 90, 0.40)",
    ```
    (Dark values follow the alpha-fill pattern already used by `darkStatusTokens`/`darkAiTokens`. `textDisabled` is a dimmer tier below darkTheme's `textMuted: "#918A7E"`. The brass rgba base `208,168,90` = darkTheme's `accentBrass: "#D0A85A"`.)

    This is ADDITIVE ONLY. Do NOT rename `ink4`/`brassSoft`/`brassLine` in the flat `C` object, do NOT remove them, do NOT change any existing `lightTheme`/`darkTheme` value. The flat `C` object stays exactly as-is this task (screens stop referencing it in later waves; it is not deleted in Phase 8).
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest MobileVisualTokens.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "textDisabled" apps/mobile/components/shared/tokens.ts` returns 2 (one in lightTheme, one in darkTheme)
    - `grep -c "accentBrassSoft" apps/mobile/components/shared/tokens.ts` returns 2
    - `grep -c "accentBrassLine" apps/mobile/components/shared/tokens.ts` returns 2
    - `grep -n 'ink4: "#B7AA99"' apps/mobile/components/shared/tokens.ts` still matches (flat C object unchanged)
    - `grep -n 'brassSoft: "#F4E7C6"' apps/mobile/components/shared/tokens.ts` still matches
    - `cd apps/mobile && npx jest MobileVisualTokens.test.ts` passes
    - `cd apps/mobile && npm run type-check` passes (tsc --noEmit, no errors)
  </acceptance_criteria>
  <done>Both lightTheme and darkTheme expose textDisabled/accentBrassSoft/accentBrassLine; flat C object and all pre-existing keys are byte-for-byte unchanged; visual-tokens test + type-check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process design-token constant; no untrusted input, no network, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | `tokens.ts` — accidental rename/removal of an existing `C.*`/theme key while adding new ones | mitigate | Additive-only edit; acceptance criteria grep-assert the pre-existing `ink4`/`brassSoft` literals still present; type-check catches any downstream key break across the app |
| T-08-00-02 | Denial of Service | Snapshot/shape test breaks on new keys, blocking the whole phase | accept | `read_first` requires reading `MobileVisualTokens.test.ts` first; if it snapshots shape, the task updates the snapshot in the same commit — low risk, single-file blast radius |
</threat_model>

<verification>
- `cd apps/mobile && npx jest MobileVisualTokens.test.ts` green
- `cd apps/mobile && npm run type-check` green
- No diff outside `apps/mobile/components/shared/tokens.ts` (and possibly its one snapshot test)
</verification>

<success_criteria>
`useTheme()` (via `getThemeTokens`) returns `textDisabled`, `accentBrassSoft`, `accentBrassLine` in both modes; all Phase 8 screen migrations can now route `C.ink4`/`C.brassSoft`/`C.brassLine` through `theme.textDisabled`/`theme.accentBrassSoft`/`theme.accentBrassLine`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-floor-role-rollout/08-00-SUMMARY.md`.
</output>
