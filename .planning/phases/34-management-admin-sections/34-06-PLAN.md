---
phase: 34-management-admin-sections
plan: 06
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/app/(dashboard)/ai/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of ai/page.tsx. No sibling wave-2 plan
# touches it. AI Copilot is Shape 3 (chat UI, per RESEARCH.md) — fundamentally different in kind
# from the other 9 sections. AICopilotBubble.tsx (the global floating widget) is a SEPARATE,
# OUT-OF-SCOPE component per CONTEXT — do not touch it, do not modify any file it imports beyond
# what's already imported by ai/page.tsx itself. ConfirmView/TaskConfirmView/AiMessage/
# CreditUsageCard are all local function components defined inline in ai/page.tsx (confirmed: no
# separate files exist for any of them). Does NOT edit en.ts/es.ts — the `aiCopilot` namespace
# exists from 34-01; only consumes it.

must_haves:
  truths:
    - "With the 'aiCopilot' flag ON, the chat shell/composer chrome uses v2 tokens (duration-fast/ease-standard/focus-visible ring); flag OFF is byte-behaviorally identical to today"
    - "CreditUsageCard's useQuery gains a loading state (a v2 skeleton while its query is loading); its pre-existing hardcoded $0.00/0% rendered values are NOT changed on error or empty data — zero behavior change to what those values display"
    - "The chat error-as-conversation pattern (API errors become an AI-role chat bubble via existing err.message/fallback text) is completely unchanged — no page-level StateBlock is introduced for chat errors"
    - "The typing/loading indicator (motion-safe:animate-bounce dots) and ConfirmView/TaskConfirmView's Button loading prop usage are confirmed unchanged in behavior, only token-styled if any hardcoded transition class remains"
  artifacts:
    - path: "apps/web/app/(dashboard)/ai/page.tsx"
      provides: "Reads isSectionRedesigned('aiCopilot', hotel) once; v2-token-ifies chat shell/composer chrome; CreditUsageCard gains a v2 loading skeleton without changing its rendered values; chat error-as-message pattern and typing indicator unchanged; legacy branch unchanged"
      contains: "isSectionRedesigned('aiCopilot'"
  key_links:
    - from: "apps/web/app/(dashboard)/ai/page.tsx (CreditUsageCard)"
      to: "the ai-risk-alerts useQuery"
      via: "isLoading destructured and used ONLY to gate a v2 skeleton overlay; rendered $0.00/0% values remain hardcoded regardless of isError/data, per zero-behavior-change"
      pattern: "aiCopilot.creditUsage"
---

<objective>
Redesign AI Copilot (`app/(dashboard)/ai/page.tsx`, 492 lines) — the phase's one non-list, non-CRUD section, a chat interface where the standard loading/empty/error triad doesn't map cleanly (RESEARCH.md Shape 3). Scope is deliberately narrow: (a) add `isSectionRedesigned('aiCopilot', hotel)` and v2-token-ify the chat shell/composer chrome (message list container, input, send button — currently using whatever mix of hardcoded/token classes exists today), (b) give `CreditUsageCard`'s query (currently destructures only `{ data }`, no loading/error handling, and renders hardcoded `$0.00`/`0%` regardless of what `data` contains) a loading skeleton WITHOUT touching its pre-existing hardcoded-value rendering — that incompleteness is a known, deliberate-per-CONTEXT non-fix (zero behavior change means the numbers must render exactly as they do today, success or failure), (c) confirm — not rebuild — that the chat's existing error-as-message pattern, the typing indicator, and `ConfirmView`/`TaskConfirmView`'s use of `Button`'s frozen `loading` prop all remain functionally untouched.

Purpose: SEC-01b for AI Copilot — the phase's example of "narrow scope, high discipline about NOT expanding beyond loading/empty/error into unrelated fixes."
Output: ai/page.tsx flag-gated, chat shell v2-token-ified, CreditUsageCard's query loading-gated without altering its output.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

@apps/web/app/(dashboard)/ai/page.tsx

# Flag + state + primitives contracts (import-only; Button.tsx frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag + v2-token-ify chat shell/composer chrome; confirm error-as-message and typing indicator unchanged</name>
  <files>apps/web/app/(dashboard)/ai/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('aiCopilot', hotel)`.
2. V2-token-ify the chat shell/composer chrome under `v2` (message list scroll container border/background, text input focus ring, send button hover/active/disabled states) using the established v2 tokens (`duration-fast`, `ease-standard`, `focus-visible:ring-[var(--focus-ring)]`, `bg-brand`, `--brand-soft`) — keep legacy classes when `!v2`. Do not restructure the DOM/layout, only className treatment.
3. `PageHeader` already used (`eyebrow="Intelligence" title="Copilot"`) — add `dataI18nSkip={v2}` per Pitfall 1 if this title/subtitle pairing is new/changing under v2; if the header text itself is unchanged from today, this is still worth setting proactively since the namespace (`aiCopilot`) is brand-new to i18n even if the visible text stays the same.
4. Confirm (read the live code, do not assume) that `sendMessage()`'s catch block — which turns API errors into an AI-role chat bubble via `addAiMsg(err instanceof ApiClientError ? err.message : 'Something went wrong...')` — is completely untouched. Do NOT introduce a page-level `StateBlock` for chat errors; this is a deliberate, existing UX pattern (errors become conversation, not a page-level interrupt) that must be preserved unchanged per CONTEXT.
5. Confirm the typing/loading indicator (`loading &&` block, three `motion-safe:animate-bounce` dots with `bg-ai`, staggered `[animation-delay:Nms]`) is already token-styled and respects `prefers-reduced-motion` — only add `duration-fast`/`ease-standard` to its CONTAINER if any transition class on that container is still hardcoded; do not touch the dots' own animation classes (already correct).
6. Confirm `ConfirmView`/`TaskConfirmView` use `Button`'s own `loading` prop (frozen primitive) for their "Confirm & Create" saving state — do not add any bespoke loading UI here; if their surrounding chrome (borders/spacing/text color) uses hardcoded classes, v2-token-ify only that surrounding chrome under `v2`, never `Button.tsx` itself.
  </action>
  <verify>cd apps/web && npm run type-check succeeds on this file in isolation; grep confirms `isSectionRedesigned('aiCopilot'` present; grep confirms `sendMessage`'s catch block still calls `addAiMsg` with the same error-to-message logic (unchanged); grep confirms no new `StateBlock` import/usage was added for chat-level errors; grep confirms the typing indicator's `motion-safe:animate-bounce` classes are unchanged; grep confirms `Button` (not a new custom spinner) still used inside ConfirmView/TaskConfirmView for the saving state</verify>
  <done>AI Copilot reads the flag once; chat shell/composer chrome v2-token-ified; chat error-as-message pattern, typing indicator, and ConfirmView/TaskConfirmView's Button-loading usage all confirmed unchanged; legacy branch unchanged flag-off</done>
</task>

<task type="auto">
  <name>Task 2: CreditUsageCard — add loading skeleton without altering its hardcoded rendered values</name>
  <files>apps/web/app/(dashboard)/ai/page.tsx</files>
  <action>
1. `CreditUsageCard` (line ~204) currently: `useQuery({ queryKey: ['ai-risk-alerts'], queryFn: () => aiApi.getRiskAlerts().then(r => r.data), refetchInterval: 60_000 })` destructuring ONLY `{ data }`. Extend the destructuring to `{ data, isLoading }` — do NOT add `isError` handling that changes what renders (see step 2).
2. Under `v2`, while `isLoading` is true, render a small v2 skeleton (`Skeleton` component, card-sized) in place of the card's content. Once loading completes (success OR failure OR partial data — `isLoading` false in all these cases since no `isError` branch is added), render EXACTLY what renders today: the hardcoded `$0.00` credit-spend value, hardcoded `0%` of-cap value, and the "X AI queries" line that DOES read `data` for its count. Do not add error-specific UI, a StateBlock, or any conditional that changes the $0.00/0%/query-count values based on success vs. failure — this preserves the pre-existing incompleteness exactly (CONTEXT: zero behavior change; this stub is a known, deliberate non-fix, not a Phase 34 bug).
3. When `!v2`, render exactly as today (no loading skeleton, immediate render of the hardcoded values + query count) — the `isLoading` field is simply unused in the legacy branch.
4. Do not touch `aiApi.getRiskAlerts()`, the query key, or the `refetchInterval`.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `CreditUsageCard`'s `useQuery` destructuring now includes `isLoading`; grep confirms NO `isError` destructured or referenced for this query; grep confirms the hardcoded `$0.00`/`0%` JSX literals are byte-unchanged from before this task (diff review); grep confirms a v2-gated `Skeleton` usage exists for `isLoading`; git status shows only ai/page.tsx changed (combined with Task 1)</verify>
  <done>CreditUsageCard's query destructuring extended with isLoading only; a v2 skeleton renders during loading; the pre-existing hardcoded $0.00/0% values and query-count display are byte-identical to before this task in both loading-complete states (success or failure); legacy branch (v2 false) completely unchanged; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Flag read once; chat shell/composer chrome v2-token-ified; error-as-chat-message pattern, typing indicator, and Button-loading usage in ConfirmView/TaskConfirmView all confirmed unchanged.
- CreditUsageCard gains a v2 loading skeleton only — its hardcoded $0.00/0% rendered values are byte-unchanged regardless of query success/failure.
- Only ai/page.tsx changed; no frozen file edited; no locale-file edit; AICopilotBubble.tsx untouched; no query/mutation logic change.
</verification>

<success_criteria>
AI Copilot renders in the new visual system behind its own flag, with chat shell/composer chrome v2-token-ified and CreditUsageCard given a loading skeleton, while its deliberate existing incompleteness (hardcoded credit-spend values) and its deliberate existing UX pattern (errors as chat messages, not page-level interrupts) are both provably unchanged — zero behavior change, narrowest-possible scope for this section's unique shape.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-06-SUMMARY.md` recording: exactly which chrome classes were v2-token-ified, confirmation (with the specific grep/diff evidence) that the chat error-as-message pattern and CreditUsageCard's hardcoded values are byte-unchanged, the `aiCopilot.*` keys actually consumed, confirmation AICopilotBubble.tsx was never touched, and any deferred item for the close-out list.
</output>
