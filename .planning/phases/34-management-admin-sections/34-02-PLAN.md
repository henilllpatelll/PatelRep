---
phase: 34-management-admin-sections
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/app/(dashboard)/reports/page.tsx
  - apps/web/app/(dashboard)/management-roi/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of reports/page.tsx and
# management-roi/page.tsx. No sibling wave-2 plan touches either file. Both are fully raw/legacy
# (Shape 1 per RESEARCH.md) multi-query pages with the same three pitfalls: (1) raw
# `animate-spin` auth-loading guards, (2) a mix of destructured vs. named-query-object useQuery
# styles that require different "wire onRetry" treatment, (3) imports of frozen `Stat`/`Pill`
# from primitives.tsx (Management ROI only) — consume only, never edit. Does NOT edit
# en.ts/es.ts — the `reports`/`managementRoi` namespaces exist from 34-01; only consumes them.

must_haves:
  truths:
    - "With the 'reports' flag ON, the Reports page's role-gated auth-loading guard is a skeleton (not a spinner), and each of the 5 tabs (DailySummaryTab, StaffPerformanceTab, MaintenanceTab, GuestRecoveryTab, AIUsageTab) renders its own loading/empty/error via a skeleton + shared StateBlock, with error onRetry calling that tab's own refetch(); flag OFF is byte-behaviorally identical to today"
    - "With the 'managementRoi' flag ON, the page's auth-loading guard is a skeleton, the 9-query stacked-error banner and single page-level EmptyState are StateBlock/i18n'd via managementRoi.*, and no query/aggregation logic changed; flag OFF is byte-behaviorally identical to today"
    - "The flag is read ONCE per page in the top-level component (`ReportsPage`, `ManagementRoiPage`) and threaded as a `redesigned`/`v2` prop into the 5 Reports tab components (Pattern 2)"
    - "No query/mutation/field/RBAC logic added or removed; Reports' 5 tabs extend their `useQuery` destructuring to include `refetch` (currently absent); Management ROI's 9 named query objects need NO destructuring change (`.refetch()` already callable)"
  artifacts:
    - path: "apps/web/app/(dashboard)/reports/page.tsx"
      provides: "Reads isSectionRedesigned('reports', hotel) once; threads redesigned to the 5 tabs; v2 skeleton for auth-loading; legacy branch unchanged"
      contains: "isSectionRedesigned('reports'"
    - path: "apps/web/app/(dashboard)/management-roi/page.tsx"
      provides: "Reads isSectionRedesigned('managementRoi', hotel) once; StateBlock-wires the stacked error banner and page EmptyState; v2 skeleton for auth-loading and per-Section loading; legacy branch unchanged"
      contains: "isSectionRedesigned('managementRoi'"
  key_links:
    - from: "apps/web/app/(dashboard)/reports/page.tsx"
      to: "the 5 tab components"
      via: "read flag once, pass redesigned={v2} prop to each tab"
      pattern: "isSectionRedesigned\\('reports'"
    - from: "each Reports tab's StateBlock error"
      to: "that tab's own useQuery"
      via: "error.onRetry = () => refetch() (refetch newly added to that tab's destructuring)"
      pattern: "refetch"
    - from: "apps/web/app/(dashboard)/management-roi/page.tsx"
      to: "the 9 named query objects"
      via: "StateBlock error.onRetry = () => <queryName>.refetch() per failing query in the stacked array"
      pattern: "isSectionRedesigned\\('managementRoi'"
---

<objective>
Redesign Reports and Management ROI — the two fully raw/legacy, multi-query, non-tab-cross-importing GM/admin pages (Shape 1 per RESEARCH.md) — each behind its own flag (`'reports'`, `'managementRoi'`). Reports uses Pattern 2 (flag read once in the thin-ish parent `page.tsx`, threaded as `redesigned` to its 5 independently-queried tab components); Management ROI is a single page with no sub-components, so the flag gates its own JSX directly. Replace each page's raw `animate-spin` auth-loading guard with a skeleton, replace Reports' 5 raw error divs + 3 bare-`<p>` empties with shared `StateBlock`/`EmptyState`, and replace Management ROI's stacked raw error divs + `EmptyState` copy with i18n'd, v2-token-styled equivalents — all wired to each query's own existing `refetch()`, no new query added.

Purpose: SEC-01b for Reports + Management ROI; establishes the flag-thread pattern for Phase 34's multi-tab section (mirrors Phase 33's Safety/Programs Pattern-2 precedent).
Output: reports/page.tsx (+ its 5 inline tab components) and management-roi/page.tsx, flag-gated and StateBlock/skeleton-wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

# Both target files (each renders inline tab/sub-components — no separate files exist)
@apps/web/app/(dashboard)/reports/page.tsx
@apps/web/app/(dashboard)/management-roi/page.tsx

# Flag + state + primitives contracts (import-only; primitives.tsx is frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
@apps/web/components/shared/PageHeader.tsx
@apps/web/components/ui/primitives.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reports — flag-thread to 5 tabs; skeleton auth-loading; StateBlock per-tab loading/empty/error</name>
  <files>apps/web/app/(dashboard)/reports/page.tsx</files>
  <action>
1. In `ReportsPage`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('reports', hotel)`. Replace the raw `animate-spin` auth-loading div with a v2 skeleton (`Skeleton` component or a locally-named `*V2` wrapper) when `v2`, keep the existing spinner when `!v2`. Replace the raw "no access" text with `t('reports.noAccess')` when `v2` (keep the current literal when `!v2`). Add `dataI18nSkip={v2}` to the page's `PageHeader` call (title/subtitle) per Pitfall 1 — new namespace, anticipate the domTranslations.ts mangling proactively rather than discovering it live.
2. Thread `redesigned={v2}` into each of the 5 tab components (`DailySummaryTab`, `StaffPerformanceTab`, `MaintenanceTab`, `GuestRecoveryTab`, `AIUsageTab`) — Pattern 2, one flag decision per page, matching 33-05's Safety/Programs precedent (5 panels instead of 4/3).
3. In each of the 5 tabs: accept a `redesigned?: boolean` prop. Extend that tab's `useQuery` destructuring from `{ data, isLoading, isError }` to `{ data, isLoading, isError, refetch }` (mechanical, currently absent in all 5 — Pitfall 3). Under `redesigned`:
   - Loading: replace the local `SkeletonBlock` (`animate-pulse rounded-lg bg-gray-100`) grid with a v2-token-styled equivalent (shared `Skeleton` component, or de-shadow `SkeletonBlock` with a v2-styled sibling per the naming convention already established in Phase 33) — same grid/sizing as today, just re-skinned.
   - Error: replace the raw alert `<div>` with `StateBlock status="error" error={{ message: t('reports.<tabKey>.loadError'), onRetry: () => refetch() }}` (tabKey = `dailySummary`/`staffPerformance`/`maintenance`/`guestRecovery`/`aiUsage`).
   - Empty: `DailySummaryTab`/`MaintenanceTab`/`GuestRecoveryTab` currently render a bare `<p>` — replace with `StateBlock status="empty" empty={{ title: t('reports.<tabKey>.empty.title'), body: t('reports.<tabKey>.empty.body') }}`. `StaffPerformanceTab`/`AIUsageTab` already use the shared `EmptyState` — keep using `EmptyState` (not StateBlock) but i18n its title/body via the same `reports.<tabKey>.empty.*` keys if its current copy is a hardcoded literal.
   - When `redesigned` is false, each tab renders exactly as today (legacy, including the original `SkeletonBlock`/raw-div/bare-`<p>` markup) — the `refetch` addition to the destructuring is safe either way since it's unused in the legacy branch.
4. Role-based tab visibility logic (`isGM`/`isSupervisor`/`role === 'engineer'`) must not change — zero RBAC change is a hard constraint; do not touch the tabs array construction logic, only its `PageHeader` labels/i18n if applicable.
5. v2 tokens for chrome touched in this task (skeleton bg, StateBlock's own styling is already token-based). Do NOT edit frozen files (`primitives.tsx`, `Button.tsx`), re-tint room-status tokens, or change any query key/fetcher/params.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('reports'` and `redesigned` threaded into all 5 tab calls in reports/page.tsx; grep confirms each of the 5 tabs' `useQuery` destructuring now includes `refetch`; grep confirms `StateBlock` used in all 5 tabs' error path and in the 3 bare-`<p>` former-empty tabs; grep confirms `dataI18nSkip={v2}` on the page's PageHeader; git status shows only reports/page.tsx changed</verify>
  <done>Reports page reads the flag once and threads redesigned to 5 tabs; auth-loading + no-access text v2-skinned/i18n'd; each tab's useQuery destructuring extended with refetch; loading uses a v2 skeleton, error uses StateBlock (onRetry: refetch), empty uses StateBlock or the existing EmptyState (i18n'd); legacy branch unchanged flag-off; dataI18nSkip set; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Management ROI — flag gate; skeleton auth-loading + per-Section loading; StateBlock stacked errors + page empty</name>
  <files>apps/web/app/(dashboard)/management-roi/page.tsx</files>
  <action>
1. In `ManagementRoiPage`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('managementRoi', hotel)`. Replace the raw `animate-spin` auth-loading div with a v2 skeleton when `v2`; keep the spinner when `!v2`. Replace the raw non-GM-guard centered text with `t('managementRoi.noAccess')` when `v2` (keep the literal when `!v2`).
2. Do NOT change the 9 named `useQuery` objects (`housekeepingEfficiencyQuery`, `inspectionTrendsQuery`, `repeatFailuresQuery`, `downtimeRevenueQuery`, `pmComplianceQuery`, `trainingReadinessQuery`, `forecastQuery`, `guestRecoveryQuery`, `maintenanceQuery`) or their `enabled: isGM` gating — `.refetch()` is already trivially callable via dot-notation, no destructuring change needed (Pitfall 3).
3. Stacked errors: the existing `errors: { isError, noun }[]` filtered array currently renders N raw alert `<div>`s with `"Failed to load {noun}."`. Under `v2`, replace this with N `StateBlock status="error"` instances (or a single mapped list of StateBlocks), each using `t('managementRoi.loadErrorFor', { noun: t(...) })` (or plain string interpolation matching however the codebase's `t()` interpolation is invoked elsewhere) and `onRetry` pointing at THAT specific failing query's own `.refetch()` (map each `noun` back to its owning query object — do not use a generic retry that refetches everything, preserve today's per-query failure granularity). Keep the legacy raw-div rendering when `!v2`.
4. `Section`/`SkeletonGrid`/`StatSkeleton` (the 4 composite loading booleans `timeSavedLoading`/`qualityLoading`/`responseLoading`/`revenueLoading`) already have a clean per-group loading UI — under `v2`, re-skin `StatSkeleton`'s `animate-pulse rounded-[var(--r-lg)] bg-gray-100` with v2 tokens (e.g. `bg-surface-3`, matching the shared `Skeleton` component's own styling) without changing the aggregation booleans or grouping logic.
5. Page-level `isEmpty` EmptyState: keep using the shared `EmptyState` component (not StateBlock, consistent with how it's already used) but under `v2` route its `title`/`body` through `t('managementRoi.empty.title')`/`t('managementRoi.empty.body')` instead of the current hardcoded copy; keep the `Gauge` icon and the `isEmpty` computation unchanged.
6. `Stat`/`Pill` are imported from `components/ui/primitives.tsx` (FROZEN — Pitfall 5) — any v2 visual treatment for their call sites must be achieved via `className` overrides at the call site, never by editing `primitives.tsx`. Add `dataI18nSkip={v2}` to the page's `PageHeader` if it has title/subtitle text, per Pitfall 1.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('managementRoi'` in management-roi/page.tsx; grep confirms the stacked-error rendering under `v2` calls `.refetch()` on the specific failing query object (not a blanket refetch-all); grep confirms `EmptyState` title/body now sourced from `t('managementRoi.empty` under `v2`; grep confirms no `primitives.tsx` file was modified; git status shows only management-roi/page.tsx changed</verify>
  <done>Management ROI page reads the flag once; auth-loading + non-GM guard v2-skinned/i18n'd; stacked per-query errors StateBlock'd with correct per-query onRetry; composite loading skeletons v2-token-styled; page EmptyState i18n'd; no query/aggregation/RBAC logic changed; legacy branch unchanged flag-off; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Reports: flag read once in page.tsx, `redesigned` threaded to 5 tabs; each tab's loading/empty/error v2-wired with `refetch` added to destructuring; legacy unchanged flag-off.
- Management ROI: flag read once; stacked per-query errors + page EmptyState StateBlock/i18n'd with correct per-query `onRetry`; no destructuring change to the 9 named queries; legacy unchanged flag-off.
- Only reports/page.tsx and management-roi/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation/field/RBAC change.
</verification>

<success_criteria>
Reports and Management ROI render in the new visual system behind their own flags, using the flag-thread-to-tabs pattern (Reports) and direct page-level gating (Management ROI), with every loading/empty/error state served by a shared skeleton/StateBlock/EmptyState component and wired to each query's own existing refetch — zero behavior/data/RBAC change.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-02-SUMMARY.md` recording: the flag-thread wiring for Reports (which prop name, confirmation all 5 tabs received it), the per-query onRetry mapping used for Management ROI's stacked errors, the `reports.*`/`managementRoi.*` keys actually consumed (and any deviation from 34-01's suggested names), confirmation `dataI18nSkip` was set on both PageHeader calls, confirmation no frozen/locale file was touched, and any deferred item (e.g. a literal found mid-task that wasn't part of the touched loading/empty/error region) for the close-out list.
</output>
