---
phase: 34-management-admin-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

# NOTE on file ownership: en.ts / es.ts are edited ONLY here. This plan is the SOLE owner of
# the locale files for Phase 34 so the six parallel wave-2 content plans (34-02..34-07) never
# collide on them. Those plans MUST consume the keys defined here and MUST NOT add new locale
# keys. If a wave-2 plan discovers a genuinely missing string inside a state it is touching, it
# reuses an existing generic `common.*` key (loading/error/retry/noResults) rather than editing
# en/es (which would conflict with its sibling wave-2 plans). Precedent: Phase 33's 33-01 did
# exactly this for the prior 9 sections.

must_haves:
  truths:
    - "Every empty-state, loading-label, error-message, and touched-chrome string that the six wave-2 plans (8 full sections + Notifications + Late Checkout) need already exists in BOTH en.ts and es.ts, with real Spanish translations (not placeholders)"
    - "Eight new top-level namespaces — `reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations` — exist in both locale files (none existed before this phase)"
    - "The `header` namespace is extended additively with a notifications-dropdown load-error key (existing `header.notifications*` keys left byte-unchanged); the `dashboard` namespace is extended only if the Late Checkout verification plan's contingent gap-key is pre-provisioned"
    - "`npm run check:i18n-parity` and `npm run verify:i18n-gate` pass — every new en key has an es counterpart and vice versa"
  artifacts:
    - path: "apps/web/i18n/locales/en.ts"
      provides: "New reports/managementRoi/staff/settings/aiCopilot/billing/guestFeedback/integrations namespaces + extended header block — English copy for every state and touched-chrome string the 10 sections render"
      contains: "managementRoi"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Exact-parity Spanish counterparts for every new/added key in en.ts"
      contains: "managementRoi"
  key_links:
    - from: "new keys in en.ts"
      to: "new keys in es.ts"
      via: "1:1 key parity enforced by scripts/check-i18n-parity.mjs"
      pattern: "managementRoi"
---

<objective>
Establish the complete i18n key set that all six wave-2 content plans (Reports+ManagementRoi, Staff, Settings-general+Billing+GuestFeedback, Opera Integration, AI Copilot, Notifications+Late Checkout — covering all 10 SEC-01b sections) will consume — in BOTH locales, in one place — so those plans can run in parallel without ever touching (and colliding on) en.ts / es.ts.

This plan writes NO component code. It only adds locale keys: per-section empty-state copy, error-state copy, loading labels where a section needs a non-default one, and the specific touched-chrome strings CONTEXT flags as in-scope. Eight sections (Reports, Management ROI, Staff [namespace only — most UI already i18n'd], Settings-general, AI Copilot, Billing, Guest Feedback, Opera Integration) currently have NO namespace at all and must get one. `header` (Notifications) gets an additive extension. `dashboard` (Late Checkout) is pre-provisioned with one contingent key in case wave-2 verification finds a real gap — Late Checkout's existing keys already cover the panel per research, so no gap is expected, but the key exists so the verification plan never has to touch locale files itself.

Purpose: satisfy the EN/ES key-parity half of the phase's Success Criterion #3 up front and unblock 34-02..34-07 for parallel execution with zero locale-file contention.
Output: eight new namespaces + one extended block (`header`) + one contingent key (`dashboard`) in en.ts and es.ts; `check:i18n-parity` + `verify:i18n-gate` green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md

# The two locale files — plain nested TS objects. Present namespaces after Phase 33:
# common, login, header, palette, nav, roles, dashboard, programs, housekeeping, evidence,
# safety, engineering, tasks, guestMessages, accessibilityGuidance, satisfaction,
# resolutionConfirmation, guestRequests, sop, logbook, lostFound, scheduling.
# ABSENT (must be created): reports, managementRoi, staff, settings, aiCopilot, billing,
# guestFeedback, integrations.
@apps/web/i18n/locales/en.ts
@apps/web/i18n/locales/es.ts

# Read these section files to copy the ACTUAL current hardcoded English strings (the source of
# truth for copy) into keys — RESEARCH.md located raw strings per section but the live file text
# is authoritative. Grep each for title=/message=/subtitle=/label= object-literal props and raw
# >text< between tags inside loading/empty/error regions specifically.
@apps/web/app/(dashboard)/reports/page.tsx
@apps/web/app/(dashboard)/management-roi/page.tsx
@apps/web/app/(dashboard)/staff/page.tsx
@apps/web/app/(dashboard)/settings/general/page.tsx
@apps/web/app/(dashboard)/settings/billing/page.tsx
@apps/web/app/(dashboard)/settings/feedback/page.tsx
@apps/web/app/(dashboard)/settings/integrations/page.tsx
@apps/web/app/(dashboard)/ai/page.tsx
@apps/web/components/shared/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all new + extended English keys to en.ts</name>
  <files>apps/web/i18n/locales/en.ts</files>
  <action>
Add EIGHT new top-level namespaces, extend `header` additively, and pre-provision one contingent `dashboard` key. Leave every pre-existing key byte-unchanged (additive only). Match the file's existing style (quote style, trailing commas, nesting). For copy, prefer the ACTUAL current hardcoded English string found in each section file (read them via context) so the flag-off legacy branch and flag-on v2 branch read identically — the key names below are the contract the wave-2 plans rely on, but the string VALUES should mirror what's on screen today.

Use `common.*` (already present: `loading`, `error`, `retry`, `noResults`) for generic state text; only add section-specific copy below. Reuse `common.retry` for any bare "Retry" button label rather than inventing a duplicate key.

=== NEW NAMESPACE `reports` ===
Reports has 5 independently-queried tabs (DailySummary, StaffPerformance, Maintenance, GuestRecovery, AIUsage), each currently rendering its own raw error div and (for 3 of the 5) a bare `<p>` for empty (StaffPerformance and AIUsage already use the shared `EmptyState` component — grep their existing copy before assuming it needs a new key; only add an empty key for those two if their current copy is a literal, not already i18n'd elsewhere).
- `pageTitle: "Reports"`, `pageSubtitle` (match current header text if any; if the page has no subtitle today, add one describing the report library, e.g. "Performance and compliance reporting")
- `noAccess: "You don't have access to this report."` (match the current raw "no access" text)
- `dailySummary: { loadError: "Couldn't load the daily summary.", empty: { title: "No summary yet", body: "Check back once today's data is in." } }`
- `staffPerformance: { loadError: "Couldn't load staff performance.", empty: { title: "No performance data", body: "No staff activity in this range." } }` (match EmptyState's current copy if it already has some)
- `maintenance: { loadError: "Couldn't load maintenance data.", empty: { title: "No maintenance data", body: "No work orders in this range." } }`
- `guestRecovery: { loadError: "Couldn't load guest recovery data.", empty: { title: "No guest recovery data", body: "No incidents in this range." } }`
- `aiUsage: { loadError: "Couldn't load AI usage data.", empty: { title: "No AI usage yet", body: "AI activity will appear here." } }` (match EmptyState's current copy if it already has some)

=== NEW NAMESPACE `managementRoi` ===
- `pageTitle: "Management ROI"`, `pageSubtitle` (match current header text)
- `noAccess: "This dashboard is only available to General Managers."` (match the current raw non-GM guard text)
- `loadErrorFor: "Failed to load {{noun}}."` (interpolated, reused for every failing query in the stacked-errors array — matches the CURRENT dynamic-noun pattern already in the code; do not create one key per noun)
- `empty: { title: "No data yet", body: "Metrics will appear here once your property has activity." }` (match the current shared EmptyState copy — this already uses `EmptyState` with icon `Gauge`, so check its existing title/body text first)

=== NEW NAMESPACE `staff` ===
Staff already uses `StateBlock`/`PageHeader` extensively — this namespace only needs the genuinely missing pieces (invitations error, EditStaffModal's two under-covered queries):
- `invitations: { loadError: "Couldn't load pending invitations." }`
- `editModal: { schedulesLoadError: "Couldn't load schedule overrides.", rolesLoadError: "Couldn't load custom roles." }`
(Do NOT add keys for the 4 modals' form-validation strings — out of scope per CONTEXT, same class as Phase 33's deferred form-validation pockets.)

=== NEW NAMESPACE `settings` ===
Targets `settings/general/page.tsx` (the hotel-profile page; the shared `settings/layout.tsx` shell stays untouched and out of scope):
- `pageTitle: "Hotel Profile"` (match the current bare `<h2>` text — optional to consume; the page has no PageHeader today and CONTEXT doesn't require adding one, only the fetch's loading/error needs new UI)
- `loadError: "Couldn't load your hotel's profile."` (genuinely new — this fetch currently has NO loading/error UI at all)

=== NEW NAMESPACE `aiCopilot` ===
AI Copilot's scope is narrow (chat UI, not a list/CRUD page) — only the `CreditUsageCard`'s query gets a loading treatment (no error copy needed since its rendered values are pre-existing hardcoded stubs that don't change on error — zero behavior change; do not invent new error messaging that would change what renders on failure):
- `creditUsage: { loading: "Loading usage…" }` (used only as an aria-label/sr-only string on the skeleton if needed; the skeleton itself is visual)
(Do NOT add chat-message error copy — errors already surface as AI-role chat bubbles via existing `err.message`/fallback text, a deliberate pattern that must not change.)

=== NEW NAMESPACE `billing` ===
Billing already uses `PageHeader` (title="Billing & Usage") — add only the 3 missing per-query error keys plus an empty key if the invoices table has a literal "no invoices" case today:
- `subscriptionLoadError: "Couldn't load your subscription."`
- `creditsLoadError: "Couldn't load your credit usage."`
- `invoicesLoadError: "Couldn't load your invoices."`
- `invoicesEmpty: "No invoices yet."` (only if the current table has a bare literal for zero invoices — check live; if none exists today, still add the key for the new v2 empty state)

=== NEW NAMESPACE `guestFeedback` ===
Guest Feedback is nearly StateBlock-complete already — match its EXISTING copy exactly (do not reword):
- `loadError: "Feedback could not load."` (match the current StateBlock `error.message` string verbatim)
- `empty: { title: "No feedback yet", body: "..." }` (match the current StateBlock `empty` object verbatim — read the live file for the real `body` text, the research pass did not capture it in full)

=== NEW NAMESPACE `integrations` ===
Targets Opera Integration (`settings/integrations/page.tsx`) — `statusQuery` already has raw loading/error markup with a working `refetch()`-wired retry button; convert to shared components, don't invent new behavior:
- `loadError: "Couldn't load your Opera connection status."` (match the current raw error text)
- `conflicts: { loadError: "Couldn't load sync conflicts.", empty: "No sync conflicts." }` (only consumed if the wave-2 plan decides to give `conflictsQuery` its own state UI — provisioned either way so that plan never touches locale files)
(Do NOT add a key for the connect/disconnect form itself — per CONTEXT/Pitfall 4, the disconnected state is a primary UI state, not an empty/error state, and stays as-is.)

=== EXTEND EXISTING `header` ===
- `notificationsLoadError: "Couldn't load notifications."` (genuinely new — the dropdown's `notificationsData` query currently has no loading/error UI at all; do NOT touch the existing `notifications`/`notificationsUnread`/`notificationsAll`/`markAllRead`/`noNotifications` keys)

=== EXTEND EXISTING `dashboard` (contingent, Late Checkout) ===
- Per research, Late Checkout's `dashboard`-flag-gated v2 branch already has complete, already-i18n'd loading/error/empty via `common.error` and `dashboard.empty.frontDeskNoLateCheckouts` — no new key is expected to be needed. Add nothing here unless a genuine gap is confirmed live; if the wave-2 Notifications+Late Checkout plan finds one, it must reuse an EXISTING `common.*`/`dashboard.*` key rather than requesting a new one (it cannot edit locale files — those are frozen after this plan).

Do NOT invent keys beyond what the ten sections render inside touched chrome/states. If a section's exact current string differs from the suggested English above, use the section's ACTUAL string as the value and keep the key name.
  </action>
  <verify>cd apps/web && npm run type-check succeeds (en.ts is the source-of-truth locale shape); grep confirms new top-level blocks `reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations` all present in en.ts; grep confirms `header` now contains `notificationsLoadError` alongside its pre-existing `noNotifications`/`markAllRead` keys (unchanged); pre-existing keys across all namespaces (spot-check `common.retry`, `dashboard.empty.frontDeskNoLateCheckouts`, `nav.reports`) still present verbatim (additive check)</verify>
  <done>en.ts gains reports/managementRoi/staff/settings/aiCopilot/billing/guestFeedback/integrations namespaces and an extended header block; every pre-existing key unchanged; type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Mirror every new/added key into es.ts with real Spanish + prove parity</name>
  <files>apps/web/i18n/locales/es.ts</files>
  <action>
Add the EXACT same key structure to es.ts with real, natural Spanish translations in the hotel-operations register, consistent with the existing es.ts tone (mirror the style of the existing `common`/`dashboard`/`header`/`guestRequests` Spanish copy — those are the newest/most complete blocks per Phase 32/33). Every key added to en.ts in Task 1 must have a 1:1 Spanish counterpart here, and vice versa — no en-only or es-only keys.

Guidance for the flagged strings:
- Section page titles: translate naturally, aligning with how the equivalent `nav.*` label is already translated where one exists (`nav.reports`, `nav.managementRoi`, `nav.staff`, `nav.aiCopilot`, `nav.billing`, `nav.integrations`, `nav.feedback` all already exist as short nav-chrome labels — check their Spanish values for consistency, but these new page-content keys are separate, same relationship as Phase 33's `nav.tasks` vs. new `tasks.*`).
- Generic empty/error: reuse the register already established in `common` (`error`, `retry`, `loading`, `noResults`) — section-specific empties should read naturally in Spanish, not literal machine translation.
- `managementRoi.loadErrorFor`: preserve the `{{noun}}` interpolation placeholder exactly (e.g. `"No se pudo cargar {{noun}}."`).
- `guestFeedback.loadError`/`empty`: translate naturally — there is no existing Spanish to match verbatim since these strings are net-new to i18n (today they're hardcoded English in the component).

Then run the parity + type gates and confirm green.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity && npm run verify:i18n-gate && npm run type-check all green (parity proves every new en key has an es counterpart and vice versa); grep confirms `reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations` blocks present in es.ts too, and es `header` gained `notificationsLoadError`</verify>
  <done>es.ts mirrors every new/added key from Task 1 with real Spanish; check:i18n-parity + verify:i18n-gate + type-check all green</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity`, `npm run verify:i18n-gate`, `npm run type-check` all green from `apps/web`.
- Eight new namespaces (`reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations`) exist in BOTH locales with matching structure.
- `header` extended additively (`notificationsLoadError`); every pre-existing key across all namespaces unchanged.
- Every one of the 10 sections has (new or reused) empty/loading/error copy available in both locales.
</verification>

<success_criteria>
The complete Phase-34 section copy set exists in both locales with real Spanish and passes parity — satisfying the EN/ES half of the phase's Success Criterion #3 up front and unblocking 34-02..34-07 to run in parallel without ever touching the locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-01-SUMMARY.md` recording: the exact final key list per section (so wave-2 executors reference the real names), any key naming that deviated from this plan, which existing keys were reused vs newly added, any string DEFERRED (e.g. Staff modal form-validation strings, AI Copilot chat error copy) for the close-out deferred list, confirmation of parity-gate pass, and an explicit statement that en.ts/es.ts are now frozen for the rest of Phase 34 (wave-2 plans must not edit them, only consume).
</output>
