---
phase: 34-management-admin-sections
plan: 07
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/components/shared/Header.tsx
  - apps/web/components/dashboard/FrontDeskDashboard.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of Header.tsx and FrontDeskDashboard.tsx
# for Phase 34 — both are "shared/careful" files per CONTEXT (Header.tsx is global, rendered on
# every dashboard page; FrontDeskDashboard.tsx is already `dashboard`-gated from Phase 32). No
# other Phase 34 plan touches either file. Neither section gets a NEW flag: Notifications
# piggybacks on the Phase-31 `shell` flag (already threaded into Header.tsx as the `redesigned`
# prop); Late Checkout piggybacks on the Phase-32 `dashboard` flag (already the `v2` var in
# FrontDeskDashboard.tsx). Touch ONLY the notification-dropdown's internal content region in
# Header.tsx and ONLY the late-checkout panel's v2 branch in FrontDeskDashboard.tsx — do not
# alter the `shell`/`dashboard` flag logic itself or any other panel/region in either file. Does
# NOT edit en.ts/es.ts — the `header.notificationsLoadError` key exists from 34-01; only
# consumes it (plus whatever pre-existing `common.*`/`dashboard.*` keys Late Checkout already
# uses).

must_haves:
  truths:
    - "With the (pre-existing) 'shell' flag ON, the Header notification dropdown's notificationsData query gains a loading skeleton and an error+retry state — both currently entirely absent — inside the same redesigned-gated region that already handles the dropdown's empty case"
    - "The dropdown's existing empty case (t('header.noNotifications')) and its unreadNotificationsData badge-count query are unchanged — this plan only adds states to notificationsData, the query driving the dropdown's LIST content"
    - "With the (pre-existing) 'dashboard' flag ON, FrontDeskDashboard's late-checkout panel is verified/confirmed to have complete loading (SkeletonRow v2)/error (StateBlock+refetch)/empty (StateBlock) states already wired, AND the LateCheckoutRow component's approve/deny resolving in-flight state is confirmed to disable/loading-style its buttons correctly during a mutation — any genuine gap found is closed using ONLY existing dashboard.*/common.* i18n keys (no new locale key)"
    - "No other panel in Header.tsx or FrontDeskDashboard.tsx is modified; RoomCard.tsx/RoomStatusBoard.tsx/RoomDetailDrawer.tsx/EngineeringRoomBoard.tsx (frozen, Room-Board regression harness) are not touched, imported differently, or otherwise affected"
  artifacts:
    - path: "apps/web/components/shared/Header.tsx"
      provides: "notificationsData query destructuring extended with isLoading/isError/refetch; dropdown content region gains a v2 skeleton (loading) and StateBlock/inline error+retry (error), gated on the existing `redesigned` prop; empty case and unreadNotificationsData untouched"
      contains: "header.notificationsLoadError"
    - path: "apps/web/components/dashboard/FrontDeskDashboard.tsx"
      provides: "Late-checkout panel's existing v2 loading/error/empty states confirmed complete; LateCheckoutRow's resolving prop confirmed to correctly gate approve/deny button disabled/loading treatment; any genuine gap closed with existing i18n keys only"
      contains: "resolving"
  key_links:
    - from: "apps/web/components/shared/Header.tsx (notification dropdown)"
      to: "notificationsData query"
      via: "extended destructuring adds isLoading/isError/refetch; error.onRetry = () => refetch()"
      pattern: "notificationsData"
    - from: "apps/web/components/dashboard/FrontDeskDashboard.tsx (LateCheckoutRow)"
      to: "the approve/deny mutation's resolving state"
      via: "resolving prop already threaded — verify its JSX usage disables/loading-styles the action buttons during the in-flight mutation"
      pattern: "resolving"
---

<objective>
Complete the two sections that have no dedicated route and instead piggyback on an already-existing flag's already-existing v2 branch, per CONTEXT's explicit decision (do NOT invent new `notifications`/`lateCheckout` flags).

**Notifications** (`components/shared/Header.tsx`): the bell dropdown's `redesigned` prop (sourced from the Phase-31 `shell` flag) already gates chrome details throughout the header, but the dropdown's LIST content itself — driven by the `notificationsData` query — has NO loading or error UI at all today (destructures only `{ data }`). The empty case is already handled and i18n'd (`t('header.noNotifications')`). This task adds the missing loading skeleton and error+retry inside the existing `redesigned`-gated region — a small, surgical addition matching CONTEXT's framing exactly.

**Late Checkout** (`components/dashboard/FrontDeskDashboard.tsx`): CONTEXT and RESEARCH.md both confirm this is ALREADY essentially complete — the `dashboard`-flag-gated `v2` branch has a fully wired query (`{ data: lateCheckoutsData, isLoading: lateCheckoutsLoading, isError: lateCheckoutsIsError, refetch: refetchLateCheckouts }`), a `SkeletonRow v2` loading state, a `StateBlock` error+retry, and a `StateBlock` empty state, all already i18n'd. This task is primarily VERIFICATION: confirm the `LateCheckoutRow` component's `resolving` prop correctly gates the approve/deny buttons' disabled/loading treatment during an in-flight mutation, and confirm no i18n gap exists in the panel. Close any genuine gap found using only existing keys (no new locale key is available — 34-01 pre-provisioned nothing beyond a contingency note).

Purpose: SEC-01b for Notifications + Late Checkout — the phase's two "piggyback flag" sections, grouped into one plan because they're both small, both touch a shared/careful file with no other Phase 34 owner, and neither needs a dedicated flag.
Output: Header.tsx's notification dropdown gains loading/error states; FrontDeskDashboard.tsx's late-checkout panel confirmed complete (or a small gap closed with existing keys).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

@apps/web/components/shared/Header.tsx
@apps/web/components/dashboard/FrontDeskDashboard.tsx

# Flag + state contracts (import-only; these files already receive `redesigned`/`v2` from their
# respective parent flags — do NOT re-derive or duplicate the flag read)
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header.tsx notification dropdown — add loading skeleton + error+retry to notificationsData</name>
  <files>apps/web/components/shared/Header.tsx</files>
  <action>
1. Locate the two notification `useQuery` calls: `unreadNotificationsData` (`queryKey: ['notifications','unread']`, drives only the badge count — leave completely untouched) and `notificationsData` (`queryKey: ['notifications', notificationsTab]`, drives the dropdown's list content — this is the one to extend).
2. Extend `notificationsData`'s destructuring from `{ data }` to `{ data, isLoading, isError, refetch }`.
3. Inside the dropdown's `max-h-[360px] overflow-y-auto` content area, gated by the existing `redesigned` prop (already threaded from `DashboardShell.tsx` off the `shell` flag — do not add a new flag read):
   - While `redesigned && isLoading`: render a v2 skeleton (2-3 `Skeleton` rows sized to a notification list item) in place of the list.
   - While `redesigned && isError`: render a compact `StateBlock status="error" error={{ message: t('header.notificationsLoadError'), onRetry: () => refetch() }}` (or an inline equivalent sized to fit the dropdown's compact width, matching whichever convention reads better inside a ~360px-tall dropdown — StateBlock's default sizing may need a `className` override for a tighter fit; do not modify `StateBlock.tsx` itself, only the call site).
   - The existing empty case (`notifications.length === 0 ? <p>{t('header.noNotifications')}</p> : notifications.map(...)`) stays exactly as-is, now only reached when NOT loading and NOT errored.
4. When `!redesigned` (legacy): render exactly as today — no loading/error UI, immediate render of whatever `data` currently is (including a flash of the empty state before data arrives, which is today's actual behavior — do not change it).
5. Do NOT touch the `shell` flag logic itself, `unreadNotificationsData`, the badge-count rendering, `markAllRead` handling, or any other header chrome (nav items, avatar menu, etc.) outside this one dropdown content region.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `notificationsData`'s destructuring now includes `isLoading`, `isError`, `refetch`; grep confirms a `redesigned &&` (or equivalent) conditional gates a new `Skeleton`/`StateBlock` usage referencing `header.notificationsLoadError`; grep confirms `unreadNotificationsData`'s destructuring and the badge-count JSX are byte-unchanged; grep confirms `header.noNotifications` still renders unconditionally once loading/error are ruled out; git status shows only Header.tsx changed (combined with Task 2's FrontDeskDashboard.tsx)</verify>
  <done>notificationsData query destructuring extended; dropdown content region gains a v2 skeleton (loading) and StateBlock/compact error+retry (error), both gated on the existing redesigned prop; empty case and unreadNotificationsData/badge count untouched; legacy branch unchanged when redesigned is false; gates green</done>
</task>

<task type="auto">
  <name>Task 2: FrontDeskDashboard.tsx late-checkout panel — verify + close any gap in resolving-state and i18n coverage</name>
  <files>apps/web/components/dashboard/FrontDeskDashboard.tsx</files>
  <action>
1. Confirm (read the live file, do not assume from research alone) the `dashboard`-flag-gated `v2` branch's late-checkout panel (~line 172 `const v2 = isSectionRedesigned('dashboard', hotel)`) has, exactly as documented:
   - Query: `{ data: lateCheckoutsData, isLoading: lateCheckoutsLoading, isError: lateCheckoutsIsError, refetch: refetchLateCheckouts }`.
   - Loading: `[...Array(2)].map((_, i) => <SkeletonRow key={i} v2 />)`.
   - Error: `<StateBlock status="error" error={{ message: t('common.error'), onRetry: () => refetchLateCheckouts() }} />`.
   - Empty: `<StateBlock status="empty" empty={{ title: t('dashboard.empty.frontDeskNoLateCheckouts') }} />`.
   If any of these four pieces is missing, incomplete, or has drifted from this description, close the gap using ONLY existing `common.*`/`dashboard.*` i18n keys (do not add a new locale key — en.ts/es.ts are frozen after 34-01) and the existing `refetchLateCheckouts` callable (do not add a new query).
2. Grep `resolving` usage inside `LateCheckoutRow`'s JSX body (line ~53). Confirm the approve/deny action buttons are `disabled`/loading-styled (e.g. spinner, reduced opacity, `aria-busy`) while `resolving` is true for that specific row, and that the row's other interactive elements don't remain clickable mid-mutation in a way that could double-submit. If this is already correctly implemented, make NO change (this is a confirmation, not a rebuild). If a genuine gap is found (e.g. `resolving` is threaded but not actually used to disable the buttons), fix it minimally — add `disabled={resolving}` (or the equivalent already-established pattern elsewhere in this file/codebase for in-flight mutation buttons) without changing the mutation's trigger logic, success/error handling, or the parent's approve/deny callback signatures.
3. Do NOT touch any other panel on `FrontDeskDashboard` (out of Phase 34 scope, belongs to Phase 32) — confirm via `git status`/diff review after this task that only the late-checkout panel region changed, if anything changed at all.
4. Do NOT touch `RoomCard.tsx`, `RoomStatusBoard.tsx`, `RoomDetailDrawer.tsx`, or `EngineeringRoomBoard.tsx` — none of these should need editing for this task, but explicitly confirm no accidental import/usage change occurred to any of them.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms the late-checkout panel's query destructuring, SkeletonRow/StateBlock loading/error/empty usage, and `LateCheckoutRow`'s `resolving`-gated button disabling all match the documented shape (or a minimal fix was applied using only existing keys); grep confirms no other FrontDeskDashboard panel's JSX changed; `git diff --stat` shows FrontDeskDashboard.tsx's diff (if any) confined to the late-checkout region; `npm run check:frozen-files` confirms zero drift on the 5 frozen Room-Board files</verify>
  <done>Late-checkout panel's loading/error/empty states confirmed complete (or a minimal gap closed using only existing i18n keys/refetch); LateCheckoutRow's resolving prop confirmed to correctly gate approve/deny button state during in-flight mutations; no other panel touched; frozen Room-Board files unaffected; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Header.tsx: notificationsData query gains loading/error states inside the existing `redesigned`-gated dropdown region; empty case and badge-count query untouched.
- FrontDeskDashboard.tsx: late-checkout panel's pre-existing loading/error/empty states confirmed (or minimally completed with existing keys); LateCheckoutRow's resolving-gated button state confirmed correct.
- Only Header.tsx and FrontDeskDashboard.tsx changed, both scoped to their single named region; no frozen file edited; no locale-file edit; no query/mutation logic change beyond what's explicitly documented above.
</verification>

<success_criteria>
Notifications' dropdown list gains a complete loading/error/empty triad (previously loading+error entirely absent) inside its existing `shell`-flag-gated region, and Late Checkout's already-mature `dashboard`-flag-gated panel is confirmed complete with correct in-flight button state — both piggybacking on their existing flags per CONTEXT, neither requiring a new flag or new locale key beyond `header.notificationsLoadError` — zero behavior/data change, and the frozen Room-Board files provably unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-07-SUMMARY.md` recording: the exact loading/error wiring added to Header.tsx's notification dropdown, the verification result for FrontDeskDashboard's late-checkout panel (complete-as-documented, or the specific minimal gap closed and with which existing key), the resolving-state button-disabling confirmation for LateCheckoutRow, confirmation the 5 frozen Room-Board files are unaffected, and any deferred item for the close-out list.
</output>
