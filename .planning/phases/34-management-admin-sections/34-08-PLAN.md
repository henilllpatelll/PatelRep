---
phase: 34-management-admin-sections
plan: 08
type: execute
wave: 3
depends_on: ["34-02", "34-03", "34-04", "34-05", "34-06", "34-07"]
files_modified: []
autonomous: true

# NOTE: verification-only plan (files_modified: []), mirroring 33-07/32-06. Runs the full
# standing web gate suite across the combined wave-2 changes, re-passes the Phase-30 Room-Board
# regression harness (these section changes must be inert to it — Late Checkout's read-only
# indicators inside FrontDeskDashboard.tsx are the closest touch-point to the frozen boards this
# phase has), and performs live flag-on/flag-off browser self-verification of all 10 sections
# (light+dark, EN+ES). Any real defect found is a blocking Rule-1 fix in the owning section file
# (which un-empties files_modified for that fix only) — otherwise this plan changes no source.

must_haves:
  truths:
    - "All standing web gates pass on the combined Phase-34 changes: type-check, check:frozen-files (zero frozen-hash / room-status drift), check:contrast (dark-mode WCAG AA), check:i18n-parity (+ verify:i18n-gate), and build"
    - "The Phase-30 Room-Board regression harness re-passes at zero pixel drift (excluding the known, pre-documented RoomDetailDrawer sub-pixel font-AA noise) — the 10 section redesigns did not perturb the frozen boards"
    - "With each section's flag ON for the test hotel, all 10 sections (Reports, Management ROI, Staff, Settings, AI Copilot, Billing, Notifications, Guest Feedback, Late Checkout, Opera Integration) render the v2 redesign incl. their empty, loading (skeleton not spinner, except AI Copilot's deliberately-preserved chat/typing-indicator pattern), and error states; with the flag OFF, each renders today's UI unchanged"
    - "Each section's empty/loading/error states render correctly in both light and dark mode and both EN and ES with no console errors, no failed/forbidden API calls, and no domTranslations.ts EN/ES hybrid mangling on any new PageHeader/chrome copy (Pitfall 1, pre-empted via dataI18nSkip on every new/changed PageHeader call site) — confirming zero behavior/data/RBAC change via the same-inputs->same-outputs network diff"
  artifacts: []
  key_links:
    - from: "the 10 sections"
      to: "their flags (8 dedicated: reports/managementRoi/staff/settings/aiCopilot/billing/guestFeedback/integrations; 2 piggybacked: shell for Notifications, dashboard for Late Checkout)"
      via: "web_redesign_sections includes each of the 8 dedicated keys, toggled on the test hotel; shell/dashboard already toggled from Phase 31/32, verified live per section"
      pattern: "isSectionRedesigned"
---

<objective>
Close Phase 34: prove the six wave-2 content plans combine cleanly. Run the full standing web gate suite over all Phase-34 changes together, re-pass the Phase-30 Room-Board regression harness (Late Checkout sits inside `FrontDeskDashboard.tsx`, which is adjacent to but does not modify the frozen boards — this is the phase's Success-Criterion-#4 safety net, structurally identical to Phase 33's LogFoundItemModal check), and live-verify all 10 sections in the browser with each section's flag both ON and OFF, in light+dark and EN+ES, confirming the redesigned empty/loading/error states render, the network diff is inert (Success Criterion #3), and no regression to adjacent surfaces (Success Criterion #4).

This satisfies the whole-phase view of all 4 Success Criteria: (1) 10 sections in the new visual system, (2) empty/loading/error redesigned not just happy path, (3) same-inputs->same-outputs + dark-mode contrast + EN/ES parity, (4) no API/behavior change ships, billing and Opera flows identical pre/post-redesign, frozen Room-Board files/harness unaffected.

Purpose: phase-close verification gate. No planned source changes (files_modified: []); a real defect found becomes a blocking targeted fix in the owning section file and is logged to .wolf/buglog.json.
Output: verification evidence in the summary; green gates + regression re-pass + live per-section confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# Prior-plan summaries — what was built, which flag keys + i18n keys exist, any deferred pockets,
# any documented discretion decisions (Management ROI's per-query error granularity, Opera
# Integration's conflictsQuery choice, AI Copilot's CreditUsageCard treatment)
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-02-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-03-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-04-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-05-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-06-SUMMARY.md
@.planning/phases/34-management-admin-sections/34-07-SUMMARY.md

# Prior close-out precedent (regression-harness local-standalone-build workaround, Supabase
# service-role flag-flip approach, dataI18nSkip mangling fix)
@.planning/phases/33-core-operational-sections/33-07-SUMMARY.md

# Regression harness + frozen manifest
@apps/web/playwright.regression.config.ts
@apps/web/frozen-files.json
@apps/web/frozen-files-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full standing gate suite + Room-Board regression re-pass on combined changes</name>
  <files></files>
  <action>
From `apps/web`, run the full standing web gate suite over the combined Phase-34 tree and confirm all green:
- `npm run type-check`
- `npm run check:frozen-files` — MUST show zero frozen-hash drift AND zero room-status value drift. Confirm the allowlist (`frozen-files-allowlist.json`) is still `entries: []` unless a wave-2 plan legitimately needed one (unlikely — none of the 7 frozen files were in any wave-2 plan's `files_modified`).
- `npm run check:contrast` — dark-mode WCAG AA over every new/extended token pairing across the 10 sections.
- `npm run check:i18n-parity` and `npm run verify:i18n-gate` — en/es parity intact after 34-01's key additions across all 8 new namespaces + the `header` extension.
- `npm run build`.
Then re-pass the Phase-30 Room-Board regression harness: `npx playwright test --config=playwright.regression.config.ts`. Per 33-07's precedent, FIRST check whether the local tree is still ahead of `origin/main` (the harness's hardcoded default production URL may be dead) — if so, repeat the local-standalone-build workaround (`npm run build` + `.next/standalone`, a temporary CSP-localhost patch to `next.config.mjs` fully reverted afterward, `PLAYWRIGHT_BASE_URL` pointed at the local build) rather than assuming the deployed URL works; if the tree has since been pushed/deployed, the default URL may work directly — verify before choosing. Flip the regression fixture tenant's `web_redesign_sections` to include the 8 new Phase-34 flag keys (`reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations`) via direct Supabase service-role access (`apps/api/.env`'s `SUPABASE_SERVICE_ROLE_KEY`, matching 32-06/33-07's precedent — no Supabase MCP write access in this environment), run the harness for BOTH flag-off and flag-on, then restore the fixture tenant's flag array back to its permanent baseline afterward.
Expect the harness to pass at zero pixel drift on the 2 boards it protects (`RoomStatusBoard`, `EngineeringRoomBoard`) — none of Phase 34's changes touch any file those boards import. `RoomDetailDrawer`'s known, pre-existing, deterministic 3-pixel/0.01% sub-pixel font-AA diff (documented by 32-06/33-07) is expected environment noise, not a regression, if it recurs identically.
If any gate fails on a real defect, fix it in the owning section file (blocking Rule-1 fix — this un-empties files_modified for that fix only) and re-run until green; log the fix to `.wolf/buglog.json`.
  </action>
  <verify>All of type-check / check:frozen-files / check:contrast / check:i18n-parity / verify:i18n-gate / build exit 0; check:frozen-files explicitly confirms all 5 room-board-related frozen files unchanged + allowlist empty (or justified); Room-Board regression suite reports zero-drift on RoomStatusBoard/EngineeringRoomBoard for both flag-off and flag-on (RoomDetailDrawer's documented sub-pixel noise excepted); any defect found is fixed and re-verified green; regression fixture tenant restored to baseline afterward</verify>
  <done>Every standing web gate green on the combined Phase-34 changes; Room-Board regression re-passes at zero drift (both flag states); zero frozen-file / room-status drift across all six content plans; fixture tenant restored</done>
</task>

<task type="auto">
  <name>Task 2: Live flag-on/flag-off browser self-verification of all 10 sections (light+dark, EN+ES, network-inert)</name>
  <files></files>
  <action>
Per the mandatory Self-Verification Policy (CLAUDE.md): start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Enable the 8 dedicated-flag sections for the test hotel by adding all eight keys — `reports`, `managementRoi`, `staff`, `settings`, `aiCopilot`, `billing`, `guestFeedback`, `integrations` — to its `web_redesign_sections` array (via `hotelsApi.update` PATCH /v1/hotels/{id} or direct Supabase service-role access, matching Phase 31-33's approach; there is no settings-UI toggle). Notifications and Late Checkout piggyback on the pre-existing `shell`/`dashboard` flags — verify these are already ON for the test hotel from prior phases (if not, add them too, matching Phase 31/32's own close-out precedent).

Verify each of the 10 sections:
- FLAG ON: the section renders the v2 redesign. Confirm its empty, loading (skeleton not spinner — Reports' 5 tabs, Management ROI's auth-guard+Sections, Staff's invitations/EditModal sub-queries, Settings-general's form skeleton, Billing's 3 queries + auth-guard, Guest Feedback, Opera Integration's statusQuery, Notifications' dropdown, Late Checkout's SkeletonRow — EXCEPT AI Copilot, whose chat/typing-indicator pattern is deliberately unchanged per its plan), and error states render. Force an error (stop the API, or point a query at a bad path via devtools) to confirm each section's StateBlock/inline error+retry shows and retry re-fires the correct query (not a sibling query — especially check Management ROI's per-query onRetry granularity and Guest Feedback's newly-added onRetry). Confirm AI Copilot's chat-error-as-message pattern is unchanged (force a chat API error, confirm it appears as an AI-role bubble, NOT a page-level StateBlock) and CreditUsageCard's $0.00/0% values are unchanged regardless of that query's success/failure. Confirm Opera Integration's disconnected/connect-form view still renders as a full form, not an "empty state" placeholder.
- FLAG OFF: each section renders today's UI unchanged (legacy branch) — spot-check at least 4 of the 10.
- LIGHT + DARK: toggle theme; confirm no contrast/legibility break (backs up check:contrast by eye) across all 10.
- EN + ES: toggle locale; confirm the 8 new namespaces (+ header extension) render in both with no missing-key fallback AND no domTranslations.ts EN/ES hybrid mangling (Pitfall 1) on any new/changed PageHeader title/subtitle/tab — this is the exact defect class that hit Phase 32 (bug-962) and Phase 33 (bug-963); check every section that has a `PageHeader` call site touched by a wave-2 plan (Reports, Management ROI, Billing, Opera Integration, AI Copilot at minimum).
- NETWORK DIFF (Success Criterion #3): with devtools Network open, spot-check at least Reports, Staff, Billing, and Opera Integration that the SAME requests fire flag-on vs flag-off (no new/removed endpoints) — proving zero behavior/data change. Confirm Billing's `portalMutation`/`checkoutMutation` and Opera Integration's 5 mutations are NOT accidentally triggered by any new v2 rendering path. Confirm no failed/forbidden (401/403) calls.
- No console errors on any section.
Record exactly what was live-verified vs code-traced, and any follow-up flagged for the orchestrator (e.g. flag-on regression not runnable this session, roles not seeded, deferred items collected from the wave-2 summaries — especially Staff's 4 modals' form-validation strings, AI Copilot's chat error copy, and whichever conflictsQuery/discretion decisions 34-05 documented).
  </action>
  <verify>Screenshots/notes captured for all 10 sections flag-on and a flag-off spot check on ≥4; empty/loading/error observed for each (with a forced error confirming retry re-fires the CORRECT query, not a sibling); AI Copilot's chat-error-as-message + hardcoded CreditUsageCard values confirmed unchanged; Opera Integration's disconnected view confirmed NOT StateBlock-empty; light/dark + EN/ES confirmed with no missing-key fallback and no domTranslations.ts mangling on any touched PageHeader; network diff spot-checked inert on ≥4 sections including confirming no mutation auto-fires; zero console errors and zero forbidden API calls in checked sections; any un-verifiable item explicitly flagged for the orchestrator</verify>
  <done>All 10 sections live-confirmed to render the v2 redesign flag-on and legacy flag-off, with empty/loading/error states in light+dark and EN+ES, network-inert (same requests flag-on vs off, no accidental mutation triggers), no console errors or forbidden calls, no i18n DOM-mangling on any new PageHeader; AI Copilot's and Opera Integration's deliberately-preserved special-case behaviors confirmed; coverage gaps + deferred items flagged precisely</done>
</task>

</tasks>

<verification>
- Full standing web gate suite green on combined changes (type-check, check:frozen-files, check:contrast, check:i18n-parity, verify:i18n-gate, build).
- check:frozen-files confirms zero drift on all 7 frozen files; Room-Board regression re-passes at zero drift (both flag states, RoomDetailDrawer's known noise excepted).
- All 10 sections live-verified flag-on (v2) and flag-off spot-checked (legacy, ≥4 sections); empty/loading/error render correctly (skeleton not spinner, except AI Copilot's deliberate chat pattern); light+dark and EN+ES confirmed with no domTranslations.ts mangling; network diff inert with no accidental mutation triggers; no console errors / forbidden calls.
</verification>

<success_criteria>
Phase 34 verified end-to-end: all 4 Success Criteria confirmed against the running app and the gate suite — 10 sections in the new visual system with redesigned empty/loading/error states, same-inputs->same-outputs network behavior, dark-mode contrast and EN/ES parity green, and zero API/behavior change (billing and Opera flows identical pre/post-redesign, frozen Room-Board files/harness provably intact). Any residual coverage gap or deferred item is precisely flagged for the orchestrator's close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-08-SUMMARY.md` recording: the exact gate results, the regression outcome (and whether flag-on was runnable this session, and which URL-reachability path was used), a per-section table of live-verified vs code-traced for all 10 sections, the state-verification evidence (incl. the forced-error/retry-targets-correct-query check, the AI Copilot/Opera Integration special-case confirmations, and the network-diff spot-check), explicit confirmation of zero frozen-file drift, any defect found+fixed (with buglog id), and a consolidated list of all deferred items gathered from the six wave-2 summaries plus any follow-up left for the orchestrator (mirroring 33-07's close-out format). Also update `.planning/STATE.md`'s Current Position to reflect Phase 34 closed and Phase 35 next.
</output>
