---
phase: 34-management-admin-sections
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/app/(dashboard)/settings/general/page.tsx
  - apps/web/app/(dashboard)/settings/billing/page.tsx
  - apps/web/app/(dashboard)/settings/feedback/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of these 3 settings/* page bodies. No
# sibling wave-2 plan touches them. All 3 live under settings/layout.tsx's shared shell, which
# per CONTEXT stays legacy/untouched (leave layout.tsx completely alone — not in this plan's
# files_modified and must not be touched). Each gets its OWN flag key (settings, billing,
# guestFeedback per CONTEXT) — they are grouped into one plan purely for effort-matching (all 3
# are small-to-medium single-query-cluster pages), not because they share a flag. Does NOT edit
# en.ts/es.ts — the `settings`/`billing`/`guestFeedback` namespaces exist from 34-01; only
# consumes them. Does NOT touch settings/layout.tsx, settings/housekeeping/page.tsx (Cleaning
# Checklists, confirmed out of scope), or any other settings/* sub-route.

must_haves:
  truths:
    - "With the 'settings' flag ON, settings/general/page.tsx's hotel-profile fetch (currently zero loading/error UI) gets a v2 skeleton while loading and a StateBlock/inline error+retry if the fetch fails; flag OFF is byte-behaviorally identical to today (silent-defaults behavior preserved)"
    - "With the 'billing' flag ON, the 3 useQuery calls (subscription/credits/invoices) each gain isError+refetch wired to a StateBlock error+retry where none exists today, and the raw animate-spin auth-loading guard is folded into a v2 skeleton; flag OFF is byte-behaviorally identical to today"
    - "With the 'guestFeedback' flag ON, the single StateBlock's error object gains an onRetry calling refetch() (currently a static message with no retry button); flag OFF is byte-behaviorally identical to today"
    - "No query/mutation/field logic added or removed on any of the 3 pages; form-submission error handling (Settings-general's toast.error, Billing's portalError/checkoutError) is untouched — only each page's FETCH loading/error gets new UI"
  artifacts:
    - path: "apps/web/app/(dashboard)/settings/general/page.tsx"
      provides: "Reads isSectionRedesigned('settings', hotel) once; adds isLoading/isError/refetch to the hotel-full useQuery destructuring; v2 skeleton + error+retry over the form fields; legacy branch (no loading/error UI at all) unchanged"
      contains: "isSectionRedesigned('settings'"
    - path: "apps/web/app/(dashboard)/settings/billing/page.tsx"
      provides: "Reads isSectionRedesigned('billing', hotel) once; extends the 3 useQuery destructurings with isError/refetch; StateBlock error+retry per query; auth-loading spinner folded into v2 skeleton; legacy branch unchanged"
      contains: "isSectionRedesigned('billing'"
    - path: "apps/web/app/(dashboard)/settings/feedback/page.tsx"
      provides: "Reads isSectionRedesigned('guestFeedback', hotel) once; extends the useQuery destructuring with refetch; adds onRetry to the existing StateBlock error object; legacy branch unchanged"
      contains: "isSectionRedesigned('guestFeedback'"
  key_links:
    - from: "apps/web/app/(dashboard)/settings/general/page.tsx"
      to: "the hotel-full useQuery"
      via: "extended destructuring { data: fullHotel, isLoading, isError, refetch }; v2 skeleton/error gated on hydratedRef + isLoading/isError"
      pattern: "isSectionRedesigned\\('settings'"
    - from: "apps/web/app/(dashboard)/settings/billing/page.tsx"
      to: "subData / creditData / invoicesData queries"
      via: "each StateBlock error.onRetry calls that specific query's own refetch()"
      pattern: "isSectionRedesigned\\('billing'"
    - from: "apps/web/app/(dashboard)/settings/feedback/page.tsx"
      to: "the feedback-submissions useQuery"
      via: "StateBlock error.onRetry = () => refetch()"
      pattern: "isSectionRedesigned\\('guestFeedback'"
---

<objective>
Redesign the three smaller Settings sub-pages — Settings-general (hotel profile, 320 lines, zero PageHeader, zero loading/error UI at all today), Billing (539 lines, already uses PageHeader, 3 queries with no error/retry wiring), and Guest Feedback (101 lines, the cleanest reference implementation in the phase, needs only an `onRetry` addition) — each behind its OWN flag (`settings`, `billing`, `guestFeedback`). Grouped into one plan purely for effort-matching (all three are single-query-cluster pages under the shared, untouched `settings/layout.tsx` shell), not because they share a flag or any code.

Settings-general has a genuine net-new gap: its `useQuery` currently destructures only `{ data: fullHotel }` with NO loading/error UI whatsoever — if the fetch fails, the form silently stays on `react-hook-form` defaults forever. Billing has 3 queries, each destructured with custom aliases, none exposing `isError`/`refetch` today. Guest Feedback is nearly done — it just needs a retry button wired to its existing (unused-for-this-purpose) query.

Purpose: SEC-01b for Settings-general + Billing + Guest Feedback.
Output: 3 settings/* page bodies flag-gated and StateBlock/skeleton-wired, `settings/layout.tsx` untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

@apps/web/app/(dashboard)/settings/general/page.tsx
@apps/web/app/(dashboard)/settings/billing/page.tsx
@apps/web/app/(dashboard)/settings/feedback/page.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings-general — flag + net-new loading/error UI for the hotel-full fetch</name>
  <files>apps/web/app/(dashboard)/settings/general/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('settings', hotel)`.
2. Extend the `useQuery` destructuring from `const { data: fullHotel } = useQuery({...})` to `const { data: fullHotel, isLoading, isError, refetch } = useQuery({...})` — this is a genuinely NEW capability (no loading/error UI exists today at all), so gate it entirely behind `v2` to preserve zero-behavior-change for the legacy path: when `!v2`, render the form exactly as today regardless of `isLoading`/`isError` (i.e. do not let the new destructured fields influence the legacy branch's rendering).
3. Under `v2`: while `isLoading && !hydratedRef.current` (the existing hydration-guard ref), render a v2 skeleton over the form-field area (approximate the form's shape — a handful of skeleton rows/blocks matching the field count) instead of the bare `react-hook-form` defaults. While `isError`, render a `StateBlock status="error" error={{ message: t('settings.loadError'), onRetry: () => refetch() }}` above or in place of the form (your call, based on what reads best — a banner above the still-editable form, or replacing it entirely; prefer a banner so the user isn't blocked from manually re-entering data even if the initial fetch failed, matching the "form still usable" spirit of the existing silent-failure behavior).
4. Do NOT touch the `hydratedRef` reset `useEffect`, the `onSubmit` → `hotelsApi.update` flow, or its `toast.error(...)` submission-error handling — those are existing, working patterns that must not change (zero behavior change to submission, only the FETCH's loading/error UI is new).
5. Optionally (not required): if a `PageHeader` or bare `<h2>Hotel Profile</h2>` is trivially convertible to `t('settings.pageTitle')` without adding new chrome, do so under `v2`; do not add a `PageHeader` component if none exists today (this page renders inside `settings/layout.tsx`'s own title chrome — adding a second title would be a behavior/layout change, out of scope).
  </action>
  <verify>cd apps/web && npm run type-check succeeds on this file in isolation; grep confirms `isSectionRedesigned('settings'` and the extended `{ data: fullHotel, isLoading, isError, refetch }` destructuring both present; grep confirms `StateBlock` and `t('settings.loadError'` appear gated under a `v2` check; grep confirms the legacy branch (v2 false) has no new conditional referencing `isLoading`/`isError` (form renders unconditionally as before)</verify>
  <done>Settings-general reads the flag once; hotel-full query destructuring extended with isLoading/isError/refetch; v2 skeleton while loading, StateBlock error+retry while error; legacy branch (v2 false) renders exactly as before regardless of fetch state; submission flow (toast.error) untouched</done>
</task>

<task type="auto">
  <name>Task 2: Billing — flag + StateBlock error+retry on 3 queries; fold auth-loading spinner into v2 skeleton</name>
  <files>apps/web/app/(dashboard)/settings/billing/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('billing', hotel)`.
2. Extend each of the 3 `useQuery` destructurings to add `isError`/`refetch` (keeping each query's existing custom aliasing style): `{ data: subData, isLoading: subLoading, isError: subIsError, refetch: subRefetch }` (and the equivalent for `creditData`/`invoicesData`) — match the file's existing alias-naming convention exactly rather than inventing a different pattern.
3. Under `v2`, wire each section's existing loading UI's error-adjacent gap: where each of the 3 data blocks renders today, add `StateBlock status="error" error={{ message: t('billing.subscriptionLoadError'|'billing.creditsLoadError'|'billing.invoicesLoadError'), onRetry: () => subRefetch()|creditRefetch()|invoicesRefetch() }}` when that query's `isError` is true. When `!v2`, these 3 queries render exactly as today (no error UI, same as now).
4. Fold the raw top-level `animate-spin` auth-loading guard (`h-6 w-6 animate-spin rounded-full border-2 border-gray-300 border-t-amber-500`) into a v2 skeleton treatment under `v2`; keep the spinner unchanged when `!v2`.
5. Re-skin the local `SkeletonBlock`-equivalent (rows-based skeleton with `bg-amber-100/50` bars, defined just above `SettingsBillingPage`, ~line 95-98 — confirm exact name live) with v2 tokens (e.g. `bg-surface-3`) under `v2`, keeping the legacy bars when `!v2`.
6. If the invoices table has a literal "no invoices" empty case today, wire it to `t('billing.invoicesEmpty')` under `v2` (only if the key was actually added by 34-01 — check 34-01-SUMMARY.md; if 34-01 found no such literal existed and skipped the key, skip this sub-step).
7. Do NOT touch `portalMutation`/`checkoutMutation` or their `portalError`/`checkoutError` local-state inline error rendering (button-adjacent action errors, not data-fetch states — explicitly out of scope per RESEARCH.md, likely stays as-is). Add `dataI18nSkip={v2}` to the page's `PageHeader` (title="Billing & Usage") per Pitfall 1.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('billing'` present; grep confirms all 3 queries' destructuring now includes an `isError`/`refetch`-equivalent alias; grep confirms 3 distinct `StateBlock` error usages referencing `billing.subscriptionLoadError`/`billing.creditsLoadError`/`billing.invoicesLoadError`; grep confirms `portalError`/`checkoutError` local-state handling is unchanged; grep confirms `dataI18nSkip={v2}` on the PageHeader call; git status shows only settings/billing/page.tsx changed (plus settings/general/page.tsx and settings/feedback/page.tsx from this same plan's Tasks 1/3)</verify>
  <done>Billing page reads the flag once; all 3 queries gain isError/refetch and StateBlock error+retry; auth-loading spinner folded into v2 skeleton; local skeleton bars v2-token-ified; portalMutation/checkoutMutation error handling untouched; legacy branch unchanged flag-off; gates green</done>
</task>

<task type="auto">
  <name>Task 3: Guest Feedback — flag + add onRetry to the existing StateBlock error</name>
  <files>apps/web/app/(dashboard)/settings/feedback/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('guestFeedback', hotel)`.
2. Extend the `useQuery` destructuring from `{ data, isLoading, isError }` to `{ data, isLoading, isError, refetch }`.
3. This page's single `StateBlock` currently has `error={{ message: 'Feedback could not load.' }}` with NO `onRetry`. Under `v2`, change this to `error={{ message: t('guestFeedback.loadError'), onRetry: () => refetch() }}`. When `!v2`, keep the exact current static-message-no-retry object (`message: 'Feedback could not load.'`, no `onRetry` key) unchanged.
4. Similarly, under `v2` route the `empty: { icon: <MessageSquareWarning/>, title: '...', body: '...' }` object's `title`/`body` through `t('guestFeedback.empty.title')`/`t('guestFeedback.empty.body')` (icon stays the same JSX element either way). Keep the current hardcoded strings when `!v2`.
5. Spot-check the `Card`/`Badge`/`notificationCopy()` surrounding chrome for any raw hex/hardcoded-color classes — research found this file "nearly v2-clean already" (uses `text-ready`/`text-alert`/`text-ink3` CSS-var-backed utilities). If any genuinely raw color is found, convert it to the matching CSS-var token under `v2`; if none is found (likely), do not force a change.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('guestFeedback'` present; grep confirms the `useQuery` destructuring includes `refetch`; grep confirms the StateBlock error object under `v2` includes `onRetry: () => refetch()` while the `!v2` branch's error object has no `onRetry` key (legacy exactly as today); git status shows only the 3 settings files from this plan changed</verify>
  <done>Guest Feedback page reads the flag once; useQuery destructuring extended with refetch; StateBlock error gains onRetry under v2 (legacy stays static-message-no-retry); empty copy i18n'd under v2; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Settings-general: net-new loading/error UI gated entirely behind `v2`, legacy path unaffected by the new destructured fields.
- Billing: all 3 queries gain isError/refetch + StateBlock error+retry; auth-loading spinner folded into skeleton; action-adjacent mutation errors untouched.
- Guest Feedback: StateBlock error gains onRetry under v2; legacy stays exactly as today (static message, no retry).
- Only the 3 named settings/* files changed; `settings/layout.tsx` and `settings/housekeeping/page.tsx` untouched; no frozen file edited; no locale-file edit; no query/mutation/field change.
</verification>

<success_criteria>
Settings-general, Billing, and Guest Feedback each render in the new visual system behind their own flag, with every fetch's loading/error state served by a shared skeleton/StateBlock component and wired to that fetch's own refetch — Settings-general closing a genuine pre-existing UI gap (silent-forever-defaults on fetch failure), Billing closing a genuine error/retry gap on 3 queries, Guest Feedback adding the one missing piece (retry) to an otherwise-complete implementation — zero behavior/data change, `settings/layout.tsx` untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-04-SUMMARY.md` recording: the exact new loading/error UI added to Settings-general (and confirmation the legacy path is unaffected), the 3 Billing query aliasing names actually used and their StateBlock wiring, the Guest Feedback onRetry addition, the `settings.*`/`billing.*`/`guestFeedback.*` keys actually consumed, confirmation `settings/layout.tsx` was never touched, and any deferred item for the close-out list.
</output>
