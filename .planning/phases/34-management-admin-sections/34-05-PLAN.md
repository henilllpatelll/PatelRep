---
phase: 34-management-admin-sections
plan: 05
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/app/(dashboard)/settings/integrations/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of settings/integrations/page.tsx. No
# sibling wave-2 plan touches it. Opera Integration is a mature connect/sync/conflict state
# machine (Shape 2/3 hybrid per RESEARCH.md) — statusQuery already has full raw loading/error/
# retry markup wired to its own refetch(); this plan converts that raw markup to shared
# components, does NOT invent new state-machine behavior. Per CONTEXT/Pitfall 4, the
# disconnected/connect-form view is a PRIMARY UI STATE (like a login form), not a StateBlock
# "empty" case — do not wrap it in StateBlock. Does NOT edit en.ts/es.ts — the `integrations`
# namespace exists from 34-01; only consumes it.

must_haves:
  truths:
    - "With the 'integrations' flag ON, statusQuery's raw animate-pulse loading bars become a shared Skeleton, and its raw error+inline-retry div becomes a StateBlock error (still calling statusQuery.refetch()); flag OFF is byte-behaviorally identical to today"
    - "The connected/disconnected view split (operaStatus.connected boolean) is preserved exactly as today — the disconnected/connect-form state is NOT wrapped in StateBlock status=\"empty\" (it is a primary application state, not a no-data placeholder)"
    - "The 5 mutations (connect/sync/test/disconnect/resolveConflict) and their local-state result banners (successBanner/errorBanner/syncResult/testResult) are v2-token-styled but functionally unchanged — no mutation logic, onSuccess/onError handler, or banner-triggering condition modified"
    - "conflictsQuery's rendering (currently conditional on length > 0 with no dedicated loading/error UI) is either given a minimal v2 loading/error treatment or explicitly left as-is with that decision documented in the plan's summary — a deliberate choice, not an oversight"
  artifacts:
    - path: "apps/web/app/(dashboard)/settings/integrations/page.tsx"
      provides: "Reads isSectionRedesigned('integrations', hotel) once; statusQuery's raw skeleton/error markup replaced with shared Skeleton/StateBlock (same refetch wiring); banners/buttons v2-token-styled; connect/disconnect state machine and all 5 mutations unchanged; legacy branch unchanged"
      contains: "isSectionRedesigned('integrations'"
  key_links:
    - from: "apps/web/app/(dashboard)/settings/integrations/page.tsx (statusQuery error)"
      to: "statusQuery"
      via: "StateBlock error.onRetry = () => statusQuery.refetch() (same call the current raw <button> already makes)"
      pattern: "statusQuery.refetch"
---

<objective>
Redesign Opera Integration (`settings/integrations/page.tsx`, 508 lines) — the phase's most mature pre-existing state machine. `statusQuery` already has all three states wired as raw markup: loading bars, an error div with a working inline `<button onClick={() => statusQuery.refetch()}>Retry</button>`, and the "disconnected" state which IS the connect form itself (not an empty state). This plan converts the raw loading/error markup to the shared `Skeleton`/`StateBlock` components — a like-for-like visual migration, not new behavior — v2-token-ifies the 5 mutations' result banners and buttons, and makes a deliberate, documented call on whether `conflictsQuery` (a secondary panel, currently no dedicated state UI) gets one.

Purpose: SEC-01b for Opera Integration — the phase's example of "migrate raw-but-complete state handling to shared components" (mirrors Phase 33's Programs page-level error migration precedent).
Output: settings/integrations/page.tsx flag-gated, statusQuery's states on shared components, connect/disconnect state machine untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

@apps/web/app/(dashboard)/settings/integrations/page.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag + migrate statusQuery's raw loading/error markup to shared Skeleton/StateBlock</name>
  <files>apps/web/app/(dashboard)/settings/integrations/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('integrations', hotel)`.
2. `statusQuery` is kept as a named query object (not destructured) — `.refetch()` is already trivially callable, no destructuring change needed.
3. Loading (line ~457-461): replace the raw `animate-pulse` bars with the shared `Skeleton` component under `v2` (matching the current bars' approximate size/count so the layout doesn't jump); keep the raw bars when `!v2`.
4. Error (line ~465-473): replace the raw `<div>` + inline `<button onClick={() => statusQuery.refetch()}>Retry</button>` with `StateBlock status="error" error={{ message: t('integrations.loadError'), onRetry: () => statusQuery.refetch() }}` under `v2` — this is the EXACT SAME refetch call the current button already makes, just moved into the shared component's `onRetry` prop; keep the raw div+button when `!v2`.
5. Do NOT wrap the disconnected/connect-form view in `StateBlock status="empty"` (Pitfall 4) — `operaStatus.connected === false` renders the connect form as a fully-featured primary state both before and after this task, under both `v2` and `!v2`. Only re-skin its buttons/inputs with v2 tokens (`duration-fast ease-standard focus-visible:ring-[var(--focus-ring)]`) under `v2` — no structural change to the form itself.
6. `dataI18nSkip={v2}` on the page's `PageHeader` per Pitfall 1 (this file already uses `PageHeader`).
  </action>
  <verify>cd apps/web && npm run type-check succeeds on this file in isolation; grep confirms `isSectionRedesigned('integrations'` present; grep confirms `StateBlock` used for statusQuery's error path with `onRetry: () => statusQuery.refetch()`; grep confirms `Skeleton` used for statusQuery's loading path; grep confirms NO `StateBlock status="empty"` (or any StateBlock) wraps the disconnected/connect-form JSX; grep confirms `dataI18nSkip={v2}` on the PageHeader call</verify>
  <done>Opera Integration reads the flag once; statusQuery's raw loading bars/error+retry migrated to shared Skeleton/StateBlock (identical refetch call); connect/disconnect state machine (including the disconnected connect-form view) structurally unchanged; legacy branch unchanged flag-off</done>
</task>

<task type="auto">
  <name>Task 2: v2-token-ify mutation banners/buttons; decide + implement (or explicitly defer) conflictsQuery state UI</name>
  <files>apps/web/app/(dashboard)/settings/integrations/page.tsx</files>
  <action>
1. The 5 mutations (`connectMutation`, `syncMutation`, `testMutation`, `disconnectMutation`, `resolveConflictMutation`) feed local `useState` banners (`successBanner`, `errorBanner`, `syncResult`, `testResult`) rendered as raw `role="alert"` divs. Under `v2`, v2-token-ify these banners' styling (border/background/text color CSS vars matching the app's existing success/alert token pairing, e.g. `--ready`/`--alert` families used elsewhere in this codebase) WITHOUT changing when they appear, what triggers them, or their text content — these are transient action-result banners, not data-fetch states, and stay as local-state banners per RESEARCH.md's recommendation (do not attempt to route them through StateBlock).
2. `ConfirmDisconnectDialog` (referenced near line 500, takes a `loading` prop from `disconnectMutation.isPending`) — v2-token-ify its buttons/borders under `v2` if it has any hardcoded chrome; do not alter its confirm/cancel logic.
3. `conflictsQuery`'s data is rendered conditionally (only shown when `.length > 0`, `enabled: Boolean(operaStatus?.connected)`) with no explicit loading/error UI of its own today. Make ONE deliberate choice and implement it under `v2`:
   - Option A (recommended default, matches the "secondary panel" framing in RESEARCH.md): add a minimal loading skeleton (`conflictsQuery.isLoading`) and a compact inline error+retry (`conflictsQuery.isError`, `onRetry: () => conflictsQuery.refetch()`, copy from `t('integrations.conflicts.loadError')`) scoped to just the conflicts panel — small footprint, doesn't compete visually with the main connection-status card.
   - Option B: explicitly leave `conflictsQuery` with no dedicated state UI (current behavior: silently shows nothing until data arrives) if, after reading the live file, the panel is judged too minor/rarely-populated to warrant it.
   Pick Option A unless the live code reveals a reason Option B is clearly better (e.g. the panel is deprecated or already covered by the parent's error state); document whichever is chosen in the plan's SUMMARY. Either way, do NOT add a new query or change `conflictsQuery`'s `queryKey`/`enabled`/`select`.
4. Do NOT edit frozen files. Do NOT change any mutation's `onSuccess`/`onError` handler logic, only its rendered banner's visual styling.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms the 5 mutations' banner JSX still references the same `successBanner`/`errorBanner`/`syncResult`/`testResult` state variables (unchanged triggering logic) with v2-conditional className additions only; grep confirms whichever conflictsQuery decision was made (Option A: `conflictsQuery.isError`/`conflictsQuery.refetch` present; Option B: no new conditional added) matches what's documented; git status shows only settings/integrations/page.tsx changed</verify>
  <done>Mutation result banners and ConfirmDisconnectDialog v2-token-styled with zero logic change; a deliberate, documented decision made and implemented for conflictsQuery's state UI; no query/mutation logic altered; legacy branch unchanged flag-off; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- statusQuery's loading/error migrated to shared Skeleton/StateBlock with identical refetch wiring; disconnected/connect-form view NOT StateBlock-wrapped.
- Mutation banners v2-token-styled with zero trigger/logic change; conflictsQuery's state-UI decision made deliberately and documented.
- Only settings/integrations/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation logic change; connect/disconnect state machine behaviorally identical flag-on vs flag-off.
</verification>

<success_criteria>
Opera Integration renders in the new visual system behind its own flag, with statusQuery's already-complete raw state handling migrated to shared Skeleton/StateBlock components (same refetch call), the connect/disconnect state machine structurally untouched (disconnected state correctly NOT treated as an empty state), mutation banners v2-token-styled, and conflictsQuery's minor state-UI gap deliberately addressed or documented — zero behavior/data change.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-05-SUMMARY.md` recording: confirmation the statusQuery migration preserves the exact same refetch call, confirmation the disconnected/connect-form view was NOT wrapped in StateBlock, which conflictsQuery option was chosen and why, the `integrations.*` keys actually consumed, confirmation no mutation logic changed, and any deferred item for the close-out list.
</output>
