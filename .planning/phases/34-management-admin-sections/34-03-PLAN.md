---
phase: 34-management-admin-sections
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - apps/web/app/(dashboard)/staff/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of staff/page.tsx. No sibling wave-2 plan
# touches it. Staff is Shape 2 (partially modernized per RESEARCH.md) — it already uses
# StateBlock/PageHeader/Card/Button unconditionally, so this plan's job is narrower than a
# ground-up migration: add the flag, v2-token-ify the ad-hoc filter/table/modal chrome, and close
# the specific gaps research identified (invitationsQuery has no error branch; EditStaffModal's
# schedulesQuery/customRolesQuery have no loading skeleton and no error state at all). Does NOT
# edit en.ts/es.ts — the `staff` namespace exists from 34-01; only consumes it.

must_haves:
  truths:
    - "With the 'staff' flag ON, the main staff table's existing StateBlock wiring (loading/error/empty/retry on staffQuery) renders with v2-token-styled surrounding chrome (search input, role/status filters, table borders/hover) instead of hardcoded amber/gray classes; flag OFF is byte-behaviorally identical to today"
    - "The Pending Invitations table's StateBlock gains an error branch (currently loading-only) wired to invitationsQuery.refetch(), in addition to its existing loading state"
    - "EditStaffModal's schedulesQuery (currently a bare 'Loading…' <p>, no error UI at all) and customRolesQuery (no loading or error UI at all) both gain a shared-Skeleton loading state and a StateBlock/inline error+retry wired to their own refetch()"
    - "No query/mutation/field/RBAC logic added or removed; the 4 modals' react-hook-form validation-error strings are untouched (deferred, out of scope)"
  artifacts:
    - path: "apps/web/app/(dashboard)/staff/page.tsx"
      provides: "Reads isSectionRedesigned('staff', hotel) once; v2-token-ifies main table + invitations chrome; invitationsQuery gains error+retry; EditStaffModal's schedulesQuery/customRolesQuery gain loading skeleton + error+retry; legacy branch unchanged"
      contains: "isSectionRedesigned('staff'"
  key_links:
    - from: "apps/web/app/(dashboard)/staff/page.tsx (invitations StateBlock)"
      to: "invitationsQuery"
      via: "error.onRetry = () => invitationsQuery.refetch()"
      pattern: "invitationsQuery"
    - from: "apps/web/app/(dashboard)/staff/page.tsx (EditStaffModal)"
      to: "schedulesQuery / customRolesQuery"
      via: "StateBlock or inline error+retry calling each query's own refetch()"
      pattern: "customRolesQuery"
---

<objective>
Redesign Staff (`staff/page.tsx`, 1075 lines) — the largest single-file section and the clearest Shape-2 "partially modernized" example (RESEARCH.md): it already uses `StateBlock`/`PageHeader`/`Card`/`Button` unconditionally for its main table, so this plan's scope is narrower than a ground-up migration. Add `isSectionRedesigned('staff', hotel)`, v2-token-ify the ad-hoc-styled surrounding chrome (search input, role/status filter selects, table row hover/borders — currently `focus:ring-amber-400`/`border-line`/`hover:bg-gray-50` hardcoded classes, same pattern already live in `tasks/page.tsx`), and close three specific gaps: (1) the Pending Invitations table's StateBlock has loading but no error branch, (2) `EditStaffModal`'s `schedulesQuery` has a bare "Loading…" text with zero error handling, (3) `EditStaffModal`'s `customRolesQuery` has zero loading or error handling at all.

Purpose: SEC-01b for Staff — the Phase-34 analog of Phase 33's "already mostly-built, needs surgical gap-closing" sections (Evidence/Guest Feedback class).
Output: staff/page.tsx flag-gated, v2-token-styled, with all three query-state gaps closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-management-admin-sections/34-CONTEXT.md
@.planning/phases/34-management-admin-sections/34-RESEARCH.md
@.planning/phases/34-management-admin-sections/34-01-SUMMARY.md

@apps/web/app/(dashboard)/staff/page.tsx

# Reference: the existing v2-token pattern already live for form/table chrome (focus-visible
# ring, duration-fast, ease-standard) — match this file's conventions exactly, do not invent new
# token names.
@apps/web/app/(dashboard)/tasks/page.tsx

# Flag + state + primitives contracts (import-only; Button.tsx is frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag + v2-token-ify main table/invitations chrome; close invitationsQuery's error gap</name>
  <files>apps/web/app/(dashboard)/staff/page.tsx</files>
  <action>
1. In `StaffPage`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('staff', hotel)`.
2. V2-token-ify the ad-hoc chrome under `v2` (search input, role/status filter `<select>`s, table container borders, row hover states): replace hardcoded `focus:ring-amber-400`-style classes with `duration-fast ease-standard focus-visible:ring-2 focus-visible:ring-[var(--focus-ring)]`, replace `border-line`/`hover:bg-gray-50`-style raw classes with the v2 token equivalents already established in `tasks/page.tsx` (read its lines ~151/162/643/658/667 for the exact pattern to mirror). Keep the legacy classes intact when `!v2` (conditional className, not a wholesale replace — e.g. `className={v2 ? "..." : "..."}` or a shared base + conditional extra classes, matching whichever style this file already leans toward for conditional rendering).
3. The main staff table's existing `<StateBlock status={staffQuery.isLoading ? 'loading' : staffQuery.isError ? 'error' : filteredStaff.length === 0 ? 'empty' : null} ...>` wiring is ALREADY complete (loading/error/empty/retry on `staffQuery`) — do NOT restructure this logic; only re-skin its surrounding chrome/copy source if `v2` (e.g. route its `error.message`/`empty.title`/`empty.body` through `t('staff.*')` only if the current copy isn't already using a suitable key — check 34-01-SUMMARY.md for what's actually available; if no staff-specific main-table key exists, this StateBlock's copy stays as today, since 34-01 intentionally scoped `staff.*` to only the invitations/editModal gaps).
4. Pending Invitations table: its `<StateBlock status={invitationsQuery.isLoading ? 'loading' : null} ...>` currently has NO error branch. Extend the `status` ternary to `invitationsQuery.isLoading ? 'loading' : invitationsQuery.isError ? 'error' : null` and add `error={{ message: t('staff.invitations.loadError'), onRetry: () => invitationsQuery.refetch() }}`. `invitationsQuery`'s destructuring already exposes `refetch` per research (or extend it if not — verify live) — do not restructure the outer conditional (`invitations.length > 0 || invitationsQuery.isLoading`) that gates whether this section renders at all; the loading skeleton itself can be re-skinned with v2 tokens under `v2` matching the pattern in the main table.
5. Do NOT touch `ROLE_AVATAR_COLORS` (decorative, out of scope) or any of the 4 modals in this task (handled in Task 2). Do NOT edit frozen `Button.tsx`/`primitives.tsx` — only consume via className overrides at call sites.
  </action>
  <verify>cd apps/web && npm run type-check succeeds on this file in isolation; grep confirms `isSectionRedesigned('staff'` present; grep confirms `invitationsQuery.isError` now feeds the StateBlock `status` ternary and an `error` object with `onRetry: () => invitationsQuery.refetch()` exists; grep confirms `focus-visible:ring-[var(--focus-ring)]` and `duration-fast` now present in the filter/search/table chrome under the v2 branch; grep confirms legacy `focus:ring-amber-400`-style classes still present in the non-v2 branch (nothing deleted, only conditionally added)</verify>
  <done>Staff page reads the flag once; main table's existing StateBlock wiring preserved and its surrounding filter/search/table chrome v2-token-ified; Pending Invitations StateBlock gains an error branch wired to invitationsQuery.refetch(); legacy classes/behavior unchanged flag-off</done>
</task>

<task type="auto">
  <name>Task 2: EditStaffModal — schedulesQuery loading skeleton + error+retry; customRolesQuery loading + error+retry</name>
  <files>apps/web/app/(dashboard)/staff/page.tsx</files>
  <action>
Inside `EditStaffModal` (defined in the same file, ~line 452):
1. `schedulesQuery` (`enabled: !!overrideRole`, `select: res => res.data`) currently renders a bare `<p className="text-xs text-gray-400">Loading…</p>` (line ~593-594) with NO error branch at all — if the fetch fails, the UI silently shows an empty schedule-override list forever. Under `v2`: replace the "Loading…" text with a small shared `Skeleton` (or a locally-scoped skeleton row matching the modal's compact layout — this is a small sub-region, not a full-page state, so keep the visual footprint modest, e.g. 2-3 skeleton rows sized to the schedule-override list). Add an error branch: `schedulesQuery.isError && <StateBlock status="error" error={{ message: t('staff.editModal.schedulesLoadError'), onRetry: () => schedulesQuery.refetch() }} />` (or an equivalent compact inline error+retry if StateBlock's default sizing doesn't fit this modal sub-region well — match whichever the modal's existing error-rendering convention already uses, since the modal has its own top-level `error: string | null` alert pattern for mutation errors; keep this NEW query-error distinct from that existing mutation-error state, do not conflate the two). When `!v2`, keep the exact current "Loading…" text and the current silent-failure behavior (no behavior change to the legacy path).
2. `customRolesQuery` (no `enabled` gate — always fires) currently has NO loading UI and NO error UI at all. Under `v2`: add a loading skeleton (same compact style as step 1) while `customRolesQuery.isLoading`, and an error+retry (`t('staff.editModal.rolesLoadError')`, `onRetry: () => customRolesQuery.refetch()`) while `customRolesQuery.isError`. When `!v2`, render exactly as today (no loading/error UI — a genuine pre-existing gap being closed only under the flag, not retroactively "fixed" for legacy users, per zero-behavior-change).
3. Do NOT touch the 3 mutations (`updateMutation`, `createScheduleMutation`, `deleteScheduleMutation`) or the existing top-level `error: string | null` mutation-error alert — those stay exactly as-is (zero behavior change to submission error handling).
4. Do NOT touch `ConfirmDeactivateDialog`, `AddDirectModal`, or `InviteModal` — none of the three has a query of its own (only mutations with `isPending`/`setError('root', ...)`), and their form-validation strings are explicitly deferred per CONTEXT (same class as Phase 33's deferred form-validation pockets). v2-token-ify only if a genuinely hardcoded chrome color (not a validation string) is trivially adjacent to code you're already touching in step 1/2 — otherwise leave them untouched to keep this plan's diff surgical.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `schedulesQuery.isError` and `customRolesQuery.isError` are both now checked somewhere in EditStaffModal's JSX under the v2 branch; grep confirms `staff.editModal.schedulesLoadError` and `staff.editModal.rolesLoadError` both referenced; grep confirms the modal's pre-existing mutation `error` state variable is untouched/still used only for mutation errors; git status shows only staff/page.tsx changed (combined with Task 1)</verify>
  <done>EditStaffModal's schedulesQuery gains a v2 skeleton (replacing "Loading…" text) and error+retry; customRolesQuery gains a v2 loading skeleton and error+retry where neither existed before; mutation-error handling untouched; legacy branch (v2 false) unchanged; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Flag read once in `StaffPage`; main table's existing StateBlock wiring preserved, surrounding chrome v2-token-ified; invitations StateBlock gains an error branch.
- EditStaffModal's schedulesQuery and customRolesQuery both gain loading skeleton + error+retry under v2, previously absent (customRolesQuery) or text-only (schedulesQuery).
- Only staff/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation/field/RBAC change; 4 modals' form-validation strings untouched.
</verification>

<success_criteria>
Staff renders in the new visual system behind its own flag, with its already-solid main-table StateBlock wiring preserved and re-skinned, its surrounding ad-hoc chrome v2-token-ified matching tasks/page.tsx's established pattern, and its three genuine query-state gaps (invitations error, EditStaffModal's two under-covered sub-queries) closed — zero behavior/data/RBAC change, form-validation strings deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/34-management-admin-sections/34-03-SUMMARY.md` recording: exactly which chrome classes were converted to v2 tokens, confirmation the main table's StateBlock logic was preserved unchanged, the exact wiring added for invitationsQuery/schedulesQuery/customRolesQuery (including whether StateBlock or a compact inline pattern was used inside the modal), the `staff.*` keys actually consumed, confirmation the 4 modals' form-validation strings and the mutation-error alert were left untouched, and any deferred item for the close-out list.
</output>
