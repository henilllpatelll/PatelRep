---
phase: 19-rbac-audit-and-normalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/routers/guest_requests.py
  - apps/api/tests/test_guest_requests_delete_rbac.py
autonomous: true

must_haves:
  truths:
    - "DELETE /v1/guest-requests/{id} returns 403 for a housekeeper (non-management role)"
    - "DELETE /v1/guest-requests/{id} returns 204 for a gm (management role) on an existing request"
    - "The existing tenant-scoping (.eq('tenant_id', current_user.hotel_id)) and cascade behavior of the delete handler is unchanged"
  artifacts:
    - path: "apps/api/routers/guest_requests.py"
      provides: "Role-gated DELETE /{request_id} reusing SLA_POLICY_ROLES"
      contains: "SLA_POLICY_ROLES"
    - path: "apps/api/tests/test_guest_requests_delete_rbac.py"
      provides: "Live-JWT TestClient assertions for 403 (housekeeper) and 204 (gm)"
      contains: "_auth_header"
  key_links:
    - from: "apps/api/routers/guest_requests.py delete_guest_request"
      to: "SLA_POLICY_ROLES membership check"
      via: "if current_user.role not in SLA_POLICY_ROLES: raise HTTPException(403)"
      pattern: "not in SLA_POLICY_ROLES"
    - from: "apps/api/tests/test_guest_requests_delete_rbac.py"
      to: "DELETE /v1/guest-requests/{id}"
      via: "TestClient(app).delete with _auth_header('housekeeper') and _auth_header('gm')"
      pattern: "client.delete.*guest-requests"
---

<objective>
Close RBAC-02: `DELETE /guest-requests/{request_id}` (guest_requests.py:585-618) is currently ungated — any authenticated tenant user, including a housekeeper, can permanently delete a guest request and cascade-delete its linked task + task_comments. Add an inline role gate restricting deletion to management roles, and prove it live with a pytest that signs a real JWT and exercises the full FastAPI + dependency chain.

Purpose: RBAC-02 requirement + ROADMAP success criterion 2 (403 for housekeeper, succeeds for management).
Output: gated delete handler + `test_guest_requests_delete_rbac.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rbac-audit-and-normalization/19-RESEARCH.md
@apps/api/routers/guest_requests.py
@apps/api/tests/test_lost_found_delete.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing RBAC test for DELETE /guest-requests/{id} (RED)</name>
  <files>apps/api/tests/test_guest_requests_delete_rbac.py</files>
  <action>
Create `apps/api/tests/test_guest_requests_delete_rbac.py` following the exact harness in `apps/api/tests/test_lost_found_delete.py` (FakeDB + real-JWT `_auth_header` + `TestClient(app)`). Details:

- Copy the `_auth_header(role, hotel_id="hotel-a", user_id="user-a-1")` helper verbatim from test_lost_found_delete.py (signs an HS256 JWT with `settings.supabase_jwt_secret`, claims: sub, role, hotel_id, aud="authenticated").
- Import the router module as `from routers import guest_requests as guest_requests_router` and monkeypatch its module-level `supabase` singleton: `monkeypatch.setattr(guest_requests_router, "supabase", db)`.
- Seed a FakeDB with one deletable request: `FakeDB({"guest_requests": [{"id": "req-1", "tenant_id": "hotel-a", "task_id": None}]})`. `task_id: None` keeps the cascade branch (task_comments/tasks deletes) from running so the test stays focused on the role gate. Import FakeDB from `tests.smoke.fake_supabase`.
- Test A `test_delete_guest_request_forbidden_for_housekeeper`: `client.delete("/v1/guest-requests/req-1", headers=_auth_header("housekeeper"))` asserts `response.status_code == 403`. Also assert the request row is still present (`db.rows["guest_requests"]` unchanged) to prove the gate blocks before any DB delete.
- Test B `test_delete_guest_request_allowed_for_gm`: `client.delete("/v1/guest-requests/req-1", headers=_auth_header("gm"))` asserts `response.status_code == 204` and `db.rows["guest_requests"] == []`.

Run `cd apps/api && python -m pytest tests/test_guest_requests_delete_rbac.py -q`. Test A MUST FAIL at this point (the endpoint is still ungated, so a housekeeper delete currently returns 204, not 403). Test B should pass. This RED state confirms the test actually exercises the gate. Do NOT edit the router in this task.
  </action>
  <verify>`cd apps/api && python -m pytest tests/test_guest_requests_delete_rbac.py -q` — `test_delete_guest_request_forbidden_for_housekeeper` FAILS (currently 204 not 403), `test_delete_guest_request_allowed_for_gm` PASSES.</verify>
  <done>Test file exists; the housekeeper-403 assertion fails against the still-ungated endpoint (RED), confirming the test is meaningful.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SLA_POLICY_ROLES gate to delete_guest_request (GREEN)</name>
  <files>apps/api/routers/guest_requests.py</files>
  <action>
In `apps/api/routers/guest_requests.py`, in `delete_guest_request` (starts line 585), add a role gate as the FIRST statement inside the function body, before the `supabase.table("guest_requests").select(...)` lookup:

```python
    if current_user.role not in SLA_POLICY_ROLES:
        raise HTTPException(status_code=403, detail="Not authorized to delete guest requests")
```

Reuse the existing module-level `SLA_POLICY_ROLES = {"gm", "housekeeping_supervisor"}` (line 40) — do NOT define a new constant. This matches the router's own inline-check idiom (this file uses inline `if current_user.role not in X: raise HTTPException(403, ...)` at lines 213, 300, 334, 444, 476 — never `Depends(require_role(...))`) and its established management-tier precedent (SLA-policy create/delete already gates on SLA_POLICY_ROLES). Per RESEARCH Decision 3, front_desk and engineer are deliberately excluded from delete authority. Do NOT change the tenant-scoping `.eq("tenant_id", current_user.hotel_id)` filters or the cascade deletes (task_comments/tasks) — only prepend the gate.

Then re-run the RBAC test — both cases must now pass (GREEN).
  </action>
  <verify>`cd apps/api && python -m pytest tests/test_guest_requests_delete_rbac.py -q` — both tests PASS (housekeeper → 403, gm → 204). `grep -n "not in SLA_POLICY_ROLES" apps/api/routers/guest_requests.py` shows the new gate inside delete_guest_request.</verify>
  <done>DELETE /v1/guest-requests/{id} returns 403 for housekeeper and 204 for gm; tenant-scoping and cascade logic unchanged.</done>
</task>

</tasks>

<verification>
- `cd apps/api && python -m pytest tests/test_guest_requests_delete_rbac.py -q` passes (2 tests).
- The gate reuses SLA_POLICY_ROLES; no new role constant introduced in guest_requests.py.
- No change to `.eq("tenant_id", ...)` scoping or the cascade delete of task_comments/tasks.
</verification>

<success_criteria>
ROADMAP success criterion 2 is satisfied live: DELETE /guest-requests/{id} returns 403 for a housekeeper and succeeds (204) for a management role, verified through a real HTTP request against the app + dependency chain.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-audit-and-normalization/19-02-SUMMARY.md`
</output>
