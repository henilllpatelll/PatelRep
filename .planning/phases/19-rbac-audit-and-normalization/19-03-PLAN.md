---
phase: 19-rbac-audit-and-normalization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/routers/lost_found.py
  - apps/api/tests/test_lost_found_delete.py
autonomous: true

must_haves:
  truths:
    - "PATCH /v1/lost-found/{item_id} returns 403 for a housekeeper (non-authorized role)"
    - "DELETE /v1/lost-found/{item_id} returns 403 for a housekeeper (non-authorized role)"
    - "PATCH and DELETE still succeed for gm (existing 204/200 delete behavior preserved)"
    - "The custody-state role set {front_desk, housekeeping_supervisor, gm} is reused — no new constant, no change to tenant scoping"
  artifacts:
    - path: "apps/api/routers/lost_found.py"
      provides: "Role-gated PATCH /{item_id} and DELETE /{item_id} matching the custody-events gate"
      contains: "Not authorized"
    - path: "apps/api/tests/test_lost_found_delete.py"
      provides: "Added housekeeper-403 cases for PATCH and DELETE alongside existing gm cases"
      contains: "housekeeper"
  key_links:
    - from: "apps/api/routers/lost_found.py update_lost_found_item / delete_lost_found_item"
      to: "custody-state role membership check"
      via: 'if current_user.role not in {"front_desk","housekeeping_supervisor","gm"}: raise HTTPException(403)'
      pattern: 'not in \{"front_desk"'
    - from: "apps/api/tests/test_lost_found_delete.py"
      to: "PATCH + DELETE /v1/lost-found/{id}"
      via: "TestClient(app) calls with _auth_header('housekeeper')"
      pattern: "_auth_header\\(.housekeeper"
---

<objective>
Close RBAC-03's two confirmed gaps in `lost_found.py`: `PATCH /{item_id}` (lines 213-247) and `DELETE /{item_id}` (lines 250-263) are both ungated. PATCH can set `status` to claimed/donated/discarded — the exact custody-state transition already role-gated on `POST /{item_id}/custody-events` — so any authenticated user can bypass that gate by calling PATCH. DELETE permanently destroys the item record. Gate both to the same role set already used by custody-events, and extend the existing delete test with housekeeper-403 cases.

Purpose: RBAC-03 requirement (lost_found.py gap closure) + reinforces success criterion 2's "403 for a non-management role" evidence.
Output: gated PATCH + DELETE handlers + extended `test_lost_found_delete.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rbac-audit-and-normalization/19-RESEARCH.md
@apps/api/routers/lost_found.py
@apps/api/tests/test_lost_found_delete.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test_lost_found_delete.py with housekeeper-403 cases for PATCH and DELETE (RED)</name>
  <files>apps/api/tests/test_lost_found_delete.py</files>
  <action>
Extend the existing `apps/api/tests/test_lost_found_delete.py` (do NOT rewrite it — keep all existing gm/404 tests intact). Reuse its existing `_auth_header` helper and `_db_with_item_and_custody_history()` fixture. Add:

- `test_delete_item_forbidden_for_housekeeper`: `client.delete("/v1/lost-found/item-1", headers=_auth_header("housekeeper"))` asserts `response.status_code == 403` and that `db.rows["lost_found_items"]` still contains item-1 (gate blocks before delete).
- `test_patch_item_forbidden_for_housekeeper`: `client.patch("/v1/lost-found/item-1", json={"status": "claimed"}, headers=_auth_header("housekeeper"))` asserts `response.status_code == 403`. (Confirm the PATCH request-body shape by reading lines 213-247 of lost_found.py — it accepts a `dict` body; `{"status": "claimed"}` is a valid disposition change.)
- `test_patch_item_allowed_for_gm`: same PATCH with `_auth_header("gm")` asserts a success status (NOT 403). Read the handler to confirm the exact success code and any DB rows the FakeDB fixture must contain for the update to succeed; adjust the seeded FakeDB (e.g. ensure the item row exists — it does, as item-1) so the gm PATCH reaches a normal (non-403) response.

Monkeypatch `lost_found_router.supabase` with the FakeDB exactly as the existing tests do. Run `cd apps/api && python -m pytest tests/test_lost_found_delete.py -q`. The two new housekeeper cases MUST FAIL (endpoints still ungated → currently no 403), the gm cases should pass. Do NOT edit the router in this task.
  </action>
  <verify>`cd apps/api && python -m pytest tests/test_lost_found_delete.py -q` — `test_delete_item_forbidden_for_housekeeper` and `test_patch_item_forbidden_for_housekeeper` FAIL (no 403 yet); all pre-existing tests still pass.</verify>
  <done>Both new housekeeper-403 assertions fail against the still-ungated PATCH/DELETE (RED); existing tests untouched and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Gate PATCH and DELETE lost-found endpoints to the custody-state role set (GREEN)</name>
  <files>apps/api/routers/lost_found.py</files>
  <action>
In `apps/api/routers/lost_found.py`, add the SAME inline gate already used by `record_lost_found_custody_event` (line 170) as the first statement in the body of BOTH:

- `update_lost_found_item` (PATCH `/{item_id}`, starts line 213):
```python
    if current_user.role not in {"front_desk", "housekeeping_supervisor", "gm"}:
        raise HTTPException(status_code=403, detail="Not authorized to update this item")
```
- `delete_lost_found_item` (DELETE `/{item_id}`, starts line 250):
```python
    if current_user.role not in {"front_desk", "housekeeping_supervisor", "gm"}:
        raise HTTPException(status_code=403, detail="Not authorized to delete this item")
```

Per RESEARCH Decision 4, reuse the literal `{"front_desk", "housekeeping_supervisor", "gm"}` set inline (matching the file's existing custody-events idiom) — do NOT introduce a new named constant and do NOT import from core/roles.py (that set is unique to this file's custody-disposition concept and not one of the RBAC-04 named collisions). Do NOT change the tenant-scoping filters or any other handler logic — only prepend each gate. `HTTPException` and `CurrentUser` are already imported.

Re-run the test — all cases pass (GREEN), including the pre-existing gm/404 tests.
  </action>
  <verify>`cd apps/api && python -m pytest tests/test_lost_found_delete.py -q` — all tests PASS (new housekeeper → 403 for PATCH and DELETE; gm cases and 404 case still green). `grep -nc 'not in {"front_desk", "housekeeping_supervisor", "gm"}' apps/api/routers/lost_found.py` returns 3 (custody-events + PATCH + DELETE).</verify>
  <done>PATCH and DELETE /v1/lost-found/{id} return 403 for housekeeper and succeed for gm; custody-events gate unchanged; tenant scoping unchanged.</done>
</task>

</tasks>

<verification>
- `cd apps/api && python -m pytest tests/test_lost_found_delete.py -q` passes (all old + new tests).
- Both PATCH and DELETE now carry the `{"front_desk","housekeeping_supervisor","gm"}` inline gate; no new constant added.
- Existing custody-events gate and tenant scoping untouched.
</verification>

<success_criteria>
RBAC-03's two confirmed lost_found.py gaps are closed: the custody-state role gate can no longer be bypassed via PATCH, and DELETE is management/front-desk-gated, both verified live via signed-JWT TestClient requests.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-audit-and-normalization/19-03-SUMMARY.md`
</output>
