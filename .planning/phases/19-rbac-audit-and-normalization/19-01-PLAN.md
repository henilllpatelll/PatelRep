---
phase: 19-rbac-audit-and-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md
autonomous: true

must_haves:
  truths:
    - "A durable, committed audit artifact inventories every role check across all 30 routers in apps/api/routers/"
    - "Each router's checks are classified as route-level gate (Depends(require_role)) vs. object-level/business-rule check (inline if current_user.role...)"
    - "The RBAC-03 review outcome is documented per router: auth.py = no gap (self-scoped/DB-validated); lost_found.py = 2 gaps (PATCH + DELETE) identified and closed this phase"
    - "The RBAC-04 constant decisions are documented as explicit product decisions (MANAGER_ROLES vs PROGRAM_MANAGER_ROLES split; ALL_STAFF_ROLES dedup-only), each noting the chief_engineer/migration-064 context"
  artifacts:
    - path: ".planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md"
      provides: "Permanent RBAC inventory + classification + RBAC-03/04 review outcomes"
      contains: "guest_requests.py"
      min_lines: 60
  key_links:
    - from: "RBAC-AUDIT.md"
      to: "19-RESEARCH.md RBAC-01 inventory table"
      via: "transcribed inventory of all 30 routers with route-level/object-level classification"
      pattern: "auth.py|guest_requests.py|lost_found.py"
---

<objective>
Create the permanent RBAC-01 audit artifact: a committed, durable document inventorying every role check across all 30 routers in `apps/api/routers/`, each classified as a route-level gate vs. an object-level/business-rule check. The document also records the RBAC-03 per-router review outcomes and the RBAC-04 constant-consolidation decisions so the phase's reasoning survives in one findable place (not buried in RESEARCH.md).

Purpose: Success criterion 1 requires an audit artifact to exist; success criterion 3 requires the lost_found.py/auth.py review outcome to be "documented per router." This plan delivers both as documentation.
Output: `.planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-rbac-audit-and-normalization/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the RBAC-AUDIT.md inventory and review-outcome artifact</name>
  <files>.planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md</files>
  <action>
Create `.planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md` as a standalone, durable audit artifact. Do NOT invent new findings — transcribe and restructure the already-verified content from `19-RESEARCH.md` (dated 2026-08-04, HIGH confidence, direct code reads). Structure the document as follows:

1. **Header / Legend** — one short paragraph defining the two classifications the audit uses:
   - "Route-level gate" = `Depends(require_role(...))` on the endpoint signature (hard 403 before handler body).
   - "Object-level / business-rule check" = inline `if current_user.role ...` inside a handler body (self-scope narrowing, redaction, response-branching, or an inline 403).
   Note the two non-role-based routers (`internal.py` = X-Cron-Secret header; `webhooks.py` = provider signature verification) are out of RBAC scope by design.

2. **Full Role-Check Inventory (RBAC-01)** — reproduce the 30-router table from `19-RESEARCH.md`'s "RBAC-01: Full Role-Check Inventory" section verbatim in substance (Router | Route-level gates | Object-level checks | Notes). Keep the exact file:line references. Include the roll-up line (3 named inline-only routers with action items, 2 non-role-based, 2 hosting the named constant collisions, 22 clean/legitimately-mixed = 30 total).

3. **chief_engineer note** — a short subsection reproducing the Critical Discovery conclusion: migration `064_merge_chief_engineer.sql` retired `chief_engineer` as an assignable role value; any router still referencing `"chief_engineer"` in a role-group constant is a harmless no-op (dead-but-harmless), NOT a live gap requiring a fix this phase. A full purge across ~19 files is explicitly out of scope.

4. **RBAC-03 Review Outcomes (per router)** — document the review conclusion for each named inline-only router (satisfies success criterion 3's "documented per router"):
   - `auth.py` — REVIEWED, NO GAP. `GET /auth/me` is self-scoped by `current_user.user_id/hotel_id`; `POST /auth/hotel-context` validates via a live `user_roles` DB lookup (stronger than a static role gate). Inline role checks are absent by design.
   - `lost_found.py` — REVIEWED, 2 GAPS FOUND AND CLOSED THIS PHASE. `PATCH /{item_id}` (lines 213-247) and `DELETE /{item_id}` (lines 250-263) were ungated, allowing any authenticated user to change custody/disposition state or destroy the record — bypassing the role gate already enforced on `POST /{item_id}/custody-events`. Both gated to `{"front_desk","housekeeping_supervisor","gm"}` (see plan 19-03).
   - `guest_requests.py` — REVIEWED, 1 GAP FOUND AND CLOSED THIS PHASE. `DELETE /{request_id}` (lines 585-618) was ungated; gated to `SLA_POLICY_ROLES = {"gm","housekeeping_supervisor"}` (see plan 19-02).

5. **RBAC-04 Constant Decisions** — record the two consolidation decisions as explicit, confirmed product decisions (Decisions 1-3 from RESEARCH's "Decisions Made"):
   - `MANAGER_ROLES` (safety.py) vs `MANAGER_ROLES` (programs.py) are TWO DIFFERENT authority tiers that shared a name — NOT one drifted value. Resolved by two distinctly-named constants in `apps/api/core/roles.py`: `MANAGER_ROLES = ("gm","housekeeping_supervisor","chief_engineer")` (leadership/compliance tier; 4 independent call sites already agree on this value) and `PROGRAM_MANAGER_ROLES = ("gm","housekeeping_supervisor","engineer","chief_engineer")` (operational-program tier including line engineers, used only by programs.py). No access added or removed anywhere.
   - `ALL_STAFF_ROLES` (hotels.py) fix is DEDUP-ONLY: the bug was a duplicate `"engineer"`, not a missing `"chief_engineer"`. Resolved to `("gm","housekeeping_supervisor","engineer","front_desk","housekeeper")` — the 5 live roles. chief_engineer is intentionally NOT re-added (retired by migration 064).
   - Note `services/policy.py`'s `_ROLE_RANK` is a different paradigm (numeric rank, not set membership) and is intentionally NOT folded into core/roles.py. Minor same-value/different-name constants (SESSION_ROLES/SHIFT_ROLES, SUPERVISOR_ROLES/_OOO_ROLES) are out of scope (no drift risk — values already identical).

Write clean markdown. This is a documentation task only — do not touch any code.
  </action>
  <verify>`test -f .planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md && grep -c "guest_requests.py\|lost_found.py\|auth.py\|MANAGER_ROLES\|PROGRAM_MANAGER_ROLES\|ALL_STAFF_ROLES\|chief_engineer" .planning/phases/19-rbac-audit-and-normalization/RBAC-AUDIT.md` returns a non-zero count; the file contains the 30-router inventory table, the RBAC-03 per-router outcomes, and the RBAC-04 decisions.</verify>
  <done>RBAC-AUDIT.md exists, inventories all 30 routers with route-level vs object-level classification, documents auth.py (no gap) and lost_found.py (2 gaps closed) and guest_requests.py (1 gap closed) review outcomes per router, and records the MANAGER_ROLES/PROGRAM_MANAGER_ROLES split and ALL_STAFF_ROLES dedup as explicit decisions.</done>
</task>

</tasks>

<verification>
- File exists and is committed.
- Inventory covers all 30 routers (excludes only `__init__.py`).
- Route-level vs object-level classification present for each router that has checks.
- RBAC-03 review outcome documented for auth.py, lost_found.py, guest_requests.py.
- RBAC-04 decisions documented with chief_engineer/migration-064 rationale.
</verification>

<success_criteria>
The audit artifact from ROADMAP success criterion 1 exists as a durable, committed document, and the per-router review outcomes required by success criterion 3 are captured in it.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-audit-and-normalization/19-01-SUMMARY.md`
</output>
