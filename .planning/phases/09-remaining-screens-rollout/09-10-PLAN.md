---
phase: 09-remaining-screens-rollout
plan: 10
type: execute
wave: 4
depends_on: ["09"]
files_modified:
  - apps/mobile/app/(app)/guest-requests/index.tsx
  - apps/mobile/app/(app)/guest-requests/[requestId].tsx
autonomous: true
requirements: [SCREENS-05]
must_haves:
  truths:
    - "guest-requests list + [requestId] detail render via useTheme() + primitives (zero C. refs each)"
    - "List rows are Pressable-wraps-Card; detail mutations behave unchanged"
    - "Neither file has an Alert.alert to convert (both at 0)"
  artifacts:
    - path: "apps/mobile/app/(app)/guest-requests/index.tsx"
      provides: "Guest requests list on primitives"
      contains: "useTheme"
    - path: "apps/mobile/app/(app)/guest-requests/[requestId].tsx"
      provides: "Guest request detail on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/guest-requests/index.tsx"
      to: "apps/mobile/app/(app)/guest-requests/[requestId].tsx"
      via: "list row navigates to detail (route unchanged)"
      pattern: "requestId"
---

<objective>
Migrate the guest-requests list+detail pair together (D-02): `guest-requests/index.tsx` (277 lines, 29 `C.` refs, 0 `Alert.alert`) and `guest-requests/[requestId].tsx` (317 lines, 40 `C.` refs, 0 `Alert.alert`, 0 `t()`). Analogs: `work-orders/index.tsx` (list) + `work-orders/[woId].tsx` (detail).

Purpose: SCREENS-05 (guest requests) — list+detail render on primitives; data behavior unchanged.
Output: both files on primitives, zero `C.` refs each, `GuestRequestsList.test.tsx` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; asymmetric padding via style. StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}. StatusBadge label caller-translated.
## Both files at 0 Alert.alert — no Toast conversion. [requestId].tsx has 0 t() — do NOT add new t() (not i18n gate scope), presentation-only.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate guest-requests/index.tsx (list)</name>
  <files>apps/mobile/app/(app)/guest-requests/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/guest-requests/index.tsx (full — 277 lines, 29 C. refs, 7 TouchableOpacity, 0 Alert.alert; list rows navigate to [requestId])
    - apps/mobile/app/(app)/work-orders/index.tsx (list exemplar), apps/mobile/components/engineering/WorkOrderCard.tsx (Pressable-wraps-Card)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 29 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild each tappable list row (currently a single `TouchableOpacity`) as `<Pressable onPress={...}><Card style={[styles.cardLayoutOverrides]}>…</Card></Pressable>`. StatusKey-matching chips (request status) → `<StatusBadge label={t(...)} />`; non-matching chips color-migrated only.
    4. Convert loading/empty/error → `<StateBlock>`; `RefreshControl tintColor={theme.primaryAction}`. Do NOT change the fetch/filter logic or the navigation route to `[requestId]`.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/GuestRequestsList.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `@('<Card', 'Pressable') | ForEach-Object { if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/index.tsx' -SimpleMatch $_ -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `npx jest __tests__/screens/GuestRequestsList.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: fetch/filter/navigation route unchanged
  </acceptance_criteria>
  <done>Guest-requests list on primitives, zero C. refs, rows on Pressable-wraps-Card, GuestRequestsList test + type-check + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate guest-requests/[requestId].tsx (detail)</name>
  <files>apps/mobile/app/(app)/guest-requests/[requestId].tsx</files>
  <read_first>
    - apps/mobile/app/(app)/guest-requests/[requestId].tsx (full — 317 lines, 40 C. refs, 11 TouchableOpacity, 0 Alert.alert, 0 t(); fetch-one + mutation controls)
    - apps/mobile/app/(app)/work-orders/[woId].tsx (Phase-8 detail exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 40 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert mutation/action controls that are `TouchableOpacity` to `<Button variant=... size=... />`; StatusKey-matching chips → `<StatusBadge label={t(...)} />`; loading/empty/error → `<StateBlock>`.
    4. Do NOT add new `t()` (not i18n gate scope). Do NOT change the fetch-one or status-mutation `api.*` logic, guards, or tenant args — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/[requestId].tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/[requestId].tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/guest-requests/[requestId].tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch-one + status-mutation api calls + tenant args unchanged
  </acceptance_criteria>
  <done>Guest-request detail on primitives, zero C. refs, mutations untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (guest-request list fetch + status mutation, hotel_id-scoped) | Role-gated reads/mutations; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10-01 | Elevation of Privilege / Information Disclosure | A styling edit drops a status-mutation guard or tenant scoping in the detail screen | mitigate | Presentation-only rule; diff review asserts api/guard/tenant unchanged |
| T-09-10-02 | Tampering | List row Card rebuild drops `onPress`, breaking navigation to detail | mitigate | Pressable-wraps-Card mandated; GuestRequestsList test exercises the row |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/GuestRequestsList.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/guest-requests/index.tsx', 'apps/mobile/app/(app)/guest-requests/[requestId].tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-05 (guest requests): list+detail render on primitives; data/mutation behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-10-SUMMARY.md`.
</output>
