---
phase: 09-remaining-screens-rollout
plan: 12
type: execute
wave: 5
depends_on: ["10", "11"]
files_modified:
  - apps/mobile/app/(app)/logbook/index.tsx
  - apps/mobile/app/(app)/logbook/new.tsx
autonomous: true
requirements: [SCREENS-06]
must_haves:
  truths:
    - "logbook/index.tsx list + logbook/new.tsx form render via useTheme() + primitives (zero C. refs each)"
    - "New-entry create behavior + tenant scoping unchanged; neither file has an Alert.alert to convert"
  artifacts:
    - path: "apps/mobile/app/(app)/logbook/index.tsx"
      provides: "Logbook list on primitives"
      contains: "useTheme"
    - path: "apps/mobile/app/(app)/logbook/new.tsx"
      provides: "Logbook new-entry form on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/app/(app)/logbook/new.tsx"
      to: "apps/mobile/components/ui/Button.tsx"
      via: "submit control is a Button primitive"
      pattern: "Button"
---

<objective>
Migrate the logbook list+new pair together (D-02): `logbook/index.tsx` (206 lines, 27 `C.` refs, 3 `TouchableOpacity`, 0 `Alert.alert`) and `logbook/new.tsx` (156 lines, 24 `C.` refs, 3 `TouchableOpacity`, 0 `Alert.alert`). Analogs: `work-orders/index.tsx` (list) + `inspect/index.tsx` (form submit).

Purpose: SCREENS-06 (logbook) — list + new-entry form on primitives; data behavior unchanged.
Output: both files on primitives, zero `C.` refs each, type-check + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}.
## Both files at 0 Alert.alert — no Toast conversion. Presentation-only; do NOT add new t() beyond existing.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate logbook/index.tsx (list)</name>
  <files>apps/mobile/app/(app)/logbook/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/logbook/index.tsx (full — 206 lines, 27 C. refs, 3 TouchableOpacity, 0 Alert.alert)
    - apps/mobile/app/(app)/work-orders/index.tsx (list exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 27 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Rebuild tappable rows as Pressable-wraps-Card; StatusKey chips → `<StatusBadge>`; loading/empty/error → `<StateBlock>`; the "new entry" control → `<Button>`. `RefreshControl tintColor={theme.primaryAction}`.
    4. Do NOT change fetch logic or the navigation route to `new.tsx`.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/index.tsx' -SimpleMatch 'useTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: fetch/navigation unchanged
  </acceptance_criteria>
  <done>logbook list on primitives, zero C. refs, type-check + lint green, data behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate logbook/new.tsx (create form)</name>
  <files>apps/mobile/app/(app)/logbook/new.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/logbook/new.tsx (full — 156 lines, 24 C. refs, 3 TouchableOpacity, 0 Alert.alert; create-entry form + submit)
    - apps/mobile/app/(app)/inspect/index.tsx (form submit exemplar)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`.
    2. Replace all 24 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert submit/cancel `TouchableOpacity` controls to `<Button variant=... />`; any category/type selector chips that match a StatusKey → `<StatusBadge>`.
    4. Do NOT change the create-entry `api.*` submit logic, form state, validation, guards, or tenant args — presentation only.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/new.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/new.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/logbook/new.tsx' -SimpleMatch 'Button' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: create-entry submit logic + form state + tenant args unchanged
  </acceptance_criteria>
  <done>logbook new-entry form on primitives, zero C. refs, submit logic untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (logbook read + create-entry, hotel_id-scoped) | Role-gated read/mutation; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12-01 | Elevation of Privilege / Information Disclosure | A styling edit drops the create-entry guard or tenant scoping in new.tsx | mitigate | Presentation-only rule; diff review asserts submit/guard/tenant unchanged |
| T-09-12-02 | Tampering | List row Card rebuild drops `onPress`, breaking navigation/interaction | mitigate | Pressable-wraps-Card mandated; type-check + manual pass |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/logbook/index.tsx', 'apps/mobile/app/(app)/logbook/new.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-06 (logbook): list + new-entry form render on primitives; create behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-12-SUMMARY.md`.
</output>
