---
phase: 09-remaining-screens-rollout
plan: 09
type: execute
wave: 3
depends_on: ["05", "06", "07", "08"]
files_modified:
  - apps/mobile/app/(app)/assets/index.tsx
  - apps/mobile/app/(app)/pm-schedules/index.tsx
autonomous: true
requirements: [SCREENS-04]
must_haves:
  truths:
    - "assets/index.tsx renders via useTheme() + primitives (zero C. refs); its 3 outcome alerts become Toast"
    - "pm-schedules/index.tsx renders via useTheme() + primitives (zero C. refs); its 1 completeError alert becomes toast.error"
    - "Create-WO + complete-PM api behavior + tenant scoping are unchanged (only feedback calls change)"
  artifacts:
    - path: "apps/mobile/app/(app)/assets/index.tsx"
      provides: "Assets screen on primitives + Toast"
      contains: "useToast"
    - path: "apps/mobile/app/(app)/pm-schedules/index.tsx"
      provides: "PM schedules screen on primitives + Toast"
      contains: "useToast"
  key_links:
    - from: "apps/mobile/app/(app)/assets/index.tsx"
      to: "apps/mobile/lib/theme/useToast.ts"
      via: "create-WO outcome alerts → toast"
      pattern: "useToast"
---

<objective>
Migrate the two engineering-adjacent screens onto primitives: `assets/index.tsx` (296 lines, 22 `C.` refs, 3 `Alert.alert` all → Toast) and `pm-schedules/index.tsx` (336 lines, 48 `C.` refs, 1 `Alert.alert` → Toast). Analog: `work-orders/index.tsx` + `inspect/index.tsx` (Toast).

Purpose: SCREENS-04 (assets + PM schedules) — render on primitives; data behavior unchanged.
Output: both screens on primitives, zero `C.` refs each, all 4 alerts → Toast.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
##          `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`
## D-10 classification (verified, all → Toast): assets:126 error / :138 woCreated success (has title+body — combine into ONE toast string) / :144 error; pm-schedules:93 completeError → toast.error.
## Pitfall 3: create-WO / complete-PM handlers mix real api.* logic with the alert — change ONLY the feedback call; never touch preceding awaited calls, guards, or tenant args.
## Card has NO onPress → Pressable wraps Card; StateBlock loading/empty/error; RefreshControl tintColor={theme.primaryAction}.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate assets/index.tsx (3 alerts → Toast)</name>
  <files>apps/mobile/app/(app)/assets/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/assets/index.tsx (full — 296 lines, 22 C. refs, Alert.alert :126 error / :138 woCreated success / :144 error; the create-WO handler body)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero/StateBlock exemplar), apps/mobile/app/(app)/inspect/index.tsx (Toast exemplar)
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` and `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace all 22 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error blocks to `<StateBlock>`; tappable rows → Pressable-wraps-Card; StatusKey chips → `<StatusBadge>`; buttons → `<Button>`. `RefreshControl tintColor={theme.primaryAction}`.
    4. Alerts (all → Toast): :126 error → `toast.error(msg)`; :138 woCreated success (title+body) → combine into ONE `toast.success(msg)` string; :144 error → `toast.error(msg)`. Change ONLY the feedback calls — do NOT touch the create-WO `await api.*` call, guards, or tenant args.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/assets/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Zero Alert.alert: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/assets/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/assets/index.tsx' -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 }; if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/assets/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: create-WO api call + guards + tenant args unchanged
  </acceptance_criteria>
  <done>assets on primitives, zero C. refs, 3 alerts → Toast (woCreated combined into one), create-WO logic untouched, type-check + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate pm-schedules/index.tsx (1 alert → Toast)</name>
  <files>apps/mobile/app/(app)/pm-schedules/index.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/pm-schedules/index.tsx (full — 336 lines, 48 C. refs, Alert.alert :93 completeError; the complete-PM handler body)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero/StateBlock exemplar)
    - apps/mobile/lib/theme/useToast.ts
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` and `import { useToast } from "@/lib/theme/useToast"; const toast = useToast();`.
    2. Replace all 48 `C.*` refs with `theme.*` per map; route dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Keep StyleSheet; color objects LAST.
    3. Convert loading/empty/error → `<StateBlock>`; tappable rows → Pressable-wraps-Card; StatusKey chips (overdue/due) → `<StatusBadge>`; buttons → `<Button>`. `RefreshControl tintColor={theme.primaryAction}`.
    4. Alert: :93 completeError → `toast.error(msg)` using the existing translated string. Change ONLY the feedback call — do NOT touch the complete-PM `await api.*` call, guards, or tenant args.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/pm-schedules/index.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Zero Alert.alert: `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/pm-schedules/index.tsx' -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/pm-schedules/index.tsx' -SimpleMatch 'useToast' -ErrorAction Stop).Count -lt 1) { exit 1 }; if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/pm-schedules/index.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `npm run type-check` + `npm run lint` green
    - Diff review: complete-PM api call + guards + tenant args unchanged
  </acceptance_criteria>
  <done>pm-schedules on primitives, zero C. refs, completeError → toast.error, complete-PM logic untouched, type-check + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (create-WO from asset, complete-PM, hotel_id-scoped) | Engineer-role-gated mutations; NOT modified — only feedback calls change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-01 | Elevation of Privilege / Information Disclosure | Converting an alert drops the create-WO / complete-PM `await api.*`, its guard, or tenant scoping | mitigate | Pitfall-3 rule: change ONLY the feedback call; diff review asserts api/guard/tenant unchanged |
| T-09-09-02 | Tampering | Row Card rebuild drops `onPress`, making asset/PM rows non-interactive | mitigate | Pressable-wraps-Card mandated; type-check + manual pass |
</threat_model>

<verification>
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/assets/index.tsx', 'apps/mobile/app/(app)/pm-schedules/index.tsx') -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
- `if (@(Select-String -LiteralPath @('apps/mobile/app/(app)/assets/index.tsx', 'apps/mobile/app/(app)/pm-schedules/index.tsx') -SimpleMatch 'Alert.alert' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-04 (assets + PM schedules): both render on primitives; 4 alerts → Toast; create-WO/complete-PM behavior + tenant scoping unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-09-SUMMARY.md`.
</output>
