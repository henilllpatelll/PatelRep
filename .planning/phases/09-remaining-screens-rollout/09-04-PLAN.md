---
phase: 09-remaining-screens-rollout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/components/engineering/EngineerHome.tsx
autonomous: true
requirements: [SCREENS-02]
must_haves:
  truths:
    - "EngineerHome renders hero/cards/state via useTheme() + primitives (zero C. refs)"
    - "Engineer dashboard cards are Pressable-wraps-Card with StatusBadge chips where a StatusKey matches"
    - "Component prop signatures + role-conditional rendering are unchanged (presentation-only)"
  artifacts:
    - path: "apps/mobile/components/engineering/EngineerHome.tsx"
      provides: "Engineer dashboard on primitives"
      contains: "useTheme"
  key_links:
    - from: "apps/mobile/components/engineering/EngineerHome.tsx"
      to: "apps/mobile/components/ui/Card.tsx"
      via: "dashboard cards rebuilt on Pressable-wraps-Card"
      pattern: "<Card"
---

<objective>
Migrate `EngineerHome.tsx` (637 lines — the largest file in the phase, 32 `C.` refs, 13 `TouchableOpacity`, 0 `Alert.alert`) onto `useTheme()` + primitives. Dedicated plan (largest file) in the SCREENS-02 dashboard wave. Analog: `work-orders/index.tsx` chrome + `WorkOrderCard.tsx` cards.

Purpose: SCREENS-02 (engineer dashboard) — renders on primitives; behavior unchanged.
Output: `EngineerHome.tsx` on primitives, zero `C.` refs, `EngineerHome.test.tsx` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md
@.planning/phases/08-floor-role-rollout/08-PATTERNS.md

<migration_reference>
## C.* → theme.* map (from 08-PATTERNS.md — name changes matter):
C.paper→theme.background  C.ink→theme.textPrimary  C.ink2→theme.textSecondary  C.ink3→theme.textMuted
C.ink4→theme.textDisabled  C.surface→theme.surface  C.surface2→theme.surfaceSubtle  C.surface3→theme.surfaceMuted
C.line→theme.border  C.line2→theme.borderSubtle  C.accent→theme.primaryAction  C.accentSoft→theme.primarySoft
C.accentLine→theme.primaryLine  C.brass→theme.accentBrass  C.brassSoft→theme.accentBrassSoft  C.brassLine→theme.accentBrassLine
C.ready→theme.status.ready  C.caution→theme.status.pickup  C.alert→theme.status.dirty  C.info→theme.status.clean  C.ooo→theme.status.outOfOrder
shellTokens.* / C.shell*→theme.shell.*  (caution→pickup, alert→dirty, info→clean, ooo→outOfOrder)

## Imports: `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();`
## Inline color merge (work-orders/index.tsx:240-256): keep StyleSheet; color object LAST. Dark hero → theme.shell.*.
## Card has NO onPress → Pressable wraps Card; asymmetric padding via style (WorkOrderCard.tsx:201-208). StatusBadge 13-key enum, label caller-translated.
## Static-object escape hatch (WorkOrderCard.tsx:27-40): plain color-map consts → static lightTheme export.
## Anti-pattern: do NOT migrate imported evening.tsx composites — only EngineerHome's own C. refs.
</migration_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate EngineerHome.tsx onto primitives (637 lines, 32 C. refs)</name>
  <files>apps/mobile/components/engineering/EngineerHome.tsx</files>
  <read_first>
    - apps/mobile/components/engineering/EngineerHome.tsx (full — 637 lines, 32 C. refs, hero chrome, 13 TouchableOpacity cards/rows; note module-level color-map consts vs render-scope refs)
    - apps/mobile/components/engineering/WorkOrderCard.tsx (Phase-8 exemplar — Pressable-wraps-Card, cardLayoutOverrides, StatusBadge, static lightTheme escape hatch)
    - apps/mobile/app/(app)/work-orders/index.tsx (hero on theme.shell.*, StateBlock)
  </read_first>
  <action>
    1. Add `import { useTheme } from "@/lib/theme/useTheme"; const theme = useTheme();` inside the component body.
    2. Replace all 32 `C.*` refs with `theme.*` per the map. Route hero/dark chrome + `shellTokens`/`C.shell*` to `theme.shell.*`. Any module-level color-map const → static `lightTheme` export. Keep StyleSheet; color objects LAST.
    3. Rebuild each tappable dashboard card/row as `<Pressable onPress={...}><Card style={[styles.cardLayoutOverrides]}>…</Card></Pressable>` re-applying asymmetric padding via style.
    4. Convert StatusKey-matching chips to `<StatusBadge statusKey=... label={t(...)} />`; non-matching tiles stay hand-rolled, color-migrated only.
    5. Convert loading/empty/error to `<StateBlock>`; buttons to `<Button>`; `RefreshControl tintColor={theme.primaryAction}`.
    6. Do NOT change prop signatures, data props, or role-conditional rendering. Do NOT touch imported evening.tsx composites.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest __tests__/screens/EngineerHome.test.tsx && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Zero C. refs: `if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/EngineerHome.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/EngineerHome.tsx' -SimpleMatch 'shellTokens' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/EngineerHome.tsx' -Pattern 'useTheme|lightTheme' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `@('<Card', 'Pressable') | ForEach-Object { if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/EngineerHome.tsx' -SimpleMatch $_ -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - `npx jest __tests__/screens/EngineerHome.test.tsx` passes; `npm run type-check` + `npm run lint` green
    - Diff review: prop signatures + role-conditional rendering unchanged
  </acceptance_criteria>
  <done>EngineerHome on primitives, zero C. refs, cards on Pressable-wraps-Card, EngineerHome test + type-check + lint green, behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role → engineer dashboard render | Engineer-role-gated widget; NOT modified — presentation only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Elevation of Privilege | A styling edit drops a role guard or changes the prop contract, mis-rendering for a non-engineer role (Pitfall 8) | mitigate | Diff review + EngineerHome render test; prop signature asserted unchanged |
| T-09-04-02 | Tampering | Card rebuild drops a card's `onPress` leaving a dashboard tile non-interactive | mitigate | Pressable-wraps-Card mandated; grep asserts Pressable + Card present |
</threat_model>

<verification>
- `cd apps/mobile && npx jest __tests__/screens/EngineerHome.test.tsx` green
- `cd apps/mobile && npm run type-check && npm run lint` green
- `if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/EngineerHome.tsx' -Pattern '\bC\.[a-zA-Z]' -ErrorAction Stop).Count -ne 0) { exit 1 }`
</verification>

<success_criteria>
SCREENS-02 (engineer dashboard): EngineerHome renders on primitives with Pressable-wraps-Card + StatusBadge; prop/role behavior unchanged; zero legacy C tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-04-SUMMARY.md`.
</output>
