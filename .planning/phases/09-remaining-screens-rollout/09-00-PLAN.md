---
phase: 09-remaining-screens-rollout
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/components/engineering/CreateWorkOrderModal.tsx
  - apps/mobile/components/housekeeping/ReportIssueModal.tsx
  - apps/mobile/components/housekeeping/SupplyRequestModal.tsx
  - apps/mobile/app/(app)/tasks/index.tsx
  - apps/mobile/eslint.config.mjs
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "All 22 raw literals (16 JSX-text + 6 placeholder attrs) across the 4 backlog files are wired through t()"
    - "Every new EN key has a matching ES key (parity), so no string silently falls back to English"
    - "The 4 ignores entries are removed from eslint.config.mjs and npm run lint passes with the gate active on all 4 files"
    - "No behavior change: submit/api/form-state logic in the 3 modals + tasks screen is untouched"
  artifacts:
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "New EN keys for the 22 literals under workOrders/tasks/reportIssue/supplies namespaces"
      contains: "roomLabel"
    - path: "apps/mobile/i18n/locales/es.json"
      provides: "Matching ES keys (parity)"
      contains: "roomLabel"
    - path: "apps/mobile/eslint.config.mjs"
      provides: "Gate widened — 4 ignores entries removed"
      contains: "markupOnly"
  key_links:
    - from: "apps/mobile/app/(app)/tasks/index.tsx"
      to: "apps/mobile/i18n/locales/en.json"
      via: "t() call resolves new key"
      pattern: "t\\(\"tasks\\."
    - from: "apps/mobile/eslint.config.mjs"
      to: "the 4 backlog files"
      via: "ignores removed → gate enforced"
      pattern: "no-literal-string"
---

<objective>
Close the i18n gate-widening backlog deferred from Phase 7→8→9 (non-negotiable Phase 9 scope). Remove the 4 `ignores` entries from `apps/mobile/eslint.config.mjs:47-50`, wire real `t()` calls for the 22 raw literals (16 JSX-text nodes the `markupOnly:true` gate enforces + 6 `placeholder=` attrs it does not), and add matching EN+ES keys so `npm run lint` enforces `i18next/no-literal-string` on all 4 formerly-exempt Phase-8 floor files.

Purpose: I18N-01 completion — the mobile i18n gate reaches full app coverage; safety-adjacent floor copy is translatable.
Output: 4 files wired to `t()`, EN/ES keys at parity, 4 ignores removed, `npm run lint` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remaining-screens-rollout/09-CONTEXT.md
@.planning/phases/09-remaining-screens-rollout/09-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-PATTERNS.md

<i18n_reference>
## D-12 rule: wire all 22 literals, keep the gate narrow — do NOT flip `markupOnly:true` (leave `eslint.config.mjs:63` untouched). The gate won't force the 6 placeholders; wire them anyway.

## Interpolation idiom (already used app-wide) — text with a variable:
Before: `<Text ...>Room {aiPreview.room_number}</Text>`
After:  `<Text ...>{t("tasks.roomLabel", { room: aiPreview.room_number })}</Text>`
EN: `"roomLabel": "Room {{room}}"`   ES: `"roomLabel": "Habitación {{room}}"`

## Placeholder idiom (Phase-8 migrated exemplar work-orders/index.tsx:275-276):
`placeholder={t("workOrders.searchPlaceholder", { defaultValue: "Search work orders…" })}`
`placeholderTextColor={theme.textDisabled}`   // placeholderTextColor already present — leave as-is

## Locale structure (i18n/locales/en.json): keys nest under per-domain namespaces —
`workOrders` (~:462), `tasks` (~:623), `reportIssue` (~:697), `supplies` (~:392).
Namespace new keys under these existing objects. Dedupe repeated strings ("Category"/"Priority"/"Room {n}") into shared keys.
</i18n_reference>

<literals>
## The 16 gate-enforced JSX-text violations (exact lines from eslint probe):
- tasks/index.tsx:253 — `Room {aiPreview.room_number}`
- CreateWorkOrderModal.tsx:121,155,166,191,221 — "New Work Order", "Location", "Category", "Priority", "Reported By"
- ReportIssueModal.tsx:95,96,101,113,159,191(×2) — "Submit Work Order", "Room {roomNumber}", "Issue title", "Category", "Priority", "Details", "(optional)" (+ two `*` required-asterisks)
- SupplyRequestModal.tsx:82,84(×2) — "Request Supplies", "Room {roomNumber} — tap what you need"

## The 6 placeholder attrs (part of the 22, gate does NOT flag — wire anyway):
- CreateWorkOrderModal.tsx:133,146,160 — "What needs to be fixed?", "Details (optional)", "Room number or area (e.g. 204, Lobby HVAC)"
- ReportIssueModal.tsx:105,195 — "e.g. Toilet not flushing, A/C not cooling", "Describe what you found…"
- SupplyRequestModal.tsx:128 — "Other (e.g. extra pillow, folding cot...)"

## The 4 ignores to delete (eslint.config.mjs:47-50):
'components/engineering/CreateWorkOrderModal.tsx',
'components/housekeeping/ReportIssueModal.tsx',
'components/housekeeping/SupplyRequestModal.tsx',
'app/(app)/tasks/index.tsx',
</literals>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all new EN + ES keys (parity-first)</name>
  <files>apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <read_first>
    - apps/mobile/i18n/locales/en.json (namespaces: workOrders ~:462, tasks ~:623, reportIssue ~:697, supplies ~:392 — read each object to reuse existing keys before adding)
    - apps/mobile/i18n/locales/es.json (same namespaces — every new EN key needs its ES twin)
  </read_first>
  <action>
    Add these keys under the matching existing namespace in BOTH en.json and es.json. Reuse an existing key if the string already exists (grep first); otherwise add. Dedupe repeats ("Category"/"Priority") into shared keys.

    tasks: `"roomLabel": "Room {{room}}"` (ES: `"Habitación {{room}}"`)
    reportIssue: `"submit": "Submit Work Order"`, `"roomLabel": "Room {{room}}"`, `"issueTitle": "Issue title"`, `"category": "Category"`, `"priority": "Priority"`, `"details": "Details"`, `"optional": "(optional)"`, `"titlePlaceholder": "e.g. Toilet not flushing, A/C not cooling"`, `"detailsPlaceholder": "Describe what you found…"`
    workOrders: `"newWorkOrder": "New Work Order"`, `"location": "Location"`, `"reportedBy": "Reported By"`, `"whatNeedsFixing": "What needs to be fixed?"`, `"detailsOptionalPlaceholder": "Details (optional)"`, `"locationPlaceholder": "Room number or area (e.g. 204, Lobby HVAC)"` (reuse existing workOrders.category/priority if present)
    supplies: `"request": "Request Supplies"`, `"roomTapHint": "Room {{room}} — tap what you need"`, `"otherPlaceholder": "Other (e.g. extra pillow, folding cot...)"`

    Provide human-quality Spanish for every ES value (safety-adjacent work-order/issue copy — do not machine-translate carelessly). The `*` required-asterisks at ReportIssueModal.tsx:191 are decoration, NOT translatable text — leave them as literal `*` (or wrap in a non-gated element); do not create a key for `*`.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const en=require('./i18n/locales/en.json'),es=require('./i18n/locales/es.json');const keys=['tasks.roomLabel','reportIssue.submit','reportIssue.roomLabel','supplies.request','supplies.roomTapHint','workOrders.newWorkOrder','workOrders.location'];const get=(o,p)=>p.split('.').reduce((a,k)=>a&&a[k],o);const miss=keys.filter(k=>!get(en,k)||!get(es,k));if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every new EN key has an ES twin: the node script above prints "parity ok" and exits 0
    - `@('apps/mobile/i18n/locales/en.json', 'apps/mobile/i18n/locales/es.json') | ForEach-Object { if (@(Select-String -LiteralPath $_ -SimpleMatch 'roomLabel' -ErrorAction Stop).Count -lt 1) { exit 1 } }` exits 0
    - No `*` asterisk key added: `if (@(Select-String -LiteralPath 'apps/mobile/i18n/locales/en.json' -SimpleMatch '"*"' -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - Both files remain valid JSON (node require succeeds)
  </acceptance_criteria>
  <done>All ~19 message keys + placeholder keys exist under the correct namespace in BOTH en.json and es.json, at full parity, valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Wire t() for the 22 literals across the 4 files</name>
  <files>apps/mobile/app/(app)/tasks/index.tsx, apps/mobile/components/engineering/CreateWorkOrderModal.tsx, apps/mobile/components/housekeeping/ReportIssueModal.tsx, apps/mobile/components/housekeeping/SupplyRequestModal.tsx</files>
  <read_first>
    - apps/mobile/app/(app)/tasks/index.tsx (:248-264 — :248 already `t("tasks.aiPreviewLabel")`, :253 raw `Room {aiPreview.room_number}`)
    - apps/mobile/components/engineering/CreateWorkOrderModal.tsx (:121,133,146,155,160,166,191,221 — text + 3 placeholders; confirm `t`/`useTranslation` is already imported)
    - apps/mobile/components/housekeeping/ReportIssueModal.tsx (:95,96,101,105,113,159,191,195 — text + 2 placeholders)
    - apps/mobile/components/housekeeping/SupplyRequestModal.tsx (:82,84,128 — text + 1 placeholder)
    - apps/mobile/i18n/locales/en.json (confirm Task 1 keys exist before referencing them)
  </read_first>
  <action>
    For each file, replace the raw literal with a `t()` call using the Task-1 key. If a file does not already have `const { t } = useTranslation();` in scope, add `import { useTranslation } from "react-i18next";` and the hook (mirror how a sibling already-wired string in the same file resolves `t`).

    tasks/index.tsx:253 → `{t("tasks.roomLabel", { room: aiPreview.room_number })}`
    CreateWorkOrderModal.tsx: :121→`{t("workOrders.newWorkOrder")}` :155→`{t("workOrders.location")}` :166→`{t("workOrders.category")}` :191→`{t("workOrders.priority")}` :221→`{t("workOrders.reportedBy")}`; placeholders :133→`placeholder={t("workOrders.whatNeedsFixing")}` :146→`placeholder={t("workOrders.detailsOptionalPlaceholder")}` :160→`placeholder={t("workOrders.locationPlaceholder")}`
    ReportIssueModal.tsx: :95→`{t("reportIssue.submit")}` :96→`{t("reportIssue.roomLabel", { room: roomNumber })}` :101→`{t("reportIssue.issueTitle")}` :113→`{t("reportIssue.category")}` :159→`{t("reportIssue.priority")}` :191→`{t("reportIssue.details")}` + `{t("reportIssue.optional")}`; placeholders :105→`placeholder={t("reportIssue.titlePlaceholder")}` :195→`placeholder={t("reportIssue.detailsPlaceholder")}`. Leave the two `*` required-asterisks as literal `*`.
    SupplyRequestModal.tsx: :82→`{t("supplies.request")}` :84→`{t("supplies.roomTapHint", { room: roomNumber })}`; placeholder :128→`placeholder={t("supplies.otherPlaceholder")}`

    Change ONLY the literal→`t()` swap and any needed `useTranslation` import/hook. Do NOT alter submit handlers, `api.*`/`enqueueAction` calls, form state, validation, or styling. These are Phase-8-migrated files already on `theme.*` — do not re-touch their colors.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` reports zero `i18next/no-literal-string` violations in all 4 files (run AFTER Task 3 removes the ignores; if run before, lint still skips them — see Task 3 gate)
    - `if (@(Select-String -LiteralPath 'apps/mobile/app/(app)/tasks/index.tsx' -SimpleMatch 't("tasks.roomLabel"' -ErrorAction Stop).Count -lt 1) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/housekeeping/ReportIssueModal.tsx' -SimpleMatch 't("reportIssue' -ErrorAction Stop).Count -lt 6) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/components/engineering/CreateWorkOrderModal.tsx' -SimpleMatch 't("workOrders' -ErrorAction Stop).Count -lt 5) { exit 1 }` exits 0
    - `npm run type-check` passes
    - Diff review: no submit/api/form-state/style line changed
  </acceptance_criteria>
  <done>All 22 literals resolve through t() with Task-1 keys; type-check green; no logic/style lines touched.</done>
</task>

<task type="auto">
  <name>Task 3: Remove 4 ignores entries + confirm gate green</name>
  <files>apps/mobile/eslint.config.mjs</files>
  <read_first>
    - apps/mobile/eslint.config.mjs (:38-51 ignores block; :63 markupOnly:true — leave markupOnly as-is per D-12)
  </read_first>
  <action>
    Delete exactly these 4 lines from the `ignores` array (`:47-50`):
    ```
    'components/engineering/CreateWorkOrderModal.tsx',
    'components/housekeeping/ReportIssueModal.tsx',
    'components/housekeeping/SupplyRequestModal.tsx',
    'app/(app)/tasks/index.tsx',
    ```
    Do NOT change `markupOnly: true` (:63) or any other rule config — D-12 keeps the gate narrow (no app-wide placeholder/aria enforcement this phase). Run `npm run lint` and confirm it is green with the gate now active on all 4 files (Task 2 must be complete first).
  </action>
  <verify>
    <automated>cd apps/mobile && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The 4 ignore strings are gone: `if (@(Select-String -LiteralPath 'apps/mobile/eslint.config.mjs' -SimpleMatch @("CreateWorkOrderModal.tsx'", "ReportIssueModal.tsx'", "SupplyRequestModal.tsx'", "app/(app)/tasks/index.tsx'") -ErrorAction Stop).Count -ne 0) { exit 1 }` exits 0
    - `if (@(Select-String -LiteralPath 'apps/mobile/eslint.config.mjs' -SimpleMatch 'markupOnly: true' -ErrorAction Stop).Count -ne 1) { exit 1 }` exits 0 (unchanged)
    - `npm run lint` exits 0 (gate enforced on all 4 files, no violations)
  </acceptance_criteria>
  <done>4 ignores removed, markupOnly untouched, npm run lint green with the gate enforcing all 4 formerly-exempt files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device → API (create-WO / report-issue / supply-request submits) | These handlers live in the 4 touched files but are NOT modified — only their display strings change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Tampering | An i18n edit accidentally changes a submit handler or `enqueueAction`/`api.*` call in one of the 3 modals | mitigate | Literal→t() swap only; acceptance criteria + diff review confirm no logic line changed |
| T-09-00-02 | Information Disclosure (safety copy) | Unreviewed Spanish for safety-adjacent work-order/issue copy ships wrong instructions | mitigate | Task 1 requires human-quality ES per key; EN/ES parity script gate |
| T-09-00-03 | Repudiation | A new EN key lacks its ES twin → silent English fallback (no lint error) | mitigate | Parity node-script in Task 1 verify fails the task if any twin is missing |
</threat_model>

<verification>
- `cd apps/mobile && npm run lint` green (gate active on all 4 files)
- `cd apps/mobile && npm run type-check` green
- EN/ES parity node-script exits 0
- PowerShell assertions confirm 4 ignores removed and markupOnly unchanged
</verification>

<success_criteria>
I18N-01 completion: the mobile `i18next/no-literal-string` gate now enforces all 4 formerly-exempt Phase-8 floor files; all 22 literals are translatable via `t()` with EN/ES keys at parity; `npm run lint` green; no behavior change.
</success_criteria>

<output>
After completion, create `.planning/phases/09-remaining-screens-rollout/09-00-SUMMARY.md`.
</output>
