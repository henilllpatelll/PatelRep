---
phase: 33-core-operational-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

# NOTE on file ownership: en.ts / es.ts are edited ONLY here. This plan is the SOLE owner of
# the locale files for Phase 33 so the five parallel wave-2 content plans (33-02..33-06) never
# collide on them. Those plans MUST consume the keys defined here and MUST NOT add new locale
# keys. If a wave-2 plan discovers a genuinely missing string inside a state it is touching, it
# reuses an existing generic `common.*` key (loading/error/retry/noResults) rather than editing
# en/es (which would conflict with its sibling wave-2 plans). This is why the key set below is
# intentionally comprehensive per section. Precedent: Phase 32's 32-01 did exactly this.

must_haves:
  truths:
    - "Every empty-state, loading-label, error-message, and touched-chrome string that the nine wave-2 sections need already exists in BOTH en.ts and es.ts, with real Spanish translations (not placeholders)"
    - "Four new sibling namespaces — `sop`, `logbook`, `lostFound`, `scheduling` — exist in both locale files (they had NO namespace before this phase)"
    - "The five existing namespaces — `guestRequests`, `safety`, `programs`, `tasks`, `evidence` — are EXTENDED additively (existing keys left byte-unchanged); `guestRequests` gains its kanban-page keys, `safety` gains `tabs.*` + sub-panel error keys, `programs`/`tasks`/`evidence` gain any missing error-state key"
    - "`npm run check:i18n-parity` and `npm run verify:i18n-gate` pass — every new en key has an es counterpart and vice versa"
  artifacts:
    - path: "apps/web/i18n/locales/en.ts"
      provides: "New sop/logbook/lostFound/scheduling namespaces + extended guestRequests/safety/programs/tasks/evidence blocks — English copy for every state and touched-chrome string the 9 sections render"
      contains: "lostFound"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Exact-parity Spanish counterparts for every new/added key in en.ts"
      contains: "lostFound"
  key_links:
    - from: "new keys in en.ts"
      to: "new keys in es.ts"
      via: "1:1 key parity enforced by scripts/check-i18n-parity.mjs"
      pattern: "lostFound"
---

<objective>
Establish the complete i18n key set that all nine wave-2 sections (Tasks, SOP, Logbook, Guest Requests, Lost & Found, Safety, Evidence, Programs, Scheduling) will consume — in BOTH locales, in one place — so the five wave-2 content plans can run in parallel without ever touching (and colliding on) en.ts / es.ts.

This plan writes NO component code. It only adds locale keys: per-section empty-state copy, error-state copy, loading labels where a section needs a non-default one, and the specific "touched-chrome" strings CONTEXT flagged as in-scope (Guest Requests' kanban page title/tabs/column/card strings, Safety's `MANAGER_TABS` labels, Scheduling's hardcoded object-literal-prop strings). Four sections (SOP, Logbook, Lost & Found, Scheduling) currently have NO namespace at all and must get one; the other five already have a namespace and get additively extended.

Purpose: satisfy the EN/ES key-parity half of Success Criterion #3 up front and unblock 33-02..33-06 for parallel execution with zero locale-file contention.
Output: four new namespaces + five extended blocks in en.ts and es.ts; `check:i18n-parity` + `verify:i18n-gate` green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md

# The two locale files — plain nested TS objects, ~1482 lines each, identical length today.
# Present namespaces: common, programs, evidence, safety, tasks, guestRequests.
# ABSENT (must be created): sop, logbook, lostFound, scheduling.
@apps/web/i18n/locales/en.ts
@apps/web/i18n/locales/es.ts

# Read these section files to copy the ACTUAL current hardcoded English strings (the source of
# truth for copy) into keys — the research located raw strings per section but the live file text
# is authoritative. Grep each for title=/message=/subtitle= object-literal props and raw >text<.
@apps/web/app/(dashboard)/sop/page.tsx
@apps/web/app/(dashboard)/logbook/page.tsx
@apps/web/app/(dashboard)/lost-found/page.tsx
@apps/web/app/(dashboard)/scheduling/page.tsx
@apps/web/components/guest-requests/GuestRequestsPage.tsx
@apps/web/app/(dashboard)/safety/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all new + extended English keys to en.ts</name>
  <files>apps/web/i18n/locales/en.ts</files>
  <action>
Add FOUR new sibling namespaces and EXTEND five existing blocks. Leave every pre-existing key byte-unchanged (additive only). Match the file's existing style (quote style, trailing commas, nesting). For copy, prefer the ACTUAL current hardcoded English string found in each section file (read them via context) so the flag-off legacy branch and flag-on v2 branch read identically — the key names below are the contract the wave-2 plans rely on, but the string VALUES should mirror what's on screen today.

Use `common.*` (already present: `loading`, `error`, `retry`, `noResults`) for generic state text; only add section-specific copy below.

=== NEW NAMESPACE `sop` ===
- `pageTitle: "SOP Library"`, `pageSubtitle: "Standard operating procedures for your property"` (match current header text)
- `empty: { title: "No procedures yet", body: "Upload your first SOP to get started" }`
- `loadError: "Couldn't load procedures"`
- `searchPlaceholder`, `uploadCta` — only if these live inside touched chrome; otherwise reuse existing. (Match on-screen text.)

=== NEW NAMESPACE `logbook` ===
- `pageTitle`, `pageSubtitle` (match current header)
- `empty: { title: "No log entries yet", body: "Shift notes and handoffs will appear here" }`
- `loadError: "Couldn't load the logbook"`
- `validation: { ... }` — ONLY the form-validation error strings that render inside a state this phase touches; if the create-form validation is NOT part of the loading/empty/error redesign, DEFER it (do not add) and note it for the close-out deferred list.

=== NEW NAMESPACE `lostFound` ===
- `pageTitle`, `pageSubtitle` (match current header)
- `empty: { title: "No items logged", body: "Found items you log will appear here" }`
- `loadError: "Couldn't load lost & found items"`
- (Do NOT add keys for `LogFoundItemModal` — that file is frozen and untouched.)

=== NEW NAMESPACE `scheduling` ===
Capture the hidden object-literal-prop strings research flagged (scheduling/page.tsx ~lines 159-160, 705, 1032, 1141, 1297-1298 — verify live):
- `pageTitle: "Staff Scheduling"`, `pageSubtitle` (match current)
- `roster: { loadError: "Failed to load roster.", empty: "No shifts defined yet…" }` (match exact current wording)
- `assignments: { loadError, empty }`, `staff: { loadError, empty }` — one error+empty pair per query (rosterQuery, assignmentsQuery, staffQuery) that has a StateBlock/EmptyState today with a hardcoded string
- Any other hardcoded `title=`/`message=`/`subtitle=` string literal inside a StateBlock/EmptyState/PageHeader in this file → its own key here.

=== EXTEND EXISTING `guestRequests` (currently covers only NewRequestModal form) ===
Add a `page` (or flat) sub-set for the kanban page — match GuestRequestsPage.tsx on-screen text:
- `pageTitle: "Guest Requests"`, `pageSubtitle`
- `tabActive: "Active Requests"`, `tabHistory: "History"`
- `columns: { open: "Open", acknowledged: "Acknowledged", verify: "Verify Resolution" }` (the COLUMNS array labels)
- `empty: { title: "No requests", body?: ... }`, `loadError`
- Card action labels currently raw: `acknowledge`, `dispatch`, `arrived`, `contacted`, `resolve`, `verify`; plus `urgent: "URGENT"`, `slaOverdue: "SLA overdue"`
- Time-ago suffixes: `timeAgo: { minutes: "{n}m ago", hours: "{n}h ago", days: "{n}d ago" }` (or the interpolation shape the codebase already uses elsewhere — check an existing time-ago key before inventing a format)

=== EXTEND EXISTING `safety` ===
- `tabs: { <id>: "<label>" }` for each entry in `MANAGER_TABS` (safety/page.tsx ~lines 18-23) — key by the tab `id`, value = its current hardcoded label
- Sub-panel error copy replacing hardcoded `"Unable to load…"`: `loadError` (shared) or per-panel `compliance.loadError`, `incidents.loadError`, `programs.loadError`, `information.loadError` — pick the granularity matching how 33-05 will wire them; err toward one shared `safety.loadError` unless panels need distinct copy
- Sub-panel `empty.*` titles for any empty state 33-05 will render

=== EXTEND EXISTING `programs` ===
- `programs.loadError` already exists — reuse it (do NOT duplicate). Add only: sub-panel empty titles (`deepClean.empty`, `housekeepingDepth.empty`, `inspectionDepth.empty`) if those panels render an empty state 33-05 will touch, and a sub-panel error key only if a panel needs copy distinct from the existing page-level `loadError`.

=== EXTEND EXISTING `tasks` (already clean, 81 t()) ===
- Add ONLY an error-state key if none exists: `tasks.loadError: "Couldn't load tasks"`. `tasks.empty.*` already exists — reuse it. Grep first; if a suitable error key already exists, add nothing.

=== EXTEND EXISTING `evidence` (already clean, reference impl) ===
- Grep for an existing error key; add `evidence.loadError` only if the reference impl doesn't already have one it passes to `error.message`. Likely nothing to add.

Do NOT invent keys beyond what the nine sections render inside touched chrome/states. If a section's exact current string differs from the suggested English above, use the section's ACTUAL string as the value and keep the key name.
  </action>
  <verify>cd apps/web && npm run type-check succeeds (en.ts is the source-of-truth locale shape); grep confirms new top-level blocks `sop`, `logbook`, `lostFound`, `scheduling` all present in en.ts; grep confirms `guestRequests` now contains a kanban key (e.g. `tabHistory`), `safety` contains `tabs`, and a `loadError`-style key exists for each of the 9 sections (or an existing one is reused); pre-existing keys (e.g. `guestRequests.newRequest`, `evidence`'s existing keys) still present (additive check)</verify>
  <done>en.ts gains sop/logbook/lostFound/scheduling namespaces and extended guestRequests/safety/programs/tasks/evidence blocks; every pre-existing key unchanged; type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Mirror every new/added key into es.ts with real Spanish + prove parity</name>
  <files>apps/web/i18n/locales/es.ts</files>
  <action>
Add the EXACT same key structure to es.ts with real, natural Spanish translations in the hotel-operations register, consistent with the existing es.ts tone (mirror the style of the existing `common`/`tasks`/`evidence`/`guestRequests` Spanish copy). Every key added to en.ts in Task 1 must have a 1:1 Spanish counterpart here, and vice versa — no en-only or es-only keys.

Guidance for the flagged strings:
- Section page titles: translate naturally (e.g. `sop.pageTitle: "Biblioteca de POE"` or match how `nav.sopLibrary` is already translated for consistency — check the existing `nav.*` Spanish labels and align section titles to them).
- Generic empty/error: reuse the register already established in `common` (`error`, `retry`, `loading`, `noResults`) — section-specific empties should read naturally in Spanish, not literal machine translation.
- Guest Requests time-ago suffixes: use the same interpolation shape as en; Spanish e.g. `hace {n}m` / `hace {n}h` / `hace {n}d` (match whatever an existing time-ago key in es.ts already does, if one exists).
- Safety tab labels: mirror the English tab ids → natural Spanish labels.

Then run the parity + type gates and confirm green.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity && npm run verify:i18n-gate && npm run type-check all green (parity proves every new en key has an es counterpart and vice versa); grep confirms `sop`, `logbook`, `lostFound`, `scheduling` blocks present in es.ts too, and es `guestRequests`/`safety` gained the same new sub-keys</verify>
  <done>es.ts mirrors every new/added key from Task 1 with real Spanish; check:i18n-parity + verify:i18n-gate + type-check all green</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity`, `npm run verify:i18n-gate`, `npm run type-check` all green from `apps/web`.
- Four new namespaces (`sop`, `logbook`, `lostFound`, `scheduling`) exist in BOTH locales with matching structure.
- Five existing namespaces extended additively; every pre-existing key unchanged.
- Every one of the 9 sections has (new or reused) empty + error copy available in both locales.
</verification>

<success_criteria>
The complete Phase-33 section copy set exists in both locales with real Spanish and passes parity — satisfying the EN/ES half of Success Criterion #3 up front and unblocking 33-02..33-06 to run in parallel without ever touching the locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-01-SUMMARY.md` recording: the exact final key list per section (so wave-2 executors reference the real names), any key naming that deviated from this plan, which existing keys were reused vs newly added (especially `programs.loadError`, `tasks.empty.*`, any existing `evidence` error key), any string DEFERRED (e.g. Logbook/SafetyPrograms form-validation strings not inside a touched state) for the close-out deferred list, confirmation of parity-gate pass, and an explicit statement that en.ts/es.ts are now frozen for the rest of Phase 33 (wave-2 plans must not edit them, only consume).
</output>
</content>
</invoke>
