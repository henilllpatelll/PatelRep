---
phase: 33-core-operational-sections
plan: 05
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/app/(dashboard)/safety/page.tsx
  - apps/web/components/safety/ComplianceDashboard.tsx
  - apps/web/components/safety/IncidentReview.tsx
  - apps/web/components/safety/SafetyInformation.tsx
  - apps/web/components/safety/SafetyPrograms.tsx
  - apps/web/app/(dashboard)/programs/page.tsx
  - apps/web/components/programs/HousekeepingDepthPanels.tsx
  - apps/web/components/programs/DeepCleanAreasPanel.tsx
  - apps/web/components/programs/InspectionDepthPanel.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of the safety/ and programs/ trees. No
# wave-2 sibling touches these (verified: zero cross-section sharing; the Programs panels DO
# cross-import each other — HousekeepingDepthPanels <-> DeepCleanAreasPanel <-> InspectionDepthPanel
# — so all three MUST live in one plan, which they do). It does NOT edit en.ts/es.ts — the
# extended `safety` and `programs` blocks exist from 33-01; it only consumes them. Both sections
# are a thin parent page.tsx (83 / 85 LOC) mounting tightly-coupled sub-components on one route:
# use Pattern 2 (read the flag ONCE in the parent, thread a `redesigned` boolean prop down) so
# there is ONE flag decision per section (Safety's 4 panels share it; Programs' 3 panels share the
# one OVERVIEW_KEY query, so one decision = one query). Both are acceptable per CONTEXT discretion.

must_haves:
  truths:
    - "With the 'safety' flag ON, the Safety page + its 4 sub-panels (ComplianceDashboard, IncidentReview, SafetyInformation, SafetyPrograms) render v2, with each panel's loading/empty/error served by the shared StateBlock (ComplianceDashboard's existing partial wiring extended to the others), the MANAGER_TABS labels i18n'd via safety.tabs.*, and hardcoded 'Unable to load…' strings replaced with safety.* error copy; flag OFF is byte-behaviorally identical to today"
    - "With the 'programs' flag ON, the Programs page + its 3 panels render v2, with the existing page-level error (programs/page.tsx:51, overview.isError) migrated to StateBlock and each sub-panel's empty (and error where structured per-panel) wired; flag OFF is byte-behaviorally identical to today"
    - "The flag is read ONCE per section in the parent page.tsx and threaded as a `redesigned` prop to the sub-components (Pattern 2) — no duplicate useHotelStore reads scattered across panels"
    - "No query/mutation/filter/field added or removed; Safety panels' onRetry points at each panel's existing load() callable, Programs' at the existing overview.refetch() already used by its refresh button"
  artifacts:
    - path: "apps/web/app/(dashboard)/safety/page.tsx"
      provides: "Reads isSectionRedesigned('safety', hotel) once; threads `redesigned` to the 4 panels; MANAGER_TABS labels resolved via t('safety.tabs.<id>') inside the component; v2 PageHeader chrome; legacy branch unchanged"
      contains: "isSectionRedesigned('safety'"
    - path: "apps/web/app/(dashboard)/programs/page.tsx"
      provides: "Reads isSectionRedesigned('programs', hotel) once; threads `redesigned` to the 3 panels; page-level overview.isError migrated to StateBlock error (onRetry: overview.refetch); v2 chrome; legacy branch unchanged"
      contains: "isSectionRedesigned('programs'"
    - path: "apps/web/components/safety/ComplianceDashboard.tsx"
      provides: "Accepts `redesigned` prop; v2 StateBlock loading/empty/error (extends its existing partial StateBlock wiring); i18n'd copy"
      contains: "redesigned"
    - path: "apps/web/components/programs/HousekeepingDepthPanels.tsx"
      provides: "Accepts `redesigned` prop; v2 empty/error state via StateBlock; i18n'd copy"
      contains: "redesigned"
  key_links:
    - from: "apps/web/app/(dashboard)/safety/page.tsx"
      to: "the 4 safety sub-panels"
      via: "read flag once, pass redesigned={v2} prop to each panel; MANAGER_TABS.map(x => ({ label: t(`safety.tabs.${x.id}`) }))"
      pattern: "isSectionRedesigned\\('safety'"
    - from: "apps/web/app/(dashboard)/programs/page.tsx"
      to: "the 3 programs panels + overview query"
      via: "read flag once, pass redesigned prop; StateBlock error onRetry = overview.refetch()"
      pattern: "isSectionRedesigned\\('programs'"
    - from: "safety sub-panels"
      to: "each panel's existing load() callable"
      via: "StateBlock error.onRetry = () => void load() (callable already defined per panel)"
      pattern: "onRetry"
---

<objective>
Redesign Safety and Programs — the two "parent page.tsx + tightly-coupled sub-components" sections — each behind its own flag (`'safety'`, `'programs'`). Both share a shape that makes Pattern 2 the clean choice: read the flag ONCE in the thin parent `page.tsx` and thread a `redesigned` boolean prop down to the sub-components (Safety's 4 panels, Programs' 3 panels — which cross-import each other and share one `OVERVIEW_KEY` query, so one flag decision maps to one query). Wire each panel's loading/empty/error through the shared `StateBlock`, extending ComplianceDashboard's existing partial wiring to its siblings; i18n the touched chrome — notably Safety's module-level `MANAGER_TABS` labels (a `PageHeader` chrome prop) and the hardcoded `"Unable to load…"` panel errors. Programs already reads `overview.isError` at page level (a factual correction to CONTEXT) — migrate that ad-hoc error card to `StateBlock`, so the wiring is trivial not net-new.

Purpose: SEC-01a for Safety + Programs; establishes the flag-thread pattern for multi-panel sections.
Output: safety/page.tsx + 4 panels and programs/page.tsx + 3 panels, flag-threaded and StateBlock-wired.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-02-SUMMARY.md

# Safety tree
@apps/web/app/(dashboard)/safety/page.tsx
@apps/web/components/safety/ComplianceDashboard.tsx
@apps/web/components/safety/IncidentReview.tsx
@apps/web/components/safety/SafetyInformation.tsx
@apps/web/components/safety/SafetyPrograms.tsx

# Programs tree (the 3 panels cross-import each other)
@apps/web/app/(dashboard)/programs/page.tsx
@apps/web/components/programs/HousekeepingDepthPanels.tsx
@apps/web/components/programs/DeepCleanAreasPanel.tsx
@apps/web/components/programs/InspectionDepthPanel.tsx

# Flag + state + primitives contracts (import-only; primitives.tsx/Button.tsx frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Safety — flag-thread from page.tsx to 4 panels; StateBlock states; i18n MANAGER_TABS + panel errors</name>
  <files>apps/web/app/(dashboard)/safety/page.tsx, apps/web/components/safety/ComplianceDashboard.tsx, apps/web/components/safety/IncidentReview.tsx, apps/web/components/safety/SafetyInformation.tsx, apps/web/components/safety/SafetyPrograms.tsx</files>
  <action>
1. In `safety/page.tsx`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('safety', hotel)`. Thread `redesigned={v2}` into each of the 4 panels. Keep the legacy path byte-behaviorally identical when `!v2` (panels render today's UI when `redesigned` is false).
2. Fix the MANAGER_TABS i18n (Pitfall 3): `MANAGER_TABS` (~safety/page.tsx:18-23) holds `{id, label}` with hardcoded English labels at module scope where `t()` can't run. Resolve labels INSIDE the component when building `PageHeader tabs`: `tabs={MANAGER_TABS.map(x => ({ ...x, label: t(`safety.tabs.${x.id}`) }))}`. Do NOT change tab ids or order (behavior).
3. In each of the 4 panels: accept a `redesigned?: boolean` prop. Under `redesigned`, render loading/empty/error via the shared `StateBlock`:
   - ComplianceDashboard already uses `StateBlock` partially — extend it to cover all three states consistently (the reference within Safety).
   - IncidentReview / SafetyInformation / SafetyPrograms use manual loading/error with hardcoded `"Unable to load…"` — replace with `StateBlock status='error' error={{ message: t('safety.loadError' or per-panel key), onRetry: () => void load() }}` (each panel already defines a `load()` callable) and `StateBlock status='empty'` with the panel's `safety.*` empty key. Loading → keep the panel's existing skeleton (restyled), NOT a StateBlock spinner, unless it already used a spinner.
   - When `redesigned` is false, each panel renders exactly as today (legacy).
4. i18n scope discipline: convert the tab labels and the panel loading/empty/error strings (in-scope). `SafetyPrograms.tsx`'s 16 raw hits are mostly FORM strings — only convert the ones inside a state/chrome this task touches; DEFER the create-form/validation pocket (note it for the close-out deferred list per CONTEXT). Do not let full-file i18n balloon this plan.
5. v2 tokens for chrome. Do NOT edit frozen files, re-tint room-status tokens, or change any query/field/mutation.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('safety'` and `redesigned` prop threaded in safety/page.tsx, `t('safety.tabs.` for the tab labels, `redesigned` accepted in all 4 panels, and `StateBlock` in each panel; grep confirms no remaining hardcoded `"Unable to load"` in the v2 paths; git status shows only the 5 safety files changed</verify>
  <done>Safety page reads the flag once and threads `redesigned` to 4 panels; MANAGER_TABS labels i18n'd via safety.tabs.*; each panel wires StateBlock loading/empty/error (onRetry: load()); legacy unchanged flag-off; deferred form-i18n pocket noted; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Programs — flag-thread from page.tsx to 3 panels; migrate page-level error to StateBlock; panel states</name>
  <files>apps/web/app/(dashboard)/programs/page.tsx, apps/web/components/programs/HousekeepingDepthPanels.tsx, apps/web/components/programs/DeepCleanAreasPanel.tsx, apps/web/components/programs/InspectionDepthPanel.tsx</files>
  <action>
1. In `programs/page.tsx`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('programs', hotel)`. Thread `redesigned={v2}` into the 3 panels. Keep legacy byte-behaviorally identical when `!v2`.
2. FACTUAL CORRECTION (verified, from 33-RESEARCH): Programs ALREADY reads `overview.isError` and renders an ad-hoc `loadError` Card at page level (programs/page.tsx:51), and `overview` already exposes `isError`/`isLoading`/`refetch` (used by the refresh button). So this is a trivial MIGRATION, not net-new: under `v2`, replace that ad-hoc error Card with `StateBlock status='error' error={{ message: t('programs.loadError'), onRetry: () => overview.refetch() }}` (reuse the EXISTING `programs.loadError` key — do NOT duplicate it). Loading → restyled skeleton (v2), NOT StateBlock spinner. Empty → StateBlock empty where the page has no data.
3. In the 3 panels: accept `redesigned?: boolean`. The panels currently guard empty on `!isLoading` only (no error branch of their own). Under `redesigned`, render each panel's empty via `StateBlock status='empty'` with its `programs.*` empty key. For panel-level ERROR: CONTEXT leaves it to discretion whether each panel gets its own error boundary or shares the parent's — since the 3 panels share the ONE `OVERVIEW_KEY` query, the parent-level StateBlock error (Task step 2) already covers the shared failure; only add a per-panel error state if a panel has its OWN independent query (verify by reading the panels). Pick the structure that matches actual data-fetching and document it. Keep the panels' cross-imports intact.
4. i18n the touched panel empty/error/chrome strings via `programs.*`. v2 tokens for chrome. Do NOT edit frozen files, re-tint room-status tokens, or change any query/field.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('programs'` and `redesigned` threaded in programs/page.tsx, `overview.refetch` as the StateBlock onRetry, `redesigned` accepted in all 3 panels, and `StateBlock` usage; grep confirms `programs.loadError` reused (not duplicated) and the ad-hoc loadError Card removed from the v2 path; git status shows only the 4 programs files changed</verify>
  <done>Programs page reads the flag once and threads `redesigned` to 3 panels; page-level error migrated from ad-hoc Card to StateBlock (onRetry: overview.refetch, reusing existing programs.loadError); panel empties (and per-panel error only where a panel has its own query) wired; legacy unchanged flag-off; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Safety: flag read once in page.tsx, `redesigned` threaded to 4 panels; MANAGER_TABS labels i18n'd; panel StateBlock loading/empty/error (onRetry: load()); legacy unchanged flag-off.
- Programs: flag read once, `redesigned` threaded to 3 panels; page-level error migrated to StateBlock (onRetry: overview.refetch, existing programs.loadError reused); panel empties wired; legacy unchanged flag-off.
- Only the 9 safety/programs files changed; no frozen file edited; no locale-file edit; no query/mutation/field added or removed; deferred form-i18n pockets (esp. SafetyPrograms) noted for close-out.
</verification>

<success_criteria>
Safety and Programs render in the new visual system behind their own flags, using the flag-thread-to-subpanels pattern, with each panel's loading/empty/error served by the shared StateBlock and the touched chrome (Safety tab labels, panel error copy; Programs page/panel states) i18n'd — zero behavior/data/RBAC change — and the Programs page-level error correctly migrated (not reinvented) from its existing overview.isError.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-05-SUMMARY.md` recording: the flag-thread wiring per section (prop name, which panels received it), how Safety's MANAGER_TABS labels were resolved to t(), the Programs error-migration (ad-hoc Card → StateBlock, existing programs.loadError reused, overview.refetch onRetry), the panel-error decision (per-panel vs shared parent, and why, based on actual query structure), the safety.*/programs.* keys consumed, any deferred form-i18n pocket (esp. SafetyPrograms) for the close-out list, and confirmation no frozen/locale file was touched.
</output>
</content>
