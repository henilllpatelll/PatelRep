---
phase: 33-core-operational-sections
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/app/(dashboard)/sop/page.tsx
  - apps/web/app/(dashboard)/logbook/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of sop/page.tsx and logbook/page.tsx.
# No wave-2 sibling touches these files (verified: zero cross-section file sharing). This plan
# does NOT edit en.ts/es.ts — the new `sop` and `logbook` namespaces exist from 33-01; it only
# consumes them. Both are single-file sections that each define a LOCAL SkeletonCard()/EmptyState()
# shadowing the shared components — this plan DELETES those locals in favor of the shared
# StateBlock/EmptyState (the exact anti-pattern the phase removes).

must_haves:
  truths:
    - "With the 'sop' flag ON, SOP Library renders v2 with its loading/empty/error states served by the shared StateBlock/EmptyState (its local SkeletonCard()/EmptyState() helpers are gone); flag OFF is byte-behaviorally identical to today"
    - "With the 'logbook' flag ON, Logbook renders v2 with loading/empty/error via shared StateBlock/EmptyState (local SkeletonCard() gone), and a real error state via isError/refetch on its existing query; flag OFF is byte-behaviorally identical to today"
    - "Every loading/empty/error string these two sections render in v2 is an i18n `t()` call into the `sop.*`/`logbook.*` namespaces (or `common.*`), not a hardcoded English literal — both sections had NO namespace before this phase"
    - "No query/mutation/filter/field added or removed; SOP's onRetry points at its existing fetchDocuments() callable, Logbook's at its existing query refetch"
  artifacts:
    - path: "apps/web/app/(dashboard)/sop/page.tsx"
      provides: "SOP flag-branched on isSectionRedesigned('sop', hotel); local SkeletonCard()/EmptyState() removed; shared StateBlock empty/error wired (onRetry: fetchDocuments); i18n'd via sop.* keys; legacy branch unchanged"
      contains: "isSectionRedesigned('sop'"
    - path: "apps/web/app/(dashboard)/logbook/page.tsx"
      provides: "Logbook flag-branched on isSectionRedesigned('logbook', hotel); local SkeletonCard() removed; shared StateBlock empty/error wired (isError/refetch added to existing query); i18n'd via logbook.* keys; legacy branch unchanged"
      contains: "isSectionRedesigned('logbook'"
  key_links:
    - from: "apps/web/app/(dashboard)/sop/page.tsx"
      to: "isSectionRedesigned('sop', hotel) + shared StateBlock"
      via: "useHotelStore direct read; StateBlock error.onRetry = () => fetchDocuments()"
      pattern: "isSectionRedesigned\\('sop'"
    - from: "apps/web/app/(dashboard)/logbook/page.tsx"
      to: "isSectionRedesigned('logbook', hotel) + shared StateBlock"
      via: "useHotelStore direct read; add isError/refetch to existing useQuery; StateBlock error.onRetry = refetch"
      pattern: "isSectionRedesigned\\('logbook'"
---

<objective>
Redesign SOP Library and Logbook onto the v2 system, each behind its own flag (`'sop'`, `'logbook'`). These two share a shape: single-file pages that each hand-rolled a LOCAL `SkeletonCard()`/`EmptyState()` helper shadowing the shared components — the exact anti-pattern this phase exists to remove. Delete those locals and route loading/empty/error through the shared `StateBlock`/`EmptyState`. Both sections had NO i18n namespace before this phase, so every state string they render in v2 must become a `t()` call into the `sop.*`/`logbook.*` keys added in 33-01. Logbook additionally needs a real error state (add `isError`/`refetch` to its existing react-query destructure).

Purpose: SEC-01a for SOP + Logbook; removes the local-shadow anti-pattern in both.
Output: `sop/page.tsx` and `logbook/page.tsx` flag-branched, locals deleted, shared StateBlock wired, i18n'd.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md
# Canonical StateBlock example from the fast known-good plan — mirror its v2 card/skeleton shell
@.planning/phases/33-core-operational-sections/33-02-SUMMARY.md

# The two section files this plan edits
@apps/web/app/(dashboard)/sop/page.tsx
@apps/web/app/(dashboard)/logbook/page.tsx

# Flag + state + primitives contracts (import-only; primitives.tsx/Button.tsx frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign SOP — flag branch, delete local SkeletonCard()/EmptyState(), wire shared StateBlock, i18n</name>
  <files>apps/web/app/(dashboard)/sop/page.tsx</files>
  <action>
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('sop', hotel)`. Keep today's JSX under the legacy branch, byte-behaviorally identical when `!v2`.
2. SOP uses manual `useState` + `fetchDocuments(silent)` with an inline `fetchError` state, and defines LOCAL `SkeletonCard()` and `EmptyState()` helpers. In the v2 branch:
   - Loading → keep a skeleton (restyle the existing local skeleton shapes to the v2 card shell from 33-02) OR inline v2 skeleton cards — do NOT route through `StateBlock status='loading'` (spinner). Reuse the shared shell, not a bespoke local component.
   - Empty → shared `<StateBlock status='empty' empty={{ title: t('sop.empty.title'), body: t('sop.empty.body') }}>`.
   - Error → shared `<StateBlock status='error' error={{ message: t('sop.loadError'), onRetry: () => fetchDocuments() }}>` (the existing callable at ~sop/page.tsx:365 — do NOT wrap it in a new fetch; call the one already defined).
   - DELETE the local `EmptyState()` helper (and the local `SkeletonCard()` if fully replaced) once the shared component covers its use — leaving a shadowing local is the anti-pattern. If `SkeletonCard()` is still needed for the restyled inline skeleton, keep it but rename/reduce so it doesn't shadow the shared `EmptyState`/`StateBlock` names.
3. i18n: every state string rendered in v2 uses `t('sop.*')`/`t('common.*')` — no hardcoded English inside the touched states/chrome. Header title/subtitle via `t('sop.pageTitle')`/`t('sop.pageSubtitle')` through the existing `PageHeader`.
4. v2 tokens for chrome (Phase-30/31 aliases). Do NOT edit frozen files, re-tint room-status tokens, or change any query/upload/field behavior. Upload-modal internals outside the touched states are lower priority — only i18n them if trivial and inside chrome you're already restyling; otherwise flag for the deferred list.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('sop'`, a shared `StateBlock`, and `fetchDocuments` as onRetry in sop/page.tsx; grep confirms the local `EmptyState`-shadowing helper is removed (no local `function EmptyState`/`const EmptyState` shadowing the shared import); git status shows only sop/page.tsx changed</verify>
  <done>SOP v2 branch uses shared StateBlock empty/error (onRetry: fetchDocuments) + restyled skeleton loading, local shadowing helper deleted, all state copy i18n'd via sop.*; legacy branch unchanged flag-off; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Logbook — flag branch, delete local SkeletonCard(), wire shared StateBlock with real error, i18n</name>
  <files>apps/web/app/(dashboard)/logbook/page.tsx</files>
  <action>
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('logbook', hotel)`. Keep today's JSX under the legacy branch, byte-behaviorally identical when `!v2`.
2. Logbook uses react-query `{ data: entries, isLoading }` and a LOCAL `SkeletonCard()`. Extend the destructure to `{ data: entries, isLoading, isError, refetch }` (zero-data-change — reads state the query already computes; do NOT add a new query). In the v2 branch:
   - Loading → restyled skeleton (v2 shell), NOT `StateBlock status='loading'` spinner. Remove/rename the local `SkeletonCard()` so it doesn't shadow the shared component.
   - Empty → shared `<StateBlock status='empty' empty={{ title: t('logbook.empty.title'), body: t('logbook.empty.body') }}>`.
   - Error → shared `<StateBlock status='error' error={{ message: t('logbook.loadError'), onRetry: () => refetch() }}>` (NEW state — today no error branch renders).
3. i18n: header + all state strings via `t('logbook.*')`/`t('common.*')`. The hardcoded form-VALIDATION error strings (10 raw hits research flagged) are only in-scope if they render inside a state this phase is touching; if they live entirely inside the create-form (outside loading/empty/error/header chrome), DEFER them (do not convert) and note the pocket for the close-out deferred list — do not let form-field i18n balloon this plan.
4. v2 tokens for chrome. Do NOT edit frozen files, re-tint room-status tokens, or change any query/field/mutation behavior.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('logbook'`, `isError`, `refetch`, and a shared `StateBlock` with `status='error'` in logbook/page.tsx; grep confirms the local shadowing `SkeletonCard` no longer shadows the shared component; git status shows only logbook/page.tsx changed; grep confirms no new useQuery/useMutation added</verify>
  <done>Logbook v2 branch uses shared StateBlock empty + real error (onRetry: refetch, isError added to existing query) + restyled skeleton loading, local skeleton no longer shadows shared, state copy i18n'd via logbook.*; legacy branch unchanged flag-off; gates green; any deferred form-validation i18n pocket noted</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- SOP: `isSectionRedesigned('sop')` branch; shared StateBlock empty/error (onRetry: fetchDocuments); local shadowing helper deleted; state copy i18n'd; legacy unchanged flag-off.
- Logbook: `isSectionRedesigned('logbook')` branch; shared StateBlock empty + NEW error (isError/refetch); local skeleton de-shadowed; state copy i18n'd; legacy unchanged flag-off.
- Only sop/page.tsx and logbook/page.tsx changed; no frozen/locale file edited; no query/mutation/field added or removed.
</verification>

<success_criteria>
SOP Library and Logbook render in the new visual system behind their own flags, with loading/empty/error served by the shared StateBlock/EmptyState (their local shadowing helpers removed) and every touched state string i18n'd into the new sop.*/logbook.* namespaces — Logbook gaining a genuine error state — zero behavior/data/RBAC change.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-03-SUMMARY.md` recording: which local helpers were deleted vs renamed in each file, the onRetry wiring per section (SOP fetchDocuments vs Logbook refetch), that Logbook's error state is net-new via existing-query isError/refetch, exactly which sop.*/logbook.* keys were consumed, any form-validation/create-form i18n pocket DEFERRED for the close-out list, and confirmation no frozen/locale file was touched.
</output>
</content>
