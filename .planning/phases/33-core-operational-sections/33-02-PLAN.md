---
phase: 33-core-operational-sections
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/app/(dashboard)/tasks/page.tsx
  - apps/web/app/(dashboard)/evidence/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of tasks/page.tsx and evidence/page.tsx.
# No wave-2 sibling touches these files (verified: zero cross-section file sharing). This plan
# does NOT edit en.ts/es.ts — all keys exist from 33-01; it only consumes them. Both sections are
# already i18n-clean (Tasks 81 t(), Evidence 74 t(), 0 raw), so this is the fast "known-good"
# plan: Tasks needs an error branch + v2 token/skeleton restyle; Evidence (the StateBlock
# reference impl) needs only token polish + confirming it reads its 'evidence' flag.

must_haves:
  truths:
    - "With the 'tasks' flag ON, the Tasks page renders the v2 visual system and now shows a real error state with retry when its query fails (today it silently renders an empty list on failure); with the flag OFF it is byte-behaviorally identical to today"
    - "With the 'evidence' flag ON, the Evidence page renders the v2 visual system via its existing StateBlock wiring (loading/empty/error already correct); with the flag OFF it is byte-behaviorally identical to today"
    - "Both sections show loading as a skeleton (not a StateBlock spinner) where they use skeletons today, empty via StateBlock empty with i18n copy, and error via StateBlock error with onRetry pointed at the query's own refetch/reload"
    - "No query, mutation, filter, or field is added or removed — adding `isError`/`refetch` to Tasks' existing useQuery destructure reads state the query already computes (zero data change)"
  artifacts:
    - path: "apps/web/app/(dashboard)/tasks/page.tsx"
      provides: "Tasks page flag-branched on isSectionRedesigned('tasks', hotel); v2 branch = new tokens + skeleton loading + StateBlock empty + NEW StateBlock error (isError/refetch added to existing query); legacy branch unchanged"
      contains: "isSectionRedesigned('tasks'"
    - path: "apps/web/app/(dashboard)/evidence/page.tsx"
      provides: "Evidence page flag-branched on isSectionRedesigned('evidence', hotel); v2 branch = token polish over its existing correct StateBlock wiring; legacy branch unchanged"
      contains: "isSectionRedesigned('evidence'"
  key_links:
    - from: "apps/web/app/(dashboard)/tasks/page.tsx"
      to: "isSectionRedesigned('tasks', hotel)"
      via: "useHotelStore((s)=>s.hotel) direct read (Phase-32 pattern)"
      pattern: "isSectionRedesigned\\('tasks'"
    - from: "apps/web/app/(dashboard)/tasks/page.tsx"
      to: "the existing tasks useQuery"
      via: "add isError, refetch to the existing { data, isLoading } destructure; wire StateBlock error.onRetry = refetch"
      pattern: "isError|refetch"
    - from: "apps/web/app/(dashboard)/evidence/page.tsx"
      to: "isSectionRedesigned('evidence', hotel)"
      via: "useHotelStore direct read"
      pattern: "isSectionRedesigned\\('evidence'"
---

<objective>
Redesign the Tasks and Evidence sections onto the v2 token/StateBlock system, each behind its own new flag key (`'tasks'`, `'evidence'`). Both are already i18n-clean and Evidence is the StateBlock reference implementation, so this is the low-risk, high-signal plan whose v2 output is the canonical example the other content plans mirror. The one real behavior-visible gap is Tasks' missing error state: it destructures only `{ data, isLoading }`, so an API failure silently renders an empty list — add `isError, refetch` (reading state the query already produces) and render `StateBlock status='error'` with retry.

Purpose: SEC-01a for Tasks + Evidence; the "known-good" StateBlock pattern reference for 33-03..33-06.
Output: `tasks/page.tsx` and `evidence/page.tsx` flag-branched with rebuilt/confirmed loading/empty/error states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md

# The two section files this plan edits
@apps/web/app/(dashboard)/tasks/page.tsx
@apps/web/app/(dashboard)/evidence/page.tsx

# Flag + state + primitives contracts (import-only; primitives.tsx/Button.tsx are frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Tasks — flag branch, v2 tokens, skeleton loading, StateBlock empty, NEW error state</name>
  <files>apps/web/app/(dashboard)/tasks/page.tsx</files>
  <action>
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('tasks', hotel)`. Keep ALL current JSX under `if (!v2)` (or an equivalent branch) byte-behaviorally identical — flag-off must equal today's UI.
2. In the existing tasks `useQuery`, extend the destructure from `{ data, isLoading }` to `{ data, isLoading, isError, refetch }`. This is zero-data-change (reads state the query already computes). Do NOT add a new query or change query keys/params.
3. v2 branch: keep the existing `PageHeader`. Wrap the task list region in `StateBlock`:
   `status={isLoading ? 'loading' : isError ? 'error' : items.length === 0 ? 'empty' : null}` — BUT per the skeleton-not-spinner convention, do NOT route the card list's loading through `StateBlock status='loading'` (that renders a Loader2 spinner). Instead keep the existing inline `animate-pulse` skeleton rows (the `h-[42px]` rows) for loading, restyled to the v2 card shell, and use `StateBlock` only for `empty` and `error`:
   - empty → `empty={{ title: t('tasks.empty.<title>'), body: t('tasks.empty.<body>') }}` using the existing `tasks.empty.*` keys.
   - error → `error={{ message: t('tasks.loadError'), onRetry: () => refetch() }}` (key from 33-01; reuse `common.error` if 33-01 reused that).
4. Apply v2 tokens to the chrome (cards, spacing, hover/focus) using the Phase-30/31 Tailwind aliases (`duration-base`, `ease-standard`, `bg-surface-*`, `focus-visible:ring-[var(--focus-ring)]`). Do NOT re-tint any room-status token for generic chrome; do NOT edit primitives.tsx/Button.tsx (frozen — import only). Do NOT add/remove/reorder any task field, filter, or action.
5. Confirm the network requests are identical flag-on vs flag-off (you changed no query params) — Success Criterion #3 is satisfied structurally.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('tasks'`, `isError`, `refetch`, and a `StateBlock` with `status='error'`/`onRetry` in tasks/page.tsx; git status shows only tasks/page.tsx changed; grep confirms no new `useQuery`/`useMutation` added</verify>
  <done>Tasks v2 branch renders new tokens with skeleton loading + StateBlock empty + a real StateBlock error (retry wired to refetch); legacy branch unchanged flag-off; no query/field/filter added or removed; all gates green</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Evidence — flag branch + v2 token polish over its existing correct StateBlock wiring</name>
  <files>apps/web/app/(dashboard)/evidence/page.tsx</files>
  <action>
Evidence is already the StateBlock reference impl (`evidence/page.tsx:512-516` wires `status`/`loadingLabel`/`error.onRetry`/`empty.title` correctly, 74 t(), 0 raw). The only structural change is gating it behind its flag; the rest is token polish.
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('evidence', hotel)`. Keep today's JSX under the legacy branch byte-behaviorally identical when `!v2`.
2. v2 branch: reuse the existing StateBlock wiring as-is (it already branches loading/empty/error with `load()` as onRetry). Apply v2 token polish to the surrounding chrome (`PageHeader`, cards, spacing) using the Phase-30/31 aliases — same visual family as Tasks (mirror Task 1's card shell so the two look consistent).
3. If Evidence's loading state currently uses `StateBlock status='loading'` (spinner) and that is its established affordance, leave it — the convention allows the spinner where it was already a spinner (per 33-RESEARCH: Evidence's spinner is acceptable). Only convert to a skeleton if that's what the surrounding cards use; do NOT introduce layout shift.
4. Consume only 33-01 keys (likely nothing new needed — Evidence copy already exists). Do NOT edit en.ts/es.ts. Do NOT edit any frozen file. Do NOT change any query/field/filter.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('evidence'` in evidence/page.tsx and the existing StateBlock wiring is intact (status/error.onRetry/empty still present); git status shows only evidence/page.tsx changed</verify>
  <done>Evidence v2 branch renders the polished v2 chrome over its existing correct StateBlock states, gated by the 'evidence' flag; legacy branch unchanged flag-off; no query/field change; all gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Tasks: `isSectionRedesigned('tasks')` branch; new StateBlock error (isError/refetch) + skeleton loading + StateBlock empty; legacy unchanged flag-off.
- Evidence: `isSectionRedesigned('evidence')` branch; existing StateBlock states preserved with v2 token polish; legacy unchanged flag-off.
- Only tasks/page.tsx and evidence/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation/field/filter added or removed.
</verification>

<success_criteria>
Tasks and Evidence render in the new visual system behind their own flags, with all three states (skeleton loading / StateBlock empty / StateBlock error-with-retry) — Tasks gaining a genuine error state it lacked — zero behavior/data/RBAC change, and this plan's v2 output stands as the canonical StateBlock example for the sibling content plans.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-02-SUMMARY.md` recording: the exact v2 card/skeleton shell used (so 33-03..33-06 mirror it), the Tasks error-state wiring (which query, that `isError`/`refetch` was added to the existing destructure with zero data change), whether Evidence kept its spinner or moved to a skeleton and why, the exact keys consumed from 33-01, and confirmation no frozen/locale file was touched.
</output>
</content>
