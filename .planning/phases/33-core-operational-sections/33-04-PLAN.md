---
phase: 33-core-operational-sections
plan: 04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/components/guest-requests/GuestRequestsPage.tsx
  - apps/web/app/(dashboard)/lost-found/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of GuestRequestsPage.tsx and
# lost-found/page.tsx. No wave-2 sibling touches these (verified: zero cross-section sharing).
# It does NOT edit en.ts/es.ts — the extended `guestRequests` block and new `lostFound` namespace
# exist from 33-01. This plan bundles the two HIGHEST-RISK sections deliberately:
#   (a) Guest Requests = the ONLY section with zero shared chrome today (net-new PageHeader from
#       scratch, not a restyle), and
#   (b) Lost & Found = the ONLY in-scope importer of the FROZEN LogFoundItemModal.
# The frozen-modal discipline is isolated here so its risk is not mixed with unrelated work.
#
# *** FROZEN-FILE CONSTRAINT (Success Criterion #4) — READ BEFORE TOUCHING LOST & FOUND ***
# apps/web/components/shared/LogFoundItemModal.tsx is Phase-30 frozen (hash-pinned in
# frozen-files.json:7 = 649c9516...a656c) and imported in TWO places: lost-found/page.tsx (in
# scope) and components/housekeeping/RoomDetailDrawer.tsx (OUT of scope — frozen Room-Board
# surface). The DEFAULT AND EXPECTED OUTCOME IS: the modal is NOT edited, and
# frozen-files-allowlist.json stays empty (entries: []). Restyle only the page chrome AROUND the
# modal. The modal may ONLY gain a NEW variant/prop if the redesign genuinely needs one — NEVER
# mutate an existing prop/variant/behavior (RoomDetailDrawer depends on today's behavior). If a
# hash bump is genuinely unavoidable, bump frozen-files.json AND add {file,new_hash,reason,
# approved} to frozen-files-allowlist.json in the SAME change, mirroring Phase 30/31/32 precedent.
# check:frozen-files and the Room-Board regression gate must both stay green.

must_haves:
  truths:
    - "With the 'guestRequests' flag ON, Guest Requests renders shared v2 chrome for the FIRST time — a PageHeader (title + the Active/History tabs via PageHeader's tabs prop, replacing the raw <h1>/<button> header) and StateBlock loading/empty/error for the kanban columns — plus a real error state (isError/refetch added to the existing query); flag OFF is byte-behaviorally identical to today"
    - "With the 'lostFound' flag ON, Lost & Found renders v2 with shared StateBlock loading/empty/error (its local SkeletonCard()/direct EmptyState usage migrated, error state added via isError/refetch); flag OFF is byte-behaviorally identical to today"
    - "LogFoundItemModal.tsx is UNCHANGED (check:frozen-files green, allowlist still empty) and the Room-Board regression gate still passes — the modal was imported and rendered exactly as before, only the page chrome around it was restyled"
    - "Every touched-chrome/state string in both sections is a t() call — Guest Requests kanban strings (page title/subtitle, tabs, column labels, card action buttons, URGENT, SLA overdue, time-ago suffixes) into the extended guestRequests.* keys, Lost & Found into the new lostFound.* keys; no hardcoded English inside touched chrome; no query/mutation/field added or removed"
  artifacts:
    - path: "apps/web/components/guest-requests/GuestRequestsPage.tsx"
      provides: "Guest Requests kanban page: net-new PageHeader (title + Active/History tabs prop + New Request action), StateBlock for column loading/empty/error (isError/refetch added to existing query), all kanban strings i18n'd via guestRequests.*; flag-branched on isSectionRedesigned('guestRequests', hotel); legacy branch unchanged"
      contains: "isSectionRedesigned('guestRequests'"
    - path: "apps/web/app/(dashboard)/lost-found/page.tsx"
      provides: "Lost & Found page chrome redesigned to v2 with shared StateBlock loading/empty/error (isError/refetch added); LogFoundItemModal imported/rendered unchanged; i18n'd via lostFound.*; flag-branched on isSectionRedesigned('lostFound', hotel); legacy branch unchanged"
      contains: "isSectionRedesigned('lostFound'"
  key_links:
    - from: "apps/web/components/guest-requests/GuestRequestsPage.tsx"
      to: "PageHeader with tabs prop"
      via: "replace raw <h1>/<button> header with <PageHeader title tabs=[{active},{history}] actions=<New Request> />"
      pattern: "PageHeader"
    - from: "apps/web/components/guest-requests/GuestRequestsPage.tsx"
      to: "isSectionRedesigned('guestRequests', hotel) + existing kanban query"
      via: "useHotelStore direct read (in GuestRequestsPage, NOT the 10-line page.tsx wrapper); add isError/refetch to existing useQuery"
      pattern: "isSectionRedesigned\\('guestRequests'"
    - from: "apps/web/app/(dashboard)/lost-found/page.tsx"
      to: "LogFoundItemModal (frozen) rendered unchanged + isSectionRedesigned('lostFound', hotel)"
      via: "useHotelStore direct read; StateBlock error.onRetry = refetch; modal props untouched"
      pattern: "isSectionRedesigned\\('lostFound'"
---

<objective>
Redesign the two highest-risk sections, isolated together in one plan so their distinct risks don't contaminate the simpler plans: Guest Requests (the ONLY section with zero shared chrome today — needs a PageHeader written from scratch, not a restyle) and Lost & Found (the ONLY in-scope importer of the FROZEN `LogFoundItemModal`). Each is gated behind its own flag (`'guestRequests'`, `'lostFound'`). Both also need a real error state added via `isError`/`refetch` on their existing queries.

Purpose: SEC-01a for Guest Requests + Lost & Found; brings Guest Requests in line with its 8 siblings for the first time; contains the frozen-modal discipline in one reviewable place.
Output: `GuestRequestsPage.tsx` with net-new PageHeader + StateBlock states; `lost-found/page.tsx` with v2 chrome + StateBlock states and the frozen modal untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-02-SUMMARY.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# The two files this plan edits
@apps/web/components/guest-requests/GuestRequestsPage.tsx
@apps/web/app/(dashboard)/lost-found/page.tsx

# Guest Requests siblings (READ for context — do NOT edit; they are not in this plan's scope)
@apps/web/components/guest-requests/HistoryTab.tsx

# Frozen modal — IMPORT AND RENDER UNCHANGED, do NOT edit
@apps/web/components/shared/LogFoundItemModal.tsx
@apps/web/frozen-files.json
@apps/web/frozen-files-allowlist.json

# Flag + state + primitives contracts (import-only; primitives.tsx/Button.tsx frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest Requests — net-new PageHeader chrome + StateBlock states + kanban i18n (flag-branched)</name>
  <files>apps/web/components/guest-requests/GuestRequestsPage.tsx</files>
  <action>
The flag read goes in `GuestRequestsPage.tsx` (the real component), NOT the 10-line `page.tsx` Suspense wrapper. Read directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('guestRequests', hotel)`. Keep today's JSX under the legacy branch, byte-behaviorally identical when `!v2`.

v2 branch (this is net-new chrome, written from scratch — the other 8 sections already had PageHeader):
1. Replace the raw `<div>`/`<h1>Guest Requests</h1>` + subtitle and the raw `<button>` Active/History tabs (~GuestRequestsPage.tsx:220-249) with a single `<PageHeader title={t('guestRequests.pageTitle')} subtitle={t('guestRequests.pageSubtitle')} tabs={[{ label: t('guestRequests.tabActive'), active: tab==='active', onClick: ()=>setTab('active') }, { label: t('guestRequests.tabHistory'), active: tab==='history', onClick: ()=>setTab('history') }]} actions={<Button …>{t('guestRequests.newRequest'... existing key)}</Button>} />`. Preserve the exact existing tab state/handlers — chrome swap only, no behavior change.
2. Extend the existing kanban `useQuery` destructure to add `isError, refetch` (zero-data-change). Leave `refetchInterval: 30_000` polling untouched (behavior). Wrap the kanban columns' loading/empty region in `StateBlock`:
   - Loading → keep the column pulse skeletons, restyled to v2 shell (NOT StateBlock spinner).
   - Empty → migrate the raw `"No requests"` text (~:274) to `StateBlock status='empty' empty={{ title: t('guestRequests.empty.title') }}` per column (or page-level, whichever matches the current empty affordance without behavior change).
   - Error → NEW: `StateBlock status='error' error={{ message: t('guestRequests.loadError'), onRetry: () => refetch() }}` (today an API failure silently shows empty columns).
3. i18n the touched chrome strings (all currently raw, all inside chrome this task rebuilds): the 3 COLUMNS labels (`Open`/`Acknowledged`/`Verify Resolution`, ~:39-62) via `t('guestRequests.columns.*')`; card action buttons (`Acknowledge`/`Dispatch`/`Arrived`/`Contacted`/`Resolve`/`Verify`) via `t('guestRequests.<action>')`; `URGENT` → `t('guestRequests.urgent')`; `SLA overdue` → `t('guestRequests.slaOverdue')`; time-ago suffixes (`m ago`/`h ago`/`d ago`) via `t('guestRequests.timeAgo.*')` with the interpolation shape 33-01 defined. Keep the status-meaning color tokens on column headers (`--info`/`--caution`/`--ready`) exactly — do NOT re-tint them.
4. Do NOT edit the sibling files (GuestRequestDrawer, HistoryTab, NewRequestModal) — out of this plan's file scope; if the tabs prop needs HistoryTab, mount it exactly as today. Do NOT edit frozen files or change any query/mutation/field.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('guestRequests'`, `PageHeader`, `isError`, `refetch`, and a `StateBlock` with `status='error'` in GuestRequestsPage.tsx; grep confirms NO remaining raw `"No requests"`/`"URGENT"`/`"Verify Resolution"` literals in the v2 branch; git status shows only GuestRequestsPage.tsx changed</verify>
  <done>Guest Requests v2 branch has a real PageHeader (title + tabs prop + New Request action), StateBlock loading/empty/error (real error via isError/refetch), all kanban strings i18n'd via guestRequests.*, status colors preserved; legacy branch unchanged flag-off; only GuestRequestsPage.tsx changed; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Lost & Found — v2 chrome + shared StateBlock states, frozen LogFoundItemModal untouched (flag-branched)</name>
  <files>apps/web/app/(dashboard)/lost-found/page.tsx</files>
  <action>
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('lostFound', hotel)`. Keep today's JSX under the legacy branch, byte-behaviorally identical when `!v2`.
2. Lost & Found uses react-query `{ data: items, isLoading }`, a LOCAL `SkeletonCard()`, and `EmptyState` directly with hardcoded titles; it imports the FROZEN `LogFoundItemModal` (lost-found/page.tsx:23). Extend the destructure to `{ data: items, isLoading, isError, refetch }` (zero-data-change). v2 branch:
   - Loading → restyled skeleton (v2 shell), NOT StateBlock spinner; de-shadow the local `SkeletonCard()`.
   - Empty → shared `<StateBlock status='empty' empty={{ title: t('lostFound.empty.title'), body: t('lostFound.empty.body') }}>` (replace the hardcoded EmptyState titles).
   - Error → NEW shared `<StateBlock status='error' error={{ message: t('lostFound.loadError'), onRetry: () => refetch() }}>`.
   - Header via `PageHeader` (already present) using `t('lostFound.pageTitle')`/`t('lostFound.pageSubtitle')`.
3. *** FROZEN MODAL — render `LogFoundItemModal` EXACTLY as today. *** Do NOT change its import, its props, or how/when it opens. Restyle only the page list/grid/header/states AROUND it. Expected outcome: `check:frozen-files` green, `frozen-files-allowlist.json` stays `entries: []`. If (and only if) the v2 redesign genuinely requires a modal change, it must be an ADDITIVE new prop/variant with a hash bump in frozen-files.json + an allowlist entry in the same change — but default to NOT needing this and confirm the modal diff is empty.
4. i18n the touched chrome/state strings via `lostFound.*`. Do NOT edit other frozen files, re-tint room-status tokens, or change any query/field.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; `git diff --stat apps/web/components/shared/LogFoundItemModal.tsx` shows NO changes to the modal; `git status` on frozen-files-allowlist.json shows it unchanged (still entries: []); grep confirms `isSectionRedesigned('lostFound'`, `isError`, `refetch`, shared `StateBlock` in lost-found/page.tsx; git status shows only lost-found/page.tsx changed (modal + allowlist untouched)</verify>
  <done>Lost & Found v2 branch uses shared StateBlock loading/empty/error (real error via isError/refetch), local skeleton de-shadowed, chrome i18n'd via lostFound.*, and LogFoundItemModal is byte-unchanged with an empty allowlist; legacy branch unchanged flag-off; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Guest Requests: net-new PageHeader (title + tabs prop + action); StateBlock loading/empty/error incl. real error (isError/refetch); kanban strings i18n'd; status colors preserved; legacy unchanged flag-off.
- Lost & Found: v2 chrome + shared StateBlock states (real error); LogFoundItemModal.tsx byte-unchanged; frozen-files-allowlist.json still empty; legacy unchanged flag-off.
- Only GuestRequestsPage.tsx and lost-found/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation/field added or removed; Room-Board regression gate (checked in 33-07) will still pass because the frozen modal is untouched.
</verification>

<success_criteria>
Guest Requests and Lost & Found render in the new visual system behind their own flags — Guest Requests gaining shared PageHeader/StateBlock chrome for the first time, both gaining a genuine error state — with every touched string i18n'd, zero behavior/data/RBAC change, and the frozen LogFoundItemModal (and the Room-Board surface that shares it) provably untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-04-SUMMARY.md` recording: the exact PageHeader/tabs wiring for Guest Requests (which handlers threaded, that behavior was preserved), the guestRequests.*/lostFound.* keys consumed, that both error states are net-new via existing-query isError/refetch, an EXPLICIT confirmation that LogFoundItemModal.tsx is byte-unchanged and frozen-files-allowlist.json is still empty (or, if genuinely bumped, the additive prop + allowlist justification), and any deferred i18n pocket for the close-out list.
</output>
</content>
