---
phase: 33-core-operational-sections
plan: 06
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - apps/web/app/(dashboard)/scheduling/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of scheduling/page.tsx. No wave-2 sibling
# touches it (verified: zero cross-section sharing). It does NOT edit en.ts/es.ts — the new
# `scheduling` namespace exists from 33-01; it only consumes it. Scheduling gets its OWN plan
# because it is the LARGEST single file (1388 LOC), has MULTIPLE react-queries (rosterQuery,
# assignmentsQuery, staffQuery), and its i18n dirt is HIDDEN inside StateBlock/EmptyState/PageHeader
# object-literal props (title=/message=/subtitle= string literals) that a naive >text< grep misses.

must_haves:
  truths:
    - "With the 'scheduling' flag ON, Staff Scheduling renders v2 across all its regions (roster, assignments, staff), with the loading/empty/error states of each of its multiple queries served by StateBlock using i18n'd copy — the currently-hardcoded object-literal-prop strings ('Failed to load roster.', 'No shifts defined yet…', 'Staff Scheduling', etc.) replaced with t('scheduling.*') calls; flag OFF is byte-behaviorally identical to today"
    - "Every StateBlock/EmptyState/PageHeader string in the v2 branch is a t() call, not a hardcoded literal passed as an object-literal prop (the specific hidden-i18n pitfall for this section)"
    - "No query/mutation/filter/field added or removed; each region's StateBlock error.onRetry points at that region's existing query.refetch() (rosterQuery/assignmentsQuery/staffQuery already expose refetch and it is already used)"
  artifacts:
    - path: "apps/web/app/(dashboard)/scheduling/page.tsx"
      provides: "Scheduling flag-branched on isSectionRedesigned('scheduling', hotel); each region's StateBlock loading/empty/error already-present wiring kept but with hardcoded object-literal-prop strings converted to scheduling.* t() calls; v2 token chrome; legacy branch unchanged"
      contains: "isSectionRedesigned('scheduling'"
  key_links:
    - from: "apps/web/app/(dashboard)/scheduling/page.tsx"
      to: "isSectionRedesigned('scheduling', hotel)"
      via: "useHotelStore direct read (Phase-32 pattern)"
      pattern: "isSectionRedesigned\\('scheduling'"
    - from: "scheduling StateBlock/EmptyState props"
      to: "scheduling.* i18n keys"
      via: "replace hardcoded title=/message=/subtitle= string literals with t('scheduling.*'); onRetry = query.refetch()"
      pattern: "t\\('scheduling\\."
---

<objective>
Redesign Staff Scheduling — the largest single-file section (1388 LOC) with multiple react-queries — behind the `'scheduling'` flag. Scheduling is a special case: it ALREADY uses `StateBlock`/`EmptyState` in several places (scheduling/page.tsx ~lines 158, 705, 1032, 1141), so it looks partly done, but its copy is HARDCODED ENGLISH inside object-literal props (`error={{ message: 'Failed to load roster.' }}`, `EmptyState title="No shifts defined yet…"`, `title="Staff Scheduling"`) — the exact hidden-i18n pitfall CONTEXT flagged as in-scope. The core work here is: gate the whole page behind the flag, apply v2 token chrome, and convert every hardcoded StateBlock/EmptyState/PageHeader string to a `t('scheduling.*')` call, with each region's `onRetry` pointed at that region's existing query `refetch()`.

Purpose: SEC-01a for Scheduling; closes the "hidden object-literal-prop i18n" gap.
Output: `scheduling/page.tsx` flag-branched with all state/chrome strings i18n'd via the new scheduling.* keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-02-SUMMARY.md

# The file this plan edits (largest in the phase — 1388 LOC)
@apps/web/app/(dashboard)/scheduling/page.tsx

# Flag + state + primitives contracts (import-only; primitives.tsx/Button.tsx frozen)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag-branch Scheduling and convert hidden object-literal-prop strings to scheduling.* i18n</name>
  <files>apps/web/app/(dashboard)/scheduling/page.tsx</files>
  <action>
1. Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('scheduling', hotel)`. Keep today's JSX under the legacy branch, byte-behaviorally identical when `!v2`.
2. Find EVERY hardcoded string — grep this file for `title=`/`message=`/`subtitle=`/`label=` STRING LITERALS (not just `>text<`), since Scheduling's i18n dirt lives inside object-literal props passed to `StateBlock`/`EmptyState`/`PageHeader`. Known hits from research (verify live line numbers): `title="Staff Scheduling"` (~159-160), `error={{ message: 'Failed to load roster.' }}` and `EmptyState title="No shifts defined yet…"` (~705, 1032, 1141, 1297-1298). Convert each to the matching `t('scheduling.*')` key from 33-01 (e.g. `t('scheduling.pageTitle')`, `t('scheduling.roster.loadError')`, `t('scheduling.roster.empty')`, and the assignments/staff equivalents). Use the ACTUAL current English wording as the reference for which key it maps to.
3. Preserve the EXISTING StateBlock/EmptyState wiring structure — Scheduling already branches loading/empty/error per region; you are re-copying (i18n) and re-skinning (v2 tokens), NOT rewiring the state logic. For each region's error `onRetry`, use that region's existing query refetch: `rosterQuery.refetch()` / `assignmentsQuery.refetch()` / `staffQuery.refetch()` (already exposed and already used elsewhere in the file — do NOT add a new query). Loading → keep the region's existing skeleton/pulse (restyled to v2 shell), NOT StateBlock spinner, unless it was already a spinner.
4. Apply v2 token chrome (Phase-30/31 aliases) across the page's cards/grids/headers. Given the file's size, restyle the CHROME and STATES; do NOT refactor the page's logic, data flow, or component structure — presentation only. Do NOT edit frozen files, re-tint room-status tokens, or add/remove any query/mutation/field/filter.
5. Any deep create-form/field labels NOT inside a loading/empty/error state or the page chrome are lower priority — convert opportunistically only if trivial and already adjacent to restyled chrome; otherwise DEFER and note the pocket for the close-out deferred list. Do not let this 1388-LOC file's full i18n audit balloon the plan beyond the touched states + chrome.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('scheduling'` in scheduling/page.tsx; grep for `title="`/`message: '`/`message: "` string literals inside StateBlock/EmptyState/PageHeader in the v2 branch returns NO hardcoded English (all converted to t('scheduling.*')); grep confirms `rosterQuery.refetch`/`assignmentsQuery.refetch`/`staffQuery.refetch` used as onRetry; git status shows only scheduling/page.tsx changed; grep confirms no new useQuery/useMutation added</verify>
  <done>Scheduling v2 branch renders with v2 tokens and every StateBlock/EmptyState/PageHeader string i18n'd via scheduling.* (hidden object-literal-prop strings converted), each region's error onRetry wired to its existing query refetch; legacy branch unchanged flag-off; no query/field/filter change; gates green; any deferred form-i18n pocket noted</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- `isSectionRedesigned('scheduling')` branch; legacy unchanged flag-off.
- No hardcoded English remains in any StateBlock/EmptyState/PageHeader object-literal prop in the v2 branch — all `t('scheduling.*')`.
- Each region's StateBlock error onRetry = that region's existing query refetch; loading uses skeleton (not spinner) where it did before.
- Only scheduling/page.tsx changed; no frozen file edited; no locale-file edit; no query/mutation/field/filter added or removed.
</verification>

<success_criteria>
Staff Scheduling renders in the new visual system behind the 'scheduling' flag, with its multiple-query loading/empty/error states re-skinned and — critically — every hidden hardcoded object-literal-prop string converted to a scheduling.* i18n key, zero behavior/data/RBAC change, legacy branch byte-identical flag-off.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-06-SUMMARY.md` recording: the exact list of hardcoded strings found and the scheduling.* keys they were mapped to (especially the hidden object-literal-prop ones), the per-region onRetry wiring (rosterQuery/assignmentsQuery/staffQuery refetch), any deep form/field i18n pocket DEFERRED for the close-out list, and confirmation no frozen/locale file was touched and no query/field changed.
</output>
</content>
