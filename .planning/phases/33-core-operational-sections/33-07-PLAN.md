---
phase: 33-core-operational-sections
plan: 07
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04", "33-05", "33-06"]
files_modified: []
autonomous: true

# NOTE: verification-only plan (files_modified: []), mirroring 32-06. Runs the full standing web
# gate suite across the combined wave-2 changes, re-passes the Phase-30 Room-Board regression
# harness (these section changes must be inert to it — and Lost & Found shares the frozen
# LogFoundItemModal with RoomDetailDrawer, so this is the direct Success-Criterion-#4 check), and
# performs live flag-on/flag-off browser self-verification of all 9 sections (light+dark, EN+ES).
# Any real defect found is a blocking Rule-3 fix in the owning section file (which un-empties
# files_modified for that fix only) — otherwise this plan changes no source.

must_haves:
  truths:
    - "All standing web gates pass on the combined Phase-33 changes: type-check, check:frozen-files (zero frozen-hash / room-status drift — LogFoundItemModal byte-unchanged, allowlist still empty), check:contrast (dark-mode WCAG AA), check:i18n-parity (+ verify:i18n-gate), and build"
    - "The Phase-30 Room-Board regression harness re-passes at zero pixel drift — the 9 section redesigns did not perturb the frozen boards, and specifically the frozen LogFoundItemModal shared with RoomDetailDrawer is unchanged (Success Criterion #4)"
    - "With each section's flag ON for the test hotel, all 9 sections (Tasks, SOP, Logbook, Guest Requests, Lost & Found, Safety, Evidence, Programs, Scheduling) render the v2 redesign incl. their empty, loading (skeleton not spinner), and error states; with the flag OFF, each renders today's UI unchanged"
    - "Each section's empty/loading/error states render correctly in both light and dark mode and both EN and ES, with no console errors and no failed/forbidden API calls; the same-inputs->same-outputs network diff holds (no new/removed requests flag-on vs flag-off), confirming zero behavior/data/RBAC change"
  artifacts: []
  key_links:
    - from: "the 9 sections"
      to: "their nine section flags"
      via: "web_redesign_sections includes each of tasks/sop/logbook/guestRequests/lostFound/safety/evidence/programs/scheduling, toggled on the test hotel, verified live per section"
      pattern: "isSectionRedesigned"
---

<objective>
Close Phase 33: prove the five wave-2 content plans combine cleanly. Run the full standing web gate suite over all Phase-33 changes together, re-pass the Phase-30 Room-Board regression harness (the sections sit inside the shell that wraps the boards, and Lost & Found shares the frozen LogFoundItemModal with the excluded RoomDetailDrawer — so this is the literal Success-Criterion-#4 safety net), and live-verify all 9 sections in the browser with each section flag both ON and OFF, in light+dark and EN+ES, confirming the redesigned empty/loading/error states render, the network diff is inert (Success Criterion #3), and no regression to adjacent surfaces.

This satisfies the whole-phase view of all 4 Success Criteria: (1) 9 sections in the new visual system, (2) empty/loading/error redesigned not just happy path, (3) same-inputs->same-outputs + dark-mode contrast + EN/ES parity, (4) LogFoundItemModal unchanged + Room-Board regression gate passes.

Purpose: phase-close verification gate. No planned source changes (files_modified: []); a real defect found becomes a blocking targeted fix in the owning section file and is logged to .wolf/buglog.json.
Output: verification evidence in the summary; green gates + regression re-pass + live per-section confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-core-operational-sections/33-CONTEXT.md
@.planning/phases/33-core-operational-sections/33-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# Prior-plan summaries — what was built, which flag keys + i18n keys exist, any deferred pockets
@.planning/phases/33-core-operational-sections/33-01-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-02-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-03-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-04-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-05-SUMMARY.md
@.planning/phases/33-core-operational-sections/33-06-SUMMARY.md

# Regression harness + frozen manifest (confirm LogFoundItemModal hash + empty allowlist)
@apps/web/playwright.regression.config.ts
@apps/web/frozen-files.json
@apps/web/frozen-files-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full standing gate suite + Room-Board regression re-pass on combined changes</name>
  <files></files>
  <action>
From `apps/web`, run the full standing web gate suite over the combined Phase-33 tree and confirm all green:
- `npm run type-check`
- `npm run check:frozen-files` — MUST show zero frozen-hash drift AND zero room-status value drift. Explicitly confirm `LogFoundItemModal.tsx`'s hash matches frozen-files.json (`649c9516...a656c`) and `frozen-files-allowlist.json` is still `entries: []` (Success Criterion #4). If any wave-2 plan bumped it, verify the allowlist entry has a justification.
- `npm run check:contrast` — dark-mode WCAG AA over every new/extended token pairing across the 9 sections (including any `*-v2` chrome token a content plan added).
- `npm run check:i18n-parity` and `npm run verify:i18n-gate` — en/es parity intact after 33-01's key additions across all 9 namespaces.
- `npm run build`.
Then re-pass the Phase-30 Room-Board regression harness: `npx playwright test --config=playwright.regression.config.ts` (needs the regression fixture credentials / base URL per 30-06; if this session lacks live Supabase MCP write access to seed/flip the fixture tenant's flags, run the flag-OFF regression at minimum and flag the flag-ON regression re-run for the orchestrator, exactly as 32-06 did). Expect the harness to pass at zero pixel drift — these section changes are outside the 3 excluded boards, and the frozen LogFoundItemModal (shared by RoomDetailDrawer) is unchanged, so the boards must not perturb.
If any gate fails on a real defect, fix it in the owning section file (blocking Rule-3 fix — this un-empties files_modified for that fix only) and re-run until green; log the fix to `.wolf/buglog.json`.
  </action>
  <verify>All of type-check / check:frozen-files / check:contrast / check:i18n-parity / verify:i18n-gate / build exit 0; check:frozen-files explicitly confirms LogFoundItemModal unchanged + allowlist empty; Room-Board regression suite reports zero-drift (flag-off at minimum, flag-on if MCP access available); any defect found is fixed and re-verified green</verify>
  <done>Every standing web gate green on the combined Phase-33 changes; Room-Board regression re-passes at zero drift; LogFoundItemModal confirmed byte-unchanged and allowlist empty; zero frozen-file / room-status drift across all five content plans</done>
</task>

<task type="auto">
  <name>Task 2: Live flag-on/flag-off browser self-verification of all 9 sections (light+dark, EN+ES, network-inert)</name>
  <files></files>
  <action>
Per the mandatory Self-Verification Policy (CLAUDE.md): start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Enable the sections for the test hotel by adding all nine keys — `tasks`, `sop`, `logbook`, `guestRequests`, `lostFound`, `safety`, `evidence`, `programs`, `scheduling` — to its `web_redesign_sections` array (via `hotelsApi.update` PATCH /v1/hotels/{id} or directly in Supabase — same manual/DB toggle Phase 31/32 used; there is no settings-UI toggle). Use the GM test account in memory (it can reach all these sections); for role-gated views rely on the flag-branch code path being role-agnostic (each section reads the same `isSectionRedesigned('<key>', hotel)`) plus per-role login where credentials exist — document which sections were live-checked vs code-traced, as 32-06 did.

Verify each of the 9 sections:
- FLAG ON: the section renders the v2 redesign. Confirm its empty, loading (skeleton, NOT spinner — except where a section legitimately kept a spinner per its 33-0x summary), and error states render. Force an error (e.g. stop the API or point a query at a bad path in devtools) to confirm the StateBlock error + retry shows and retry re-fires the query. Confirm Guest Requests shows the net-new PageHeader (title + Active/History tabs + New Request action). Confirm Lost & Found's LogFoundItemModal still opens and behaves exactly as before.
- FLAG OFF: the section renders today's UI unchanged (legacy branch).
- LIGHT + DARK: toggle theme; confirm no contrast/legibility break (backs up check:contrast by eye). EN + ES: toggle locale; confirm the new namespaces render in both with no missing-key fallback (this is where a missed key or an en-only key would surface).
- NETWORK DIFF (Success Criterion #3): with devtools Network open, spot-check at least Tasks, Guest Requests, Lost & Found, and Scheduling that the SAME requests fire flag-on vs flag-off (no new/removed endpoints) — proving zero behavior/data change. Confirm no failed/forbidden (401/403) calls.
- No console errors on any section.
Record exactly what was live-verified vs code-traced, and any follow-up flagged for the orchestrator (e.g. flag-on regression not runnable this session, roles not seeded, deferred i18n pockets collected from the wave-2 summaries).
  </action>
  <verify>Screenshots/notes captured for all 9 sections flag-on and a flag-off spot check; empty/loading/error observed for each (with a forced error confirming retry re-fires); Guest Requests PageHeader + Lost & Found modal confirmed; light/dark + EN/ES confirmed with no missing-key fallback; network diff spot-checked inert on ≥4 sections; zero console errors and zero forbidden API calls in checked sections; any un-verifiable item explicitly flagged for the orchestrator</verify>
  <done>All 9 sections live-confirmed to render the v2 redesign flag-on and legacy flag-off, with empty/loading/error states in light+dark and EN+ES, network-inert (same requests flag-on vs off), no console errors or forbidden calls; LogFoundItemModal behavior confirmed unchanged; coverage gaps + deferred i18n pockets flagged precisely</done>
</task>

</tasks>

<verification>
- Full standing web gate suite green on combined changes (type-check, check:frozen-files, check:contrast, check:i18n-parity, verify:i18n-gate, build).
- check:frozen-files confirms LogFoundItemModal byte-unchanged + allowlist empty; Room-Board regression re-passes at zero drift (flag-off minimum; flag-on if MCP access, else flagged).
- All 9 sections live-verified flag-on (v2) and flag-off (legacy); empty/loading/error render (skeleton not spinner); Guest Requests net-new PageHeader confirmed; light+dark and EN+ES confirmed; network diff inert; no console errors / forbidden calls.
</verification>

<success_criteria>
Phase 33 verified end-to-end: all 4 Success Criteria confirmed against the running app and the gate suite — 9 sections in the new visual system with redesigned empty/loading/error states, same-inputs->same-outputs network behavior, dark-mode contrast and EN/ES parity green, and the frozen LogFoundItemModal + Room-Board regression gate provably intact. Any residual coverage gap or deferred i18n pocket is precisely flagged for the orchestrator's close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/33-core-operational-sections/33-07-SUMMARY.md` recording: the exact gate results, the regression outcome (and whether flag-on was runnable this session), a per-section table of live-verified vs code-traced, the state-verification evidence (incl. the forced-error/retry check and the network-diff spot-check), explicit confirmation LogFoundItemModal is unchanged + allowlist empty, any defect found+fixed (with buglog id), and a consolidated list of all deferred i18n pockets gathered from the wave-2 summaries plus any follow-up left for the orchestrator (mirroring 32-06's close-out format).
</output>
</content>
