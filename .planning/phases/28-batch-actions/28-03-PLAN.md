---
phase: 28-batch-actions
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps/web/lib/api/housekeeping.ts
  - apps/web/components/housekeeping/PredictionPanel.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "A supervisor/GM sees a selection checkbox on each actionable HIGH-risk room row (canAct rows only); MEDIUM-risk rows have no checkbox and can never be selected"
    - "Selecting >=1 room reveals a contextual action bar showing the selected count, a Batch reassign button, a Batch acknowledge button, and a Deselect-all control — not a modal dialog"
    - "Confirming a batch action calls the batch endpoint once and then renders a per-room result summary (e.g. '3 reassigned, 1 escalated: no capacity'), not a single pass/fail toast and not N toasts"
    - "After the board refreshes (onActionComplete), the selection is cleared so it can't reference rows whose risk level changed"
    - "A non-supervisor (no canAssignRooms) sees no checkboxes and no action bar"
  artifacts:
    - path: "apps/web/lib/api/housekeeping.ts"
      provides: "batchReassignAtRiskRooms + batchAcknowledgeAtRiskRooms typed against Plan 28-01's response shapes"
      contains: "batch-reassign"
    - path: "apps/web/components/housekeeping/PredictionPanel.tsx"
      provides: "Checkbox selection state + contextual action bar with confirm step + per-room result summary"
      contains: "selected"
    - path: "apps/web/i18n/locales/en.ts"
      provides: "New housekeeping.predictionPanel.* batch keys"
      contains: "batchReassign"
  key_links:
    - from: "apps/web/components/housekeeping/PredictionPanel.tsx"
      to: "housekeepingApi.batchReassignAtRiskRooms / batchAcknowledgeAtRiskRooms"
      via: "action-bar confirm -> mutation call with [...selected]"
      pattern: "batch(Reassign|Acknowledge)AtRiskRooms"
    - from: "apps/web/lib/api/housekeeping.ts"
      to: "/v1/housekeeping/room-readiness/batch-reassign"
      via: "POST with { room_ids }"
      pattern: "batch-reassign"
---

<objective>
Add checkbox multi-select and a contextual batch action bar to `PredictionPanel.tsx`, wired to two new typed API client methods, so a supervisor/GM can batch-reassign or batch-acknowledge HIGH-risk rooms and see a per-room outcome.

Purpose: AI-09/AI-10 frontend. Stay visually consistent with `PredictionPanel.tsx`'s existing inline-confirm-subrow model (idle -> confirm-X -> result) — the action bar's own confirm step is that same pattern scaled to N rooms. No modal (rules out `BulkArchiveModal.tsx`'s chrome).
Output: 2 new API client methods, selection UI + action bar in the panel, new EN/ES i18n keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-batch-actions/28-CONTEXT.md
@.planning/phases/28-batch-actions/28-RESEARCH.md
@.planning/phases/28-batch-actions/28-01-SUMMARY.md

@apps/web/components/housekeeping/PredictionPanel.tsx
@apps/web/lib/api/housekeeping.ts
@apps/web/i18n/locales/en.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch API client methods + EN/ES i18n keys</name>
  <files>apps/web/lib/api/housekeeping.ts, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
In `apps/web/lib/api/housekeeping.ts` (mirror the existing single-item methods at lines 126-141 and their typed discriminated-union response types), add:
- `batchReassignAtRiskRooms(roomIds: string[])` -> `POST /housekeeping/room-readiness/batch-reassign` with body `{ room_ids: roomIds }`.
- `batchAcknowledgeAtRiskRooms(roomIds: string[])` -> `POST /housekeeping/room-readiness/batch-acknowledge` with body `{ room_ids: roomIds }`.

Type the responses against Plan 28-01's FINAL shapes (read `28-01-SUMMARY.md` for the exact fields — do NOT guess). Expected shape: `{ data: { results: BatchRoomResult[]; succeeded: number; failed: number } }`, where a reassign `BatchRoomResult` is a discriminated union on `action`: `{ room_id; action: 'reassigned'; housekeeper_id } | { room_id; action: 'escalated'; reason: 'no_eligible_housekeeper' } | { room_id; action: 'error'; status: number; detail: string }`, and acknowledge results are `{ room_id; action: 'acknowledged' | 'already_acknowledged' } | { room_id; action: 'error'; ... }`. Follow the existing inline-union convention in this file.

In `apps/web/i18n/locales/en.ts` and `es.ts`, extend the existing `housekeeping.predictionPanel.*` namespace (en.ts:431-461) with batch keys — follow the existing flat present-tense convention. Suggested keys (exact naming is Claude's discretion): `batchReassign`, `batchAcknowledge`, `selectAll`, `deselectAll`, `selectedCount` ('{{count}} selected'), `confirmBatchReassign` ('Reassign {{count}} rooms to least-loaded housekeeper?'), `confirmBatchAcknowledge` ('Acknowledge {{count}} rooms?'), `batchResultSummary` ('{{succeeded}} succeeded, {{failed}} need attention'), and reuse the existing per-outcome strings (`reassignedTo`, `escalatedNoCapacity`, `acknowledged`, `alreadyAcknowledged`, `actionFailed`) for the per-room result lines. Confirm EN/ES key parity (same key set in both files).
  </action>
  <verify>`cd apps/web && npm run type-check` clean for housekeeping.ts; `grep -c "batchReassign" i18n/locales/en.ts i18n/locales/es.ts` returns 1 for each (parity).</verify>
  <done>Two typed batch methods exist matching 28-01's response shapes; new batch i18n keys present in both en.ts and es.ts at parity; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add checkbox selection + contextual action bar to PredictionPanel.tsx</name>
  <files>apps/web/components/housekeeping/PredictionPanel.tsx</files>
  <action>
Selection state lives at the `PredictionPanel` level (lines 253-336), above the `.map()` at line 323 where "N selected"/"select all" naturally sit. Use `useState<Set<string>>` for selected room_ids (adapt the state pattern from `BulkArchiveModal.tsx:22,80-87` — the Set + toggle helper — but NOT its modal chrome).

Row checkbox: render a checkbox in `PredictionRow` ONLY when `canAct` is true (`canAct = canAssignRooms && prediction.risk_level === 'HIGH'`, PredictionPanel.tsx:89). MEDIUM-risk rows and non-supervisor views get NO checkbox — this exactly mirrors the single-item action gate; do not introduce a separate selection gate. Wire the checkbox to toggle the room_id in the panel's selected Set (pass a toggle callback + `isSelected` down as props).

Contextual action bar: render once `selected.size >= 1`, inside the panel (top or bottom of the expanded list — Claude's discretion, follow the existing layout with least structural change; inline, consistent with the existing confirm-subrow, NOT a `fixed inset-0` overlay). It shows: `selectedCount`, a `Select all` / `Deselect all` toggle (scoped to the currently-rendered actionable rows only — the `canAct` HIGH rows in `atRiskRooms`, NOT hotel-wide), a `Batch reassign` button, and a `Batch acknowledge` button. `Select all` selects only the currently-rendered `canAct` room_ids.

Confirm step: tapping `Batch reassign` / `Batch acknowledge` switches the bar into a confirm sub-state (local `batchMode: 'idle' | 'confirm-reassign' | 'confirm-acknowledge'`, mirroring `PredictionRow`'s own mode machine) showing `confirmBatchReassign`/`confirmBatchAcknowledge` with the count and a confirm/cancel pair. Confirming fires a `useMutation` calling `housekeepingApi.batchReassignAtRiskRooms([...selected])` / `batchAcknowledgeAtRiskRooms([...selected])`.

Result rendering: on success, render a per-room result summary/list from `data.results` (e.g. a compact list "Room 214 — reassigned", "Room 217 — escalated: no capacity", plus the `batchResultSummary` count line) using the existing per-outcome i18n strings — NOT a single toast, NOT N toasts.

Selection reset: on `onActionComplete` (the existing board-refresh callback prop already passed to the panel — see 27-03's `onActionComplete` wiring), clear the selected Set and reset `batchMode` to idle, so a post-refresh board never has stale selections referencing rows whose risk changed (CONTEXT.md Claude's-discretion: recommend yes -> do it).

Non-regression: the existing single-row Reassign/Escalate/Acknowledge inline buttons and their confirm-subrows must keep working unchanged; the batch bar is additive.
  </action>
  <verify>`cd apps/web && npm run type-check && npm run lint` clean for PredictionPanel.tsx; visually confirm in Task 3.</verify>
  <done>HIGH-risk actionable rows show a checkbox; selecting rows reveals an inline action bar with count, select/deselect-all, batch reassign, batch acknowledge; confirm step then per-room result summary; selection clears on refresh; single-row actions still work.</done>
</task>

<task type="auto">
  <name>Task 3: Live self-verification on localhost</name>
  <files>apps/web/components/housekeeping/PredictionPanel.tsx</files>
  <action>
Per CLAUDE.md Self-Verification Policy: start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running (kill any stale uvicorn/`multiprocessing.spawn` worker on the API port first — 27-03 and 06-05 both hit stale-server gotchas where `--reload` missed new routes; confirm `GET /openapi.json` lists `batch-reassign`/`batch-acknowledge` before testing). Log in as the GM/supervisor test account (see memory reference_test_account) and navigate to the housekeeping page's PredictionPanel.

Exercise: expand the panel; confirm checkboxes appear only on HIGH-risk rows and only for a supervisor/GM; select 2+ rooms; confirm the action bar shows the right count; run Batch acknowledge through its confirm step; confirm a per-room result summary renders and the board refreshes with selection cleared. Check console for zero errors and the Network tab for a single `batch-acknowledge` call (not N single calls).

DATA FALLBACK (documented precedent, 27-03): this tenant has historically had 0 HIGH-risk rooms (`GET /v1/housekeeping/predictions` -> `at_risk_count: 0`). If no HIGH-risk rooms exist at test time, you cannot click through to a real batch round-trip. In that case: (a) prove API reachability by calling `POST /v1/housekeeping/room-readiness/batch-acknowledge` directly with a real room_id from the board and confirm the endpoint returns the router's real business-logic response (not a route-not-found 404); (b) prove the checkbox/action-bar gating by code inspection + a non-HIGH board (verify no checkboxes render); (c) rely on Plan 28-01's automated tests for the full success/failure matrix. Document exactly which paths were live-verified vs. covered-by-test.
  </action>
  <verify>Screenshot or logged confirmation that checkboxes + action bar render correctly for a supervisor and are absent for MEDIUM rows; a batch call reaches the API (live click-through OR direct-call fallback documented); zero console errors; single batched network call.</verify>
  <done>Batch selection UI verified working on localhost (or API-reachability + gating verified with the documented data fallback); no console errors; no regression to single-row actions.</done>
</task>

</tasks>

<verification>
- `npm run type-check` and `npm run lint` clean across the three changed web files.
- EN/ES batch keys at parity.
- Live: checkboxes only on HIGH actionable rows, inline action bar (no modal), per-room result summary, selection clears on refresh, single batched API call, zero console errors.
</verification>

<success_criteria>
Supervisors/GMs can multi-select HIGH-risk rooms via checkboxes, trigger a confirmed batch reassign or acknowledge from an inline contextual action bar, and see a per-room outcome summary; MEDIUM rows and non-supervisors have no selection UI; selection resets on refresh; single-row actions remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/28-batch-actions/28-03-SUMMARY.md` documenting what was live-verified vs. covered-by-test (data fallback), the final i18n keys added, and any deviations.
</output>
