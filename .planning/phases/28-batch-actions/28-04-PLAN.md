---
phase: 28-batch-actions
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - apps/web/lib/api/engineering.ts
  - apps/web/app/(dashboard)/engineering/predictions/page.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "An engineer/GM sees a selection checkbox on each actionable (canManage && !is_acknowledged) prediction card; already-acknowledged cards and non-manager views have no checkbox"
    - "chief_engineer does NOT see the batch checkbox/action bar (mirrors canManage, which excludes chief_engineer)"
    - "Selecting >=1 prediction reveals a contextual action bar with the selected count, a Batch acknowledge button, and a Deselect-all control — not a modal"
    - "Confirming batch acknowledge calls the batch endpoint once and renders a per-prediction result summary (acknowledged / not found / error), then invalidates the failure-predictions query keys"
    - "Selection clears after the list refreshes"
  artifacts:
    - path: "apps/web/lib/api/engineering.ts"
      provides: "batchAcknowledgeFailurePredictions typed against Plan 28-02's response shape"
      contains: "batch-acknowledge"
    - path: "apps/web/app/(dashboard)/engineering/predictions/page.tsx"
      provides: "Checkbox selection + contextual action bar + confirm step + per-prediction result summary"
      contains: "selected"
    - path: "apps/web/i18n/locales/en.ts"
      provides: "New engineering batch i18n keys"
      contains: "batchAcknowledge"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/predictions/page.tsx"
      to: "engineeringApi.batchAcknowledgeFailurePredictions"
      via: "action-bar confirm -> mutation with [...selected]"
      pattern: "batchAcknowledgeFailurePredictions"
    - from: "apps/web/lib/api/engineering.ts"
      to: "/v1/assets/failure-predictions/batch-acknowledge"
      via: "POST with { prediction_ids }"
      pattern: "batch-acknowledge"
---

<objective>
Add checkbox multi-select and a contextual batch action bar to the engineering predictions page, wired to a new typed API client method, so an engineer/GM can batch-acknowledge asset-failure predictions and see a per-prediction outcome.

Purpose: AI-11 frontend. This page uses a card grid (`PredictionCard`), not the compact row list of the housekeeping panel, so adapt the selection pattern to cards. Stay inline/contextual (no modal). Selection gate mirrors the current single-item Acknowledge gate exactly — `canManage && !prediction.is_acknowledged` (page.tsx:293) — which means chief_engineer (only `canAuthorize`) sees no batch UI, and there is NO new HIGH-only restriction (LOCKED: mirror the single-item unacknowledged-only gate, don't diverge).
Output: 1 new API client method, selection UI + action bar on the page, new EN/ES i18n keys.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-batch-actions/28-CONTEXT.md
@.planning/phases/28-batch-actions/28-RESEARCH.md
@.planning/phases/28-batch-actions/28-02-SUMMARY.md

@apps/web/app/(dashboard)/engineering/predictions/page.tsx
@apps/web/lib/api/engineering.ts
@apps/web/i18n/locales/en.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch API client method + EN/ES i18n keys</name>
  <files>apps/web/lib/api/engineering.ts, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
In `apps/web/lib/api/engineering.ts` (mirror `acknowledgeFailurePrediction` at lines 257-260), add `batchAcknowledgeFailurePredictions(predictionIds: string[])` -> `POST /assets/failure-predictions/batch-acknowledge` with body `{ prediction_ids: predictionIds }`.

Type the response against Plan 28-02's FINAL shape (read `28-02-SUMMARY.md` — do NOT guess). Expected: `{ data: { results: BatchPredictionResult[]; succeeded: number; failed: number } }` where `BatchPredictionResult` is a discriminated union on `action`: `{ prediction_id; action: 'acknowledged' } | { prediction_id; action: 'not_found' } | { prediction_id; action: 'error'; status: number; detail: string }`.

In `apps/web/i18n/locales/en.ts` and `es.ts`, add batch keys under the sibling engineering namespace already used on this page (`engineering.predictionsPage.*` / `engineering.failurePrediction.*` — used at page.tsx:479-483, 354). Suggested keys (naming is Claude's discretion, follow the existing convention there): `batchAcknowledge`, `selectAll`, `deselectAll`, `selectedCount` ('{{count}} selected'), `confirmBatchAcknowledge` ('Acknowledge {{count}} predictions?'), `batchResultSummary` ('{{succeeded}} acknowledged, {{failed}} need attention'), `resultNotFound` ('Not found'). Reuse existing acknowledge/error strings for per-item lines where they exist. Confirm EN/ES key parity.
  </action>
  <verify>`cd apps/web && npm run type-check` clean for engineering.ts; batch keys present in both en.ts and es.ts at parity.</verify>
  <done>Typed `batchAcknowledgeFailurePredictions` exists matching 28-02's response shape; new engineering batch i18n keys at EN/ES parity; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add checkbox selection + contextual action bar to the predictions page</name>
  <files>apps/web/app/(dashboard)/engineering/predictions/page.tsx</files>
  <action>
Add `useState<Set<string>>` for selected prediction ids at the page level (near the existing filter state). Scope selection to the currently-rendered filtered list (`riskFilter`/`statusFilter`-filtered, page.tsx:421-429) — matching CONTEXT.md's "scoped to the currently-rendered list" rule.

Card checkbox: render a checkbox on a `PredictionCard` ONLY when its Acknowledge button would show — i.e. `canManage && !prediction.is_acknowledged` (page.tsx:293). Do NOT add a HIGH-only restriction (LOCKED: mirror the single-item unacknowledged-only gate; the asset single-item UI has no risk-level gate today). This automatically means chief_engineer (who lacks `canManage`) sees no checkboxes. Pass a toggle callback + `isSelected` into `PredictionCard`.

Contextual action bar: render once `selected.size >= 1`, inline (Claude's discretion on placement — a sticky bar above/below the grid; NOT a modal). Shows `selectedCount`, `Select all` / `Deselect all` (scoped to the currently-rendered `canManage && !is_acknowledged` cards only), and a `Batch acknowledge` button. `Select all` selects only the currently-rendered actionable prediction ids.

Confirm step: `Batch acknowledge` -> a confirm sub-state showing `confirmBatchAcknowledge` with the count + confirm/cancel. Confirm fires a `useMutation` calling `engineeringApi.batchAcknowledgeFailurePredictions([...selected])`; on success invalidate BOTH `['failure-predictions-history']` and `['failure-predictions']` (same keys as the existing single-item `acknowledgeMutation`, page.tsx:384-390).

Result rendering: render a per-prediction result summary from `data.results` (acknowledged / not found / error) plus `batchResultSummary` — NOT a single toast, NOT N toasts. Clear the selected Set on success (after the query invalidation refresh) so stale selections don't persist.

Non-regression: the existing single-card Acknowledge / Create Work Order buttons, the `canAuthorize` path, and the risk/status filters must keep working unchanged; the batch bar is additive.
  </action>
  <verify>`cd apps/web && npm run type-check && npm run lint` clean for page.tsx.</verify>
  <done>Actionable (canManage && !is_acknowledged) cards show a checkbox; selecting reveals an inline action bar with count, select/deselect-all, batch acknowledge; confirm step then per-prediction result summary; both query keys invalidate; selection clears; single-card actions and chief_engineer's canAuthorize path unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Live self-verification on localhost</name>
  <files>apps/web/app/(dashboard)/engineering/predictions/page.tsx</files>
  <action>
Per CLAUDE.md Self-Verification Policy: ensure `npm run dev:web` (:3000) and `npm run dev:api` (:8000) are running fresh (kill stale API workers; confirm `GET /openapi.json` lists `/v1/assets/failure-predictions/batch-acknowledge` before testing). Log in as the GM/engineer test account and navigate to `/engineering/predictions`.

Exercise: confirm checkboxes appear only on unacknowledged cards for a manager; select 2+ predictions; confirm the action bar count; run Batch acknowledge through its confirm step; confirm a per-prediction result summary renders, the list refreshes (acknowledged cards drop their checkbox), and selection clears. Check console for zero errors and confirm a single `batch-acknowledge` network call.

DATA FALLBACK (27-03 precedent): if this tenant has no unacknowledged predictions at test time, prove API reachability by calling `POST /v1/assets/failure-predictions/batch-acknowledge` directly with a real prediction_id (or a fabricated id to confirm the `not_found` per-item path returns the router's real response, not a route-not-found 404), verify checkbox gating by code inspection, and rely on Plan 28-02's automated tests for the full matrix. Also spot-check that a chief_engineer account (if available) sees no batch UI; if no such account exists, note it as a deferral backed by 28-02's automated chief_engineer-403 test (same second-account limitation as prior phases). Document what was live-verified vs. covered-by-test.
  </action>
  <verify>Logged confirmation that checkboxes + inline action bar render for a manager and are absent on acknowledged cards; a batch call reaches the API (live or documented direct-call fallback); zero console errors; single batched network call.</verify>
  <done>Batch acknowledge UI verified on localhost (or API-reachability + gating verified via the documented fallback); no console errors; no regression to single-card actions or filters.</done>
</task>

</tasks>

<verification>
- `npm run type-check` and `npm run lint` clean across the three changed web files.
- EN/ES engineering batch keys at parity.
- Live: checkboxes only on unacknowledged manager cards (no HIGH-only restriction, no chief_engineer batch UI), inline action bar (no modal), per-prediction result summary, both query keys invalidated, selection clears, single batched API call, zero console errors.
</verification>

<success_criteria>
Engineers/GMs can multi-select unacknowledged asset-failure predictions via checkboxes, trigger a confirmed batch acknowledge from an inline contextual action bar, and see a per-prediction outcome summary; acknowledged cards, chief_engineer, and non-managers have no selection UI; selection resets on refresh; single-card actions remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/28-batch-actions/28-04-SUMMARY.md` documenting live-verified vs. covered-by-test paths, the final i18n keys, and any deviations. This is the last plan in Phase 28.
</output>
