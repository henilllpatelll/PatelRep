---
phase: 10-dark-mode-accessibility-qa
plan: 08
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/app/(app)/my-rooms/index.tsx
  - apps/mobile/app/(app)/my-rooms/[roomId].tsx
  - apps/mobile/app/(app)/work-orders/[woId].tsx
  - apps/mobile/app/(app)/scheduling/index.tsx
  - apps/mobile/app/(app)/tasks/index.tsx
  - apps/mobile/__tests__/components/OperationsSemanticOnColor.test.ts
autonomous: true
requirements: [DARK-02, DARK-05]
must_haves:
  truths:
    - "Room, work-order, scheduling, and task controls never assume white text/icon over a semantic color that changes between themes"
    - "The known light-only room warning fill and owned primary, status, and AI foregrounds resolve through contrast-tested tokens"
    - "Owned icon-only and status controls expose meaningful labels/state and interactive targets are at least 44pt"
    - "At 200% text scale, owned action rows use minimum rather than fixed heights and do not hide required task-completion actions"
    - "Offline queueing, mutations, confirmations, routing, payloads, and RBAC behavior remain unchanged"
  artifacts:
    - path: "apps/mobile/__tests__/components/OperationsSemanticOnColor.test.ts"
      provides: "Source-level guard against theme-unsafe raw on-colors and light-only fills in audited operations files"
    - path: "apps/mobile/app/(app)/my-rooms/[roomId].tsx"
      provides: "Theme-safe room warning and action states"
    - path: "apps/mobile/app/(app)/tasks/index.tsx"
      provides: "Theme-safe AI action states"
  key_links:
    - from: "owned operations controls"
      to: "apps/mobile/components/shared/tokens.ts"
      via: "theme.onPrimary/onDestructive/onAi and semantic soft fills"
      pattern: "onPrimary|onDestructive|onAi"
    - from: "apps/mobile/__tests__/components/OperationsSemanticOnColor.test.ts"
      to: "the five owned operations runtime files"
      via: "explicit audited source inventory"
      pattern: "#fff|#FFFFFF|#FFF7F7"
---

<objective>
Burn down known hardcoded on-colors, light-only fills, and large-text/target risks in room, work-order, scheduling, and task workflows.

Purpose: Token AA tests only protect callers that use semantic roles. These five audited files still contain raw white or a light-only warning fill on dynamic primary, status, or AI backgrounds.
Output: A bounded source guard and presentation-only fixes for operations controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/08-floor-role-rollout/08-VERIFICATION.md
@.planning/phases/09-remaining-screens-rollout/09-VERIFICATION.md
@apps/mobile/components/shared/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the operations source and target contract</name>
  <files>apps/mobile/__tests__/components/OperationsSemanticOnColor.test.ts</files>
  <action>
Create a test with an explicit list of the five owned room, work-order, scheduling, and task runtime files. Make it fail on raw `#fff/#FFFFFF` foregrounds and the `#FFF7F7` light-only warning fill identified by research/current audit. Add targeted source assertions that each file resolves semantic foregrounds and fills through the current theme.

Alongside the automated guard, record a per-file checklist in the test comments for interactive `Pressable`/`TouchableOpacity` controls: >=44x44 or minHeight 44, accessible label/role, selected/disabled/busy state where applicable, and no fixed-height text container that hides a required action at 200%. Do not create a broad regex that misclassifies decorative dots, avatars, or passive status marks as touch targets.

Run the test and retain the RED output before production edits.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/OperationsSemanticOnColor.test.ts --runInBand</verify>
  <done>The explicit operations source contract fails on the known unsafe foreground/fill sites and documents the human-reviewed target inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Fix room, work-order, scheduling, and task semantic foregrounds and targets</name>
  <files>apps/mobile/app/(app)/my-rooms/index.tsx, apps/mobile/app/(app)/my-rooms/[roomId].tsx, apps/mobile/app/(app)/work-orders/[woId].tsx, apps/mobile/app/(app)/scheduling/index.tsx, apps/mobile/app/(app)/tasks/index.tsx</files>
  <action>
Apply the Task 1 semantic on-color and target checklist. Specifically replace My Rooms offline red/white with banner semantic roles; replace the room-detail `#FFF7F7` critical warning with the active theme dirty-soft/border pairing; and route work-order, schedule, task primary, AI, and status action foregrounds through `onPrimary`, `onAi`, or the exact tested on-role for that fill.

Raise only interactive targets below 44 and add missing accessibility labels, roles, and states without changing visual status meaning. Allow labels and actions to grow at 200% and Spanish width; do not remove necessary `numberOfLines` from non-action metadata unless it blocks task completion.

Preserve offline enqueue/reconnect behavior, all API calls and payloads, role guards, filters, list/detail routes, native blocking confirmations, Toast outcomes, and test IDs. Run the narrowest existing screen test after each sub-group and fix behavior regressions before moving on.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/components/OperationsSemanticOnColor.test.ts __tests__/screens/MyRoomsScreen.test.tsx __tests__/screens/RoomDetail.test.tsx __tests__/screens/WorkOrderDetail.test.tsx __tests__/screens/TasksVariationA.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>All five audited operations files pass the semantic source guard and focused behavior tests with no workflow, RBAC, data, or offline changes.</done>
</task>

</tasks>

<verification>
- Operations semantic source guard and all named focused screen suites pass.
- `npm run type-check` and `npm run lint` pass.
- No raw audited white or light-only foreground/fill remains in the five files.
- Diff review confirms only styles, accessibility props, and layout minima changed; handlers, endpoints, payloads, routing, RBAC, offline queueing, and confirmations did not.
</verification>

<success_criteria>
- Operations actions remain readable in both modes and at 200% text scale.
- Interactive targets and semantics meet the locked accessibility bar.
- Existing bilingual, offline, and role behavior stays intact.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-08-SUMMARY.md`.
</output>
