---
phase: 10-dark-mode-accessibility-qa
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/lib/theme/navigationTheme.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/(app)/_layout.tsx
  - apps/mobile/__tests__/lib/theme/navigationTheme.test.ts
  - apps/mobile/__tests__/lib/theme/AppChrome.test.ts
autonomous: true
requirements: [DARK-03, DARK-05]
must_haves:
  truths:
    - "The saved/effective appearance is hydrated before the first navigator frame or splash dismissal"
    - "Expo Router navigation semantics, tabs, headers, status bar, and Copilot FAB resolve from the same effective theme mode"
    - "Light and dark chrome switch immediately with no transition and no auto/light flash"
    - "The existing per-role tab allow-list, hidden routes, redirects, notification badges, and deep links are unchanged"
  artifacts:
    - path: "apps/mobile/lib/theme/navigationTheme.ts"
      provides: "React Navigation theme mapped from PatelRep semantic tokens"
      exports: ["getNavigationTheme"]
    - path: "apps/mobile/app/_layout.tsx"
      provides: "Hydration-gated splash, navigation provider, and explicit StatusBar"
    - path: "apps/mobile/app/(app)/_layout.tsx"
      provides: "Theme-reactive tab/header/FAB chrome"
    - path: "apps/mobile/__tests__/lib/theme/AppChrome.test.ts"
      provides: "First-frame and static-chrome wiring regression contract"
  key_links:
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/lib/theme/ThemeProvider.tsx"
      via: "useAppearancePreference isHydrated gate"
      pattern: "isHydrated"
    - from: "apps/mobile/app/_layout.tsx"
      to: "apps/mobile/lib/theme/navigationTheme.ts"
      via: "NavigationThemeProvider value"
      pattern: "getNavigationTheme"
    - from: "apps/mobile/app/(app)/_layout.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "tab/header/FAB render-time tokens"
      pattern: "useTheme"
---

<objective>
Wire the hydrated effective appearance into every native navigation boundary without disturbing routing or role behavior.

Purpose: DARK-03 is not satisfied by themed screen content alone. The splash, navigation container, status bar, tabs, headers, and FAB must all agree on the first visible frame.
Output: A tested navigation-theme mapper and fully theme-reactive Expo Router root/app layouts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-VERIFICATION.md
@apps/mobile/app/_layout.tsx
@apps/mobile/app/(app)/_layout.tsx
@apps/mobile/lib/navigation/roleTabs.ts
@apps/mobile/lib/theme/ThemeProvider.tsx
@apps/mobile/components/shared/tokens.ts

<interfaces>
The React Navigation object must map PatelRep tokens into:
```typescript
{
  dark: mode === "dark",
  colors: { primary, background, card, text, border, notification },
  fonts: ...
}
```
Use the Expo Router/React Navigation provider already exposed by the installed router stack; alias it `NavigationThemeProvider` to avoid colliding with PatelRep's provider. Add no navigation dependency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test and implement the navigation theme adapter</name>
  <files>apps/mobile/lib/theme/navigationTheme.ts, apps/mobile/__tests__/lib/theme/navigationTheme.test.ts</files>
  <action>
Write the focused tests first. Assert light and dark objects set the correct `dark` boolean and map `primary`, app background, deep shell/card, text, border, and notification colors from the corresponding `getThemeTokens(mode)` object. Assert the two results are not the same object/value snapshot and contain no static `C` reads.

Implement one small adapter with the complete React Navigation theme shape required by the installed type. Map navigation card/chrome to the theme's deeper shell, not content `surface`; preserve native default font families rather than inventing typography. Do not pass PatelRep's token object directly because its shape is not a React Navigation theme.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/navigationTheme.test.ts --runInBand; npm run type-check</verify>
  <done>The adapter produces typed, distinct light/dark navigation themes from PatelRep tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the first frame and theme the root navigation/status bar</name>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/__tests__/lib/theme/AppChrome.test.ts</files>
  <action>
Create the root/chrome regression test before changing the layout. It must prove the source no longer uses `StatusBar style="auto"`, splash hiding depends on both auth resolution and appearance hydration, the navigation theme provider receives the effective mode mapping, status-bar content style/background derive from the same mode/shell, and `animated={false}` is explicit.

Refactor root into an outer PatelRep `ThemeProvider` and an inner consumer so `useAppearancePreference()` is legal where splash and chrome are controlled. Preserve all five existing auth, timeout, NetInfo/sync, and notification effects and their dependency behavior. Hide the splash only when `!isLoading && isHydrated`; before hydration do not expose Stack/StatusBar navigation content. AsyncStorage failure already settles hydration in 10-01, so do not add another timeout or error route.

Wrap Stack in aliased `NavigationThemeProvider`, use `getNavigationTheme(mode)`, and render `StatusBar` with light content in dark mode/dark content in light mode, themed shell background, and `animated={false}`. Theme changes themselves must not use LayoutAnimation, Animated, crossfade, or navigation transition overrides.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/AppChrome.test.ts __tests__/lib/theme/navigationTheme.test.ts --runInBand; npm run type-check</verify>
  <done>No Stack/status bar is exposed before theme hydration, and the first visible root frame uses one effective mode for navigation and status chrome.</done>
</task>

<task type="auto">
  <name>Task 3: Theme role tabs, headers, and the Copilot FAB</name>
  <files>apps/mobile/app/(app)/_layout.tsx, apps/mobile/__tests__/lib/theme/AppChrome.test.ts</files>
  <action>
Replace every `C.shell*`, `C.paper`, `C.ink*`, and `C.ai` chrome value with render-time `useTheme()` values. Move FAB/background/shadow colors out of the static StyleSheet; use semantic `theme.ai`/on-color tokens. Keep the tab shell deeper than content, with a visible border; do not rely on shadow alone.

Add translated tab accessibility labels from each existing `titleKey`, keep label/icon order, and localize the FAB label through existing `tabs.copilot` rather than hardcoded English. Preserve the exact `getTabsForRole`, `ALL_ROLE_TAB_ROUTES`, `HIDDEN_APP_ROUTES`, redirect, badge, notification listener, route names, and FAB destination. Do not fix the duplicate engineer case or change RBAC/routing in this presentation phase.

Extend the chrome test to assert no runtime `C.*` remains in either layout, tab options still derive from `visibleTabs`, hidden routes still use `href:null`, FAB target remains `/(app)/copilot`, and all chrome values come from `theme`.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/lib/theme/AppChrome.test.ts __tests__/lib/roleTabs.test.ts --runInBand; npm run type-check; npm run lint</verify>
  <done>Tabs/headers/FAB are theme-reactive and accessible while every role/RBAC/navigation contract stays byte-for-byte equivalent in behavior.</done>
</task>

</tasks>

<verification>
- Root/navigation/chrome and role-tab tests pass.
- `npm run type-check` and `npm run lint` pass.
- `rg -n "\\bC\\.|StatusBar style=\"auto\"" apps/mobile/app/_layout.tsx apps/mobile/app/(app)/_layout.tsx` returns no runtime match.
- Splash hide is gated by both auth and appearance hydration.
- No route name, tab array, hidden-route set, redirect, deep-link handler, or RBAC branch changes.
</verification>

<success_criteria>
- Navigator chrome matches effective light/dark mode on its first visible frame.
- Live System or explicit mode changes update all chrome immediately with no theme animation.
- Role-gated navigation behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-03-SUMMARY.md`.
</output>
