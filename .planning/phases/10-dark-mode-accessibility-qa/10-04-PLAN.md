---
phase: 10-dark-mode-accessibility-qa
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - apps/mobile/app/(app)/profile/index.tsx
  - apps/mobile/app/(auth)/login.tsx
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/__tests__/screens/ProfileHandoff.test.tsx
  - apps/mobile/__tests__/screens/LoginTheme.test.tsx
autonomous: true
requirements: [DARK-01, DARK-02, DARK-05]
must_haves:
  truths:
    - "Profile shows a directly visible full-width System/Light/Dark segmented control with System selected by default"
    - "Each appearance option is translated, at least 44pt, announced as a radio with selected state, and applies immediately"
    - "Profile language segments and bell action also meet 44pt and remain usable in EN/ES at 200% text scale"
    - "The pre-auth login surface follows the hydrated theme and exposes no English fallback in Spanish except PatelRep/property names"
    - "Language persistence, profile fetch, sync, sign-out confirmation, and auth requests are unchanged"
  artifacts:
    - path: "apps/mobile/app/(app)/profile/index.tsx"
      provides: "Accessible appearance preference control"
    - path: "apps/mobile/app/(auth)/login.tsx"
      provides: "Theme-reactive bilingual auth surface"
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "English appearance/auth labels"
    - path: "apps/mobile/i18n/locales/es.json"
      provides: "Spanish appearance/auth labels with key parity"
    - path: "apps/mobile/__tests__/screens/ProfileHandoff.test.tsx"
      provides: "Preference selection/semantics/target regression coverage"
  key_links:
    - from: "apps/mobile/app/(app)/profile/index.tsx"
      to: "apps/mobile/lib/theme/ThemeProvider.tsx"
      via: "useAppearancePreference preference + setPreference"
      pattern: "useAppearancePreference"
    - from: "apps/mobile/app/(auth)/login.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "render-time semantic colors"
      pattern: "useTheme"
    - from: "apps/mobile/app/(app)/profile/index.tsx"
      to: "apps/mobile/i18n/locales/en.json and es.json"
      via: "profile.appearance translated keys"
      pattern: "profile\\.appearance"
---

<objective>
Expose the locked three-way appearance choice in Profile and close the small pre-auth theme/i18n island.

Purpose: DARK-01 must be a thumb-friendly, understandable staff control, not a hidden switch. Login must not flash or remain light-only before staff reach Profile.
Output: Accessible Profile selector, target/text-scale fixes, bilingual login theming, locale parity, and focused tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-dark-mode-accessibility-qa/10-CONTEXT.md
@.planning/phases/10-dark-mode-accessibility-qa/10-RESEARCH.md
@.planning/phases/09-remaining-screens-rollout/09-01-SUMMARY.md
@.planning/phases/09-remaining-screens-rollout/09-UAT.md
@apps/mobile/app/(app)/profile/index.tsx
@apps/mobile/app/(auth)/login.tsx
@apps/mobile/__tests__/screens/ProfileHandoff.test.tsx

<interfaces>
Locked labels:
- EN: Appearance, System, Light, Dark
- ES: Apariencia, Sistema, Claro, Oscuro

Each option: `accessibilityRole="radio"` and `accessibilityState={{ selected }}`. The visual group is a radiogroup where supported and remains full-width/equal-width with `minHeight >= 44`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Profile appearance radio group test-first</name>
  <files>apps/mobile/app/(app)/profile/index.tsx, apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json, apps/mobile/__tests__/screens/ProfileHandoff.test.tsx</files>
  <action>
Update the existing Profile characterization test first. Mock the complete 10-01 theme API, then add failing assertions that all three translated options render directly in Preferences, expose radio roles and selected state, each resolves to `minHeight >= 44`, and pressing Light/Dark/System calls `setPreference` with the exact value. Add an ES case proving Sistema/Claro/Oscuro render without English fallback and remain distinguishable.

Implement a full-width equal-segment control bound to `preference`/`setPreference`; do not store an effective mode or add account sync. Put the appearance label and control on a layout that can grow vertically and wrap at narrow width/200% text size. Use `theme.onPrimary` for selected text, no hardcoded white, and no animation.

In the same focused accessibility pass, raise the existing 32pt language options and 38pt bell action to >=44, give language options radio semantics/selected state, keep their existing localized Inglés behavior, and allow values/rows to wrap rather than hide actions. Preserve notifications routing, hotel fetch, language persistence, pending sync, local sign-out confirmation, and all test IDs.

Add EN/ES `profile.appearance.*` keys with exact parity. Do not introduce an accent picker, density control, or account-synced preference.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/screens/ProfileHandoff.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>System/Light/Dark is immediate, persistent through the provider, translated, radio-semantic, and >=44pt; existing Profile workflows remain covered.</done>
</task>

<task type="auto">
  <name>Task 2: Theme and localize the login surface without changing auth</name>
  <files>apps/mobile/app/(auth)/login.tsx, apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json, apps/mobile/__tests__/screens/LoginTheme.test.tsx</files>
  <action>
Write a focused login render test before implementation. Render under controlled light and dark provider modes and assert semantic theme values are used for background, text, inputs, borders, buttons, icons, and disabled/busy states. Switch the mocked language to ES and prove every staff-facing login phrase resolves in Spanish; PatelRep/property names may remain proper names.

Replace static `C.*`/raw foregrounds with `useTheme()` semantic roles and existing shared Button where it preserves the current loading/disabled handler contract. Inputs and toggles must retain >=44pt targets, labels/placeholders, accessible busy/disabled states, and flexible text. Add missing EN/ES auth keys for the existing literal copy; translate the exact current presentation without changing password vs magic-link request logic, redirect URI, Alert behavior, validation, or Supabase calls.

Use the contrast-tested `onPrimary`/surface roles. Do not add theme animation or a new auth workflow.
  </action>
  <verify>cd apps/mobile; npx jest __tests__/screens/LoginTheme.test.tsx --runInBand; npm run type-check; npm run lint</verify>
  <done>Login renders correctly in both themes and languages with unchanged authentication behavior and no runtime `C.*` reference.</done>
</task>

</tasks>

<verification>
- Profile and Login focused suites pass.
- EN/ES locale leaf-key sets remain identical.
- `npm run type-check` and `npm run lint` pass.
- `rg -n "\\bC\\.|#fff|#FFFFFF" apps/mobile/app/(app)/profile/index.tsx apps/mobile/app/(auth)/login.tsx` returns no theme-role bypass.
- No Supabase/API payload, redirect, routing, sign-out, language persistence, or account data behavior changes.
</verification>

<success_criteria>
- Users can directly select System/Light/Dark from Profile with accessible state and immediate effect.
- The selection and neighboring Profile controls remain operable at narrow width and 200% scale.
- Login and Profile are contrast-safe and bilingual in both modes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dark-mode-accessibility-qa/10-04-SUMMARY.md`.
</output>
