---
phase: 02-housekeeper-workflow
plan: "06"
type: execute
wave: 4
depends_on:
  - "02-05"
files_modified: []
autonomous: false
requirements:
  - HK-01
  - HK-02
  - HK-03
  - HK-04
  - HK-05
  - HK-06
  - HK-07
  - PROF-01
  - PROF-02
  - L10N-01
must_haves:
  truths:
    - "Housekeeper sees only their assigned rooms (not all hotel rooms)"
    - "Room cards show room number, floor, status, ETA, and VIP badge where applicable"
    - "Status update DIRTY to IN_PROGRESS to CLEAN completes in under 3 taps"
    - "Offline status update syncs to web dashboard after reconnect within 10 seconds"
    - "Report Issue button opens modal; submission creates a work order on engineering dashboard"
    - "Profile shows name, role, and hotel name; Sign Out returns to login"
  artifacts:
    - path: "apps/mobile/__tests__/lib/offline/sync.test.ts"
      provides: "Automated sync contract verification"
      contains: "flushSyncQueue"
    - path: "apps/mobile/__tests__/components/ReportIssueModal.test.tsx"
      provides: "Automated issue reporting verification"
      contains: "ReportIssueModal"
  key_links:
    - from: "Physical device / simulator"
      to: "All phase 2 success criteria"
      via: "Manual verification steps below"
      pattern: "manual"
---

<objective>
Human verification of the complete housekeeper workflow on a physical device or simulator. All automated tests must be green before this checkpoint begins.

Purpose: Interaction-based behaviors (tap count, VIP data rendering, offline sync timing, push notification arrival) cannot be verified by automated tests — they require a running app on a device.
Output: User confirms all 5 success criteria are met, or documents issues for gap closure.
</objective>

<execution_context>
@C:/Users/Henil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Henil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite (pre-flight gate)</name>
  <files></files>
  <action>
Run the full jest suite and confirm all tests are green before asking for manual verification. This is the automated gate before the human checkpoint.

```bash
cd C:/Users/Henil/projects/PatelRep/apps/mobile && npx jest --passWithNoTests
```

If any tests fail, do NOT proceed to the manual checkpoint. Fix the failures first.

Also run a quick API import check:
```bash
cd C:/Users/Henil/projects/PatelRep/apps/api && python -c "import routers.housekeeping; print('API OK')"
```
  </action>
  <verify>
    <automated>cd C:/Users/Henil/projects/PatelRep/apps/mobile && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass (0 failures); API imports cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of full housekeeper workflow on device</name>
  <action>
Use a housekeeper test account (not a GM account — /my-rooms requires housekeeper role) on a physical device or simulator.

**SC-1: Room list shows only assigned rooms (HK-01, HK-02, HK-07)**
1. Log in as housekeeper test account on simulator/device
2. Navigate to My Rooms tab
3. Confirm: only rooms assigned to this housekeeper appear (not all hotel rooms)
4. Confirm each card shows: room number, floor, status badge
5. If a room has predicted_ready_at set in DB: confirm ETA displays (e.g. "Ready by 2:15 PM")
6. If a room has vip_flag=true: confirm gold VIP badge appears on the card
7. Open a VIP room's detail: confirm VIP section and check-in time are visible

**SC-2: Status update in under 3 taps (HK-03)**
1. Find a DIRTY room in the list
2. Tap the room card (tap 1 — opens detail)
3. Tap "Start Cleaning" (tap 2 — IN_PROGRESS, optimistic update)
4. Confirm room detail now shows IN_PROGRESS immediately
5. Tap "Mark Clean" (tap 3 — CLEAN, optimistic update)
6. Confirm: total taps from room list to CLEAN = 3

**SC-3: Offline sync (HK-04)**
1. Enable airplane mode on device
2. Find a DIRTY room, tap it, tap "Start Cleaning"
3. Confirm: status updates to IN_PROGRESS on the card (optimistic)
4. Confirm: NO Alert.alert dialog appears (OfflineBanner shows instead)
5. Find a second DIRTY room, change it to IN_PROGRESS
6. Restore connectivity (disable airplane mode)
7. Wait up to 10 seconds
8. Open web dashboard (app.patelrep.com or localhost:3000) housekeeping board
9. Confirm: both rooms show IN_PROGRESS on web dashboard

**SC-4: Report Issue creates work order (HK-06)**
1. Open any room detail screen
2. Scroll to bottom — confirm "Report Issue" button is visible (red border)
3. Tap "Report Issue" — confirm modal opens with description field
4. Type a description (e.g. "Bathroom faucet dripping")
5. Tap Submit
6. Confirm: modal closes without navigating away from room
7. Open web dashboard → Engineering → Work Orders
8. Confirm: a new work order appears with the room pre-filled and the description you entered

**SC-5: Profile screen (PROF-01, PROF-02)**
1. Navigate to Profile tab
2. Confirm: full name is displayed
3. Confirm: role is displayed (e.g. "HOUSEKEEPER")
4. Confirm: hotel name is displayed (fetched from API — may take 1-2 seconds to appear)
5. Tap Sign Out → confirm "Sign Out" confirmation dialog → confirm
6. Confirm: app navigates back to login screen

**SC-6: Language check (L10N-01)**
1. On profile tab, toggle Español switch ON
2. Navigate to a room detail — confirm "Report Issue" button shows "Reportar Problema"
3. Open the modal — confirm title shows "Reportar un Problema"
4. Toggle language back to English
  </action>
  <what-built>
Complete housekeeper workflow — 6 plans across wave 0–3:
- API: /my-rooms now returns vip_flag, checkin_time, predicted_ready_at, risk_level; POST /assignments fires push notification
- Mobile: Room list shows ETA, floor, VIP badge; room detail shows VIP section and check-in time; offline Alert removed
- Sync: refreshRooms fixed to call /housekeeping/my-rooms and unwrap response.data
- ReportIssueModal: new component, POST /work-orders, offline enqueue fallback
- Profile: hotel name fetched from API; Sign Out unchanged
- Push: projectId read from Constants.expoConfig instead of hardcoded placeholder
- i18n: 7 new keys in EN + ES
  </what-built>
  <verify>
    <automated>MISSING — this task requires human interaction on a physical device</automated>
  </verify>
  <done>User confirms all 6 success criteria pass on device, or documents specific failures</done>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which checks failed with details</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria confirmed by user on device.
Full jest suite green (from Task 1 pre-flight).
</verification>

<success_criteria>
- SC-1: Room list shows only assigned rooms with number, floor, status, ETA (when available), VIP badge (when applicable)
- SC-2: DIRTY to IN_PROGRESS to CLEAN completes in exactly 3 taps
- SC-3: Two offline status updates appear on web dashboard within 10 seconds of reconnect
- SC-4: Report Issue modal submits; work order appears on engineering dashboard with room pre-filled
- SC-5: Profile shows name + role + hotel name; Sign Out returns to login screen
- SC-6: New UI strings appear in Spanish when language toggled
</success_criteria>

<output>
After completion, create `.planning/phases/02-housekeeper-workflow/02-06-SUMMARY.md`
</output>
