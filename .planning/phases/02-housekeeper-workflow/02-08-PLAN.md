---
phase: 02-housekeeper-workflow
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
  - apps/mobile/components/housekeeping/ReportIssueModal.tsx
autonomous: true
requirements:
  - L10N-01
gap_closure: true

must_haves:
  truths:
    - "Submit button in ReportIssueModal shows 'Enviar' (not 'Submit') when device language is Spanish"
    - "Submit button in ReportIssueModal shows 'Submit' when device language is English"
  artifacts:
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "rooms.submit translation key"
      contains: "\"submit\": \"Submit\""
    - path: "apps/mobile/i18n/locales/es.json"
      provides: "rooms.submit translation key"
      contains: "\"submit\": \"Enviar\""
    - path: "apps/mobile/components/housekeeping/ReportIssueModal.tsx"
      provides: "Localized Submit button"
      contains: "t(\"rooms.submit\")"
  key_links:
    - from: "apps/mobile/components/housekeeping/ReportIssueModal.tsx line 115"
      to: "apps/mobile/i18n/locales/en.json rooms.submit"
      via: "t(\"rooms.submit\")"
      pattern: "t\\(\"rooms\\.submit\"\\)"
---

<objective>
Fix the hardcoded `"Submit"` string on line 115 of ReportIssueModal.tsx so Spanish users see "Enviar" instead of "Submit" when tapping the submit button on the issue report modal.

Purpose: L10N-01 requires all new UI strings have EN and ES translations. The submit button is the only remaining hardcoded string — all other strings in the modal already use `t()`.

Output: `rooms.submit` key added to both locale files; `ReportIssueModal.tsx` line 115 uses `t("rooms.submit")`.
</objective>

<execution_context>
@C:/Users/Henil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Henil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing locale structure — executor must match exactly -->

apps/mobile/i18n/locales/en.json (partial):
```json
{
  "rooms": {
    "reportIssue": "Report Issue",
    "reportIssueTitle": "Report an Issue",
    "reportIssueDescription": "Describe the issue...",
    "issueSubmitted": "Issue reported successfully",
    "issueCategory": "Category",
    "checkinTime": "Check-in: {{time}}"
  }
}
```
Add `"submit": "Submit"` inside the `"rooms"` object (after `"checkinTime"`).

apps/mobile/i18n/locales/es.json (partial):
```json
{
  "rooms": {
    "reportIssue": "Reportar Problema",
    "reportIssueTitle": "Reportar un Problema",
    "reportIssueDescription": "Describe el problema...",
    "issueSubmitted": "Problema reportado exitosamente",
    "issueCategory": "Categoría",
    "checkinTime": "Check-in: {{time}}"
  }
}
```
Add `"submit": "Enviar"` inside the `"rooms"` object (after `"checkinTime"`).

apps/mobile/components/housekeeping/ReportIssueModal.tsx (line 115 current):
```tsx
<Text style={styles.submitText}>Submit</Text>
```
Change to:
```tsx
<Text style={styles.submitText}>{t("rooms.submit")}</Text>
```

Test file mock: `jest.mock("react-i18next", () => ({ useTranslation: () => ({ t: (k: string) => k }) }))`
This means `t("rooms.submit")` returns the string `"rooms.submit"` in tests.
The existing test uses `getByText(/submit/i)` with a fallback to `getByTestId("submit-button")`.
After this change, `getByText(/submit/i)` will match `"rooms.submit"` (contains "submit") — tests still pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rooms.submit translation key to both locale files</name>
  <files>apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <action>
In `apps/mobile/i18n/locales/en.json`, add `"submit": "Submit"` to the `rooms` object, after the `"checkinTime"` entry. The rooms object should end with:
```json
    "checkinTime": "Check-in: {{time}}",
    "submit": "Submit"
```

In `apps/mobile/i18n/locales/es.json`, add `"submit": "Enviar"` to the `rooms` object, after the `"checkinTime"` entry. The rooms object should end with:
```json
    "checkinTime": "Check-in: {{time}}",
    "submit": "Enviar"
```

No other keys or structure should change in either file.
  </action>
  <verify>
    <automated>grep -q '"submit": "Submit"' C:/Users/Henil/projects/PatelRep/apps/mobile/i18n/locales/en.json && grep -q '"submit": "Enviar"' C:/Users/Henil/projects/PatelRep/apps/mobile/i18n/locales/es.json && echo "PASS: both submit keys present"</automated>
  </verify>
  <done>
    - `en.json` contains `"submit": "Submit"` inside the `rooms` object
    - `es.json` contains `"submit": "Enviar"` inside the `rooms` object
    - Both JSON files parse without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded "Submit" with t("rooms.submit") in ReportIssueModal</name>
  <files>apps/mobile/components/housekeeping/ReportIssueModal.tsx</files>
  <action>
On line 115, replace the hardcoded string literal:
```tsx
<Text style={styles.submitText}>Submit</Text>
```
with:
```tsx
<Text style={styles.submitText}>{t("rooms.submit")}</Text>
```

The `useTranslation` hook is already imported and `t` is already destructured on line 29 (`const { t } = useTranslation()`). No new imports needed.

No other changes to the file.

Then run the jest tests to confirm the existing 4 test cases still pass:
```bash
cd C:/Users/Henil/projects/PatelRep && npx jest --testPathPattern="ReportIssueModal" --no-coverage
```

The test mock returns `(k: string) => k`, so `t("rooms.submit")` returns `"rooms.submit"`.
The test queries `getByText(/submit/i)` — this regex matches the string `"rooms.submit"` because it contains "submit". Tests pass without modification.
  </action>
  <verify>
    <automated>cd C:/Users/Henil/projects/PatelRep && npx jest --testPathPattern="ReportIssueModal" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>
    - Line 115 of ReportIssueModal.tsx contains `t("rooms.submit")` (no hardcoded "Submit" string)
    - All 4 ReportIssueModal jest tests pass (renders, online submit, offline submit, cancel)
  </done>
</task>

</tasks>

<verification>
Automated:
- `grep '"submit": "Submit"' apps/mobile/i18n/locales/en.json` exits 0
- `grep '"submit": "Enviar"' apps/mobile/i18n/locales/es.json` exits 0
- Jest: all 4 ReportIssueModal tests green

Manual (covered by 02-06 human checkpoint in a re-run):
- Toggle language to Español on profile screen, open any room, tap Report Issue
- Submit button shows "Enviar"
</verification>

<success_criteria>
- `rooms.submit` key exists in en.json ("Submit") and es.json ("Enviar")
- ReportIssueModal.tsx line 115 uses `t("rooms.submit")` — no hardcoded English string
- All 4 existing ReportIssueModal tests pass without modification
- L10N-01 fully satisfied: every new UI string has EN + ES translations
</success_criteria>

<output>
After completion, create `.planning/phases/02-housekeeper-workflow/02-08-SUMMARY.md`
</output>
