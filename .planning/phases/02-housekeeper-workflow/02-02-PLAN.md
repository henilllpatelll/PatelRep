---
phase: 02-housekeeper-workflow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/i18n/locales/en.json
  - apps/mobile/i18n/locales/es.json
autonomous: true
requirements:
  - L10N-01
must_haves:
  truths:
    - "en.json and es.json contain all keys needed by ReportIssueModal (rooms.reportIssue, rooms.reportIssueTitle, rooms.reportIssueDescription, rooms.issueSubmitted, rooms.issueCategory, rooms.checkinTime) and profile screen (profile.hotel, profile.title)"
  artifacts:
    - path: "apps/mobile/i18n/locales/en.json"
      provides: "English translations for new UI strings"
      contains: "reportIssue"
    - path: "apps/mobile/i18n/locales/es.json"
      provides: "Spanish translations for new UI strings"
      contains: "reportIssue"
  key_links:
    - from: "apps/mobile/i18n/locales/en.json rooms.reportIssue"
      to: "apps/mobile/components/housekeeping/ReportIssueModal.tsx"
      via: "t('rooms.reportIssue')"
      pattern: "rooms.reportIssue"
    - from: "apps/mobile/i18n/locales/en.json profile.hotel"
      to: "apps/mobile/app/(app)/profile/index.tsx"
      via: "t('profile.hotel')"
      pattern: "profile.hotel"
---

<objective>
Add all missing i18n translation keys for Phase 2 new UI (ReportIssueModal and profile hotel name) to both EN and ES locale files. Translation keys go in BEFORE the components are built — this is the project rule (missing keys cause silent blank text, see Pitfall 5 in research).

Purpose: Components in plans 02-03, 02-04, and 02-05 reference these keys. Writing them first means `t("rooms.reportIssue")` returns real text, not the raw key string.
Output: Updated en.json and es.json with 7 new keys each.
</objective>

<execution_context>
@C:/Users/Henil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Henil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/mobile/i18n/locales/en.json
@apps/mobile/i18n/locales/es.json
</context>

<interfaces>
<!-- Current locale file structure (from codebase read) -->

en.json current "rooms" section ends at line 49. Keys to add inside "rooms":
- "reportIssue": "Report Issue"
- "reportIssueTitle": "Report an Issue"
- "reportIssueDescription": "Describe the issue..."
- "issueSubmitted": "Issue reported successfully"
- "issueCategory": "Category"
- "checkinTime": "Check-in: {{time}}"

en.json has no "profile" section. Add new top-level section:
- "profile": { "hotel": "Hotel", "title": "Profile" }

Note: en.json already has "rooms.vipGuest" and "rooms.eta" — do NOT duplicate them.
Note: es.json mirrors en.json structure exactly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add missing keys to en.json and es.json</name>
  <files>apps/mobile/i18n/locales/en.json, apps/mobile/i18n/locales/es.json</files>
  <action>
Edit `apps/mobile/i18n/locales/en.json`:
- Inside the existing `"rooms"` object, add these 6 keys AFTER the existing `"eta"` key:
  ```json
  "reportIssue": "Report Issue",
  "reportIssueTitle": "Report an Issue",
  "reportIssueDescription": "Describe the issue...",
  "issueSubmitted": "Issue reported successfully",
  "issueCategory": "Category",
  "checkinTime": "Check-in: {{time}}"
  ```
- Add a new top-level `"profile"` section (after `"notifications"`):
  ```json
  "profile": {
    "title": "Profile",
    "hotel": "Hotel"
  }
  ```

Edit `apps/mobile/i18n/locales/es.json` with identical structure but Spanish values:
- Inside `"rooms"`:
  ```json
  "reportIssue": "Reportar Problema",
  "reportIssueTitle": "Reportar un Problema",
  "reportIssueDescription": "Describe el problema...",
  "issueSubmitted": "Problema reportado exitosamente",
  "issueCategory": "Categoría",
  "checkinTime": "Check-in: {{time}}"
  ```
- New top-level `"profile"` section:
  ```json
  "profile": {
    "title": "Perfil",
    "hotel": "Hotel"
  }
  ```

Both files must remain valid JSON. Do not remove any existing keys.
  </action>
  <verify>
    <automated>cd C:/Users/Henil/projects/PatelRep && python3 -c "
import json, sys
en = json.load(open('apps/mobile/i18n/locales/en.json'))
es = json.load(open('apps/mobile/i18n/locales/es.json'))
keys = ['reportIssue','reportIssueTitle','issueSubmitted','checkinTime','issueCategory','reportIssueDescription']
for k in keys:
    assert k in en['rooms'], f'EN missing rooms.{k}'
    assert k in es['rooms'], f'ES missing rooms.{k}'
assert 'hotel' in en['profile'], 'EN missing profile.hotel'
assert 'hotel' in es['profile'], 'ES missing profile.hotel'
print('All keys present')
"</automated>
  </verify>
  <done>Python validation script prints "All keys present"; both JSON files parse without error; no existing keys removed</done>
</task>

</tasks>

<verification>
`python3 -c "import json; json.load(open('apps/mobile/i18n/locales/en.json'))"` exits 0 (valid JSON).
`python3 -c "import json; json.load(open('apps/mobile/i18n/locales/es.json'))"` exits 0 (valid JSON).
Validation script above prints "All keys present".
</verification>

<success_criteria>
- en.json and es.json both contain rooms.reportIssue, rooms.reportIssueTitle, rooms.reportIssueDescription, rooms.issueSubmitted, rooms.issueCategory, rooms.checkinTime
- en.json and es.json both contain profile.hotel and profile.title
- Both files are valid JSON with no existing keys removed
</success_criteria>

<output>
After completion, create `.planning/phases/02-housekeeper-workflow/02-02-SUMMARY.md`
</output>
