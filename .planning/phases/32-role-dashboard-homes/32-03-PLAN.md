---
phase: 32-role-dashboard-homes
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/components/dashboard/HousekeeperDashboard.tsx
  - apps/web/components/dashboard/EngineerDashboard.tsx
autonomous: true

# NOTE: touches only these two role component files — no overlap with 32-02/04/05, runs fully
# parallel. Does NOT edit en.ts/es.ts (all copy from 32-01) and does NOT edit shared modules or
# frozen primitives (import Stat/Pill/SectionLabel/Mono/StatusDot/Bar from primitives.tsx as-is).

must_haves:
  truths:
    - "With the 'dashboard' flag ON, a housekeeper sees their active room queue (unchanged information priority) restyled to the v2 token system, with skeleton-card loading, 'No rooms assigned yet' empty copy, and an error state where a query fails"
    - "With the flag ON, an engineer sees their SLA-prioritized open work orders (unchanged information priority) restyled to v2, with skeleton-card loading, 'No open work orders' empty copy, and an error state"
    - "With the flag OFF, both dashboards render byte-behaviorally identical to today (legacy branch untouched)"
    - "Neither dashboard's information architecture is restructured — this is chrome + state redesign only (HOME-01 for these two roles)"
  artifacts:
    - path: "apps/web/components/dashboard/HousekeeperDashboard.tsx"
      provides: "v2-flag-branched restyle of the my-queue/tasks/heads-up home + rebuilt skeleton/empty/error states"
      contains: "isSectionRedesigned('dashboard'"
    - path: "apps/web/components/dashboard/EngineerDashboard.tsx"
      provides: "v2-flag-branched restyle of the prioritized work-order home + rebuilt skeleton/empty/error states"
      contains: "isSectionRedesigned('dashboard'"
  key_links:
    - from: "both dashboards"
      to: "isSectionRedesigned('dashboard', hotel)"
      via: "useHotelStore direct read (pattern 2)"
      pattern: "isSectionRedesigned\\('dashboard'"
    - from: "empty/error regions"
      to: "StateBlock"
      via: "status='empty'/'error' with dashboard.empty.* copy + onRetry refetch"
      pattern: "StateBlock"
---

<objective>
Deliver the housekeeper and engineer slices of HOME-01: restyle both existing dashboard homes to the Phase-30/31 v2 token system and rebuild their empty/loading/error states, WITHOUT restructuring their (already-correct) information priority. Gate the redesign behind `isSectionRedesigned('dashboard', hotel)`; keep the legacy branch byte-behaviorally identical when off.

Per CONTEXT: for the 5 non-GM roles this phase is a visual/chrome redesign of already-correct information architecture — do not move data around, restyle it and rebuild the non-happy-path states.

Purpose: HOME-01 (housekeeper + engineer) + their slice of Criterion-3 (states) and Criterion-4 (flag/contrast/i18n gates).
Output: flag-branched v2 restyle of `HousekeeperDashboard.tsx` and `EngineerDashboard.tsx` with rebuilt states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

@apps/web/components/dashboard/HousekeeperDashboard.tsx
@apps/web/components/dashboard/EngineerDashboard.tsx
@apps/web/components/dashboard/DashboardGreeting.tsx
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/primitives.tsx
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle HousekeeperDashboard to v2 + rebuild states (flag-branched)</name>
  <files>apps/web/components/dashboard/HousekeeperDashboard.tsx</files>
  <action>
Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('dashboard', hotel)`. Keep the current JSX as the `!v2` legacy branch, byte-behaviorally unchanged.

v2 branch — SAME information architecture (stat strip: rooms today/done/avg/inspect-now; "My queue" room list; AI "Heads up" predictions; "My tasks"), restyled only:
- Apply v2 chrome tokens via className (Tailwind aliases: `duration-base`/`ease-standard` transitions, `focus-visible:ring-[var(--focus-ring)]`, `bg-brand`/`text-brand` for accents). Keep importing `Stat`/`Pill`/`SectionLabel`/`Mono`/`StatusDot`/`Bar` from primitives.tsx AS-IS — do NOT edit primitives.tsx. Do NOT re-tint any room-status token (`--alert/--info/--ready/--progress/--caution/--blocked`); room status keeps its exact frozen colors.
- Loading: replace the inline `h-[58px]` pulse rows with skeleton cards sized to match the v2 card shell (avoid layout shift). Keep `animate-pulse` + `bg-surface-2`/`bg-surface-3`. Do NOT use `StateBlock status='loading'` (spinner — forbidden by the locked "skeleton not spinner" decision).
- Empty: My-queue empty → `<StateBlock status='empty' empty={{ title: t('dashboard.empty.housekeeperNoRooms') }} />`; My-tasks empty → `dashboard.empty.housekeeperNoTasks`. (Use the section labels `dashboard.section.myQueue`/`myTasks`/`headsUp` where a heading isn't already keyed.)
- Error: wire each query's `isError` → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
Do NOT add/remove any query or change data priority — chrome + states only.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast all green; grep confirms `isSectionRedesigned('dashboard'` and `StateBlock` and `dashboard.empty.housekeeperNoRooms` usage; git status shows only HousekeeperDashboard.tsx changed (no primitives.tsx / locale edits)</verify>
  <done>Housekeeper home v2-branched: same queue/tasks/heads-up IA, v2 tokens, skeleton-card loading, role-appropriate empty + error states; legacy branch unchanged; frozen + contrast gates green</done>
</task>

<task type="auto">
  <name>Task 2: Restyle EngineerDashboard to v2 + rebuild states (flag-branched)</name>
  <files>apps/web/components/dashboard/EngineerDashboard.tsx</files>
  <action>
Same pattern and constraints as Task 1. Read the flag directly; keep current JSX as the `!v2` legacy branch.

v2 branch — SAME IA (stat strip: open/urgent/completed/PM-due; prioritized WO list; urgent callout banner), restyled only:
- v2 chrome tokens via className; import primitives as-is; no room-status re-tint.
- Loading: restyle the local `SkeletonRow()` to match the v2 card shell (keep it a skeleton, not a spinner).
- Empty: WO list empty → `<StateBlock status='empty' empty={{ title: t('dashboard.empty.engineerNoWorkOrders') }} />`.
- Error: `listWorkOrders` `isError` → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
- Keep the urgent-callout banner behavior; restyle its chrome only (use a `*-v2` token if it needs a non-room-status accent tint — do NOT re-tint `--alert`).
Do NOT change the query or WO prioritization logic — chrome + states only.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast all green; grep confirms `isSectionRedesigned('dashboard'`, `StateBlock`, and `dashboard.empty.engineerNoWorkOrders`; git status shows only EngineerDashboard.tsx changed</verify>
  <done>Engineer home v2-branched: same WO-priority IA, v2 tokens, skeleton loading, empty + error states; legacy branch unchanged; frozen + contrast gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast` all green from `apps/web`.
- Both dashboards: flag ON → v2 tokens + rebuilt skeleton/empty/error; flag OFF → today's UI unchanged.
- No information-architecture change (same queries, same ordering); no primitives.tsx / locale / shared-module edits; no room-status token re-tinted.
</verification>

<success_criteria>
HOME-01 met for housekeeper and engineer: both homes render in the v2 system behind the flag with redesigned empty/loading/error states, unchanged information priority, passing frozen/contrast gates.
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-03-SUMMARY.md` recording: the v2 chrome decisions (which tokens/aliases applied), the skeleton-card shapes chosen per home, the exact dashboard.* keys consumed, confirmation that no query/IA changed and no frozen/locale/shared file was edited, and any `*-v2` token introduced (with why).
</output>
