---
phase: 32-role-dashboard-homes
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/components/dashboard/GMDashboard.tsx
  - apps/web/app/(dashboard)/dashboard/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of page.tsx and the SOLE creator of
# GMDashboard.tsx. The wave-2 sibling plans (32-03/04/05) touch only their own role component
# files and do NOT touch page.tsx, so this runs fully parallel with them. This plan does NOT
# edit en.ts/es.ts — all GM copy (dashboard.gm.*, dashboard.empty.gmNoAlerts) already exists
# from 32-01. This plan does NOT edit the shared modules (LiveOpsGrid/ROIMetricsStrip/
# TrendChartsRow/AIRiskAlertsPanel) — it composes them AS-IS (they already carry room-status
# VALUE-frozen colors; editing them risks the frozen-token gate — see 32-RESEARCH frozen section).

must_haves:
  truths:
    - "A GM logging in with the 'dashboard' section flag ON lands on a dedicated portfolio-health snapshot: at-a-glance room-status counts (ready/dirty/pickup/inspected), open work orders including an urgent count, active guest-request count, and staff-on-shift — composed from existing endpoints, no new backend"
    - "Below the snapshot, the GM sees the AI risk & alerts feed (AIRiskAlertsPanel, reused as-is with its Phase-26 deep links), then a compact operations summary card that links out to Management ROI and Billing — financial/credit detail is a drill-down link, not inlined at report depth"
    - "The GM home is a dedicated component file (components/dashboard/GMDashboard.tsx), no longer an inline function in page.tsx — HOME-02 satisfied"
    - "With the flag OFF, the GM sees today's dashboard byte-behaviorally unchanged (legacy branch preserved)"
    - "The GM home shows skeleton cards (not a spinner) while loading, role-appropriate empty copy (dashboard.empty.gmNoAlerts / gmNoData) when there is no data, and an error state with retry where a query fails"
  artifacts:
    - path: "apps/web/components/dashboard/GMDashboard.tsx"
      provides: "Dedicated first-class GM home: DashboardGreeting header + portfolio snapshot + AIRiskAlertsPanel + compact ops summary card, flag-branched (v2 vs legacy), with skeleton/empty/error states"
      exports: ["GMDashboard"]
      min_lines: 120
    - path: "apps/web/app/(dashboard)/dashboard/page.tsx"
      provides: "Imports GMDashboard from components/dashboard; inline GMDashboard function removed; switch routes gm -> <GMDashboard/>"
      contains: "GMDashboard"
  key_links:
    - from: "apps/web/app/(dashboard)/dashboard/page.tsx"
      to: "apps/web/components/dashboard/GMDashboard.tsx"
      via: "import { GMDashboard } and case 'gm' in the role switch"
      pattern: "import.*GMDashboard"
    - from: "apps/web/components/dashboard/GMDashboard.tsx"
      to: "existing portfolio endpoints"
      via: "hotelsApi.getStats + reportsApi.getDailySummary + guestRequestsApi.listRequests + engineeringApi.listWorkOrders (client-side urgent split)"
      pattern: "getStats|getDailySummary|listRequests"
    - from: "apps/web/components/dashboard/GMDashboard.tsx"
      to: "isSectionRedesigned('dashboard', hotel)"
      via: "useHotelStore direct read (pattern 2 — home is page content, not shell-mounted)"
      pattern: "isSectionRedesigned\\('dashboard'"
---

<objective>
Deliver HOME-02: extract the inline `GMDashboard()` out of `page.tsx` into a dedicated first-class component `components/dashboard/GMDashboard.tsx`, and redesign its body around the locked portfolio-snapshot-first layout — lead with a cross-department health snapshot, put the AI risk/alerts feed below it, and add a compact operations summary card that drills into Management ROI / Billing. Gate the v2 redesign behind `isSectionRedesigned('dashboard', hotel)`; keep the legacy branch byte-behaviorally identical when the flag is off.

IMPORTANT framing correction (from 32-RESEARCH): the GM does NOT currently borrow `SupervisorDashboard`. `page.tsx` already contains a bespoke *inline* `GMDashboard()` (lines ~24-132) composing `DashboardGreeting` + departures/ready cards + `ROIMetricsStrip` + `AIRiskAlertsPanel` + `LiveOpsGrid` + `TrendChartsRow`. So HOME-02 = extract that inline function into its own file AND redesign it to the portfolio-snapshot layout — not "stop referencing SupervisorDashboard" (there is no such reference).

Purpose: HOME-02 in full + the GM's slice of HOME-01/Criterion-3 (states) and Criterion-4 (flag gating).
Output: new `GMDashboard.tsx`, `page.tsx` importing it with the inline function removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# The file to extract FROM (inline GMDashboard lives at ~lines 24-132; role switch at ~151-170)
@apps/web/app/(dashboard)/dashboard/page.tsx
# Shared modules to compose AS-IS (do NOT edit — they carry frozen room-status colors)
@apps/web/components/dashboard/AIRiskAlertsPanel.tsx
@apps/web/components/dashboard/ROIMetricsStrip.tsx
@apps/web/components/dashboard/LiveOpsGrid.tsx
@apps/web/components/dashboard/DashboardGreeting.tsx
# Endpoint shapes for the snapshot
@apps/web/lib/api/hotels.ts
@apps/web/lib/api/reports.ts
@apps/web/lib/api/guestRequests.ts
@apps/web/lib/api/engineering.ts
# Flag + state contracts
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
# Primitives to import (do NOT edit — frozen)
@apps/web/components/ui/primitives.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract inline GMDashboard into components/dashboard/GMDashboard.tsx and wire page.tsx</name>
  <files>apps/web/components/dashboard/GMDashboard.tsx, apps/web/app/(dashboard)/dashboard/page.tsx</files>
  <action>
1. Create `apps/web/components/dashboard/GMDashboard.tsx` (`'use client'`) exporting `function GMDashboard()`. Move the ENTIRE current inline `GMDashboard()` body from `page.tsx` (lines ~24-132) into this new file verbatim first (imports and all) — get it compiling and behaviorally identical BEFORE any redesign. This is the safe extraction step: legacy behavior preserved 1:1.
2. In `page.tsx`: delete the inline `GMDashboard` function, add `import { GMDashboard } from '@/components/dashboard/GMDashboard'` alongside the other dashboard-component imports, and confirm the `case 'gm':` in the role switch renders `<GMDashboard />`. Remove any now-unused imports in page.tsx that were only used by the inline function (e.g. housekeepingApi, mutation hooks, Card/StateBlock if unused elsewhere in page.tsx) — run type-check to catch orphans.
3. Do NOT redesign yet in this task — Task 2 does the redesign. This task's success = extraction with zero behavior change, build green.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build green; grep confirms `export function GMDashboard` in the new file and `import { GMDashboard }` + `<GMDashboard` in page.tsx; grep confirms the inline `function GMDashboard` NO LONGER exists in page.tsx; no unused-import/type errors</verify>
  <done>GMDashboard is a standalone component file rendering identically to the old inline version; page.tsx imports it and the inline function is gone; build + type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Redesign GMDashboard to the portfolio-snapshot layout (flag-branched) + skeleton/empty/error states</name>
  <files>apps/web/components/dashboard/GMDashboard.tsx</files>
  <action>
Read the flag directly (pattern 2): `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('dashboard', hotel)`. Keep the existing legacy JSX under `if (!v2)` (byte-behaviorally unchanged from Task 1's extraction). Build the v2 branch as follows.

DATA (all existing endpoints — NO new backend; see 32-RESEARCH GM data-sources table):
- `hotelsApi.getStats(hotelId)` → `{ room_count, active_staff, open_tasks, open_work_orders }` (staff-on-shift = `active_staff`; get `hotelId` via `getHotelIdFromSession(session.access_token)`, the established pattern used by ROIMetricsStrip/LiveOpsGrid/FrontDeskDashboard).
- `reportsApi.getDailySummary()` → `room_status_breakdown: Record<string,number>` for ready/dirty/pickup/inspected counts + `open_work_orders`.
- `guestRequestsApi.listRequests({ status: 'open' })` → `.length` for active guest requests.
- `engineeringApi.listWorkOrders({ per_page: 100 })` → client-side filter `priority === 'urgent'` for the urgent count (the one "by urgency" split not pre-aggregated — compose client-side exactly as EngineerDashboard/ChiefEngineerDashboard already do).
Each is its own React Query with `isLoading`/`isError`/`refetch`.

LAYOUT (locked order — snapshot FIRST, alerts BELOW, financial as compact drill-down):
1. `<DashboardGreeting name subtitle />` header (keep it — it is the sanctioned dashboard PageHeader-equivalent; do NOT swap to PageHeader).
2. Portfolio snapshot (LEAD): a set of large, clear summary cards/tiles (GM density is "light/drill-down", not a dense BI grid). Use the existing `Stat`/`Pill`/`SectionLabel` primitives from `primitives.tsx` (import only — do NOT edit them). Show room-status counts (ready/dirty/pickup/inspected — label via `dashboard.gm.rooms*`), open WOs + urgent count (`dashboard.gm.openWorkOrders`/`urgentWorkOrders`), active guest requests (`dashboard.gm.activeGuestRequests`), staff on shift (`dashboard.gm.staffOnShift`), open tasks (`dashboard.gm.openTasks`). Title with `dashboard.gm.snapshotTitle`. For the room-status counts, keep the exact frozen room-status color tokens (`--ready/--alert/--caution/--info` etc. via the primitives' existing tone mapping) — do NOT re-tint them for chrome (frozen VALUE constraint). Whether the snapshot is one combined module or 3-4 smaller per-department cards is your discretion — pick whichever composes most cleanly; per-department cards are recommended for the "light" density.
3. `<AIRiskAlertsPanel />` BELOW the snapshot (reuse as-is; it already renders its own loading/empty and Phase-26 deep links). Section label `dashboard.gm.alertsTitle`.
4. Compact operations summary card (secondary): reuse `<ROIMetricsStrip />` as the ops-KPI summary (occupancy/inspected/open WOs/tasks — it is operational KPIs, NOT financial), titled `dashboard.gm.opsSummaryTitle`, with drill-down links to `/management-roi` (`dashboard.gm.viewManagementRoi`) and `/settings/billing` (`dashboard.gm.viewBilling`). This resolves 32-RESEARCH Open Question #2: reuse ROIMetricsStrip + deep links for scope-minimalism rather than building a new dollars/credits card; full financial detail already lives at /management-roi and /settings/billing.
5. DROP the old departures/ready-room action columns and their checkout/checkin mutations from the v2 GM branch (they duplicate FrontDesk and violate the light/drill-down GM density — 32-RESEARCH Open Question #3). The snapshot's room-status counts convey the same at-a-glance signal; detail lives in FrontDesk/Housekeeping. (Legacy branch keeps them — only the v2 branch drops them.) You MAY keep `LiveOpsGrid`/`TrendChartsRow` as tertiary modules below the ops summary if they fit the light density — optional, your discretion.

STATES (Criterion #3 — rebuild all three, per 32-RESEARCH state contract):
- Loading → inline skeleton cards matching the snapshot card shell (reuse the animate-pulse + bg-surface-2/3 shapes from page.tsx lines 138-149 / ROIMetricsStrip's 4×h-24 / LiveOpsGrid's 6×h-88). Do NOT route loading through `StateBlock status='loading'` (that renders a spinner — violates the "skeleton not spinner" locked decision).
- Empty → `<StateBlock status='empty' empty={{ title: t('dashboard.empty.gmNoAlerts') }} />` for the alerts region and `dashboard.empty.gmNoData` for the snapshot if getStats/getDailySummary return nothing.
- Error → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />` per failing query.

TOKENS: prefer the Phase-30/31 Tailwind aliases (`duration-base`, `ease-standard`, `bg-brand`, `text-brand`, `focus-visible:ring-[var(--focus-ring)]`, `z-*`) for new chrome over raw `var(...)`. Do NOT edit primitives.tsx/Button.tsx or any frozen file; if you need a new chrome tint, add a `*-v2` token rather than re-tinting a room-status token (none needed if you follow the above).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('dashboard'`, `getStats`, `getDailySummary`, `listRequests`, and a `priority === 'urgent'` (or equivalent) client filter in GMDashboard.tsx; grep confirms NO edits to primitives.tsx/LiveOpsGrid/ROIMetricsStrip/AIRiskAlertsPanel (git status shows only GMDashboard.tsx + page.tsx changed)</verify>
  <done>v2 GM branch renders the portfolio snapshot first, AIRiskAlertsPanel below, a compact ops-summary card with Management-ROI/Billing links, with skeleton/empty/error states; legacy branch unchanged when flag off; departures/ready action columns dropped from v2 only; all standing gates green; no frozen file edited</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Inline `GMDashboard` removed from page.tsx; `import { GMDashboard }` present; `case 'gm'` renders it.
- v2 GM home = snapshot (lead) → AIRiskAlertsPanel (below) → compact ops summary card (links to /management-roi + /settings/billing). Flag off = legacy behavior byte-identical.
- Loading = skeleton cards (never StateBlock spinner); empty = dashboard.empty.gm* copy; error = StateBlock with retry.
- No shared dashboard module (LiveOpsGrid/ROIMetricsStrip/TrendChartsRow/AIRiskAlertsPanel) or frozen primitive edited; no room-status token re-tinted.
</verification>

<success_criteria>
HOME-02 met: GM has a dedicated, first-class dashboard home component redesigned around the portfolio-snapshot layout, composed entirely from existing endpoints, gated by the 'dashboard' flag with rebuilt empty/loading/error states, no new backend, zero frozen-file drift.
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-02-SUMMARY.md` recording: the final snapshot composition (combined vs per-department cards), the ops/financial-card decision (ROIMetricsStrip reuse + drill links — resolving Open Question #2), the departures/ready-columns drop decision (Open Question #3), exactly which endpoints the snapshot queries and how the urgent-WO split was computed client-side, confirmation that no shared module or frozen file was edited, and the exact dashboard.* keys consumed.
</output>
