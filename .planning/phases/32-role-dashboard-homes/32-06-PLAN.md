---
phase: 32-role-dashboard-homes
plan: 06
type: execute
wave: 3
depends_on: ["32-02", "32-03", "32-04", "32-05"]
files_modified: []
autonomous: true

# NOTE: verification-only plan (files_modified: []), mirroring 31-06. Runs the full standing web
# gate suite across the combined wave-2 changes, re-passes the Phase-30 Room-Board regression
# harness (dashboard-home changes must be inert to it), and performs live flag-on/flag-off browser
# self-verification of all 6 homes (light+dark, EN+ES). Any real defect found is a blocking Rule-3
# fix in the owning component file (which un-empties files_modified for that fix only) — otherwise
# this plan changes no source.

must_haves:
  truths:
    - "All standing web gates pass on the combined Phase-32 changes: type-check, check:frozen-files (zero frozen-hash / room-status drift), check:contrast (dark-mode WCAG AA), check:i18n-parity (+ verify:i18n-gate), and build"
    - "The Phase-30 Room-Board regression harness re-passes at zero pixel drift — dashboard-home changes did not perturb the frozen boards"
    - "With the 'dashboard' flag ON for the test hotel, all 6 role homes render the v2 redesign — GM shows the dedicated portfolio snapshot (not the old inline composition); with the flag OFF, all 6 render today's UI unchanged"
    - "Each home's empty, loading (skeleton, not spinner), and error states are confirmed to render, in both light and dark mode and both EN and ES, with no console errors and no failed/forbidden API calls"
  artifacts: []
  key_links:
    - from: "the 6 role homes"
      to: "the 'dashboard' section flag"
      via: "web_redesign_sections includes 'dashboard' toggled on the test hotel, verified live per role"
      pattern: "isSectionRedesigned\\('dashboard'"
---

<objective>
Close Phase 32: prove the four wave-2 plans combine cleanly. Run the full standing web gate suite over all Phase-32 changes together, re-pass the Phase-30 Room-Board regression harness (the dashboard homes sit inside the shell that wraps the boards — changes should be inert to the pixel-diff), and live-verify all 6 role homes in the browser with the 'dashboard' flag both ON and OFF, in light+dark and EN+ES, confirming the redesigned empty/loading/error states render and no regression to adjacent surfaces.

This satisfies the whole-phase view of all 4 Success Criteria: HOME-01 (6 homes redesigned), HOME-02 (GM dedicated portfolio home, no longer inline/borrowed), Criterion-3 (density/StateBlock/empty-loading-error), Criterion-4 (flag + contrast + i18n gates).

Purpose: phase-close verification gate. No planned source changes (files_modified: []); a real defect found becomes a blocking targeted fix in the owning component and is logged.
Output: verification evidence recorded in the summary; green gates + regression re-pass + live per-role confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# Prior-plan summaries to know what was built and which dashboard.* keys exist
@.planning/phases/32-role-dashboard-homes/32-01-SUMMARY.md
@.planning/phases/32-role-dashboard-homes/32-02-SUMMARY.md
@.planning/phases/32-role-dashboard-homes/32-03-SUMMARY.md
@.planning/phases/32-role-dashboard-homes/32-04-SUMMARY.md
@.planning/phases/32-role-dashboard-homes/32-05-SUMMARY.md

# Regression harness
@apps/web/playwright.regression.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full standing gate suite + Room-Board regression re-pass on combined changes</name>
  <files></files>
  <action>
From `apps/web`, run the full standing web gate suite over the combined Phase-32 tree and confirm all green:
- `npm run type-check`
- `npm run check:frozen-files` — MUST show zero frozen-hash drift AND zero room-status value drift across all four wave-2 plans combined (the whole point: dashboard homes composed frozen primitives / room-status tokens but never edited them).
- `npm run check:contrast` — dark-mode WCAG AA over every new/extended token pairing introduced by the homes (including any `*-v2` chrome token a wave-2 plan added).
- `npm run check:i18n-parity` and `npm run verify:i18n-gate` — en/es parity intact after 32-01's key additions.
- `npm run build`.
Then re-pass the Phase-30 Room-Board regression harness: `npx playwright test --config=playwright.regression.config.ts` (needs the regression fixture credentials / base URL per 30-06; if this executor session lacks live Supabase MCP write access to seed/flip the fixture tenant's flag, run the flag-OFF regression at minimum and flag the flag-ON regression re-run for the orchestrator, exactly as 31-06 did). Expect 12/12 at zero pixel drift — dashboard-home changes are outside the 3 excluded boards and must not perturb them.
If any gate fails on a real defect, fix it in the owning component file (blocking Rule-3 fix — this un-empties files_modified for that fix only) and re-run until green; log the fix to `.wolf/buglog.json`.
  </action>
  <verify>All of type-check / check:frozen-files / check:contrast / check:i18n-parity / verify:i18n-gate / build exit 0; Room-Board regression suite reports 12/12 zero-drift (flag-off at minimum, flag-on if MCP access available); any defect found is fixed and re-verified green</verify>
  <done>Every standing web gate green on the combined Phase-32 changes; Room-Board regression re-passes at zero drift; zero frozen-file / room-status drift confirmed across all four wave-2 plans</done>
</task>

<task type="auto">
  <name>Task 2: Live flag-on/flag-off browser self-verification of all 6 homes (light+dark, EN+ES)</name>
  <files></files>
  <action>
Per the mandatory Self-Verification Policy (CLAUDE.md): start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Enable the `'dashboard'` section for the test hotel by adding `'dashboard'` to its `web_redesign_sections` array (via `hotelsApi.update` PATCH /v1/hotels/{id} or directly in Supabase — same manual/DB toggle Phase 31 used for `'shell'`; there is no settings-UI toggle). Use the GM test account in memory; for roles the fixture doesn't seed, rely on the flag-branch code path being role-agnostic (each home reads the same `isSectionRedesigned('dashboard', hotel)`) plus per-role login where credentials exist — document which roles were live-checked vs code-traced, exactly as 31-06 did.

Verify, using the project's installed `@playwright/test` (ad-hoc scripts) or manual browser as available:
- FLAG ON: each of the 6 homes renders the v2 redesign. Specifically confirm the GM home shows the dedicated portfolio snapshot FIRST, AIRiskAlertsPanel below, and the compact ops-summary card with working links to /management-roi and /settings/billing (HOME-02) — and that the old inline departures/ready action columns are gone from the GM v2 view.
- FLAG OFF: each home renders today's UI unchanged (legacy branch).
- STATES: for at least the GM home and one data-empty role, confirm the loading state shows skeleton cards (not a spinner), the empty state shows the role-appropriate `dashboard.empty.*` copy, and a forced query error shows the StateBlock error + retry.
- LIGHT + DARK: toggle theme; confirm no contrast/legibility break (backs up check:contrast by eye). EN + ES: toggle locale; confirm the new dashboard.* copy renders in both with no missing-key fallback.
- No console errors; no failed/forbidden API calls (open Network tab; confirm no home fires an endpoint the role can't access).
Record exactly what was live-verified vs code-traced, and any follow-up flagged for the orchestrator (e.g. flag-on regression, roles not seeded).
  </action>
  <verify>Screenshots/notes captured for all 6 homes flag-on and a flag-off spot check; GM portfolio snapshot + drill-down links confirmed working; skeleton/empty/error states observed for at least GM + one empty role; light/dark + EN/ES confirmed; zero console errors and zero forbidden API calls in the checked roles; any un-verifiable item explicitly flagged for the orchestrator</verify>
  <done>All 6 role homes live-confirmed to render the v2 redesign flag-on and legacy flag-off, with skeleton/empty/error states rendering in light+dark and EN+ES, no console errors or forbidden calls; GM dedicated portfolio home confirmed (HOME-02); coverage gaps flagged precisely</done>
</task>

</tasks>

<verification>
- Full standing web gate suite green on combined changes (type-check, check:frozen-files, check:contrast, check:i18n-parity, verify:i18n-gate, build).
- Room-Board regression re-passes at zero drift (flag-off minimum; flag-on if MCP access available, else flagged).
- All 6 homes live-verified flag-on (v2) and flag-off (legacy); GM shows dedicated portfolio snapshot (HOME-02); empty/loading/error states render (skeleton not spinner); light+dark and EN+ES confirmed; no console errors / forbidden calls.
</verification>

<success_criteria>
Phase 32 verified end-to-end: HOME-01 (6 redesigned homes), HOME-02 (dedicated GM portfolio home), Criterion-3 (density/StateBlock/empty-loading-error), Criterion-4 (flag + contrast + i18n) all confirmed against the running app and the gate suite, with zero regression to the frozen Room Boards. Any residual coverage gap is precisely flagged for the orchestrator's close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-06-SUMMARY.md` recording: the exact gate results, the regression outcome (and whether flag-on was runnable this session), a per-role table of live-verified vs code-traced, the state-verification evidence, any defect found+fixed (with buglog id), and a precise list of any follow-up left for the orchestrator (mirroring 31-06's close-out format).
</output>
