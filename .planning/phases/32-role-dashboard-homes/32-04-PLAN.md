---
phase: 32-role-dashboard-homes
plan: 04
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/components/dashboard/SupervisorDashboard.tsx
  - apps/web/components/dashboard/ChiefEngineerDashboard.tsx
autonomous: true

# NOTE: touches only these two role component files — no overlap with 32-02/03/05, runs fully
# parallel. Does NOT edit en.ts/es.ts (all copy from 32-01). SupervisorDashboard COMPOSES the
# shared modules LiveOpsGrid/StaffProgress/PredictionsWidget/RoomGridMini/ActivityFeed — restyle
# ONLY SupervisorDashboard's own chrome + its stat-strip/section states; do NOT edit LiveOpsGrid
# or any other shared/frozen file (they carry room-status VALUE-frozen colors; GM plan 32-02 also
# renders LiveOpsGrid as-is, so leaving it untouched keeps both plans conflict-free).

must_haves:
  truths:
    - "With the 'dashboard' flag ON, a housekeeping_supervisor sees the same room-status board summary + team assignment overview (unchanged IA) restyled to v2, with skeleton-card loading and rebuilt empty/error states"
    - "With the flag ON, a chief_engineer sees the same cross-team WO overview + asset health/failure predictions (unchanged IA) restyled to v2, with skeleton-card loading and rebuilt empty/error states"
    - "With the flag OFF, both dashboards render byte-behaviorally identical to today (legacy branch untouched); the GM no longer reuses SupervisorDashboard (that decoupling lands in 32-02, independently)"
    - "Neither dashboard's information architecture is restructured — chrome + state redesign only (HOME-01 for these two roles)"
  artifacts:
    - path: "apps/web/components/dashboard/SupervisorDashboard.tsx"
      provides: "v2-flag-branched restyle of the supervisor home chrome + rebuilt stat-strip skeleton / per-widget empty+error states; shared modules composed as-is"
      contains: "isSectionRedesigned('dashboard'"
    - path: "apps/web/components/dashboard/ChiefEngineerDashboard.tsx"
      provides: "v2-flag-branched restyle of the chief-engineer home + rebuilt skeleton/empty/error states"
      contains: "isSectionRedesigned('dashboard'"
  key_links:
    - from: "both dashboards"
      to: "isSectionRedesigned('dashboard', hotel)"
      via: "useHotelStore direct read (pattern 2)"
      pattern: "isSectionRedesigned\\('dashboard'"
    - from: "empty/error regions"
      to: "StateBlock"
      via: "status='empty'/'error' with dashboard.empty.* copy + onRetry refetch"
      pattern: "StateBlock"
---

<objective>
Deliver the supervisor and chief-engineer slices of HOME-01: restyle both existing dashboard homes to the v2 token system and rebuild their empty/loading/error states, WITHOUT restructuring their already-correct information priority. Gate behind `isSectionRedesigned('dashboard', hotel)`; keep the legacy branch byte-identical when off.

SupervisorDashboard composes several internal/shared sub-components (`LiveOpsGrid`, `StaffProgress`, `PredictionsWidget`, `RoomGridMini`, `ActivityFeed`). Restyle SupervisorDashboard's OWN chrome and its stat-strip/section-level states only — do NOT edit `LiveOpsGrid` or other shared modules (they carry frozen room-status colors and are also rendered by the GM home; leaving them as-is keeps 32-02 and 32-04 non-conflicting and avoids the frozen-token gate).

Purpose: HOME-01 (supervisor + chief_engineer) + their slice of Criterion-3 (states) and Criterion-4 (gates).
Output: flag-branched v2 restyle of `SupervisorDashboard.tsx` and `ChiefEngineerDashboard.tsx` with rebuilt states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

@apps/web/components/dashboard/SupervisorDashboard.tsx
@apps/web/components/dashboard/ChiefEngineerDashboard.tsx
@apps/web/components/dashboard/LiveOpsGrid.tsx
@apps/web/components/dashboard/DashboardGreeting.tsx
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/primitives.tsx
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SupervisorDashboard chrome to v2 + rebuild states (flag-branched)</name>
  <files>apps/web/components/dashboard/SupervisorDashboard.tsx</files>
  <action>
Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('dashboard', hotel)`. Keep current JSX as the `!v2` legacy branch, byte-behaviorally unchanged.

v2 branch — SAME IA (greeting + actions [Broadcast modal / New task]; AI morning-briefing card; stat strip; `LiveOpsGrid`; `StaffProgress`; `PredictionsWidget`; `RoomGridMini`; `ActivityFeed`), restyle only SupervisorDashboard's OWN wrappers/cards:
- Apply v2 chrome tokens via className on SupervisorDashboard's own card shells / section headers / action buttons (Tailwind aliases: `duration-base`/`ease-standard`, `focus-visible:ring-[var(--focus-ring)]`, `bg-brand`/`text-brand`). Import primitives as-is; no room-status re-tint.
- Render `<LiveOpsGrid />`, `<StaffProgress/>`, `<PredictionsWidget/>`, `<RoomGridMini/>`, `<ActivityFeed/>` AS-IS — do NOT edit those files. If any is defined inline within SupervisorDashboard.tsx (vs imported), you may restyle its chrome in-place, but do NOT touch `LiveOpsGrid.tsx` (it's a separate shared file rendered by the GM home too).
- Loading: restyle the `h-16` pulse stat tiles to match the v2 card shell (skeleton, not spinner). Sub-widgets keep their own loading.
- Empty: supervisor alerts region empty → `dashboard.empty.supervisorNoAlerts`; open-guest-requests region empty → `dashboard.empty.supervisorNoRequests`. Use `dashboard.section.atRiskRooms`/`teamOverview` for headings not already keyed.
- Error: wire the top-level queries' `isError` (`reportsApi.getDailySummary`, `aiApi.getRiskAlerts`, etc.) → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
Do NOT change any query, the Broadcast/New-task actions, or data priority — chrome + states only.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast all green; grep confirms `isSectionRedesigned('dashboard'`, `StateBlock`, `dashboard.empty.supervisorNoAlerts`; git status shows ONLY SupervisorDashboard.tsx changed (LiveOpsGrid.tsx / primitives.tsx / locales untouched)</verify>
  <done>Supervisor home v2-branched: same IA + actions, v2 chrome, skeleton stat strip, rebuilt empty/error states; shared modules composed as-is; legacy branch unchanged; frozen + contrast gates green</done>
</task>

<task type="auto">
  <name>Task 2: Restyle ChiefEngineerDashboard to v2 + rebuild states (flag-branched)</name>
  <files>apps/web/components/dashboard/ChiefEngineerDashboard.tsx</files>
  <action>
Same pattern and constraints. Read the flag directly; keep current JSX as the `!v2` legacy branch.

v2 branch — SAME IA (stat strip: open/urgent/PM-due/assets; WO queue; "PM due 7d"; "High-risk assets" AI card; SLA-compliance callout), restyled only:
- v2 chrome tokens via className; import primitives as-is; no room-status re-tint.
- Loading: restyle local `SkeletonRow()` to the v2 card shell (skeleton, not spinner).
- Empty: WO queue empty → `dashboard.empty.chiefNoWorkOrders`; high-risk assets empty → `dashboard.empty.chiefNoAssets`.
- Error: wire `listWorkOrders`/`getFailurePredictions`/`getMaintenance` `isError` → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
- SLA-compliance callout: restyle chrome only; use a `*-v2` token if a non-room-status accent is needed (do NOT re-tint `--alert`/`--caution`).
Do NOT change queries or WO/asset prioritization — chrome + states only.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast all green; grep confirms `isSectionRedesigned('dashboard'`, `StateBlock`, `dashboard.empty.chiefNoWorkOrders`/`chiefNoAssets`; git status shows only ChiefEngineerDashboard.tsx changed</verify>
  <done>Chief-engineer home v2-branched: same cross-team WO + asset-health IA, v2 chrome, skeleton loading, rebuilt empty/error states; legacy branch unchanged; frozen + contrast gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast` all green from `apps/web`.
- Both dashboards: flag ON → v2 tokens + rebuilt skeleton/empty/error; flag OFF → today's UI unchanged.
- No IA change; no edits to LiveOpsGrid / primitives.tsx / locale / any shared-or-frozen file; no room-status token re-tinted.
</verification>

<success_criteria>
HOME-01 met for housekeeping_supervisor and chief_engineer: both homes render in the v2 system behind the flag with redesigned empty/loading/error states, unchanged information priority, passing frozen/contrast gates. (GM's decoupling from SupervisorDashboard is handled independently in 32-02.)
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-04-SUMMARY.md` recording: the v2 chrome decisions, skeleton shapes chosen, exact dashboard.* keys consumed, confirmation that LiveOpsGrid and all other shared/frozen files were left untouched (git status), and any `*-v2` token introduced.
</output>
