---
phase: 32-role-dashboard-homes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

# NOTE on file ownership: en.ts / es.ts are edited ONLY here. This plan is the SOLE owner of
# the locale files for Phase 32 so the four parallel wave-2 home plans (32-02..32-05) never
# collide on them. Those plans MUST consume the `dashboard.*` / `common.*` keys defined here
# and MUST NOT add new locale keys. If a wave-2 plan discovers a genuinely missing string, it
# reuses an existing generic `common.*` key rather than editing en/es (which would conflict
# with its sibling wave-2 plans). This is why the key set below is intentionally comprehensive.

must_haves:
  truths:
    - "Every empty-state string, GM portfolio-snapshot label, and card title the six redesigned dashboard homes need already exists under the `dashboard.*` namespace in BOTH en.ts and es.ts, with real Spanish translations (not placeholders)"
    - "`npm run check:i18n-parity` and `npm run verify:i18n-gate` pass — every new en key has an es counterpart and vice versa"
    - "The `dashboard.greeting.*` keys already present are left byte-unchanged (additive only)"
  artifacts:
    - path: "apps/web/i18n/locales/en.ts"
      provides: "Full dashboard-home English copy: dashboard.empty.* (role-specific), dashboard.gm.* (snapshot labels + card titles + drill-down links), dashboard.section.* (shared section labels)"
      contains: "gmNoAlerts"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Exact-parity Spanish counterparts for every new dashboard.* key added to en.ts"
      contains: "gmNoAlerts"
  key_links:
    - from: "dashboard.* keys in en.ts"
      to: "dashboard.* keys in es.ts"
      via: "1:1 key parity enforced by scripts/check-i18n-parity.mjs"
      pattern: "dashboard"
---

<objective>
Establish the complete `dashboard.*` i18n key set that all six redesigned dashboard homes will consume, in both locales, in one place — so the four wave-2 home plans can run in parallel without ever touching (and colliding on) en.ts / es.ts.

This plan writes NO component code. It only adds locale keys: role-specific empty-state copy, the GM portfolio-snapshot labels and card titles, and shared section labels, mirrored exactly across en/es with real Spanish. It also fixes the `'dashboard'` section-key convention in the summary so downstream plans and the verification plan enable the flag consistently.

Purpose: satisfy Success Criterion #4's EN/ES key-parity requirement up front and unblock 32-02..32-05 for parallel execution.
Output: extended `dashboard` block in en.ts and es.ts; `check:i18n-parity` + `verify:i18n-gate` green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md

# The two locale files — the dashboard block currently only holds greeting.* (en.ts ~line 129)
@apps/web/i18n/locales/en.ts
@apps/web/i18n/locales/es.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the full dashboard.* key set to en.ts</name>
  <files>apps/web/i18n/locales/en.ts</files>
  <action>
Under the EXISTING `dashboard:` block in en.ts (which currently only holds `greeting: { morning, afternoon, evening }` — leave those byte-unchanged), add three new sub-blocks. Do NOT touch any other block. Keep alphabetical/logical ordering consistent with the file's style.

1. `dashboard.empty.*` — role-specific empty-state copy (used by 32-02..32-05 for `StateBlock status='empty'`):
   - `housekeeperNoRooms: "No rooms assigned yet"`
   - `housekeeperNoTasks: "No tasks for you right now"`
   - `engineerNoWorkOrders: "No open work orders"`
   - `supervisorNoAlerts: "No alerts right now"`
   - `supervisorNoRequests: "No open guest requests"`
   - `chiefNoWorkOrders: "No open work orders across the team"`
   - `chiefNoAssets: "No high-risk assets"`
   - `frontDeskNoRequests: "No guest requests need attention"`
   - `frontDeskNoLateCheckouts: "No late checkout requests"`
   - `frontDeskNoArrivals: "No arrivals or departures today"`
   - `gmNoAlerts: "No active alerts"`
   - `gmNoData: "No activity to summarize yet"`

2. `dashboard.gm.*` — GM portfolio-snapshot labels, card titles, and drill-down link labels (used by 32-02):
   - `snapshotTitle: "Portfolio snapshot"`
   - `roomsReady: "Rooms ready"`
   - `roomsDirty: "Needs cleaning"`
   - `roomsPickup: "Pickup"`
   - `roomsInspected: "Inspected"`
   - `openWorkOrders: "Open work orders"`
   - `urgentWorkOrders: "Urgent"`
   - `activeGuestRequests: "Active guest requests"`
   - `staffOnShift: "Staff on shift"`
   - `openTasks: "Open tasks"`
   - `alertsTitle: "Risk & alerts"`
   - `opsSummaryTitle: "Operations summary"`
   - `creditUsageTitle: "AI credit usage"`
   - `creditsUsed: "Credits used this month"`
   - `interactions: "AI interactions"`
   - `viewManagementRoi: "View Management ROI"`
   - `viewBilling: "View billing"`
   - `viewAllWorkOrders: "View all work orders"`
   - `viewGuestRequests: "View guest requests"`

3. `dashboard.section.*` — shared section/heading labels reused across homes where a heading isn't already sourced from an existing key (used by 32-02..32-05; only add labels that don't already exist elsewhere in the file — reuse existing `nav.*`/`common.*` where one already fits):
   - `myQueue: "My queue"`
   - `myTasks: "My tasks"`
   - `headsUp: "Heads up"`
   - `atRiskRooms: "At-risk rooms"`
   - `teamOverview: "Team overview"`

Do NOT invent keys beyond this set unless a real component need surfaces during wave 2 — but since wave-2 plans cannot edit en/es, this set is designed to be sufficient. If ordering/lint rules (trailing commas, quote style) apply, match the file's existing conventions exactly.
  </action>
  <verify>cd apps/web && npm run type-check succeeds (en.ts is typed as the source-of-truth locale shape); grep confirms `gmNoAlerts`, `snapshotTitle`, `staffOnShift`, `opsSummaryTitle`, `myQueue` all present under the dashboard block in en.ts; the existing `greeting.morning` key still present (additive check)</verify>
  <done>en.ts dashboard block gains empty.*, gm.*, section.* sub-blocks with all keys above; greeting.* unchanged; type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Mirror every new key into es.ts with real Spanish + prove parity</name>
  <files>apps/web/i18n/locales/es.ts</files>
  <action>
Add the EXACT same key structure to the `dashboard:` block in es.ts with real, natural Spanish translations (hotel-operations register, consistent with the existing es.ts tone — e.g. the existing `greeting`/`common` translations). Suggested values (adjust for naturalness, keep meaning precise):

`empty`: `housekeeperNoRooms: "Aún no hay habitaciones asignadas"`, `housekeeperNoTasks: "No tienes tareas por ahora"`, `engineerNoWorkOrders: "No hay órdenes de trabajo abiertas"`, `supervisorNoAlerts: "No hay alertas por ahora"`, `supervisorNoRequests: "No hay solicitudes de huéspedes abiertas"`, `chiefNoWorkOrders: "No hay órdenes de trabajo abiertas en el equipo"`, `chiefNoAssets: "No hay activos de alto riesgo"`, `frontDeskNoRequests: "No hay solicitudes de huéspedes que atender"`, `frontDeskNoLateCheckouts: "No hay solicitudes de salida tardía"`, `frontDeskNoArrivals: "No hay llegadas ni salidas hoy"`, `gmNoAlerts: "No hay alertas activas"`, `gmNoData: "Aún no hay actividad para resumir"`.

`gm`: `snapshotTitle: "Resumen de la propiedad"`, `roomsReady: "Habitaciones listas"`, `roomsDirty: "Requiere limpieza"`, `roomsPickup: "Recogida"`, `roomsInspected: "Inspeccionadas"`, `openWorkOrders: "Órdenes de trabajo abiertas"`, `urgentWorkOrders: "Urgentes"`, `activeGuestRequests: "Solicitudes de huéspedes activas"`, `staffOnShift: "Personal en turno"`, `openTasks: "Tareas abiertas"`, `alertsTitle: "Riesgos y alertas"`, `opsSummaryTitle: "Resumen de operaciones"`, `creditUsageTitle: "Uso de créditos de IA"`, `creditsUsed: "Créditos usados este mes"`, `interactions: "Interacciones de IA"`, `viewManagementRoi: "Ver ROI de gestión"`, `viewBilling: "Ver facturación"`, `viewAllWorkOrders: "Ver todas las órdenes de trabajo"`, `viewGuestRequests: "Ver solicitudes de huéspedes"`.

`section`: `myQueue: "Mi cola"`, `myTasks: "Mis tareas"`, `headsUp: "Atención"`, `atRiskRooms: "Habitaciones en riesgo"`, `teamOverview: "Resumen del equipo"`.

Then run the parity gates and confirm they pass.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity && npm run verify:i18n-gate && npm run type-check all green (parity proves every new en key has an es counterpart and vice versa); grep confirms `gmNoAlerts`/`snapshotTitle`/`staffOnShift` present in es.ts too</verify>
  <done>es.ts mirrors every new dashboard.* key from Task 1 with real Spanish; check:i18n-parity + verify:i18n-gate + type-check all green</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity`, `npm run verify:i18n-gate`, `npm run type-check` all green from `apps/web`.
- The `dashboard` block contains `empty.*`, `gm.*`, `section.*` in BOTH locales with matching key structure.
- Pre-existing `dashboard.greeting.*` keys unchanged (additive only).
</verification>

<success_criteria>
The complete dashboard-home copy set exists in both locales with real Spanish and passes parity — satisfying the EN/ES half of Phase 32 Success Criterion #4 and unblocking 32-02..32-05 to run in parallel without touching the locale files. Section-key convention `'dashboard'` documented for downstream flag toggling.
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-01-SUMMARY.md` recording: the exact final `dashboard.*` key list (so wave-2 executors reference the real names), any key naming that deviated from this plan, confirmation of parity-gate pass, and an explicit statement that en.ts/es.ts are now frozen for the rest of Phase 32 (wave-2 plans must not edit them) plus the confirmed `'dashboard'` section-flag key.
</output>
