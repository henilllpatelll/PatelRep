---
phase: 32-role-dashboard-homes
plan: 05
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/components/dashboard/FrontDeskDashboard.tsx
autonomous: true

# NOTE: touches only FrontDeskDashboard.tsx — no overlap with 32-02/03/04, runs fully parallel.
# Does NOT edit en.ts/es.ts (all copy from 32-01) and does NOT edit shared modules or frozen
# primitives. This is a single-file plan because FrontDeskDashboard is the most section-dense of
# the non-GM homes (stat strip + room breakdown + guest requests + late checkouts + arrivals/
# departures + DND welfare + lost&found, each with its own state) and carries several mutations —
# restyling all its sections + rebuilding per-section states is a full plan's worth of work.

must_haves:
  truths:
    - "With the 'dashboard' flag ON, front_desk sees the same home (room readiness for arrivals, late-checkout requests, guest requests needing attention — unchanged IA) restyled to v2, with skeleton-card loading and rebuilt empty/error states per section"
    - "The existing front-desk mutations (checkOut / checkIn / welfareCheck / resolve) behave identically — only their chrome is restyled"
    - "With the flag OFF, the dashboard renders byte-behaviorally identical to today (legacy branch untouched)"
    - "The dashboard's information architecture is not restructured — chrome + state redesign only (HOME-01 for front_desk)"
  artifacts:
    - path: "apps/web/components/dashboard/FrontDeskDashboard.tsx"
      provides: "v2-flag-branched restyle of all front-desk sections + rebuilt per-section skeleton/empty/error states, mutations behaviorally unchanged"
      contains: "isSectionRedesigned('dashboard'"
  key_links:
    - from: "FrontDeskDashboard"
      to: "isSectionRedesigned('dashboard', hotel)"
      via: "useHotelStore direct read (pattern 2)"
      pattern: "isSectionRedesigned\\('dashboard'"
    - from: "per-section empty/error regions"
      to: "StateBlock"
      via: "status='empty'/'error' with dashboard.empty.frontDesk* copy + onRetry refetch"
      pattern: "StateBlock"
---

<objective>
Deliver the front_desk slice of HOME-01: restyle the FrontDeskDashboard to the v2 token system and rebuild every section's empty/loading/error states, WITHOUT restructuring its already-correct information priority and WITHOUT changing any mutation behavior. Gate behind `isSectionRedesigned('dashboard', hotel)`; keep the legacy branch byte-identical when off.

FrontDeskDashboard is the most section-dense non-GM home: stat strip (ready%/open-reqs/in-progress/needs-cleaning), room-status breakdown, guest requests, late checkouts, arrivals & departures, DND welfare, and a Lost&Found link — plus checkOut/checkIn/welfareCheck/resolve mutations. Restyle each section's chrome and rebuild its state, keeping data flow and mutations intact.

Purpose: HOME-01 (front_desk) + its slice of Criterion-3 (states) and Criterion-4 (gates).
Output: flag-branched v2 restyle of `FrontDeskDashboard.tsx` with rebuilt per-section states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-role-dashboard-homes/32-CONTEXT.md
@.planning/phases/32-role-dashboard-homes/32-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

@apps/web/components/dashboard/FrontDeskDashboard.tsx
@apps/web/components/dashboard/DashboardGreeting.tsx
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/primitives.tsx
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle FrontDeskDashboard stat strip + room breakdown + arrivals/departures + DND welfare to v2, with states</name>
  <files>apps/web/components/dashboard/FrontDeskDashboard.tsx</files>
  <action>
Read the flag directly: `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('dashboard', hotel)`. Keep the current JSX as the `!v2` legacy branch, byte-behaviorally unchanged. (Both tasks edit this one file sequentially within the same plan — that's fine.)

v2 branch, first half of the home — SAME IA, restyled:
- Stat strip (ready%/open-reqs/in-progress/needs-cleaning) + room-status breakdown + arrivals & departures + DND welfare sections. Apply v2 chrome tokens via className (`duration-base`/`ease-standard`, `focus-visible:ring-[var(--focus-ring)]`, `bg-brand`/`text-brand`). Import `Stat`/`Pill`/`SectionLabel`/`Mono`/`StatusDot`/`Bar` from primitives.tsx AS-IS — do NOT edit primitives.tsx. Do NOT re-tint any room-status token (the room-status breakdown must keep its exact frozen colors).
- Loading: restyle the local `SkeletonRow()` (and the stat-strip skeleton) to the v2 card shell (skeleton, not spinner — do NOT use `StateBlock status='loading'`).
- Empty: arrivals/departures empty → `dashboard.empty.frontDeskNoArrivals`.
- Error: wire `getBoard`/`hotelsApi.getStats` `isError` for these sections → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
- Preserve checkOut/checkIn/welfareCheck mutation wiring exactly — restyle their trigger buttons' chrome only.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast all green; grep confirms `isSectionRedesigned('dashboard'` and `dashboard.empty.frontDeskNoArrivals`; git status shows only FrontDeskDashboard.tsx changed</verify>
  <done>Front-desk stat strip / room breakdown / arrivals-departures / DND welfare v2-restyled with skeleton loading + arrivals empty + error states; mutations intact; legacy branch unchanged; frozen + contrast gates green</done>
</task>

<task type="auto">
  <name>Task 2: Restyle FrontDeskDashboard guest-requests + late-checkouts + Lost&Found sections to v2, with states</name>
  <files>apps/web/components/dashboard/FrontDeskDashboard.tsx</files>
  <action>
Continue the v2 branch (second half of the home) — SAME IA, restyled:
- Guest-requests (open + active) and late-checkouts sections + the Lost&Found link. Apply the same v2 chrome tokens as Task 1; import primitives as-is; no room-status re-tint.
- Loading: same skeleton-card treatment as Task 1 for these sections.
- Empty: guest-requests empty → `dashboard.empty.frontDeskNoRequests`; late-checkouts empty → `dashboard.empty.frontDeskNoLateCheckouts`.
- Error: wire `guestRequestsApi.listRequests` (open + active) and `lateCheckoutApi.list` `isError` → `<StateBlock status='error' error={{ message: t('common.error'), onRetry: () => refetch() }} />`.
- Preserve the `resolve` mutation (and any late-checkout action) wiring exactly — restyle chrome only.
Confirm the whole component's `!v2` legacy branch is still byte-behaviorally identical after both tasks.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `dashboard.empty.frontDeskNoRequests` and `dashboard.empty.frontDeskNoLateCheckouts`; git status shows only FrontDeskDashboard.tsx changed (no primitives/locale edits)</verify>
  <done>Front-desk guest-requests / late-checkouts / Lost&Found sections v2-restyled with skeleton loading + role-appropriate empty + error states; resolve mutation intact; full home renders v2 when flag on, legacy when off; all standing gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Flag ON → every front-desk section on v2 tokens with rebuilt skeleton/empty/error; flag OFF → today's UI unchanged.
- All mutations (checkOut/checkIn/welfareCheck/resolve) behaviorally unchanged; no IA change; no primitives.tsx/locale/shared-or-frozen edits; no room-status token re-tinted.
</verification>

<success_criteria>
HOME-01 met for front_desk: the home renders in the v2 system behind the flag with redesigned per-section empty/loading/error states, unchanged information priority and mutation behavior, passing frozen/contrast/i18n gates.
</success_criteria>

<output>
After completion, create `.planning/phases/32-role-dashboard-homes/32-05-SUMMARY.md` recording: the v2 chrome decisions, skeleton shapes chosen, exact dashboard.* keys consumed per section, confirmation that all four mutations were left behaviorally intact and no frozen/locale/shared file was edited, and any `*-v2` token introduced.
</output>
