---
phase: 13-ai-copilot-reliability
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - apps/web/components/ai/AICopilotBubble.tsx
autonomous: true

must_haves:
  truths:
    - "Confirming an AI-parsed task, work order, guest request, or assignment that fails on the backend shows a visible error in the chat thread, not a silently-reset spinner."
    - "A chat-message send failure surfaces the real ApiClientError detail when available, not always a hardcoded generic string."
    - "The main chat page's pre-existing behavior (client fast-path, intent parsing, SOP Q&A, credit fast-path) is unchanged and regression-verified after this fix."
    - "All 3 AI Copilot entry points (chat bubble, housekeeping assignment sidebar, engineering triage) now share the identical catch -> ApiClientError.message -> visible-in-context surface -> finally-reset error-handling shape."
  artifacts:
    - path: "apps/web/components/ai/AICopilotBubble.tsx"
      provides: "handleConfirmTasks/WorkOrders/GuestRequests/Assignments and sendMessage all catch failures and surface ApiClientError.message inline in the chat thread"
  key_links:
    - from: "apps/web/components/ai/AICopilotBubble.tsx handleConfirmTasks/WorkOrders/GuestRequests/Assignments"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "each handler wraps its aiApi.confirmX call in try/catch, appending an inline error chat bubble on failure"
      pattern: "ApiClientError"
    - from: "apps/web/components/ai/AICopilotBubble.tsx sendMessage"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "catch replaces the hardcoded generic string with err instanceof ApiClientError ? err.message : generic fallback"
      pattern: "ApiClientError"
---

<objective>
Close the last gap in the "reference" error-handling pattern and finalize cross-surface consistency (success criterion 3). `AICopilotBubble.tsx`'s `sendMessage` (lines 269-279) already catches failures and appends a visible inline chat bubble — but the catch is bare (`catch { ... }`) and always shows the hardcoded "Something went wrong. Please try again." even when a real, more specific `ApiClientError.message` is available (e.g. "AI service temporarily unavailable. Please try again later."). Worse, the four confirm handlers (`handleConfirmTasks`, `handleConfirmWorkOrders`, `handleConfirmGuestRequests`, `handleConfirmAssignments`, lines 281-300) have **no catch at all** — a failed `aiApi.confirmX(...)` call propagates up through `ConfirmView`/`TaskConfirmView`'s `try { await onConfirm() } finally { setConfirming(false) }` (also no catch), so the spinner silently resets to the unconfirmed state with zero visible explanation. This is exactly the "silent failure" pattern the whole phase exists to eliminate — on the one surface positioned as the reference pattern for the other two.

This plan runs after 13-01 and 13-02 because it defines the final, audited version of the shared error-handling convention those two plans already established independently (`catch (err) => err instanceof ApiClientError ? err.message : localized fallback`), and because verifying "consistency across all 3 entry points" (success criterion 3) requires those two surfaces to already be fixed for comparison.

Purpose: The chat page — the most-used AI Copilot surface — must never leave a user staring at a silently-reset confirm button with no explanation, and its pre-existing working behavior must not regress in the process.
Output: All chat-page AI interaction paths (send + all 4 confirm flows) surface real errors inline; full regression pass proves fast-path/intent-parsing/SOP-Q&A/credit behavior is unchanged; final cross-surface consistency confirmed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-copilot-reliability/13-01-SUMMARY.md
@.planning/phases/13-ai-copilot-reliability/13-02-SUMMARY.md

@apps/web/components/ai/AICopilotBubble.tsx
@apps/web/lib/api/ai.ts
@apps/web/lib/api/client.ts
@apps/web/lib/ai/clientFastPath.ts
@apps/api/routers/ai_copilot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface real confirm-flow and send-message errors inline in the chat thread</name>
  <files>apps/web/components/ai/AICopilotBubble.tsx</files>
  <action>
    Import `ApiClientError` from `@/lib/api/client`.

    Wrap each of the four confirm handlers (`handleConfirmTasks`, `handleConfirmWorkOrders`, `handleConfirmGuestRequests`, `handleConfirmAssignments`, lines 281-300) in try/catch. On success, keep the existing behavior (invalidate the relevant query, `setTimeout(() => setOpen(false), 1200)`). On failure, catch locally (do not let the error propagate to `ConfirmView`/`TaskConfirmView` — their `finally` already resets the spinner correctly; they don't need to know why it failed) and append an inline AI chat bubble: `setMessages((prev) => [...prev, { id: generateId(), role: 'ai', content: err instanceof ApiClientError ? err.message : 'Something went wrong. Please try again.' }])`. This makes a failed confirm show a clear reason in the thread instead of the button silently reverting to its unconfirmed state with no explanation.

    Fix `sendMessage`'s existing catch (line 274, currently bare `catch { ... }` with a hardcoded string) to: `catch (err) { setMessages((prev) => [...prev, { id: generateId(), role: 'ai', content: err instanceof ApiClientError ? err.message : 'Something went wrong. Please try again.' }]) }` — this surfaces genuine backend detail (e.g. the 503 "AI service temporarily unavailable..." message from `ai_copilot.py`) instead of always showing the same generic text, matching the pattern now used identically in 13-01 (toast) and 13-02 (notice box).
  </action>
  <verify>
    `cd apps/web && npm run type-check` passes.
    With `npm run dev:api` and `npm run dev:web` running, log in and open the AI Copilot bubble:
    - Send "101 needs towels" — confirm it still resolves via the client fast-path instantly (no network call, per `clientFastPath.ts`), producing a `task_preview` with Confirm & Create still working normally (existing behavior unaffected).
    - Send a message that requires a real backend round trip (e.g. "Room 412 needs towels" — routes through `task_creation` intent since "towels"/"needs" match `task_kw`), confirm the task preview appears and clicking "Confirm & Create" succeeds normally.
    - To exercise the failure path: temporarily stop the API dev server, then attempt to send a chat message — confirm the inline bubble now shows the real `ApiClientError` network message instead of always showing the exact same string as before (verify the message text is sourced from `err.message`, not hardcoded inline).
    - Restart the API, create a task preview again, then stop the API before clicking "Confirm & Create" — confirm an inline error bubble now appears in the thread (previously: silent spinner reset with zero explanation).
  </verify>
  <done>All 4 confirm handlers and sendMessage catch failures and append a real, error-specific inline chat bubble instead of silently resetting or showing only a hardcoded generic string.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression verification of unaffected chat-page behavior and final cross-surface consistency check</name>
  <files>none (verification only)</files>
  <action>
    Per success criterion 4 and the Non-Regression Policy, prove Task 1's changes did not alter any existing working chat-page behavior:
    - Client fast-path: send "101 needs towels" and confirm it still resolves instantly via `clientFastPath()` with zero network call (check the Network tab).
    - Off-topic short-circuit: send a message matching `isOffTopic()` (check `clientFastPath.ts` for the exact off-topic detection, or use an unrelated message like "what's the weather") and confirm the `OFF_TOPIC_RESPONSE` still fires without a network call.
    - Task-creation intent: "Room 412 needs towels" still produces a `task_preview` and Confirm & Create still invalidates `['tasks']` and closes the panel.
    - SOP Q&A intent: send a "how do I..." message and confirm it still routes to the `sop_query` branch (200 response — either a real SOP answer or the "No SOPs uploaded yet" canned message, both valid non-error outcomes depending on tenant data).
    - Credit fast-path / quick actions: click one of the role-based quick-action chips (e.g. "At-risk rooms today") and confirm it still sends and resolves normally.
    - Run `cd apps/api && python -m pytest tests/smoke/ -q` — confirm the full suite (baseline count from STATE.md plus the 2 new tests added in 13-01) is still green; Task 1 touches no backend code, so this is a sanity confirmation per the mandatory non-regression policy, not expected to surface anything new.

    Finally, document (in this task's completion notes / summary) the cross-surface consistency confirmation for success criterion 3: all 3 AI Copilot entry points — chat bubble (this plan), housekeeping assignment sidebar (13-01), engineering triage notice (13-02) — now share the identical shape: `catch (err) => err instanceof ApiClientError ? err.message : <localized generic fallback>`, surfaced visibly in-context (inline chat bubble / toast / notice box respectively), with loading state always reset in a `finally` block. Note that the three different visible-surface mechanisms (chat bubble vs. toast vs. notice box) are an intentional, pre-existing presentation difference per surface — not a behavioral inconsistency.
  </action>
  <verify>
    All 5 manual chat-page scenarios above behave exactly as before Task 1's change, with zero console errors.
    `python -m pytest tests/smoke/ -q` passes with no regressions.
  </verify>
  <done>Chat page's fast-path, off-topic short-circuit, task-creation intent, SOP Q&A, and quick-action behaviors are demonstrably unchanged; full backend suite green; cross-surface error-handling consistency confirmed across all 3 AI Copilot entry points.</done>
</task>

</tasks>

<verification>
- `apps/web` type-check clean.
- Live browser walkthrough proves both the new inline-error behavior (Task 1) and zero regression to pre-existing chat behavior (Task 2).
- `apps/api` full smoke suite green.
- All 3 AI Copilot entry points confirmed to share the same catch -> message -> visible-surface -> finally-reset pattern.
</verification>

<success_criteria>
AI-02's cross-surface consistency clause and success criteria 3 and 4 satisfied: the chat page never silently swallows a confirm-flow or send failure, its pre-existing behavior is unchanged, and all 3 AI Copilot entry points now share one consistent, audited error-handling pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-copilot-reliability/13-03-SUMMARY.md`
</output>
