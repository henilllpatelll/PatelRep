---
phase: 13-ai-copilot-reliability
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/app/(dashboard)/engineering/work-orders/page.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "An engineer clicking AI triage sees a visually and textually distinct notice when the AI call fails versus succeeds — a failure is never worded or styled as 'applied'."
    - "The kanban always re-sorts to a safe, deterministic priority order (overdue/unassigned first) regardless of whether the AI call succeeded or failed, since that ordering is real client-side logic, not dependent on the AI response."
    - "A genuine backend/network error surfaces its real detail via the notice, not just a generic unavailability string, when the error came from the API (ApiClientError)."
    - "Adjacent Engineering surfaces sharing this page (Room Board tab, Work Order detail drawer, create-work-order modal) are unaffected by the state-shape change."
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      provides: "handleAITriage distinguishes success/error state; notice JSX renders alert-toned styling on failure"
    - path: "apps/web/i18n/locales/en.ts"
      provides: "aiTriageErrorDetail key added (EN)"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "aiTriageErrorDetail key added (ES)"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx handleAITriage"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "catch (err) sets aiTriageNotice.isError=true using err.message when err instanceof ApiClientError"
      pattern: "ApiClientError"
    - from: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      to: "sortWOs(wos, aiTriageActive)"
      via: "finally block always sets aiTriageActive(true) regardless of success/failure"
      pattern: "setAiTriageActive\\(true\\)"
---

<objective>
Fix the real defect behind AI-02: `handleAITriage` (`apps/web/app/(dashboard)/engineering/work-orders/page.tsx:306-330`) calls `aiApi.chat(...)` but discards the response entirely, then shows one of two hardcoded strings — `aiTriageApplied` on success or `aiTriageFallback` on *any* failure — both worded and styled identically (ai-soft/success-toned box with a Sparkles icon). A real backend or network error is therefore indistinguishable from a genuine success: "AI triage fallback applied while the service is unavailable" reads as a soft success, not an error. Note the real (already-correct) client-side deterministic sort in `sortWOs()` runs unconditionally in the `finally` block regardless of outcome — that part is honest and does not need to change; only the notice's copy/styling must stop disguising failure as success.

Establishes and reuses the canonical error-handling pattern from 13-01 (`catch (err) { ... err instanceof ApiClientError ? err.message : fallback ... }`) so this surface and the housekeeping surface behave identically per success criterion 3.

Purpose: An engineer must be able to tell, at a glance, whether the AI triage call actually worked or failed — a failure must never be worded as if it "applied."
Output: `handleAITriage` sets a typed `{ message, isError }` notice; the notice box renders distinct alert styling on failure; adjacent Engineering surfaces confirmed unaffected.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-copilot-reliability/13-01-SUMMARY.md

@apps/web/app/(dashboard)/engineering/work-orders/page.tsx
@apps/web/lib/api/ai.ts
@apps/web/lib/api/client.ts
@apps/api/routers/ai_copilot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distinguish real AI-triage success from failure in handleAITriage and its notice UI</name>
  <files>apps/web/app/(dashboard)/engineering/work-orders/page.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
    In `work-orders/page.tsx`:
    - Import `ApiClientError` from `@/lib/api/client`.
    - Change `const [aiTriageNotice, setAiTriageNotice] = useState<string | null>(null)` to `useState<{ message: string; isError: boolean } | null>(null)`.
    - In `handleAITriage` (lines 306-330): on success (after the `await aiApi.chat(...)` resolves) set `setAiTriageNotice({ message: t('engineering.workOrdersPage.aiTriageApplied'), isError: false })`. In the `catch (err)` block, replace the current bare `catch {}` with `catch (err) { const detail = err instanceof ApiClientError ? err.message : null; setAiTriageNotice({ message: detail ? t('engineering.workOrdersPage.aiTriageErrorDetail', { error: detail }) : t('engineering.workOrdersPage.aiTriageFallback'), isError: true }) }`. Keep the existing `finally { setAiTriageActive(true); setAiTriageLoading(false) }` unchanged — the deterministic `sortWOs` reordering is real, independent logic and should keep firing regardless of the AI call's outcome; only the notice content/styling must stop implying success on failure.
    - Update the notice-rendering block (~lines 401-406): change `{aiTriageNotice && (...)}` to branch styling on `aiTriageNotice.isError`. Error case: `bg-[var(--alert-soft)] border-[var(--alert-line)] text-[var(--alert)]` with the already-imported `AlertCircle` icon. Success case: keep the existing `bg-ai-soft border-ai-line text-ai` with the `Sparkles` icon. Render `{aiTriageNotice.message}` in both cases.

    In `apps/web/i18n/locales/en.ts` and `es.ts` (`engineering.workOrdersPage` block, ~line 1200):
    - Add `aiTriageErrorDetail: "AI triage unavailable: {{error}}"` (EN) / `"Triaje de IA no disponible: {{error}}"` (ES) directly after the existing `aiTriageFallback` key.
    - Leave `aiTriageApplied` and `aiTriageFallback` text unchanged — `aiTriageFallback` remains the fallback wording for non-`ApiClientError` failures (e.g. network errors), now correctly alert-styled instead of success-styled.
  </action>
  <verify>
    `cd apps/web && npm run type-check` passes.
    With `npm run dev:api` and `npm run dev:web` running, log in as engineer or GM, open Engineering → Work Orders, click "AI triage":
    - Success path (API running normally): confirm the ai-soft/success-toned notice appears with the Sparkles icon, and the kanban columns re-sort with overdue/unassigned work floated first.
    - Failure path: stop the API dev server (or block the `/ai/copilot/chat` request via browser devtools network throttling/blocking) and click "AI triage" again — confirm the notice switches to the alert-toned box with the `AlertCircle` icon and non-"applied" wording (either the `aiTriageErrorDetail` message with real error text, or the generic `aiTriageFallback` string for a network failure), and confirm the kanban still re-sorts (the deterministic fallback ordering still functions even when the AI call fails).
    - Zero console errors in either path.
  </verify>
  <done>Success and failure notices are visually and textually distinct; a real error is never worded as "applied"; the deterministic triage ordering always applies regardless of AI-call outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-verify adjacent Engineering surfaces sharing this page</name>
  <files>none (verification only)</files>
  <action>
    Per the Non-Regression Policy, Task 1 changed the `aiTriageNotice` state shape and this file's imports — confirm no adjacent feature on the same page broke:
    - Switch to the "Room Board" tab (`activeTab === 'room-board'`, renders `EngineeringRoomBoard`) and confirm it still renders with no console errors and no layout shift from the notice-box styling change.
    - Switch back to "Work Orders" tab, click a work order card to open `WorkOrderDetailDrawer`, confirm it still opens/closes correctly.
    - Open the "New Work Order" modal (`CreateWorkOrderModal`) and confirm it still opens and closes without interference from the AI-triage state changes.
    - Confirm the emergency/urgent alert banner (rendered just above the AI-triage notice block) still renders correctly alongside the now-conditionally-styled notice with no visual overlap or collision.
  </action>
  <verify>Manual browser walkthrough of all four adjacent surfaces (Room Board tab, Work Order detail drawer, create-work-order modal, emergency/urgent alert banner) shows correct rendering and zero console errors.</verify>
  <done>All adjacent Engineering surfaces on this page confirmed unaffected by the Task 1 change.</done>
</task>

</tasks>

<verification>
- `apps/web` type-check clean.
- Live browser walkthrough confirms distinct success/failure notice styling and wording, with the deterministic sort always applied.
- Adjacent Engineering page surfaces (Room Board, detail drawer, create modal) confirmed unaffected.
</verification>

<success_criteria>
AI-02 satisfied: an engineer clicking AI triage always gets either a genuine AI-triage-applied notice or a clearly distinct, honestly-worded error notice — never a disguised failure — while the deterministic floor-order prioritization always applies as a real, working fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-copilot-reliability/13-02-SUMMARY.md`
</output>
