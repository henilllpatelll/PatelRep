---
phase: 13-ai-copilot-reliability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(dashboard)/housekeeping/assignments/page.tsx
  - apps/web/app/(dashboard)/ai/page.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "A supervisor clicking 'Auto-Assign with AI' in Housekeeping receives either a correct room-to-housekeeper assignment suggestion or a clear on-screen error message — never a fabricated success or unexplained failure. This must hold on BOTH surfaces that expose the button: the Room Board tab (AssignmentSidebar.tsx, already fixed in 13-01) AND the sibling Assignments tab (app/(dashboard)/housekeeping/assignments/page.tsx, previously missed)."
    - "The error-handling pattern is consistent across all AI Copilot entry points, including the chat page. This must hold for BOTH chat surfaces: the floating AICopilotBubble widget (already fixed in 13-03) AND the dedicated full-page chat at app/(dashboard)/ai/page.tsx, reached via the primary sidebar nav item labeled 'AI Copilot' (previously missed)."
    - "A failed confirm action on the /ai page (tasks, work orders, guest requests, or assignments) shows a visible inline error and leaves the Confirm button in a retryable, non-confirmed state — never a silent reset with no explanation."
    - "A chat-message send failure on the /ai page surfaces the real ApiClientError detail when available, not always a hardcoded generic string."
    - "The /ai page's pre-existing behavior (client fast-path, off-topic short-circuit, task-creation intent round trip, SOP Q&A, quick actions) is unchanged after this fix."
    - "The Assignments tab's pre-existing suggestion-table rendering and date/import controls are unchanged after this fix."
  artifacts:
    - path: "apps/web/app/(dashboard)/housekeeping/assignments/page.tsx"
      provides: "handleAiAutoAssign reads the real data.suggestions response shape and renders an honest suggestion-only UX using the backend's own scenario-specific message, matching the AssignmentSidebar.tsx (13-01) fix pattern exactly"
    - path: "apps/web/app/(dashboard)/ai/page.tsx"
      provides: "sendMessage and all 4 confirm handlers (handleConfirmTasks/WorkOrders/GuestRequests/Assignments) catch failures, surface ApiClientError.message inline in the chat thread, and (for confirm handlers) rethrow so ConfirmView/TaskConfirmView correctly skip the false-success checkmark, matching the AICopilotBubble.tsx (13-03) fix pattern exactly"
    - path: "apps/web/i18n/locales/en.ts"
      provides: "assignmentsPage copy updated to suggestion-only language (EN), noRoomsNeedWork fallback key added, unused aiSuccessGeneric key removed"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "assignmentsPage copy updated to suggestion-only language (ES), noRoomsNeedWork fallback key added, unused aiSuccessGeneric key removed"
  key_links:
    - from: "apps/web/app/(dashboard)/housekeeping/assignments/page.tsx handleAiAutoAssign"
      to: "POST /housekeeping/ai-suggest-assignments"
      via: "housekeepingApi.aiSuggestAssignments(date) reading result.data.suggestions and result.data.message, summing room_count"
      pattern: "data\\?\\.suggestions"
    - from: "apps/web/app/(dashboard)/housekeeping/assignments/page.tsx handleAiAutoAssign"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "catch (err) surfaces err instanceof ApiClientError ? err.message : localized fallback"
      pattern: "ApiClientError"
    - from: "apps/web/app/(dashboard)/ai/page.tsx handleConfirmTasks/WorkOrders/GuestRequests/Assignments"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "each handler wraps its aiApi.confirmX call in try/catch, appends an inline error chat bubble on failure via err instanceof ApiClientError ? err.message : generic fallback, then rethrows"
      pattern: "ApiClientError"
    - from: "apps/web/app/(dashboard)/ai/page.tsx handleConfirmTasks/WorkOrders/GuestRequests/Assignments"
      to: "ConfirmView / TaskConfirmView submit (try { await onConfirm(); setDone(true) } finally { setSaving(false) })"
      via: "rethrown error causes await onConfirm() to reject, so setDone(true) is skipped and finally resets the button to a retryable, non-confirmed state"
      pattern: "throw err"
    - from: "apps/web/app/(dashboard)/ai/page.tsx sendMessage"
      to: "apps/web/lib/api/client.ts ApiClientError"
      via: "catch replaces the bare catch { addAiMsg('Something went wrong...') } with err instanceof ApiClientError ? err.message : generic fallback"
      pattern: "ApiClientError"
---

<objective>
Close the two gaps identified in `.planning/phases/13-ai-copilot-reliability/13-VERIFICATION.md` (2026-08-03) by replicating the exact, already-proven fix patterns from 13-01 and 13-03 onto the two duplicate UI surfaces those plans missed. This is NOT new design work — it is applying an established pattern to files that were never touched.

**Gap 1 (partial):** 13-01 fixed `AssignmentSidebar.tsx` (Room Board tab), but missed the sibling "Auto-Assign with AI" button on the Assignments tab (`apps/web/app/(dashboard)/housekeeping/assignments/page.tsx`). Its `handleAiAutoAssign` (lines 82-100) still reads the nonexistent `data.assignments_created`/`data.count` keys from the same `/housekeeping/ai-suggest-assignments` response, always shows the fabricated generic toast (`aiSuccessGeneric`), still invalidates the assignments-page query cache implying a persisted change that never happened, and catches errors with a bare `err?.message` instead of the canonical `ApiClientError` pattern.

**Gap 2 (failed):** 13-03 fixed `AICopilotBubble.tsx` (the floating widget), believing it to be "the main chat page." The app has a second, separate, full-page chat implementation at `apps/web/app/(dashboard)/ai/page.tsx`, reached via the primary sidebar nav item labeled "AI Copilot" — and `AICopilotBubble.tsx` itself hides on this route (`isAiPage` check), proving this is the real dedicated chat page. It was never touched by any of the 3 original plans and still has the identical bugs 13-03 was created to eliminate: `sendMessage`'s catch is bare (lines 287-288, hardcoded generic string, no `ApiClientError` ever surfaced), and all 4 confirm handlers (`handleConfirmTasks`/`WorkOrders`/`GuestRequests`/`Assignments`, lines 294-309) have zero try/catch, so a failed confirm silently propagates through `ConfirmView`/`TaskConfirmView`'s uncaught `try { await onConfirm(); setDone(true) } finally { setSaving(false) }` — resetting the button/spinner with no visible explanation.

Purpose: A supervisor or any AI Copilot user must get the same trustworthy behavior no matter which of the two duplicate surfaces they happen to use — never a fabricated success, never a silent failure, on any of the app's AI Copilot entry points.
Output: `AssignmentsPage`'s AI auto-assign button and `app/(dashboard)/ai/page.tsx`'s chat send/confirm flows both match the already-audited error-handling pattern used everywhere else in Phase 13; no regression to either surface's pre-existing working behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-copilot-reliability/13-VERIFICATION.md
@.planning/phases/13-ai-copilot-reliability/13-01-SUMMARY.md
@.planning/phases/13-ai-copilot-reliability/13-03-SUMMARY.md

@apps/web/app/(dashboard)/housekeeping/assignments/page.tsx
@apps/web/components/housekeeping/AssignmentSidebar.tsx
@apps/web/app/(dashboard)/ai/page.tsx
@apps/web/components/ai/AICopilotBubble.tsx
@apps/web/lib/api/housekeeping.ts
@apps/web/lib/api/client.ts
@apps/web/lib/ai/clientFastPath.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the 13-01 AssignmentSidebar fix pattern to the Assignments tab's handleAiAutoAssign</name>
  <files>apps/web/app/(dashboard)/housekeeping/assignments/page.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
    In `apps/web/app/(dashboard)/housekeeping/assignments/page.tsx`:
    - Import `ApiClientError` from `@/lib/api/client`.
    - In `handleAiAutoAssign` (lines 82-100), delete `const count = (result as any)?.data?.assignments_created ?? (result as any)?.data?.count ?? null` (neither key exists in the real `/housekeeping/ai-suggest-assignments` response). Instead read `const suggestions = (result as any)?.data?.suggestions ?? []` and compute `const roomCount = suggestions.reduce((sum: number, s: any) => sum + (s.room_count ?? s.rooms?.length ?? 0), 0)` — identical logic to `AssignmentSidebar.tsx`'s Task 1 fix from 13-01.
    - Remove the `queryClient.invalidateQueries({ queryKey: ['housekeeping-assignments-page', date] })` call from the success path — `suggest_assignments` is read-only and never writes to the database (confirmed in 13-01), so invalidating this cache falsely implies a completed action.
    - Replace the success branch: if `roomCount === 0`, call `toast.info((result as any)?.data?.message || t('housekeeping.assignmentsPage.noRoomsNeedWork'))` — sourcing the backend's own scenario-specific message (distinguishing "no rooms need assignment" from "no active housekeepers found") rather than a single hardcoded string. If `roomCount > 0`, call `toast.success(roomCount === 1 ? t('housekeeping.assignmentsPage.aiSuccessOne', { count: roomCount }) : t('housekeeping.assignmentsPage.aiSuccessOther', { count: roomCount }))`.
    - Standardize the catch block to: `catch (err) { toast.error(err instanceof ApiClientError ? err.message : t('housekeeping.assignmentsPage.aiFailure')) }`, replacing the current bare `err?.message` fallback.
    - Rename the button label key usage from implying a completed action: change `autoAssignWithAi` copy to "Suggest Assignments with AI" (and Spanish equivalent) to match the sidebar's 13-01 wording, since the endpoint only returns suggestions.

    In `apps/web/i18n/locales/en.ts` and `apps/web/i18n/locales/es.ts` (both have an `assignmentsPage` block around line 720):
    - Add `noRoomsNeedWork: "No rooms currently need assignment."` (EN) / `"Ningún cuarto necesita asignación por ahora."` (ES) as a translated fallback used only if `result.data.message` is missing.
    - Update `autoAssignWithAi` from "Auto-Assign with AI" to "Suggest Assignments with AI" (EN) / from "Asignar automáticamente con IA" to "Sugerir asignaciones con IA" (ES).
    - Remove `aiSuccessGeneric` — after this fix it is unreferenced (the `roomCount === 0` branch now uses `toast.info` + `noRoomsNeedWork`, and the `roomCount > 0` branch always uses the count-specific strings). Grep to confirm no other usage before deleting.
    - Leave `aiSuccessOne`/`aiSuccessOther`/`aiFailure`/`thinking` wording as-is (already say "suggested", not "assigned" or "created").
  </action>
  <verify>
    `cd apps/web && npm run type-check` passes with no new errors.
    Grep confirms `assignments_created`, the broken `?.data?.count` lookup, and `aiSuccessGeneric` no longer appear in `apps/web/app/(dashboard)/housekeeping/assignments/page.tsx` or the locale files.
    With `npm run dev:api` and `npm run dev:web` running, log in as a GM or housekeeping_supervisor, navigate to `/housekeeping/assignments`, click "Suggest Assignments with AI":
    - If rooms need work, confirm the toast shows an accurate room count matching the Network tab's actual response body.
    - If the board has zero dirty/pickup/in-progress rooms, confirm an info-toned toast reads the backend's exact message "No rooms currently need assignment."
    - If rooms need work but zero active housekeepers are on shift, confirm the info toast instead reads "No active housekeepers found for this property" — proving the message is sourced from `result.data.message`, not a single collapsed string.
    - Confirm the assignments table does NOT visibly change after clicking (nothing was persisted).
    - Confirm zero console errors.
    - Separately, re-verify the Room Board tab's AI button (`/housekeeping`, `AssignmentSidebar.tsx`) still behaves exactly as it did after 13-01 — this task does not touch that file, so this is a quick non-regression spot-check only.
  </verify>
  <done>Toast messages on the Assignments tab accurately reflect the real `data.suggestions`/`data.message` response; both distinct zero-suggestion cases show their own honest, backend-accurate info message; no cache invalidation implies a persisted change; errors surface via `ApiClientError.message`; button/locale copy matches the sidebar's suggestion-only language.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 13-03 AICopilotBubble fix pattern to the full-page chat at app/(dashboard)/ai/page.tsx</name>
  <files>apps/web/app/(dashboard)/ai/page.tsx</files>
  <action>
    Import `ApiClientError` from `@/lib/api/client`.

    Wrap each of the four confirm handlers (`handleConfirmTasks`, `handleConfirmWorkOrders`, `handleConfirmGuestRequests`, `handleConfirmAssignments`, lines 294-309) in try/catch. On success, keep the existing behavior (`await aiApi.confirmX(...)` then `queryClient.invalidateQueries(...)`). On failure: catch the error, append an inline AI chat bubble via the existing `addAiMsg` helper — `addAiMsg(err instanceof ApiClientError ? err.message : 'Something went wrong. Please try again.')` — **then rethrow it** (`throw err`). Do NOT swallow the error: this page's `ConfirmView`/`TaskConfirmView` (lines 46-66, 68-87) run `try { await onConfirm(); setDone(true) } finally { setSaving(false) }` — `setDone(true)` only fires if `onConfirm()` does not throw. If the confirm handler swallows the error instead of rethrowing, `await onConfirm()` resolves normally and `setDone(true)` fires anyway, showing a false success checkmark (e.g. "3 tasks created.") at the exact moment the inline chat bubble says it failed. Rethrowing ensures `await onConfirm()` still rejects, `setDone(true)` is correctly skipped, and the existing `finally { setSaving(false) }` resets the "Confirm & Create" button to its normal, retryable (non-confirmed) state. `ConfirmView` and `TaskConfirmView` themselves must NOT be modified — this is exactly the pattern 13-03 applied to `AICopilotBubble.tsx`'s identical `ConfirmView`/`TaskConfirmView` components.

    Fix `sendMessage`'s existing catch (lines 287-288, currently `catch { addAiMsg('Something went wrong. Please try again.') }` — bare, no error captured) to: `catch (err) { addAiMsg(err instanceof ApiClientError ? err.message : 'Something went wrong. Please try again.') }` — surfacing genuine backend detail (e.g. a 503 "AI service temporarily unavailable..." message) instead of always showing the same generic text, matching the pattern already applied to `AICopilotBubble.tsx` in 13-03.
  </action>
  <verify>
    `cd apps/web && npm run type-check` passes.
    With `npm run dev:api` and `npm run dev:web` running, log in and navigate to the "AI Copilot" sidebar nav item (`/ai`):
    - Send "101 needs towels" — confirm it still resolves via the client fast-path instantly (no network call), producing a task preview with "Confirm & Create" still working normally.
    - Send a message that requires a real backend round trip and skips every `clientFastPath` pattern (e.g. "Multiple rooms need turndown service before tonight's VIP arrivals") — confirm the task preview appears via a genuine `/ai/copilot/chat` network call and "Confirm & Create" succeeds normally.
    - To exercise the failure path: stop the API dev server, then send a chat message — confirm the inline bubble now shows the real `ApiClientError` message instead of always the same hardcoded string.
    - Restart the API, create a task preview again (non-fast-path message), then stop the API before clicking "Confirm & Create" — confirm an inline error bubble appears in the thread AND the "Confirm & Create" button reverts to its normal clickable (non-confirmed) state, not a false "N task(s) created." checkmark.
    - Restart the API and click "Confirm & Create" again on the same still-open preview — confirm it now succeeds, proving the button is genuinely retryable.
  </verify>
  <done>`sendMessage` and all 4 confirm handlers on `/ai` catch failures and surface the real `ApiClientError` message inline instead of silently resetting or showing only a hardcoded generic string; on confirm failure the error is rethrown so `ConfirmView`/`TaskConfirmView` correctly skip the success checkmark and the button reverts to a retryable, non-confirmed state.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression check on both surfaces and final cross-surface consistency confirmation</name>
  <files>none (verification only)</files>
  <action>
    Per the Non-Regression Policy, prove Tasks 1-2 did not alter any existing working behavior on either surface:

    On `/housekeeping/assignments`:
    - Confirm the date picker, "Today" button, and "Import from Opera" modal still work unchanged.
    - Confirm the housekeeper table's expand/collapse row behavior and room-status badges render unchanged.
    - Confirm the empty-state block (`StateBlock` with `emptyTitle`/`emptySubtitle`) still renders correctly when zero assignments exist for a date.

    On `/ai`:
    - Client fast-path ("101 needs towels") still resolves instantly with zero network call.
    - Off-topic short-circuit (e.g. "what's the weather") still fires `OFF_TOPIC_RESPONSE` without a network call.
    - Task-creation intent (a message avoiding every `clientFastPath` pattern) still produces a `task_preview` via a genuine `/ai/copilot/chat` call, and "Confirm & Create" still invalidates `['tasks']`.
    - SOP Q&A intent ("how do I...") still routes to the `sop_query` branch (200 response, real SOP answer or the canned "no SOPs uploaded" message).
    - A quick-action chip (e.g. "At-risk rooms today") still sends and resolves normally.
    - Chat history persistence (localStorage save/restore keyed by `copilot-page-{userId}-{date}`) still works across a page refresh.

    Run `cd apps/api && python -m pytest tests/smoke/ -q` — confirm the full suite (baseline from STATE.md) is still green; neither task touches backend code, so this is a sanity confirmation per the mandatory non-regression policy.

    Finally, document the closure of both verification gaps: confirm all 4 AI Copilot UI surfaces now share the identical `catch (err) => err instanceof ApiClientError ? err.message : <localized generic fallback>` shape — AssignmentSidebar.tsx (13-01), work-order triage (13-02), AICopilotBubble.tsx (13-03), and now AssignmentsPage + app/(dashboard)/ai/page.tsx (this plan) — surfaced visibly in-context per surface (toast / notice box / inline chat bubble), with loading state always reset in a `finally` block, and confirm-style flows rethrowing so parent success-state logic correctly stays in its failed/retryable state on error.
  </action>
  <verify>
    All manual scenarios above behave exactly as they did before this plan's changes, with zero console errors.
    `python -m pytest tests/smoke/ -q` passes with no regressions.
  </verify>
  <done>Both surfaces' pre-existing behavior is demonstrably unchanged; full backend suite green; both verification gaps from 13-VERIFICATION.md are closed with the identical, already-audited error-handling pattern used everywhere else in Phase 13.</done>
</task>

</tasks>

<verification>
- `apps/web` type-check clean.
- Live browser walkthrough confirms honest suggestion-only UX on `/housekeeping/assignments` (has-rooms, zero-rooms, no-staff cases) and correct inline-error + retryable-button behavior on `/ai` (send failure, confirm failure for all 4 confirm types), with zero console errors.
- `apps/api` full smoke suite green, no regressions.
- Grep confirms no remaining reference to the nonexistent `assignments_created`/`data?.count` response keys, and no remaining bare `catch { ... }` or un-caught `await aiApi.confirmX(...)` calls in `app/(dashboard)/ai/page.tsx`.
- Neither `AssignmentSidebar.tsx`, `AICopilotBubble.tsx`, nor `work-orders/page.tsx` were modified by this plan (already correctly fixed in 13-01/13-02/13-03).
</verification>

<success_criteria>
Both gaps from `13-VERIFICATION.md` are closed: (1) a supervisor using either "Auto-Assign with AI" surface (Room Board tab or Assignments tab) always gets an accurate suggestion summary, an honest scenario-specific "nothing to suggest" message, or a clear visible error — never a fabricated success; (2) the error-handling pattern is now genuinely consistent across all AI Copilot entry points, including BOTH chat surfaces (floating bubble and the dedicated `/ai` page reached via the primary sidebar nav) — no silent failures, no false success checkmarks, on any of the app's 4 AI Copilot UI surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-copilot-reliability/13-04-SUMMARY.md`
</output>
