---
phase: 17-backlog-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/services/guest_recovery/contracts.py
  - apps/api/tests/test_management_roi.py
autonomous: true

must_haves:
  truths:
    - "The Management ROI page's 'Minutes / Occupied Room' tooltip shows plain, human-readable language, not a raw Python-variable-name formula string."
    - "The GM-facing transparency intent (what the number means) is preserved — the tooltip still explains the calculation, just not as pseudo-code."
  artifacts:
    - path: "apps/api/services/guest_recovery/contracts.py"
      provides: "calculate_housekeeping_efficiency()'s definition field is human-readable prose"
      contains: "Average minutes"
    - path: "apps/api/tests/test_management_roi.py"
      provides: "Regression test locking in that the definition string is prose, not a snake_case formula"
      contains: "definition"
---

<objective>
Close the Management ROI half of UX-04: the page no longer leaks its internal calculation formula string to the UI.

Root cause (confirmed by direct read this session): `apps/api/services/guest_recovery/contracts.py::calculate_housekeeping_efficiency()` line 322 returns
```python
"definition": "minutes_per_occupied_room = total clean minutes / distinct (room, day) clean sessions",
```
The docstring (lines 274-275) explains this was *intentional* — GM-facing transparency about what the number means — but the string itself is a literal pseudo-code assignment expression using the internal Python variable name (`minutes_per_occupied_room`), not prose. `apps/web/app/(dashboard)/management-roi/page.tsx` line 291 renders it verbatim as a stat tooltip (`hint={housekeeping?.definition}`) — the only `hint={...?.definition}` usage on the page and the only `"definition"` key returned anywhere in this file.

Purpose: keep the transparency intent, drop the code-like notation.

Output: the `definition` string is rewritten to plain language; no frontend change needed since the render site already just displays whatever string the backend sends.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-backlog-cleanup/17-RESEARCH.md

# calculate_housekeeping_efficiency(), line 322 specifically; also skim other calculate_* functions
# in this file for any other "definition"-shaped field that might have the same leak pattern
@apps/api/services/guest_recovery/contracts.py

# Existing test at test_housekeeping_efficiency_minutes_per_occupied_room (line 198), which already
# asserts "definition" in result at line 222 — extend it, do not duplicate a new test file
@apps/api/tests/test_management_roi.py

# Confirms hint={housekeeping?.definition} is the only render site (no frontend change needed)
@apps/web/app/(dashboard)/management-roi/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the leaked formula string to plain GM-facing language</name>
  <files>apps/api/services/guest_recovery/contracts.py</files>
  <action>
    In `calculate_housekeeping_efficiency()`, change line 322 from:
    ```python
    "definition": "minutes_per_occupied_room = total clean minutes / distinct (room, day) clean sessions",
    ```
    to:
    ```python
    "definition": "Average minutes spent cleaning each occupied room, based on completed clean sessions.",
    ```
    Keep everything else in the returned dict unchanged (`occupied_room_days`, `total_clean_minutes`, `minutes_per_occupied_room`, `by_room_type`).

    While in this file, grep the other `calculate_*` functions (`calculate_downtime_revenue_impact`, `calculate_repeat_failures`, `calculate_pm_compliance`, `calculate_inspection_trends`, `calculate_training_readiness`, `calculate_room_downtime_hours`, `project_seven_day_labor_forecast`) for any other `"definition"` or similarly formula-shaped explanatory field. If one exists AND is actually rendered by the frontend (check `apps/web/app/(dashboard)/management-roi/page.tsx` for a matching `hint={...}` usage), apply the same plain-language fix; if none exists or none is wired to a frontend render, make no further change and note that in the summary.
  </action>
  <verify>
    `grep -n "definition" apps/api/services/guest_recovery/contracts.py` shows the new prose string, no `=` assignment syntax, no snake_case variable name.
  </verify>
  <done>The definition string returned by calculate_housekeeping_efficiency() is plain, human-readable language with no internal variable-name/formula notation.</done>
</task>

<task type="auto">
  <name>Task 2: Lock in the fix with a regression assertion + live verify</name>
  <files>apps/api/tests/test_management_roi.py</files>
  <action>
    Extend the existing `test_housekeeping_efficiency_minutes_per_occupied_room` test (line 198, which already asserts `"definition" in result` at line 222) to also assert the string no longer contains the internal variable name or formula syntax:
    ```python
    assert "definition" in result
    assert "minutes_per_occupied_room =" not in result["definition"]
    assert "Average minutes" in result["definition"]
    ```
    Place these additional asserts right after the existing `assert "definition" in result` line — do not restructure the rest of the test.
  </action>
  <verify>
    `cd apps/api && python -m pytest tests/test_management_roi.py -q` passes.
    `cd apps/api && python -m pytest tests/ -q` — full suite green, no regressions.
    Live check: with dev servers running, load `/management-roi` as a GM, hover/open the "Minutes / Occupied Room" stat's tooltip, confirm it now reads the plain-language sentence instead of the raw formula.
  </verify>
  <done>Regression test locks in the plain-language definition string; full suite green; live tooltip confirmed showing GM-readable text.</done>
</task>

</tasks>

<verification>
- `cd apps/api && python -m pytest tests/ -q` — full suite green.
- Live browser check on `/management-roi`: the "Minutes / Occupied Room" tooltip reads plain language, not a formula.
</verification>

<success_criteria>
The Management ROI page no longer leaks its internal calculation formula string to the UI (ROADMAP Phase 17 success criterion 3, ROI half / UX-04).
</success_criteria>

<output>
After completion, create `.planning/phases/17-backlog-cleanup/17-04-SUMMARY.md`
</output>
</content>
