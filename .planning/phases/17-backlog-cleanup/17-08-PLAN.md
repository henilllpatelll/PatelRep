---
phase: 17-backlog-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/web/app/(dashboard)/housekeeping/inspections/page.tsx"
autonomous: true

must_haves:
  truths:
    - "A housekeeping_supervisor appears as a selectable option in the inspection re-assign drawer's staff dropdown, matching the main Housekeeping board's assignment picker which already includes supervisors."
    - "A plain housekeeper still appears in the same dropdown — the fix adds supervisors, it does not narrow the existing housekeeper set."
  artifacts:
    - path: "apps/web/app/(dashboard)/housekeeping/inspections/page.tsx"
      provides: "Re-assign drawer's housekeepers filter includes both housekeeper and housekeeping_supervisor roles"
      contains: "housekeeping_supervisor"
---

<objective>
Close STAFF-01: supervisors (`housekeeping_supervisor` role) appear as assignable staff in the housekeeper room-assignment picker (product decision resolved 2026-08-03: include supervisors — this is locked, not up for re-litigation).

Root cause (confirmed by direct read this session): there are two distinct "assign a housekeeper to a room" pickers in `apps/web`, and they're inconsistent. The **main assignment picker** — `HousekeeperBar` in `apps/web/app/(dashboard)/housekeeping/page.tsx` line 97 — is already correct: `.filter((s: any) => s.role === 'housekeeper' || s.role === 'housekeeping_supervisor')`. Backend support already exists too (`_ensure_housekeeper()` in `housekeeping.py:93-104`, the AI-suggest endpoint in `ai_copilot.py:216,238` both already allow both roles). The **inspection re-assign picker** — `apps/web/app/(dashboard)/housekeeping/inspections/page.tsx` line 170 — still excludes supervisors: `const housekeepers = (staffData?.data?.staff ?? []).filter((s) => s.role === 'housekeeper')`. This feeds the `<select>` dropdown (lines ~502-516) in the "re-assign drawer" shown after a failed inspection.

**Scope decision (explicit, per 17-RESEARCH.md Open Question 3):** `apps/web/app/(dashboard)/tasks/page.tsx:240`'s housekeeper-only filter is for **task assignment**, a different feature (not the room-assignment picker REQUIREMENTS.md's wording specifically names). This plan does NOT touch `tasks/page.tsx` — that filter is explicitly OUT of scope for STAFF-01, documented here rather than silently caught or silently missed.

Purpose: make the inspection re-assign picker consistent with the already-correct main picker, no backend change needed (`GET /staff` already returns supervisors in the list — the role filter there gates the *caller*, not the returned rows).

Output: `inspections/page.tsx:170`'s filter widened to include `housekeeping_supervisor`, matching `housekeeping/page.tsx:97`'s existing pattern exactly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-backlog-cleanup/17-RESEARCH.md

# The file to fix — line 170's filter, and the <select> at lines ~502-516 that consumes it
@apps/web/app/(dashboard)/housekeeping/inspections/page.tsx

# Reference: the already-correct pattern to mirror exactly (line 97)
@apps/web/app/(dashboard)/housekeeping/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the re-assign picker's role filter to include supervisors</name>
  <files>apps/web/app/(dashboard)/housekeeping/inspections/page.tsx</files>
  <action>
    Change line 170 from:
    ```tsx
    const housekeepers = (staffData?.data?.staff ?? []).filter((s) => s.role === 'housekeeper')
    ```
    to:
    ```tsx
    const housekeepers = (staffData?.data?.staff ?? []).filter((s) => s.role === 'housekeeper' || s.role === 'housekeeping_supervisor')
    ```
    This is the exact same pattern already used in `apps/web/app/(dashboard)/housekeeping/page.tsx` line 97. No other line in this file needs to change — the `<select>` dropdown (~lines 502-516) already just maps over whatever `housekeepers` contains, and no backend change is needed (`GET /staff` already returns supervisor rows in the same response).
  </action>
  <verify>
    `grep -n "const housekeepers" apps/web/app/\(dashboard\)/housekeeping/inspections/page.tsx` shows the widened filter.
    `npm run type-check --workspace=@patelrep/web` passes.
    Live check: with dev servers running, log in as a role that can view Inspections, fail an inspection to open the re-assign drawer, confirm the staff dropdown now lists both housekeepers and housekeeping_supervisors, and that selecting a supervisor and submitting the re-assign succeeds (the backend's `_ensure_housekeeper()` already accepts supervisors, so this should work without any API change).
  </verify>
  <done>The inspection re-assign picker's dropdown includes housekeeping_supervisor staff alongside housekeeper staff; re-assigning to a supervisor succeeds end-to-end; tasks/page.tsx untouched.</done>
</task>

</tasks>

<verification>
- `npm run type-check --workspace=@patelrep/web` passes.
- Live browser check: the Inspections re-assign drawer's staff dropdown includes housekeeping_supervisor staff, and re-assigning to a supervisor succeeds end-to-end.
- `apps/web/app/(dashboard)/tasks/page.tsx:240` is confirmed untouched (explicit scope exclusion, documented in this plan's objective).
</verification>

<success_criteria>
Supervisors appear as assignable staff in the housekeeper room-assignment picker (ROADMAP Phase 17 success criterion 5, STAFF-01 half) — consistently across both the main board picker and the inspection re-assign picker.
</success_criteria>

<output>
After completion, create `.planning/phases/17-backlog-cleanup/17-08-SUMMARY.md`
</output>
</content>
