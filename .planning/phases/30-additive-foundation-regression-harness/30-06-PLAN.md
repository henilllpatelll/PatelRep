---
phase: 30-additive-foundation-regression-harness
plan: 06
type: execute
wave: 4
depends_on: ["30-01", "30-03", "30-04", "30-05"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "The frozen-file guard, dark-mode contrast gate, and EN/ES key-parity gate run as CI jobs on every PR and push to main"
    - "The Room-Board pixel-diff regression runs in CI against the committed baseline as the phase exit gate"
    - "A regression, frozen, contrast, or i18n-parity failure fails CI (not continue-on-error)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "New CI jobs: frozen-guard, contrast, i18n-parity, room-board-regression"
      contains: "check-frozen-files"
  key_links:
    - from: ".github/workflows/ci.yml frozen-guard/contrast/i18n-parity jobs"
      to: "the npm scripts from 30-03 and 30-05"
      via: "npm run check:* invocations"
      pattern: "check:frozen-files|check:contrast|check:i18n-parity"
    - from: ".github/workflows/ci.yml room-board-regression job"
      to: "apps/web/playwright.regression.config.ts + committed baseline"
      via: "playwright test --config=playwright.regression.config.ts"
      pattern: "playwright.regression.config.ts"
---

<objective>
Wire all four new verification gates into the single CI workflow (this plan exclusively owns `.github/workflows/ci.yml` to avoid same-file conflicts across the parallel gate plans). Adds the frozen-file guard, dark-mode contrast, EN/ES key-parity, and Room-Board pixel-diff regression as real, failing CI jobs. Depends on every gate-producing plan (30-01, 30-03, 30-04, 30-05).

Purpose: Make the freeze and the regression harness structural — enforced automatically on every PR/push, per the RBAC-05..08 precedent (CI-enforced, not documentation-only).
Output: Extended `ci.yml` with 4 new jobs + updated PR-status summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-additive-foundation-regression-harness/30-CONTEXT.md
@.planning/phases/30-additive-foundation-regression-harness/30-RESEARCH.md

# The CI workflow being extended
@.github/workflows/ci.yml

# The scripts/configs being wired (from 30-01, 30-03, 30-05)
@apps/web/playwright.regression.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static-analysis gate jobs (frozen, contrast, i18n-parity)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Append three new jobs to the `jobs:` map in `.github/workflows/ci.yml`, mirroring the existing `lint-web` job shape (checkout, setup-node@v7 node 22, `cd apps/web && npm ci`, then the check). Do NOT modify existing jobs; append distinct blocks:
- `frozen-guard`: runs `cd apps/web && npm run check:frozen-files`.
- `contrast`: runs `cd apps/web && npm run check:contrast`.
- `i18n-parity`: runs `cd apps/web && npm run check:i18n-parity`.
These are hard gates — NOT `continue-on-error`. They need no build (pure static analysis over source), so they can run without `needs:` and in parallel with lint.
Also (per RESEARCH.md open question #3, low-cost) wire the two currently-orphaned scripts by adding `npm run verify:i18n-gate` and `npm run check:floor-copy` steps into the `i18n-parity` job (or a sibling step) so they finally run in CI too.
  </action>
  <verify>ci.yml is valid YAML (e.g. `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"`); the three jobs invoke the three npm scripts; none are continue-on-error; existing jobs unchanged</verify>
  <done>frozen-guard, contrast, and i18n-parity jobs exist as hard CI gates invoking the 30-03/30-05 npm scripts; the two orphaned i18n scripts are also wired</done>
</task>

<task type="auto">
  <name>Task 2: Add Room-Board regression job + update PR status summary</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Append a `room-board-regression` job mirroring `test-web-public-smoke` (checkout, setup-node 22 with npm cache, `npm ci`, `npx playwright install --with-deps chromium`, then `npx playwright test --config=playwright.regression.config.ts`). This is the FOUND-03 exit gate: it diffs the 3 excluded surfaces against the committed baseline and fails on any drift. Hard gate, not continue-on-error.

**Env/secrets (updated per 30-01's fixture-tenant data-source decision):** the harness now authenticates as the two regression fixture-tenant users and runs against the fixture-backed deployment. Provide via GitHub secrets, referenced as `${{ secrets.* }}` (never hardcode):
- `REGRESSION_GM_EMAIL`, `REGRESSION_GM_PASSWORD`
- `REGRESSION_SUP_EMAIL`, `REGRESSION_SUP_PASSWORD`
- `PLAYWRIGHT_BASE_URL` (the deployment reading the fixture-seeded Supabase project)
These SUPERSEDE the earlier single `PLAYWRIGHT_TEST_EMAIL`/`PLAYWRIGHT_TEST_PASSWORD` pair mentioned in the original plan — use the four fixture-credential secrets + base URL. The fixture tenant is never operated on, so the underlying board data is stable and the diff is deterministic; the CI job does NOT seed data (the fixture is seeded once during 30-01 and persists).

Add a comment in the job that it requires these secrets configured in the repo; if a required secret is absent the job should fail loudly (surfacing the missing config), not silently skip — a skipped regression gate is worse than a failing one. (Configuring the actual secret values is a repo-admin action outside code; flag it in the SUMMARY.)

Update the `pr-comment` job's `needs:` array to include the four new jobs (`frozen-guard`, `contrast`, `i18n-parity`, `room-board-regression`) and add corresponding rows to its status-table body so the PR summary reflects them.
  </action>
  <verify>ci.yml is valid YAML; the regression job runs the regression config and references the four fixture-credential secrets + PLAYWRIGHT_BASE_URL (no hardcoded creds); pr-comment `needs:` and table include all four new jobs</verify>
  <done>room-board-regression runs the pixel-diff against the baseline as a hard exit gate using the fixture-tenant secret creds; PR status summary lists all four new gates</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` is valid YAML; four new jobs present (frozen-guard, contrast, i18n-parity, room-board-regression), none continue-on-error.
- Each job invokes the correct npm script / playwright config from the earlier plans.
- No credentials hardcoded; regression job reads the four `REGRESSION_*` fixture creds + `PLAYWRIGHT_BASE_URL` from secrets.
- Existing jobs untouched except pr-comment's needs/table.
</verification>

<success_criteria>
The full FOUND-02/03/04/05 enforcement is live in CI: every PR and push runs the frozen-file guard, dark-mode contrast, EN/ES key-parity, and the Room-Board pixel-diff regression (against a deterministic fixture-tenant baseline) as hard gates. Phase 30's foundation is structurally protected for all of Phases 31-36.
</success_criteria>

<output>
After completion, create `.planning/phases/30-additive-foundation-regression-harness/30-06-SUMMARY.md`, and flag the one human follow-up: configuring the GitHub repo secrets `REGRESSION_GM_EMAIL/PASSWORD`, `REGRESSION_SUP_EMAIL/PASSWORD`, and `PLAYWRIGHT_BASE_URL` so the regression job can authenticate against the fixture-tenant deployment.
</output>
