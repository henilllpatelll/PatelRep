---
phase: 30-additive-foundation-regression-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/playwright.regression.config.ts
  - apps/web/e2e/global-setup.ts
  - apps/web/e2e/room-board-baseline.spec.ts
  - apps/web/e2e/room-board-baseline.spec.ts-snapshots/
autonomous: true

must_haves:
  truths:
    - "Baseline light+dark screenshots of RoomStatusBoard, RoomDetailDrawer, and EngineeringRoomBoard exist for at least 2 roles, committed to git"
    - "A Playwright regression config runs an authenticated, deterministic pixel-diff of the 3 excluded surfaces against the committed baseline"
    - "The pixel-diff is deterministic: motion disabled, dynamic regions (timestamps, ETA, sync badge) masked, maxDiffPixelRatio 0"
  artifacts:
    - path: "apps/web/playwright.regression.config.ts"
      provides: "Regression pixel-diff project config (authenticated storageState, reduced motion)"
      contains: "reducedMotion"
    - path: "apps/web/e2e/global-setup.ts"
      provides: "One-time authenticated login producing storageState for the regression run"
    - path: "apps/web/e2e/room-board-baseline.spec.ts"
      provides: "Baseline capture + diff spec for the 3 excluded surfaces x light/dark x >=2 roles"
      contains: "toHaveScreenshot"
    - path: "apps/web/e2e/room-board-baseline.spec.ts-snapshots/"
      provides: "Committed baseline PNG snapshots (>=12: 3 surfaces x 2 modes x 2 roles)"
  key_links:
    - from: "apps/web/e2e/room-board-baseline.spec.ts"
      to: "apps/web/e2e/global-setup.ts"
      via: "storageState reuse via playwright.regression.config.ts"
      pattern: "storageState"
    - from: "apps/web/e2e/room-board-baseline.spec.ts"
      to: "the 3 excluded board routes"
      via: "authenticated page navigation + toHaveScreenshot with masks"
      pattern: "toHaveScreenshot"
---

<objective>
Stand up the Playwright pixel-diff regression harness that captures pre-redesign baseline screenshots of the 3 excluded Room Board surfaces (RoomStatusBoard, RoomDetailDrawer, EngineeringRoomBoard) in light and dark mode for at least 2 roles, and re-checks them for zero drift. This is FOUND-03 — the deepest safety guarantee of the whole v2.0 milestone, and per the requirement it must be captured BEFORE any token/primitive work begins. This plan runs in Wave 1 so the baseline reflects the untouched tree.

Purpose: Every later phase (30-04 tokens onward, and Phases 31-36) re-runs this diff to prove the excluded boards never visually drift. Capturing it first makes it the true "golden" reference.
Output: A committed baseline snapshot set + a regression config/spec that any phase can run.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-additive-foundation-regression-harness/30-CONTEXT.md
@.planning/phases/30-additive-foundation-regression-harness/30-RESEARCH.md

# Existing Playwright wiring to mirror (structure, baseURL, headless)
@apps/web/playwright.phase1.config.ts
@apps/web/e2e/phase1-work-orders.spec.ts

# The excluded surfaces being baselined (READ ONLY — never edit these)
@apps/web/components/housekeeping/RoomStatusBoard.tsx
@apps/web/components/housekeeping/RoomDetailDrawer.tsx
@apps/web/components/engineering/EngineeringRoomBoard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authenticated globalSetup + deterministic regression config</name>
  <files>apps/web/e2e/global-setup.ts, apps/web/playwright.regression.config.ts</files>
  <action>
Create the authenticated, deterministic harness. DECIDED APPROACH (resolves the RESEARCH.md MEDIUM-confidence execution risk — do not leave open):

**Environment/seed strategy:** Run against the production web app URL (same pattern as `playwright.phase1.config.ts`, which already targets the Railway prod URL). Authenticate as the GM test account from memory `reference_test_account.md` — resolve the LIVE credentials at execution time (the memory file is stale; the account is the Sonesta ES Suites GM). The GM role sees both the Housekeeping Room Board and the Engineering board, giving 1 of the >=2 roles. For the second role use `housekeeping_supervisor` (also sees the Housekeeping board). If a second live test credential is not available, capture the second-role snapshots by switching the GM's viewed role is NOT possible — instead document that the 2-role requirement is met by GM + housekeeping_supervisor and obtain/confirm the supervisor credential during execution; if genuinely unavailable, fall back to two distinct viewport/density presets of the GM view as the "second axis" and record the deviation with rationale in the SUMMARY. Determinism comes from masking, not from a frozen DB seed, since prod data shifts — see masks below.

`e2e/global-setup.ts`: a Playwright `globalSetup` that logs in once via the login form (email/password), waits for the dashboard to load, and saves `storageState` to `e2e/.auth/state.json` (gitignore this path — add `e2e/.auth/` to `apps/web/.gitignore`). Read credentials from env vars `PLAYWRIGHT_TEST_EMAIL` / `PLAYWRIGHT_TEST_PASSWORD` (documented defaults pointing to the GM test account), never hardcode secrets.

`playwright.regression.config.ts`: mirror `playwright.phase1.config.ts` but add: `globalSetup: './e2e/global-setup.ts'`, `testMatch: 'room-board-baseline.spec.ts'`, `use: { storageState: 'e2e/.auth/state.json', reducedMotion: 'reduce', headless: true, baseURL: process.env.PLAYWRIGHT_BASE_URL ?? '<prod web URL>' }`, and `expect: { toHaveScreenshot: { maxDiffPixelRatio: 0, animations: 'disabled' } }`. Single chromium project, fixed viewport (e.g. 1440x900).
  </action>
  <verify>cd apps/web && npx tsc --noEmit -p tsconfig.json (config + setup type-check clean); npx playwright test --config=playwright.regression.config.ts --list shows the baseline spec discovered</verify>
  <done>globalSetup logs in and writes storageState; regression config loads with reducedMotion + maxDiffPixelRatio 0; e2e/.auth/ is gitignored</done>
</task>

<task type="auto">
  <name>Task 2: Baseline spec for 3 surfaces x light/dark x >=2 roles + capture snapshots</name>
  <files>apps/web/e2e/room-board-baseline.spec.ts, apps/web/e2e/room-board-baseline.spec.ts-snapshots/</files>
  <action>
Write `e2e/room-board-baseline.spec.ts` covering the capture matrix: 3 surfaces x {light, dark} x >=2 roles (GM + housekeeping_supervisor) = >=12 `toHaveScreenshot` assertions.

- Housekeeping RoomStatusBoard: navigate to `/housekeeping`, wait for the board to render.
- RoomDetailDrawer: open a room's detail drawer from the board (click a room card), wait for drawer.
- EngineeringRoomBoard: navigate to `/engineering`, wait for the room board view.
- Dark mode: toggle via the app's `.theme-dark` mechanism (`uiPreferencesStore` writes the class on the shell root) — flip the user's theme preference in the UI or via `page.evaluate` on the store, then re-capture. Confirm the class is present before asserting.

**Determinism (critical — boards carry Supabase Realtime + live timestamps):** every `toHaveScreenshot` call passes `{ animations: 'disabled', mask: [<locators for>: relative timestamps (e.g. "5m ago"), predicted-ready ETA text, the Realtime sync/connection badge, any live clock], maxDiffPixelRatio: 0 }`. Identify the exact mask locators by reading the 3 excluded components (they are in @context) — do NOT guess; find the elements rendering time/ETA/sync state and give them stable `data-testid` ONLY if adding a testid is impossible without editing an excluded file — if it would require editing a frozen board, prefer masking by role/text locator instead (adding a data-testid to a frozen board is a frozen-file edit and is forbidden). Use text/role/CSS locators that already exist.

Run with `--update-snapshots` to generate the baseline PNGs, then commit them under `e2e/room-board-baseline.spec.ts-snapshots/`. Re-run WITHOUT update to confirm the diff is green (zero drift on the same tree).
  </action>
  <verify>cd apps/web && npx playwright test --config=playwright.regression.config.ts --update-snapshots (generates >=12 PNGs); then npx playwright test --config=playwright.regression.config.ts (passes with zero diff); git status shows committed snapshots under the -snapshots/ dir</verify>
  <done>>=12 baseline PNGs committed (3 surfaces x 2 modes x 2 roles); a clean re-run passes with maxDiffPixelRatio 0; dynamic regions are masked so the diff is stable</done>
</task>

</tasks>

<verification>
- `npx playwright test --config=playwright.regression.config.ts` passes green against the committed baseline (zero drift).
- Snapshot directory contains >=12 PNGs spanning all 3 excluded surfaces, both themes, both roles.
- No excluded board file (`RoomStatusBoard.tsx`, `RoomDetailDrawer.tsx`, `EngineeringRoomBoard.tsx`) or frozen primitive was edited to make the harness work.
- `e2e/.auth/` is gitignored; no credentials committed.
</verification>

<success_criteria>
FOUND-03 baseline captured: light+dark screenshots of all 3 excluded surfaces for >=2 roles exist and a deterministic pixel-diff against them passes with zero drift. This becomes the golden reference re-run by 30-04 (after tokens land) and by every later phase.
</success_criteria>

<output>
After completion, create `.planning/phases/30-additive-foundation-regression-harness/30-01-SUMMARY.md` documenting: the chosen environment/credentials, the exact mask locators used, the role coverage actually achieved (and any documented deviation), and the snapshot count.
</output>
