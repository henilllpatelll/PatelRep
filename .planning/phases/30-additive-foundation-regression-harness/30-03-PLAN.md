---
phase: 30-additive-foundation-regression-harness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/check-i18n-parity.mjs
  - apps/web/package.json
autonomous: true

must_haves:
  truths:
    - "A script flattens en.ts and es.ts key sets and fails if either locale is missing a key the other has"
    - "The check is separate from and stronger than the existing no-literal-string eslint rule"
    - "The check is runnable via an npm script and passes on the current (in-parity) locales"
  artifacts:
    - path: "apps/web/scripts/check-i18n-parity.mjs"
      provides: "EN/ES key-parity checker using the TypeScript compiler API"
      contains: "createSourceFile"
    - path: "apps/web/package.json"
      provides: "check:i18n-parity npm script"
      contains: "check:i18n-parity"
  key_links:
    - from: "apps/web/scripts/check-i18n-parity.mjs"
      to: "apps/web/i18n/locales/en.ts and es.ts"
      via: "static AST parse of the exported object literal"
      pattern: "en\\.ts|es\\.ts"
---

<objective>
Add an EN/ES i18n key-parity gate (FOUND-05) that statically compares the key sets of `i18n/locales/en.ts` and `es.ts` and fails if either is missing a key the other has. This closes the silent-regression class the existing `i18next/no-literal-string` eslint rule cannot catch (that rule proves no raw JSX literal, not locale parity).

Purpose: New copy in later phases cannot ship missing a Spanish (or English) translation.
Output: A zero-dependency Node script + npm script. (CI wiring happens in plan 30-06.)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-additive-foundation-regression-harness/30-CONTEXT.md
@.planning/phases/30-additive-foundation-regression-harness/30-RESEARCH.md

# Precedent: existing locale-aware Node gate scripts to mirror in style
@apps/web/scripts/verify-i18n-gate.mjs
@apps/web/scripts/check-programs-i18n.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: check-i18n-parity.mjs (TypeScript compiler API, static parse)</name>
  <files>apps/web/scripts/check-i18n-parity.mjs</files>
  <action>
Create `apps/web/scripts/check-i18n-parity.mjs`. Each locale file is `const x = { ... nested object ... }; export default x` (~1425 lines each today, currently equal). Use the already-present `typescript` devDep compiler API — do NOT execute the modules (avoids needing tsx / running app code):
- `ts.createSourceFile('en.ts', <source>, ts.ScriptTarget.Latest, true)` for both files.
- Walk the object-literal AST (`ObjectLiteralExpression` -> `PropertyAssignment`), recursing into nested object literals, to produce a flattened dot-path key set for each locale (e.g. `dashboard.title`, `tasks.empty.heading`).
- Diff the two sets. Print any key present in EN but missing in ES, and any present in ES but missing in EN. Exit 1 if the diff is non-empty (listing every offending key with its direction), exit 0 otherwise.
- Mirror the reporting/CLI style of `verify-i18n-gate.mjs` (clear actionable messages, no external deps beyond `typescript`).
Match style/precision to the existing scripts; handle both `PropertyAssignment` and `ShorthandPropertyAssignment`, and string/template-literal keys.
  </action>
  <verify>cd apps/web && node scripts/check-i18n-parity.mjs exits 0 on the current in-parity locales; temporarily deleting one ES key makes it exit 1 naming that key (revert the temp edit after)</verify>
  <done>Script flattens both locales, diffs key sets, exits 0 when in parity and 1 (with the offending keys) when not; no new dependency added</done>
</task>

<task type="auto">
  <name>Task 2: Wire npm script</name>
  <files>apps/web/package.json</files>
  <action>
Add `"check:i18n-parity": "node scripts/check-i18n-parity.mjs"` to `apps/web/package.json` scripts (alongside the existing `verify:i18n-gate` and `check:floor-copy`). Do not remove or alter existing scripts.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity exits 0</verify>
  <done>`npm run check:i18n-parity` runs the gate and passes on the current locales</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity` passes on the current tree.
- Introducing a missing key in either locale makes it fail and name the key.
- No new npm dependency added (uses the existing `typescript` devDep).
</verification>

<success_criteria>
FOUND-05 met: an automated EN/ES key-parity check exists as an npm script, distinct from and stronger than `no-literal-string`, ready to be wired into CI by plan 30-06.
</success_criteria>

<output>
After completion, create `.planning/phases/30-additive-foundation-regression-harness/30-03-SUMMARY.md`.
</output>
