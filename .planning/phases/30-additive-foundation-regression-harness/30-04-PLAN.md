---
phase: 30-additive-foundation-regression-harness
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - apps/web/app/globals.css
  - apps/web/tailwind.config.ts
  - apps/web/frozen-files.json
  - .planning/phases/30-additive-foundation-regression-harness/FROZEN.md
autonomous: true

must_haves:
  truths:
    - "New design tokens (motion, easing, z-index, elevation/surface, v2 brand ramp, focus ring) exist in globals.css added additively; every pre-existing token value is byte-unchanged in :root and .theme-dark"
    - "A documented frozen-primitive list exists naming Button, IconButton, ui/primitives (StatusDot/Pill), RoomCard, LogFoundItemModal, plus every token the excluded boards read"
    - "The room-status color values (dirty/clean/inspected/occupied/in_progress/pickup/ooo) are captured as a value-frozen manifest, tagged distinctly from the name-freeze, with no allowlist escape"
    - "After the additive tokens land, the 30-01 pixel-diff re-runs with zero drift on the excluded boards"
  artifacts:
    - path: "apps/web/frozen-files.json"
      provides: "Machine-readable freeze manifest: file->sha256 for the 7 frozen files + room-status token name->value pairs (light+dark) parsed from source"
      contains: "room-status"
    - path: ".planning/phases/30-additive-foundation-regression-harness/FROZEN.md"
      provides: "Human-readable frozen-primitive list + room-status hard-constraint table + double-duty-token warning for Phases 31-36"
      contains: "room-status"
    - path: "apps/web/app/globals.css"
      provides: "New additive tokens in :root and .theme-dark"
      contains: "--motion-base"
  key_links:
    - from: "apps/web/frozen-files.json"
      to: "the 7 frozen component files + room-status token values"
      via: "sha256 hashes + parsed hex values"
      pattern: "sha256|room-status"
    - from: "apps/web/app/globals.css new tokens"
      to: "the excluded boards"
      via: "additive-only — boards keep reading old token names, pixel-diff proves zero drift"
      pattern: "--brand|--motion|--z-"
---

<objective>
Land the additive design-token foundation (FOUND-01) and author the frozen-primitive manifest + documentation (FOUND-02 documentation half; enforcement is plan 30-05). Every token is NEW — no existing value or name is touched — and the room-status color values are captured as a distinctly-tagged value-freeze. After tokens land, re-run the 30-01 baseline diff to prove the excluded boards did not drift.

Purpose: Give Phases 31-36 a real design system (motion/z-index/elevation scales, a v2 brand ramp, focus ring) to opt into, while guaranteeing the excluded boards render byte-identically.
Output: Extended globals.css + tailwind.config.ts (additive), `frozen-files.json` manifest, `FROZEN.md` doc.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-additive-foundation-regression-harness/30-CONTEXT.md
@.planning/phases/30-additive-foundation-regression-harness/30-RESEARCH.md

# The token layer being extended (additive only)
@apps/web/app/globals.css
@apps/web/tailwind.config.ts

# The frozen room-status color source-of-truth (READ to extract exact values — do NOT edit)
@apps/web/components/ui/primitives.tsx
@apps/web/components/housekeeping/RoomCard.tsx
@apps/web/components/ui/Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive tokens to globals.css + tailwind.config.ts</name>
  <files>apps/web/app/globals.css, apps/web/tailwind.config.ts</files>
  <action>
Add NEW tokens ONLY — never edit, rename, or remove any existing token value/name (doing so silently re-skins the excluded boards). Add to BOTH `:root` and `.theme-dark` in `app/globals.css`, using the RESEARCH.md "Warm operational calm, systematized" direction (Claude's-discretion visual identity, grounded in the existing warm-paper/terracotta system):

```css
/* Motion — functional, floor-appropriate; prefers-reduced-motion already handled globally */
--motion-fast: 120ms; --motion-base: 180ms; --motion-slow: 260ms;
--ease-standard: cubic-bezier(0.2,0,0,1); --ease-emphasized: cubic-bezier(0.3,0,0,1); --ease-exit: cubic-bezier(0.4,0,1,1);
/* Z-index scale */
--z-base:0; --z-dropdown:1000; --z-sticky:1100; --z-header:1200; --z-drawer:1300; --z-modal:1400; --z-popover:1500; --z-toast:1600; --z-tooltip:1700;
/* Elevation / surface tint (does NOT touch --surface-2/3) */
--surface-raised:#ffffff;   /* dark: #211e1a */
--surface-overlay:#fbf9f4;  /* dark: #262320 */
--shadow-xs:0 1px 1px rgba(26,24,21,0.03);
/* v2 brand ramp + focus (existing --accent/--ink stay frozen) */
--brand:#b8431c;      /* dark: #e08a63 */
--brand-ink:#ffffff;
--brand-soft:#fbe9df;  /* dark: #2e1e16 */
--brand-line:#f0c8b3;  /* dark: #5e3c2a */
--focus-ring: color-mix(in srgb, var(--brand) 45%, transparent);
```
Design light values first, then hand-pick dark values (do NOT auto-derive) — the contrast gate in 30-05 will prove them AA.

In `tailwind.config.ts`: add NEW color/utility aliases for the new tokens (e.g. `brand`, `brand-soft`, `brand-line`, `surface-raised`, `surface-overlay`) and, if useful, expose the z-index scale — additive only, never rename/remove an existing alias or the hardcoded `colors.status.*` room-status hex.

Keep the existing IBM Plex Sans/Mono + Instrument Serif stack (no new webfont — first-paint cost on floor phones violates the "save floor-staff time" filter). Keep all 4 accent themes and all 3 density modes untouched — the new tokens are density-agnostic. Record the visual-direction rationale (and the "keep 4 accents / keep terracotta" calls) in the SUMMARY.
  </action>
  <verify>cd apps/web && npm run build succeeds; grep confirms new tokens in both :root and .theme-dark; git diff shows ONLY additions in globals.css (no existing line changed) — verify with `git diff --word-diff app/globals.css` that no pre-existing value was modified</verify>
  <done>New motion/ease/z-index/elevation/brand/focus tokens exist in both light and dark; no existing token value or name changed; build green; density/accent systems untouched</done>
</task>

<task type="auto">
  <name>Task 2: Author frozen-files.json manifest (hashes + room-status value freeze)</name>
  <files>apps/web/frozen-files.json</files>
  <action>
Create `apps/web/frozen-files.json` — the machine-readable freeze manifest consumed by the 30-05 guard. Two distinct sections:

1. `"files"`: `{ "<path>": "<sha256>" }` for the 7 frozen files, at their CURRENT (byte-unchanged this phase) contents:
   - `apps/web/components/ui/Button.tsx`
   - `apps/web/components/ui/primitives.tsx`
   - `apps/web/components/housekeeping/RoomCard.tsx`
   - `apps/web/components/shared/LogFoundItemModal.tsx`
   - `apps/web/components/housekeeping/RoomStatusBoard.tsx`
   - `apps/web/components/housekeeping/RoomDetailDrawer.tsx`
   - `apps/web/components/engineering/EngineeringRoomBoard.tsx`
   Compute sha256 of each file's bytes.

2. `"room_status_values"`: the VALUE-freeze (the HARD CONSTRAINT), tagged distinctly. For each room status, the token name -> {light, dark} hex, read from `globals.css` `:root`/`.theme-dark` (verified against the maps in `primitives.tsx`/`RoomCard.tsx`). Per RESEARCH.md exact table:
   - DIRTY/OCCUPIED: `--alert`/`--alert-soft`/`--alert-line`
   - CLEAN: `--info`/`--info-soft`/`--info-line`
   - INSPECTED: `--ready`/`--ready-soft`/`--ready-line`
   - IN_PROGRESS: `--progress`/`--progress-soft`/`--progress-line`
   - PICKUP: `--caution`/`--caution-soft`/`--caution-line`
   - OOO: `--blocked`/`--blocked-soft`/`--blocked-line`
   Also include the hardcoded room-status hex in `tailwind.config.ts` `colors.status.*` (inspected #0c6e63, clean #265d8a, in-progress #7c3aed, dirty #a6263c, oos #b8431c, vip #a16207). Do NOT include `colors.risk.*` (risk is explicitly un-frozen).
   Mark this section with a note that room-status values have NO allowlist escape — a change here is always a hard failure.

Note in the manifest: globals.css and tailwind.config.ts are NOT file-hash-frozen (they are additively extended); only their specific room-status VALUES are frozen and re-parsed by the guard.
  </action>
  <verify>frozen-files.json parses as valid JSON; the 7 file paths exist and their hashes match a fresh sha256 of each; the room-status values match the RESEARCH.md table and the values in globals.css</verify>
  <done>Manifest has correct sha256 for all 7 frozen files + a distinctly-tagged room-status value map (light+dark) with a no-allowlist note</done>
</task>

<task type="auto">
  <name>Task 3: FROZEN.md doc + re-run baseline zero-drift proof</name>
  <files>.planning/phases/30-additive-foundation-regression-harness/FROZEN.md</files>
  <action>
Write `FROZEN.md` — the human-readable FOUND-02 deliverable for Phases 31-36:
- The frozen-primitive list: `Button`/`IconButton`, `ui/primitives` (`StatusDot`/`Pill`), `RoomCard`, `LogFoundItemModal`, the 3 excluded boards, plus "every token name the boards read" (list them from RESEARCH.md), and every Tailwind alias — all NAME-frozen (extend with new names/variants only; never rename/remove/mutate).
- The room-status HARD CONSTRAINT table (status -> tokens -> light/dark hex), tagged as VALUE-frozen everywhere they render (legends, filter chips, "my rooms" lists in Phase 36 too), with the explicit rule: these values can never change and have no allowlist path.
- The double-duty-token WARNING (Pitfall 2): `--alert`/`--info`/`--caution`/`--ready`/`--progress`/`--blocked` are BOTH generic chrome AND room-status; a later phase wanting a different generic alert/error color MUST introduce a NEW token (e.g. `--danger-v2`), never re-tint `--alert`.
- How to legitimately change a frozen FILE later: bump its hash in `frozen-files.json` AND add a reasoned allowlist entry (mechanism built in 30-05).
- Note primitive v2 variants (e.g. `Button variant="v2"`) are intentionally DEFERRED to the phases that first need them (added then via the allowlist flow), keeping all 7 frozen files byte-unchanged in Phase 30.

Then prove the additive tokens did not drift the boards: re-run the 30-01 harness against the committed baseline.
  </action>
  <verify>cd apps/web && npx playwright test --config=playwright.regression.config.ts passes with ZERO drift after the new tokens landed (proves additive-only); FROZEN.md contains the room-status table and the double-duty warning</verify>
  <done>FROZEN.md documents the full frozen list, the room-status value-freeze, and the double-duty-token constraint; the post-token pixel-diff is zero-drift — additive-only is proven</done>
</task>

</tasks>

<verification>
- `git diff --word-diff apps/web/app/globals.css` shows only additions — no existing token value/name modified.
- New tokens present in both `:root` and `.theme-dark`; `npm run build` green.
- `frozen-files.json` hashes match fresh sha256 of the 7 files; room-status value map matches source.
- `FROZEN.md` names the full frozen list, the room-status hard constraint, and the double-duty warning.
- The 30-01 regression diff re-runs with zero drift after tokens land.
</verification>

<success_criteria>
FOUND-01 met: additive tokens exist, existing values byte-unchanged. FOUND-02 (documentation) met: frozen-primitive list documented, room-status values captured as a distinct value-freeze, all primitives untouched (only extended in future via allowlist). The exclusion holds — proven by zero pixel drift.
</success_criteria>

<output>
After completion, create `.planning/phases/30-additive-foundation-regression-harness/30-04-SUMMARY.md` recording the visual-identity direction chosen, the accent/typography decisions, the exact new token names/values, and the zero-drift confirmation.
</output>
