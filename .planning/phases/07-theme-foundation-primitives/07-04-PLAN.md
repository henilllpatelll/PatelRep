---
phase: 07-theme-foundation-primitives
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/mobile/components/ui/Card.tsx
  - apps/mobile/components/ui/StatusBadge.tsx
autonomous: true
requirements: [UI-02, UI-04]
must_haves:
  truths:
    - "Card is a reusable surface wrapper (bg/border/radius/shadow) taking children + optional dimmed"
    - "StatusBadge always renders icon + label + color together (no icon-only mode)"
    - "StatusBadge resolves every status color through useTheme() -> theme.status (never an inline status hex)"
    - "StatusBadge covers both room-status and work-order status families via one statusKey prop"
  artifacts:
    - path: "apps/mobile/components/ui/Card.tsx"
      provides: "Card surface primitive extracted from RoomQueueCard shell"
      exports: ["Card"]
    - path: "apps/mobile/components/ui/StatusBadge.tsx"
      provides: "StatusBadge (statusKey -> icon+color+label)"
      exports: ["StatusBadge"]
      contains: "Ionicons"
  key_links:
    - from: "apps/mobile/components/ui/Card.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "useTheme() for surface/border/shadow colors"
      pattern: "useTheme"
    - from: "apps/mobile/components/ui/StatusBadge.tsx"
      to: "apps/mobile/lib/theme/useTheme.ts"
      via: "useTheme().status for the color triplet"
      pattern: "theme.status"
---

<objective>
Create two presentational primitives: `Card` (`components/ui/Card.tsx`), a reusable surface extracted from `RoomQueueCard`'s ad-hoc shell; and `StatusBadge` (`components/ui/StatusBadge.tsx`), which formalizes `WorkOrderCard`'s existing icon+color+label convention into one component driven by a `statusKey` prop, reading colors reactively via `useTheme().status` from day one (dark-ready before Phase 10).

Purpose: Satisfies UI-02 (Card) and UI-04 (StatusBadge — never color alone). `WorkOrderCard`/`RoomQueueCard` are NOT migrated to consume these yet (Phase 8).
Output: `apps/mobile/components/ui/Card.tsx`, `apps/mobile/components/ui/StatusBadge.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-theme-foundation-primitives/07-CONTEXT.md
@.planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md
@.planning/phases/07-theme-foundation-primitives/07-PATTERNS.md

<interfaces>
`useTheme()` from `@/lib/theme/useTheme` (plan 01) returns the light theme object. Relevant keys:
- surfaces: `surface` (#FFFDFC), `surfaceMuted` (#F0EBE1), `border` (#E4D6C4), `borderSubtle` (#EEE8DB), `textPrimary` (#2C2621), `textMuted` (#807A70).
- `status` sub-object (= `statusTokens`) has EXACTLY these keys: `ready`, `clean`, `dirty`, `occupied`, `pickup`, `outOfOrder`, and their `*Soft`/`*Line` companions for `ready`/`clean`/`dirty`/`pickup`/`outOfOrder` (there is NO `occupiedSoft`/`occupiedLine` — occupied shares the dirty fills; there is NO `alert`/`caution` key — those are legacy `C` aliases where `alert`=`dirty` and `caution`=`pickup`).

`RoomQueueCard` shell to extract (`evening.tsx` `styles.card` ~lines 337-353):
```
backgroundColor: C.surface, borderWidth: 1, borderColor: C.line, borderRadius: R.lg (16),
shadowColor: C.ink, shadowOpacity: 0.06, shadowRadius: 10, shadowOffset: {0,3}, elevation: 2
// dimmed: backgroundColor C.surface2, borderColor C.line2, shadow/elevation off, opacity 0.7
```
Import alias `@/` = `apps/mobile/`. Icon lib: `@expo/vector-icons` `Ionicons` (already installed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Card primitive</name>
  <files>apps/mobile/components/ui/Card.tsx</files>
  <read_first>
    - apps/mobile/components/shared/evening.tsx (RoomQueueCard styles.card + cardDimmed — the shell to extract)
    - apps/mobile/lib/theme/useTheme.ts (color source, plan 01)
    - apps/mobile/components/shared/tokens.ts (confirm R.lg=16, S.card=16)
    - .planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md (§"Card (UI-02)")
  </read_first>
  <action>
Create `apps/mobile/components/ui/Card.tsx` — a pure surface wrapper.

Props:
```ts
interface CardProps {
  children: React.ReactNode;
  dimmed?: boolean;
  style?: StyleProp<ViewStyle>;
}
```
Colors via `const theme = useTheme();` (inline at render). Keep static layout in `StyleSheet.create`. Base style (default, non-dimmed) — use these EXACT values:
- `backgroundColor: theme.surface`
- `borderWidth: 1`, `borderColor: theme.border`
- `borderRadius: 16` (`R.lg`)
- `padding: 16` (`S.card`) — symmetric on all sides (unlike `RoomQueueCard`'s asymmetric 16/12; the base Card has no left status rail)
- iOS shadow: `shadowColor: theme.textPrimary`, `shadowOpacity: 0.06`, `shadowRadius: 10`, `shadowOffset: { width: 0, height: 3 }`
- Android: `elevation: 2`

Dimmed variant (when `dimmed` is true): `backgroundColor: theme.surfaceMuted`, `borderColor: theme.borderSubtle`, `shadowOpacity: 0`, `elevation: 0`, `opacity: 0.7`.

Render `<View style={[styles.base, dimmed && dimmedColorProps, { ...themed colors }, props.style]}>{children}</View>`. Style merge order (LOCKED, Pitfall 6): base first, variant (dimmed) next, caller `style` LAST.

Do NOT import `C`, `lightTheme`, or `darkTheme` directly — colors ONLY via `useTheme()`. No user-facing text in this component.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useTheme" apps/mobile/components/ui/Card.tsx` matches; `grep -En "\\bC\\.|lightTheme|darkTheme" apps/mobile/components/ui/Card.tsx` returns nothing.
    - `grep -n "borderRadius: 16" apps/mobile/components/ui/Card.tsx` matches (R.lg) and `grep -n "padding: 16" apps/mobile/components/ui/Card.tsx` matches (S.card symmetric).
    - `grep -n "theme.surface" apps/mobile/components/ui/Card.tsx` and `grep -n "theme.border" apps/mobile/components/ui/Card.tsx` both match.
    - `grep -n "dimmed" apps/mobile/components/ui/Card.tsx` matches (muted variant present).
    - `grep -n "props.style\|style]" apps/mobile/components/ui/Card.tsx` shows caller `style` is last in the merge array.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>Card renders a themed surface with the exact extracted shell values + a dimmed variant, theme-only colors, locked merge order; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the StatusBadge primitive</name>
  <files>apps/mobile/components/ui/StatusBadge.tsx</files>
  <read_first>
    - apps/mobile/components/engineering/WorkOrderCard.tsx (icon+color chip convention, ~lines 95-110, 173 — the canonical Ionicons)
    - apps/mobile/components/shared/evening.tsx (STATUS_META / StatusPill — room-status color mapping + pill render, ~lines 22-53)
    - apps/mobile/lib/theme/useTheme.ts (color source, plan 01)
    - apps/mobile/components/shared/tokens.ts (confirm theme.status key names available)
    - .planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md (§"StatusBadge (UI-04)" — the full statusKey table)
    - .planning/phases/07-theme-foundation-primitives/07-PATTERNS.md (§"components/ui/StatusBadge.tsx")
  </read_first>
  <action>
Create `apps/mobile/components/ui/StatusBadge.tsx`.

Props:
```ts
type StatusKey =
  | "ready" | "clean" | "dirty" | "occupied" | "pickup" | "outOfOrder"
  | "emergency" | "urgent" | "low" | "onHold" | "overdue" | "inProgress" | "completed";
interface StatusBadgeProps {
  statusKey: StatusKey;
  label: string;               // caller-provided, already-translated — NO default, never t() here
  style?: StyleProp<ViewStyle>;
}
```
There is NO icon-only / compact mode (D-11 — guards UI-04 "never color alone"). ALWAYS render icon + label + color together.

Colors via `const theme = useTheme(); const s = theme.status;`. Build a `statusKey → { icon, bg, fg, border }` map using these EXACT resolutions (icon = Ionicons name; bg/fg/border come from `theme.status.*`, resolving the UI-SPEC's `alert*`→`dirty*` and `caution*`→`pickup*` legacy aliases):

| statusKey | icon | bg | fg | border |
|---|---|---|---|---|
| ready | `checkmark-circle` | `s.readySoft` | `s.ready` | `s.readyLine` |
| clean | `checkmark-done-outline` | `s.cleanSoft` | `s.clean` | `s.cleanLine` |
| dirty | `brush-outline` | `s.dirtySoft` | `s.dirty` | `s.dirtyLine` |
| occupied | `person-outline` | `s.dirtySoft` | `s.occupied` | `s.dirtyLine` |
| pickup | `alert-circle-outline` | `s.pickupSoft` | `s.pickup` | `s.pickupLine` |
| outOfOrder | `close-circle-outline` | `s.outOfOrderSoft` | `s.outOfOrder` | `s.outOfOrderLine` |
| emergency | `warning` | `s.dirtySoft` | `s.dirty` | `s.dirtyLine` |
| urgent | `flash` | `s.dirtySoft` | `s.dirty` | `s.dirtyLine` |
| low | `arrow-down` | `theme.surfaceMuted` | `theme.textMuted` | `theme.border` |
| onHold | `pause-circle-outline` | `s.pickupSoft` | `s.pickup` | `s.pickupLine` |
| overdue | `alert-circle` | `s.dirtySoft` | `s.dirty` | `s.dirtyLine` |
| inProgress | `stopwatch-outline` | `s.pickupSoft` | `s.pickup` | `s.pickupLine` |
| completed | `checkmark-circle` | `s.readySoft` | `s.ready` | `s.readyLine` |

Note on `low`: the UI-SPEC names `surface3`/`ink4`/`line`; `ink4` (#B7AA99) has NO theme-object key, so use `theme.textMuted` (the nearest themed neutral) for `fg` to keep the badge theme-reactive per D-12 — do NOT inline `#B7AA99`. Note the accepted deviation in the SUMMARY.

Container style (pill): `borderRadius: 999`, `borderWidth: 1`, `paddingHorizontal: 8`, `paddingVertical: 4`, `minHeight: 22`, `flexDirection: "row"`, `alignItems: "center"`, `gap: 4`, `backgroundColor: bg`, `borderColor: border`. (Not a touch target — the 44pt floor does not apply to an informational label.)
Icon: `<Ionicons name={icon} size={12} color={fg} />`.
Label: `<Text>` with `color: fg`, `fontSize: 11`, `fontWeight: "800"`, `textTransform: "uppercase"`, `letterSpacing: 0.3` (Label role, matches `statusPillText`).

Keep static layout in `StyleSheet.create`; pull `bg`/`fg`/`border` inline from the theme map. Style merge order (LOCKED): `style={[styles.container, { backgroundColor: bg, borderColor: border }, props.style]}` — base first, caller `style` LAST.

Never inline a status hex (Pitfall 9). Never call `t()` — `label` is caller-provided.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "theme.status" apps/mobile/components/ui/StatusBadge.tsx` matches; `grep -En "#[0-9A-Fa-f]{6}" apps/mobile/components/ui/StatusBadge.tsx` returns nothing (no inline status/color hex — Pitfall 9).
    - `grep -En "\\bC\\.|lightTheme|darkTheme" apps/mobile/components/ui/StatusBadge.tsx` returns nothing.
    - `grep -oE "\"(ready|clean|dirty|occupied|pickup|outOfOrder|emergency|urgent|low|onHold|overdue|inProgress|completed)\"" apps/mobile/components/ui/StatusBadge.tsx | sort -u | wc -l` returns 13 (all statusKeys covered).
    - `grep -n "Ionicons" apps/mobile/components/ui/StatusBadge.tsx` matches and `grep -n "fontWeight: \"800\"\|textTransform: \"uppercase\"" apps/mobile/components/ui/StatusBadge.tsx` matches (Label role) — icon AND label always render (D-11).
    - `grep -n "t(" apps/mobile/components/ui/StatusBadge.tsx` returns nothing (no internal translation / no default label).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>StatusBadge covers all 13 statusKeys with icon+label+color, colors resolved via theme.status (no inline hex), required caller label, locked merge order; type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Presentation-only. Both primitives render caller-supplied content; no network/storage/auth. StatusBadge participates in the protected cross-platform status-color contract (do not alter hues).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | status-color contract (shared with web) | mitigate | All StatusBadge colors route through `theme.status.*`; no inline status hex (Pitfall 9). Acceptance criterion asserts zero 6-digit hex literals in the file. |
| T-07-08 | Information disclosure | Card/StatusBadge | accept | Pure presentation; render only what the caller passes; no data access. |
</threat_model>

<verification>
- `npx tsc --noEmit` from `apps/mobile` is clean.
- No new dependency added.
- No screen imports `Card` or `StatusBadge` yet; `WorkOrderCard`/`RoomQueueCard` unchanged (Phase 8 migrates them).
</verification>

<success_criteria>
- `Card` primitive available as the single container pattern (ROADMAP SC3, UI-02).
- `StatusBadge` pairs color with icon + label for every room/work-order status, never color alone (ROADMAP SC3, UI-04).
- Zero visual change to existing screens — no adoption this phase (ROADMAP SC5).
</success_criteria>

<output>
After completion, create `.planning/phases/07-theme-foundation-primitives/07-04-SUMMARY.md`.
</output>
