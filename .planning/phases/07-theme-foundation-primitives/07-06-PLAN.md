---
phase: 07-theme-foundation-primitives
plan: 06
type: execute
wave: 4
depends_on: ["03", "04", "05"]
files_modified:
  - apps/mobile/eslint.config.mjs
  - apps/mobile/package.json
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "`npm run lint` exists in apps/mobile and runs ESLint with the i18next/no-literal-string rule"
    - "The rule is set to 'error' (hard CI failure), not 'warn', on components/ui/** and the floor-facing dirs"
    - "The four new primitives (Button, Card, EmptyState, StateBlock, StatusBadge) pass the gate cleanly"
    - "A raw JSX string literal added to a components/ui file fails the gate"
  artifacts:
    - path: "apps/mobile/eslint.config.mjs"
      provides: "flat ESLint config with i18next/no-literal-string error gate scoped to floor dirs + components/ui"
      contains: "no-literal-string"
    - path: "apps/mobile/package.json"
      provides: "lint script + eslint-plugin-i18next devDependency"
      contains: "lint"
  key_links:
    - from: "apps/mobile/package.json"
      to: "apps/mobile/eslint.config.mjs"
      via: "lint script invokes eslint"
      pattern: "eslint"
---

<objective>
Add mobile's first ESLint config and a `lint` script that enforces `i18next/no-literal-string` as a HARD failure (mirroring web's gate exactly) on the four new primitives (`components/ui/**`) plus the existing floor-facing screens/components. This closes the tooling gap that lets English-only strings ship silently on mobile.

Purpose: Satisfies I18N-01 (D-15 scope, D-16 hard-fail). Runs last in the phase so it verifies the real primitives (built in plans 03–05) pass the gate.
Output: new `apps/mobile/eslint.config.mjs`; `lint` script + `eslint-plugin-i18next` devDependency in `apps/mobile/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-theme-foundation-primitives/07-CONTEXT.md
@.planning/phases/07-theme-foundation-primitives/07-UI-SPEC.md
@.planning/phases/07-theme-foundation-primitives/07-PATTERNS.md

<interfaces>
Web's reference gate (`apps/web/eslint.config.mjs` lines 29-59) — the exact rule to mirror:
```js
{
  files: [ /* floor-facing dirs */ ],
  ignores: [ '**/*.test.*', '**/*.spec.*', /* English-only admin dirs */ ],
  plugins: { i18next },
  rules: {
    'i18next/no-literal-string': ['error', {
      markupOnly: true,
      'jsx-attributes': { include: ['aria-label', 'placeholder', 'title'] },
    }],
  },
}
```
Web already depends on `eslint-plugin-i18next` (a devDependency). Mobile currently has NO ESLint config and NO `lint` script (package.json scripts: start/android/ios/web/type-check/test). Mobile uses React 19 and installs with `legacy-peer-deps`. Import alias `@/` = `apps/mobile/`.

D-15 floor-facing scope for mobile: `components/ui/**` PLUS `app/(app)/{my-rooms,room-board,room-status,work-orders,tasks,inspect}/**` and `components/{housekeeping,engineering,tasks}/**`. Non-floor screens (profile, supervisor, home, sop, lost-found, notifications, guest-requests, logbook, etc.) are OUT of scope this phase (Phase 9 adds them gate-by-gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eslint-plugin-i18next devDep, lint script, and the mobile eslint.config.mjs</name>
  <files>apps/mobile/package.json, apps/mobile/eslint.config.mjs</files>
  <read_first>
    - apps/web/eslint.config.mjs (the exact reference gate to mirror — D-15/D-16)
    - apps/mobile/package.json (current scripts + devDependencies — no lint script, no eslint yet)
    - apps/web/package.json (to copy the exact `eslint` + `eslint-plugin-i18next` versions already vetted in this repo)
    - .planning/phases/07-theme-foundation-primitives/07-PATTERNS.md (§"Mobile ESLint i18next/no-literal-string gate (NEW config)")
  </read_first>
  <action>
1. In `apps/mobile/package.json`:
   - Add a `"lint": "eslint ."` entry to `scripts`.
   - Add `eslint` and `eslint-plugin-i18next` to `devDependencies`, matching the EXACT versions in `apps/web/package.json` (read them first — do not guess a version). These are devDependencies only — they do NOT touch the Hermes/babel/EAS runtime fault line (Pitfall 3 concerns runtime deps), but do NOT add a `babel.config.js`/`metro.config.js` change.
2. Create `apps/mobile/eslint.config.mjs` as a flat config mirroring `apps/web/eslint.config.mjs`'s i18next block, adapted to mobile's paths:
   - Use `import` of `eslint-plugin-i18next` (flat-config style, matching web's `createRequire` approach if the plugin is CJS).
   - A top-level `ignores` block for build/junk: `['node_modules/**', '.expo/**', 'android/**', 'ios/**', 'dist/**', 'babel.config.js', 'metro.config.js']`.
   - The gated block with `files` = the D-15 scope:
     ```js
     files: [
       'components/ui/**',
       'app/(app)/my-rooms/**',
       'app/(app)/room-board/**',
       'app/(app)/room-status/**',
       'app/(app)/work-orders/**',
       'app/(app)/tasks/**',
       'app/(app)/inspect/**',
       'components/housekeeping/**',
       'components/engineering/**',
       'components/tasks/**',
     ],
     ignores: ['**/*.test.*', '**/*.spec.*'],
     plugins: { i18next },
     rules: {
       'i18next/no-literal-string': ['error', {
         markupOnly: true,
         'jsx-attributes': { include: ['aria-label', 'placeholder', 'title'] },
       }],
     },
     ```
   - The rule severity MUST be `'error'` (hard CI failure, D-16) — never `'warn'`.
   - Do NOT gate any non-floor directory this phase (D-15). Do NOT add rules beyond the i18next gate (this is not a general lint-quality pass — keep the config minimal and focused).
3. Run `npm install` (with `--legacy-peer-deps` if the repo's install convention requires it) from `apps/mobile` to install the new devDeps. Verify it emits no new peer-dependency errors beyond the known React 19 ones.
  </action>
  <verify>
    <automated>cd apps/mobile && npm install --legacy-peer-deps >/dev/null 2>&1; node -e "const p=require('./package.json'); if(!p.scripts.lint) throw new Error('no lint script'); if(!p.devDependencies['eslint-plugin-i18next']) throw new Error('no i18next plugin'); console.log('deps OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\"lint\"" apps/mobile/package.json` matches and the script invokes `eslint`.
    - `grep -n "eslint-plugin-i18next" apps/mobile/package.json` matches under devDependencies, with the SAME version string as `apps/web/package.json`.
    - `grep -n "no-literal-string" apps/mobile/eslint.config.mjs` matches and `grep -n "'error'\|\"error\"" apps/mobile/eslint.config.mjs` confirms severity is error (not warn — D-16).
    - `grep -n "components/ui/\*\*" apps/mobile/eslint.config.mjs` matches AND all six floor `app/(app)/...` dirs + the three floor `components/...` dirs from D-15 are present.
    - `grep -En "profile|supervisor|home/|guest-requests|logbook|sop|lost-found|notifications" apps/mobile/eslint.config.mjs` returns nothing in the `files` array (non-floor screens NOT gated this phase — D-15).
    - `grep -En "babel.config|metro.config" apps/mobile/*.js` shows no NEW babel/metro plugin edits (Pitfall 3).
    - The node verify command prints `deps OK`.
  </acceptance_criteria>
  <done>Mobile has an eslint.config.mjs enforcing i18next/no-literal-string as error on components/ui + the D-15 floor dirs only; lint script + pinned devDeps installed; no babel/metro changes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the gate passes on the new primitives and fails on a planted literal</name>
  <files>apps/mobile/eslint.config.mjs</files>
  <read_first>
    - apps/mobile/eslint.config.mjs (the config just created)
    - apps/mobile/components/ui/Button.tsx, Card.tsx, StatusBadge.tsx, EmptyState.tsx, StateBlock.tsx (the primitives that must pass the gate)
  </read_first>
  <action>
1. Run `npm run lint` from `apps/mobile`. It MUST exit 0 — the five new primitives in `components/ui/` were authored (plans 03–05) with zero literal JSX strings (all copy is caller-provided), so they pass cleanly. If lint reports a literal-string error in any `components/ui/` file, that is a real defect in that primitive — fix the primitive to take a caller-provided/translated string (do NOT weaken the rule or add an eslint-disable to silence it).
2. Negative test (proves the gate actually bites — do NOT commit this change): temporarily add a raw JSX string literal to a `components/ui/` file, e.g. insert `<Text>Hello</Text>` into `Button.tsx`'s render, run `npm run lint`, confirm it exits non-zero with an `i18next/no-literal-string` error, then REVERT the temporary edit and re-run `npm run lint` to confirm it exits 0 again. Leave the working tree clean.
3. Do NOT widen the gate to non-floor screens, and do NOT run a repo-wide autofix. This task only verifies the gate; it changes no source.
  </action>
  <verify>
    <automated>cd apps/mobile && npm run lint 2>&1 | tail -20; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` from `apps/mobile` exits 0 with the current tree (the five primitives pass — verified by `echo "exit=$?"` printing `exit=0`).
    - No `eslint-disable` / `eslint-disable-next-line i18next/no-literal-string` comment was added to any `components/ui/*` file (`grep -rn "eslint-disable" apps/mobile/components/ui/` returns nothing).
    - The negative-test literal was reverted: `git status --porcelain apps/mobile/components/ui/` shows no unstaged changes to the primitives after the check.
    - The SUMMARY records that the negative test failed as expected (planted literal → non-zero exit) before revert.
  </acceptance_criteria>
  <done>`npm run lint` passes on the real primitives; a planted literal was confirmed to fail the gate and then reverted; no rule weakened, no disables added, working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Tooling/config change. The only runtime-adjacent risk is the new devDependency disturbing the fragile EAS/Hermes build pipeline (Pitfall 3) — mitigated because the deps are dev-only and no babel/metro change is made. Full EAS-build verification is a Phase 10 gate (DARK-04), out of scope here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Denial of service | EAS/Hermes build pipeline (Pitfall 3) | mitigate | New packages are devDependencies only, pinned to the exact versions already vetted in `apps/web`; no `babel.config.js`/`metro.config.js` edits. Acceptance criteria assert no babel/metro changes. Full EAS build is Phase 10's DARK-04 gate. |
| T-07-12 | Tampering | lint rule severity downgraded to hide English strings | mitigate | Acceptance criterion asserts severity is `'error'` and that no `eslint-disable` was added; negative test proves the gate bites. |
</threat_model>

<verification>
- `npm run lint` from `apps/mobile` exits 0 on the current tree.
- Rule severity is `error`; scope is exactly the D-15 floor set + `components/ui/**`.
- Only devDependencies added; no babel/metro/runtime change.
</verification>

<success_criteria>
- CI fails a mobile PR that introduces a raw JSX string literal in a floor-facing component/screen, mirroring web's gate (ROADMAP SC4, I18N-01 / D-16).
- The four new primitives pass the gate on day one (they take only caller-provided strings).
- Zero visual change to any screen (config-only change; ROADMAP SC5).
</success_criteria>

<output>
After completion, create `.planning/phases/07-theme-foundation-primitives/07-06-SUMMARY.md`.
</output>
