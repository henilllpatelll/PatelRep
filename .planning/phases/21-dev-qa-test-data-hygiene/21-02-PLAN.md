---
phase: 21-dev-qa-test-data-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md
autonomous: true
must_haves:
  truths:
    - "A human-reviewable allowlist/preserve document exists that explicitly names the standing QA fixture tenant to keep"
    - "The document lists every delete-eligible tenant by UUID, name, and why-safe, derived from live data"
    - "The document explicitly records that controlled_incidents and controlled_incident_events are excluded (append-only)"
  artifacts:
    - path: ".planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md"
      provides: "Ratified delete-allowlist + preserve-list, the source-of-truth for Plan 03's DELETE_ALLOWLIST / PRESERVE constants"
      contains: "23264962-aa09-4e4f-a49d-fc345cc91414"
  key_links:
    - from: ".planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md"
      to: "live tenant inventory on oacnwalhcpqdabivweki"
      via: "UUIDs transcribed from a live SELECT, not from stale prose"
      pattern: "23264962"
---

<objective>
Produce the human-reviewed delete-allowlist / preserve-list document (QA-02).

Purpose: Success Criterion #2 — a document must exist that explicitly names the standing QA fixture(s) to preserve and enumerates the tenants safe to delete. The orchestrator has already ratified the allowlist (LOCKED): PRESERVE = the one tenant with real data; DELETE = every other tenant. This plan writes that ratification into a durable doc that Plan 03 transcribes its constants from.
Output: `.planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-dev-qa-test-data-hygiene/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-query live tenant inventory (authoritative source)</name>
  <files>.planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md</files>
  <action>
Before transcribing anything, pull the CURRENT tenant inventory from live data via `mcp__plugin_supabase_supabase__execute_sql` (project_id `oacnwalhcpqdabivweki`). Do NOT rely solely on the research's inventory table — the research itself flags that the snapshot can drift AND its prose ("10 delete candidates") disagrees with its own table (which lists 9 non-fixture UUIDs). Live data is the tiebreaker.

Run a single query joining per-tenant data counts, e.g.:
```sql
SELECT t.id, t.name, t.slug, t.is_test,
  (SELECT count(*) FROM public.users u WHERE u.tenant_id = t.id)  AS users,
  (SELECT count(*) FROM public.rooms r WHERE r.tenant_id = t.id)  AS rooms,
  (SELECT count(*) FROM public.controlled_incidents ci WHERE ci.tenant_id = t.id) AS incidents
FROM public.tenants t
ORDER BY users DESC, rooms DESC, t.name;
```
(Adjust child-table names if a count errors; users/rooms/controlled_incidents are the load-bearing ones — the fixture is the tenant with users and rooms, and no candidate may own controlled_incidents.)

Capture the full result set — you will transcribe it verbatim into the doc in Task 2. Confirm exactly one tenant (`23264962-aa09-4e4f-a49d-fc345cc91414`, "Sonesta ES Suites Fossil Creek", slug `-2`) has real users+rooms; that is the PRESERVE fixture. Every other tenant is a delete candidate.
  </action>
  <verify>The query returns the tenant list with counts; the fixture 23264962-... is the sole row with users>0 and rooms>0, and it has incidents=0 (or its incidents remain untouched — they are never deleted regardless).</verify>
  <done>You hold an authoritative, current list of all tenants split into exactly one PRESERVE fixture and N delete candidates.</done>
</task>

<task type="auto">
  <name>Task 2: Write 21-ALLOWLIST.md</name>
  <files>.planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md</files>
  <action>
Write `.planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md` with these sections:

1. **Header + ratification statement** — state that this allowlist was ratified by the orchestrator with delegated user authority on 2026-08-04, that the research's live tenant inventory served as the human review, and that live data was re-confirmed at authoring time (Task 1).

2. **PRESERVE (never delete)** — a table with the fixture: `23264962-aa09-4e4f-a49d-fc345cc91414` | Sonesta ES Suites Fossil Creek (slug `-2`) | rationale: only tenant with real data (users, rooms, tasks, work orders) — the standing QA fixture. State plainly: the cleanup script MUST keep `is_test = false` on this row and MUST NOT include it in any delete.

3. **DELETE ALLOWLIST** — a table of every non-fixture tenant from the Task 1 live query: `tenant_id | name | slug | users | rooms | why safe to delete`. Include ALL of them (do not trust the research's "10" vs its table's "9" — use the live count from Task 1 and state the exact number explicitly, e.g. "N delete-eligible tenants as of 2026-08-04"). Flag the confusing near-duplicate "Sonesta ES Suites Fossil Creek" empty rows and the `isoval-…` isolation-validation leftover so a reviewer sees why each is safe.

4. **Excluded tables (append-only)** — explicitly record that `controlled_incidents` and `controlled_incident_events` are NEVER touched by the cleanup script: they carry live BEFORE UPDATE/DELETE immutability triggers (migration 070) that fire even for the service-role key, and are Texas-safety compliance records. This is QA-03's exclusion requirement, documented here for the reviewer.

5. **Scope note** — this doc authorizes the TOOL's allowlist only; it does not authorize a real `--execute` run. A destructive run is a separate, later, human-authorized step, and the live inventory should be re-queried immediately before it.

Do NOT delete or modify any tenant data in this task — this is a documentation deliverable only.
  </action>
  <verify>`.planning/phases/21-dev-qa-test-data-hygiene/21-ALLOWLIST.md` exists, names `23264962-aa09-4e4f-a49d-fc345cc91414` under PRESERVE, lists every non-fixture tenant UUID under DELETE ALLOWLIST with a stated exact count, and names both `controlled_incidents` and `controlled_incident_events` as excluded.</verify>
  <done>The document explicitly identifies the preserve fixture, the full delete allowlist (count stated), and the two excluded append-only tables — reviewable by a human without touching the database.</done>
</task>

</tasks>

<verification>
- `21-ALLOWLIST.md` exists with PRESERVE, DELETE ALLOWLIST, and excluded-tables sections.
- The PRESERVE fixture UUID is explicitly named and marked never-delete.
- Delete allowlist UUIDs match the live inventory from Task 1 (not stale prose); the exact count is stated.
- No tenant data was modified by this plan.
</verification>

<success_criteria>
Phase Success Criterion #2 satisfied: a human-reviewed hotel_id delete-allowlist and preserve-list document exists, explicitly naming the standing QA fixture tenant to keep.
</success_criteria>

<output>
After completion, create `.planning/phases/21-dev-qa-test-data-hygiene/21-02-SUMMARY.md` recording the doc path, the PRESERVE UUID, and the exact delete-allowlist count derived from live data.
</output>
