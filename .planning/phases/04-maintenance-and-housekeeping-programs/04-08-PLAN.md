---
phase: 04-maintenance-and-housekeeping-programs
plan: 08
type: execute
wave: 7
depends_on: ["04-05", "04-07"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx
  - apps/web/components/engineering/PMCompletionModal.tsx
  - apps/web/components/programs/HousekeepingDepthPanels.tsx
  - apps/web/components/engineering/WorkOrderCard.tsx
  - apps/web/eslint.config.mjs
  - apps/web/package.json
  - apps/web/scripts/verify-i18n-gate.mjs
  - apps/web/playwright.phase4.config.ts
  - apps/web/e2e/phase4-programs.spec.ts
autonomous: true
requirements: [BL-01, BL-02, BL-03, D-03, D-04, G10]

must_haves:
  truths:
    - "PM completion, deep-clean, DND config, work-orders, tasks, and their empty/error/notification copy render in English and Spanish"
    - "A raw user-facing string literal added to a floor-facing directory fails `npm run lint`"
    - "GM-facing analytics/config/inspector-export dirs are allowlisted and stay English"
    - "Critical floor workflows are verified in EN and ES at 390px via Playwright"
  artifacts:
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Spanish keys for all floor-role critical workflows"
      contains: "programs"
    - path: "apps/web/eslint.config.mjs"
      provides: "Scoped no-literal-string override on floor-facing dirs"
      contains: "no-literal-string"
    - path: "apps/web/playwright.phase4.config.ts"
      provides: "EN/ES 390px config"
      contains: "390"
  key_links:
    - from: "floor components"
      to: "useTranslation()/t()"
      via: "react-i18next keys in en.ts/es.ts"
      pattern: "useTranslation"
    - from: "eslint.config.mjs floor override"
      to: "i18next/no-literal-string"
      via: "scoped files glob + allowlist"
      pattern: "no-literal-string"
---

<objective>
Slice 4C — the bilingual floor contract (closing slice, D-02/D-03/D-04). Extend the LIVE react-i18next locale objects (`i18n/locales/en.ts` + `es.ts` — NOT the deleted `.json` variants, bug-448/bug-448) across every floor-role critical workflow this phase touched, add a SCOPED CI hard-fail that prevents new untranslated floor-facing copy from landing (D-04), and verify the critical workflows at phone width (390px) in both languages. GM-facing analytics/config/inspector-export stay English (D-03 carve-out).

Purpose: Critical floor workflows are genuinely usable in English and Spanish (Phase 4 exit criteria), and the gate keeps them that way.
Output: Full EN/ES coverage of floor surfaces, a scoped `no-literal-string` gate wired into the existing lint job with a proof fixture, and EN/ES 390px Playwright coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-05-SUMMARY.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-07-SUMMARY.md

<interfaces>
<!-- READ apps/web/node_modules/next/dist/docs/ before non-trivial web work (AGENTS.md). -->
<!-- Run `npm view eslint-plugin-i18next version` before pinning (research A1). -->

i18n (react-i18next 17.0.10 / i18next 26.3.6):
  Edit ONLY apps/web/i18n/locales/en.ts + es.ts. The `programs` namespace already exists (~line 110 in each).
  `.json` locale files were deleted — do NOT recreate them (bug-448).
  Usage pattern in components: `const { t } = useTranslation()` then `t('namespace.key')`.

Floor-facing surfaces needing translation (D-03 scope):
  - app/(dashboard)/engineering/pm-schedules/page.tsx (G10: hardcoded "PM Schedules","Overdue","Complete",
    "Deactivate","Create WO", both modals)
  - components/engineering/PMCompletionModal.tsx (from 04-05)
  - components/programs/HousekeepingDepthPanels.tsx (from 04-07 — deep-clean, DND, par, stayover, sampling)
  - components/engineering/* (G10: WorkOrderCard t()=2, WorkOrderList=1, FailurePredictionSidebar=1,
    EngineeringRoomBoard=3 — low counts, hardcoded English)
  - app/(dashboard)/tasks, housekeeping surfaces + their empty-state/error/validation/notification copy

STAYS ENGLISH (D-03 allowlist): GM dashboards, config, inspector export, reports/analytics.

eslint (flat config — apps/web/eslint.config.mjs):
  Base = eslint-config-next/core-web-vitals. Existing lint job runs `npm run lint` (CI lint-web).
  Research scoped override target dirs:
    components/housekeeping/**, components/engineering/**,
    app/(dashboard)/{housekeeping,engineering,tasks,programs}/**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate all floor-role critical workflows (EN + ES)</name>
  <read_first>
    - apps/web/i18n/locales/en.ts (programs namespace ~line 110, structure)
    - apps/web/i18n/locales/es.ts (mirror structure)
    - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx (hardcoded English strings, G10)
    - apps/web/components/engineering/PMCompletionModal.tsx (from 04-05)
    - apps/web/components/programs/HousekeepingDepthPanels.tsx (from 04-07)
    - apps/mobile/i18n/ (existing EN/ES floor-string translations to draw Spanish wording from — reference only, mobile stays parked)
  </read_first>
  <action>
    Add keys to `en.ts` + `es.ts` (matching nested structure; every EN key MUST have an ES counterpart) covering: pm-schedules page (title, statuses Overdue/Due Soon/Upcoming, actions Complete/Deactivate/Create WO, both modals), PM completion form (item results pass/fail/n-a, verifier, measurements, parts, defects, evidence, failed-check corrective notice, validation/error copy), housekeeping depth panels (deep-clean, public areas, DND policy timing + escalation instructions, par alerts, stayover, sampling + quality labels), engineering components (WorkOrderCard/List, FailurePredictionSidebar, EngineeringRoomBoard), tasks, and all empty-state / error / validation / notification copy on these surfaces. Then rewire each floor component to call `useTranslation()` + `t('...')` in place of the raw strings. Draw Spanish wording from `apps/mobile/i18n` where equivalent strings exist. Do NOT translate GM/config/inspector-export/analytics surfaces (D-03).
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx` >= 1
    - Every new key present in BOTH en.ts and es.ts (spot-check: no key in en.ts missing from es.ts for the added namespaces)
    - `grep -v '^\s*//' apps/web/i18n/locales/es.ts | grep -c "programs\|pmCompletion\|deepClean\|dnd"` shows the new namespaces
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>All floor surfaces render via t() with matching EN + ES keys; GM surfaces untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Scoped no-literal-string CI gate + proof fixture (D-04)</name>
  <read_first>
    - apps/web/eslint.config.mjs (flat config)
    - apps/web/package.json (lint script + devDependencies)
    - 04-RESEARCH.md §Code Examples (scoped ESLint no-literal-string snippet)
  </read_first>
  <action>
    (a) Run `npm view eslint-plugin-i18next version`, then `npm install --save-dev eslint-plugin-i18next@<pinned>` in apps/web.
    (b) In `eslint.config.mjs`, add a scoped override AFTER the Next config: `import i18next from 'eslint-plugin-i18next'`, then a block with `files: ['components/housekeeping/**','components/engineering/**','app/(dashboard)/{housekeeping,engineering,tasks,programs}/**']`, `plugins: { i18next }`, `rules: { 'i18next/no-literal-string': ['error', { markupOnly: true, 'jsx-attributes': { include: ['aria-label','placeholder','title'] } }] }`. Add an allowlist ignore for GM/admin/config/export dirs + test ids so D-03 English surfaces are exempt. The rule plugs into the existing `npm run lint` (no new CI job — research anti-pattern).
    (c) Create `apps/web/scripts/verify-i18n-gate.mjs`: programmatically run ESLint (the `ESLint` API) against an in-memory/temp fixture string containing a raw JSX literal MAPPED to a floor-facing file path, and assert that a `i18next/no-literal-string` error IS reported (exit non-zero if the gate does NOT fire). This proves the gate bites without committing a real lint failure into a floor dir. Add an npm script `verify:i18n-gate`.
  </action>
  <verify>
    <automated>cd apps/web && npm run lint && node scripts/verify-i18n-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "no-literal-string" apps/web/eslint.config.mjs` present with the scoped files glob + allowlist
    - `eslint-plugin-i18next` in apps/web/package.json devDependencies (pinned)
    - `npm run lint` passes clean on the now-translated tree (proves no residual floor literals)
    - `node scripts/verify-i18n-gate.mjs` exits 0 (gate fired on the fixture)
  </acceptance_criteria>
  <done>Floor-facing raw literals hard-fail lint; GM dirs allowlisted; a fixture proves the gate bites.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright EN/ES 390px coverage (BL-03)</name>
  <read_first>
    - apps/web/playwright.phase1.config.ts (existing phase config to mirror)
    - apps/web/e2e/phase1-work-orders.spec.ts (spec structure + auth setup)
    - .planning/phases/04-maintenance-and-housekeeping-programs/04-VALIDATION.md (Wave 0: playwright.phase4.config.ts + spec)
  </read_first>
  <action>
    Create `apps/web/playwright.phase4.config.ts` mirroring the phase1 config but with a 390px-wide viewport project (phone width). Create `apps/web/e2e/phase4-programs.spec.ts` covering, in BOTH English and Spanish (toggle the language), the critical floor workflows: PM completion form (open + core labels render translated), deep-clean schedule config, and DND welfare policy config. Assert a representative translated string differs between EN and ES (e.g., a heading), and that the 390px layout has no horizontal overflow / clipped controls on these screens. Reuse the existing e2e auth/session bootstrap.
  </action>
  <verify>
    <automated>cd apps/web && npx playwright test --config=playwright.phase4.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/playwright.phase4.config.ts` exists with a 390px viewport project
    - `apps/web/e2e/phase4-programs.spec.ts` covers PM completion + deep-clean + DND in EN and ES
    - Playwright run green (or documented env prerequisite if the dev server/auth is unavailable locally)
  </acceptance_criteria>
  <done>Critical floor workflows verified in EN + ES at 390px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor-facing copy -> CI lint | Untranslated literals must be blocked before merge |
| GM-facing dirs -> gate | Must be exempt (D-03) without weakening floor enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-26 | Tampering (process) | untranslated floor copy regressions | mitigate | Scoped hard-fail no-literal-string in the existing lint job (D-04); proof fixture asserts the gate fires |
| T-04-27 | (Usability integrity) | ES parity gaps | mitigate | Every EN key requires an ES counterpart; Playwright asserts EN≠ES on representative strings at 390px |
| T-04-28 | Over-broad enforcement | GM English surfaces flagged | accept | GM/admin/config/export dirs allowlisted per D-03 carve-out |
</threat_model>

<verification>
- `cd apps/web && npm run lint && npm run type-check` clean
- `node scripts/verify-i18n-gate.mjs` proves the gate bites
- `npx playwright test --config=playwright.phase4.config.ts` green (EN + ES @ 390px)
</verification>

<success_criteria>
- Floor workflows usable in EN + ES incl. empty/error/notification copy (BL-01)
- Scoped hard-fail gate blocks new floor-facing literals; GM dirs exempt (BL-02/D-04)
- EN/ES 390px Playwright coverage over PM completion, deep-clean, DND (BL-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-08-SUMMARY.md`
</output>
