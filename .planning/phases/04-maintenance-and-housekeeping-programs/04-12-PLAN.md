---
phase: 04-maintenance-and-housekeeping-programs
plan: 12
type: execute
wave: 4
depends_on: ["04-11"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/app/(dashboard)/housekeeping/inspections/page.tsx
  - apps/web/app/(dashboard)/housekeeping/rooms/page.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The housekeeping inspections page and rooms page render every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/app/(dashboard)/housekeeping/inspections/page.tsx"
      provides: "Bilingual inspections route"
      contains: "useTranslation"
    - path: "apps/web/app/(dashboard)/housekeeping/rooms/page.tsx"
      provides: "Bilingual rooms route"
      contains: "useTranslation"
  key_links:
    - from: "app/(dashboard)/housekeeping/rooms/page.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under housekeeping.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the remaining housekeeping route pages `.../inspections/page.tsx` and `.../rooms/page.tsx` into EN + ES. Both are inside D-04's locked gate glob and D-03's housekeeping directory scope; they must be literal-free before the gate widens (04-16).

Purpose: Complete D-03 housekeeping-directory coverage.
Output: inspections + rooms route pages fully bilingual with matching en.ts/es.ts keys.

Depends on 04-11 (shares en.ts/es.ts — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
Namespace assignment (under top-level `housekeeping`):
  - housekeeping.inspectionsPage.* → housekeeping/inspections/page.tsx
  - housekeeping.roomsPage.*       → housekeeping/rooms/page.tsx
Reuse shared keys (housekeeping.roomStatus.*/roomCard.*/inspectionModal.* from 04-09/04-10, common.*, nav.*) where copy is identical rather than duplicating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate housekeeping/inspections/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/housekeeping/inspections/page.tsx (564 lines — ~33 literals)
    - apps/web/components/housekeeping/InspectionModal.tsx (already translated in 04-10 — reuse its inspection keys where copy matches)
    - apps/web/i18n/locales/en.ts (existing housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.inspectionsPage.*` keys to en.ts AND es.ts for all user-facing copy (heading, filter/tab labels, quality-trend and sampling labels, action buttons, empty/error states, aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Reuse housekeeping.inspectionModal.* / programs.sampling.* keys where identical. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/housekeeping/inspections/page.tsx"` returns >= 1
    - `grep -c "inspectionsPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>inspections/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate housekeeping/rooms/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/housekeeping/rooms/page.tsx (819 lines — ~37 literals)
    - apps/web/components/housekeeping/RoomStatusBoard.tsx (translated in 04-09 — reuse roomStatus keys where copy matches)
    - apps/web/i18n/locales/en.ts (existing housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.roomsPage.*` keys to en.ts AND es.ts for all user-facing copy (heading, status/filter labels, action buttons, empty/loading/error states, aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Reuse housekeeping.roomStatus.*/roomCard.* keys where identical. Leave dynamic room data raw or wrap with data-i18n-skip. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/housekeeping/rooms/page.tsx"` returns >= 1
    - `grep -c "roomsPage" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>rooms/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | Inside the D-04 gate glob; must be literal-free before 04-16 widens it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-35 | Tampering (process) | untranslated HK route copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-36 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- Both pages return `useTranslation` grep count >= 1
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- inspections + rooms pages usable in EN + ES; housekeeping directory fully covered (D-03/BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-12-SUMMARY.md`
</output>
