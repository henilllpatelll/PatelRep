---
phase: 04-maintenance-and-housekeeping-programs
plan: 16
type: execute
wave: 8
depends_on: ["04-09", "04-10", "04-11", "04-12", "04-13", "04-14", "04-15"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/app/(dashboard)/tasks/page.tsx
  - apps/web/components/engineering/PMCompletionModal.tsx
  - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx
  - apps/web/eslint.config.mjs
autonomous: true
requirements: [BL-01, BL-02, D-03, D-04]

must_haves:
  truths:
    - "The tasks page renders every user-facing string via t() in EN and ES"
    - "The FULL D-03 locked bilingual floor contract is covered: components/housekeeping/**, components/engineering/**, components/programs/**, and app/(dashboard)/{housekeeping,engineering,tasks,programs}/** all render floor copy via t() with EN + ES parity"
    - "A raw user-facing string literal added anywhere in the full floor-facing directory set fails `npm run lint`"
    - "GM-facing analytics/config/inspector-export dirs remain English and are allowlisted"
    - "The two previously-uncaught non-word-shaped placeholder literals (PMCompletionModal e.g. 45, pm-schedules UUID pattern) are wrapped in t()"
  artifacts:
    - path: "apps/web/eslint.config.mjs"
      provides: "Widened no-literal-string gate over the full floor-facing directory set"
      contains: "components/housekeeping"
    - path: "apps/web/app/(dashboard)/tasks/page.tsx"
      provides: "Bilingual tasks route"
      contains: "useTranslation"
  key_links:
    - from: "eslint.config.mjs floor override"
      to: "i18next/no-literal-string"
      via: "widened files glob covering all floor dirs + GM allowlist"
      pattern: "no-literal-string"
    - from: "app/(dashboard)/tasks/page.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under tasks.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Final closing slice of the D-03/D-04 bilingual floor contract: (1) translate the last floor surface — `app/(dashboard)/tasks/page.tsx` — into EN + ES; (2) fix the two non-word-shaped placeholder literals the current narrow gate missed (04-VERIFICATION.md Anti-Patterns); (3) widen the `eslint.config.mjs` `i18next/no-literal-string` gate from the narrow 6-file scope to the FULL floor-facing directory set named by D-04, now that every file in that set is translated (04-09 → 04-15). After this plan, the next goal-backward verifier must check against D-03's full locked scope, not the narrowed scope 04-08 shipped.

Purpose: Close truth #34 (bilingual floor contract) and truth #32 (gate blind spots) from 04-VERIFICATION.md; make the gate protect all floor surfaces going forward.
Output: tasks page bilingual, two literals fixed, widened hard-fail gate, `npm run lint` clean.

Depends on ALL translation plans (04-09..04-15) — this MUST run last, because widening the glob before those files are translated would hard-fail `npm run lint` on untranslated code.

Note (resolved, no task needed): migrations 081 (pm_evidence_linkage) and 083 (program_template_facilities) are confirmed applied to production Supabase project oacnwalhcpqdabivweki (evidence_records_related_entity_type_check includes 'pm_completion'; the facility/document canonical allowlists include 'backflow' and 'domestic_water'). 04-VERIFICATION.md's first human_verification item is therefore resolved and must not be re-flagged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-VERIFICATION.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-08-SUMMARY.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
  Namespace assignment: tasks.* → app/(dashboard)/tasks/page.tsx.

eslint (flat config — apps/web/eslint.config.mjs): the scoped override currently lists exactly 6 files
  (lines ~33-41). Widen its `files` array to the full floor-facing directory set; keep the existing
  `ignores` allowlist (reports/**, billing/**, settings/**, *inspector-export*, *.test.*, *.spec.*) so
  the D-03 GM/config/export carve-out holds. Replace the narrowing comment block (lines ~19-32) with a
  note that the full floor set is now covered. Rule config stays identical:
    'i18next/no-literal-string': ['error', { markupOnly: true, 'jsx-attributes': { include: ['aria-label','placeholder','title'] } }]

  Target widened files glob (paths are relative to apps/web, matching the existing entries' style):
    'components/housekeeping/**'
    'components/engineering/**'
    'components/programs/**'
    'app/(dashboard)/housekeeping/**'
    'app/(dashboard)/engineering/**'
    'app/(dashboard)/tasks/**'
    'app/(dashboard)/programs/**'

  Note: components/programs/** (HousekeepingDepthPanels/DeepCleanAreasPanel/InspectionDepthPanel) was
  already translated by 04-08 but is NOT in the current narrow 6-file gate scope. Include it in the
  widened glob so it keeps hard-fail protection going forward — omitting it would silently drop
  enforcement on already-bilingual floor panels (plan-checker finding, 2026-07-23).

  The existing proof fixture apps/web/scripts/verify-i18n-gate.mjs (npm run verify:i18n-gate) still
  applies — it asserts the rule fires on a floor path; re-run it unchanged to confirm the gate still bites.

Anti-pattern literals to fix (04-VERIFICATION.md):
  - components/engineering/PMCompletionModal.tsx:472 → placeholder="e.g. 45" (add e.g. programs.pmCompletion.intervalHint or reuse an existing hint key)
  - app/(dashboard)/engineering/pm-schedules/page.tsx:389 → placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" (add e.g. programs.pmSchedules.addModal.assetIdPlaceholder)
  Both files are ALREADY inside the current narrow gate; these slipped past because the markupOnly
  word-shape heuristic ignores non-word literals. Wrapping them in t() closes truth #32.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate tasks/page.tsx</name>
  <read_first>
    - apps/web/app/(dashboard)/tasks/page.tsx (715 lines — ~41 literals)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog for t() wiring)
    - apps/web/i18n/locales/en.ts (structure; nav.tasks already exists)
  </read_first>
  <action>
    Add a top-level `tasks.*` namespace to en.ts AND es.ts for all user-facing copy in tasks/page.tsx (heading/eyebrow, status/priority labels, filter/tab labels, action buttons, form/modal copy, empty/loading/error/validation states, aria-label/placeholder/title). Rewire tasks/page.tsx to `useTranslation()` + `t('tasks...')`. Reuse common.*/nav.* keys where identical. Leave dynamic task data raw or wrap non-copy nodes with `data-i18n-skip="true"`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" "apps/web/app/(dashboard)/tasks/page.tsx"` returns >= 1
    - `grep -c "^  tasks:" apps/web/i18n/locales/en.ts` and `... es.ts` both return 1 (namespace present in both)
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>tasks/page.tsx renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two non-word-shaped placeholder literals</name>
  <read_first>
    - apps/web/components/engineering/PMCompletionModal.tsx (around line 472 — placeholder="e.g. 45")
    - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx (around line 389 — placeholder="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx")
    - apps/web/i18n/locales/en.ts (programs.pmCompletion.* and programs.pmSchedules.addModal.* namespaces already exist)
  </read_first>
  <action>
    Wrap both placeholders in t(). Add the two keys to en.ts AND es.ts under the existing programs namespaces (e.g. programs.pmCompletion for the "e.g. 45" hint, programs.pmSchedules.addModal.assetIdPlaceholder for the UUID hint), and replace the raw placeholder attributes with `placeholder={t('...')}`. Both files are already in the gate scope; this closes 04-VERIFICATION.md truth #32. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e.g. 45" apps/web/components/engineering/PMCompletionModal.tsx` returns 0
    - `grep -c "xxxxxxxx-xxxx" "apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx"` returns 0
    - Both new keys present in en.ts AND es.ts
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>Both non-word placeholders render via t(); no raw placeholder literal remains.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the no-literal-string gate to the full floor-facing directory set</name>
  <read_first>
    - apps/web/eslint.config.mjs (the scoped override at lines ~19-62; narrow files list at ~33-41, allowlist ignores at ~42-51)
    - apps/web/scripts/verify-i18n-gate.mjs (existing proof fixture — do not weaken it)
    - apps/web/package.json (lint + verify:i18n-gate scripts)
  </read_first>
  <action>
    In eslint.config.mjs, replace the narrow 6-file `files` array with the full floor-facing directory glob set: `components/housekeeping/**`, `components/engineering/**`, `components/programs/**`, `app/(dashboard)/housekeeping/**`, `app/(dashboard)/engineering/**`, `app/(dashboard)/tasks/**`, `app/(dashboard)/programs/**`. Keep the existing `ignores` allowlist unchanged (reports/**, billing/**, settings/**, *inspector-export*, *.test.*, *.spec.*) so the D-03 GM/config/export English carve-out holds. Update the narrowing comment block to state the gate now covers the full floor set (remove the "several sibling files predate this phase" caveat, which is no longer true). Keep the rule options identical (markupOnly + aria-label/placeholder/title). Do NOT modify the underlying i18next-plugin literal-shape heuristic. Then run `npm run lint` — it MUST pass clean, proving every file in the widened glob is now literal-free (this is the real end-to-end verification that all of 04-09..04-15 + Tasks 1-2 landed completely).
  </action>
  <verify>
    <automated>cd apps/web && npm run lint && node scripts/verify-i18n-gate.mjs && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "components/housekeeping" apps/web/eslint.config.mjs` returns >= 1 and `grep -c "app/(dashboard)/tasks" apps/web/eslint.config.mjs` returns >= 1
    - `grep -c "components/programs" apps/web/eslint.config.mjs` returns >= 1 (already-translated program-depth panels keep gate protection, not just the newly-translated dirs)
    - The GM allowlist entries (reports/billing/settings/inspector-export) are still present in the override `ignores`
    - `npm run lint` exits 0 with the widened glob active (no residual floor literals anywhere in the set)
    - `node scripts/verify-i18n-gate.mjs` exits 0 (gate still fires on a floor-path fixture)
  </acceptance_criteria>
  <done>The gate covers the full D-03/D-04 floor directory set; lint is clean; GM dirs stay allowlisted; the proof fixture still bites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint | Untranslated literals anywhere in the full floor set must hard-fail before merge |
| GM-facing dirs → gate | Must stay exempt (D-03) without weakening floor enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-43 | Tampering (process) | untranslated floor-copy regressions | mitigate | Widened scoped hard-fail no-literal-string over the full floor directory set (D-04); proof fixture asserts the gate fires |
| T-04-44 | (Usability integrity) | non-word-shaped literals slipping past gate | mitigate | The two known instances wrapped in t(); markupOnly rule retained (word-shape heuristic is a plugin limitation, accepted below) |
| T-04-45 | Over-broad enforcement | GM English surfaces flagged | accept | reports/billing/settings/inspector-export allowlisted per D-03 carve-out |
| T-04-46 | (Coverage) | plugin misses non-word literals generally | accept | Underlying literal-shape heuristic is plugin behavior, out of this phase's scope; two known instances fixed directly |
</threat_model>

<verification>
- `cd apps/web && npm run lint` clean with the widened glob (end-to-end proof all floor files are literal-free)
- `node scripts/verify-i18n-gate.mjs` exits 0
- `npm run type-check` clean
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- tasks page usable in EN + ES; full D-03 floor directory scope complete (BL-01)
- Widened hard-fail gate blocks new floor-facing literals across all floor dirs; GM dirs exempt (BL-02/D-04)
- The two non-word placeholder blind spots closed (truth #32)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-16-SUMMARY.md`
</output>
