---
phase: 04-maintenance-and-housekeeping-programs
plan: 10
type: execute
wave: 2
depends_on: ["04-09"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/components/housekeeping/RoomDetailDrawer.tsx
  - apps/web/components/housekeeping/InspectionModal.tsx
  - apps/web/components/housekeeping/OccupancyImportModal.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The room detail drawer, inspection modal, and occupancy import modal render every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/components/housekeeping/RoomDetailDrawer.tsx"
      provides: "Bilingual room detail drawer"
      contains: "useTranslation"
    - path: "apps/web/components/housekeeping/InspectionModal.tsx"
      provides: "Bilingual inspection modal"
      contains: "useTranslation"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Spanish keys for HK drawer/modal surfaces"
      contains: "roomDetail"
  key_links:
    - from: "components/housekeeping/RoomDetailDrawer.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under housekeeping.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the deepest housekeeping floor surfaces — the Room Detail Drawer (1199 lines, the drill-down housekeepers open per room), the Inspection Modal, and the Occupancy Import Modal — into EN + ES. Continues closing the D-03 locked bilingual floor contract (04-VERIFICATION.md truth #34).

Purpose: These are the highest-detail daily housekeeping interactions; D-03 requires them bilingual.
Output: RoomDetailDrawer, InspectionModal, OccupancyImportModal fully bilingual with matching en.ts/es.ts keys.

Depends on 04-09 (shares en.ts/es.ts — sequential to avoid locale-file write conflicts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
  Pattern (analog: components/engineering/WorkOrderCard.tsx): `const { t } = useTranslation()` then `t('housekeeping.roomDetail.key')`.
Namespace assignment for this plan (under top-level `housekeeping`):
  - housekeeping.roomDetail.*      → RoomDetailDrawer.tsx
  - housekeeping.inspectionModal.* → InspectionModal.tsx
  - housekeeping.occupancyImport.* → OccupancyImportModal.tsx
Reuse existing shared keys where identical copy already exists (e.g. common.cancel, programs.pmCompletion.* for evidence/verifier wording) rather than duplicating.
Spanish reference: apps/mobile/i18n/ (reference only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate RoomDetailDrawer.tsx</name>
  <read_first>
    - apps/web/components/housekeeping/RoomDetailDrawer.tsx (1199 lines — read fully; ~44 user-facing literals across section headings, tabs, status/action labels, form labels, empty/error copy, aria-label/placeholder/title)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog)
    - apps/web/i18n/locales/en.ts (housekeeping.* keys from 04-09)
  </read_first>
  <action>
    Add `housekeeping.roomDetail.*` keys to en.ts AND es.ts for every user-facing string in RoomDetailDrawer.tsx (tab labels, section headings, status/priority labels, action buttons, form field labels, note/placeholder text, empty and error states, all aria-label/placeholder/title). Rewire to `useTranslation()` + `t()`. Where identical copy already exists in shared namespaces (common.*, programs.*), reference the existing key instead of duplicating. Leave dynamic data (room numbers, timestamps, guest data) raw or wrap non-copy nodes with `data-i18n-skip="true"`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/housekeeping/RoomDetailDrawer.tsx` returns >= 1
    - `grep -c "roomDetail" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>RoomDetailDrawer renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate InspectionModal.tsx</name>
  <read_first>
    - apps/web/components/housekeeping/InspectionModal.tsx (613 lines — ~24 literals)
    - apps/web/i18n/locales/en.ts (housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.inspectionModal.*` keys to en.ts AND es.ts covering headings, inspection item labels, pass/fail/result labels, action buttons, validation/error copy, aria-label/placeholder/title. Rewire InspectionModal.tsx to `useTranslation()` + `t()`. Reuse existing shared result/severity keys (e.g. programs.pmCompletion.resultPassed/resultFailed) where the copy is identical. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/housekeeping/InspectionModal.tsx` returns >= 1
    - `grep -c "inspectionModal" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>InspectionModal renders via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 3: Translate OccupancyImportModal.tsx</name>
  <read_first>
    - apps/web/components/housekeeping/OccupancyImportModal.tsx (255 lines — ~18 literals)
    - apps/web/i18n/locales/en.ts (housekeeping.* keys)
  </read_first>
  <action>
    Add `housekeeping.occupancyImport.*` keys to en.ts AND es.ts covering title, upload/import instructions, column/field labels, validation and error copy, action buttons, aria-label/placeholder/title. Rewire OccupancyImportModal.tsx to `useTranslation()` + `t()`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/housekeeping/OccupancyImportModal.tsx` returns >= 1
    - `grep -c "occupancyImport" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>OccupancyImportModal renders via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | These files must be literal-free before the gate widens (04-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-31 | Tampering (process) | untranslated HK drawer/modal copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-32 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart (grep-verified equal counts) |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- All three files return `useTranslation` grep count >= 1
- en.ts / es.ts new-key counts equal (parity)
</verification>

<success_criteria>
- RoomDetailDrawer, InspectionModal, OccupancyImportModal usable in EN + ES (D-03/BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-10-SUMMARY.md`
</output>
