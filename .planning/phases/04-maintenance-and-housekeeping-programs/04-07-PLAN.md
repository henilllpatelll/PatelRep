---
phase: 04-maintenance-and-housekeeping-programs
plan: 07
type: execute
wave: 6
depends_on: ["04-05", "04-06"]
files_modified:
  - apps/web/lib/api/programs.ts
  - apps/web/app/(dashboard)/programs/page.tsx
  - apps/web/components/programs/HousekeepingDepthPanels.tsx
  - apps/api/routers/internal.py
  - apps/api/tests/test_operational_programs.py
autonomous: false
requirements: [HK-01, HK-04, HK-05, HK-06, G12]

must_haves:
  truths:
    - "A manager can configure DND welfare timing (threshold hours) + a documented escalation policy from the web"
    - "The DND welfare cron reads the per-property policy threshold and escalation roles, writing one event per window (duplicate-prevented)"
    - "A manager can create/list deep-clean schedules and public areas and see next-due dates from the web"
    - "Linen/chemical/amenity par shortages surface as a passive dashboard badge"
    - "A manager can set stayover linen change-frequency rules and inspection sampling rules from the web"
  artifacts:
    - path: "apps/web/components/programs/HousekeepingDepthPanels.tsx"
      provides: "Deep-clean, public-area, DND policy, par/stayover, sampling/quality UI"
      min_lines: 150
    - path: "apps/web/lib/api/programs.ts"
      provides: "Complete HK typed clients (deep-clean, public-area, sampling, quality, DND, stayover, pars)"
      contains: "deepClean"
  key_links:
    - from: "ProgramsPage DND config"
      to: "PUT /programs/dnd-welfare-policy"
      via: "typed client"
      pattern: "dnd-welfare-policy"
    - from: "internal.py escalations/check DND block"
      to: "dnd_welfare_policies.threshold_hours"
      via: "per-property policy read"
      pattern: "dnd_welfare_policies"
    - from: "par alerts badge"
      to: "overview.supply_alerts"
      via: "passive dashboard surface"
      pattern: "supply_alerts"
---

<objective>
Slice 4B (completion + web). 04-06 delivered inspection sampling/quality and deep-clean/public-area API reads. This plan completes 4B: (1) surface the property-configurable DND welfare timing + documented escalation policy on the web and confirm the existing escalation cron reads it (the DND welfare loop is already implemented in `internal.py escalations/check` per research positive finding #6 — this plan does NOT rebuild it; it verifies the per-property policy wiring and adds the config UI); (2) linen/chemical/amenity par alerts as a PASSIVE dashboard badge (Claude's discretion per CONTEXT — passive chosen: no cron, lower risk, `build_supply_alerts` already computes shortages in the overview response; a queued `notification_deliveries` path is intentionally NOT added); (3) stayover linen change-frequency config; (4) the full 4B web depth surfaces on `(dashboard)/programs`.

Purpose: Deep-clean/public-area schedules replace paper calendars and DND welfare timing is property-configurable with a documented, duplicate-prevented escalation policy (Phase 4 exit criteria).
Output: Complete HK typed clients, the housekeeping depth UI, and a verified DND policy → cron read path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-05-SUMMARY.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-06-SUMMARY.md

<interfaces>
<!-- READ apps/web/node_modules/next/dist/docs/ before non-trivial web work (AGENTS.md). -->

Existing API (programs.py) the UI consumes:
  GET  /programs/overview -> { templates, deep_clean_schedules, supply_pars, supply_alerts,
                               inspection_sampling_rules, stayover_rule, dnd_welfare_policy }
  GET  /programs/deep-clean-schedules, GET /programs/public-areas   (added in 04-06)
  GET  /programs/inspection-sample, GET /programs/inspection-quality (04-06)
  POST /programs/public-areas, POST /programs/deep-clean-schedules, POST /.../{id}/complete
  POST /programs/supply-pars (upsert), PUT /programs/stayover-rule, PUT /programs/dnd-welfare-policy,
  POST /programs/inspection-sampling-rules
  UpdateDndWelfarePolicyRequest { threshold_hours 1-72, escalation_roles[], escalation_instructions? }

DND welfare cron (research positive finding #6 — ALREADY IMPLEMENTED, do not rebuild):
  apps/api/routers/internal.py escalations/check (lines ~601-669): reads dnd_welfare_policies.threshold_hours,
  writes ONE immutable dnd_welfare_events per window, dup-prevented via existing-event + existing-task checks,
  notifies escalation_roles through _notify_role (which writes notification_deliveries). Writes cron_health.

apps/web/lib/api/programs.ts (current — MISSING deep-clean, public-area, sampling, quality clients — G12):
  programsApi.overview / initializeTemplates / updateDndPolicy / updateStayoverRule / upsertSupplyPar

apps/web/app/(dashboard)/programs/page.tsx -> ProgramsPage (bilingual `programs` namespace already exists ~en.ts:110)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the housekeeping typed clients + confirm DND policy → cron read path</name>
  <read_first>
    - apps/web/lib/api/programs.ts (current programsApi)
    - apps/api/routers/internal.py (escalations/check DND block ~601-669 — CONFIRM it reads dnd_welfare_policies per-tenant)
    - apps/api/routers/programs.py (routes added in 04-06)
  </read_first>
  <action>
    (a) Extend `apps/web/lib/api/programs.ts` with typed clients + return types for: `listDeepCleanSchedules()`, `createDeepCleanSchedule(payload)`, `completeDeepClean(id, payload)`, `listPublicAreas()`, `createPublicArea(payload)`, `inspectionSample()`, `inspectionQuality()`, `upsertSamplingRule(payload)`. Keep the existing DND/stayover/par clients. Extend the `ProgramOverview` interface with any new fields.
    (b) CONFIRM (read + note in SUMMARY) that `internal.py escalations/check` reads `dnd_welfare_policies` per tenant (threshold_hours + escalation_roles) and dup-prevents. If — and only if — the cron uses a hardcoded threshold instead of the policy row, wire it to read the per-property policy (tenant-scoped, None-safe). Do not add a new cron or a parallel DND mechanism (D-07/D-10). Add/confirm a unit test in `test_operational_programs.py` for `should_create_dnd_escalation` covering threshold + existing-event dedup.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check && cd ../api && python -m pytest tests/test_operational_programs.py -k "dnd" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deepClean\|publicArea\|inspectionSample\|inspectionQuality\|samplingRule" apps/web/lib/api/programs.ts` shows the new clients
    - SUMMARY confirms the DND cron reads dnd_welfare_policies per tenant (or records the wiring fix)
    - `npm run type-check` clean; DND dedup unit test green
  </acceptance_criteria>
  <done>All HK clients typed; DND policy→cron read path confirmed (or wired) with dedup test.</done>
</task>

<task type="auto">
  <name>Task 2: Build the housekeeping program depth UI</name>
  <read_first>
    - apps/web/app/(dashboard)/programs/page.tsx (ProgramsPage — existing template/DND/stayover/par sections)
    - apps/web/components/ui/ (Card, Button, Input)
    - apps/web/lib/hooks/useRole.ts (manager gating)
  </read_first>
  <action>
    Create `apps/web/components/programs/HousekeepingDepthPanels.tsx` and wire it into ProgramsPage. Panels (all manager-gated via useRole):
    - Deep-clean schedules: list (target, next_due_on, interval), create form (room|public_area target, interval_days, next_due_on), complete action.
    - Public areas: list + create (name, location_detail).
    - DND welfare policy config: threshold_hours slider/input (1-72), escalation_roles multi-select (housekeeping_supervisor/front_desk/gm), escalation_instructions textarea → PUT /programs/dnd-welfare-policy. Present it as the documented escalation policy.
    - Par alerts: PASSIVE badge listing `overview.supply_alerts` shortages (name, on_hand/par, shortage); par upsert form (linen/chemical/amenity). No notification queueing.
    - Stayover rule: linen_change_frequency_days + opt_out_allowed → PUT /programs/stayover-rule.
    - Inspection: sampling-rule config (room_type/experience_band/risk/sample_percent) + a quality-trends view rendering by_item/by_room_type/by_employee from `inspectionQuality()`, and a "today's sample" view from `inspectionSample()`.
    Use React Query (invalidate `['programs-overview']` on mutations). Keep `min-h-[44px]` touch targets. Plain English strings here (4C/04-08 applies i18n; the `programs` namespace already exists and should be extended there, not final-hardcoded in a way that blocks it).
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/programs/HousekeepingDepthPanels.tsx` exists (>=150 lines) covering deep-clean, public-area, DND policy, par (passive), stayover, sampling + quality
    - ProgramsPage imports and renders it
    - `npm run type-check && npm run lint` clean
  </acceptance_criteria>
  <done>The full 4B depth UI is present and type/lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Housekeeping program depth UI: deep-clean schedules + public areas, DND welfare timing/escalation policy config, passive par-alert badges, stayover rules, and inspection sampling + quality trends — all wired to the tenant-scoped programs API.</what-built>
  <how-to-verify>
    1. Start `npm run dev:api` + `npm run dev:web`. Log in as the GM test account. Go to /programs.
    2. Create a public area, then a deep-clean schedule targeting it; confirm it appears with a next-due date.
    3. Set the DND welfare threshold to a low value, add escalation instructions, save; reload and confirm persistence.
    4. Add a supply par where on_hand < par_level; confirm a passive shortage badge appears.
    5. Set a stayover linen frequency and an inspection sampling rule; open the quality-trends view and confirm it renders by item/room-type/employee (empty state acceptable if no inspections).
    6. Confirm no console errors and every read is scoped to the property.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager -> HK program config | Only managers may configure DND/par/stayover/deep-clean |
| tenant A -> tenant B HK config | Cross-tenant config read/write attempt |
| DND policy -> escalation cron | Per-property timing must not leak across tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Elevation of Privilege | HK config mutations | mitigate | All config routes already require_role management set; UI gates on useRole; ASVS V4 |
| T-04-23 | Info Disclosure / Tampering | cross-tenant HK config | mitigate | Every query `.eq("tenant_id")`; overview + config reads tenant-scoped |
| T-04-24 | Repudiation | DND welfare escalation | mitigate | Existing cron writes one immutable dnd_welfare_events per window (dup-prevented) + notification_deliveries; not rebuilt |
| T-04-25 | Tampering | par alert as authoritative gate | accept | Passive advisory badge only; no automated action taken on par shortage (discretion) |
</threat_model>

<verification>
- `cd apps/web && npm run type-check && npm run lint` clean
- `cd apps/api && python -m pytest tests/ -q` green
- Human verification of the programs depth golden path (checkpoint)
- DND cron confirmed reading per-property policy
</verification>

<success_criteria>
- Deep-clean + public-area schedules configurable from the web (HK-01)
- DND welfare timing + documented escalation policy configurable + duplicate-prevented (HK-06)
- Par alerts passive (HK-05); stayover rules configurable (HK-04)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-07-SUMMARY.md`
</output>
