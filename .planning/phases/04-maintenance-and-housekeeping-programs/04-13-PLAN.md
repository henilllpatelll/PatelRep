---
phase: 04-maintenance-and-housekeeping-programs
plan: 13
type: execute
wave: 5
depends_on: ["04-12"]
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
  - apps/web/components/engineering/WorkOrderDetailDrawer.tsx
autonomous: true
requirements: [BL-01, D-03]

must_haves:
  truths:
    - "The work-order detail drawer renders every user-facing string via t() in EN and ES"
  artifacts:
    - path: "apps/web/components/engineering/WorkOrderDetailDrawer.tsx"
      provides: "Bilingual work-order detail drawer"
      contains: "useTranslation"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Spanish keys for the work-order detail surface"
      contains: "workOrderDetail"
  key_links:
    - from: "components/engineering/WorkOrderDetailDrawer.tsx"
      to: "useTranslation()/t()"
      via: "react-i18next keys under engineering.* in en.ts/es.ts"
      pattern: "useTranslation"
---

<objective>
Translate the Work-Order Detail Drawer (1106 lines, ~76 user-facing literals — the single heaviest untranslated floor surface) into EN + ES. It is the drill-down engineers open per work order and is explicitly named by D-03 ("engineering / work-orders"). Given its size, the file is translated in two passes (top/status/meta section, then activity/forms/actions section) to keep each task within budget.

Purpose: Close the largest slice of the D-03 work-orders bilingual gap.
Output: WorkOrderDetailDrawer fully bilingual with matching en.ts/es.ts keys.

Depends on 04-12 (shares en.ts/es.ts — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md

<interfaces>
i18n (react-i18next): Edit ONLY apps/web/i18n/locales/en.ts + es.ts (NOT .json — bug-448). Full EN/ES parity.
  Pattern (analog: components/engineering/WorkOrderCard.tsx, already translated — copy its useTranslation/t() wiring and its formatSLA(t) helper-with-t pattern for time strings).
  Extend the existing top-level `engineering` namespace (it already holds engineering.workOrderCard.*).
Namespace assignment: engineering.workOrderDetail.* → WorkOrderDetailDrawer.tsx
Reuse engineering.workOrderCard.* (status/priority/SLA labels), common.*, and programs.* keys where copy is identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate WorkOrderDetailDrawer.tsx — header, status, meta, detail fields</name>
  <read_first>
    - apps/web/components/engineering/WorkOrderDetailDrawer.tsx (1106 lines — read fully; this task handles the top half: header/title, status & priority labels, meta rows, detail/description fields, category/location/asset labels)
    - apps/web/components/engineering/WorkOrderCard.tsx (translated analog — status/priority/SLA wording to reuse)
    - apps/web/i18n/locales/en.ts (engineering.workOrderCard.* keys)
  </read_first>
  <action>
    Add `engineering.workOrderDetail.*` keys to en.ts AND es.ts for the header/status/meta/detail portion of the drawer (title, status & priority labels, SLA copy, category/location/asset field labels, detail/description headings, aria-label/title). Reuse engineering.workOrderCard.* labels where identical (do not duplicate). Rewire those sections to `useTranslation()` + `t()`. Leave dynamic values (WO number, room number, timestamps) raw or wrap non-copy nodes with `data-i18n-skip="true"`. Full EN/ES parity.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTranslation" apps/web/components/engineering/WorkOrderDetailDrawer.tsx` returns >= 1
    - `grep -c "workOrderDetail" apps/web/i18n/locales/en.ts` equals `... es.ts`, non-zero
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>Header/status/meta/detail sections render via t() with matching EN/ES keys.</done>
</task>

<task type="auto">
  <name>Task 2: Translate WorkOrderDetailDrawer.tsx — activity, comments, parts/labor, actions, modals</name>
  <read_first>
    - apps/web/components/engineering/WorkOrderDetailDrawer.tsx (the remaining lower half after Task 1: activity/comment feed, parts/labor/defect editors, status-transition actions, confirm/cancel modals, empty/error/validation copy)
    - apps/web/i18n/locales/en.ts (engineering.workOrderDetail.* keys from Task 1)
    - apps/web/components/engineering/PMCompletionModal.tsx (translated — reuse its parts/labor/defect/measurement labels under programs.pmCompletion.* where copy matches)
  </read_first>
  <action>
    Extend `engineering.workOrderDetail.*` in en.ts AND es.ts for the remaining copy (activity/comment labels, parts/labor/defect editor labels, status-transition action buttons, confirm/cancel modal copy, empty/error/validation/notification strings, remaining aria-label/placeholder/title). Reuse programs.pmCompletion.* parts/labor keys where identical. Rewire the remaining sections to `t()`. Full EN/ES parity. After this task, the whole file must be literal-free under the i18next markupOnly rule.
  </action>
  <verify>
    <automated>cd apps/web && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - No user-facing JSX text/placeholder/title literal remains in WorkOrderDetailDrawer.tsx (manual scan of the file confirms all copy uses t())
    - `grep -c "workOrderDetail" apps/web/i18n/locales/en.ts` equals `... es.ts`
    - `npm run type-check` clean
  </acceptance_criteria>
  <done>Entire WorkOrderDetailDrawer renders via t() with matching EN/ES keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new floor copy → CI lint (later gate) | This file must be literal-free before the gate widens (04-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-37 | Tampering (process) | untranslated work-order detail copy | mitigate | Full t() coverage now; widened gate (04-16) blocks regressions |
| T-04-38 | (Usability integrity) | EN/ES parity gaps | mitigate | Every EN key requires an ES counterpart |
</threat_model>

<verification>
- `cd apps/web && npm run type-check` clean
- WorkOrderDetailDrawer returns `useTranslation` grep count >= 1 and no residual copy literals
- en.ts / es.ts parity preserved
</verification>

<success_criteria>
- Work-order detail drawer usable in EN + ES (D-03/BL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-13-SUMMARY.md`
</output>
