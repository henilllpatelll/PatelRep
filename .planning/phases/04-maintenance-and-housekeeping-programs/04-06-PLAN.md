---
phase: 04-maintenance-and-housekeeping-programs
plan: 06
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - apps/api/routers/programs.py
  - apps/api/services/programs/contracts.py
  - apps/api/models/requests.py
  - apps/api/tests/test_programs_routes.py
  - apps/api/tests/test_operational_programs.py
autonomous: true
requirements: [HK-01, HK-02, HK-03, G11, G12]

must_haves:
  truths:
    - "A supervisor gets a rule-driven inspection sample (by experience band / room type / risk) rather than a flat aggregate"
    - "Inspection quality trends break down by item, room type, and employee — not only by overall result"
    - "Deep-clean schedules and public areas are fully creatable, listable, and completable via the API"
  artifacts:
    - path: "apps/api/services/programs/contracts.py"
      provides: "Pure inspection-sampling selection + multi-dimension quality aggregation"
      contains: "sampling"
    - path: "apps/api/routers/programs.py"
      provides: "Sampling execution + deepened inspection-quality + deep-clean/public-area completeness"
      contains: "inspection"
  key_links:
    - from: "inspection sampling execution route"
      to: "inspection_sampling_rules"
      via: "rule-driven sample selection"
      pattern: "inspection_sampling_rules"
    - from: "get_inspection_quality"
      to: "by_item / by_room_type / by_employee"
      via: "multi-dimension aggregation"
      pattern: "by_room_type"
---

<objective>
Slice 4B (API) — deepen housekeeping inspection intelligence (G11, HK-02, HK-03) and finish the deep-clean / public-area API surface (HK-01, G12). Today `inspection_sampling_rules` can be configured but nothing consumes them (no sampling execution), and `get_inspection_quality` aggregates only by `overall_result`. Deliver rule-driven sample selection and multi-dimension quality trends, plus the missing deep-clean/public-area read + completion completeness the web depth UI (04-07) will consume.

Purpose: Inspection sampling is rule-driven and quality trends are visible by item/room-type/employee (Phase 4 exit intent); deep-clean/public-area schedules replace paper calendars.
Output: Pure sampling + aggregation functions, execution + deepened routes, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-04-SUMMARY.md

<interfaces>
apps/api/routers/programs.py:
  GET /programs/inspection-quality (require_role(*MANAGER_ROLES)) -> aggregates ONLY by overall_result today (190-195)
    query: inspections.select("overall_result, inspected_by, room_id, completed_at, rooms(room_type_id)")
  POST /programs/inspection-sampling-rules -> upsert rule (exists)
  Deep-clean: POST /programs/deep-clean-schedules (create), POST /.../{id}/complete (exists);
    MISSING: GET list of schedules/occurrences, GET public-areas list
  build_supply_alerts (contracts.py) -> passive shortage list (keep; discretion allows passive)

DB facts (071):
  inspection_sampling_rules(room_type_id, experience_band IN new_hire|standard|trusted,
    risk_level IN standard|high, sample_percent 1-100, UNIQUE(tenant,room_type_id,experience_band,risk_level))
  deep_clean_schedules(target_type room|public_area, room_id|public_area_id, interval_days, next_due_on, is_active)
  deep_clean_occurrences(schedule_id, completed_by, completed_at, checklist_results, notes) — immutable
  public_areas(name, location_detail, is_active, UNIQUE(tenant,name))
  inspections(overall_result, inspected_by, room_id, completed_at) + rooms(room_type_id) + housekeeper_profiles (per CLAUDE.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure inspection-sampling selection + multi-dimension quality aggregation</name>
  <read_first>
    - apps/api/services/programs/contracts.py (pure-function conventions)
    - apps/api/tests/test_operational_programs.py (pure-function test style)
    - supabase/migrations/071_operational_programs.sql (inspection_sampling_rules, lines 111-120)
  </read_first>
  <behavior>
    - select_inspection_sample(rooms, rules, assignments): given rooms with room_type_id + assigned housekeeper experience band + risk level, returns the subset to inspect honoring the matching rule's sample_percent (ceil), deterministic ordering; falls back to a default percent when no rule matches
    - aggregate_inspection_quality(inspections): returns { by_result, by_item, by_room_type, by_employee, sample_size } where each dimension is a list of {key, count, pass_rate/fail_rate}
  </behavior>
  <action>
    Add two pure functions to `contracts.py`:
    (a) `select_inspection_sample(*, rooms, rules, default_percent=10)` — match each room to the most specific rule (room_type + experience_band + risk_level), compute how many to sample via `math.ceil(count * pct/100)`, return the selected room ids deterministically (e.g., sorted). Keep it pure (no DB).
    (b) `aggregate_inspection_quality(inspections)` — aggregate by overall_result AND by checklist item (from inspection item results if present), by room_type, and by employee (inspected_by / cleaned_by), producing counts + pass/fail rates. Pure; accept the already-joined inspection dicts.
    Write pure-function tests in `test_operational_programs.py` for both.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_operational_programs.py -k "sampling or quality" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def select_inspection_sample\|def aggregate_inspection_quality" apps/api/services/programs/contracts.py` present
    - Tests cover rule-match, default fallback, ceil rounding, and all four quality dimensions
  </acceptance_criteria>
  <done>Pure sampling + multi-dimension aggregation exist and are tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sampling execution + deepen inspection-quality + deep-clean/public-area reads</name>
  <read_first>
    - apps/api/routers/programs.py (get_inspection_quality:187-195; deep-clean routes:122-157; overview:45-62)
    - apps/api/services/programs/contracts.py (functions from Task 1)
  </read_first>
  <action>
    (a) Add `GET /programs/inspection-sample` (require_role(gm, housekeeping_supervisor, chief_engineer)): loads active rooms + their assigned housekeeper experience band (from housekeeper_profiles / room_assignments) + risk + the tenant's sampling rules, calls `select_inspection_sample`, returns the rooms to inspect today. Tenant-scoped.
    (b) Replace `get_inspection_quality`'s body to call `aggregate_inspection_quality` and return by_result + by_item + by_room_type + by_employee + sample_size (keep the route + role gate).
    (c) Add the missing reads for the depth UI (04-07): `GET /programs/deep-clean-schedules` (list active + next_due_on + target join), `GET /programs/public-areas` (list). Add `public_areas.is_active` handling. All `.eq("tenant_id")` scoped, None-safe (bug-449 guard). Include these lists in the existing `/programs/overview` response if cleaner, per discretion.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py -k "inspection or sample or quality or deep_clean or public_area" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inspection-sample\|aggregate_inspection_quality" apps/api/routers/programs.py` present
    - inspection-quality returns by_room_type + by_employee + by_item (grep response keys)
    - deep-clean-schedules + public-areas list routes exist, tenant-scoped
  </acceptance_criteria>
  <done>Sampling executes rule-driven; quality trends are multi-dimension; deep-clean/public-area reads complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route tests — sampling execution, quality dimensions, deep-clean reads, RBAC</name>
  <read_first>
    - apps/api/tests/test_programs_routes.py (harness)
  </read_first>
  <behavior>
    - test_inspection_sample_rule_driven: rooms + a 50% rule -> sample size honors ceil; RBAC housekeeper 403
    - test_inspection_quality_dimensions: response includes by_item, by_room_type, by_employee, sample_size
    - test_deep_clean_and_public_area_reads: lists return tenant-scoped rows only; cross-tenant 0 rows
  </behavior>
  <action>
    Add the three route tests using the fake-supabase harness (04-01). Assert tenant scoping (no cross-tenant leakage), RBAC, and the response shapes.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py tests/test_operational_programs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - New tests assert sampling size, quality dimensions, and tenant-scoped reads
    - Full targeted suite green
  </acceptance_criteria>
  <done>Sampling + quality + deep-clean reads proven by automated route tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| floor user -> inspection/quality reads | Only management may read quality trends + samples |
| tenant A -> tenant B inspection/deep-clean data | Cross-tenant read attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | inspection-quality / sample reads | mitigate | require_role management set; RBAC test asserts housekeeper 403; ASVS V4 |
| T-04-20 | Info Disclosure / Tampering | cross-tenant reads | mitigate | Every query `.eq("tenant_id")`; route tests assert zero cross-tenant rows |
| T-04-21 | Denial of Service | maybe_single None on new reads | mitigate | bug-449 None-safe guard on all new reads |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/ -q` green
- Sampling rule-driven; quality multi-dimension; deep-clean/public-area reads tenant-scoped
</verification>

<success_criteria>
- Inspection sampling consumes rules (HK-02); quality trends by item/room-type/employee (HK-03)
- Deep-clean + public-area API complete for the depth UI
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-06-SUMMARY.md`
</output>
