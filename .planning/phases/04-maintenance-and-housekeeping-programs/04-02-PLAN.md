---
phase: 04-maintenance-and-housekeeping-programs
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/081_pm_evidence_linkage.sql
  - apps/api/models/requests.py
  - apps/api/routers/evidence.py
  - apps/api/services/programs/execution.py
  - apps/api/routers/assets.py
  - apps/api/tests/test_programs_routes.py
autonomous: true
requirements: [PM-03, PM-06, D-06, G1]

must_haves:
  truths:
    - "PM completion photos and vendor certificates are stored in the private evidence-files bucket, never as raw JSONB URLs"
    - "A PM completion read returns short-lived signed URLs for attachments, never a public storage URL or storage path"
    - "An evidence reference from another tenant cannot be attached to a PM completion"
    - "evidence_records can be linked to a pm_completion via related_entity_type='pm_completion'"
  artifacts:
    - path: "supabase/migrations/081_pm_evidence_linkage.sql"
      provides: "Adds 'pm_completion' to evidence_records related-entity constraint + 076 trigger"
      contains: "pm_completion"
    - path: "apps/api/services/programs/execution.py"
      provides: "persist_pm_completion stores evidence_record IDs + backfills linkage; no raw URLs"
      contains: "evidence"
  key_links:
    - from: "persist_pm_completion"
      to: "evidence_records"
      via: "tenant-ownership validation of submitted evidence IDs"
      pattern: "evidence_records"
    - from: "PM completion read path"
      to: "_create_evidence_signed_url"
      via: "signed URL resolution"
      pattern: "signed"
---

<objective>
Slice 0, HIGH-severity production-security fix (G1, D-06). Today `CompletePMProgramRequest.photos` / `certificate_attachments` are `List[str]` written straight into JSONB — no private bucket, no signed URL, no `evidence_records` linkage, no content-type/size validation. Route PM completion photos and vendor certificates through the existing Phase 2 evidence platform: attachments are uploaded as `evidence_records` into the private `evidence-files` bucket, the completion payload carries evidence-record IDs (not raw strings), `persist_pm_completion` validates tenant ownership and links them to the completion, and the completion read path returns short-lived signed URLs only.

Purpose: Close a D-06 violation on a live production surface. Attachments must be tenant-isolated and delivered only by signed URL.
Output: Migration 081 (applied), evidence-linked PM completion write/read paths, and route tests proving signed-URL-only delivery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-maintenance-and-housekeeping-programs/04-CONTEXT.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-RESEARCH.md
@.planning/phases/04-maintenance-and-housekeeping-programs/04-01-SUMMARY.md

<interfaces>
<!-- Evidence platform reuse contract (D-06). Do NOT build a new bucket or URL scheme. -->

apps/api/routers/evidence.py:
  RELATED_ENTITY_TABLES = { "staff": (...), "task": (...), "asset": ("assets","id"), "room": (...),
                            "inspection": (...), "incident": (...), "sop": (...) }   # ADD "pm_completion"
  POST /evidence/records            -> create evidence_record (client uploads BEFORE completion)
  POST /evidence/records/{id}/file  -> upload_evidence_file (private bucket, content-type allowlist, 10MB cap)
  GET  /evidence/records/{id}/file-url -> returns { url (signed, 3600s), expires_in_seconds }
  def _create_evidence_signed_url(storage_path) -> str        # reuse for read path
  def _record_audit_event(*, current_user, resource_type, resource_id, action, old_state=None,
                          new_state=None, reason_code=None, reason_note=None) -> None

apps/api/models/requests.py:
  class EvidenceRecordRequest: related_entity_type: Optional[Literal["staff","task","asset","room",
      "inspection","incident","sop"]]   # line 517 — ADD "pm_completion"
  class CompletePMProgramRequest: photos: List[str], certificate_attachments: List[str]  # now = evidence_record IDs

DB facts (verified this planning session):
  evidence_records.related_entity_type CHECK (069:58, PG auto-name evidence_records_related_entity_type_check) IN ('staff','task','asset','room','inspection','incident','sop')
  migration 076 function enforce_evidence_record_tenant_links() validates each type against its table (076:36-55), fired by trigger evidence_records_require_same_tenant_links (076:63)
  evidence_records is MUTABLE (no immutability trigger) -> related_entity_id backfill via UPDATE is allowed
  pm_completion_records is IMMUTABLE -> store evidence IDs at INSERT time, never UPDATE

apps/api/services/programs/execution.py (current — persist_pm_completion):
  completion_payload["photos"] = payload.get("photos") or []          # raw strings TODAY -> must be evidence IDs
  completion_payload["certificate_attachments"] = payload.get("certificate_attachments") or []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Migration 081 — add pm_completion to evidence linkage, apply via Supabase MCP</name>
  <read_first>
    - supabase/migrations/069_evidence_foundation.sql (evidence_records table + related_entity_type CHECK at line 58)
    - supabase/migrations/076_evidence_record_controls.sql (enforce_evidence_record_tenant_links() function at 076:14 + evidence_records_require_same_tenant_links trigger at 076:63; per-type branches at 076:36-55)
    - .planning/STATE.md (migration-application convention: applied to prod via Supabase MCP apply_migration)
  </read_first>
  <action>
    Create `supabase/migrations/081_pm_evidence_linkage.sql`:
    (a) Drop + recreate the `evidence_records.related_entity_type` CHECK constraint (`evidence_records_related_entity_type_check`, 069:58) to add `'pm_completion'` to the allowed set (keep all existing values).
    (b) Update the `enforce_evidence_record_tenant_links()` trigger function (076:14) to add a branch: `IF NEW.related_entity_type = 'pm_completion' AND NOT EXISTS (SELECT 1 FROM public.pm_completion_records WHERE id = NEW.related_entity_id AND tenant_id = NEW.tenant_id) THEN RAISE ...`. Preserve the exact structure/style of the existing branches (076:36-55). Use `CREATE OR REPLACE FUNCTION` with `SET search_path = public` matching the original.
    (c) No new SECURITY DEFINER RPC is introduced (D-09 N/A); if the recreated function is SECURITY DEFINER, keep the existing REVOKE/GRANT discipline from 076/079.
    Then APPLY the migration to production via the Supabase MCP `apply_migration` tool (project convention — TypeScript/build pass without applying because types come from config, not the live DB; false-positive risk if skipped). Verify with a `SELECT` that the constraint now includes pm_completion.
  </action>
  <verify>
    <automated>MISSING — Supabase MCP apply_migration then verify constraint via list_tables/SELECT; record applied=true in SUMMARY. Local check: `grep -c pm_completion supabase/migrations/081_pm_evidence_linkage.sql` >= 2</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/081_pm_evidence_linkage.sql` exists and references pm_completion + pm_completion_records
    - Supabase MCP confirms the migration applied and the CHECK constraint includes 'pm_completion'
    - SUMMARY records applied=true + advisor check shows no new RLS/grant holes
  </acceptance_criteria>
  <done>Migration written AND applied to prod DB; evidence_records accepts pm_completion linkage.</done>
</task>

<task type="auto">
  <name>Task 2: Route PM completion attachments through the evidence platform (write path)</name>
  <read_first>
    - apps/api/services/programs/execution.py (persist_pm_completion)
    - apps/api/routers/evidence.py (RELATED_ENTITY_TABLES:48, _record_audit_event:64, upload/signed-url helpers:132-142,512-544)
    - apps/api/models/requests.py (lines 517, 644-657)
    - apps/api/routers/assets.py (the /pm-schedules/{id}/complete handler)
  </read_first>
  <action>
    (a) In `apps/api/routers/evidence.py`: add `"pm_completion": ("pm_completion_records", "id")` to `RELATED_ENTITY_TABLES`. In `apps/api/models/requests.py:517`, add `"pm_completion"` to the `related_entity_type` Literal.
    (b) Reinterpret `CompletePMProgramRequest.photos` and `certificate_attachments` as lists of `evidence_records.id` (UUID strings). Keep them `List[str]` but document the contract in the model docstring. Add the same for per-item `PMChecklistResultItem.evidence` (already `List[str]` — now evidence IDs).
    (c) In `persist_pm_completion` (execution.py): BEFORE inserting the completion, validate that every submitted evidence ID (photos + certificate_attachments + item.evidence) exists in `evidence_records` scoped to this `tenant_id` (`.eq("tenant_id", tenant_id)`); raise a `ValueError` (mapped to 422 by the router) if any is missing or belongs to another tenant. Store the validated ID lists (not raw strings) in the immutable completion/item rows.
    (d) AFTER inserting the completion, backfill each referenced evidence_record with `related_entity_type='pm_completion'`, `related_entity_id=completion["id"]` via an UPDATE scoped to tenant (evidence_records is mutable — allowed). This creates the defensible traceability link.
    The client-side upload flow (create evidence_record linked to the asset, POST file, submit the returned IDs) is built in 04-05; this task establishes the server contract.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_operational_programs.py tests/test_programs_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pm_completion" apps/api/routers/evidence.py` shows the RELATED_ENTITY_TABLES entry
    - `grep -n "pm_completion" apps/api/models/requests.py` shows it in the Literal
    - persist_pm_completion validates evidence IDs against evidence_records scoped by tenant (grep: `evidence_records` + `.eq("tenant_id"`)
    - No raw-URL passthrough: completion no longer stores arbitrary strings without evidence-table validation
  </acceptance_criteria>
  <done>PM completion accepts only tenant-owned evidence_record IDs and links them to the completion; no raw JSONB URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Signed-URL read path + evidence route tests</name>
  <read_first>
    - apps/api/routers/assets.py (PM completion read/list endpoints)
    - apps/api/routers/evidence.py (_create_evidence_signed_url:136)
    - apps/api/tests/test_programs_routes.py (harness from 04-01)
  </read_first>
  <behavior>
    - test_pm_completion_returns_signed_urls: a completion with attachments read back exposes signed URLs (or resolvable evidence IDs), never a raw public storage URL or storage_path
    - test_cross_tenant_evidence_rejected: submitting an evidence_record ID owned by tenant B on a tenant-A completion -> 422/404, zero completion rows written
    - test_evidence_required_item_422_route: an item with requires_evidence and no evidence at the route level -> 422 (mirrors the pure-logic test at HTTP layer)
  </behavior>
  <action>
    Add a read helper: when returning a PM completion (the `assets.py` completion read/list, or a new `GET /assets/pm-schedules/{id}/completions/{cid}`), resolve each stored evidence ID to a short-lived signed URL via `_create_evidence_signed_url` (import/reuse; do not duplicate). Never return `storage_path` or a public URL. Then add the three route tests above to `test_programs_routes.py` using the 04-01 harness. Assert the signed-URL shape and the cross-tenant rejection with zero writes.
  </action>
  <verify>
    <automated>cd apps/api && python -m pytest tests/test_programs_routes.py -k "signed or evidence or cross_tenant" -q</automated>
  </verify>
  <acceptance_criteria>
    - Read path returns signed URLs; grep confirms no `storage_path` leaked in the completion response serializer
    - `grep -c "def test_" apps/api/tests/test_programs_routes.py` increased by >= 3 vs 04-01
    - Cross-tenant evidence test asserts zero completion rows written
  </acceptance_criteria>
  <done>Attachments delivered by signed URL only; cross-tenant evidence rejected; route tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PM completion payload | Untrusted attachment references cross here |
| API -> Supabase Storage | Private bucket; only signed URLs may leave |
| tenant A completion -> tenant B evidence_record | Cross-tenant reference attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | PM attachment delivery | mitigate | Private `evidence-files` bucket + short-lived signed URLs only; read path never returns storage_path/public URL (Task 3); ASVS V8 |
| T-04-07 | Tampering / Info Disclosure | cross-tenant evidence reference | mitigate | Task 2 validates every evidence ID against evidence_records scoped by tenant; Task 3 test asserts 422 + zero writes |
| T-04-08 | Spoofing | unvalidated file content type/size | mitigate | Reuse evidence.py upload allowlist + 10MB cap (client path, 04-05); ASVS V12 |
| T-04-09 | Repudiation | attachment provenance | mitigate | Task 2 backfills related_entity linkage -> completion traceable to evidence_records |
</threat_model>

<verification>
- `cd apps/api && python -m pytest tests/ -q` green
- Migration 081 applied to prod (Supabase MCP) and constraint verified
- No raw-URL passthrough remains in the PM completion write/read paths
</verification>

<success_criteria>
- PM photos/certs stored via the evidence platform, delivered by signed URL only (D-06)
- Cross-tenant evidence references rejected with zero writes
- Migration 081 applied to production
</success_criteria>

<output>
After completion, create `.planning/phases/04-maintenance-and-housekeeping-programs/04-02-SUMMARY.md`
</output>
