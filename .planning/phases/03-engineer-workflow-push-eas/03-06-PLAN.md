---
phase: 03-engineer-workflow-push-eas
plan: "06"
type: execute
wave: 4
depends_on: ["03-05"]
files_modified: []
autonomous: false
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05, ENG-06, ENG-07, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "Engineer can see, claim, and complete work orders on a physical device"
    - "Push notification delivered on physical device when WO is assigned"
    - "Tapping push notification from killed-app state opens the correct WO screen"
    - "Offline WO claim and complete sync after airplane mode test"
    - "EAS Android APK installs and runs on physical device"
  artifacts:
    - path: "apps/mobile/__tests__/lib/offline/sync.test.ts"
      provides: "All sync cases GREEN"
    - path: "apps/mobile/__tests__/screens/WorkOrdersList.test.tsx"
      provides: "WO list tests GREEN"
    - path: "apps/mobile/__tests__/lib/notifications.test.ts"
      provides: "Push token API contract GREEN"
  key_links:
    - from: "push notification tap (killed state)"
      to: "/(app)/work-orders/<woId>"
      via: "getLastNotificationResponseAsync in _layout.tsx"
      pattern: "getLastNotificationResponseAsync"
---

<objective>
Verify the complete Phase 3 engineer workflow end-to-end on a physical device, confirm push delivery and deep linking, and sign off on EAS build distribution.

Purpose: Automated tests verify contracts in isolation. This checkpoint confirms the full system works together — push token registration via API, Expo Push delivery, deep link navigation on notification tap, offline sync, and sideloaded APK installation — behaviors that cannot be unit tested.
Output: Human sign-off confirming all Phase 3 success criteria pass on device.
</objective>

<execution_context>
@C:/Users/Henil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Henil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-engineer-workflow-push-eas/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite — confirm all Phase 3 tests GREEN</name>
  <files></files>
  <action>
    Run the complete mobile and API test suites and confirm all Phase 3 tests pass before human verification begins.

    Mobile tests:
    ```
    cd apps/mobile && npm test -- --passWithNoTests
    ```

    API smoke tests:
    ```
    cd apps/api && python -m pytest tests/smoke/test_wo_push.py -v
    ```

    If any test is RED, stop and fix before proceeding to the human verification checkpoint. Do not skip the automated gate.
  </action>
  <verify>
    <automated>cd C:/Users/Henil/projects/PatelRep/apps/mobile && npm test -- --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm test exits with code 0. All sync, notifications, WorkOrdersList, WorkOrderDetail, OfflineBanner, ReportIssueModal, and API client tests pass. test_wo_push.py passes. No RED tests.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Physical device verification — full engineer workflow + push + EAS</name>
  <what-built>
    - Engineer work order list with title/priority/status/room number on cards (ENG-01, ENG-02)
    - Offline claim and complete with sync on reconnect (ENG-03, ENG-04, ENG-05, ENG-07)
    - Push notifications via Expo Push API with deep link navigation (ENG-06, INFRA-02)
    - EAS Android APK for sideload distribution (INFRA-03)
  </what-built>
  <how-to-verify>
    SC-1 — Engineer work order list:
    - Log in as a user with role "engineer"
    - Navigate to Work Orders tab
    - Confirm: you see work orders assigned to you on the "In Progress" tab AND open/unassigned orders on the "Open" tab
    - Confirm: each card shows title, priority badge, status chip, and room number (if applicable)

    SC-2 — Claim and complete:
    - On the Open tab, find an unassigned work order and tap "Claim" — confirm status changes to "In Progress" and card moves to the In Progress tab
    - On an In Progress WO, enter a completion note and tap "Mark Complete" — confirm WO shows as completed and web dashboard reflects "completed" status with the note

    SC-3 — Offline sync:
    - Put device in airplane mode
    - Tap "Claim" on an open work order — confirm optimistic update (card shows "in_progress")
    - Restore connectivity
    - Confirm within 10 seconds the work order appears as "in_progress" on the web dashboard

    SC-4 — Push notification delivery (requires APK or Expo Go on physical device):
    - From the web dashboard (logged in as GM or supervisor), create a new work order and assign it to the engineer's account
    - Confirm the engineer's physical device receives a push notification with title "Work Order Assigned"
    - Confirm the notification body shows the WO title

    SC-5 — Push notification deep link (killed-app state):
    - Force-quit the app on the engineer's device (swipe away from app switcher)
    - Tap the push notification from the notification tray
    - Confirm the app opens directly to the correct work order detail screen (not the home tab)

    SC-6 — EAS Android APK (if build completed in plan 03-05):
    - Download the APK from the EAS dashboard (expo.dev -> your project -> Builds)
    - Transfer to a fresh Android device or emulator
    - Sideload: `adb install patelrep.apk` or enable "Install from unknown sources" and open the file
    - Confirm the app launches, login works, and the engineer workflow is functional
  </how-to-verify>
  <resume-signal>
    Type "SC-1 SC-2 SC-3 SC-4 SC-5 SC-6" for all criteria passing, or list which passed (e.g., "SC-1 SC-2 SC-3 passed, SC-4 failed — describe issue"). If iOS TestFlight was not set up, SC-6 with APK alone is acceptable for phase completion.
  </resume-signal>
</task>

</tasks>

<verification>
Full automated suite:
`cd C:/Users/Henil/projects/PatelRep/apps/mobile && npm test -- --passWithNoTests 2>&1 | tail -20`
`cd C:/Users/Henil/projects/PatelRep/apps/api && python -m pytest tests/smoke/test_wo_push.py -v 2>&1 | tail -10`
</verification>

<success_criteria>
All automated tests pass GREEN. Human confirms SC-1 through SC-5 pass on physical device. SC-6 (EAS APK sideload) confirmed installable. Phase 3 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-engineer-workflow-push-eas/03-06-SUMMARY.md`
</output>
