---
phase: 39-ai-shift-handover-and-gm-morning-brief
plan: 04
type: execute
wave: 3
depends_on: ["39-03"]
files_modified:
  - apps/web/app/(dashboard)/logbook/page.tsx
  - apps/web/lib/hooks/useArrivalReadiness.ts
  - apps/web/components/dashboard/OvernightRecapStrip.tsx
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

must_haves:
  truths:
    - "On the Logbook page, wherever the AI shift summary text is shown, an Acknowledge button appears when the summary is unacknowledged; after clicking (or if already acknowledged by anyone) it is replaced by a read-only 'Acknowledged by {name} · {relative time}' line"
    - "On the GM dashboard, OvernightRecapStrip shows an inline Acknowledge action when the overnight summary is unacknowledged and a compact '✓ Acknowledged' indicator when it is — without breaking its single-line h-[52px] truncated layout"
    - "Acknowledgment state is read via logbookApi.getShiftSummary and written via logbookApi.acknowledgeShiftSummary, with the relevant React Query cache invalidated so the UI updates without a manual reload"
    - "Every new user-facing string exists in BOTH en.ts and es.ts (no English-only string)"
    - "When there is no stored shift_summary to acknowledge (e.g. no shift_id on the overnight entry, or a 404 from getShiftSummary), the acknowledge affordance is simply hidden — no crash, no error toast"
  artifacts:
    - path: "apps/web/lib/hooks/useArrivalReadiness.ts"
      provides: "OvernightSummary type extended with id/acknowledgedAt/acknowledgedByName + a dependent query hydrating them"
    - path: "apps/web/components/dashboard/OvernightRecapStrip.tsx"
      provides: "compact acknowledge affordance inside the existing strip"
    - path: "apps/web/app/(dashboard)/logbook/page.tsx"
      provides: "acknowledge button / acknowledged line on the AI summary panel"
  key_links:
    - from: "apps/web/components/dashboard/OvernightRecapStrip.tsx"
      to: "logbookApi.acknowledgeShiftSummary"
      via: "useMutation on click, invalidating the overnight/shift-summary query"
      pattern: "acknowledge"
    - from: "apps/web/lib/hooks/useArrivalReadiness.ts"
      to: "logbookApi.getShiftSummary(entry.shift_id)"
      via: "dependent useQuery keyed on the overnight AI entry's shift_id"
      pattern: "getShiftSummary"
---

<objective>
Surface acknowledgment in the two existing UI surfaces named in CONTEXT — the Logbook AI-summary panel and the GM dashboard's OvernightRecapStrip — reusing the Plan 03 client methods. No new page, no new dashboard widget, no layout redesign: just an Acknowledge affordance and an acknowledged-state readout, plus bilingual strings.

Purpose: closes the loop so the "next shift confirms they read this" action is actually reachable by staff, in the places they already look.
Output: an Acknowledge button + acknowledged line on the Logbook summary, a compact acknowledge affordance in OvernightRecapStrip, the hook wiring to feed it, and en/es strings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-CONTEXT.md
@apps/web/app/(dashboard)/logbook/page.tsx
@apps/web/lib/hooks/useArrivalReadiness.ts
@apps/web/components/dashboard/OvernightRecapStrip.tsx
@apps/web/lib/api/logbook.ts

**IMPORTANT — read `apps/web/AGENTS.md` before editing web files:** this project runs a modified Next.js; the auto-injected AGENTS block in `apps/web/CLAUDE.md` must be left intact / committed with the work, not deleted from the diff.

**Locked decisions from 39-CONTEXT.md:**
- Logbook: the AI summary is rendered inside `AISummaryPanel` in `page.tsx`. Add an Acknowledge button when unacknowledged; once acknowledged, replace it with a read-only "Acknowledged by {name} · {relative time}" line. `formatDistanceToNow` from `date-fns` is ALREADY imported in this file — reuse it for relative time (do not add a new date util).
- OvernightRecapStrip: add a SMALL affordance next to the existing "Read full recap" link — a text-link "Acknowledge" when unacknowledged, or a compact "✓ Acknowledged" badge when acked by anyone. Must NOT break the single-line, `h-[52px]`-constrained truncated layout (the `<p>` uses `flex-1 min-w-0 truncate`; keep the new element `shrink-0`, like the existing link). No new row/section.
- Extend `OvernightSummary` (`useArrivalReadiness.ts`) from `{ text, href }` to also carry `id`, `acknowledgedAt`, and `acknowledgedByName` (names Claude's discretion; keep camelCase to match the type).
- Do NOT create a new route/page, do NOT add a dashboard widget beyond this strip, do NOT restructure `SimplifiedDashboard.tsx` beyond any minimal prop-threading needed to pass richer summary data / an acknowledge handler through to the strip.
- i18n: add all new strings to BOTH `en.ts` and `es.ts` under the existing `dashboard.gm.*` and `logbook.*` namespaces (grep `dashboard.gm.overnightTitle` / `dashboard.gm.readFullRecap` to find the exact object location; add siblings like `acknowledge`, `acknowledged`).

**Wiring the two surfaces to a shift_summaries row (key challenge — read carefully):**
- The Logbook `AISummaryPanel` generates on demand via `logbookApi.generateShiftSummary({ shift_id: 'today', ... })`, which stores a `shift_summaries` row keyed by `shift_id = 'today'`. To get that row's `id` + acknowledgment state, call `logbookApi.getShiftSummary('today')` (React Query) once a summary exists for the day, and drive the Acknowledge button from it. On 404 (no stored summary yet — e.g. before first generation), hide the button.
- The dashboard `overnightSummary` (`useArrivalReadiness.ts`) currently derives from the earliest `is_ai_generated` logbook entry of the day. A `LogbookEntry` carries an optional `shift_id`. Add a dependent `useQuery` (enabled only when that entry has a `shift_id`) calling `logbookApi.getShiftSummary(entry.shift_id)` to hydrate `id`/`acknowledgedAt`/`acknowledgedByName` onto `overnightSummary`. If the entry has no `shift_id` or the query 404s, leave those fields undefined and render no acknowledge affordance (graceful degrade — the recap text still shows). Do not fabricate a linkage that isn't there.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acknowledge affordance on the Logbook AI summary panel</name>
  <files>apps/web/app/(dashboard)/logbook/page.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
In `AISummaryPanel`: after a summary exists (either freshly generated or present), fetch `logbookApi.getShiftSummary('today')` via `useQuery` (enabled when the panel is open and a summary is present; `retry: false` so a 404 resolves fast to "no ack row"). From its data read `{ id, acknowledged_at, acknowledged_by_name }`.
- When `acknowledged_at` is null: render an Acknowledge `Button` (reuse the existing `Button` component + `Sparkles`/a check icon) wired to a `useMutation` calling `logbookApi.acknowledgeShiftSummary(id)`, and on success invalidate the `getShiftSummary('today')` query (and refetch) so the button flips to the acknowledged line.
- When `acknowledged_at` is set: render a read-only line "Acknowledged by {acknowledged_by_name ?? fallback} · {formatDistanceToNow(new Date(acknowledged_at), { addSuffix: true })}" (reuse the already-imported `formatDistanceToNow`).
Place this inside the open panel, near the summary text / Regenerate button. Use `t('logbook.acknowledge')` and `t('logbook.acknowledgedBy', { name, time })` — add both keys to `en.ts` and `es.ts` under the `logbook` namespace. Guard: if `getShiftSummary` 404s or has no `id`, render nothing (no button).
  </action>
  <verify>`cd apps/web && npx tsc --noEmit` clean for this file. Reading the component: unacknowledged → button present; acknowledged → read-only line; 404 → neither. New `logbook.acknowledge` / `logbook.acknowledgedBy` keys exist in both locale files with matching interpolation placeholders.</verify>
  <done>The Logbook AI summary shows an Acknowledge button that, once clicked, becomes an "Acknowledged by … · … ago" line, bilingual, with graceful no-op when no stored summary exists.</done>
</task>

<task type="auto">
  <name>Task 2: Thread acknowledgment through useArrivalReadiness's overnight summary</name>
  <files>apps/web/lib/hooks/useArrivalReadiness.ts</files>
  <action>
Extend the `OvernightSummary` interface to `{ text: string; href: string; id?: string; acknowledgedAt?: string | null; acknowledgedByName?: string | null }`.
In the `overnightSummary` derivation, keep the existing "earliest is_ai_generated entry" logic, but also capture that entry's `shift_id`. Add a dependent `useQuery` (queryKey e.g. `['overnight-shift-summary', entry?.shift_id]`, `enabled: !!entry?.shift_id`, `retry: false`) that calls `logbookApi.getShiftSummary(entry.shift_id)`. Merge its `{ id, acknowledged_at, acknowledged_by_name }` into the returned `overnightSummary` object as `id`/`acknowledgedAt`/`acknowledgedByName`. If the entry has no `shift_id` or the query errors/404s, leave those fields undefined. Because `overnightSummary` is currently a `useMemo`, restructure minimally so the acknowledgment fields (which come from a query, not memo inputs) are folded in — e.g. compute the base memo, then spread the query data over it. Do not change any other part of the hook's return.
  </action>
  <verify>`cd apps/web && npx tsc --noEmit` clean. Reading the hook: `OvernightSummary` has the three new optional fields; a dependent query keyed on the overnight entry's `shift_id` hydrates them; nothing else in the hook's return shape changed.</verify>
  <done>overnightSummary now carries the shift_summary id + acknowledgment state (when derivable), without disturbing the rest of useArrivalReadiness.</done>
</task>

<task type="auto">
  <name>Task 3: Compact acknowledge affordance in OvernightRecapStrip</name>
  <files>apps/web/components/dashboard/OvernightRecapStrip.tsx, apps/web/i18n/locales/en.ts, apps/web/i18n/locales/es.ts</files>
  <action>
In `OvernightRecapStrip`, after the "Read full recap" `Link` (keep it), add a `shrink-0` element driven by `summary`:
- If `summary.acknowledgedAt` is set: a compact badge "✓ {t('dashboard.gm.acknowledged')}" (small, muted, e.g. `text-[11px] text-ink3`), optionally titled with `summary.acknowledgedByName`. No mutation.
- Else if `summary.id` is present: a small text-button "Acknowledge" (`text-[12px] font-medium text-brand`, styled like the existing link, `shrink-0`) wired to a `useMutation` calling `logbookApi.acknowledgeShiftSummary(summary.id)`; on success invalidate the GM overnight query (`queryKey: ['gm-overnight-summary']`) and the `['overnight-shift-summary']` query so the strip flips to the acked badge. Use `useQueryClient` + import `logbookApi`.
- Else (no id): render nothing extra (unchanged strip).
Keep the parent `Card` at `h-[52px]`-compatible single-line layout: the new element must be `shrink-0` and must not wrap. Add `dashboard.gm.acknowledge` and `dashboard.gm.acknowledged` to BOTH `en.ts` and `es.ts` beside `overnightTitle`/`readFullRecap`. If `SimplifiedDashboard.tsx` needs a trivial prop pass-through for this to receive the summary, make only that minimal change (the strip already receives `summary`; prefer keeping the mutation self-contained inside the strip so no dashboard change is needed).
  </action>
  <verify>`cd apps/web && npx tsc --noEmit` clean. Visually reason about the layout: with a long summary.text (truncated) + link + new affordance, the row stays single-line (`truncate` on the text, `shrink-0` on link+affordance). New `dashboard.gm.acknowledge`/`acknowledged` keys in both locales.</verify>
  <done>OvernightRecapStrip shows Acknowledge (unacked) or ✓ Acknowledged (acked) inline without breaking its compact single-line layout, bilingual, self-contained.</done>
</task>

</tasks>

<verification>
Both surfaces read acknowledgment via getShiftSummary and write via acknowledgeShiftSummary with proper cache invalidation. Logbook shows button→acknowledged-line; OvernightRecapStrip shows inline Acknowledge→✓ badge without breaking its h-[52px] layout. All new strings present in en.ts AND es.ts. No stored summary ⇒ affordance hidden, no crash. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
A staff member can acknowledge the shift handoff from either the Logbook or the GM dashboard, see who acknowledged it and when, in English or Spanish, with no layout regression to the compact recap strip.
</success_criteria>

<output>
After completion, create `.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-04-SUMMARY.md`
</output>
