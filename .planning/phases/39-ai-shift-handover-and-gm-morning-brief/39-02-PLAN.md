---
phase: 39-ai-shift-handover-and-gm-morning-brief
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/services/ai/shift_summary.py
  - apps/api/tests/test_shift_summary_enrichment.py
autonomous: true

must_haves:
  truths:
    - "generate_shift_summary now pulls four additional tenant-scoped signals — VIP arrivals, pending guest issues, low-stock engineering parts, and SLA-breached WOs+tasks — into the prompt, each capped at 10 items in the rendered text"
    - "The stats JSONB gains vip_arrivals_count, pending_guest_issues_count, low_stock_parts_count, and sla_breaches_count reflecting the FULL (un-truncated) counts, alongside the four pre-existing keys which are unchanged"
    - "The prompt gains one new section per signal (same style as the existing LOGBOOK ENTRIES / TASKS COMPLETED blocks) plus a 5th instruction bullet telling the model to flag those four signals for the next shift"
    - "The anthropic model call, credits_charged (3.0), ai_interactions logging shape, cron schedule, and the four existing stats keys are all untouched"
  artifacts:
    - path: "apps/api/services/ai/shift_summary.py"
      provides: "four new tenant-scoped queries + four prompt sections + four additive stats keys inside generate_shift_summary"
    - path: "apps/api/tests/test_shift_summary_enrichment.py"
      provides: "coverage for each new signal's predicate, the 10-item cap, the full-count stats, and additive (non-destructive) stats behavior"
  key_links:
    - from: "apps/api/services/ai/shift_summary.py"
      to: "guest_requests terminal-status set"
      via: ".not_.in_(\"status\", [\"resolved\", \"verified\", \"cancelled\"])"
      pattern: "resolved.*verified.*cancelled"
    - from: "apps/api/services/ai/shift_summary.py"
      to: "SLA-breach predicate (mirrors internal.py check_escalations)"
      via: ".in_(\"status\", [\"open\", \"in_progress\"]) + .lt(\"due_at\", now)"
      pattern: "due_at"
    - from: "apps/api/services/ai/shift_summary.py"
      to: "low-stock predicate (mirrors inventory.py list_parts)"
      via: "total_on_hand (summed engineering_part_stock) < minimum_stock"
      pattern: "minimum_stock"
---

<objective>
Enrich the existing AI shift-handover summary's inputs so it can do the "flag what needs the next shift" job the OSS research doc asks for. Today `generate_shift_summary()` only sees logbook entries, completed tasks, and open work orders — it has no idea about VIP arrivals, pending guest issues, low-stock parts, or SLA-breached items. This plan adds those four tenant-scoped signals to the data pull, renders a prompt section for each, adds one instruction bullet, and records four new count keys in the `stats` JSONB.

Purpose: implements the core value of Phase 39 (research Output-3 item #2 / cross-module opportunity #4) by widening the summary's inputs — NOT by changing the model, the cron, the credit accounting, or the acknowledgment flow (that is Plan 03).
Output: `generate_shift_summary()` composes a richer prompt from four new queries and stores four new stat counts, verified by a new test file using this repo's fake-Supabase harness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-CONTEXT.md
@apps/api/services/ai/shift_summary.py
@apps/api/routers/inventory.py
@apps/api/routers/internal.py
@apps/api/tests/test_inventory.py

**Locked predicates from 39-CONTEXT.md — reuse EXACTLY, do not invent variants:**
- **VIP arrivals:** `room_status` rows for this hotel where `clean_type = 'DEP'` AND `vip_flag = true`, joined to `rooms(room_number)`. This is the same hotel-agnostic arrival proxy `useArrivalReadiness.ts` already uses client-side (`r.clean_type === 'DEP'`, `r.vip_flag`); there is no PMS-arrival feed for non-Opera hotels. `clean_type` and `vip_flag` live on the `room_status` row (see the board-row shape in `useArrivalReadiness.ts` and the `room.get("vip_flag")` usage in `housekeeping.py`). Respect the `rooms!inner(...)` join gotcha in CLAUDE.md. Collect room numbers + count.
- **Pending guest issues:** `guest_requests` where `status NOT IN ('resolved', 'verified', 'cancelled')` — the exact terminal set the guest-request→WO bridge uses (`guest_requests.py`). Supabase SDK: `.not_.in_("status", ["resolved", "verified", "cancelled"])`. Collect a short description per row + count.
- **Low-stock parts (engineering only):** replicate the predicate from `inventory.py`'s `list_parts` / `_total_on_hand_map`: select active `engineering_parts` (id, name, minimum_stock) for the tenant, sum `engineering_part_stock.quantity` per part, keep those where `total_on_hand < minimum_stock` (treat missing `minimum_stock` as 0 exactly like `float(part.get("minimum_stock") or 0)`). Do NOT `from routers.inventory import ...` — replicate the two-query pattern inline to keep the flat services layer clean (per CLAUDE.md's A1 services-depth rule). Collect part names + count.
- **SLA breaches:** mirror `internal.py`'s `check_escalations` ground truth — `work_orders` where `status IN ('open','in_progress') AND due_at < now()`, and `tasks` where `status IN ('open','in_progress') AND due_at < now()`. Use `datetime.now(timezone.utc).isoformat()` for `now` and `.lt("due_at", now)`. Do NOT use `escalation_level` as the signal. Collect titles from both; the combined count = WO breaches + task breaches.

**Discretion (from CONTEXT):** engineering parts only — do NOT add `housekeeping_supply_pars`; keep the query surface tight. Prompt wording for the new sections/bullet is yours as long as it matches the existing factual/actionable tone and mirrors the `- ...` bullet style of the existing blocks.

**Truncation style to match:** existing code renders `completed_tasks[:20]` and `open_work_orders[:10]` in the prompt text while storing `len(...)` in stats. Do the same: cap each new list at `[:10]` in the rendered section, but store the FULL `len(...)` in the new stat key.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the four enrichment queries, prompt sections, and stats keys</name>
  <files>apps/api/services/ai/shift_summary.py</files>
  <action>
Inside `generate_shift_summary()`, after the existing step 4 (open work orders) and before step 5 (build prompt context), add four new tenant-scoped queries, each guarded to return `[]` on empty, following the exact predicates in the context block above:

1. `vip_arrivals` — `room_status` where `tenant_id = hotel_id`, `clean_type = "DEP"`, `vip_flag = True`, select `rooms!inner(room_number)`. Extract room numbers.
2. `pending_guest_issues` — `guest_requests` where `tenant_id = hotel_id`, `.not_.in_("status", ["resolved", "verified", "cancelled"])`. Select a description-ish column (e.g. `description` / `summary` / `request_type` — check the guest_requests schema/columns actually selected elsewhere in `guest_requests.py`; use whatever short human-readable field exists, fall back to `request_type`).
3. `low_stock_parts` — active `engineering_parts` (id, name, minimum_stock) for the tenant + summed `engineering_part_stock.quantity` per part; filter `total < float(part.get("minimum_stock") or 0)`. Extract part names.
4. `sla_breaches` — `work_orders` (title) where `tenant_id`, `status IN ('open','in_progress')`, `due_at < now_iso`; plus `tasks` (title) where `tenant_id`, `status IN ('open','in_progress')`, `due_at < now_iso`. Combine titles; combined count = len(wo) + len(tasks). Add `from datetime import datetime, timezone` at the top and compute `now_iso = datetime.now(timezone.utc).isoformat()`.

Then extend the prompt: add four new sections after `OPEN WORK ORDERS (...)`, each in the existing `- item` bullet style with a `No ... .` fallback string, capped `[:10]`:
```
VIP ARRIVALS TODAY:
{vip_text}

PENDING GUEST ISSUES:
{guest_issues_text}

LOW-STOCK PARTS:
{low_stock_text}

SLA BREACHES (overdue work orders & tasks):
{sla_text}
```
Add a 5th numbered instruction bullet to the writing-guidance list, e.g. "5. Flag VIP arrivals, pending guest issues, low-stock items, and SLA breaches that need the next shift's attention."

Extend the `stats` dict passed to the `shift_summaries` insert with FOUR additive keys — `vip_arrivals_count`, `pending_guest_issues_count`, `low_stock_parts_count`, `sla_breaches_count` (full counts, not the truncated list length). Do NOT remove or rename `tasks_completed`, `open_work_orders`, `logbook_entries_count`, or `model_used`. Do NOT touch the `ai_interactions` insert, the `client.messages.create(...)` call, `credits_charged`, or the function's return dict shape beyond what already exists (returning the four new counts in the return dict is optional and Claude's discretion — the stats JSONB is the source of truth).
  </action>
  <verify>Read back the edited function: four new queries present with the exact predicates, four new prompt sections rendered with `[:10]` caps and fallback strings, one new instruction bullet, four new additive stats keys, and the four original stats keys + the anthropic call + ai_interactions insert unchanged. `cd apps/api && python -c "import ast; ast.parse(open('services/ai/shift_summary.py').read())"` parses clean.</verify>
  <done>generate_shift_summary pulls and renders all four new signals and stores their full counts additively, with no change to the model/cron/credit/logging surface.</done>
</task>

<task type="auto">
  <name>Task 2: Test the enrichment with the fake-Supabase harness</name>
  <files>apps/api/tests/test_shift_summary_enrichment.py</files>
  <action>
Create `test_shift_summary_enrichment.py` following the existing fake-Supabase test pattern in `test_inventory.py` (a `FakeQuery`/`FakeTable` that records `.eq/.in_/.not_/.lt` filters and returns seeded rows), and stub the anthropic client so no real API call is made (monkeypatch `anthropic.Anthropic` or `services.ai.shift_summary.anthropic.Anthropic` to return a fake message with `.content[0].text`).

Cover:
1. A seeded tenant with 2 VIP DEP rooms, 3 non-terminal guest requests, 2 low-stock parts (on-hand < minimum), and 1 overdue WO + 1 overdue task produces stats `vip_arrivals_count=2`, `pending_guest_issues_count=3`, `low_stock_parts_count=2`, `sla_breaches_count=2`.
2. Guest requests with status in `('resolved','verified','cancelled')` are excluded (seed some terminal ones and confirm they don't count).
3. A part whose on-hand >= minimum_stock is NOT counted low-stock; a part with null minimum_stock is treated as minimum 0 (not low-stock unless on-hand < 0).
4. A WO/task with `due_at` in the future or status not in (open,in_progress) is NOT counted as an SLA breach.
5. The four pre-existing stats keys still appear with correct values, proving the change is additive.
6. (Cap) if more than 10 items exist for a signal, the stored count is the full length (e.g. seed 12 pending guest issues → `pending_guest_issues_count=12`), while the rendered prompt text only lists 10 — assert on the count; asserting the prompt cap is optional/discretionary.

Capture the `stats` payload by asserting against what the fake `shift_summaries.insert(...)` received.
  </action>
  <verify>`cd apps/api && python -m pytest tests/test_shift_summary_enrichment.py -v` passes; no real network call is made (anthropic stubbed).</verify>
  <done>New test file green, exercising all four predicates, exclusion rules, the additive-stats guarantee, and the full-count-vs-cap behavior.</done>
</task>

</tasks>

<verification>
generate_shift_summary composes a prompt with four new factual sections and stores four new additive count keys, all tenant-scoped, using the exact locked predicates. The anthropic model call, credit charge, cron, ai_interactions logging, and original stats keys are unchanged. New test file passes with the anthropic client stubbed.
</verification>

<success_criteria>
The AI shift summary's inputs now include VIP arrivals, pending guest issues, low-stock engineering parts, and SLA breaches, each capped at 10 in-prompt with a full count in stats — verified by a green `test_shift_summary_enrichment.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-02-SUMMARY.md`
</output>
