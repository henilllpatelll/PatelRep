---
phase: 39-ai-shift-handover-and-gm-morning-brief
plan: 05
type: execute
wave: 4
depends_on: ["39-02", "39-03", "39-04"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The full apps/api pytest suite passes, not just the two new Phase 39 test files"
    - "A live golden path works: a staff member acknowledges a shift summary from the Logbook AND from the GM dashboard OvernightRecapStrip, and the acknowledged-by name + time render on both surfaces"
    - "The enriched shift summary provably includes the four new stats keys (vip_arrivals_count, pending_guest_issues_count, low_stock_parts_count, sla_breaches_count) — verified against the live dev DB or via the generate endpoint response, not just unit tests"
    - "Re-clicking Acknowledge (or a second staff member acknowledging) does not error and does not change the recorded acknowledger — idempotency holds over the network"
    - "Adjacent already-shipped flows still work: Logbook entry create/edit/delete, the on-demand 'Generate Summary' trigger, and the GM dashboard's arrival-readiness hero/blockers that share useArrivalReadiness"
  artifacts: []
  key_links:
    - from: "apps/web dev server (localhost:3000)"
      to: "apps/api dev server (localhost:8003)"
      via: "live browser session driven by Claude, per CLAUDE.md's mandatory Self-Verification Policy"
---

<objective>
Close out Phase 39 with the mandatory self-verification pass: run the full backend test suite, then drive the real web app in a browser to confirm the acknowledgment golden path works end-to-end on live servers (both UI surfaces), confirm the enriched summary stats are actually produced, and spot-check that nothing adjacent (logbook CRUD, on-demand generation, GM arrival-readiness dashboard) broke. Required by this repo's CLAUDE.md Self-Verification and Non-Regression policies — no Phase 39 task is "done" until this passes.

Purpose: unit tests with fake Supabase clients can pass while real wiring (route registration on `/v1/logbook`, the migration-105 columns, React Query invalidation, i18n key resolution, the getShiftSummary→acknowledge round-trip, the OvernightRecapStrip single-line layout) is broken. This plan catches that class of bug.
Output: a verified, working feature end-to-end; nothing else is created.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-CONTEXT.md
@CLAUDE.md

**Local dev gotchas (from project memory — trust these over the generic port-8000 assumption):**
- `npm run dev:api` serves on **port 8003**, not 8000 (8000 is occupied by an unrelated process on this machine). Confirm via `GET http://localhost:8003/openapi.json` returning PatelRep-shaped paths before trusting the server.
- Windows `uvicorn --reload` has left orphaned `python.exe` processes bound to the port in past sessions. Before starting, check `Get-CimInstance Win32_Process -Filter "Name = 'python.exe'"` for stale `uvicorn`/`multiprocessing` processes and kill them, or you may be testing stale code.
- New Python deps go in `apps/api/.venv` — not relevant here (Phase 39 adds no new deps), but if pytest passes yet the dev server 500s on ImportError, this is why.
- `apps/api/.env` has real Supabase creds — Supabase-backed paths (logbook, acknowledge, get_shift_summary) can be exercised locally end-to-end. **`ANTHROPIC_API_KEY` is ABSENT locally**, so the actual AI generation call in `generate_shift_summary` cannot run end-to-end locally — the enrichment (Plan 02) is verified by its unit tests + by inspecting the stored `stats` on any summary row that DOES exist; the acknowledge flow (Plans 03/04) is fully exercisable locally because it does not call anthropic. Flag this AI-key limitation explicitly in the SUMMARY rather than claiming the live generation path was exercised.

**Migration precondition:** confirm migration 105's columns exist on whatever DB `apps/api/.env`'s `SUPABASE_URL` points at (should be project `oacnwalhcpqdabivweki`, where Plan 01 applied it). If `.env` points elsewhere, run this verification against the environment that actually has migration 105 — do not verify against a DB missing the columns.

**Test account:** use the GM login recorded in project memory (`reference_test_account.md`) for the browser flow; for the "any-role can acknowledge" check, a non-GM account is ideal but if none is readily available, note that the any-role behavior is covered by Plan 03's test and by the absence of a require_role gate (state which method was used in the SUMMARY).

**Browser tooling:** the Chrome extension MCP tools may not be connected in this session. `playwright-cli` (the skill) is installed in this repo and is an acceptable fallback for driving localhost:3000 — use whichever is available.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full backend test suite</name>
  <files>none</files>
  <action>
Run the complete apps/api suite, not just the phase's new files:
```
cd apps/api && python -m pytest tests/ -v
```
If any pre-existing test fails, determine whether it's a regression from Plan 02 (shift_summary.py stats/prompt change) or Plan 03 (logbook.py new endpoint / get_shift_summary change) versus a pre-existing flake. Fix any regression before proceeding — do not continue with a red suite.
  </action>
  <verify>`cd apps/api && python -m pytest tests/ -v` exits 0, including `test_shift_summary_enrichment.py`, `test_shift_summary_acknowledgment.py`, and every pre-existing file (`test_logbook_timezone.py`, `test_internal_escalations.py`, `test_inventory.py`, smoke/*, etc.).</verify>
  <done>Full pytest suite green, zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Live golden-path verification via browser (both acknowledge surfaces)</name>
  <files>none</files>
  <action>
Per CLAUDE.md's Self-Verification Policy: start both dev servers if not running (`npm run dev:api` → 8003, `npm run dev:web` → 3000), confirm `GET http://localhost:8003/openapi.json` shows PatelRep paths, then drive this sequence against a GM test account via browser (Chrome MCP or `playwright-cli`):

1. **Seed a summary row if needed.** Because `ANTHROPIC_API_KEY` is absent locally, the on-demand "Generate for today" button will error at the AI call. To exercise acknowledge, ensure at least one `shift_summaries` row exists for the tenant (one may already exist from prior sessions/cron; if not, insert a minimal row directly via the Supabase MCP-applied dev DB or note that acknowledge could only be verified against a pre-existing row). Confirm the row's `acknowledged_at` is NULL to start.
2. **Logbook surface:** navigate to `/logbook`, open today's AI Shift Summary panel. If a stored summary exists for `shift_id='today'`, confirm the Acknowledge button renders. Click it. Confirm it flips to "Acknowledged by {name} · … ago" without a page reload, and the network POST to `/v1/logbook/shift-summary/{id}/acknowledge` returns 200.
3. **Idempotency:** reload `/logbook`; confirm the acknowledged line persists (not the button). If a second account is available, acknowledge again as that user and confirm the displayed acknowledger name does NOT change (idempotent no-overwrite over the network).
4. **Dashboard surface:** navigate to the GM dashboard. In `OvernightRecapStrip`, if the overnight AI entry links to a stored summary, confirm the acknowledge affordance renders correctly — a "✓ Acknowledged" badge if already acked in step 2 (same underlying shift_summary), or an "Acknowledge" link if a different summary. If it shows the link, click it and confirm it flips to the badge and the strip stays a single line (no wrap, height unchanged).
5. **Stats check:** for a summary row that exists, read its `stats` JSONB (via the Supabase dev DB query or the getShiftSummary/generate response if available) and confirm the four new keys — `vip_arrivals_count`, `pending_guest_issues_count`, `low_stock_parts_count`, `sla_breaches_count` — are present alongside the four original keys. (If no summary was produced post-Plan-02 because generation needs the AI key, note this and rely on Plan 02's unit test for the stats guarantee.)
6. Check the browser console + network tab throughout — no errors or 4xx/5xx from Phase 39 endpoints, and confirm the Spanish locale renders the new strings (toggle language if the app exposes it, or spot-check `es.ts` keys resolve).
  </action>
  <verify>Steps complete without console errors, failed Phase-39 network requests, or UI crashes. Acknowledge round-trips on at least the Logbook surface (and the dashboard surface when a linked summary exists); the acknowledged name+time render; idempotency holds; the recap strip stays single-line. Any step blocked purely by the absent ANTHROPIC_API_KEY is explicitly noted, not silently skipped.</verify>
  <done>The acknowledgment golden path works end-to-end on live servers across both surfaces (subject to the documented AI-key limitation on live generation), with the enriched stats confirmed present on an existing summary row.</done>
</task>

<task type="auto">
  <name>Task 3: Non-regression spot-check of adjacent flows</name>
  <files>none</files>
  <action>
Per the Non-Regression Policy, spot-check flows sharing code paths with Phase 39's changes:
1. **Logbook CRUD:** on `/logbook`, create a new entry, edit it, and delete it — confirm all still work (page.tsx was edited for the acknowledge affordance; the entry list/modals must be unaffected).
2. **On-demand Generate Summary:** click "Generate for today" in the AI panel. With no local AI key it is expected to surface the existing error path gracefully (not crash the panel) — confirm the panel still handles the error as before (regression check on the panel wiring, not on AI output).
3. **GM dashboard arrival-readiness:** confirm the dashboard hero (arrivals count, ready-for-arrival, blockers list) still renders and refreshes — `useArrivalReadiness.ts` was modified for the overnight acknowledgment fields; its hero/blockers/shift/pace outputs must be unchanged. Watch for any console error from the new dependent getShiftSummary query when the overnight entry has no shift_id (should be silently disabled, not erroring).
4. **OvernightRecapStrip empty state:** if no overnight summary exists, confirm the strip still shows the "no overnight summary" message and does not render a stray acknowledge affordance.
  </action>
  <verify>All four spot-checks complete without error. Logbook CRUD, the generate-summary error path, the arrival-readiness hero/blockers, and the recap-strip empty state are all unaffected by Phase 39.</verify>
  <done>Adjacent flows confirmed working — no previously-working feature broken by this phase.</done>
</task>

</tasks>

<verification>
Full pytest suite green. Acknowledgment golden path verified live on both UI surfaces (with the ANTHROPIC_API_KEY limitation on live generation documented). Enriched stats keys confirmed on an existing summary. Idempotency holds over the network. No regression in logbook CRUD, generate-summary, or the GM arrival-readiness dashboard.
</verification>

<success_criteria>
Phase 39 is verifiably complete: not just "code written" but "seen working" per CLAUDE.md's Self-Verification Policy — the enriched summary carries the four new signals, any staff member can acknowledge a handoff from either surface, and no adjacent feature regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/39-ai-shift-handover-and-gm-morning-brief/39-05-SUMMARY.md`
</output>
