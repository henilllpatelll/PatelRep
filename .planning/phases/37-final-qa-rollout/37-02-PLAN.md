---
phase: 37-final-qa-rollout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .wolf/buglog.json
autonomous: true

must_haves:
  truths:
    - "The automated NAV-05 role×nav matrix test still passes unchanged, confirming navigation.matrix.json is still the exact source of truth for all 6 roles"
    - "For every web-loginable role with known, already-documented test-tenant credentials (a subset of gm, housekeeping_supervisor, front_desk, chief_engineer — determined by Task 1 without any credential mutation), a live browser session against a safe is_test=true tenant with ALL ~21 section flags on simultaneously shows correct nav highlighting, correct breadcrumbs, correct command-palette cross-section search results, and consistent shell chrome (Sidebar/Header) — the specific cross-section interaction surface no single content phase's close-out ever exercised; any role without known credentials is code-traced instead, documented plainly, never silently skipped"
    - "For the 2 MOBILE_ONLY_ROLES (housekeeper, engineer), reachability is explicitly re-confirmed as web-blocked-by-design, and their nav allow-sets are confirmed via the automated matrix + code trace against their dashboard components — documented plainly as code-trace-only, not silently skipped"
  artifacts: []
  key_links:
    - from: "apps/web/lib/utils/navigation.test.ts"
      to: "apps/web/lib/utils/navigation.matrix.json"
      via: "npm run test:unit (tsx --test)"
      pattern: "getAllowedNavItems"
    - from: "live browser session, all 21 flags on"
      to: "apps/web/components/shared/CommandPalette.tsx, Sidebar.tsx, Header.tsx"
      via: "manual walkthrough per role"
      pattern: "nav|breadcrumb|command"
---

<objective>
QA-01: perform the full 6-role navigation walkthrough against the Phase-31 NAV-05 role×nav matrix. Re-run the existing automated matrix test as a cheap deterministic regression check, AND perform a genuinely fresh LIVE browser walkthrough — against a safe `is_test=true` tenant, never the real production tenant — with ALL ~21 section flags on simultaneously for every web-loginable role that has known, already-documented credentials. This is the first point in the whole milestone where every section is v2 at once, which is specifically what could surface cross-section interaction bugs (nav highlighting, breadcrumbs, command-palette search spanning sections, shell chrome) that no single content phase's isolated close-out could ever catch.

Per 37-RESEARCH.md: only 4 of 6 roles are literally web-loginable (`housekeeper`/`engineer` are `MOBILE_ONLY_ROLES`, blocked by `routeGuard.ts`/`login/page.tsx` before any route-rule logic runs). Every phase since 32 has verified these 2 roles via code-trace only — continue that established precedent explicitly and plainly rather than attempting an auth bypass (37-RESEARCH.md's Pitfall 2 explicitly warns against this). This plan additionally treats any web-loginable role lacking known, already-documented credentials on the chosen test tenant the same way — code-trace, never a credential reset/creation — since autonomously mutating a real person's auth credentials is out of bounds regardless of which tenant it targets.

Purpose: catch the ONE class of bug that structurally cannot be found by any individual section phase's own close-out — cross-section chrome/nav interactions only visible when everything is simultaneously v2 — without ever writing to the real production tenant or mutating anyone's real credentials.
Output: automated matrix re-confirmed green; live walkthrough evidence (for however many of the 4 web-loginable roles have known credentials) with all 21 flags on against a safe test tenant; code-trace confirmation for every remaining role; any defect found fixed or precisely flagged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-final-qa-rollout/37-CONTEXT.md
@.planning/phases/37-final-qa-rollout/37-RESEARCH.md

# The matrix + test being re-run, and the role-gating logic that blocks 2 of 6 roles on web
@apps/web/lib/utils/navigation.matrix.json
@apps/web/lib/utils/navigation.test.ts
@apps/web/lib/utils/navigation.ts
@apps/web/lib/utils/routeGuard.ts
@apps/web/app/(auth)/login/page.tsx

# Cross-section chrome surfaces this walkthrough must exercise
@apps/web/components/shared/CommandPalette.tsx
@apps/web/components/shared/Sidebar.tsx
@apps/web/components/shared/Header.tsx

# Prior code-trace-only precedent for MOBILE_ONLY_ROLES (32-06/34-08/36-04)
@.planning/phases/36-housekeeping-section-chrome/36-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated matrix re-run + all-21-flags live walkthrough setup</name>
  <files></files>
  <action>
From `apps/web`, run `npm run test:unit` (bundles `navigation.test.ts` with 3 other unit suites in one `tsx --test` invocation — this is the existing script, do not try to isolate just the nav test). Confirm all assertions pass, specifically: `getAllowedNavItems()` output for all 6 roles deep-equals `navigation.matrix.json`'s committed baseline, and every allowed href for every role falls within a known sidebar group. Confirm via `git log -- apps/web/lib/utils/navigation.matrix.json apps/web/lib/utils/navigation.test.ts` (or diff against the Phase-31 commit) that neither file has been touched since Phase 31 — this is expected, not a gap.

Then set up the live walkthrough. **Do NOT use the real production tenant (`23264962-aa09-4e4f-a49d-fc345cc91414`, "Sonesta ES Suites Fossil Creek") for this — that tenant's `web_redesign_sections` must never be written by this plan, even temporarily.** Instead, query `tenants(id, name, is_test)` (read-only, service-role) for an `is_test=true` tenant to use as the walkthrough target — prefer "Patel Test Hotel" if present, otherwise fall back to any one of the other confirmed `is_test=true` rows from 37-RESEARCH.md's census (the Lakeside Inn & Suites or Sonesta ES Suites test duplicates). Record the chosen tenant's id and name in the summary. Temporarily set ONLY that test tenant's `web_redesign_sections` to the FULL 21-key array via direct Supabase service-role update (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` from `apps/api/.env`, same mechanism `seed-regression-tenant.mjs` uses): `shell, dashboard, tasks, evidence, engineering, reports, managementRoi, aiCopilot, logbook, staff, lostFound, programs, sop, scheduling, safety, guestRequests, billing, settings, guestFeedback, integrations, housekeeping`. Record the tenant's PRE-change value first so it can be restored exactly (expected `[]` per research's tenant census, but confirm rather than assume). Start `npm run dev:web` and `npm run dev:api` if not already running.

For each web-loginable role (`gm`, `housekeeping_supervisor`, `front_desk`, `chief_engineer`) on the chosen test tenant: use ONLY already-known, already-documented credentials (e.g. an existing regression-fixture-style env var, or a credential already on file from a prior phase's SUMMARY for this specific tenant). **Do NOT reset, generate, or otherwise mutate any staff member's real auth password or credentials via the Supabase Auth Admin API or any other means, for any role, on any tenant, in this plan** — that is a real person's real login, autonomously mutated without their knowledge, which is out of bounds regardless of which tenant it targets. If a role's credentials are not already known/documented for the chosen test tenant, do not attempt to obtain or create them — explicitly fall back to code-trace-only verification for that role instead, exactly matching the established precedent already used for `housekeeper`/`engineer` (see Task 2), and document the disposition plainly (which roles were live-verified vs. code-traced, and why) rather than silently skipping any role. It is expected and acceptable that this may reduce the live-verified role count below 4 — correctness of disposition matters more than maximizing live coverage.
  </action>
  <verify>`npm run test:unit` exits 0 with all navigation assertions passing. Chosen `is_test=true` tenant's `web_redesign_sections` confirmed set to the full 21-key array via a read-back query (production tenant `23264962-...` untouched — confirm via a separate read-only check it still shows its pre-plan value). Per-role credential availability determined for all 4 web-loginable roles, with any role lacking known credentials explicitly flagged for code-trace fallback (no credential mutation attempted).</verify>
  <done>Automated matrix test green, confirming zero drift since Phase 31; a safe `is_test=true` tenant (not the real tenant) is live with all 21 flags on, ready for the walkthrough; each role's live-vs-code-trace disposition is determined and documented, with zero auth mutation performed.</done>
</task>

<task type="auto">
  <name>Task 2: Live 4-role walkthrough (all 21 flags on) + 2-role code-trace, restore tenant, report</name>
  <files></files>
  <action>
For each web-loginable role that Task 1 confirmed has known, already-documented credentials on the chosen `is_test=true` tenant (a subset of `gm`, `housekeeping_supervisor`, `front_desk`, `chief_engineer` — exactly which ones depends on Task 1's findings, per its no-credential-mutation constraint), log into that test tenant (all 21 flags on) and walk through:
- **Nav highlighting/active-state:** click through at least 5 different sidebar sections in sequence; confirm the active nav item visually/semantically updates correctly each time (no stale highlight, no missing highlight) across the full breadth of sections now simultaneously v2.
- **Breadcrumbs:** visit at least 3 distinct routes at different nesting depths (e.g. a top-level dashboard route, a `/settings/*` subroute, a detail/drawer route) and confirm breadcrumb trails render correctly and consistently now that every section shares the same v2 chrome.
- **Command palette:** open `CommandPalette` (confirm its trigger — likely a keyboard shortcut or header button) and run at least 2 searches that should return results spanning MULTIPLE different sections (e.g. a term matching both a Task and a Work Order, or a term matching both Housekeeping and Engineering content) — confirm results are correctly scoped/labeled per section and none are missing or misattributed now that all sections are simultaneously v2.
- **Shell chrome (Sidebar/Header):** confirm the Sidebar's full nav item list for this role renders consistently (matches `navigation.matrix.json`'s allow-set for the role, no items missing/duplicated/misplaced) and Header renders without visual glitches across a few different section pages.
- Confirm zero console errors and zero failed/forbidden (401/403) API calls during the walkthrough.

For `housekeeper` and `engineer` (always code-trace, per `MOBILE_ONLY_ROLES`) and for any of `gm`/`housekeeping_supervisor`/`front_desk`/`chief_engineer` that Task 1 could not resolve known credentials for: code-trace only, per established Phase 32/34/36 precedent — confirm via source read that `HousekeeperDashboard.tsx`/`EngineerDashboard.tsx` (or the relevant role's dashboard/nav-consuming component) render the nav-relevant chrome consistent with `navigation.matrix.json`'s allow-set for that role, and confirm `routeGuard.ts`'s `MOBILE_ONLY_ROLES` gate still blocks `housekeeper`/`engineer` before any route-rule logic (re-confirm this is still true, don't assume). Do NOT attempt to bypass `middleware.ts`/`routeGuard.ts` to force a live session for these roles, and do NOT attempt any credential reset/creation for the roles lacking known credentials — code-trace is the correct, final disposition for them in this plan.

Restore the chosen test tenant's `web_redesign_sections` back to its Task-1-recorded pre-change value (expected `[]`) via the same service-role mechanism. Confirm the restore via a read-back query. Also confirm, via a read-only check, that the real production tenant (`23264962-...`) was never touched by this plan.

Record, per role, what was live-verified vs code-traced, and any defect found. If a real cross-section interaction bug is found (not a single-section issue already closed by a prior phase), fix it if scoped/tractable within this plan's file ownership (`.wolf/buglog.json` only listed in frontmatter — if a source fix is genuinely needed, note the deviation and the file touched); otherwise log to `.wolf/buglog.json` and flag precisely for the orchestrator.
  </action>
  <verify>All 4 (or fewer, with fallback documented) web-loginable roles walked through live with nav highlighting/breadcrumbs/command-palette/shell chrome all confirmed correct across multiple simultaneously-v2 sections; zero console errors/forbidden calls; housekeeper/engineer (and front_desk if blocked) code-traced against navigation.matrix.json's allow-sets with MOBILE_ONLY_ROLES gate re-confirmed; test tenant's web_redesign_sections restored to its pre-change baseline and confirmed via read-back.</verify>
  <done>QA-01 fully verified: automated matrix green, live cross-section walkthrough complete for every reachable web role with all 21 flags on simultaneously, remaining roles code-traced and explicitly documented, tenant restored to baseline, any defect found fixed or precisely flagged.</done>
</task>

</tasks>

<verification>
- `navigation.test.ts`/`navigation.matrix.json` re-confirmed unchanged and green since Phase 31.
- Live browser walkthrough completed, against a safe `is_test=true` tenant (never the real production tenant), for every web-loginable role with known/already-documented credentials — nav highlighting, breadcrumbs, command-palette cross-section search, and shell chrome all confirmed correct.
- housekeeper/engineer, plus any of gm/housekeeping_supervisor/front_desk/chief_engineer lacking known credentials, explicitly code-traced, not silently skipped, with the MOBILE_ONLY_ROLES gate re-confirmed. Zero credential creation/reset attempted anywhere in this plan.
- Test tenant's `web_redesign_sections` restored to its exact pre-change baseline, confirmed via read-back.
</verification>

<success_criteria>
QA-01 (full 6-role navigation walkthrough against the NAV-05 role×nav matrix) passes: every web-loginable role with known credentials is live-verified against a safe `is_test=true` tenant with every section simultaneously v2 (the specific new coverage this phase adds), every remaining role (including housekeeper/engineer and any web-loginable role lacking known credentials) is code-traced per established precedent, zero regressions found or all found regressions fixed/flagged, test tenant state restored, real production tenant never touched, zero credential mutation performed.
</success_criteria>

<output>
After completion, create `.planning/phases/37-final-qa-rollout/37-02-SUMMARY.md` recording: which `is_test=true` tenant was chosen and why, automated test result, a per-role table of live-verified vs code-traced (with the reason for each role's disposition — known credentials vs. none available), confirmation zero auth mutation was performed anywhere, any defect found and its disposition (fixed/deferred with buglog id), and explicit confirmation both the chosen test tenant and the real production tenant were left in their correct end states (test tenant restored to `[]`, real tenant never touched).
</output>
