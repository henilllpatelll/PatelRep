---
phase: 37-final-qa-rollout
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The 3 excluded Room-Board surfaces (RoomStatusBoard, RoomDetailDrawer, EngineeringRoomBoard) remain pixel-identical to the Phase-30 baseline when tested individually at zero drift, matching every close-out since Phase 30"
    - "The same 3 surfaces ALSO stay pixel-identical when the regression fixture tenant has ALL ~21 section keys populated simultaneously (post-rollout simulation) — proving the frozen boards stay frozen even when every surrounding section is v2, not just individually, which is what every prior phase's own close-out actually tested"
    - "The regression fixture tenant is restored to its `[]` baseline afterward, with zero leakage into any other phase's future baseline runs"
  artifacts: []
  key_links:
    - from: "apps/web/e2e/room-board-baseline.spec.ts"
      to: "the regression fixture tenant (a0000000-0000-4000-a000-000000000001)"
      via: "web_redesign_sections set to [] then to the full 21-key array, via direct Supabase service-role update"
      pattern: "web_redesign_sections"
---

<objective>
QA-03: run the final pixel-diff of the 3 excluded Room-Board surfaces. `apps/web/e2e/room-board-baseline.spec.ts` already covers exactly these surfaces and has passed clean at every phase's close-out since Phase 30 (most recently Phase 36: 12/12). Per CONTEXT.md's locked decision, this is a re-run — no new coverage needed — EXCEPT for one addition: run it ONCE MORE with the regression fixture tenant's `web_redesign_sections` populated with ALL ~21 section keys simultaneously (simulating post-rollout state), to prove the excluded/frozen boards stay pixel-identical even when every surrounding section is v2 at once, not just individually (which is all any prior phase's own close-out ever tested).

Per 37-RESEARCH.md: `origin/main` is confirmed stale (local HEAD ahead by commits, hardcoded default production URL is dead) — this WILL require the same local-standalone-build workaround every phase since ~32 has used (temporary `REGRESSION_LOCAL_CSP`-gated CSP patch to `next.config.mjs`, fully reverted after).

Purpose: final confirmation, not construction — this is the last check that the 3 permanently-frozen surfaces from Phase 30's foundation are provably untouched, even under the specific new condition (all-21-flags-on) this milestone's close is about to make permanent.
Output: 24 total snapshot comparisons (12 baseline flag-off + 12 with all-21-flags-on) at zero pixel drift; fixture tenant restored.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-final-qa-rollout/37-CONTEXT.md
@.planning/phases/37-final-qa-rollout/37-RESEARCH.md

# The harness itself — read only, no edits expected (chromeMasks() already
# includes the page-header mask from Phase 35, confirmed reusable as-is)
@apps/web/e2e/room-board-baseline.spec.ts
@apps/web/playwright.regression.config.ts
@apps/web/e2e/global-setup.ts
@apps/web/e2e/fixtures/seed-regression-tenant.mjs
@apps/web/next.config.mjs

# Prior close-out precedent for the local-standalone-build workaround
@.planning/phases/36-housekeeping-section-chrome/36-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local standalone build + baseline regression re-run (flag-off, 12/12 expected)</name>
  <files>apps/web/next.config.mjs</files>
  <action>
Confirm `git rev-list --left-right --count origin/main...HEAD` still shows local HEAD ahead (expect this, per 37-RESEARCH.md) — if so, `playwright.regression.config.ts`'s hardcoded default `baseURL` (the stale, pre-account-migration Railway URL) will not work. Set up the established local-standalone-build workaround: temporarily add an env-gated localhost allowance to `next.config.mjs`'s `buildCSP()` `connectSrc` array (e.g. `...(process.env.REGRESSION_LOCAL_CSP ? ['http://localhost:*', 'http://127.0.0.1:*'] : [])`, mirroring the existing `isDev` pattern already in the file) — this must be a MINIMAL, clearly-temporary addition, reverted in Task 2.

Build: `cd apps/web && npm run build`, then copy `public/` and `.next/static/` into `.next/standalone/` per this project's documented Railway build steps (see root CLAUDE.md's Commands section), then run `REGRESSION_LOCAL_CSP=1 node .next/standalone/server.js` on a free local port (e.g. 3100) in the background.

Run the baseline regression suite against it: `PLAYWRIGHT_BASE_URL=http://localhost:3100 npx playwright test --config=playwright.regression.config.ts`. Confirm the regression fixture tenant (`a0000000-0000-4000-a000-000000000001`) currently has `web_redesign_sections = []` (read-back first — do not assume) before running, since this first pass IS the flag-off baseline. Expect 12/12 pass, zero pixel drift, matching every close-out since Phase 30 (RoomDetailDrawer's documented pre-existing sub-pixel font-AA noise is expected environment noise if it recurs identically, not a regression — visually inspect any diff before dismissing it, per established precedent).
  </action>
  <verify>Local standalone build serves successfully; regression suite reports 12/12 pass at zero pixel drift for the flag-off baseline against the local build.</verify>
  <done>Flag-off baseline re-confirmed clean (12/12, zero drift) against a working local standalone build, matching every prior phase's close-out result.</done>
</task>

<task type="auto">
  <name>Task 2: All-21-flags regression run, restore fixture + revert CSP patch, report</name>
  <files>apps/web/next.config.mjs</files>
  <action>
Set the regression fixture tenant's `web_redesign_sections` to the FULL 21-key array (`shell, dashboard, tasks, evidence, engineering, reports, managementRoi, aiCopilot, logbook, staff, lostFound, programs, sop, scheduling, safety, guestRequests, billing, settings, guestFeedback, integrations, housekeeping`) via the same service-role mechanism `seed-regression-tenant.mjs` already uses (confirm via read-back). Re-run the SAME regression suite against the same local standalone build (no rebuild needed — the flag lives in the database, read at request time via `/auth/me`, not baked into the build): `PLAYWRIGHT_BASE_URL=http://localhost:3100 npx playwright test --config=playwright.regression.config.ts`.

Expect 12/12 pass, zero pixel drift — this is the NEW check this phase adds: proving `RoomStatusBoard`/`RoomDetailDrawer`/`EngineeringRoomBoard` stay pixel-identical even when every surrounding section (PageHeader, Sidebar, Header, everything `chromeMasks()` already masks) is simultaneously v2, not just individually. If ANY drift appears, visually inspect the diff image: if it falls strictly within the already-masked regions (page-header, shell landmarks, live-sync badge, date-nav — per `chromeMasks()`), it's a masking gap to fix by extending `chromeMasks()` narrowly (this would touch `apps/web/e2e/room-board-baseline.spec.ts`, un-emptying this plan's `files_modified` for that specific fix); if it falls INSIDE the frozen board's own content area, that is a genuine, serious regression — stop and flag it prominently rather than attempting a workaround, since the frozen boards must never change.

Restore the regression fixture tenant's `web_redesign_sections` back to `[]` (confirmed via read-back) — this is the harness's permanent baseline state and MUST be restored exactly, since `room-board-baseline.spec.ts`'s existing 12 committed snapshots assume flag-off. Revert the temporary `next.config.mjs` CSP patch completely: confirm via `git diff --exit-code apps/web/next.config.mjs` that the file is byte-identical to its pre-patch state. Stop the local standalone server process.
  </action>
  <verify>All-21-flags run reports 12/12 pass at zero pixel drift (or any found drift is either a narrow masking gap that's fixed and re-verified, or a genuine regression that's prominently flagged, not silently worked around). Fixture tenant confirmed restored to `[]` via read-back. `git diff --exit-code apps/web/next.config.mjs` confirms zero residual diff. Local server process terminated.</verify>
  <done>QA-03 fully verified: 24/24 total snapshot comparisons (12 flag-off + 12 all-21-flags-on) at zero pixel drift across both runs, fixture tenant restored to its permanent `[]` baseline, temporary CSP patch fully reverted with no residual diff.</done>
</task>

</tasks>

<verification>
- Both regression runs (flag-off baseline, all-21-flags-on) pass 12/12 at zero pixel drift against a local standalone build (origin/main confirmed stale, workaround correctly applied and correctly reverted).
- Regression fixture tenant restored to its permanent `[]` baseline, confirmed via read-back — no leakage into any future phase's baseline runs.
- `next.config.mjs`'s temporary CSP patch fully reverted, confirmed via `git diff --exit-code`.
</verification>

<success_criteria>
QA-03 passes: the 3 excluded Room-Board surfaces are confirmed pixel-identical to the Phase-30 baseline both individually (flag-off) AND under the specific new post-rollout-simulation condition (all 21 section flags on simultaneously) — the one new check this close-out phase adds beyond every prior phase's own re-run. Zero drift, zero regression, fixture state and build config left exactly as found.
</success_criteria>

<output>
After completion, create `.planning/phases/37-final-qa-rollout/37-04-SUMMARY.md` recording: both regression run results (12/12 each, or any finding and its disposition), confirmation the local-standalone-build workaround was needed and correctly reverted, and explicit confirmation the fixture tenant was restored to baseline.
</output>
