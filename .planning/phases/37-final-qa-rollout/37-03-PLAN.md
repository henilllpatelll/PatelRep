---
phase: 37-final-qa-rollout
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - .wolf/buglog.json
autonomous: true

must_haves:
  truths:
    - "Every redesigned section/dashboard renders correctly in Spanish, live, with no missing translation keys and no raw/untranslated copy — QA-02's literal bar"
    - "bug-965's fix (37-01) is confirmed working live, in the context of a real full-section walkthrough, not just the isolated regression spec — the StateBlock/PageHeader hybrid-mangling defect class does not recur anywhere across all ~21 sections when every section is simultaneously v2"
    - "bug-1021's residual cold-reload hydration-race behavior is re-tested post-fix and its exact observed outcome (correct-Spanish flash vs still-hybrid) is recorded plainly, not assumed"
  artifacts: []
  key_links:
    - from: "live browser walkthrough, es locale"
      to: "apps/web/i18n/domTranslations.ts (post 37-01 fix)"
      via: "dual-method check (live-toggle + cold-reload-with-es-persisted) across all ~21 sections"
      pattern: "es\\.ts|domTranslations"
---

<objective>
QA-02: perform a full Spanish-locale walkthrough of every redesigned section and dashboard, confirming no missing or raw copy anywhere — with 37-01's bug-965 fix already shipped, this plan's job is to prove that fix holds up under a genuinely comprehensive, cross-section live walkthrough (not just the single isolated regression spec 37-01 added), and to re-characterize bug-1021's residual behavior now that the forward-translation path has changed.

CONTEXT.md's locked decision: Phase 37 is the dedicated closing gate for bug-965 — this plan verifies that gate actually closed, using the established dual-method check (live-toggle for bug-965-class issues, cold-reload-with-es-persisted for bug-1021-class issues) from Phase 35/36 precedent, applied across ALL ~21 sections simultaneously rather than one phase's own new content.

Purpose: confirm QA-02's "no missing or raw copy" bar is met app-wide, not just for the one call site 37-01's spec directly exercises.
Output: live EN/ES walkthrough evidence across all redesigned sections; bug-1021's post-fix behavior recorded; any residual defect fixed or precisely flagged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-final-qa-rollout/37-CONTEXT.md
@.planning/phases/37-final-qa-rollout/37-RESEARCH.md

# What 37-01 shipped — read its summary to know exactly what changed before re-verifying it
@.planning/phases/37-final-qa-rollout/37-01-SUMMARY.md

# The fixed file, and the new committed spec's exact scenario (don't duplicate it — this
# plan covers everything ELSE)
@apps/web/i18n/domTranslations.ts
@apps/web/e2e/i18n-dom-translator.spec.ts

# Dual-method precedent (live-toggle + cold-reload-with-es-persisted) established here
@.planning/phases/35-engineering-section-chrome/35-07-SUMMARY.md
@.planning/phases/36-housekeeping-section-chrome/36-04-SUMMARY.md

# The 21 gated sections/dashboards this walkthrough must cover
@apps/web/lib/utils/redesignFlag.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip all 21 flags on test tenant, walk every section EN and live-toggled ES</name>
  <files></files>
  <action>
Using the same test tenant (`23264962-aa09-4e4f-a49d-fc345cc91414`) and the same service-role flag-flip mechanism as prior phases, record its current `web_redesign_sections` value (should already be `[]` if 37-02 restored it — confirm via read-back rather than assuming plan-execution order guarantees this) and set it to the full 21-key array: `shell, dashboard, tasks, evidence, engineering, reports, managementRoi, aiCopilot, logbook, staff, lostFound, programs, sop, scheduling, safety, guestRequests, billing, settings, guestFeedback, integrations, housekeeping`.

Start `npm run dev:web`/`npm run dev:api` if not running. Log in as `gm` (has the broadest section access). For EVERY one of the 21 sections, visit its route(s) in EN first (confirm no obviously-broken EN copy as a baseline), then toggle to Spanish via the real `LanguageToggle` (live-toggle method) and re-visit or re-render each section, checking for: missing translation keys (raw i18next key strings like `housekeeping.page.board.assignMode` literally rendering instead of translated text — a distinct failure mode from bug-965's hybrid mangling), and any hybrid English/Spanish mangling (the bug-965 class this phase's 37-01 plan fixed).

Where a section has an easily-forceable error/empty state (StateBlock loading/error/empty branches, similar to Housekeeping's staff-list/my-rooms errors), force at least one such state per section type category (pick a representative sample — not literally all ~40+ StateBlock call sites, but at least one per major dashboard: Tasks, Evidence, Engineering, Reports, Management ROI, AI Copilot, Logbook, Staff, Lost & Found, Programs, SOP, Scheduling, Safety, Guest Requests, Billing, Settings, Guest Feedback, Integrations, Housekeeping, plus the shared Dashboard/shell chrome) via devtools route interception, to specifically exercise the async-mount pattern bug-965's mechanism targets — this is the highest-yield way to find any residual instance the isolated 37-01 spec didn't cover.

Record a pass/fail per section. If ANY raw-key or hybrid-mangling instance is found, treat it as a REAL finding: if it's the SAME domTranslations.ts mechanism 37-01 already fixed but somehow still recurring (would indicate the fix is incomplete), that's a blocking regression on 37-01 — investigate and fix domTranslations.ts further before proceeding. If it's a DIFFERENT root cause (e.g. a genuinely missing i18next key never added by any phase), fix the missing key in `en.ts`/`es.ts` directly (this un-empties `files_modified` for that fix). Log any fix to `.wolf/buglog.json`.
  </action>
  <verify>All 21 sections visited in both EN and ES via live-toggle; representative StateBlock error states forced and checked across the major dashboard categories; zero raw i18next keys rendering; zero hybrid mangling recurrence (or any found instance fixed and re-verified clean).</verify>
  <done>Full EN/ES live-toggle walkthrough complete across all 21 redesigned sections with a recorded pass/fail per section; any real defect found is fixed and re-verified, not silently noted.</done>
</task>

<task type="auto">
  <name>Task 2: Cold-reload-with-es-persisted re-check (bug-1021 class), restore tenant, report</name>
  <files></files>
  <action>
Re-run Phase 35's exact bug-1021 repro (per 37-RESEARCH.md Open Question 2): pre-seed `localStorage.setItem('patelrep-language', 'es')` before navigation (matching `room-board-baseline.spec.ts`'s `gotoWithTheme`-style `addInitScript` pattern), then cold-navigate directly to the Predictions page (the original bug-1021 discovery site, per 35-07-SUMMARY.md) and re-check its subtitle rendering. Record the EXACT observed outcome plainly — per 37-RESEARCH.md's prediction, 37-01's fix should turn any transient pre-hydration flash from an EN/ES HYBRID into a flash of FULLY-CORRECT Spanish (since the recovered English now round-trips through the same full dictionary), but this is a prediction to be VALIDATED, not assumed. Also spot-check 2-3 other sections' PageHeader instances with the same cold-reload-with-es-persisted method to broaden coverage beyond the single originally-discovered site.

Do NOT attempt to fix bug-1021's underlying SSR/hydration-race root cause in this plan — per CONTEXT.md, that remains legitimately out of scope for Phase 37 (it's an architectural timing issue, not the dictionary-completeness gap 37-01 fixed). Simply record the precise post-fix observed behavior (still-hybrid / correct-Spanish-flash / no-flash-at-all) so the close-out summary can state factually whether QA-02's "no missing or raw copy" bar is fully met or whether a residual, already-scoped-out-by-CONTEXT.md flash remains and why that's an acceptable disposition (transient sub-second flash of grammatically-correct Spanish, not a persistent hybrid/raw defect, if that's what's observed).

Restore the test tenant's `web_redesign_sections` back to its pre-Task-1 baseline (expected `[]`) via the same service-role mechanism, confirmed via read-back.
  </action>
  <verify>Cold-reload-with-es-persisted re-check performed on the original bug-1021 site plus 2-3 additional PageHeader instances; exact observed outcome recorded (not assumed); tenant restored to baseline and confirmed via read-back.</verify>
  <done>bug-1021's post-37-01-fix behavior is factually characterized (not assumed) and its continued out-of-scope disposition is explicitly justified per CONTEXT.md if it still shows any residual flash; tenant restored.</done>
</task>

</tasks>

<verification>
- All 21 redesigned sections walked through live in EN and ES (live-toggle method); zero raw i18next keys; zero bug-965-class hybrid mangling anywhere (confirming 37-01's fix holds app-wide, not just at its own spec's one call site).
- Cold-reload-with-es-persisted re-check performed on bug-1021's original discovery site plus additional spot-checks; exact post-fix behavior recorded plainly.
- Any real defect found (regression in the fix, or a genuinely missing key) is fixed and re-verified, not deferred.
- Test tenant restored to baseline, confirmed via read-back.
</verification>

<success_criteria>
QA-02 passes: a full Spanish-locale walkthrough of every redesigned section/dashboard shows no missing or raw copy, with bug-965 confirmed fixed app-wide (not just at 37-01's own spec's single call site) and bug-1021's residual behavior factually characterized post-fix rather than assumed.
</success_criteria>

<output>
After completion, create `.planning/phases/37-final-qa-rollout/37-03-SUMMARY.md` recording: a per-section EN/ES pass/fail table, confirmation bug-965 does not recur anywhere in the broader walkthrough, the exact bug-1021 post-fix observation, any defect found+fixed, and explicit confirmation the tenant was restored.
</output>
