---
phase: 35-engineering-section-chrome
plan: 06
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - apps/web/app/(dashboard)/engineering/predictions/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of predictions/page.tsx. No sibling wave-2
# plan touches it. This is the page CONTEXT.md originally flagged as having zero isError handling
# (the ONE gap CONTEXT's discussion explicitly named) — confirmed true: `const { data: predictions,
# isLoading } = useQuery(...)` has no isError/refetch at all today. Does NOT edit en.ts/es.ts —
# consumes the ONE new key added in 35-01 (`engineering.predictionsPage.loadError`).

must_haves:
  truths:
    - "With the 'engineering' flag ON, the predictions list's query gains isError + refetch (currently absent entirely) and a StateBlock error state (new engineering.predictionsPage.loadError key, onRetry: refetch) renders on failure, taking precedence over the existing loading/empty/data 3-way branch; flag OFF is byte-behaviorally identical to today (no error UI at all, matching current behavior)"
    - "With the 'engineering' flag ON, the existing SkeletonCard-based loading state is preserved (already a skeleton, not a spinner) with only its underlying token classes re-skinned if any raw gray-* classes remain; the existing rich empty state (clear-filters / go-to-asset-register / 3 sample chips) is preserved structurally, only its typography tokens updated to match EmptyState's visual language"
    - "No query key/params, no RBAC (canManage/canAuthorize) logic, and no acknowledge/create-work-order/authorize mutation behavior changed"
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/predictions/page.tsx"
      provides: "Reads isSectionRedesigned('engineering', hotel) once; adds isError+refetch to the predictions query (genuinely new); StateBlock error using the new predictionsPage.loadError key; SkeletonCard loading preserved; rich empty-state structure preserved with v2-token typography; legacy branch unchanged"
      contains: "isSectionRedesigned('engineering'"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/predictions/page.tsx (predictions query)"
      to: "StateBlock error"
      via: "isError + refetch newly destructured; error.onRetry = () => refetch()"
      pattern: "refetch"
---

<objective>
Redesign predictions/page.tsx's chrome — its `PageHeader` and the predictions list's loading/error/empty states — behind the `engineering` flag. Predictions is the one page CONTEXT.md's discussion originally identified as having a genuine, total absence of error handling (`const { data: predictions, isLoading } = useQuery(...)` — no `isError`, no `refetch`) — add both, wire a new `StateBlock status="error"` using the new `engineering.predictionsPage.loadError` key from 35-01. The existing `SkeletonCard`-based loading state already follows the skeleton-not-spinner convention (preserve it, only re-skin any remaining raw color classes). The existing empty state is unusually rich (a `clearFilters` conditional link OR a `goToAssetRegister` link, plus 3 sample-risk cards) — per RESEARCH's Open Question 2 recommendation, preserve this structure exactly, only bringing its title/body typography in line with `EmptyState`'s visual language (matching the same approach taken for Assets' empty state in 35-04, for consistency across the two richest empty states in this phase).

Purpose: ENG-01 for the Predictions page — closes this phase's most clear-cut, CONTEXT-flagged gap.
Output: predictions/page.tsx, flag-gated, isError/refetch added to its one query, StateBlock error-wired, empty-state typography v2-token-ified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md

@apps/web/app/(dashboard)/engineering/predictions/page.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag; add isError+refetch to the predictions query; new StateBlock error state</name>
  <files>apps/web/app/(dashboard)/engineering/predictions/page.tsx</files>
  <action>
1. Read the flag once in the top-level page component — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`. Add `dataI18nSkip={v2}` to the `PageHeader` call (`title={t('engineering.predictionsPage.heading')}`, `subtitle={t('engineering.predictionsPage.subtitle')}`).
2. Extend `const { data: predictions, isLoading } = useQuery({ queryKey: ['failure-predictions-history'], ... })` to `const { data: predictions, isLoading, isError, refetch } = useQuery({...})` — this query currently has ZERO error handling; this is a mechanical, purely-additive destructuring change (no queryFn/queryKey/select change).
3. In the render section (the "Predictions list" block, currently `isLoading ? <SkeletonCard/... /> : filtered.length === 0 ? <empty block> : <PredictionCard list>`), add a FOURTH leading branch under `v2`: `isError ? <StateBlock status="error" error={{ message: t('engineering.predictionsPage.loadError'), onRetry: () => refetch() }} /> : isLoading ? ... : filtered.length === 0 ? ... : ...` — error takes precedence over loading/empty/data. Under `!v2`, `isError`/`refetch` are simply unused and the existing 3-way conditional is unchanged (matching today's behavior — no error UI at all, which is today's actual, if incomplete, behavior).
4. Do not change `filtered` derivation, `riskFilter`/`statusFilter`/`filtersActive`, or any of the 5 mutations (`acknowledgeMutation`/`batchAcknowledgeMutation`/`createWOMutation`/`authorizeRecommendationMutation`).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms `dataI18nSkip={v2}` on PageHeader; grep confirms the `predictions` query destructuring now includes `isError`/`refetch`; grep confirms `StateBlock` used for a new error branch under `v2`, referencing `engineering.predictionsPage.loadError` and calling `refetch()`; grep confirms the legacy 3-way loading/empty/data conditional is unchanged for `!v2`; git status shows only predictions/page.tsx changed</verify>
  <done>Predictions page reads the flag once; PageHeader dataI18nSkip'd; predictions query gains isError+refetch; a StateBlock error branch (new predictionsPage.loadError key, onRetry: refetch) takes precedence under v2; !v2 unchanged (no error UI, matching today); no mutation/RBAC change; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Re-skin SkeletonCard's raw classes if any; empty-state typography v2-token-ify (preserve rich structure)</name>
  <files>apps/web/app/(dashboard)/engineering/predictions/page.tsx</files>
  <action>
1. Inspect `SkeletonCard` (the loading-state component used in the "Predictions list" block) for any remaining raw `bg-gray-*`/non-token classes. If found, under `v2` replace those specific classes with the shared `Skeleton` component or the equivalent v2 token class (`bg-surface-3`) — same sizing/layout, just re-skinned. If `SkeletonCard` is already fully token-based, this step is a no-op (confirm and note it as such — do not force a component swap purely for its own sake). Under `!v2`, `SkeletonCard` renders unchanged either way.
2. Empty state (the `filtered.length === 0` block: icon-circle + `emptyHeading` + EITHER `emptyFilteredText`+`clearFilters` link+`emptyFilteredSuffix` (when `filtersActive`) OR `emptyHelp`+`goToAssetRegister` link (when not), PLUS a conditional 3-sample-chip grid when `!filtersActive`): under `v2`, preserve this ENTIRE structure and both conditional branches exactly, only updating the raw `text-gray-800`/`text-gray-500`/`text-gray-400` typography classes on the heading/body text to `EmptyState`-equivalent tokens (`text-ink` for the title, `text-ink3` for the body/help copy), matching the same typography-token approach taken for Assets' empty state in plan 35-04, for cross-page visual consistency. The `clearFilters`/`goToAssetRegister` links' own accent-color classes (`text-[var(--caution)]`) are already token-based — leave unchanged. Under `!v2`, keep the current raw `gray-*` classes unchanged.
3. Do not change the `filtersActive` computation, the `setRiskFilter('all')`/`setStatusFilter('all')` clear-filters handler, or the `<a href="/engineering/assets">` link's destination.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms the empty-state title/body classes under `v2` use `text-ink`/`text-ink3` (not `gray-800`/`gray-500`/`gray-400`) while both conditional branches (filtered vs. not) and the 3-sample-chip grid remain structurally present; grep confirms the legacy (`!v2`) classes are still present, byte-unchanged; git status shows only predictions/page.tsx changed (combined with Task 1)</verify>
  <done>SkeletonCard confirmed already-token-based or re-skinned; empty state's rich structure (both filtered/unfiltered branches, sample chips, both links) fully preserved with typography updated to v2 tokens under v2; legacy branch unchanged; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Flag read once; PageHeader dataI18nSkip'd; predictions query gains isError+refetch (genuinely new); StateBlock error takes precedence under v2 using the new predictionsPage.loadError key.
- SkeletonCard confirmed token-consistent (re-skinned if needed); rich empty state's full structure (both branches, sample chips) preserved with only typography updated.
- Legacy (`!v2`) branch renders byte-identical to pre-phase markup throughout (including the current total absence of error UI).
- Only predictions/page.tsx changed; no frozen file, no locale file, no query/mutation/RBAC logic changed.
</verification>

<success_criteria>
Predictions renders its PageHeader and predictions-list loading/error/empty states in the new visual system behind the engineering flag — closing this phase's most clear-cut gap (a page with zero prior error handling) via a newly-added isError+refetch and a StateBlock error state, while preserving its already-skeleton-based loading and unusually rich empty-state structure — zero behavior/data/RBAC change.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-06-SUMMARY.md` recording: confirmation the predictions query's isError/refetch addition is purely additive (no queryFn/select change), the exact StateBlock error wiring, the SkeletonCard re-skin outcome (was it already token-based, or what was changed), the empty-state typography-token decision (mirroring 35-04's Assets approach), and any deferred item for the close-out list.
</output>
