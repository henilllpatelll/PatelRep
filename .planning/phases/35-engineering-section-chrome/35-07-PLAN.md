---
phase: 35-engineering-section-chrome
plan: 07
type: execute
wave: 3
depends_on: ["35-02", "35-03", "35-04", "35-05", "35-06"]
files_modified:
  - apps/web/components/shared/PageHeader.tsx
  - apps/web/e2e/room-board-baseline.spec.ts
autonomous: true

# NOTE: this close-out plan is NOT files_modified: [] like 33-07/34-08's pure-verification
# precedent — Pitfall 1 (35-RESEARCH.md) requires ONE small, additive, required source edit before
# the regression harness can be trusted at all: this is the FIRST phase (30-36) where a
# v2-restyled PageHeader/tab-bar sits inside the SAME full-page regression screenshot as a frozen
# board (EngineeringRoomBoard, captured at /engineering/work-orders). The harness's chromeMasks()
# currently masks only the Sidebar/Header shell landmarks (Phase 31) — it does NOT mask the
# in-page PageHeader/tab-bar region this phase intentionally restyled. Task 1 below MUST run
# BEFORE Task 2's regression re-pass, not after — running the regression suite unmodified first
# would show a false-positive failure on this phase's own correct, in-scope chrome change, wasting
# a fix-then-rerun cycle. Neither edited file is in frozen-files.json (PageHeader.tsx is the
# shared, non-frozen component; room-board-baseline.spec.ts is a test file) — both edits are
# permitted and required.

must_haves:
  truths:
    - "All standing web gates pass on the combined Phase-35 changes: type-check, check:frozen-files (zero frozen-hash / room-status drift — EngineeringRoomBoard.tsx's hash specifically unchanged), check:contrast (dark-mode WCAG AA), check:i18n-parity (+ verify:i18n-gate), and build"
    - "chromeMasks() in apps/web/e2e/room-board-baseline.spec.ts is extended to mask the in-page PageHeader/tab-bar region (via a new data-testid=\"page-header\" added to the shared, non-frozen PageHeader.tsx) BEFORE the regression harness is trusted, and the Phase-30 Room-Board regression harness then re-passes at zero pixel drift on EngineeringRoomBoard specifically (RoomDetailDrawer's known, pre-documented sub-pixel font-AA noise excepted) for both flag-off and flag-on"
    - "With the 'engineering' flag ON for the test hotel, all 4 pages (work-orders' work-orders+archived tabs, assets, pm-schedules, predictions) render the v2 redesign including their empty, loading (skeleton not spinner), and error states; the work-orders' room-board tab renders EngineeringRoomBoard completely unchanged; with the flag OFF, all 4 pages render today's UI unchanged"
    - "Each page's empty/loading/error states render correctly in both light and dark mode and both EN and ES with no console errors, no failed/forbidden API calls, no domTranslations.ts EN/ES hybrid mangling on any new PageHeader or StateBlock copy (Pitfall 3/bug-965-class check), and the same-inputs→same-outputs network diff confirms zero behavior/data change"
  artifacts:
    - path: "apps/web/components/shared/PageHeader.tsx"
      provides: "Adds a stable data-testid=\"page-header\" to the outer wrapper div — purely additive, no visual/behavioral change, reusable by Phase 36's Housekeeping chrome close-out too"
      contains: "data-testid=\"page-header\""
    - path: "apps/web/e2e/room-board-baseline.spec.ts"
      provides: "chromeMasks() extended with page.locator('[data-testid=\"page-header\"]') so the harness's diff is scoped to the frozen board's own content, not this phase's intentional chrome restyle"
      contains: "page-header"
  key_links:
    - from: "apps/web/e2e/room-board-baseline.spec.ts chromeMasks()"
      to: "apps/web/components/shared/PageHeader.tsx's data-testid"
      via: "page.locator('[data-testid=\"page-header\"]')"
      pattern: "data-testid=\\\"page-header\\\""
---

<objective>
Close Phase 35: prove the five wave-2 content plans (work-orders' 2 plans + assets + pm-schedules + predictions) combine cleanly and that `EngineeringRoomBoard` is provably untouched. FIRST (Task 1, required, not optional cleanup): add a `data-testid="page-header"` to the shared, non-frozen `PageHeader.tsx` and extend the Phase-30 regression harness's `chromeMasks()` to mask it — this phase is the first (30-36) to intentionally restyle chrome that shares a full-page regression screenshot with a frozen board, and without this mask the harness will show a false-positive failure on this phase's own correct, in-scope change. THEN (Task 2) run the full standing web gate suite and re-pass the Room-Board regression harness (flag-off AND flag-on). THEN (Task 3) live-verify all 4 pages in the browser (light+dark, EN+ES, network-inert), including a live check that none of this phase's new `StateBlock` error strings suffer the bug-965-class domTranslations.ts mangling (proactive verification per CONTEXT, since that bug class remains unfixed app-wide and this phase's own new StateBlock usages are new to the legacy translator's glossary).

This satisfies the whole-phase view of all 4 Success Criteria from ROADMAP.md: (1) PageHeader/tabs/4 pages in the new visual system, (2) EngineeringRoomBoard visually/functionally identical inside the new chrome — regression pixel-diff passes, (3) empty/loading/error states redesigned across all 4 pages, (4) dark-mode contrast, EN/ES parity, and same-inputs→same-outputs network diff all green.

Purpose: phase-close verification gate, with one required, narrowly-scoped source edit (the regression-harness mask fix) as a precondition for that gate being meaningful at all.
Output: chromeMasks() extended + data-testid added; verification evidence in the summary; green gates + regression re-pass + live per-page confirmation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/30-additive-foundation-regression-harness/FROZEN.md

# Prior-plan summaries — what was built, exact keys/props used, any documented discretion
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md
@.planning/phases/35-engineering-section-chrome/35-02-SUMMARY.md
@.planning/phases/35-engineering-section-chrome/35-03-SUMMARY.md
@.planning/phases/35-engineering-section-chrome/35-04-SUMMARY.md
@.planning/phases/35-engineering-section-chrome/35-05-SUMMARY.md
@.planning/phases/35-engineering-section-chrome/35-06-SUMMARY.md

# Prior close-out precedent (regression-harness local-standalone-build workaround, Supabase
# service-role flag-flip approach, dataI18nSkip mangling fix, bug-965 discovery)
@.planning/phases/34-management-admin-sections/34-08-SUMMARY.md

# Files this plan edits + the harness/frozen manifest
@apps/web/components/shared/PageHeader.tsx
@apps/web/e2e/room-board-baseline.spec.ts
@apps/web/playwright.regression.config.ts
@apps/web/frozen-files.json
@apps/web/frozen-files-allowlist.json
</context>

<tasks>

<task type="auto">
  <name>Task 1 (REQUIRED, run FIRST): Add data-testid to PageHeader; extend chromeMasks() before trusting any regression result</name>
  <files>apps/web/components/shared/PageHeader.tsx, apps/web/e2e/room-board-baseline.spec.ts</files>
  <action>
Do this task BEFORE Task 2. Running the regression suite unmodified first will show a false-positive failure on this phase's own correct chrome change — do not "run it, see it fail, then fix" — fix first, matching RESEARCH.md's explicit warning against wasting a cycle.

1. In `apps/web/components/shared/PageHeader.tsx`: add `data-testid="page-header"` to the component's outer wrapper `<div className={cn('pb-0', className)}>` (the top-level returned element). This is purely additive — no className, structure, or behavior change. `PageHeader.tsx` is NOT in `apps/web/frozen-files.json`, so this edit is permitted and does not require any allowlist entry.
2. In `apps/web/e2e/room-board-baseline.spec.ts`'s `chromeMasks(page)` function: add one new entry to the returned array — `page.locator('[data-testid="page-header"]')` — alongside the existing shell-landmark masks (`aside[aria-label="Main navigation"]`, `header`). Add a short comment explaining why (mirroring the existing Phase-31-fix comment block above it): this phase is the first to intentionally restyle in-page chrome that shares a full-page regression screenshot with a frozen board, and the mask keeps the diff scoped to the board's own content. Note in the comment that this mask is reusable for Phase 36's Housekeeping chrome close-out too (same situation will recur on `/housekeeping`).
3. Do not modify any other test, any other locator, the `FIXTURE_ROOM_NUMBERS`, `gotoWithTheme`, or the `ROLES`/mode loop structure.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `data-testid="page-header"` present on PageHeader.tsx's outer div; grep confirms `chromeMasks()` in room-board-baseline.spec.ts now includes `page.locator('[data-testid="page-header"]')`; grep confirms PageHeader.tsx is still absent from frozen-files.json's `files` map (unaffected by this edit); grep confirms no other line in room-board-baseline.spec.ts changed besides the one new mask entry + its explanatory comment</verify>
  <done>PageHeader.tsx has a stable data-testid; chromeMasks() masks it; both edits are minimal, additive, and applied BEFORE any regression run in this plan</done>
</task>

<task type="auto">
  <name>Task 2: Full standing gate suite + Room-Board regression re-pass on combined changes</name>
  <files></files>
  <action>
From `apps/web`, run the full standing web gate suite over the combined Phase-35 tree and confirm all green:
- `npm run type-check`
- `npm run check:frozen-files` — MUST show zero frozen-hash drift AND zero room-status value drift. Confirm `EngineeringRoomBoard.tsx`'s hash specifically is unchanged (it must not appear in any diff — none of the 6 wave-2 plans list it in their `files_modified`). Confirm the allowlist (`frozen-files-allowlist.json`) is still `entries: []` unless a wave-2 plan legitimately needed one (unlikely — none of the 7 frozen files were in any wave-2 plan's `files_modified`).
- `npm run check:contrast` — dark-mode WCAG AA over every new/extended token pairing across the 4 pages.
- `npm run check:i18n-parity` and `npm run verify:i18n-gate` — en/es parity intact after 35-01's 3 key additions and confirmation `programs.pmSchedules.*` was not migrated/renamed.
- `npm run build`.
Then re-pass the Phase-30 Room-Board regression harness (now with Task 1's mask in place): `npx playwright test --config=playwright.regression.config.ts`. Per 33-07/34-08's precedent, FIRST check whether the local tree is still ahead of `origin/main` (the harness's hardcoded default production URL may be dead) — if so, repeat the local-standalone-build workaround (`npm run build` + `.next/standalone`, a temporary CSP-localhost patch to `next.config.mjs` fully reverted afterward, `PLAYWRIGHT_BASE_URL` pointed at the local build) rather than assuming the deployed URL works; if the tree has since been pushed/deployed, verify the default URL works directly before relying on it. Flip the regression fixture tenant's `web_redesign_sections` to include `engineering` via direct Supabase service-role access (`apps/api/.env`'s `SUPABASE_SERVICE_ROLE_KEY`, matching 32-06/33-07/34-08's precedent), run the harness for BOTH flag-off and flag-on, then restore the fixture tenant's flag array back to its permanent baseline afterward.
Expect the harness to pass at zero pixel drift on `EngineeringRoomBoard` specifically for both flag states, now that Task 1's mask excludes the intentionally-restyled `PageHeader`/tab-bar region from the diff. If a residual mismatch appears anywhere OTHER than the masked region, visually inspect the diff image (per RESEARCH's Pitfall-1 "warning signs" — a diff concentrated in the room-card grid itself, not the masked header area, would indicate a REAL regression, not the expected chrome-change artifact) before concluding it's spurious. `RoomStatusBoard`/`RoomDetailDrawer` (on `/housekeeping`, untouched by this phase) should show zero drift trivially. `RoomDetailDrawer`'s known, pre-existing, deterministic sub-pixel font-AA diff (documented by 32-06/33-07/34-08) is expected environment noise, not a regression, if it recurs identically.
If any gate fails on a real defect, fix it in the owning wave-2 file (blocking Rule-1 fix — this un-empties files_modified for that fix only) and re-run until green; log the fix to `.wolf/buglog.json`.
  </action>
  <verify>All of type-check / check:frozen-files / check:contrast / check:i18n-parity / verify:i18n-gate / build exit 0; check:frozen-files explicitly confirms EngineeringRoomBoard.tsx (and all 6 other frozen files) unchanged + allowlist empty (or justified); Room-Board regression suite reports zero-drift on EngineeringRoomBoard for both flag-off and flag-on (RoomDetailDrawer's documented sub-pixel noise excepted), with any residual diff visually confirmed to be within the newly-masked PageHeader region (not the frozen board's own content) before being dismissed; any defect found is fixed and re-verified green; regression fixture tenant restored to baseline afterward</verify>
  <done>Every standing web gate green on the combined Phase-35 changes; Room-Board regression re-passes at zero drift on EngineeringRoomBoard (both flag states) now that the chrome mask is in place; zero frozen-file / room-status drift across all six wave-2 content plans; fixture tenant restored</done>
</task>

<task type="auto">
  <name>Task 3: Live flag-on/flag-off browser self-verification of all 4 pages (light+dark, EN+ES, network-inert, i18n-mangling check)</name>
  <files></files>
  <action>
Per the mandatory Self-Verification Policy (CLAUDE.md): start `npm run dev:web` (:3000) and `npm run dev:api` (:8000) if not running. Enable the `engineering` flag for the test hotel by adding it to its `web_redesign_sections` array (via `hotelsApi.update` PATCH /v1/hotels/{id} or direct Supabase service-role access, matching Phase 31-34's approach; there is no settings-UI toggle).

Verify each of the 4 pages:
- FLAG ON: work-orders' `work-orders` tab (5-column Kanban, each column independently Skeleton-loading and StateBlock-erroring with correct per-column retry — force at least one column's query to fail via devtools network throttling/blocking and confirm ONLY that column shows an error, others keep working), `archived` tab (Skeleton/StateBlock/EmptyState), and `room-board` tab (confirm `EngineeringRoomBoard` renders EXACTLY as it did before this phase — same layout, same data, zero visual difference, since it's frozen and untouched). Assets' table (Skeleton rows, StateBlock error via forced failure, rich empty state with 3 sample chips intact). PM Schedules' table+mobile-cards (Skeleton, StateBlock error via `programs.pmSchedules.*` copy, EmptyState). Predictions' list (SkeletonCard, NEW StateBlock error via forced failure — confirm this is a genuinely new state that didn't exist before this phase, and its rich empty state with sample chips/clear-filters/go-to-asset-register intact). Confirm `FailurePredictionSidebar` (visible on the work-orders tab) and `WorkOrderDetailDrawer` (open any work order) show their new error/notice states when forced to fail, and that the drawer's basic-info fallback still works (open a work order, block the detail-fetch request, confirm the drawer still shows the Kanban card's basic fields via the `fullWo` fallback, plus the new non-blocking notice). Confirm `CreateWorkOrderModal`'s room dropdown and `BulkArchiveModal`'s candidate list each show a skeleton then either their data or a generic StateBlock error+retry when the underlying request is forced to fail.
- FLAG OFF: spot-check all 4 pages render today's UI unchanged (legacy branch) — including confirming Predictions still shows NO error UI at all (matching its pre-phase behavior) and work-orders' Kanban columns show no per-column error UI.
- LIGHT + DARK: toggle theme; confirm no contrast/legibility break across all 4 pages and their sub-components (KanbanColumn, ArchivedWorkOrdersPanel, FailurePredictionSidebar, WorkOrderDetailDrawer, CreateWorkOrderModal, BulkArchiveModal, PMCompletionModal, AssetDetailModal).
- EN + ES: toggle locale; confirm all touched PageHeader title/subtitle render correctly with no missing-key fallback and no domTranslations.ts EN/ES hybrid mangling (Pitfall 3, the bug-962/963/964 defect class). CRITICALLY, per CONTEXT's explicit instruction: live-check this phase's THREE NEW StateBlock error strings specifically (predictionsPage.loadError, failurePrediction.loadError, workOrderDetail.loadError) in ES — since bug-965 (flagged, unfixed, deferred by 34-08) suspects StateBlock error text has the SAME domTranslations.ts vulnerability as PageHeader but has never been proven. If any of these 3 new strings render as an EN/ES hybrid under the full-app DOM (not just in isolation), this is a REAL, newly-discovered defect for THIS phase (not the deferred bug-965 itself, but a live instance of its class) — apply a `data-i18n-skip` mitigation if `StateBlock.tsx` can be given one via a small additive prop change (mirroring `PageHeader`'s existing `dataI18nSkip` mechanism), fix it as a blocking Rule-1 defect, log to `.wolf/buglog.json`, and re-verify. If clean, explicitly record that this phase's new StateBlock strings were checked and found NOT mangled (useful confirming/disconfirming evidence for whichever future phase tackles bug-965 app-wide).
- NETWORK DIFF (Success Criterion #4): with devtools Network open, spot-check work-orders, assets, and predictions that the SAME requests fire flag-on vs flag-off (no new/removed endpoints, aside from any query that was already being made but previously had no isError branch — the request itself is unchanged, only what renders on failure changed). Confirm no mutation (archive/create/complete/acknowledge/authorize) is accidentally auto-triggered by any new v2 rendering path. Confirm no failed/forbidden (401/403) calls.
- No console errors on any page.
Record exactly what was live-verified vs code-traced, and any follow-up flagged for the orchestrator (e.g. flag-on regression not runnable this session, roles not seeded, deferred items collected from the six wave-2 summaries).
  </action>
  <verify>Screenshots/notes captured for all 4 pages (+ work-orders' 3 tabs, + the 4 sub-components: FailurePredictionSidebar/WorkOrderDetailDrawer/CreateWorkOrderModal/BulkArchiveModal, + PMCompletionModal/AssetDetailModal) flag-on and a flag-off spot check on all 4; empty/loading/error observed for each (with a forced per-column error on work-orders confirming ONLY that column shows an error and retry re-fires the CORRECT column's query); EngineeringRoomBoard confirmed visually/functionally identical to pre-phase; WorkOrderDetailDrawer's fullWo fallback confirmed still working on a forced detail-fetch failure; light/dark confirmed with no contrast break; EN/ES confirmed with no missing-key fallback and no domTranslations.ts mangling on any touched PageHeader OR on the 3 new StateBlock strings specifically (explicit pass/fail recorded for the bug-965-class check); network diff spot-checked inert on ≥3 pages including confirming no mutation auto-fires; zero console errors and zero forbidden API calls in checked pages; any un-verifiable item explicitly flagged for the orchestrator</verify>
  <done>All 4 pages (+ work-orders' 3 tabs and all 6 non-frozen sub-components) live-confirmed to render the v2 redesign flag-on and legacy flag-off, with empty/loading/error states in light+dark and EN+ES, network-inert, no console errors or forbidden calls, no i18n DOM-mangling on any new PageHeader OR StateBlock content (bug-965-class check explicitly resolved pass/fail for this phase's 3 new strings); EngineeringRoomBoard confirmed pixel/functionally identical; coverage gaps + deferred items flagged precisely</done>
</task>

</tasks>

<verification>
- Task 1's PageHeader data-testid + chromeMasks() extension is in place BEFORE Task 2's regression run (verified by task order and by the regression re-pass showing zero drift attributable to the intentional chrome change).
- Full standing web gate suite green on combined changes (type-check, check:frozen-files, check:contrast, check:i18n-parity, verify:i18n-gate, build).
- check:frozen-files confirms zero drift on all 7 frozen files (EngineeringRoomBoard.tsx specifically); Room-Board regression re-passes at zero drift on EngineeringRoomBoard (both flag states, RoomDetailDrawer's known noise excepted).
- All 4 pages + work-orders' 3 tabs + 6 non-frozen sub-components live-verified flag-on (v2) and flag-off (legacy, all 4 spot-checked); empty/loading/error render correctly (skeleton not spinner); light+dark and EN+ES confirmed with no domTranslations.ts mangling on PageHeader OR the 3 new StateBlock strings; network diff inert with no accidental mutation triggers; no console errors / forbidden calls.
</verification>

<success_criteria>
Phase 35 verified end-to-end: all 4 ROADMAP Success Criteria confirmed against the running app and the gate suite — Engineering's PageHeader/tabs/4 pages render in the new visual system with redesigned empty/loading/error states, EngineeringRoomBoard is provably pixel-identical and functionally unchanged (regression harness passes with the newly-required chrome mask in place), and the section passes dark-mode contrast, EN/ES parity, and same-inputs→same-outputs network diff. Any residual coverage gap, deferred item, or newly-discovered bug-965-class defect is precisely flagged for the orchestrator's close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-07-SUMMARY.md` recording: the exact gate results, confirmation Task 1's mask fix was applied BEFORE the regression run (not as a reactive fix), the regression outcome (and whether flag-on was runnable this session, and which URL-reachability path was used), a per-page/per-sub-component table of live-verified vs code-traced, the state-verification evidence (incl. the forced-per-column-error/retry-targets-correct-column check on work-orders, the EngineeringRoomBoard pixel/functional-identity confirmation, and the network-diff spot-check), the EXPLICIT pass/fail result of the bug-965-class check on this phase's 3 new StateBlock strings (this is new evidence for whichever future phase tackles bug-965 app-wide — record it precisely even if the result is "clean, not mangled"), confirmation of zero frozen-file drift, any defect found+fixed (with buglog id), and a consolidated list of all deferred items gathered from the six wave-2 summaries plus any follow-up left for the orchestrator (mirroring 33-07/34-08's close-out format). Also update `.planning/STATE.md`'s Current Position to reflect Phase 35 closed and Phase 36 next.
</output>
