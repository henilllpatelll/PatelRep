---
phase: 35-engineering-section-chrome
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - apps/web/app/(dashboard)/engineering/assets/page.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of assets/page.tsx. No sibling wave-2 plan
# touches it. Assets is ALREADY the most state-complete of the 4 pages (it already destructures
# isError and has a working error+retry block) — this plan's job is narrower than a ground-up
# migration: convert the existing bespoke error/loading/empty markup to the shared
# Skeleton/StateBlock primitives, and v2-token-ify the empty state's typography, all behind the
# flag. `AssetDetailModal` is a LOCAL function component defined inline in this same file (no
# separate file exists) — its own loading/error gap is in scope too. Does NOT edit en.ts/es.ts —
# reuses the EXISTING `engineering.assetsPage.loadError`/`retry` keys (already present; no new key
# needed since this page already has working error copy).

must_haves:
  truths:
    - "With the 'engineering' flag ON, the assets table's loading state uses the shared Skeleton component (re-skinning the existing SkeletonRow's gray-100 placeholders to v2 tokens) and the error state uses StateBlock (reusing the existing engineering.assetsPage.loadError/retry keys, retry wired to the existing queryClient.invalidateQueries mechanism); flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, the empty-state table cell (both the zero-assets-at-all case and the no-matches-filtered case) uses v2-token typography consistent with EmptyState's visual language, while its richer information architecture (3 sample chips, the Add-Asset action) is preserved as-is — this state stays structurally bespoke (it lives inside a <td>, where EmptyState's own layout does not fit) rather than being literally wrapped in the <EmptyState> component"
    - "With the 'engineering' flag ON, AssetDetailModal's detail-fetch query's loading spinner is replaced with a v2 skeleton (skeleton-not-spinner convention); its query gains no new error handling beyond what's needed to avoid a raw spinner, since a full error redesign of the detail modal is out of this phase's scope"
    - "No query key/params, no RBAC (canEdit) logic, and no create/edit mutation behavior changed"
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/assets/page.tsx"
      provides: "Reads isSectionRedesigned('engineering', hotel) once; table loading uses Skeleton, table error uses StateBlock (existing keys), empty-state typography v2-token-ified while preserving its rich structure; AssetDetailModal's loading spinner replaced with a skeleton; legacy branch unchanged"
      contains: "isSectionRedesigned('engineering'"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/assets/page.tsx (assetsData query)"
      to: "StateBlock error"
      via: "error.onRetry = () => queryClient.invalidateQueries({ queryKey: ['assets'] }) — the EXISTING retry mechanism, not a new one"
      pattern: "invalidateQueries"
---

<objective>
Redesign assets/page.tsx's chrome — its `PageHeader`, the assets table's loading/error/empty states, and the inline `AssetDetailModal`'s loading treatment — behind the `engineering` flag. Assets is the phase's most state-complete page (already has `isError` + a working retry-via-`invalidateQueries` error block and rich `emptyHeading`/`emptyHelp`/3-sample-chip empty copy) — this plan's job is conversion (bespoke markup → shared `Skeleton`/`StateBlock` primitives, reusing existing i18n keys) and typography-level v2-token-ification of the empty state, NOT inventing new states or new copy. The empty state (Open Question 2 from RESEARCH.md, resolved here): since it renders inside a `<td colSpan={7}>` and carries materially more information architecture (3 sample-asset chips, a conditional Add-Asset CTA) than `EmptyState`'s `icon+title+body+action` shape accommodates cleanly inside a table cell, it stays structurally bespoke — only its title/body typography classes are aligned to `EmptyState`'s visual language (font-size/weight/color tokens), not literally re-parented into the `<EmptyState>` component.

Purpose: ENG-01 for the Assets page.
Output: assets/page.tsx, flag-gated, Skeleton/StateBlock-wired for the table, empty-state typography v2-token-ified, AssetDetailModal's spinner replaced with a skeleton.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md

@apps/web/app/(dashboard)/engineering/assets/page.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag; table loading→Skeleton, error→StateBlock (reuse existing keys/retry)</name>
  <files>apps/web/app/(dashboard)/engineering/assets/page.tsx</files>
  <action>
1. In the page's default-exported component: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`. Add `dataI18nSkip={v2}` to the page's `PageHeader` call.
2. Error block (currently at the top of the `Card` wrapping the table, `isError ? <div className="flex flex-col items-center justify-center py-16 text-center p-6">...` with a raw `AlertTriangle`, `t('engineering.assetsPage.loadError')`, and a retry `Button` calling `queryClient.invalidateQueries({ queryKey: ['assets'] })`): under `v2`, replace this whole block with `<StateBlock status="error" error={{ message: t('engineering.assetsPage.loadError'), onRetry: () => queryClient.invalidateQueries({ queryKey: ['assets'] }) }} />` — SAME existing key, SAME existing retry mechanism (do not switch to `.refetch()` — this page's established pattern is `invalidateQueries`, per RESEARCH.md's note that both patterns are valid and should be preserved as-is per page). Under `!v2`, keep the current raw markup byte-unchanged.
3. Loading rows (`SkeletonRow` function component, currently 7 `<td>`s each with a raw `<div className="h-4 bg-gray-100 rounded ...">`): under `v2`, replace `SkeletonRow`'s per-cell placeholders with the shared `Skeleton` component (`<Skeleton variant="text" className="h-4 w-3/4" />` etc., matching each cell's current width classes) — keep the `<tr>`/`<td>` structure and column count identical, only swap the inner placeholder div for `Skeleton`. Under `!v2`, keep `SkeletonRow` rendering its current raw `bg-gray-100` markup unchanged (i.e. `SkeletonRow` itself should branch internally on `v2`, or the table body's `isLoading` branch should choose between a v2-styled and legacy-styled row renderer — pick whichever keeps the diff smallest; document the choice in the summary).
4. Do not change `assetsData`/`assets`/`filtered` derivation, the search/filter state, or the `isError`/`isLoading` destructuring itself (already correct — `const { data: assetsData, isLoading, isError } = useQuery({...})`, no change needed here).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms `dataI18nSkip={v2}` on PageHeader; grep confirms `StateBlock` used for the error path under `v2`, still calling `queryClient.invalidateQueries({ queryKey: ['assets'] })`; grep confirms `Skeleton` used in the v2 loading-row path; grep confirms the legacy (`!v2`) error block and `SkeletonRow` raw markup are still present, byte-unchanged, for the `!v2` path; git status shows only assets/page.tsx changed</verify>
  <done>Assets page reads the flag once; PageHeader gets dataI18nSkip; table error uses StateBlock (existing loadError key, existing invalidateQueries retry); table loading uses Skeleton under v2; legacy branch renders exactly as before this task; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Empty-state typography v2-token-ify (preserve rich structure); AssetDetailModal spinner→skeleton</name>
  <files>apps/web/app/(dashboard)/engineering/assets/page.tsx</files>
  <action>
1. Table empty state (inside `<td colSpan={7}>`, two sub-cases: `assets.length === 0` shows the rich `emptyHeading`/`emptyHelp`/3-sample-chip/Add-Asset block; otherwise shows the plain `noMatchFilters` text): under `v2`, keep the EXACT same structure (both sub-cases, same conditions, same 3 sample-chip loop, same conditional Add-Asset button) but replace the raw `text-gray-700`/`text-gray-400` typography classes on the heading/body `<p>` tags with `EmptyState`-equivalent typography tokens (`text-ink` for the title-weight text, `text-ink3` for the body/help text — matching `EmptyState.tsx`'s own `text-[14px] font-medium text-ink` / `text-[13px] leading-relaxed text-ink3` classes exactly, for visual consistency with every other redesigned empty state in the app, without physically wrapping this content in the `<EmptyState>` component since it lives inside a `<td>` and carries more structure than that component's props accommodate). Under `!v2`, keep the current raw `gray-*` classes unchanged.
2. `AssetDetailModal` (local component, ~line 119): its own `useQuery(['asset', assetId], ...)` destructures `{ data, isLoading }` (no `isError` — leave that way, this modal is out of scope for a full error-state addition per CONTEXT's narrow-scope mandate for this phase; only the loading treatment is in scope). Currently, `isLoading` gates a raw `<Loader2 size={24} className="animate-spin text-gray-400" />` (~line 225). Under `v2` (thread this same `v2` boolean down into `AssetDetailModal` as a new prop, since it's a local component in this same file — simple prop, not a cross-file/cross-plan contract issue), replace this spinner with a `Skeleton`-based placeholder (skeleton-not-spinner convention, matching every other loading state this phase touches) sized to roughly the space the detail panel's header/fields occupy. Under `!v2`, keep the `Loader2` spinner unchanged.
3. Do not touch `editMode`/`editFields`/`saving`/`saveError` state, the save mutation, or any of the detail panel's field rendering once `data` is loaded.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms the empty-state title/body classes under `v2` use `text-ink`/`text-ink3` (not `gray-700`/`gray-400`) while the sample-chip loop and Add-Asset button JSX structure is otherwise unchanged; grep confirms `AssetDetailModal` now accepts and uses a `v2` prop for its loading state; grep confirms `Skeleton` used in place of `Loader2`/`animate-spin` under `v2` in `AssetDetailModal`; grep confirms the legacy (`!v2`) empty-state classes and `AssetDetailModal`'s spinner are still present, byte-unchanged; git status shows only assets/page.tsx changed (combined with Task 1)</verify>
  <done>Empty-state typography is v2-token-consistent under v2 while its rich structure (chips, Add-Asset action, both sub-cases) is fully preserved; AssetDetailModal's loading spinner is replaced with a skeleton under v2; legacy branch (v2 false) unchanged in both; no field/save/mutation logic touched; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- Flag read once; PageHeader dataI18nSkip'd; table error/loading use StateBlock/Skeleton with EXISTING keys and EXISTING invalidateQueries retry mechanism; empty state's rich structure preserved, only typography tokens updated; AssetDetailModal's spinner replaced with a skeleton under v2.
- Legacy (`!v2`) branch renders byte-identical to pre-phase markup throughout.
- Only assets/page.tsx changed; no frozen file, no locale file, no query/mutation/RBAC logic changed.
</verification>

<success_criteria>
Assets renders its table's loading/error states and PageHeader in the new visual system behind the engineering flag, using the shared Skeleton/StateBlock primitives wired to its EXISTING i18n keys and retry mechanism (no new copy needed — this page already had working error handling), while its unusually rich empty state keeps its full information architecture with only typography brought in line with the shared visual language — zero behavior/data/RBAC change.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-04-SUMMARY.md` recording: confirmation the existing `engineering.assetsPage.loadError`/`retry` keys and `invalidateQueries` retry mechanism were reused verbatim (no new key, no new retry pattern), the exact empty-state typography-token decision made (documenting the "stays bespoke, not literally wrapped in EmptyState" call per RESEARCH's Open Question 2), confirmation AssetDetailModal's field/save logic is untouched, and any deferred item for the close-out list.
</output>
