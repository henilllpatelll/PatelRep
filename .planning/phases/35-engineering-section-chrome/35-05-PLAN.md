---
phase: 35-engineering-section-chrome
plan: 05
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx
  - apps/web/components/engineering/PMCompletionModal.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of these 2 files. No sibling wave-2 plan
# touches either. PMCompletionModal.tsx is used exclusively by pm-schedules/page.tsx (grep-
# confirmed: no other importer), so bundling it with the page here (rather than a separate plan)
# keeps ownership cohesive. Does NOT edit en.ts/es.ts — this page DELIBERATELY reuses the
# pre-existing `programs.pmSchedules.*` namespace (do NOT migrate to a new
# `engineering.pmSchedulesPage.*` namespace — locked decision, Deferred Ideas in 35-CONTEXT.md;
# migrating risks breaking the Programs section's own already-shipped Phase-33-verified usage of
# these exact same keys). `programs.pmSchedules.*` already has `failedToLoad`/`retry`/
# `noSchedules`/`noSchedulesHelp`/`createSchedule` — everything needed already exists, zero new
# keys required for this plan.

must_haves:
  truths:
    - "With the 'engineering' flag ON, pm-schedules/page.tsx's table loading state uses the shared Skeleton component and its already-working error state (isError already destructured today) uses StateBlock, both reusing the EXISTING programs.pmSchedules.failedToLoad/retry keys and the EXISTING invalidateQueries retry mechanism; flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, the mobile-card empty state (md:hidden branch) uses the shared EmptyState component reusing the EXISTING programs.pmSchedules.noSchedules/noSchedulesHelp/createSchedule keys; flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, PMCompletionModal's two previously-unhandled queries (overviewData, staffData) gain a minimal loading treatment using the shared Skeleton component; neither query's absence of error handling is expanded beyond what's needed to avoid a raw unhandled-loading gap, per the modal-treatment scope boundary"
    - "No key name in programs.pmSchedules.* is renamed or migrated; no query key/params, no RBAC (canEdit) logic, and no schedule-completion mutation behavior changed"
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx"
      provides: "Reads isSectionRedesigned('engineering', hotel) once; table loading uses Skeleton, table error uses StateBlock (existing programs.pmSchedules keys), mobile empty state uses EmptyState (existing keys); legacy branch unchanged"
      contains: "isSectionRedesigned('engineering'"
    - path: "apps/web/components/engineering/PMCompletionModal.tsx"
      provides: "Self-reads the flag; overviewData/staffData queries gain a v2 loading skeleton; completion mutation unchanged"
      contains: "isSectionRedesigned('engineering'"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx (schedulesData query)"
      to: "StateBlock error"
      via: "error.onRetry = () => queryClient.invalidateQueries({ queryKey: ['pm-schedules'] }) — the EXISTING retry mechanism"
      pattern: "invalidateQueries"
---

<objective>
Redesign pm-schedules/page.tsx's chrome — its `PageHeader`, table loading/error states, and mobile-card empty state — plus `PMCompletionModal`'s two currently-unhandled supporting queries, behind the `engineering` flag. PM Schedules is (with Assets) one of only two Engineering pages that already has working `isError` + retry-via-`invalidateQueries` error handling — this plan converts that bespoke markup to the shared `Skeleton`/`StateBlock`/`EmptyState` primitives while deliberately preserving its existing `programs.pmSchedules.*` i18n namespace UNCHANGED (do not migrate to `engineering.pmSchedulesPage.*` — this is a locked decision from CONTEXT: that namespace is also used by the already-shipped Programs section's own PM view, and migrating risks breaking that unrelated, working usage for zero user-facing benefit).

Purpose: ENG-01 for the PM Schedules page.
Output: pm-schedules/page.tsx and PMCompletionModal.tsx, flag-gated, Skeleton/StateBlock/EmptyState-wired using the EXISTING programs.pmSchedules.* keys, zero namespace migration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md

@apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx
@apps/web/components/engineering/PMCompletionModal.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag; table loading→Skeleton, error→StateBlock, mobile-empty→EmptyState (all reusing existing programs.pmSchedules.* keys)</name>
  <files>apps/web/app/(dashboard)/engineering/pm-schedules/page.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`. Add `dataI18nSkip={v2}` to the `PageHeader` call (`title={t('programs.pmSchedules.title')}`, `subtitle={t('programs.pmSchedules.subtitle')}` — pre-existing, non-namespace-new keys, but set proactively per this phase's established practice).
2. Error block (`isError ? <div className="flex flex-col items-center justify-center py-16 text-center p-6">` with raw `AlertTriangle`, `t('programs.pmSchedules.failedToLoad')`, retry `Button` calling `queryClient.invalidateQueries({ queryKey: ['pm-schedules'] })`): under `v2`, replace with `<StateBlock status="error" error={{ message: t('programs.pmSchedules.failedToLoad'), onRetry: () => queryClient.invalidateQueries({ queryKey: ['pm-schedules'] }) }} />` — SAME existing key, SAME existing retry mechanism. Under `!v2`, keep the current raw markup unchanged.
3. Mobile-card loading (`md:hidden` branch, `isLoading ? <div className="p-4 space-y-3">{Array.from({length:4}).map(...<div className="h-32 animate-pulse rounded-xl bg-gray-100" />)}</div>`): under `v2`, replace each placeholder with `<Skeleton variant="card" className="h-32" />`. Under `!v2`, keep unchanged.
4. Mobile-card empty state (`schedules.length === 0` branch, currently a bespoke icon-circle + `noSchedules`/`noSchedulesHelp` text + conditional `createSchedule` button): under `v2`, replace with `<EmptyState icon={<Calendar size={22} />} title={t('programs.pmSchedules.noSchedules')} body={t('programs.pmSchedules.noSchedulesHelp')} action={canEdit ? <Button variant="primary" onClick={() => setShowCreateModal(true)}><Plus size={14} />{t('programs.pmSchedules.createSchedule')}</Button> : undefined} />` — SAME existing keys, same conditional action. Under `!v2`, keep the current bespoke markup unchanged. (The DESKTOP table's own empty-row rendering, if visually distinct from the mobile card empty state, should receive the equivalent EmptyState treatment under `v2` using the same keys — inspect the live file for whether desktop has a separate empty-row block beyond the mobile-only one shown here, and apply the same conversion if so.)
5. Do NOT rename, move, or duplicate any `programs.pmSchedules.*` key — this plan is READ-ONLY on the locale files. Do not change `schedulesData`/`schedules`/`activeSchedules`/`dueThisWeekCount`/`overdueCount` derivation or the `isError`/`isLoading` destructuring itself (already correct).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms `dataI18nSkip={v2}` on PageHeader; grep confirms `StateBlock` used for the error path, still calling `queryClient.invalidateQueries({ queryKey: ['pm-schedules'] })`; grep confirms `Skeleton` used for mobile-card loading and `EmptyState` used for the empty state, BOTH still referencing `programs.pmSchedules.*` keys (not a new `engineering.pmSchedulesPage.*` namespace — explicitly confirm no such namespace appears anywhere in the diff); grep confirms the legacy (`!v2`) markup is still present, byte-unchanged; git status shows only pm-schedules/page.tsx changed</verify>
  <done>PM Schedules page reads the flag once; PageHeader dataI18nSkip'd; table error uses StateBlock, mobile loading uses Skeleton, mobile empty uses EmptyState — all reusing the EXISTING programs.pmSchedules.* keys and EXISTING invalidateQueries retry, zero namespace migration; legacy branch unchanged; gates green</done>
</task>

<task type="auto">
  <name>Task 2: PMCompletionModal — flag, v2 skeleton for its 2 unhandled supporting queries</name>
  <files>apps/web/components/engineering/PMCompletionModal.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`.
2. `overviewData` and `staffData` (two separate `useQuery` calls, both currently destructuring ONLY `{ data }`, no `isLoading`/`isError` at all — genuine gap): extend each to also destructure `isLoading` (do NOT add `isError` handling for either — this modal's scope per CONTEXT is narrow gap-closing, not a full error-state redesign; a full error treatment for a completion-confirmation modal's supporting data is out of this phase's boundary).
3. Under `v2`, wherever `overviewData`/`staffData` currently render immediately with no loading guard (i.e. whatever content in this 630-line modal depends on either value before it may have arrived), add a `Skeleton`-based loading placeholder gated on the relevant `isLoading` flag, sized to roughly the space that content occupies once loaded. Under `!v2`, render exactly as today (no loading guard, immediate render — matching current behavior, including any current pre-load flash/empty-value rendering, unchanged).
4. Do not touch the completion-submission mutation, its success/close behavior, or any of the modal's form fields (notes/labor-hours/parts-used equivalents, if present in this modal beyond what's already covered by the `pm-schedules` page's own `completeSuccessMessage`/`woCreatedMessage` keys).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms both `overviewData` and `staffData` queries now destructure `isLoading`; grep confirms NO `isError` destructured or referenced for either query (deliberate scope limit); grep confirms `Skeleton` used under `v2` gated on the new `isLoading` flags; git status shows only PMCompletionModal.tsx changed</verify>
  <done>PMCompletionModal reads the flag itself; overviewData/staffData queries gain isLoading (not isError) destructuring; a v2 Skeleton placeholder renders while either is loading; completion mutation and form fields unchanged; !v2 branch unchanged; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- pm-schedules/page.tsx: flag read once; table error/loading/empty all StateBlock/Skeleton/EmptyState-wired using EXISTING `programs.pmSchedules.*` keys and EXISTING invalidateQueries retry — zero namespace migration, zero new key.
- PMCompletionModal.tsx: self-reads the flag; overviewData/staffData gain isLoading-gated Skeleton loading only (no new isError handling).
- Legacy (`!v2`) branch renders byte-identical to pre-phase markup throughout both files.
- Only these 2 files changed; no frozen file, no locale file, no query/mutation/RBAC logic changed.
</verification>

<success_criteria>
PM Schedules renders its table's loading/error/empty states and PageHeader in the new visual system behind the engineering flag, reusing its existing `programs.pmSchedules.*` copy and retry mechanism verbatim (zero namespace migration, honoring the locked CONTEXT decision), while PMCompletionModal's two previously-unhandled supporting queries gain a minimal loading treatment — zero behavior/data/RBAC change.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-05-SUMMARY.md` recording: explicit confirmation NO new `engineering.pmSchedulesPage.*` namespace was created and NO `programs.pmSchedules.*` key was renamed/moved (the locked decision held), confirmation the existing `invalidateQueries` retry mechanism was reused verbatim, the isLoading-only (no isError) scope decision for PMCompletionModal's 2 queries, and any deferred item for the close-out list.
</output>
