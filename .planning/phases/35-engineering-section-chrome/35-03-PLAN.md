---
phase: 35-engineering-section-chrome
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - apps/web/components/engineering/FailurePredictionSidebar.tsx
  - apps/web/components/engineering/WorkOrderDetailDrawer.tsx
  - apps/web/components/engineering/CreateWorkOrderModal.tsx
  - apps/web/components/engineering/BulkArchiveModal.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of these 4 files. No sibling wave-2 plan
# touches any of them. None of these 4 is in frozen-files.json — treat with the CONTEXT-mandated
# "close a genuine gap only, don't do a ground-up rewrite" discipline (same class as Phase 34's
# Staff modals), NOT a full redesign of every field/section inside them.
#
# CROSS-PLAN CONTRACT (read before editing): all 4 of these components are also rendered from
# work-orders/page.tsx, which is owned by sibling wave-2 plan 35-02 (running in parallel this same
# wave). Per 35-02's own file-ownership note, it does NOT pass a `v2`/`redesigned` prop into any of
# these 4 call sites — each component in THIS plan reads `isSectionRedesigned('engineering',
# hotel)` ITSELF, internally, via `useHotelStore`. This decouples the two plans so they can run in
# parallel without agreeing on a prop-name contract. Do not add a prop for the flag to any of these
# 4 components' public interfaces — self-read only.
#
# Does NOT edit en.ts/es.ts. Reuses ONE new key from 35-01 (`engineering.workOrderDetail.
# loadError`, for WorkOrderDetailDrawer only) plus `engineering.failurePrediction.loadError`
# (also new from 35-01, for FailurePredictionSidebar). CreateWorkOrderModal and BulkArchiveModal's
# gap-fixes reuse StateBlock's own BUILT-IN default copy (`common.error`/`common.retry`/
# `common.loading`, used automatically when no explicit `message`/`loadingLabel` is passed to
# StateBlock) — no new key needed for either modal.

must_haves:
  truths:
    - "With the 'engineering' flag ON, FailurePredictionSidebar shows a v2 skeleton while loading (already close to this via SkeletonItem — re-skinned, not rebuilt) and a StateBlock error with retry on failure (a genuinely new state — the query currently has no error handling at all); flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, WorkOrderDetailDrawer surfaces a small, non-blocking notice when its detail-enrichment query fails, WITHOUT removing the existing graceful fallback (fullWo = woDetail?.data ?? wo) that lets the drawer keep functioning with the basic wo data already passed in; flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, CreateWorkOrderModal's room-list query and BulkArchiveModal's archive-candidates query each gain a minimal loading/error treatment (StateBlock, using its own built-in default copy) where today there is none at all; neither modal's create/archive mutation behavior changes"
    - "No query key, mutation, or RBAC logic changed in any of the 4 files; each component reads the flag itself (no new prop threaded from work-orders/page.tsx)"
  artifacts:
    - path: "apps/web/components/engineering/FailurePredictionSidebar.tsx"
      provides: "Self-reads isSectionRedesigned('engineering', hotel); v2 Skeleton loading + new StateBlock error with retry; legacy branch unchanged"
      contains: "isSectionRedesigned('engineering'"
    - path: "apps/web/components/engineering/WorkOrderDetailDrawer.tsx"
      provides: "Self-reads the flag; adds isError to the woDetail query; v2 non-blocking partial-load-error notice wired to refetchDetail(); existing wo-fallback rendering preserved exactly"
      contains: "isSectionRedesigned('engineering'"
    - path: "apps/web/components/engineering/CreateWorkOrderModal.tsx"
      provides: "Self-reads the flag; v2 minimal loading/error treatment for the rooms-list query; create-work-order flow unchanged"
      contains: "isSectionRedesigned('engineering'"
    - path: "apps/web/components/engineering/BulkArchiveModal.tsx"
      provides: "Self-reads the flag; v2 minimal loading/error treatment for the archive-candidates query; archive mutations unchanged"
      contains: "isSectionRedesigned('engineering'"
  key_links:
    - from: "apps/web/components/engineering/FailurePredictionSidebar.tsx"
      to: "its own useQuery(['failure-predictions'])"
      via: "isError destructured (currently absent); StateBlock error.onRetry = () => refetch()"
      pattern: "isError"
    - from: "apps/web/components/engineering/WorkOrderDetailDrawer.tsx"
      to: "its woDetail useQuery"
      via: "isError: detailError destructured; non-blocking banner onRetry = refetchDetail (already destructured today)"
      pattern: "detailError"
---

<objective>
Close the specific, genuine loading/error gaps in Engineering's 4 non-frozen work-orders sub-components — `FailurePredictionSidebar` (query has zero error handling), `WorkOrderDetailDrawer` (its enrichment query has zero error handling, though it already gracefully degrades to the basic `wo` prop), `CreateWorkOrderModal` (its rooms-list query has zero loading/error handling), and `BulkArchiveModal` (its archive-candidates query has zero loading/error handling) — with the same narrow, gap-closing-only discipline Phase 34 applied to Staff's modals. Each component reads `isSectionRedesigned('engineering', hotel)` itself (no prop from the parent page, per the cross-plan decoupling contract with sibling plan 35-02). Full ground-up redesign of any of these 4 components' business logic, forms, or field layout is explicitly OUT of scope per CONTEXT — only the identified loading/error gaps and light v2-token chrome polish are in scope.

Purpose: ENG-01's non-happy-path coverage for work-orders' sidebar/modals/drawer, narrowest-possible scope per CONTEXT's explicit modal-treatment boundary.
Output: 4 files, each self-flag-gated, with their one identified genuine state gap closed via shared StateBlock/Skeleton primitives.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md

@apps/web/components/engineering/FailurePredictionSidebar.tsx
@apps/web/components/engineering/WorkOrderDetailDrawer.tsx
@apps/web/components/engineering/CreateWorkOrderModal.tsx
@apps/web/components/engineering/BulkArchiveModal.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FailurePredictionSidebar — flag, v2 skeleton, new StateBlock error</name>
  <files>apps/web/components/engineering/FailurePredictionSidebar.tsx</files>
  <action>
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`.
2. Extend the `useQuery(['failure-predictions'], ...)` destructuring from `{ data: predictions, isLoading }` to `{ data: predictions, isLoading, isError, refetch }` (currently absent — genuine gap).
3. Under `v2`, add a THIRD branch to the existing `isLoading ? ... : items.length === 0 ? ... : ...` conditional (currently only loading/empty/data — no error branch at all): when `isError`, render `<StateBlock status="error" error={{ message: t('engineering.failurePrediction.loadError'), onRetry: () => refetch() }} className="py-6" />` in place of the loading/empty/data content (i.e. check `isError` before the existing `isLoading` check, or combine so error takes precedence once loading completes).
4. Under `v2`, the existing `SkeletonItem` (already token-styled: `bg-surface-2`, `animate-pulse`, `border-line-2`) can stay as a bespoke local component OR be swapped for the shared `Skeleton` — since it's already visually consistent with the v2 system (not the raw-gray-100 class of gap this phase targets elsewhere), leave `SkeletonItem` as-is; do not spend effort re-implementing an already-correct skeleton with the shared primitive purely for the sake of using it. Only the missing ERROR branch is the genuine gap here.
5. Under `!v2`: render exactly as today — `isError`/`refetch` simply unused, the existing loading/empty/data 3-way conditional unchanged.
6. Do not change `acknowledgeMutation`, `createWOMutation`, `PredictionCard`, or the `hasHighRisk` styling logic on the outer `Card`.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms `useQuery` destructuring now includes `isError`/`refetch`; grep confirms `StateBlock` used for the new error branch under `v2`; grep confirms `engineering.failurePrediction.loadError` key referenced; git status shows only FailurePredictionSidebar.tsx changed</verify>
  <done>FailurePredictionSidebar reads the flag itself; useQuery destructuring extended with isError/refetch; a StateBlock error branch (using the new engineering.failurePrediction.loadError key, onRetry: refetch) exists under v2, absent under !v2; existing SkeletonItem/empty/data rendering otherwise unchanged; gates green</done>
</task>

<task type="auto">
  <name>Task 2: WorkOrderDetailDrawer — flag, isError on woDetail query, non-blocking partial-load-error notice</name>
  <files>apps/web/components/engineering/WorkOrderDetailDrawer.tsx</files>
  <action>
1. Read the flag once at the top of the component — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`.
2. Extend the `woDetail` query's destructuring (currently `const { data: woDetail, isLoading: detailLoading, refetch: refetchDetail } = useQuery({...})`) to also destructure `isError: detailError`.
3. **Preserve the existing graceful-degradation behavior exactly**: `const fullWo: WorkOrder = (woDetail?.data ?? wo) as WorkOrder` must remain completely unchanged — on a fetch failure, the drawer still renders using the basic `wo` prop passed in from the Kanban card/list, just without the extra enrichment (comments/photos/full fields) that only `woDetail` provides. Do NOT replace the drawer's body with a full-screen `StateBlock status="error"` — that would be a behavior change (today, a fetch failure is silently absorbed by the fallback, the drawer keeps working).
4. Under `v2`, when `detailError && !detailLoading`, render a small, dismissible-by-nature (i.e. it naturally disappears once `refetchDetail()` succeeds) inline notice near the top of the drawer's content area — reuse the existing alert-token visual language already used elsewhere in this same file (e.g. the `transitionMutation.isError` `<p className="text-xs text-[var(--alert)]">` pattern at line ~712, or a compact `StateBlock`-style banner) with text `t('engineering.workOrderDetail.loadError')` and a retry affordance calling `refetchDetail()` (already destructured today, no change needed there). This is additive — it does not replace or gate any of the drawer's existing fields/sections, which continue to render from `fullWo` regardless.
5. Under `!v2`: `detailError` is simply unused; no visual change from today.
6. Do not touch `claimMutation`, `completeMutation`, `transitionMutation`, `commentMutation`, the housekeeping-task-push flow, the photo upload flow, or any of the existing `isPending`/`isError` UI already present on those 5 mutations (lines ~498-959) — those are unrelated to this task's scope (the drawer's own detail-fetch query, not its mutations).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present; grep confirms `woDetail` query destructuring now includes `isError: detailError`; grep confirms `fullWo = (woDetail?.data ?? wo)` line is byte-unchanged from before this task; grep confirms a new v2-gated notice referencing `engineering.workOrderDetail.loadError` exists; grep confirms none of the 5 existing mutation blocks (`claimMutation`/`completeMutation`/`transitionMutation`/`commentMutation`/housekeeping-task mutation) changed; git status shows only WorkOrderDetailDrawer.tsx changed</verify>
  <done>WorkOrderDetailDrawer reads the flag itself; woDetail query gains isError; a small non-blocking v2 notice (new engineering.workOrderDetail.loadError key, retry via existing refetchDetail) appears on detail-fetch failure without altering the fullWo fallback or any of the 5 existing mutation UIs; !v2 branch unchanged; gates green</done>
</task>

<task type="auto">
  <name>Task 3: CreateWorkOrderModal + BulkArchiveModal — flag, minimal Skeleton/StateBlock for each modal's list query</name>
  <files>apps/web/components/engineering/CreateWorkOrderModal.tsx, apps/web/components/engineering/BulkArchiveModal.tsx</files>
  <action>
Both modals currently fetch a supporting list with ZERO loading/error handling (`const { data: roomsData } = useQuery({...})` in CreateWorkOrderModal; `const { data } = useQuery({...})` in BulkArchiveModal) — genuine gaps, not present in either today.

CreateWorkOrderModal.tsx:
1. Read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`.
2. Extend the `roomsData` query's destructuring to `{ data: roomsData, isLoading: roomsLoading, isError: roomsError, refetch: refetchRooms }`.
3. Under `v2`: while the room dropdown/select is populated from `roomsData`, if `roomsLoading` show a small inline `<Skeleton variant="text" className="h-9 w-full" />` in place of the select control; if `roomsError`, render a compact `<StateBlock status="error" error={{ onRetry: () => refetchRooms() }} className="py-3" />` (no explicit `message`/no explicit retry label — let it fall back to StateBlock's own built-in `common.error`/`common.retry` defaults, since this is a minor supporting-list failure, not a page-level concern; no new i18n key needed). The room/"other location" manual-entry fallback (`noSpecificRoom`/`otherLocationLabel`) must remain usable regardless of `roomsError` — do not block work-order creation if the room list fails to load.
4. Under `!v2`: unchanged — the new destructured fields are simply unused.
5. Do not touch the AI-assisted creation toggle, the category/priority selects, the photo upload, or `createError`/`titleRequired`/`describeRequired`/`locationRequired` validation copy.

BulkArchiveModal.tsx:
1. Read the flag once, same pattern.
2. Extend the `data` query's destructuring to `{ data, isLoading: candidatesLoading, isError: candidatesError, refetch: refetchCandidates }` (queryKey `['work-orders', 'bulk-archive-candidates']`).
3. Under `v2`: while `candidatesLoading`, show 2-3 `<Skeleton variant="text" className="h-10 w-full" />` rows in place of the selectable-candidates list; if `candidatesError`, render a compact `<StateBlock status="error" error={{ onRetry: () => refetchCandidates() }} className="py-3" />` (built-in default copy again, no new key). The existing `archiveModalNoneAvailable` empty-copy path (when `candidates.length === 0` and no error) is unchanged — only the genuinely-new loading/error states are added.
4. Under `!v2`: unchanged.
5. Do not touch `archiveSelectedMutation`, `archiveByAgeMutation`, or the existing `error` state / `archiveModalError` banner used for mutation failures (a different, already-handled concern from this task's query-loading gap).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `isSectionRedesigned('engineering'` present in BOTH files; grep confirms both files' respective list-query destructuring now includes `isLoading`/`isError`/`refetch` (or equivalent aliased names); grep confirms `Skeleton`/`StateBlock` used in both files under `v2`; grep confirms StateBlock's `error` prop has NO explicit `message` override in either file (relying on the built-in `common.error`/`common.retry` default); grep confirms no `en.ts`/`es.ts` edit accompanies this task; git status shows only these 2 files changed</verify>
  <done>Both CreateWorkOrderModal and BulkArchiveModal read the flag themselves; each modal's previously-unhandled list query gains a v2 Skeleton (loading) + StateBlock (error, built-in default copy, retry wired to that query's own refetch); neither modal's create/archive mutation flow or existing mutation-error banner changed; !v2 branch unchanged in both; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- All 4 files self-read `isSectionRedesigned('engineering', hotel)` — no prop threaded from work-orders/page.tsx.
- FailurePredictionSidebar and WorkOrderDetailDrawer each gain a genuinely new isError-driven state, wired to the new 35-01 keys and each component's existing refetch mechanism.
- CreateWorkOrderModal and BulkArchiveModal each gain a genuinely new loading/error state for their previously-unhandled list query, using StateBlock's built-in default copy (no new i18n key).
- WorkOrderDetailDrawer's `fullWo = woDetail?.data ?? wo` fallback and all 5 existing mutation UIs are byte-unchanged.
- No frozen file, no locale file, no query key/mutation/RBAC logic changed in any of the 4 files.
</verification>

<success_criteria>
The 4 non-frozen work-orders sub-components each close their one identified genuine loading/error gap using the shared Skeleton/StateBlock primitives, self-gated behind the engineering flag, with zero change to any existing mutation, form-validation, or graceful-degradation behavior — narrowest-possible scope per CONTEXT's explicit "gap-closing only, not ground-up rewrite" boundary for modals.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-03-SUMMARY.md` recording: confirmation each of the 4 components self-reads the flag (no prop from work-orders/page.tsx, confirming the cross-plan decoupling held), the exact isError/refetch destructuring added per file, confirmation WorkOrderDetailDrawer's fullWo fallback and 5 existing mutation UIs are byte-unchanged, confirmation CreateWorkOrderModal/BulkArchiveModal rely on StateBlock's built-in default copy (no new key), and any deferred item for the close-out list.
</output>
