---
phase: 35-engineering-section-chrome
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - apps/web/app/(dashboard)/engineering/work-orders/page.tsx
  - apps/web/components/engineering/ArchivedWorkOrdersPanel.tsx
autonomous: true

# NOTE on file ownership: this plan is the SOLE editor of work-orders/page.tsx and
# ArchivedWorkOrdersPanel.tsx. No sibling wave-2 plan touches either file. `EngineeringRoomBoard`
# (imported and rendered bare at the `room-board` tab, line ~480) is FROZEN — this plan makes ZERO
# changes to it, does not wrap it in a new div/StateBlock/Skeleton, and does not alter the tab
# switch logic around it beyond what's needed to restyle the tab BUTTONS themselves. Does NOT
# edit en.ts/es.ts — reuses `engineering.workOrderList.loadError` (existing, generic) for all new
# error copy in this plan; no new key needed.
#
# CROSS-PLAN CONTRACT (read before editing): work-orders/page.tsx also renders
# `<FailurePredictionSidebar />`, `<WorkOrderDetailDrawer />`, `<CreateWorkOrderModal />`, and
# `<BulkArchiveModal />` (all owned by sibling wave-2 plan 35-03, running in parallel this same
# wave). Do NOT add a `v2`/`redesigned` prop to any of those 4 call sites — leave them exactly as
# they render today (same props, same JSX). Each of those 4 components reads the
# `isSectionRedesigned('engineering', hotel)` flag ITSELF internally (35-03's job) rather than
# receiving it as a prop from this file — this decouples the two plans so they can run in parallel
# without agreeing on a prop-name contract. `ArchivedWorkOrdersPanel`, by contrast, IS owned by
# THIS plan (same file list) — thread a `redesigned` prop into it directly, matching the
# established Reports-tabs pattern (Phase 34's 34-02).
#
# NOT IN SCOPE: `WorkOrderCard.tsx` (rendered inside each Kanban card via a component not shown in
# this plan's excerpts) is ALSO imported by `apps/web/app/(dashboard)/ai/page.tsx` and
# `apps/web/components/ai/cards.tsx` — a cross-section shared card component used by the
# already-redesigned-and-closed AI Copilot section (Phase 34). Do NOT touch `WorkOrderCard.tsx` in
# this plan; a change there risks a silent regression to AI Copilot's chat cards. This phase's
# goal is chrome (PageHeader/tabs/columns/states), not the individual card's internals.

must_haves:
  truths:
    - "With the 'engineering' flag ON, work-orders/page.tsx's PageHeader gains dataI18nSkip, and the 5 Kanban columns each show a v2 skeleton while loading and their OWN StateBlock error (not a shared/aggregate one) with onRetry calling that exact column's query.refetch() on failure; flag OFF is byte-behaviorally identical to today"
    - "With the 'engineering' flag ON, ArchivedWorkOrdersPanel shows a v2 skeleton while loading, a StateBlock error (wired to its own query's refetch) on failure, and EmptyState when there are zero archived work orders; flag OFF is byte-behaviorally identical to today"
    - "EngineeringRoomBoard (frozen) is rendered with zero changes — no new wrapper element, no prop change, no touch to EngineeringRoomBoard.tsx itself; apps/web/frozen-files.json hash for it is unaffected"
    - "The flag is read ONCE in WorkOrdersPageContent; no RBAC (isEngineer/canManage) logic changes; no query key/fetcher/params change on any of the 5 Kanban queries or the archived-list query"
  artifacts:
    - path: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      provides: "Reads isSectionRedesigned('engineering', hotel) once; v2-restyles PageHeader/tab-bar/urgent-banner chrome; gives each of the 5 Kanban queries a v2 skeleton + per-column StateBlock error wired to that query's own refetch(); threads redesigned into ArchivedWorkOrdersPanel; EngineeringRoomBoard untouched; legacy branch unchanged"
      contains: "isSectionRedesigned('engineering'"
    - path: "apps/web/components/engineering/ArchivedWorkOrdersPanel.tsx"
      provides: "Accepts a redesigned?: boolean prop; v2 skeleton/StateBlock-error/EmptyState under redesigned; legacy branch unchanged"
      contains: "redesigned"
  key_links:
    - from: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx (openQ/escalatedQ/progressQ/holdQ/completedQ)"
      to: "each Kanban column's own StateBlock error"
      via: "columnError[status] = <status>Q.isError; onRetry: () => <status>Q.refetch() — per-column, not aggregate"
      pattern: "\\.refetch\\(\\)"
    - from: "apps/web/app/(dashboard)/engineering/work-orders/page.tsx"
      to: "apps/web/components/engineering/ArchivedWorkOrdersPanel.tsx"
      via: "<ArchivedWorkOrdersPanel redesigned={v2} />"
      pattern: "redesigned=\\{v2\\}"
---

<objective>
Redesign work-orders/page.tsx's chrome — the section's `PageHeader`/3-tab bar, the `work-orders` tab's urgent-alert banner and 5-column Kanban board, and the `archived` tab (`ArchivedWorkOrdersPanel.tsx`) — while leaving the `room-board` tab's `EngineeringRoomBoard` (frozen, pixel-regression-protected) completely untouched. Add `isSectionRedesigned('engineering', hotel)` once in `WorkOrdersPageContent`. Under the flag: replace the 5 Kanban columns' bespoke `animate-pulse` loading rows with the shared `Skeleton` component and give each column its OWN `StateBlock status="error"` (per-column granularity, matching the existing per-column loading pattern — a genuine partial failure, e.g. one status query 500ing while others succeed, is plausible and per-column error is more informative than one aggregate banner) wired to that exact query's own `.refetch()` — none of the 5 `useQuery` calls need destructuring changes since they're stored as full query-result objects (`openQ`, `escalatedQ`, etc.), so `.isError`/`.refetch()` are already available via dot-notation. Convert `ArchivedWorkOrdersPanel`'s loading/empty markup to `Skeleton`/`EmptyState` and add its currently-missing `isError` + `StateBlock` error with retry.

Purpose: ENG-01 for work-orders' own chrome (excluding EngineeringRoomBoard); establishes the per-column error-state pattern this codebase has never needed before (5 simultaneous same-shape queries feeding one board).
Output: work-orders/page.tsx and ArchivedWorkOrdersPanel.tsx, flag-gated, Skeleton/StateBlock/EmptyState-wired, EngineeringRoomBoard provably untouched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md
@.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md

@apps/web/app/(dashboard)/engineering/work-orders/page.tsx
@apps/web/components/engineering/ArchivedWorkOrdersPanel.tsx

# Flag + state + primitives contracts (import-only)
@apps/web/lib/utils/redesignFlag.ts
@apps/web/stores/hotelStore.ts
@apps/web/components/ui/StateBlock.tsx
@apps/web/components/ui/EmptyState.tsx
@apps/web/components/ui/Skeleton.tsx
@apps/web/components/shared/PageHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flag + PageHeader/tab-bar/urgent-banner v2 restyle in work-orders/page.tsx</name>
  <files>apps/web/app/(dashboard)/engineering/work-orders/page.tsx</files>
  <action>
1. In `WorkOrdersPageContent`: read the flag once — `const hotel = useHotelStore((s) => s.hotel); const v2 = isSectionRedesigned('engineering', hotel)`.
2. Add `dataI18nSkip={v2}` to the `PageHeader` call (title/subtitle both come from the pre-existing `engineering.workOrdersPage.*` namespace — lower mangling risk than a brand-new namespace, but set proactively per established precedent, same as every prior phase's PageHeader touch-points). Do NOT change the `tabs` array's `label` VALUES — `tabRoomBoard`'s current English value ('Room Board') is hardcoded-matched by `apps/web/e2e/room-board-baseline.spec.ts`'s `page.getByRole('button', { name: 'Room Board' })` locator; changing that string breaks the regression harness's tab click, not just this phase's own gate.
3. Under `v2`, re-skin any remaining raw (non-token) Tailwind classes within the touched chrome region (PageHeader is already fully token-styled — no change needed to PageHeader.tsx itself) — specifically the urgent-alert banner and AI-triage-notice `<div>`s already use `var(--alert-soft)`/`var(--alert-line)`/`var(--alert)` tokens (already v2-correct, leave as-is). Confirm no raw `gray-*`/`red-*` classes exist in this region before assuming a change is needed — if none are found, this step is a no-op and that's expected (this region was already token-consistent).
4. Under `!v2`, render this region exactly as today (no visual change) — since PageHeader/urgent-banner/AI-notice markup is unconditional today (not previously gated by any flag), verify their current classes remain byte-identical in the legacy branch; only the loading/error treatment inside the Kanban columns (Task 2) and Archived panel (Task 3) actually differ between branches.
  </action>
  <verify>cd apps/web && npm run type-check succeeds on this file; grep confirms `isSectionRedesigned('engineering'` present in work-orders/page.tsx; grep confirms `dataI18nSkip={v2}` on the PageHeader call; grep confirms the `tabs` array's 3 label strings (`tabWorkOrders`/`tabRoomBoard`/`tabArchived` translation keys) are unchanged from before this task; grep confirms `EngineeringRoomBoard` import and its bare `<EngineeringRoomBoard />` render (no new wrapper) are byte-unchanged</verify>
  <done>work-orders/page.tsx reads the flag once; PageHeader gets dataI18nSkip; tab labels' underlying i18n keys/values unchanged; EngineeringRoomBoard's import and bare render are untouched; gates green</done>
</task>

<task type="auto">
  <name>Task 2: Per-column Skeleton + StateBlock error for the 5 Kanban queries</name>
  <files>apps/web/app/(dashboard)/engineering/work-orders/page.tsx</files>
  <action>
1. `openQ`/`escalatedQ`/`progressQ`/`holdQ`/`completedQ` (built via the shared `queryOpts(status)` factory, lines ~276-292) are stored as full `useQuery(...)` return objects, NOT destructured — so `openQ.isError`/`openQ.refetch()` etc. are already usable without changing the `useQuery(queryOpts(status))` call sites themselves.
2. Build a `columnError: Record<KanbanStatus, boolean>` map alongside the existing `columnLoading` map (lines ~302-308): `open: openQ.isError, escalated: escalatedQ.isError, in_progress: progressQ.isError, on_hold: holdQ.isError, completed: completedQ.isError`.
3. Build a `columnRefetch: Record<KanbanStatus, () => void>` map the same way: `open: () => openQ.refetch(), escalated: () => escalatedQ.refetch(), ...` (one entry per status, mapping each column back to its OWN query's refetch — never a blanket refetch-all).
4. Pass `isError={columnError[status]}` and `onRetry={columnRefetch[status]}` as two new props into `<KanbanColumn>` (alongside the existing `isLoading={columnLoading[status]}`).
5. Inside `KanbanColumn` (function component defined in this same file, ~line 140): accept the two new props (`isError: boolean`, `onRetry: () => void`). Under `v2` (thread the `v2` boolean into `KanbanColumn` as a new prop too, read once in the parent):
   - Loading: replace the 3 `animate-pulse` placeholder `<div>`s (currently `h-[88px] bg-surface-3 rounded-[var(--r-lg)] animate-pulse border border-line`) with 3 `<Skeleton variant="card" className="h-[88px]" />` instances — same visual sizing, shared component.
   - Error (new — only rendered when `isError` and not `isLoading`): `<StateBlock status="error" error={{ message: t('engineering.workOrderList.loadError'), onRetry }} className="py-6" />` — reuse the EXISTING generic key (do not add a new one; the message is intentionally the same across all 5 columns, only `onRetry` differs per column).
   - Empty (when `!isLoading && !isError && workOrders.length === 0`): replace the current bare `<p>{t('engineering.workOrdersPage.emptyColumn', {...})}</p>` with `<StateBlock status="empty" empty={{ title: t('engineering.workOrdersPage.emptyColumn', { label: label.toLowerCase() }) }} className="py-6" />` — same existing key, same interpolation, now rendered through the shared primitive.
   Under `!v2`: render exactly today's markup (3 raw `animate-pulse` divs for loading, bare `<p>` for empty, and — since there was NO error UI at all before this task — render nothing extra for the error case, i.e. `isError` is simply unused in the legacy branch, matching zero-behavior-change).
6. Do not change `columnData`, the `queryOpts` factory, any query key, `refetchInterval`, or `enabled` condition. Do not touch `EngineeringRoomBoard`.
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `columnError` and a per-status `.refetch()` mapping exist in work-orders/page.tsx; grep confirms `KanbanColumn` now accepts `isError`/`onRetry` props; grep confirms `StateBlock` used for both the new error path and the (now-shared-primitive) empty path inside `KanbanColumn`; grep confirms `Skeleton` used for the v2 loading path; grep confirms none of the 5 `useQuery(queryOpts(...))` call sites changed their query options object; git status shows only work-orders/page.tsx changed (combined with Task 1); check:frozen-files explicitly shows zero drift on EngineeringRoomBoard.tsx's hash</verify>
  <done>Each of the 5 Kanban columns independently shows a v2 skeleton while loading and its own StateBlock error (message from the existing engineering.workOrderList.loadError key, onRetry calling that column's own query.refetch()) on failure, and a StateBlock empty state reusing the existing emptyColumn key; legacy branch (v2 false) renders exactly as before this task; no query/RBAC change; EngineeringRoomBoard frozen and untouched; gates green</done>
</task>

<task type="auto">
  <name>Task 3: ArchivedWorkOrdersPanel — flag prop, Skeleton/StateBlock/EmptyState conversion, add isError</name>
  <files>apps/web/components/engineering/ArchivedWorkOrdersPanel.tsx</files>
  <action>
1. In work-orders/page.tsx (same plan, Task 1's flag read), change `<ArchivedWorkOrdersPanel />` (line ~482, rendered when `activeTab === 'archived'`) to `<ArchivedWorkOrdersPanel redesigned={v2} />`.
2. In `ArchivedWorkOrdersPanel.tsx`: accept a new `redesigned?: boolean` prop. Extend the existing `useQuery` destructuring from `{ data, isLoading }` to `{ data, isLoading, isError, refetch }` (currently absent — genuine gap per RESEARCH.md).
3. Under `redesigned`:
   - Loading: replace the 3 `animate-pulse` divs (`h-16 bg-surface-3 rounded-[var(--r-lg)] animate-pulse border border-line`) with 3 `<Skeleton variant="card" className="h-16" />`.
   - Error (new — `isError && !isLoading`): `<StateBlock status="error" error={{ message: t('engineering.workOrderList.loadError'), onRetry: () => refetch() }} />` — reuse the SAME existing generic key as the Kanban columns (Task 2), for consistency; this panel and the Kanban board share the same underlying `work-orders` query family conceptually.
   - Empty (`workOrders.length === 0 && !isLoading && !isError`): replace the current bare icon+`<p>` block (`ClipboardList` icon, `t('engineering.workOrdersPage.archivedPanelEmpty')`) with `<EmptyState icon={<ClipboardList className="w-10 h-10" />} title={t('engineering.workOrdersPage.archivedPanelEmpty')} />` — reuse the existing key, now through the shared component.
4. Under `!redesigned` (including when the prop is omitted/undefined — default to legacy): render EXACTLY today's markup — the 3 raw `animate-pulse` divs, bare icon+`<p>` empty block, and no error UI (matching today's behavior where a fetch failure silently renders as if there's no data — `isError` is simply unused in this branch).
5. Do not change the `restoreMutation`, the success/error `message` state banner above the list, or the query key (`['work-orders', 'archived']`).
  </action>
  <verify>cd apps/web && npm run type-check && npm run build && npm run check:frozen-files && npm run check:contrast && npm run check:i18n-parity all green; grep confirms `redesigned?: boolean` prop accepted; grep confirms `useQuery` destructuring now includes `isError`/`refetch`; grep confirms `Skeleton`/`StateBlock`/`EmptyState` all used under the `redesigned` branch; grep confirms the legacy branch's 3 `animate-pulse` divs and bare `<p>` empty block are still present, byte-unchanged, for the `!redesigned` path; git status shows only ArchivedWorkOrdersPanel.tsx changed (combined with work-orders/page.tsx's 1-line call-site change from this same task)</verify>
  <done>ArchivedWorkOrdersPanel accepts a redesigned prop from work-orders/page.tsx; under redesigned, loading/error/empty all route through Skeleton/StateBlock/EmptyState with isError+refetch newly wired; legacy branch (prop false/omitted) unchanged; gates green</done>
</task>

</tasks>

<verification>
- `npm run type-check`, `build`, `check:frozen-files`, `check:contrast`, `check:i18n-parity` all green from `apps/web`.
- work-orders/page.tsx: flag read once; PageHeader dataI18nSkip'd; 5 Kanban columns each independently Skeleton/StateBlock-wired (error via that column's own refetch, not a blanket one); ArchivedWorkOrdersPanel called with `redesigned={v2}`.
- ArchivedWorkOrdersPanel.tsx: accepts `redesigned` prop; Skeleton/StateBlock/EmptyState-wired with newly-added isError+refetch; legacy unchanged when prop is false/omitted.
- EngineeringRoomBoard: zero changes, zero new wrapper, frozen-files hash unaffected.
- Only work-orders/page.tsx and ArchivedWorkOrdersPanel.tsx changed; no frozen file, no locale file, no query/mutation/RBAC logic changed; WorkOrderCard.tsx untouched.
</verification>

<success_criteria>
work-orders/page.tsx's PageHeader/tab-bar chrome and its `work-orders`+`archived` tabs (NOT `room-board`) render in the new visual system behind the `engineering` flag, with per-column loading/error/empty states served by shared Skeleton/StateBlock primitives and wired to each query's own existing refetch — zero behavior/data/RBAC change, EngineeringRoomBoard provably untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-02-SUMMARY.md` recording: confirmation EngineeringRoomBoard.tsx was never touched (with the specific frozen-files hash check evidence), the per-column error/retry wiring used, the `redesigned` prop contract given to ArchivedWorkOrdersPanel, confirmation WorkOrderCard.tsx was never touched, and any deferred item for the close-out list.
</output>
