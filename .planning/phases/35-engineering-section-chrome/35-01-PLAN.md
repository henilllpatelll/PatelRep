---
phase: 35-engineering-section-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/i18n/locales/en.ts
  - apps/web/i18n/locales/es.ts
autonomous: true

# NOTE on file ownership: en.ts / es.ts are edited ONLY here. This plan is the SOLE owner of the
# locale files for Phase 35 so the five parallel wave-2 content plans (35-02..35-06) never collide
# on them. Unlike Phase 34, this phase does NOT create any new top-level namespace — the
# `engineering.*` and `programs.pmSchedules.*` namespaces already exist and are already
# substantial (confirmed via direct read of both locale files, line-aligned between en.ts/es.ts).
# This plan adds exactly THREE new leaf keys, additively, into three EXISTING sub-namespaces.
# Every other state this phase touches (Kanban columns, ArchivedWorkOrdersPanel, Assets, PM
# Schedules, BulkArchiveModal, CreateWorkOrderModal) reuses keys that already exist today:
# `engineering.workOrderList.loadError`, `engineering.assetsPage.loadError`/`retry`,
# `programs.pmSchedules.failedToLoad`/`retry`, and StateBlock's own built-in defaults
# (`common.error`/`common.retry`/`common.loading`) for the two modals' minor list queries. Do NOT
# invent additional keys beyond the three below — wave-2 plans MUST reuse the existing keys named
# in their own plan text, not request new ones (this plan is their only chance to add locale keys
# for Phase 35).

must_haves:
  truths:
    - "The three genuinely-missing loading/error strings this phase's wave-2 plans need (Predictions page load-error, FailurePredictionSidebar load-error, WorkOrderDetailDrawer partial-load-error) exist in both en.ts and es.ts with real Spanish translations, added additively inside their existing namespaces"
    - "No new top-level namespace is created; every pre-existing key in engineering.* and programs.pmSchedules.* (and every other namespace) is byte-unchanged"
    - "npm run check:i18n-parity and npm run verify:i18n-gate pass — the 3 new en keys each have an es counterpart and vice versa"
  artifacts:
    - path: "apps/web/i18n/locales/en.ts"
      provides: "engineering.predictionsPage.loadError, engineering.failurePrediction.loadError, engineering.workOrderDetail.loadError — added additively inside their existing namespaces"
      contains: "engineering.predictionsPage.loadError"
    - path: "apps/web/i18n/locales/es.ts"
      provides: "Exact-parity Spanish counterparts for the 3 new en.ts keys"
      contains: "engineering"
  key_links:
    - from: "new keys in en.ts (predictionsPage.loadError, failurePrediction.loadError, workOrderDetail.loadError)"
      to: "matching keys in es.ts"
      via: "1:1 key parity enforced by scripts/check-i18n-parity.mjs"
      pattern: "loadError"
---

<objective>
Add the ONLY three i18n keys Phase 35's wave-2 content plans genuinely need — Predictions page's page-level load error (currently has zero error copy), FailurePredictionSidebar's load error (currently has zero error copy), and WorkOrderDetailDrawer's non-blocking partial-load-error banner copy (currently has zero error copy for its detail-fetch query) — additively inside the EXISTING `engineering.predictionsPage`, `engineering.failurePrediction`, and `engineering.workOrderDetail` sub-namespaces. Everything else this phase touches (5 work-orders Kanban columns, ArchivedWorkOrdersPanel, Assets, PM Schedules, BulkArchiveModal, CreateWorkOrderModal) reuses copy that already exists today — confirmed by direct read of both locale files — so this plan does NOT create any new namespace, unlike every Phase 34 section.

Purpose: unblock the five wave-2 content plans (35-02..35-06) to run in parallel without ever touching en.ts/es.ts, and satisfy the EN/ES key-parity half of the phase's Success Criterion #4 up front.
Output: 3 new leaf keys added additively to both locale files; check:i18n-parity + verify:i18n-gate green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-engineering-section-chrome/35-CONTEXT.md
@.planning/phases/35-engineering-section-chrome/35-RESEARCH.md

# The two locale files. engineering.* starts at line 973 in both files (confirmed line-aligned).
# Existing keys wave-2 plans will REUSE (do not touch, listed here for reference only):
#   engineering.workOrderList.loadError (line ~1125 en / ~1117 namespace start es)
#   engineering.assetsPage.loadError / engineering.assetsPage.retry (line ~1179-1180 en)
#   programs.pmSchedules.failedToLoad / programs.pmSchedules.retry (line ~247-248 en)
#   common.error / common.retry / common.loading (line 2-15 en) — StateBlock's own built-in
#   defaults when no explicit message/label is passed.
# Namespaces this plan ADDS keys into (all pre-existing, confirmed present in both files):
#   engineering.predictionsPage (en.ts line 1244, es.ts line 1244 — exact line-for-line parity)
#   engineering.failurePrediction (en.ts line 1140)
#   engineering.workOrderDetail (en.ts line 983)
@apps/web/i18n/locales/en.ts
@apps/web/i18n/locales/es.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 3 new English keys to en.ts</name>
  <files>apps/web/i18n/locales/en.ts</files>
  <action>
Add exactly three new leaf keys, additively, into three EXISTING sub-namespaces inside the `engineering` top-level block. Leave every pre-existing key in the file byte-unchanged. Match the file's existing style (single quotes, trailing commas, 6-space nesting for these depths).

1. Inside `engineering.predictionsPage` (starts `predictionsPage: {` around line 1244), add a `loadError` key. Place it near `subtitle` (the page currently has zero error copy — no `isError` is destructured on its query today, this is a genuine gap):
   `loadError: 'Failed to load predictions.',`

2. Inside `engineering.failurePrediction` (starts `failurePrediction: {` around line 1140), add a `loadError` key. Place it near `heading`/`activeCount` (this sidebar's query also has zero error copy today):
   `loadError: 'Failed to load failure predictions.',`

3. Inside `engineering.workOrderDetail` (starts `workOrderDetail: {` around line 983), add a `loadError` key. Place it near the other error-copy keys already in this namespace (`transitionError`, `completeError`, `hkTaskError`) for consistency. This is NOT a page-level "everything failed" message — `WorkOrderDetailDrawer`'s `woDetail` query already falls back to the basic `wo` prop on failure (`fullWo = woDetail?.data ?? wo`), so this string labels a non-blocking "extra details didn't load" notice, not a full-screen error:
   `loadError: "Couldn't load full details — showing basic info.",`

Do NOT add any other key. Do NOT create a new top-level namespace. Do NOT touch `engineering.workOrderList.loadError`, `engineering.assetsPage.loadError`/`retry`, `programs.pmSchedules.failedToLoad`/`retry`, or `common.*` — these are reused as-is by wave-2 plans.
  </action>
  <verify>cd apps/web && npm run type-check succeeds; grep confirms `engineering.predictionsPage` block now contains `loadError`, `engineering.failurePrediction` block now contains `loadError`, `engineering.workOrderDetail` block now contains `loadError`; grep confirms no other engineering.* or programs.pmSchedules.* key changed (diff review shows exactly 3 added lines, zero removed/modified lines)</verify>
  <done>en.ts gains exactly 3 new leaf keys (predictionsPage.loadError, failurePrediction.loadError, workOrderDetail.loadError), all pre-existing keys byte-unchanged, type-check green</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the 3 keys into es.ts with real Spanish + prove parity</name>
  <files>apps/web/i18n/locales/es.ts</files>
  <action>
Add the exact same 3 keys, in the exact same 3 namespaces, at the same relative position, with real natural Spanish in the hotel-operations register (match the tone of the existing `engineering.*` Spanish copy already in this file — e.g. `programs.pmSchedules.failedToLoad`'s Spanish equivalent, or `engineering.workOrderDetail.completeError`'s Spanish equivalent — for register consistency):

1. `engineering.predictionsPage.loadError`: `'No se pudieron cargar las predicciones.'`
2. `engineering.failurePrediction.loadError`: `'No se pudieron cargar las predicciones de fallas.'`
3. `engineering.workOrderDetail.loadError`: `'No se pudieron cargar todos los detalles. Mostrando información básica.'`

Then run the parity + type gates and confirm green.
  </action>
  <verify>cd apps/web && npm run check:i18n-parity && npm run verify:i18n-gate && npm run type-check all green; grep confirms all 3 keys present in es.ts at the matching namespaces; diff review confirms exactly 3 added lines, zero removed/modified lines elsewhere in es.ts</verify>
  <done>es.ts mirrors all 3 new keys from Task 1 with real Spanish; check:i18n-parity + verify:i18n-gate + type-check all green</done>
</task>

</tasks>

<verification>
- `npm run check:i18n-parity`, `npm run verify:i18n-gate`, `npm run type-check` all green from `apps/web`.
- Exactly 3 new leaf keys exist in both locales, inside pre-existing namespaces (`engineering.predictionsPage`, `engineering.failurePrediction`, `engineering.workOrderDetail`); every other key across both files is byte-unchanged.
- No new top-level namespace created.
</verification>

<success_criteria>
The complete Phase-35 net-new copy set (3 keys) exists in both locales with real Spanish and passes parity — satisfying the EN/ES half of the phase's Success Criterion #4 up front and unblocking 35-02..35-06 to run in parallel without ever touching the locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/35-engineering-section-chrome/35-01-SUMMARY.md` recording: the exact 3 keys added (namespace + key + English + Spanish value), explicit confirmation of every EXISTING key wave-2 plans will reuse (`engineering.workOrderList.loadError`, `engineering.assetsPage.loadError`/`retry`, `programs.pmSchedules.failedToLoad`/`retry`, `common.error`/`common.retry`/`common.loading`) so wave-2 executors don't have to re-derive them, confirmation of parity-gate pass, and an explicit statement that en.ts/es.ts are now frozen for the rest of Phase 35 (wave-2 plans must not edit them, only consume/reuse existing keys).
</output>
