---
phase: 38-engineering-work-order-labor-and-cost-capture
plan: 05
type: execute
wave: 4
depends_on: ["38-02", "38-03", "38-04"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "The full apps/api pytest suite passes, not just the new Phase 38 test files"
    - "A live golden path works end-to-end: GM sets a part's unit_cost and a staff member's hourly_rate, an engineer completes a work order with labor_hours and that part consumed, and the work order detail view shows a non-null total cost"
    - "A non-GM role's /staff list response contains no hourly_rate key at all (verified over the network, not just by reading source)"
    - "Adjacent, already-shipped engineering flows (WO claim, WO transition/hold/cancel, parts stock decrement, PM completion) still work after this phase's changes"
  artifacts: []
  key_links:
    - from: "apps/web dev server (localhost:3000)"
      to: "apps/api dev server (localhost:8003)"
      via: "live browser session driven by Claude, per CLAUDE.md's mandatory Self-Verification Policy"
---

<objective>
Close out Phase 38 with the mandatory self-verification pass: run the full backend test suite, then drive the actual web app in a browser to confirm the golden path works end-to-end on a live server (not just in unit tests), and spot-check that nothing adjacent broke. This is required by this repo's CLAUDE.md Self-Verification Policy and Non-Regression Policy — no task in this phase is "done" until this plan passes.

Purpose: unit tests with fake Supabase clients can pass while the real wiring (route registration, RLS, Supabase client field pass-through, i18n key resolution, React Query cache invalidation) is broken — this plan is the check that catches that class of bug.
Output: a verified, working feature end-to-end; nothing else is created (this plan's job is to prove Plans 01-04 actually work together).
</objective>

<context>
@.planning/phases/38-engineering-work-order-labor-and-cost-capture/38-CONTEXT.md
@CLAUDE.md

**Local dev gotchas (from project memory, more specific than generic instructions — trust these over any assumption of port 8000):**
- `npm run dev:api` serves on **port 8003**, not 8000 (port 8000 on this machine is occupied by an unrelated process). Confirm via `GET http://localhost:8003/openapi.json` before trusting the server is up and current.
- Windows `uvicorn --reload` has a history of leaving orphaned `python.exe` processes bound to the port after a prior session. Before starting, check `Get-CimInstance Win32_Process -Filter "Name = 'python.exe'"` for anything with `uvicorn` or `multiprocessing` in its command line and kill stale ones, otherwise you may be testing against stale code.
- New Python deps must be installed into `apps/api/.venv` specifically, not the global interpreter — not expected to be relevant here (this phase adds no new dependencies), but if `pytest` passes and the dev server 500s with an ImportError, this is why.
- `apps/api/.env` has real Supabase credentials (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_JWT_SECRET) — Supabase-backed paths, including this phase's, can be exercised locally end-to-end. `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/Twilio/Opera credentials are absent, but this phase touches none of those paths.

**Test account:** GM login for whichever environment ends up used — check `apps/api/.env`'s `SUPABASE_URL` against the project ref the migration was applied to in Plan 01 (`oacnwalhcpqdabivweki`). If they match, seed/use a local dev GM account (create one via existing onboarding/staff-invite flow if none exists, or reuse whatever test tenant already exists in that Supabase project). If local `.env` points elsewhere, this verification must run against wherever `oacnwalhcpqdabivweki` is actually served from instead — do not verify against an environment that doesn't have migration 104 applied.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full backend test suite</name>
  <files>none</files>
  <action>
Run the complete apps/api test suite, not just the phase's new files:
```
cd apps/api && python -m pytest tests/ -v
```
If any pre-existing test fails, determine whether it's a regression introduced by Plans 02/03 (cost computation changed `complete_work_order`'s update payload shape, or `list_staff`'s output shape) versus a pre-existing flake unrelated to this phase. Fix any regression before proceeding — do not proceed to Task 2 with a failing suite.
  </action>
  <verify>`cd apps/api && python -m pytest tests/ -v` exits 0 with all tests passing (including test_work_order_cost_capture.py, test_staff_hourly_rate.py, test_inventory.py, and every pre-existing file — test_work_order_parts_consumption.py, test_work_order_archive.py, test_pm_schedules.py, smoke/*, etc.).</verify>
  <done>Full pytest suite green, zero regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Live golden-path verification via browser</name>
  <files>none</files>
  <action>
Per CLAUDE.md's mandatory Self-Verification Policy: start both dev servers if not already running (`npm run dev:api` → port 8003, `npm run dev:web` → port 3000), confirm `GET http://localhost:8003/openapi.json` returns PatelRep-shaped paths (not the unrelated port-8000 process), then use a browser (Playwright or the `playwright-cli` skill) to drive this exact sequence against a GM test account:

1. Navigate to `/settings/...` or wherever Engineering → Parts inventory panel lives (`PartsPanel.tsx`, check the actual mounted route via the existing engineering section) — create or edit a part, set a `unit_cost` (e.g. 15.00), submit, confirm it persists (reload and re-check, or confirm the on-hand row now shows the unit cost display added in Plan 04 Task 2).
2. Navigate to `/staff`, open Edit on any staff member, set `hourly_rate` (e.g. 20.00), save, confirm no error and the modal reflects the saved value on reopen.
3. Read the network response of the `/staff` GET request (via browser devtools network panel or `mcp__claude-in-chrome__read_network_requests` if using the Chrome MCP tools) and confirm the JSON payload includes `hourly_rate` for the GM's own session.
4. If a second, non-GM test account exists (or one can be created via staff invite/add-direct as engineer/chief_engineer role), log in as that role and confirm the `/staff` GET response contains no `hourly_rate` key anywhere in the payload. If creating a second account isn't practical within this task, this can instead be verified by re-reading `apps/api/routers/staff.py`'s `list_staff` implementation to confirm the `if current_user.role == "gm":` gate from Plan 03 is present and unconditional — note in the SUMMARY which method was used.
5. Navigate to Engineering → Work Orders, open or create a work order, claim it (transitions to in_progress), then complete it: enter `labor_hours` (e.g. 2), consume the part priced in step 1 via the parts-consumed picker, submit completion.
6. Reopen the completed work order's detail drawer and confirm the cost line added in Plan 04 Task 1 renders with non-dash values for Labor/Parts/Total (or, if the completing user has no hourly_rate on file, confirm it correctly shows a partial cost — Parts populated, Labor as "—", Total still summing what's known).
7. Check the browser console and network tab for errors during this entire sequence — none should appear related to this phase's changes (pre-existing unrelated warnings, if any, are out of scope).
  </action>
  <verify>Each of the 7 steps above completes without a console error, a failed network request (4xx/5xx) from this phase's endpoints, or a UI crash. The completed work order visibly shows cost data matching what was set up in steps 1-2 and 5.</verify>
  <done>The full labor+cost capture golden path works end-to-end on a live server: set unit_cost, set hourly_rate (gm-only, not leaked to other roles), complete a WO with labor+parts, see the computed cost in the detail drawer.</done>
</task>

<task type="auto">
  <name>Task 3: Non-regression spot-check of adjacent engineering flows</name>
  <files>none</files>
  <action>
Per this repo's Non-Regression Policy, spot-check the flows that share code paths with what Plans 02-04 touched, since `complete_work_order`, `staff.py`, and the parts CRUD are all shared surfaces:

1. Claim a different (uncompleted) work order and confirm the claim flow still works (unaffected router, but shares the file with `complete_work_order`).
2. Transition a work order to `on_hold` and back to `in_progress`, and separately `cancel` one — confirm the reason-code flow (unrelated to this phase, same file) still works.
3. If a PM (preventive maintenance) schedule exists, complete one via the PM completion flow (`PMCompletionModal` / `programsApi.completePM`) and confirm it still records `parts_used`/`labor_hours` correctly — this is a separate completion path from `complete_work_order` per the domain map, but shares the `engineering_parts`/`engineering_part_transactions` tables touched by Plan 02/03's schema.
4. On the Staff page, edit a staff member's `role` (not `hourly_rate`) and confirm that still works — Plan 03 changed `update_staff`'s `allowed_fields` set and `list_staff`'s output shape; confirm role/department/custom_role editing is unaffected.
5. Adjust stock on an existing part (add/remove/count transaction, not unit_cost) via `PartsPanel`'s stock-adjustment form and confirm it still works — Plan 03/04 added a new field to parts but didn't touch this transaction flow.
  </action>
  <verify>All 5 spot-checks complete without error. No previously-working feature is broken by this phase's changes.</verify>
  <done>Adjacent flows (WO claim/transition/cancel, PM completion, staff role edit, stock adjustment) confirmed unaffected.</done>
</task>

</tasks>

<verification>
Full pytest suite green. Live golden path (parts unit_cost → staff hourly_rate → WO completion → cost display) verified working on a real server via browser. hourly_rate confirmed absent from non-GM /staff responses. No regression in adjacent engineering/staff flows.
</verification>

<success_criteria>
Phase 38 is verifiably complete: not just "code written," but "seen working" per CLAUDE.md's Self-Verification Policy, with the full existing test suite still green and no adjacent feature broken.
</success_criteria>

<output>
After completion, create `.planning/phases/38-engineering-work-order-labor-and-cost-capture/38-05-SUMMARY.md`
</output>
