---
milestone: v1.5
audited: 2026-08-11T00:00:00Z
status: passed
scores:
  requirements: 4/4
  phases: 2/2
  integration: 5/5
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 24-ci-guard-bare-role-comparisons
    items:
      - "check_bare_role_comparisons.py's imported_core_roles_names() hand-duplicates (rather than imports) generate_rbac_matrix.py's one-line core.roles import-matching condition (module == \"roles\"/\"core.roles\"/ends with \".roles\"). Currently produces identical results; if generate_rbac_matrix.py's import detection ever changes (e.g. to support `import core.roles as roles_mod`) without a matching update here, the two scripts could silently diverge on what counts as \"properly sourced.\" Low severity, not a current bug — worth a shared-helper refactor if it recurs."
      - "check_bare_role_comparisons.py defines ROLES_PATH/ROUTERS_DIR module-level constants that are never referenced (find_violations/regenerate_allowlist recompute the paths inline instead). Dead code, cosmetic only."
---

# v1.5 "RBAC Enforcement Tooling" — Milestone Audit

**Scope:** Phases 23-24 (Route×Role Permission Matrix, CI Guard Against New Bare Role Comparisons).
**Audited:** 2026-08-11

## Phase Verification Summary

| Phase | Status | Score | Notes |
|---|---|---|---|
| 23. Route×Role Permission Matrix | ✓ passed | 5/5 | AST-based `RBAC-MATRIX.md` generator; code review found+fixed a critical inline-role-gate misclassification bug (CR-01) before verification |
| 24. CI Guard Against New Bare Role Comparisons | ✓ passed | 5/5 | AST-based bare-comparison detector + 25-entry allowlist; code review found+fixed a critical allowlist-matching evasion bug (CR-01) before verification |

Both phases individually verified as passed with 0 gaps at their own phase-gate, each after a code-review-fix cycle caught and closed a real critical defect before the phase-gate verifier ran. This milestone-level audit additionally confirmed the two phases' outputs remain mutually consistent post-fix (see Cross-Phase Integration Check below) — the seam neither phase's own verification was scoped to check.

## Requirements Coverage

All 4 v1.5 requirements are satisfied per phase verification and independently re-confirmed by this audit (see `.planning/REQUIREMENTS.md` traceability table).

| Requirement | Phase | Status |
|---|---|---|
| RBAC-05, RBAC-06 | 23 | ✓ Satisfied |
| RBAC-07, RBAC-08 | 24 | ✓ Satisfied |

**Score: 4/4** — no requirement-level gaps.

## Cross-Phase Integration Check

Spawned `gsd-integration-checker` against the actual current codebase (not phase-summary prose) to check wiring between Phase 23's generator and Phase 24's detector, and to confirm both phases' review-fix cycles left the classification logic consistent with each other.

**Findings:**

1. ✓ **Reuse wired correctly**: `check_bare_role_comparisons.py` loads `generate_rbac_matrix.py` via `importlib` and calls its `parse_role_constants` directly — genuine reuse, not re-derivation, of the `core/roles.py` constant-value parsing.
2. ✓ **Coverage matches**: both scripts glob the identical `apps/api/routers/*.py` set (30 files, excluding `__init__.py`) — no router present in one tool's scope but absent from the other's.
3. ✓ **Guards coexist cleanly**: `cd apps/api && pytest tests/smoke/ -v` → 271 passed, including both `test_rbac_matrix_contract.py` (3 tests) and `test_bare_role_comparison_guard.py` (5 tests) together; `.github/workflows/ci.yml` already runs this exact command, no new CI wiring needed.
4. ✓ **Milestone goal composition holds by mechanics**: Phase 24's detector scans router source directly via AST, independent of `RBAC-MATRIX.md` state — a developer cannot bypass the bare-comparison guard by skipping matrix regeneration. The two guards enforce on independent axes (generated-artifact drift vs. live-source bare-comparison scan) and both gate the same CI step.
5. ✓ **Post-fix classification consistency confirmed**: every route Phase 23 now labels `role-restricted (inline, see source)` (after its CR-01 fix) has a matching Phase 24 allowlist entry at the same file:line (after Phase 24's own CR-01 fix); the additional allowlist entries are correctly the filter-only/self-scoping comparisons Phase 23 legitimately classifies `none`. Independently re-ran the standalone detector (bypassing the allowlist) and confirmed the true live inventory — 9 files, 25 occurrences — matches the committed allowlist exactly.

**One doc-only inconsistency found and fixed during this audit**: `24-01-SUMMARY.md` stated "10 files, 25 occurrences" in its dry-run description; the actual/verified count (from both the allowlist and a live re-scan) is 9 files. Corrected in `.planning/phases/24-ci-guard-bare-role-comparisons/24-01-SUMMARY.md` — cosmetic prose fix only, no code/allowlist/test change (all of those were already correct at 9/25).

**Score: 5/5** — no integration gaps. Two minor, non-blocking tech-debt items noted above (a hand-duplicated one-line condition, and unused constants); neither affects current correctness.

## E2E Flow Check

**Flow: "Router adds an inline role check outside require_role()/core.roles → CI blocks the PR."**

Traced mechanically end-to-end: new bare `.role` comparison in a router → not in the allowlist → `find_violations()` returns non-empty → `test_no_unlisted_bare_role_comparisons` fails → `pytest tests/smoke/` (the exact `ci.yml` CI step) fails → PR blocked. Both phases' own deliberate-drift proofs (recorded in their SUMMARYs) independently demonstrated each guard in isolation; this audit additionally confirmed the two guards coexist and are classification-consistent in the same test run.

**Score: 1/1** — flow complete, no gaps.

## Conclusion

**Status: PASSED.** All 4 requirements satisfied, both phases verified, cross-phase integration confirmed with zero wiring gaps, and the milestone's stated goal ("make role-check drift structurally hard to reintroduce, instead of relying on periodic manual audits") is achieved and independently re-verified against live code and a live CI run — not just trusted from phase-level self-reports. Two trivial tech-debt items are documented above and accepted as-is (not blocking); no gap-closure phase is needed.

Notably, this milestone is a case where the *phase-level* code-review-fix cycles (not just the milestone audit) caught the two most severe defects in the entire milestone — both critical, both in the security-classification logic itself (Phase 23's inline-gate mislabeling, Phase 24's allowlist evasion) — before either phase's own goal-backward verifier ran. This audit's incremental contribution was confirming those two independently-fixed defects didn't leave the two phases' outputs mutually inconsistent with each other, which is exactly the class of gap no single phase's verification is scoped to catch.

**Next:** `/gsd-complete-milestone v1.5` to archive.
