---
milestone: v1.0
audited: 2026-07-28T22:37:08Z
status: passed
resolved: 2026-07-28T23:45:00Z
scores:
  requirements: 7/7
  phases: 7/7
  integration: 11/11
  flows: 11/11
gaps:
  requirements: []
  integration:
    - id: "AI-COPILOT-GR-01"
      status: "resolved"
      phase: "06-pms-and-ai-expansion (bug pre-dates phase, but guest_request_creation was in D-01 audit scope and was missed)"
      claimed_by_plans: ["06-01-PLAN.md", "06-03-PLAN.md"]
      completed_by_plans: []
      verification_status: "resolved 2026-07-28 — fixed post-audit, see commits cfb58714 (RED) / f29cf192 (GREEN)"
      evidence: "apps/api/routers/ai_copilot.py:571-589 confirm_guest_requests did a bare insert into guest_requests with only tenant_id/title/description/room_id/guest_name/status/created_by. It never computed sla_minutes, never set due_at/category/priority/guest_impact, never auto-created the linked tasks row, and never recorded a guest_request_events audit row — unlike the canonical apps/api/routers/guest_requests.py::create_guest_request (lines 87-166), which does all of this and explicitly notes in a comment that the linked task 'is what the escalation cron watches for SLA breaches.' FIXED: confirm_guest_requests now mirrors create_guest_request's full contract (validation, SLA resolution, linked task, audit event). 4 new tests (tests/test_ai_copilot_guest_request_escalation.py), full suite 500/500 passing, web type-check clean."
  flows:
    - id: "FLOW-AI-GR-ESCALATION"
      description: "AI copilot guest-request fast-path -> guest-recovery domain -> task creation -> escalation cron -> audit trail"
      status: "resolved"
      breaks_at: "apps/api/routers/ai_copilot.py::confirm_guest_requests — RESOLVED, now creates the linked task and SLA fields the escalation cron depends on"
tech_debt:
  - phase: 02-evidence-foundation
    items:
      - "No formal VERIFICATION.md or SUMMARY.md requirements-completed frontmatter exists for REQ-201..REQ-207 (predates that GSD convention). Direct codebase inspection during this audit confirms all 7 are genuinely implemented (see Requirements Coverage below), but the formal traceability artifact is retroactively missing."
  - phase: 03-texas-compliance-and-staff-safety
    items:
      - "Uses an older planning format (03-EXECUTION-PLAN.md, 03-DISCUSSION-LOG.md) predating the PLAN.md/SUMMARY.md/VERIFICATION.md convention. No formal VERIFICATION.md exists. STATE.md documents extensive manual verification (312 API tests, live production deploy verification) but not in the standard artifact form."
  - phase: 06-pms-and-ai-expansion
    items:
      - "guest_requests.py uses inline current_user.role checks (lines 213, 300, 334, 375, 444, 476, 493) rather than the require_role() dependency pattern used elsewhere in the codebase — functionally equivalent, inconsistent idiom, not a security gap (found during integration check, out of Phase 6's own audit scope which focused on ai_copilot.py/integrations.py)."
      - "CLAUDE.md's documented cron mechanism (.github/workflows/cron-jobs.yml + X-Cron-Secret) is stale — that workflow was retired in favor of an in-process APScheduler (apps/api/core/scheduler.py) at some point before this milestone closed. The actual code is correctly wired and self-consistent; only the top-level project doc is out of date."
nyquist:
  compliant_phases: ["02-evidence-foundation", "06-pms-and-ai-expansion"]
  partial_phases: []
  missing_phases: ["03-texas-compliance-and-staff-safety", "04-maintenance-and-housekeeping-programs", "05-guest-recovery-and-management-roi"]
  overall: "partial — VALIDATION.md exists and nyquist_compliant:true for Phases 2 and 6 only; Phases 3-5 predate or don't use this specific artifact, though all have other verification evidence (VERIFICATION.md for 4/5, EXECUTION-PLAN for 3)"
---

# Milestone v1.0 "Hotel Standards Execution Plan" — Audit Report

**Audited:** 2026-07-28
**Status:** ✓ passed (blocker found and resolved same session)
**Scope:** Phases 0 through 6 (7 phases total)

## Summary

This milestone's 7 phases are individually well-verified — Phase 6 alone passed intra-phase verification (20/20 must-haves), conversational UAT (0 issues), and a security audit (21/21 threats closed). But this cross-phase audit — checking phase boundaries an intra-phase audit cannot see — found **one real, previously-undetected, untested integration gap**: the AI Copilot's guest-request creation path silently bypassed the entire SLA/escalation/audit contract that Phase 5's guest-recovery system depends on. A front-desk/GM user creating a guest request via the AI Copilot chat got a request that would never assign a fulfillment task, never SLA-escalate, and left no audit trail — with no error surfaced anywhere. This directly undermined the guest-recovery value proposition Phase 5 shipped, specifically for AI-created requests.

**This gap was fixed the same session** (commits `cfb58714` RED / `f29cf192` GREEN) rather than deferred — consistent with this project's established precedent for real bugs found during audits. `confirm_guest_requests` now mirrors the canonical `create_guest_request` contract in full. 4 new regression tests added; full suite 500/500 passing; web type-check clean. Audit status upgraded from `gaps_found` to `passed` after the fix landed and was independently re-verified against the actual code (not just the fix's own claim).

## Requirements Coverage

REQUIREMENTS.md only formally defines REQ-201 through REQ-207, all assigned to Phase 2 (predates the practice of tracking every phase's requirements this way — Phases 3 through 6 use CONTEXT.md decision IDs (D-01, D-02, ...) as their traceability anchor instead, which is this project's actual, intentional convention for those phases, not a gap).

Phase 2 has no formal VERIFICATION.md or SUMMARY.md `requirements-completed` frontmatter linking these REQ-IDs (predates that GSD convention). This audit verified each directly against the live codebase instead of trusting absent documents:

| REQ-ID | Description | Status | Evidence |
|--------|-------------|--------|----------|
| REQ-201 | Facility/service/brand applicability | ✓ satisfied | `evidence.py:226-239` (GET/PUT `/evidence/applicability`), reused verbatim by `programs.py:56-65,148-158` for facility-gated program templates; UI wired via `apps/web/lib/api/evidence.ts` + `evidence/page.tsx` |
| REQ-202 | Controlled document lifecycle | ✓ satisfied | `evidence.py:243-397` full CRUD + approve/supersede/history/assignments; `services/evidence/contracts.py:53` version/supersession logic |
| REQ-203 | Tenant-isolated evidence records (7 types) | ✓ satisfied | `models/requests.py:514` Literal type matches requirement text verbatim; CRUD at `evidence.py:475-533`; signed-URL file access, never public; 35 tenant-scoping filters in file |
| REQ-204 | Assignment/ack/competency/retraining | ✓ satisfied | `evidence.py:354,414,436-469`; retraining auto-copy at `services/evidence/contracts.py:95`; test-verified (`test_evidence_foundation.py:495`) |
| REQ-205 | GM exceptions dashboard + reminders + export | ✓ satisfied | `evidence.py:548-643` (6-state exception queue, actions), reminders (`:673-707`), export (`:646`) |
| REQ-206 | Audit events + notification delivery + RBAC + tenant filter | ✓ satisfied | `_record_audit_event` called at every mutating endpoint; 13 RBAC gates + 35 tenant filters in `evidence.py` |
| REQ-207 | Phase 2 verification suite coverage | ✓ satisfied | `tests/test_evidence_foundation.py` — 29/29 passing, covers versioning, tenant isolation, RBAC matrix, reminders, export |

**7/7 satisfied.** All genuinely implemented in code, confirmed by direct inspection rather than formal document traceability (which doesn't exist for this phase).

## Cross-Phase Integration

| Flow | Result | Evidence |
|------|--------|----------|
| Property applicability (Ph2) → Program template gating (Ph4) | ✓ wired | `programs.py:56-65,148-158` reuses `evidence.py`'s pattern exactly |
| PM/asset completion (Ph4) → Evidence records + signed URLs (Ph2) | ✓ wired | `assets.py:8` imports `_create_evidence_signed_url` from `routers.evidence`; `:405-428` |
| Safety incidents/drills (Ph3) → Evidence records (Ph2) | ✓ wired | `safety.py:62,226` reads `evidence_records` scoped by `tenant_id` |
| Housekeeping inspections (Ph1/3) → Management ROI (Ph5) | ✓ wired | `management_roi.py:228-236` |
| PM schedules/completions/deferrals (Ph4) → Management ROI (Ph5) | ✓ wired | `management_roi.py:254-262` |
| Safety training (Ph3) → Management ROI (Ph5) | ✓ wired | `management_roi.py:276` |
| In-process cron scheduler → 13 internal endpoints | ✓ wired | `core/scheduler.py` build-time mismatch check confirms 1:1 handler/schedule mapping |
| AI copilot task/work-order fast-path → escalation cron | ✓ wired | `ai_copilot.py::confirm_tasks`/`confirm_work_orders` correctly compute `sla_minutes`/`due_at` |
| Opera pilot flag → all 7 endpoints + cron + webhook | ✓ wired | Cross-confirmed against 06-VERIFICATION.md and spot-checked directly |
| Bilingual i18n gate (Ph4) → Evidence UI (Ph2) | ✓ wired | `en.ts`/`es.ts` both carry full parallel `evidence:` key blocks |
| **AI copilot guest-request fast-path → guest-recovery domain → escalation cron → audit trail** | ✓ **RESOLVED** | See below — fixed same session |

**11/11 flows wired** (after fix). No orphaned code, no broken imports (`python -c "import main"` clean), full test suite green (500/500 — 496 baseline + 4 new), web type-check clean.

## Blocker — Found and Resolved

### AI Copilot guest-request fast-path bypasses Phase 5's guest-recovery contract

**File:** `apps/api/routers/ai_copilot.py:571-589` (`confirm_guest_requests`)

**What's broken:** The canonical `apps/api/routers/guest_requests.py::create_guest_request` (lines 87-166) validates category/priority, resolves `sla_minutes` from tenant SLA policies, sets `due_at`, auto-creates a linked `tasks` row (the row the escalation cron actually watches), back-fills `guest_requests.task_id`, and records a `guest_request_events` audit row. `confirm_guest_requests` does a bare `insert()` with only `tenant_id`/`title`/`description`/`room_id`/`guest_name`/`status`/`created_by` — none of the above. `GuestRequestPreview` and its upstream parser never even capture `category`/`priority`/`guest_impact`, so there's no data to forward even if the handler wanted to.

**Impact:** Any guest request created via the AI Copilot chat interface is invisible to `internal.py::check_escalations` (scans only `tasks`), never SLA-escalates, and leaves no audit trail — with zero error surfaced to the user, front desk, or GM. This silently degrades the guest-recovery promise Phase 5 shipped, specifically for AI-created requests. No test exists that would catch this (existing coverage only checks auth/role requirements on the endpoint).

**Why this is a milestone-level gap, not just a Phase 6 footnote:** `guest_request_creation` was explicitly listed in Phase 6's own audit scope (06-CONTEXT.md D-01), so this should have been caught by 06-01 or 06-03's audits — it wasn't, because both plans focused on RBAC/tenant-isolation/credit-accounting for the endpoints without tracing this specific handler's data completeness against Phase 5's contract. This is precisely the kind of cross-phase gap an intra-phase audit structurally cannot see.

**Fix applied (2026-07-28, commits `cfb58714` RED / `f29cf192` GREEN):** `confirm_guest_requests` now replicates `create_guest_request`'s full contract — `GuestRequestPreview` and the parser schema capture `category`/`priority`/`guest_impact` (with sensible defaults when the LLM omits them), the handler validates accessibility→urgent, resolves `sla_minutes` from tenant SLA policies, sets `due_at`, auto-creates the linked `tasks` row, back-fills `task_id`, and records a `guest_request_events` row (`source="automation"` — `"ai_copilot"` is not a valid value per the table's CHECK constraint, caught during implementation). 4 new tests in `apps/api/tests/test_ai_copilot_guest_request_escalation.py` prove: linked task + SLA fields are created, tenant SLA policy resolution works (not just a hardcoded default), accessibility/non-urgent is rejected with zero partial writes, and sensible defaults apply when the parser omits fields. Full suite: 500/500 passing (was 496). Web type-check clean (no frontend changes required — new preview fields are optional-with-defaults).

## Tech Debt (non-blocking)

- **Phase 2:** No formal VERIFICATION.md/requirements-completed frontmatter for REQ-201..207 (predates convention). Functionally verified via direct code inspection this session — not re-litigating, just noting the process gap for awareness.
- **Phase 3:** Older planning format (EXECUTION-PLAN.md), no formal VERIFICATION.md. Extensive manual verification evidence exists in STATE.md (312 tests, production deploy verification) but not in the standard artifact form.
- **Phase 6:** `guest_requests.py` uses inline role checks instead of `require_role()` — cosmetic inconsistency, not a security gap.
- **Project docs:** CLAUDE.md's cron mechanism description (GitHub Actions) is stale — the project now runs crons in-process via APScheduler. Code is correct; only the doc needs updating.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant | Note |
|-------|---------------|-----------|------|
| 02-evidence-foundation | exists | true | — |
| 03-texas-compliance-and-staff-safety | missing | — | Older planning format, predates this artifact |
| 04-maintenance-and-housekeeping-programs | missing | — | Has VERIFICATION.md + SECURITY.md instead |
| 05-guest-recovery-and-management-roi | missing | — | Has VERIFICATION.md + HUMAN-UAT.md instead |
| 06-pms-and-ai-expansion | exists | true | — |

Not a blocker — this reflects the project's convention evolving over 7 phases, not missing verification per se (Phases 4/5 have equivalent-strength VERIFICATION.md evidence via a different artifact).

## Recommendation

**Resolved.** The guest-request blocker was fixed the same session it was found — a contained change (mirroring the existing `confirm_tasks`/`confirm_work_orders` pattern in the same file against the canonical `create_guest_request` contract), not a redesign. This matches this milestone's own established precedent (Phase 6's D-05: "real bugs found are fixed in-phase, not deferred"; Phase 2's migration-079 fix; Phase 3's bug-449 fix). Milestone v1.0 is now clear to complete.
